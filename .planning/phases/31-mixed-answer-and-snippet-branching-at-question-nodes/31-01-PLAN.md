---
phase: 31-mixed-answer-and-snippet-branching-at-question-nodes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/graph-model.ts
  - src/graph/canvas-parser.ts
  - src/graph/graph-validator.ts
  - src/__tests__/canvas-parser.test.ts
  - src/__tests__/graph-validator.test.ts
autonomous: true
requirements:
  - SNIPPET-BRANCH-03
  - SNIPPET-BRANCH-04
must_haves:
  truths:
    - "SnippetNode в graph-model имеет optional поля snippetLabel и radiprotocol_snippetSeparator"
    - "canvas-parser парсит оба поля, нормализуя пустые строки и невалидные enum значения в undefined"
    - "graph-validator не выдаёт ошибку на question с outgoing рёбрами только к snippet-узлам или mixed answer+snippet"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "SnippetNode с новыми полями"
      contains: "snippetLabel?: string"
    - path: "src/graph/canvas-parser.ts"
      provides: "Парсинг snippetLabel + radiprotocol_snippetSeparator"
      contains: "radiprotocol_snippetLabel"
    - path: "src/graph/graph-validator.ts"
      provides: "Reworded dead-end question error message (branch-agnostic)"
      contains: "has no outgoing branches"
  key_links:
    - from: "src/graph/canvas-parser.ts case 'snippet'"
      to: "SnippetNode.snippetLabel / radiprotocol_snippetSeparator"
      via: "object literal append"
      pattern: "radiprotocol_snippetSeparator"
---

<objective>
Расширить модель Snippet-узла двумя новыми полями (`snippetLabel`, `radiprotocol_snippetSeparator`) и ослабить graph-validator, чтобы он молча разрешал question с исходящими рёбрами к snippet-узлам (mixed или snippet-only).

Purpose: Foundation для фазы 31 — без этих полей и без relaxed валидатора остальные waves не смогут прочитать/отредактировать/выполнить mixed-branching граф.
Output: graph-model + canvas-parser + graph-validator + unit-тесты для парсера и валидатора.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-VALIDATION.md
@src/graph/graph-model.ts
@src/graph/canvas-parser.ts
@src/graph/graph-validator.ts

<interfaces>
<!-- Current SnippetNode (graph-model.ts:67-70): -->
```ts
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  subfolderPath?: string;  // D-02, D-03: absence = root
}
```

<!-- Current answer-separator parser pattern (canvas-parser.ts:202-204): -->
```ts
radiprotocol_separator: props['radiprotocol_separator'] === 'space' ? 'space'
  : props['radiprotocol_separator'] === 'newline' ? 'newline'
  : undefined,
```

<!-- Current subfolderPath parse (canvas-parser.ts:262-269, WR-02): -->
```ts
const rawPath = props['radiprotocol_subfolderPath'];
subfolderPath: (typeof rawPath === 'string' && rawPath !== '') ? rawPath : undefined,
```

<!-- Current dead-end question check (graph-validator.ts:60-71): -->
```ts
if (node.kind === 'question') {
  const outgoing = graph.adjacency.get(id);
  if (!outgoing || outgoing.length === 0) {
    errors.push(`Question "${node.questionText || id}" has no answers. Add at least one answer node connected from this question.`);
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SnippetNode model + parser + parser tests</name>
  <files>src/graph/graph-model.ts, src/graph/canvas-parser.ts, src/__tests__/canvas-parser.test.ts</files>
  <read_first>
    - src/graph/graph-model.ts (смотреть lines 33-70 — pattern `radiprotocol_separator` на AnswerNode и SnippetNode shape)
    - src/graph/canvas-parser.ts (смотреть lines 194-205 answer case + lines 261-272 snippet case)
    - src/__tests__/canvas-parser.test.ts (найти существующий test на `radiprotocol_subfolderPath` — копировать его структуру)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md §1 и §2
  </read_first>
  <behavior>
    - Parser test 1: canvas-узел с `radiprotocol_nodeType=snippet` и `radiprotocol_snippetLabel="Adrenal templates"` → node.snippetLabel === "Adrenal templates"
    - Parser test 2: пустая строка `radiprotocol_snippetLabel=""` → node.snippetLabel === undefined
    - Parser test 3: отсутствие `radiprotocol_snippetLabel` → node.snippetLabel === undefined
    - Parser test 4: `radiprotocol_snippetSeparator="space"` → node.radiprotocol_snippetSeparator === "space"
    - Parser test 5: `radiprotocol_snippetSeparator="newline"` → node.radiprotocol_snippetSeparator === "newline"
    - Parser test 6: `radiprotocol_snippetSeparator="garbage"` → node.radiprotocol_snippetSeparator === undefined
    - Parser test 7: отсутствие поля separator → node.radiprotocol_snippetSeparator === undefined
    - Regression: existing subfolderPath parse test продолжает проходить — не трогать
  </behavior>
  <action>
    1. В `src/graph/graph-model.ts` в интерфейсе `SnippetNode` (lines 67-70) **append** два optional поля ПОСЛЕ существующего `subfolderPath?: string;`:
       ```ts
       /** Phase 31 D-01: optional label shown on branch-list button when snippet node is reached as a variant. Fallback "📁 Snippet". */
       snippetLabel?: string;
       /** Phase 31 D-04: per-node separator override; applies to both branch-entered and auto-advanced snippet pickers. */
       radiprotocol_snippetSeparator?: 'newline' | 'space';
       ```
       Не трогать другие интерфейсы в файле.

    2. В `src/graph/canvas-parser.ts` внутри `case 'snippet'` (lines 261-272) — внутри object literal append новые поля ПОСЛЕ `subfolderPath`:
       ```ts
       const rawLabel = props['radiprotocol_snippetLabel'];
       const rawSep = props['radiprotocol_snippetSeparator'];
       const node: SnippetNode = {
         ...base,
         kind: 'snippet',
         subfolderPath: (typeof rawPath === 'string' && rawPath !== '') ? rawPath : undefined,
         // Phase 31 D-01: empty string normalised to undefined (mirror WR-02 pattern)
         snippetLabel: (typeof rawLabel === 'string' && rawLabel !== '') ? rawLabel : undefined,
         // Phase 31 D-04: strict enum normalisation (mirror radiprotocol_separator pattern)
         radiprotocol_snippetSeparator: rawSep === 'space' ? 'space'
           : rawSep === 'newline' ? 'newline'
           : undefined,
       };
       ```
       НЕ переписывать окружающие case-ы; НЕ трогать парсинг subfolderPath beyond the append. Сохранить существующие комментарии WR-02.

    3. В `src/__tests__/canvas-parser.test.ts` найти существующий тест на `radiprotocol_subfolderPath` и рядом добавить новый `describe('snippet node extra fields (Phase 31)')` с семью тестами из `<behavior>`. Использовать существующий helper для создания canvas JSON (тот же, что используется для subfolderPath test).
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/canvas-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "snippetLabel?: string" src/graph/graph-model.ts` → 1 match
    - `grep -n "radiprotocol_snippetSeparator?: 'newline' | 'space'" src/graph/graph-model.ts` → 1 match
    - `grep -n "radiprotocol_snippetLabel" src/graph/canvas-parser.ts` → ≥1 match
    - `grep -n "radiprotocol_snippetSeparator" src/graph/canvas-parser.ts` → ≥1 match
    - `grep -c "subfolderPath" src/graph/canvas-parser.ts` ≥ 1 (regression: subfolderPath parsing не удалён)
    - vitest: `canvas-parser.test.ts` зелёный, включая минимум 7 новых тестов фазы 31
  </acceptance_criteria>
  <done>Модель snippet-узла имеет два новых optional поля; canvas-parser парсит их с нормализацией; unit-тесты покрывают happy path, empty-string/undefined fallback и invalid enum fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relax graph-validator dead-end-question rule + tests</name>
  <files>src/graph/graph-validator.ts, src/__tests__/graph-validator.test.ts</files>
  <read_first>
    - src/graph/graph-validator.ts (lines 60-71 — Check 5)
    - src/__tests__/graph-validator.test.ts (найти test `'detects dead-end question...'` — копировать структуру fixtures)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md §3
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md D-07
  </read_first>
  <behavior>
    - Test 1 (reword): dead-end question (0 outgoing) → errors.length === 1; message.includes('has no outgoing branches')
    - Test 2 (new, D-07): question с outgoing рёбрами только к snippet-узлам → validate возвращает `[]` (no errors, no warnings)
    - Test 3 (new, D-06): question с mixed outgoing (answer + snippet) → validate возвращает `[]`
    - Test 4 (regression): question с outgoing только к answer — остаётся валидным (уже покрыто существующими тестами, не переписывать)
  </behavior>
  <action>
    1. В `src/graph/graph-validator.ts` (lines 60-71) изменить ТОЛЬКО текст сообщения — с `"has no answers. Add at least one answer node connected from this question."` на `"has no outgoing branches. Add at least one answer or snippet node connected from this question."`. Правило `!outgoing || outgoing.length === 0` остаётся. Никаких новых checks, никаких удалений существующих правил в файле.

    2. В `src/__tests__/graph-validator.test.ts`:
       - Обновить ожидаемый substring в существующем dead-end question тесте с `'has no answers'` на `'has no outgoing branches'`.
       - Добавить новый `describe('Phase 31: mixed and snippet-only question branches')` с двумя тестами (Test 2 и Test 3 из `<behavior>`). Построить fixtures вручную по образцу соседних тестов: question-узел + snippet-узел + (для mixed) answer-узел + edges.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/graph-validator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "has no outgoing branches" src/graph/graph-validator.ts` → 1 match
    - `grep -c "has no answers" src/graph/graph-validator.ts` → 0 (старая wording удалена)
    - `grep -n "Phase 31" src/__tests__/graph-validator.test.ts` → ≥1 match
    - vitest: все существующие тесты зелёные + минимум 2 новых теста фазы 31 зелёные
    - Никакие правила кроме Check 5 wording не изменены (diff должен быть узкий)
  </acceptance_criteria>
  <done>graph-validator использует branch-agnostic message; тесты подтверждают что mixed и snippet-only question branches проходят валидацию молча.</done>
</task>

</tasks>

<verification>
- `npm test -- --run src/__tests__/canvas-parser.test.ts src/__tests__/graph-validator.test.ts` всё зелёное
- `npm run build` проходит без TS ошибок (проверяет что новые поля используются консистентно с кодовой базой)
- Проверка CLAUDE.md append-only: `git diff` на graph-model.ts показывает только добавленные строки внутри SnippetNode; canvas-parser.ts — только добавление внутри case 'snippet'; graph-validator.ts — только 1 строка reword
</verification>

<success_criteria>
- SnippetNode имеет `snippetLabel?: string` и `radiprotocol_snippetSeparator?: 'newline' | 'space'`
- canvas-parser round-trips оба поля с нормализацией "" → undefined и invalid enum → undefined
- graph-validator молча проходит mixed + snippet-only question branches
- Unit tests зелёные; zero regressions в существующих parser/validator тестах
</success_criteria>

<output>
Создать `.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-01-SUMMARY.md` (на русском языке) после завершения.
</output>
</content>
</invoke>