---
phase: 31-mixed-answer-and-snippet-branching-at-question-nodes
plan: 04
type: execute
wave: 3
depends_on:
  - "31-01"
  - "31-02"
  - "31-03"
files_modified:
  - src/views/runner-view.ts
  - src/styles/runner-view.css
autonomous: false
requirements:
  - SNIPPET-BRANCH-01
  - SNIPPET-BRANCH-02
  - SNIPPET-BRANCH-05
must_haves:
  truths:
    - "На question-узле с outgoing рёбрами к answer + snippet runner рендерит две секции: answers сверху, snippets снизу"
    - "Snippet-варианты отображаются как кнопки с лейблом `snippetLabel` или fallback `📁 Snippet`"
    - "Клик по snippet-варианту вызывает `runner.chooseSnippetBranch(snippetNode.id)` и открывает Phase 30 picker"
    - "Step-back из picker, открытого через branch выбор, возвращает к branch list (не к предшественнику question)"
    - "CSS фазы 31 добавлен append-only в runner-view.css с комментарием `/* Phase 31: ... */`"
    - "styles.css regenerate через `npm run build`; styles.css не отредактирован вручную"
  artifacts:
    - path: "src/views/runner-view.ts"
      provides: "Partitioned branch rendering в case 'question'"
      contains: "chooseSnippetBranch"
    - path: "src/styles/runner-view.css"
      provides: "Phase 31 append block"
      contains: "rp-snippet-branch-btn"
  key_links:
    - from: "runner-view.ts case 'question' snippet button click"
      to: "this.runner.chooseSnippetBranch(snippetNode.id)"
      via: "registerDomEvent"
      pattern: "chooseSnippetBranch"
---

<objective>
Отрендерить на question-узле partitioned branch list (answers + snippets), подключить клик по snippet-варианту к `chooseSnippetBranch`, добавить CSS фазы 31 и провести manual smoke-тест через UAT checkpoint.

Purpose: Делает фазу 31 видимой для пользователя-радиолога. Все wave 1-2 изменения сходятся здесь в реальный UX.
Output: runner-view.ts partitioned rendering в case 'question'; append-only Phase 31 CSS block; regenerated styles.css; UAT checkpoint подтверждающий все 5 success criteria из ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md
@src/views/runner-view.ts
@src/styles/runner-view.css

<interfaces>
<!-- runner-view.ts case 'question' (lines 332-355) current shape: -->
```ts
case 'question': {
  questionZone.createEl('p', { text: node.questionText, cls: 'rp-question-text' });
  const answerList = questionZone.createDiv({ cls: 'rp-answer-list' });
  const neighborIds = this.graph.adjacency.get(state.currentNodeId) ?? [];
  for (const answerId of neighborIds) {
    const answerNode = this.graph.nodes.get(answerId);
    if (answerNode === undefined || answerNode.kind !== 'answer') continue;
    const btn = answerList.createEl('button', {
      cls: 'rp-answer-btn',
      text: answerNode.displayLabel ?? answerNode.answerText,
    });
    this.registerDomEvent(btn, 'click', () => {
      this.runner.syncManualEdit(this.previewTextarea?.value ?? '');
      this.runner.chooseAnswer(answerNode.id);
      void this.autoSaveSession();
      void this.renderAsync();
    });
  }
  break;
}
```

<!-- runner-view.css analog (lines 103-118): -->
```css
.rp-answer-list { display: flex; flex-direction: column; gap: var(--size-2-1); }
.rp-answer-btn  { width: 100%; text-align: left; min-height: 40px; padding: var(--size-4-1) var(--size-4-2); white-space: normal; word-break: break-word; font-size: var(--font-text-size); line-height: 1.5; }
```

<!-- Phase 30 picker rendering уже существует в runner-view.ts для status === 'awaiting-snippet-pick' — НЕ трогать, оно будет использоваться автоматически после chooseSnippetBranch перевёл runner в это состояние. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Partitioned branch list в case 'question' + Phase 31 CSS block + build</name>
  <files>src/views/runner-view.ts, src/styles/runner-view.css</files>
  <read_first>
    - src/views/runner-view.ts (lines 320-355 case 'question'; также обзорно посмотреть Phase 30 `case 'awaiting-snippet-pick'` rendering чтобы убедиться что оно сработает для branch-entered пути — без изменений)
    - src/styles/runner-view.css (весь файл — для понимания порядка секций и CSS variables; append только внизу)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md §6 и §8
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md D-01, D-02
    - CLAUDE.md (CSS append-only per phase; НЕ редактировать styles.css)
  </read_first>
  <action>
    1. В `src/views/runner-view.ts` **в теле `case 'question'` только** (lines 332-355) заменить текущий single-loop рендер на partitioned рендер. Сохранить `questionZone.createEl('p', { text: node.questionText, cls: 'rp-question-text' })`. После создания `<p>` question text:
       ```ts
       const neighborIds = this.graph.adjacency.get(state.currentNodeId) ?? [];
       const answerNeighbors: AnswerNode[] = [];
       const snippetNeighbors: SnippetNode[] = [];
       for (const nid of neighborIds) {
         const n = this.graph.nodes.get(nid);
         if (n === undefined) continue;
         if (n.kind === 'answer') answerNeighbors.push(n);
         else if (n.kind === 'snippet') snippetNeighbors.push(n);
       }

       if (answerNeighbors.length > 0) {
         const answerList = questionZone.createDiv({ cls: 'rp-answer-list' });
         for (const answerNode of answerNeighbors) {
           const btn = answerList.createEl('button', {
             cls: 'rp-answer-btn',
             text: answerNode.displayLabel ?? answerNode.answerText,
           });
           this.registerDomEvent(btn, 'click', () => {
             this.runner.syncManualEdit(this.previewTextarea?.value ?? '');  // BUG-01
             this.runner.chooseAnswer(answerNode.id);
             void this.autoSaveSession();  // SESSION-01
             void this.renderAsync();
           });
         }
       }

       if (snippetNeighbors.length > 0) {
         // Phase 31 D-02: snippet variants rendered below answers, visually distinct.
         const snippetList = questionZone.createDiv({ cls: 'rp-snippet-branch-list' });
         for (const snippetNode of snippetNeighbors) {
           const label = (snippetNode.snippetLabel && snippetNode.snippetLabel.length > 0)
             ? `\uD83D\uDCC1 ${snippetNode.snippetLabel}`   // 📁
             : '\uD83D\uDCC1 Snippet';                      // D-01 fallback
           const btn = snippetList.createEl('button', {
             cls: 'rp-snippet-branch-btn',
             text: label,
           });
           this.registerDomEvent(btn, 'click', () => {
             this.runner.syncManualEdit(this.previewTextarea?.value ?? '');  // BUG-01
             this.runner.chooseSnippetBranch(snippetNode.id);
             void this.autoSaveSession();  // SESSION-01
             void this.renderAsync();
           });
         }
       }
       break;
       ```
       **Критично (CLAUDE.md):** НЕ трогать никакие другие case в switch (free-text-input, loop-end, awaiting-snippet-pick, awaiting-snippet-fill, complete, error, default). НЕ удалять и не переупорядочивать функции в файле. Импорт `AnswerNode`, `SnippetNode` типов — добавить если их ещё нет в верхнем `import type` блоке, скопировав способ существующего импорта из graph-model в этом файле.

    2. В `src/styles/runner-view.css` **append в самом конце файла** (ниже всех существующих правил, включая блок Phase 30):
       ```css

       /* Phase 31: mixed answer + snippet branches at question nodes */
       .rp-snippet-branch-list {
         display: flex;
         flex-direction: column;
         gap: var(--size-2-1);
         margin-top: var(--size-4-2);
       }

       .rp-snippet-branch-btn {
         width: 100%;
         text-align: left;
         min-height: 40px;
         padding: var(--size-4-1) var(--size-4-2);
         white-space: normal;
         word-break: break-word;
         font-size: var(--font-text-size);
         line-height: 1.5;
         /* визуально отделено от answer-кнопок — более мягкий accent */
         border-style: dashed;
       }
       ```
       НЕ трогать ни одно существующее правило (answer-list, snippet-folder-row, snippet-item-row, и т.д.).

    3. Запустить `npm run build` чтобы перегенерировать `styles.css`. НЕ коммитить ручные правки styles.css — только сгенерированные.

    4. Прогнать быстрые тесты runner-view-adjacent: `npm test -- --run` должно быть зелёным (unit тесты не трогают DOM рендер но ловят TS ошибки).
  </action>
  <verify>
    <automated>npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "chooseSnippetBranch" src/views/runner-view.ts` → ≥1 match
    - `grep -n "rp-snippet-branch-list" src/views/runner-view.ts` → 1 match
    - `grep -n "rp-snippet-branch-btn" src/views/runner-view.ts` → 1 match
    - `grep -n "rp-snippet-branch-btn" src/styles/runner-view.css` → 1 match
    - `grep -n "/\\* Phase 31:" src/styles/runner-view.css` → 1 match
    - `grep -c "rp-answer-btn" src/styles/runner-view.css` ≥ 1 (regression: Phase 12 CSS сохранён)
    - `grep -c "rp-answer-list" src/views/runner-view.ts` ≥ 1 (regression: answer rendering сохранён)
    - `grep -c "chooseAnswer" src/views/runner-view.ts` ≥ 1 (regression)
    - `grep -n "📁\\|\\\\uD83D\\\\uDCC1" src/views/runner-view.ts` → ≥1 match (emoji label)
    - `npm run build` без ошибок → styles.css обновлён
    - `npm test -- --run` вся suite зелёная
    - `git status` не показывает ручные правки styles.css сверх результата build
  </acceptance_criteria>
  <done>runner-view рендерит answers и snippets как две секции на question-узле; клик по snippet-варианту вызывает chooseSnippetBranch и открывает существующий Phase 30 picker; CSS добавлен append-only и styles.css перегенерирован.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — end-to-end mixed branching flow</name>
  <what-built>
    Phase 31 полностью собрана. На question-узле с исходящими рёбрами одновременно к answer- и snippet-узлам runner должен показать обе секции, позволить выбрать любой вариант, корректно отработать step-back из branch-entered picker, применить per-node separator для snippet'а, и сохранить/восстановить сессию если прервать посередине.
  </what-built>
  <how-to-verify>
    Подготовь тестовый canvas (вручную в Obsidian) со следующей структурой:
    - start → question "Тип исследования"
    - question → answer "КТ" → text-block "КТ протокол"
    - question → answer "МРТ" → text-block "МРТ протокол"
    - question → snippet S1 (subfolderPath='CT', snippetLabel='CT templates', snippetSeparator=space)
    - question → snippet S2 (subfolderPath='MRI', snippetLabel пустой, snippetSeparator не задан)
    - S1 → end, S2 → end
    Убедись что есть минимум 1-2 snippet файла в `.radiprotocol/snippets/CT/` и `.radiprotocol/snippets/MRI/` (хотя бы один с `{{placeholder}}`).

    Выполни все 5 success criteria из ROADMAP Phase 31:

    1. **SNIPPET-BRANCH-01 — mixed branch list:** Open Protocol Runner, запусти canvas. На вопросе должны отобразиться: сначала кнопки "КТ" и "МРТ" (rp-answer-btn), ниже — отдельная секция с "📁 CT templates" и "📁 Snippet" (fallback для S2 без label). Оба snippet-варианта визуально отличимы (dashed border).

    2. **SNIPPET-BRANCH-02 — snippet insertion contract:** Нажми "📁 CT templates". Должен открыться Phase 30 drill-down picker с корнем `.radiprotocol/snippets/CT/`. Выбери snippet с placeholder'ами → модалка откроется → заполни → submit → в textarea добавлен ТОЛЬКО отрендеренный текст snippet'а (без текста question, без label). Отделён от предыдущего контента **пробелом** (а не newline — подтверждает D-04 per-node override).

    3. **SNIPPET-BRANCH-03 — editor fields round-trip:** Закрой runner. Открой S1 в Node Editor. Убедись что поле "Branch label" показывает "CT templates" и "Separator override" показывает "Space". Открой S2 — "Branch label" пустой, "Separator override" — "use global default". Измени S2 label на "MRI templates" → save → reopen canvas → runner на том же question показывает "📁 MRI templates" вместо "📁 Snippet".

    4. **SNIPPET-BRANCH-04 — validator silent acceptance:** Проверь что canvas со snippet-only question (удали answer edges, оставь только 2 snippet edges) запускается без validator errors в Notice. Также canvas с mixed (answers + snippets) не выдаёт warning.

    5. **SNIPPET-BRANCH-05 — step-back from branch-entered picker:** Запусти mixed canvas. Нажми "📁 CT templates" → picker открылся → нажми Step Back. Runner должен вернуться **на тот же question с обеими секциями** (не к предыдущему узлу). Теперь нажми "КТ" (answer) → продолжи → Step Back отсюда должен откатить нормально к question.

    **Session resume bonus:** Сделай clean reload Obsidian между шагом 5 click "📁 CT templates" и click Step Back — runner после reload должен всё ещё показывать picker S1 (D-09), и step back от туда всё равно возвращает к branch list.

    Если любой шаг падает — опиши что пошло не так с конкретными reproduction steps.
  </how-to-verify>
  <resume-signal>Напиши "approved" если все 5 SNIPPET-BRANCH-0X критериев работают. Иначе опиши найденные проблемы.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` зелёный, `npm test -- --run` зелёный (с плaнами 01 + 03 вместе)
- UAT checkpoint approved
- Визуальная проверка: dashed border на snippet-кнопках отличает их от answer-кнопок
- `git diff src/views/runner-view.ts` — изменения ограничены `case 'question'` body + import add (если нужен)
- `git diff src/styles/runner-view.css` — только append внизу файла под `/* Phase 31: ... */`
- styles.css regenerated by build — не редактирован вручную
</verification>

<success_criteria>
- Все 5 ROADMAP success criteria Phase 31 подтверждены UAT
- Zero регрессий в answer branching, Phase 30 picker, step-back из других мест
- CLAUDE.md append-only invariant сохранён (никаких удалений соседнего кода)
</success_criteria>

<output>
Создать `.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-04-SUMMARY.md` (на русском) после завершения. Включить UAT findings.
</output>
</content>
</invoke>