---
phase: 50.1-loop-exit-plus-prefix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/fixtures/unified-loop-valid.canvas
  - src/__tests__/fixtures/unified-loop-nested.canvas
  - src/__tests__/fixtures/unified-loop-long-body.canvas
  - src/__tests__/fixtures/unified-loop-missing-exit.canvas
  - src/__tests__/fixtures/unified-loop-duplicate-exit.canvas
  - src/__tests__/fixtures/unified-loop-no-body.canvas
  - src/__tests__/fixtures/unified-loop-stray-body-label.canvas
  - src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas
  - src/__tests__/fixtures/unified-loop-labeled-body.canvas
  - src/__tests__/fixtures/unified-loop-empty-plus.canvas
autonomous: true
requirements:
  - EDGE-03
tags:
  - loop-exit
  - fixtures

must_haves:
  truths:
    - "unified-loop-valid.canvas uses +выход on the exit edge (unlabeled body preserved) — passes LOOP-04"
    - "unified-loop-nested.canvas uses +выход on both outer + inner exit edges — passes LOOP-04 (two loop nodes, each with 1 +-exit)"
    - "unified-loop-long-body.canvas uses +выход on the exit edge — passes LOOP-04"
    - "unified-loop-missing-exit.canvas retains its 0 outgoing-labeled state — exercises D-04"
    - "unified-loop-duplicate-exit.canvas has at least 2 +-prefixed edges — exercises D-06"
    - "unified-loop-no-body.canvas uses +выход on the sole outgoing edge (0 non-+) — exercises D-07"
    - "unified-loop-stray-body-label.canvas migrated to exit +выход + body проверка — validates under 50.1 (labeled body is allowed)"
    - "NEW unified-loop-legacy-vyhod.canvas — 0 +-edges + 1 legacy выход-labeled edge — exercises D-05"
    - "NEW unified-loop-labeled-body.canvas — 1 +выход exit + 1 labeled body edge into Answer with displayLabel (Phase 49 <-> 50 conflict regression) — passes LOOP-04"
    - "NEW unified-loop-empty-plus.canvas — edge with label equal to a single + — exercises D-08"
  artifacts:
    - path: src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas
      provides: "D-05 legacy-hint test target"
    - path: src/__tests__/fixtures/unified-loop-labeled-body.canvas
      provides: "Phase 49 vs Phase 50 conflict regression target"
    - path: src/__tests__/fixtures/unified-loop-empty-plus.canvas
      provides: "D-08 per-edge empty-caption test target"
  key_links:
    - from: src/__tests__/graph-validator.test.ts
      to: src/__tests__/fixtures/unified-loop-*.canvas
      via: "parseFixture()"
      pattern: "parseFixture\\('unified-loop-"
    - from: src/__tests__/runner/protocol-runner-loop-picker.test.ts
      to: src/__tests__/fixtures/unified-loop-labeled-body.canvas
      via: "parseFixture()"
      pattern: "parseFixture\\('unified-loop-labeled-body"
---

<objective>
In-place migration of the seven existing unified-loop-*.canvas fixtures (Phase 49 "выход" labels to Phase 50.1 "+выход" labels) plus three new fixtures required by Plan 02 and Plan 03 tests (D-05, D-08, and the Phase 49 vs Phase 50 conflict regression). This plan is pure JSON edits — no TypeScript changes. No file beyond the fixtures directory is modified.

Purpose: Plans 02 and 03 both depend on these fixtures (Plan 02 validator tests; Plan 03 Runner conflict-regression test). Running Plan 04 in Wave 1 alongside Plan 01 unblocks both downstream plans in Wave 2.

Output:
- Seven fixture files in-place-migrated.
- Three new fixture files added: unified-loop-legacy-vyhod.canvas, unified-loop-labeled-body.canvas, unified-loop-empty-plus.canvas.
- Two commits: one chore for the in-place migration, one feat for the three new fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md

Canvas-format edge shape expected by the parser (example — DO NOT paste literally):
  id: string, fromNode: string, toNode: string, label: optional string

Canvas-format node shape (example — DO NOT paste literally):
  id: string, type: "text", text: string, x/y/width/height: numbers,
  radiprotocol_nodeType: "start" | "loop" | "question" | "answer" | "text-block" | "snippet",
  plus per-kind extras (radiprotocol_headerText on loop; radiprotocol_questionText on question;
  radiprotocol_answerText + optional radiprotocol_displayLabel on answer;
  radiprotocol_content on text-block).

The executor should inspect an existing fixture (for example src/__tests__/fixtures/unified-loop-valid.canvas) to see the exact JSON style in use (indentation, column alignment, field ordering) and match it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the seven existing unified-loop-*.canvas fixtures in-place to "+"-prefix labels</name>
  <files>src/__tests__/fixtures/unified-loop-valid.canvas, src/__tests__/fixtures/unified-loop-nested.canvas, src/__tests__/fixtures/unified-loop-long-body.canvas, src/__tests__/fixtures/unified-loop-missing-exit.canvas, src/__tests__/fixtures/unified-loop-duplicate-exit.canvas, src/__tests__/fixtures/unified-loop-no-body.canvas, src/__tests__/fixtures/unified-loop-stray-body-label.canvas</files>
  <read_first>
    - .planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md section D-13 (per-fixture migration instructions)
    - src/__tests__/fixtures/unified-loop-valid.canvas (current: e3 label "выход" — change to "+выход")
    - src/__tests__/fixtures/unified-loop-nested.canvas (current: e3 and e5 both "выход" — change both to "+выход")
    - src/__tests__/fixtures/unified-loop-long-body.canvas (current: e3 "выход" — change to "+выход")
    - src/__tests__/fixtures/unified-loop-missing-exit.canvas (current: no labeled edges — NO CHANGE needed; fixture keeps its D-04 role)
    - src/__tests__/fixtures/unified-loop-duplicate-exit.canvas (current: e2 "проверка", e3 "выход", e4 "выход" — REWRITE per instructions below so the fixture exercises D-06 = at-least-2 +-edges)
    - src/__tests__/fixtures/unified-loop-no-body.canvas (current: only e2 "выход", no unlabeled outgoing — change to "+выход"; keeps its D-07 role)
    - src/__tests__/fixtures/unified-loop-stray-body-label.canvas (current: e2 "проверка" + e3 "выход" — change only e3 to "+выход"; e2 stays as a labeled body edge, fixture is VALID under 50.1)
  </read_first>
  <action>
Migrate each fixture as follows. In every case the `nodes` array stays byte-identical; only edge labels change. Use the Edit tool (single replace per edit) on each fixture.

Fixture 1 — unified-loop-valid.canvas
  Edit e3 only. Change the label value from выход to +выход on the single edge with id "e3". All other edges untouched.

Fixture 2 — unified-loop-nested.canvas
  Two edges need migration. Edit e3 (outer exit) label выход to +выход. Edit e5 (inner exit) label выход to +выход. All other edges untouched.

Fixture 3 — unified-loop-long-body.canvas
  Edit e3 only: label выход to +выход.

Fixture 4 — unified-loop-missing-exit.canvas
  NO CHANGE. The fixture has zero labeled outgoing edges already. Under 50.1 that triggers D-04 (clean zero-exit). Leave the file byte-identical.

Fixture 5 — unified-loop-duplicate-exit.canvas — REWRITE the entire edges array (nodes array byte-identical)
  Goal: 2 +-edges (e3, e4) + 1 unlabeled body (e2). The new edges block:
    e1: n-start to n-loop, no label
    e2: n-loop to n-q1, NO LABEL (was "проверка" — strip it)
    e3: n-loop to n-end1, label "+выход"
    e4: n-loop to n-end2, label "+готово"   (different caption so D-06's edgeIds list is unambiguous in test output)
    e5: n-q1 to n-a1, no label
    e6: n-a1 to n-loop, no label
  Replace the entire "edges" array in the file with those six entries, preserving the existing column-alignment style of the file. Do not change any node. Do not add new nodes.

Fixture 6 — unified-loop-no-body.canvas
  Edit e2 only: label выход to +выход. After migration the fixture has 1 +-edge and 0 non-+ outgoing from the loop, so D-07 (no body) fires.

Fixture 7 — unified-loop-stray-body-label.canvas
  Edit e3 only: label выход to +выход. Do NOT touch e2 — its "проверка" label stays (a legitimate labeled body edge under 50.1). Post-migration the fixture has 1 +-edge (e3) + 1 labeled-but-non-+ body (e2) — validates cleanly under 50.1.

Commit strategy: single commit covering all seven fixture edits.

Commit message: chore(50.1-04): migrate unified-loop-*.canvas fixtures to "+"-prefix labels (D-13)
  </action>
  <verify>
    <automated>grep -l '"+выход"' src/__tests__/fixtures/unified-loop-valid.canvas src/__tests__/fixtures/unified-loop-nested.canvas src/__tests__/fixtures/unified-loop-long-body.canvas src/__tests__/fixtures/unified-loop-no-body.canvas src/__tests__/fixtures/unified-loop-stray-body-label.canvas src/__tests__/fixtures/unified-loop-duplicate-exit.canvas | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-valid.canvas  equals 0
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-valid.canvas  equals 1
    - grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-nested.canvas  equals 0
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-nested.canvas  equals 2
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-long-body.canvas  equals 1
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-no-body.canvas  equals 1
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-stray-body-label.canvas  equals 1
    - grep -c '"label": "проверка"' src/__tests__/fixtures/unified-loop-stray-body-label.canvas  equals 1 (body label preserved)
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-duplicate-exit.canvas  equals 1
    - grep -c '"label": "+готово"' src/__tests__/fixtures/unified-loop-duplicate-exit.canvas  equals 1
    - grep -c '"label": "проверка"' src/__tests__/fixtures/unified-loop-duplicate-exit.canvas  equals 0 (stripped from body edge e2)
    - unified-loop-missing-exit.canvas has zero changes — diff against git HEAD is empty for that file
    - Every fixture parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync(process.argv[1], 'utf8'))"` run on each of the seven files exits 0
    - Commit exists with message matching chore\(50\.1-04\):.*\+.*prefix
  </acceptance_criteria>
  <done>All seven fixtures migrated (or confirmed unchanged); JSON parses; single commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Create three new fixtures for D-05, D-08, and the Phase 49 vs Phase 50 conflict regression</name>
  <files>src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas, src/__tests__/fixtures/unified-loop-labeled-body.canvas, src/__tests__/fixtures/unified-loop-empty-plus.canvas</files>
  <read_first>
    - .planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md section D-14 (three new fixtures specification)
    - src/__tests__/fixtures/unified-loop-valid.canvas (use as style reference — column alignment, field order, indentation)
    - src/__tests__/fixtures/unified-loop-no-body.canvas (use as minimal-shape reference when the fixture only needs 3 nodes)
  </read_first>
  <action>
Create three new files under src/__tests__/fixtures/. Match the JSON indentation + column-alignment style of the existing unified-loop-*.canvas files — opening brace on line 1, one node per line with padded field values, edges array likewise.

File A — src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas
  Purpose: 0 +-edges + 1 labeled non-+ outgoing edge (the legacy "выход" literal) so GraphValidator LOOP-04 emits D-05 listing the candidate edge id.
  Shape:
    Nodes:
      n-start  type=start
      n-loop   type=loop,  headerText="Lesion loop"
      n-q1     type=question, questionText="Size?"
      n-a1     type=answer,   answerText="1 cm"
      n-end    type=text-block, content="Done"
    Edges:
      e1 n-start -> n-loop (unlabeled)
      e2 n-loop  -> n-q1   (unlabeled body)
      e3 n-loop  -> n-end  label "выход"      (legacy literal, NO +)
      e4 n-q1    -> n-a1   (unlabeled)
      e5 n-a1    -> n-loop (back-edge)
  Expected validator behavior: D-05 fires with "(e3)" in the edgeIds list.

File B — src/__tests__/fixtures/unified-loop-labeled-body.canvas
  Purpose: Phase 49 vs Phase 50 conflict regression. 1 +-exit + 1 labeled body edge (Phase 50 reconciler would legitimately label this body edge because it points to an Answer with displayLabel).
  Shape:
    Nodes:
      n-start  type=start
      n-loop   type=loop,  headerText="Lesion loop"
      n-q1     type=question, questionText="Size?"
      n-ans    type=answer,   answerText="1 cm",  displayLabel="Small"
      n-end    type=text-block, content="Done"
    Edges:
      e1 n-start -> n-loop (unlabeled)
      e2 n-loop  -> n-q1   label "Small"      (labeled body — Phase 50 sync of n-ans.displayLabel onto the incoming edge from n-loop's perspective; see .planning/notes/answer-label-edge-sync.md)
      e3 n-loop  -> n-end  label "+выход"      (the lone +-exit)
      e4 n-q1    -> n-ans  label "Small"      (classic Question->Answer edge reflecting displayLabel)
      e5 n-ans   -> n-loop (back-edge)
  Expected validator behavior: NO LOOP-04 error — exactly 1 +-edge (e3), body edges (e2) allowed to carry labels.
  Expected Runner behavior (per Plan 03 test): loop picker renders 2 buttons — one exit ("выход" after stripExitPrefix) and one body (target node label "Small" via nodeLabel).

File C — src/__tests__/fixtures/unified-loop-empty-plus.canvas
  Purpose: D-08 per-edge empty-caption regression. One edge with label equal to a single + character.
  Shape:
    Nodes:
      n-start  type=start
      n-loop   type=loop,  headerText="Lesion loop"
      n-q1     type=question, questionText="Size?"
      n-a1     type=answer,   answerText="1 cm"
      n-end    type=text-block, content="Done"
    Edges:
      e1 n-start -> n-loop (unlabeled)
      e2 n-loop  -> n-q1   (unlabeled body)
      e3 n-loop  -> n-end  label "+"           (the empty-caption +-edge)
      e4 n-q1    -> n-a1   (unlabeled)
      e5 n-a1    -> n-loop (back-edge)
  Expected validator behavior: D-08 fires once for edge e3 ('"+"-ребро e3 не имеет подписи...'). Since the fixture also has a valid body (e2) and exactly one +-edge (so D-06 does not fire), D-08 is the sole LOOP-04 error.

All three files must parse as JSON. Every node has the required x/y/width/height coordinates (use the same coordinate style as unified-loop-valid.canvas — arbitrary valid values, for example x=0,y=0 or x=260,y=120).

Commit message: feat(50.1-04): add three new fixtures (legacy-vyhod D-05, labeled-body conflict, empty-plus D-08)
  </action>
  <verify>
    <automated>node -e "for (const f of ['unified-loop-legacy-vyhod.canvas','unified-loop-labeled-body.canvas','unified-loop-empty-plus.canvas']) { JSON.parse(require('fs').readFileSync('src/__tests__/fixtures/'+f,'utf8')); console.log(f, 'OK'); }"</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas exists and parses as JSON
    - src/__tests__/fixtures/unified-loop-labeled-body.canvas exists and parses as JSON
    - src/__tests__/fixtures/unified-loop-empty-plus.canvas exists and parses as JSON
    - grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas  equals 1 (bare legacy literal, no +)
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-legacy-vyhod.canvas  equals 0
    - grep -c '"label": "+выход"' src/__tests__/fixtures/unified-loop-labeled-body.canvas  equals 1
    - grep -c 'radiprotocol_displayLabel' src/__tests__/fixtures/unified-loop-labeled-body.canvas  equals 1
    - grep -c '"label": "Small"' src/__tests__/fixtures/unified-loop-labeled-body.canvas  equals 2 (Phase 50 sync would label both Question-to-Answer and Loop-to-Question edges when they target the same Answer; Plan 02 assertion expects validation PASS)
    - grep -c '"label": "+"' src/__tests__/fixtures/unified-loop-empty-plus.canvas  equals 1
    - Each new fixture has exactly one node with radiprotocol_nodeType equal to "loop"
    - Commit exists with message matching feat\(50\.1-04\):.*new fixtures
  </acceptance_criteria>
  <done>Three new fixtures created with the specified shapes; JSON parses; commit landed.</done>
</task>

</tasks>

<verification>
- All seven existing unified-loop-*.canvas fixtures migrated in-place (six touched, one intentionally unchanged — unified-loop-missing-exit.canvas).
- Three new fixtures present: unified-loop-legacy-vyhod.canvas, unified-loop-labeled-body.canvas, unified-loop-empty-plus.canvas.
- Every fixture parses as valid JSON.
- No fixture references the bare "выход" literal on the exit edge — all exits are "+выход" — EXCEPT unified-loop-legacy-vyhod.canvas which intentionally uses the legacy literal to exercise D-05.
- Two commits landed in this plan: chore(50.1-04):...migrate... and feat(50.1-04):...new fixtures...
</verification>

<success_criteria>
- Running `node -e "for (const f of require('fs').readdirSync('src/__tests__/fixtures').filter(n => n.startsWith('unified-loop-'))) JSON.parse(require('fs').readFileSync('src/__tests__/fixtures/'+f,'utf8'))"` exits 0 (all unified-loop-*.canvas files parse as JSON).
- Downstream Plans 02 and 03 can consume the fixtures without further fixture edits.
</success_criteria>

<output>
After completion, create .planning/phases/50.1-loop-exit-plus-prefix/50.1-04-SUMMARY.md summarizing:
- Per-fixture migration outcome (which ones were touched, which stayed byte-identical)
- Edge-id layout of the three new fixtures (so Plan 02 + Plan 03 assertions can reference specific ids)
- Confirmation that unified-loop-stray-body-label.canvas is VALID under 50.1 (so Plan 02 rewrites its test to assert validity)
- Two commit SHAs
</output>
