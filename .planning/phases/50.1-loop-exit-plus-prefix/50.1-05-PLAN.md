---
phase: 50.1-loop-exit-plus-prefix
plan: 05
type: execute
wave: 3
depends_on:
  - "50.1-01"
  - "50.1-02"
  - "50.1-03"
  - "50.1-04"
files_modified:
  - .planning/notes/loop-node-exit-edge-convention.md
autonomous: false
requirements:
  - EDGE-03
tags:
  - loop-exit
  - gate
  - uat
  - docs

must_haves:
  truths:
    - "npm run build exits 0 and produces a valid main.js"
    - "npm test (full vitest suite) exits 0 — all prior greens preserved + all new 50.1 tests pass"
    - "Zero runtime literal-выход audit: grep for literal 'выход' in src/graph/, src/runner/, src/views/ returns only the Phase 43 MIGRATE-01 block in graph-validator.ts (line 48-50)"
    - "Alias-removal guard passes: grep for 'export const isExitEdge = isLabeledEdge' in src/ returns 0"
    - "Phase 50 reconciler test file stays green untouched (no imports of 50.1 modules into reconciler/service/canvas-live-editor)"
    - "Click-handler ordering invariant preserved in runner-view.ts (five calls in locked order)"
    - "CSS untouched across the phase — zero edits in src/styles/ and zero changes to esbuild.config.mjs CSS list"
    - "Canonical note loop-node-exit-edge-convention.md rewritten to describe the +-prefix convention with an explicit 'Supersedes' header"
    - "Human UAT in TEST-BASE signs off on at least three scenarios: (a) +-prefix exit button caption is stripped; (b) D-05 legacy-hint error panel for bare 'выход' canvas; (c) labeled-body fixture loads + runs without validation error"
  artifacts:
    - path: .planning/notes/loop-node-exit-edge-convention.md
      provides: "Phase 50.1 +-prefix convention; supersedes v1.7 literal-«выход» + Phase 49 labeled-edge"
      contains: "Supersedes"
  key_links:
    - from: .planning/notes/loop-node-exit-edge-convention.md
      to: .planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md
      via: "supersede header references Phase 50.1"
      pattern: "Phase 50\\.1"
---

<objective>
End-of-phase integration gate for Phase 50.1. Runs the build + full test suite, audits regression-protection invariants (literal-«выход» runtime cleanup, alias-removal, Phase 50 reconciler untouched, click-handler invariant, CSS zero-touch), rewrites the canonical design note to describe the +-prefix convention with an explicit supersede header, and pauses for a human UAT checkpoint in TEST-BASE covering the three observable behavioral changes Phase 50.1 introduces.

Purpose: Ensures Plans 01-04 compose into a shipping-ready phase with no silent regressions. Captures developer sign-off before the orchestrator flips the ROADMAP row.

Output:
- Automated gate report: build + test + audits (returned in the task transcript, no source change).
- Rewritten `.planning/notes/loop-node-exit-edge-convention.md` describing the +-prefix convention.
- Human UAT decision (approved or revisions requested).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-01-SUMMARY.md
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-02-SUMMARY.md
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-03-SUMMARY.md
@.planning/phases/50.1-loop-exit-plus-prefix/50.1-04-SUMMARY.md
@CLAUDE.md
@.planning/notes/loop-node-exit-edge-convention.md
@.planning/notes/answer-label-edge-sync.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite canonical design note to describe the +-prefix convention</name>
  <files>.planning/notes/loop-node-exit-edge-convention.md</files>
  <read_first>
    - .planning/notes/loop-node-exit-edge-convention.md (current: Phase 49 convention text — 30 lines, will be rewritten wholesale)
    - .planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md (authoritative decisions D-04..D-10 + the canonical_refs block mandating this rewrite)
    - .planning/notes/answer-label-edge-sync.md (Phase 50 convention — cite as the reason for the +-prefix convention)
  </read_first>
  <action>
Rewrite `.planning/notes/loop-node-exit-edge-convention.md` to describe the Phase 50.1 +-prefix convention. Keep the same top-level structure (Rule, Validation, Why, How to apply) so readers familiar with the Phase 49 note find their bearings.

The new file content (replace the file wholesale):

```
---
title: "Loop-node exit edge convention (+-prefix)"
date: 2026-04-19
context: design
supersedes:
  - "v1.7 literal-«выход» convention"
  - "Phase 49 labeled-edge convention (any non-empty label == exit)"
---

## Supersedes

This note supersedes the Phase 49 convention (the sole labeled outgoing edge of a loop
node is the exit) and the earlier v1.7 convention (the exit edge is whatever edge carries
the literal label «выход»). See Phase 50.1 CONTEXT for the rationale — Phase 49's «one
labeled edge == exit» rule conflicted with Phase 50's automatic sync of an Answer node's
`displayLabel` onto every incoming edge, because a loop body branch pointing to an
Answer with a displayLabel now legitimately carries a label.

## Rule

For a loop-node, outgoing edges are interpreted by their label prefix:

- The exit edge is the outgoing edge whose label, after trim, starts with `+`.
  The caption shown on the Runner exit button is the text after the `+` (with any
  following whitespace stripped).
- All other outgoing edges are body branches. They may be unlabeled or carry any label
  that does not start with `+` — including labels placed there by Phase 50's
  Answer.displayLabel sync.

Exactly one +-prefixed outgoing edge is allowed per loop node.

## Validation

GraphValidator LOOP-04 emits five distinct Russian error texts (see
`.planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md` D-04..D-08 for the
verbatim strings):

- D-04: zero +-edges AND zero other labeled edges — clean zero-exit; author is told to
  mark exactly one outgoing edge with a `+` prefix.
- D-05: zero +-edges AND at least one labeled non-+ edge — legacy-hint; the error lists
  the candidate edge ids so the author can pick which to prefix.
- D-06: two or more +-edges — error lists offending edge ids; author removes `+` from
  all but one.
- D-07: zero non-+ outgoing edges — no body; author adds at least one body edge.
- D-08: a +-edge whose label has empty caption after stripping (for example the bare
  `+` or `+ `) — emitted once per offending edge.

Error-check order inside LOOP-04 is D-04/D-05 then D-06 then D-08 then D-07. Multiple
errors per loop node accumulate and are all shown in the Runner Error panel.

## Why

Phase 49 used «the sole labeled outgoing edge is the exit» as the exit discriminator.
Phase 50 then introduced automatic two-way sync between an Answer node's `displayLabel`
and the label on every incoming edge. On a multi-incoming topology where the same
Answer is reached both from a Question and from a loop body branch, Phase 50 would
legitimately place a label on the loop body edge — and Phase 49 would then misclassify
that body edge as a second exit, breaking validation. The +-prefix convention resolves
the conflict: the exit discriminator is no longer «does this edge have any label» but
«does the label start with `+`». Body edges may freely carry labels (including ones
written by Phase 50) without ambiguity.

Keeping the prefix in the source label (rather than a separate property) preserves the
property that the canvas `.canvas` JSON is the single source of truth: no migration
step, no `isExit` boolean, no separate export of «which edge is the exit». The author
sees «+выход» on the canvas edge and knows exactly what it means.

## How to apply

- Node Editor / canvas validation: LOOP-04 runs on save and before starting a Runner
  session. The five error texts above direct the author to the specific fix.
- Runner: for a loop-node, the picker renders one exit button per +-prefixed outgoing
  edge (uniqueness enforced by validator, so exactly one) and one body button per
  non-+ outgoing edge (labeled or unlabeled — both are bodies under this convention).
  Exit button caption uses `stripExitPrefix(label)`; body button caption uses
  `nodeLabel(targetNode)` per the shared `src/graph/node-label.ts` module.
- Legacy canvases using a bare `label: "выход"` (no `+`): no auto-migration. Validator
  emits D-05 directing the author to add `+` manually.

## Implementation anchors

- `src/graph/node-label.ts` — `isExitEdge` (D-10) and `stripExitPrefix` (D-09).
- `src/graph/graph-validator.ts` — LOOP-04 block with D-04..D-08 Russian error copy.
- `src/runner/protocol-runner.ts` — `chooseLoopBranch` dispatches via `isExitEdge`.
- `src/views/runner-view.ts` — awaiting-loop-pick arm; exit caption via
  `stripExitPrefix`; body caption via `nodeLabel`.
```

Commit message: docs(50.1-05): rewrite loop-exit convention note for +-prefix (supersedes Phase 49)
  </action>
  <verify>
    <automated>grep -c "Supersedes" .planning/notes/loop-node-exit-edge-convention.md && grep -c '+-prefix' .planning/notes/loop-node-exit-edge-convention.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Supersedes" .planning/notes/loop-node-exit-edge-convention.md  at least 1
    - grep -c "+-prefix" .planning/notes/loop-node-exit-edge-convention.md  at least 2
    - grep -c "Phase 50.1" .planning/notes/loop-node-exit-edge-convention.md  at least 1
    - grep -c "stripExitPrefix" .planning/notes/loop-node-exit-edge-convention.md  at least 1
    - grep -c "D-04\|D-05\|D-06\|D-07\|D-08" .planning/notes/loop-node-exit-edge-convention.md  at least 5
    - grep -c "displayLabel" .planning/notes/loop-node-exit-edge-convention.md  at least 1 (Phase 50 conflict rationale)
    - Commit exists with message matching docs\(50\.1-05\):.*supersedes
  </acceptance_criteria>
  <done>Canonical note rewritten with supersede header, D-04..D-08 summary, Phase 49 vs Phase 50 rationale, implementation anchors; commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Automated gate — build + full test suite + regression-protection audits</name>
  <files>(no source changes; this task produces a gate report in the transcript)</files>
  <read_first>
    - .planning/phases/50.1-loop-exit-plus-prefix/50.1-CONTEXT.md (the regression-protection requirements)
    - CLAUDE.md (CSS zero-touch, Shared Pattern G)
    - .planning/STATE.md sections on Standing Pitfalls (click-handler invariant, CSS build)
  </read_first>
  <action>
Run the following gate steps in order. Record every result in the task transcript. If any step fails, STOP and do not proceed to Task 3 — report the failure so the orchestrator can escalate.

Gate Step 1 — Build:
  Run `npm run build`. Capture exit code. Assert exit code is 0. Confirm `main.js` was regenerated (file mtime more recent than gate-start).

Gate Step 2 — Full test suite:
  Run `npm test` (vitest). Capture totals. Assert exit code is 0. Expected baseline: 484 passed / 1 skipped / 0 failed post-Phase-50. Phase 50.1 adds approximately 10 new node-label tests (D-15 + alias-removal + 7 stripExitPrefix), 2 new graph-validator tests (D-05 + D-08) plus 1 conflict regression, and 2 new runner-loop-picker tests (caption-strip + Phase 49-50 conflict). Total ~499 passed / 1 skipped / 0 failed (approximate — executor records actuals).

Gate Step 3 — Literal-«выход» runtime audit:
  Run `grep -rn 'выход' src/graph/ src/runner/ src/views/` and inspect results. The ONLY acceptable hits are in `src/graph/graph-validator.ts` lines 48-50 (the Phase 43 MIGRATE-01 message mentioning «выход» as a legacy migration keyword — preserved verbatim) and possibly in comments/JSDoc of node-label.ts / graph-validator.ts that explain the Phase 50.1 convention by referencing the legacy literal as context. NO runtime string compare against «выход» anywhere (zero instances of `edge.label === 'выход'` or similar). If any unexpected hit appears, STOP.

Gate Step 4 — Alias-removal guard:
  Run `grep -rn "export const isExitEdge = isLabeledEdge" src/` — must return 0 hits. Also run `grep -rn "isExitEdge = isLabeledEdge" src/` — must return 0 hits outside the Phase 49 historical SUMMARY (which lives in .planning/ and is not grepped here).

Gate Step 5 — Phase 50 reconciler untouched:
  Run `git diff --name-only ce61dc9..HEAD -- src/graph/edge-label-reconciler.ts src/canvas/edge-label-sync-service.ts src/canvas/canvas-live-editor.ts src/__tests__/edge-label-reconciler.test.ts` — must return empty (Phase 50.1 did not touch these files). Then run `npx vitest run src/__tests__/edge-label-reconciler.test.ts` — must exit 0.

Gate Step 6 — Click-handler ordering invariant:
  Use grep with context to extract the click-handler block from `src/views/runner-view.ts`:
    `grep -n 'registerDomEvent(btn' src/views/runner-view.ts | head -5`
  Then read the 8 lines at that offset and confirm the five calls appear in this order:
    capturePendingTextareaScroll then syncManualEdit then chooseLoopBranch then autoSaveSession then renderAsync
  This is the RUNFIX-02 + Standing Pitfall invariant.

Gate Step 7 — CSS zero-touch:
  Run `git diff --name-only ce61dc9..HEAD -- src/styles/ esbuild.config.mjs` — must return empty. Phase 50.1 ships zero CSS changes.

Gate Step 8 — Shared Pattern G audit (byte-identical preservation of untouched regions):
  Run:
    `grep -c "export function nodeLabel" src/graph/node-label.ts`  must equal 1
    `grep -c "export function isLabeledEdge" src/graph/node-label.ts`  must equal 1
    `grep -c "return edge.label != null && edge.label.trim() !== ''" src/graph/node-label.ts`  must equal 1 (isLabeledEdge body byte-identical to Phase 49)
    `grep -c "устаревшие узлы loop-start/loop-end" src/graph/graph-validator.ts`  must equal 1 (MIGRATE-01 preserved)
    `grep -c "метка «выход» на одном из исходящих рёбер" src/graph/graph-validator.ts`  must equal 1 (MIGRATE-01 message line 49 preserved)

Gate Step 9 — Required Phase 50.1 insertions:
  Run:
    `grep -c "export function isExitEdge" src/graph/node-label.ts`  must equal 1 (D-10 function, not alias)
    `grep -c "export function stripExitPrefix" src/graph/node-label.ts`  must equal 1 (D-09)
    `grep -c "stripExitPrefix" src/views/runner-view.ts`  at least 2 (import + call site)
    `grep -c "Phase 50.1 EDGE-03" src/runner/protocol-runner.ts`  at least 1
    `grep -c "\\*\\*EDGE-03\\*\\*" .planning/REQUIREMENTS.md`  at least 1
    `grep -c "unified-loop-labeled-body.canvas" src/__tests__/graph-validator.test.ts`  at least 1
    `grep -c "unified-loop-labeled-body.canvas" src/__tests__/runner/protocol-runner-loop-picker.test.ts`  at least 1

Record the results in the transcript as a checklist. If all nine steps pass, proceed to Task 3.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && echo "---" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Gate Step 1: npm run build exits 0; main.js regenerated.
    - Gate Step 2: npm test exits 0; green count is at or above the pre-phase baseline of 484 passed + approx 15 new Phase 50.1 tests; 0 failed; 1 skipped (unchanged).
    - Gate Step 3: Every literal-«выход» hit in src/graph/, src/runner/, src/views/ is either the MIGRATE-01 block or an explanatory comment. Zero runtime string-compare.
    - Gate Step 4: zero hits for the Phase 49 alias export.
    - Gate Step 5: Phase 50 reconciler files unchanged since ce61dc9 (Phase 50.1 start); reconciler test file exits 0.
    - Gate Step 6: click-handler block shows the five calls in the locked order.
    - Gate Step 7: zero CSS diff.
    - Gate Step 8: all byte-identical preservation greps return exactly 1.
    - Gate Step 9: all required Phase 50.1 insertions present.
  </acceptance_criteria>
  <done>All nine gate steps pass; results recorded in transcript; proceed to Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — Phase 50.1 observable behavior in TEST-BASE</name>
  <what-built>
    - Phase 50.1 +-prefix convention across node-label.ts, graph-validator.ts, runner-view.ts, protocol-runner.ts.
    - Main.js redeployed to TEST-BASE by Task 2's build.
    - Automated gate passed — build, full test suite, and all regression-protection audits green.
    - Canonical design note rewritten to describe the +-prefix convention.
  </what-built>
  <how-to-verify>
    Perform the following three scenarios in TEST-BASE (the Obsidian test vault). Report PASS or FAIL for each, and for FAIL include the observed vs expected.

    Scenario A — exit caption strips the + prefix (D-09/D-11 runtime check):
      1. Open a canvas that has a loop node with an outgoing edge labeled `+выход` (use a canvas you previously authored, or create one: add a loop node, draw an outgoing edge to a text-block, and label the edge literally `+выход` — with no space after the `+`).
      2. Start the Runner on this canvas.
      3. Walk until you reach the loop picker.
      4. Expected: the exit button caption reads exactly `выход` (no leading `+`).
      5. Also test `+ выход` (with ASCII space) on a second canvas — the caption should still read exactly `выход`.

    Scenario B — D-05 legacy-hint error panel (the migration guardrail):
      1. Open (or author) a canvas where a loop node has a single outgoing edge labeled bare `выход` (no `+` prefix — the Phase 49 style).
      2. Start the Runner on this canvas.
      3. Expected: the Runner shows an Error panel (does NOT reach the loop picker). The error text contains the fragment `не имеет выхода с префиксом "+"` and instructs the author to add `+` to one of the labeled edges, listing the candidate edge id(s).

    Scenario C — Phase 49 vs Phase 50 conflict fixture (labeled body edge coexists with +-exit):
      1. Open (or author) a canvas with:
         - a loop node,
         - one outgoing edge labeled `+выход` pointing to a terminal text-block,
         - one outgoing edge (body branch) pointing to a question that fans into an Answer node with a non-empty displayLabel. Phase 50's EdgeLabelSyncService automatically labels the body edge with the Answer's displayLabel.
      2. Save the canvas and start the Runner.
      3. Expected: Runner reaches the loop picker (no validation error) and shows exactly two buttons — one with caption `выход` (the stripped +-exit) and one with caption equal to the target node's label (per `nodeLabel` — for a Question target this is the questionText). Clicking the body button walks the body and returns to the picker; clicking the exit button ends the session cleanly.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
      - `approved` — all three scenarios PASS, no regressions observed.
      - A list of scenarios that failed + observed vs expected + any side-effects noticed (e.g. RUNFIX-02 scroll regression, manual-edit loss, CSS regressions).
  </resume-signal>
</task>

</tasks>

<verification>
- All five Phase 50.1 plans landed: 50.1-01, 50.1-02, 50.1-03, 50.1-04, 50.1-05.
- Gate Steps 1-9 all pass.
- Canonical note .planning/notes/loop-node-exit-edge-convention.md describes the +-prefix convention with an explicit supersede header.
- Human UAT sign-off received for all three scenarios.
</verification>

<success_criteria>
- Phase 50.1 is shippable: full test suite green, build green, TEST-BASE runtime behavior matches the three UAT scenarios, zero regression on Phase 50 reconciler + RUNFIX-02 invariant + CSS.
- EDGE-03 can be flipped to complete in REQUIREMENTS.md by the orchestrator rollup (out of scope for this plan).
- ROADMAP row for Phase 50.1 can be checked off by the orchestrator rollup.
</success_criteria>

<output>
After completion, create .planning/phases/50.1-loop-exit-plus-prefix/50.1-05-SUMMARY.md summarizing:
- Gate Step results (build, test totals, each audit).
- Canonical note rewrite confirmation.
- UAT decision + per-scenario PASS/FAIL.
- Single commit SHA (docs rewrite) + transcript reference for the gate + UAT.
</output>
