---
phase: 28-auto-node-coloring
plan: "01"
type: execute
wave: 1
depends_on:
  - "28-00"
files_modified:
  - src/views/editor-panel-view.ts
autonomous: true
requirements:
  - NODE-COLOR-01
  - NODE-COLOR-02

must_haves:
  truths:
    - "Сохранение question-узла через EditorPanel всегда записывает color: '5' в canvas JSON"
    - "Сохранение поля (не смены типа) на уже типизированном узле также записывает правильный цвет"
    - "Сохранение узла с неизвестным типом не записывает color"
    - "Unmark-путь (тип = '') удаляет color из canvas — поведение сохранено"
    - "Pattern B path (saveLive) получает enrichedEdits с color перед вызовом"
    - "npm test зелёный"
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "saveNodeEdits с инъекцией цвета"
      contains: "enrichedEdits"
    - path: "main.js"
      provides: "Скомпилированный плагин с новой логикой"
  key_links:
    - from: "src/views/editor-panel-view.ts:saveNodeEdits"
      to: "src/canvas/node-color-map.ts:NODE_COLOR_MAP"
      via: "import + lookup"
      pattern: "NODE_COLOR_MAP"
    - from: "enrichedEdits"
      to: "saveLive call"
      via: "передаётся вместо edits"
      pattern: "saveLive.*enrichedEdits"
---

<objective>
Wave 1: реализация инъекции цвета в `saveNodeEdits`.

Добавляет ~20 строк в `editor-panel-view.ts`:
- Создаёт `enrichedEdits = { ...edits }` в начале `saveNodeEdits`
- Разрешает тип из `enrichedEdits['radiprotocol_nodeType']` (type-change) ИЛИ из уже-существующего узла в Strategy A (field-only)
- Устанавливает `enrichedEdits['color']` из `NODE_COLOR_MAP` для известных типов
- Передаёт `enrichedEdits` вместо `edits` в `saveLive` и в Strategy A цикл записи

После реализации все тесты Wave 0 становятся зелёными.

Purpose: Удовлетворить NODE-COLOR-01 (оба пути: Pattern B и Strategy A) и NODE-COLOR-02 (всегда перезаписывать color).
Output: Обновлённый `editor-panel-view.ts` + собранный `main.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-auto-node-coloring/28-CONTEXT.md
@.planning/phases/28-auto-node-coloring/28-RESEARCH.md
@.planning/phases/28-auto-node-coloring/28-00-SUMMARY.md

<interfaces>
<!-- Текущая сигнатура saveNodeEdits (src/views/editor-panel-view.ts строки 146-227) -->
```typescript
async saveNodeEdits(
  filePath: string,
  nodeId: string,
  edits: Record<string, unknown>
): Promise<void> {
  // LIVE-03: Attempt live save via internal Canvas API first (Pattern B).
  try {
    const savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, edits);
    if (savedLive) { return; }
  } catch (err) { ... }

  // Strategy A path: vault.modify()
  const file = ...
  let raw = await this.plugin.app.vault.read(file as TFile);
  let canvasData = JSON.parse(raw);
  const nodeIndex = canvasData.nodes.findIndex(n => n['id'] === nodeId);

  const PROTECTED_FIELDS = new Set(['id', 'x', 'y', 'width', 'height', 'type']);
  const nodeTypeEdit = edits['radiprotocol_nodeType'];
  const isUnmarking = nodeTypeEdit === '' || nodeTypeEdit === undefined;
  // ... применяет edits к node объекту ...
  // Unmark path: удаляет radiprotocol_* и delete node['color']
  // Regular path: цикл for...of entries(edits) с PROTECTED_FIELDS.has(key) guard
}
```

<!-- NODE_COLOR_MAP — авторитетный источник -->
```typescript
// src/canvas/node-color-map.ts
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
  'start': '4', 'question': '5', 'answer': '2',
  'free-text-input': '2', 'text-block': '3',
  'loop-start': '1', 'loop-end': '1',
};
```

<!-- Существующий паттерн в onTypeDropdownChange (строки 560-587) — REPLICATE this -->
```typescript
const mappedColor = (NODE_COLOR_MAP as Record<string, string | undefined>)[selectedType];
if (mappedColor !== undefined) {
  edits['color'] = mappedColor;
}
```

<!-- CRITICAL: color НЕ входит в PROTECTED_FIELDS ни в editor-panel-view, ни в canvas-live-editor -->
<!-- Verified in regression.test.ts:127 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить инъекцию цвета в saveNodeEdits</name>
  <files>src/views/editor-panel-view.ts</files>
  <read_first>
    - src/views/editor-panel-view.ts (весь файл — строки 1-250 минимум, увидеть импорты и saveNodeEdits полностью)
    - src/canvas/node-color-map.ts (NODE_COLOR_MAP — для проверки импорта)
    - .planning/phases/28-auto-node-coloring/28-CONTEXT.md (D-01 через D-06)
    - .planning/phases/28-auto-node-coloring/28-RESEARCH.md (раздел "Code Examples" и "Pitfall 1")
    - src/__tests__/canvas-write-back.test.ts (тесты Wave 0 — понять что именно нужно реализовать)
  </read_first>
  <behavior>
    Тесты из Wave 0 описывают поведение. Реализация должна их удовлетворить:
    - Test TYPE-CHANGE 'question': saveNodeEdits({ radiprotocol_nodeType: 'question' }) → color: '5' в JSON
    - Test TYPE-CHANGE 'start': saveNodeEdits({ radiprotocol_nodeType: 'start' }) → color: '4' в JSON
    - Test FIELD-ONLY на question-узле: saveNodeEdits({ radiprotocol_questionText: 'x' }) на узле с radiprotocol_nodeType:'question' → color: '5' в JSON
    - Test OVERWRITE: узел с color:'6' + edits без типа → color: '5' в итоговом JSON
    - Test UNKNOWN: saveNodeEdits({ radiprotocol_nodeType: 'custom' }) → NO color в JSON
    - Test UNMARK: saveNodeEdits({ radiprotocol_nodeType: '' }) → color удалён (уже работает, не сломать)
    - Test LIVE-SAVE: saveLive получает enrichedEdits включая color: '5' для question
  </behavior>
  <action>
    ВАЖНО: прочитать файл ПЕРЕД редактированием. Не удалять существующий код.

    Изменения в `saveNodeEdits` (src/views/editor-panel-view.ts):

    **Шаг 1: Добавить импорт NODE_COLOR_MAP**
    Проверить импорты в начале файла. Если `NODE_COLOR_MAP` ещё не импортирован, добавить:
    ```typescript
    import { NODE_COLOR_MAP } from '../canvas/node-color-map';
    ```

    **Шаг 2: В начале функции saveNodeEdits — создать enrichedEdits и инъецировать color для type-change**
    Вставить сразу после открывающей скобки функции (до try/saveLive блока):
    ```typescript
    // Phase 28: D-01, D-02 — inject color before forking into Pattern B / Strategy A
    const enrichedEdits = { ...edits };
    const editedType = enrichedEdits['radiprotocol_nodeType'] as string | undefined;
    // D-06: unmark path — type is '' or key present with undefined/empty value
    const isTypeChange = 'radiprotocol_nodeType' in enrichedEdits;
    const isUnmarkingType = isTypeChange && (editedType === '' || editedType === undefined);

    if (!isUnmarkingType && editedType) {
      // D-04 priority 1: type explicitly changed — inject color immediately
      const mapped = (NODE_COLOR_MAP as Record<string, string | undefined>)[editedType];
      if (mapped !== undefined) {
        enrichedEdits['color'] = mapped; // D-02: overwrite regardless of prior color
      }
    }
    ```

    **Шаг 3: Заменить `edits` на `enrichedEdits` в вызове saveLive**
    Найти строку:
    ```typescript
    const savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, edits);
    ```
    Заменить на:
    ```typescript
    const savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, enrichedEdits);
    ```

    **Шаг 4: Внутри Strategy A блока — добавить fallback для FIELD-ONLY saves (D-04 priority 2)**
    После строки `const nodeIndex = canvasData.nodes.findIndex(...)` и после проверки `nodeIndex === -1`,
    но ДО существующего блока с PROTECTED_FIELDS, вставить:
    ```typescript
    // Phase 28: D-04 — fallback type resolution for field-only saves (type not in edits)
    if (!isTypeChange && !isUnmarkingType) {
      const existingNode = canvasData.nodes[nodeIndex] as Record<string, unknown> | undefined;
      const existingType = existingNode?.['radiprotocol_nodeType'] as string | undefined;
      if (existingType) {
        const mapped = (NODE_COLOR_MAP as Record<string, string | undefined>)[existingType];
        if (mapped !== undefined) {
          enrichedEdits['color'] = mapped; // D-02: always overwrite
        }
      }
    }
    ```

    **Шаг 5: В Strategy A цикле записи — заменить `edits` на `enrichedEdits`**
    Найти все места где используется `edits` в Strategy A части (после saveLive блока):
    - `const nodeTypeEdit = edits['radiprotocol_nodeType'];` → `const nodeTypeEdit = enrichedEdits['radiprotocol_nodeType'];`
    - `for (const [key, value] of Object.entries(edits))` → `for (const [key, value] of Object.entries(enrichedEdits))`
    - Остальные references на `edits` в нижней части функции — заменить на `enrichedEdits`

    ВАЖНО: переменная `isUnmarking` в текущем коде (строка ~198) должна быть согласована с новой `isUnmarkingType`.
    Проверить что логика unmark не дублируется — использовать единое определение.

    ВАЖНО: не удалять `delete node['color']` в unmark блоке — это D-06, его нужно сохранить.

    После изменений запустить:
    ```
    npm run build
    npm test -- --run
    ```
  </action>
  <verify>
    <automated>npm run build && npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run build завершается без ошибок TypeScript
    - npm test -- --run возвращает все тесты зелёными (0 failing)
    - grep "NODE_COLOR_MAP" src/views/editor-panel-view.ts возвращает >= 2 совпадения (импорт + usage)
    - grep "enrichedEdits" src/views/editor-panel-view.ts возвращает >= 4 совпадения
    - grep "saveLive.*enrichedEdits" src/views/editor-panel-view.ts возвращает совпадение (Pattern B получает enrichedEdits)
    - grep "delete node\['color'\]" src/views/editor-panel-view.ts возвращает совпадение (unmark path сохранён — D-06)
    - Тесты canvas-write-back: TYPE-CHANGE, FIELD-ONLY, OVERWRITE, UNKNOWN TYPE все проходят
    - Тест unmark-cleanup остаётся зелёным (регрессия не введена)
  </acceptance_criteria>
  <done>saveNodeEdits инъецирует color из NODE_COLOR_MAP для всех known типов на обоих путях (Pattern B и Strategy A). Все тесты green. Build успешен.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EditorPanel → canvas file | Пользовательские edits записываются в .canvas файл через vault.modify() |
| EditorPanel → Canvas view (Pattern B) | edits передаются в internal Canvas API через saveLive() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | saveNodeEdits — color injection | accept | color это palette string "1"-"6" из hardcoded константы NODE_COLOR_MAP. Нет внешнего ввода — только mapping из in-memory enum. Невозможно ввести произвольные данные через color injection. |
| T-28-01-02 | Tampering | enrichedEdits mutation | accept | enrichedEdits = spread copy от edits. Мутация не влияет на caller. PROTECTED_FIELDS guard уже предотвращает запись id/x/y/etc. |
| T-28-01-03 | Denial of Service | Strategy A vault.modify() | accept | Уже защищено try/catch с Notice. Phase 28 не изменяет error handling. |
</threat_model>

<verification>
После Wave 1:
```bash
npm run build   # должен завершиться без ошибок
npm test -- --run  # все тесты green, включая новые из Wave 0
```

Ручная проверка (опционально, не блокирует):
- Открыть canvas в Obsidian
- Открыть EditorPanel на question-узле
- Сохранить любое поле
- Убедиться что узел стал cyan в canvas (color = '5')
</verification>

<success_criteria>
- `npm run build` — OK, без TypeScript ошибок
- `npm test -- --run` — все тесты зелёные, включая:
  - TYPE-CHANGE color injection (question → '5', start → '4')
  - FIELD-ONLY color injection (existing typed node)
  - OVERWRITE (wrong color → correct color)
  - UNKNOWN TYPE (no color written)
  - UNMARK path (color deleted — регрессия не введена)
  - LIVE-SAVE (enrichedEdits с color передан в saveLive)
- NODE-COLOR-01: оба пути (Pattern B + Strategy A) покрыты
- NODE-COLOR-02: color всегда перезаписывается
</success_criteria>

<output>
После завершения создать `.planning/phases/28-auto-node-coloring/28-01-SUMMARY.md`
</output>
