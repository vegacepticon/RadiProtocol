---
phase: 28-auto-node-coloring
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/canvas-write-back.test.ts
  - src/__tests__/test-utils/make-canvas-node.ts
autonomous: true
requirements:
  - NODE-COLOR-03

must_haves:
  truths:
    - "Старый тест 'color are never written' заменён на тест нового контракта"
    - "Хелпер makeCanvasNode создаёт узел с правильным color из NODE_COLOR_MAP"
    - "npm test проходит зелёным (тесты ожидают реализацию Wave 1 — могут быть pending/skipped или fail-fast)"
  artifacts:
    - path: "src/__tests__/test-utils/make-canvas-node.ts"
      provides: "Фабрика типизированных узлов canvas"
      exports: ["makeCanvasNode"]
    - path: "src/__tests__/canvas-write-back.test.ts"
      provides: "Обновлённый контракт сохранения цвета"
      contains: "color is always written"
  key_links:
    - from: "src/__tests__/test-utils/make-canvas-node.ts"
      to: "src/canvas/node-color-map.ts"
      via: "import NODE_COLOR_MAP"
      pattern: "NODE_COLOR_MAP\\[type\\]"
---

<objective>
Wave 0: подготовка тест-инфраструктуры для Phase 28.

Создаёт два изменения в тестах ДО реализации:
1. Обновляет старый контракт в `canvas-write-back.test.ts` (строка 57): "color never written" → "color always written for known types"
2. Добавляет новые тесты для NODE-COLOR-01/02 поведения (Strategy A)
3. Создаёт хелпер `makeCanvasNode(type, overrides?)` — D-07, D-08

После Wave 0 тесты описывают НОВЫЙ контракт. Часть из них будет RED (падать) до выполнения Wave 1.

Purpose: Обеспечить Nyquist-compliant тест-инфраструктуру; Wave 1 получает чёткие ожидания.
Output: `src/__tests__/test-utils/make-canvas-node.ts` + обновлённый `canvas-write-back.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-auto-node-coloring/28-CONTEXT.md
@.planning/phases/28-auto-node-coloring/28-RESEARCH.md

<interfaces>
<!-- Из src/canvas/node-color-map.ts -->
```typescript
import type { RPNodeKind } from '../graph/graph-model';

export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
  'start':           '4',  // green
  'question':        '5',  // cyan
  'answer':          '2',  // orange
  'free-text-input': '2',  // orange
  'text-block':      '3',  // yellow
  'loop-start':      '1',  // red
  'loop-end':        '1',  // red
};
```

<!-- Из src/__tests__/canvas-write-back.test.ts — строка 57, СТАРЫЙ тест для замены -->
```typescript
it('PROTECTED_FIELDS: id, x, y, width, height, type, color are never written', async () => {
  // ... заменить на новый контракт color
});
```

<!-- Сигнатура saveLive в тестах -->
mockSaveLive = vi.fn().mockResolvedValue(false);  // false = Strategy A path
mockSaveLive = vi.fn().mockResolvedValue(true);   // true = Pattern B path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Обновить контракт теста color в canvas-write-back.test.ts</name>
  <files>src/__tests__/canvas-write-back.test.ts</files>
  <read_first>
    - src/__tests__/canvas-write-back.test.ts (полный файл — текущее состояние)
    - src/canvas/node-color-map.ts (NODE_COLOR_MAP — значения цветов для assertions)
    - .planning/phases/28-auto-node-coloring/28-CONTEXT.md (D-03, D-04, D-05, D-06)
    - .planning/phases/28-auto-node-coloring/28-RESEARCH.md (раздел "Test Contract Changes")
  </read_first>
  <behavior>
    - Старый тест строка 57 "PROTECTED_FIELDS: id, x, y, width, height, type, color are never written"
      ЗАМЕНЯЕТСЯ на 5 новых тестов:
      1. TYPE-CHANGE: `{ radiprotocol_nodeType: 'question' }` → Strategy A записывает `color: '5'` в JSON (per D-01, NODE-COLOR-01)
      2. TYPE-CHANGE: `{ radiprotocol_nodeType: 'start' }` → записывает `color: '4'` (per NODE-COLOR-01)
      3. FIELD-ONLY на уже типизированном узле: edits `{ radiprotocol_questionText: 'text' }` на узле с `radiprotocol_nodeType: 'question'` в canvas JSON → записывает `color: '5'` (per D-04, NODE-COLOR-02)
      4. OVERWRITE: узел имеет `color: '6'` в JSON, edits `{ radiprotocol_questionText: 'x' }` на `question` узле → `color` в записанном JSON равен `'5'` (per NODE-COLOR-02)
      5. UNKNOWN TYPE: `{ radiprotocol_nodeType: 'custom-unknown' }` → `color` НЕ записывается (per D-05)
    - Унарк тест (строка 126) остаётся без изменений — он уже проверяет правильное поведение
    - Тест live-save (строка 113) обновляется: `mockSaveLive` должен получить enriched edits с `color` для known type (per D-01, D-02) — т.е. `{ radiprotocol_nodeType: 'question', color: '5' }`
    - Существующие тесты 'radiprotocol_* fields are written' и 'undefined values delete the key' остаются, при необходимости проверить что они ожидают `color` в записанном JSON
    - Для FIELD-ONLY и OVERWRITE тестов использовать `makeCanvasJson({ radiprotocol_nodeType: 'question' })` чтобы узел имел тип
    - ВАЖНО: тесты TYPE-CHANGE/FIELD-ONLY/OVERWRITE будут RED (failing) до Wave 1 — это ожидаемо
  </behavior>
  <action>
    1. Прочитать весь `canvas-write-back.test.ts` (уже сделано через read_first)
    2. Найти тест строка 57 — удалить его полностью
    3. Вместо него вставить 5 новых тестов (описаны в behavior)
    4. Для тестов FIELD-ONLY и OVERWRITE: `mockVaultRead` должен возвращать `makeCanvasJson({ radiprotocol_nodeType: 'question' })` через `beforeEach` или через `mockVaultRead.mockResolvedValueOnce(...)` внутри теста
    5. Обновить live-save тест (строка 113): после Phase 28, `saveLive` получит `{ radiprotocol_nodeType: 'question', color: '5' }` — обновить `expect(mockSaveLive).toHaveBeenCalledWith(...)` чтобы включал `color: '5'`
    6. НЕ удалять никакие другие тесты (un-mark cleanup, radiprotocol_* fields, undefined values)
    7. НЕ изменять helper `makeCanvasJson` — использовать как есть
    8. Запустить `npm test -- --run canvas-write-back` и убедиться что:
       - Новые тесты на color либо fail (ожидаемо, Wave 1 ещё не реализован), либо pass если реализация уже была
       - Старые тесты (кроме удалённого) остаются green
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "color is always written\|TYPE-CHANGE\|FIELD-ONLY\|OVERWRITE\|UNKNOWN TYPE" src/__tests__/canvas-write-back.test.ts возвращает >= 5
    - grep "color are never written" src/__tests__/canvas-write-back.test.ts возвращает пустой результат (старый тест удалён)
    - npm test -- --run выполняется без синтаксических ошибок (test suite компилируется)
    - Существующие тесты 'radiprotocol_* fields are written', 'undefined values delete', 'un-mark cleanup' остаются в файле (grep найдёт их)
  </acceptance_criteria>
  <done>Старый тест "color never written" удалён. 5 новых тестов добавлены. Live-save тест обновлён. Тест-файл компилируется.</done>
</task>

<task type="auto">
  <name>Task 2: Создать хелпер makeCanvasNode (D-07, D-08)</name>
  <files>src/__tests__/test-utils/make-canvas-node.ts</files>
  <read_first>
    - src/canvas/node-color-map.ts (NODE_COLOR_MAP — единственный источник правды)
    - src/graph/graph-model.ts (тип RPNodeKind — для импорта)
    - .planning/phases/28-auto-node-coloring/28-CONTEXT.md (D-07, D-08)
    - .planning/phases/28-auto-node-coloring/28-RESEARCH.md (раздел "New test utility: makeCanvasNode helper")
  </read_first>
  <action>
    1. Создать директорию `src/__tests__/test-utils/` если не существует
    2. Создать файл `src/__tests__/test-utils/make-canvas-node.ts` со следующим содержимым:

    ```typescript
    // src/__tests__/test-utils/make-canvas-node.ts
    // Canonical test helper for creating typed canvas node fixtures.
    // Color is automatically derived from NODE_COLOR_MAP — prevents manual sync drift (D-07, D-08).
    //
    // Phase 28: NODE-COLOR-03

    import { NODE_COLOR_MAP } from '../../canvas/node-color-map';
    import type { RPNodeKind } from '../../graph/graph-model';

    /**
     * Creates a canvas node record with the correct `color` field derived from NODE_COLOR_MAP.
     * Use this helper in all tests that need a typed canvas node fixture.
     *
     * @param type - RPNodeKind for the node (must be a known type in NODE_COLOR_MAP)
     * @param overrides - Optional fields to override defaults
     */
    export function makeCanvasNode(
      type: RPNodeKind,
      overrides: Record<string, unknown> = {}
    ): Record<string, unknown> {
      return {
        id: `node-${Math.random().toString(36).slice(2, 9)}`,
        type: 'text',
        x: 0,
        y: 0,
        width: 200,
        height: 60,
        radiprotocol_nodeType: type,
        color: NODE_COLOR_MAP[type],
        ...overrides,
      };
    }
    ```

    3. Запустить `npm test -- --run` чтобы убедиться что новый файл не вызывает ошибок компиляции
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Файл `src/__tests__/test-utils/make-canvas-node.ts` существует
    - grep "export function makeCanvasNode" src/__tests__/test-utils/make-canvas-node.ts возвращает совпадение
    - grep "NODE_COLOR_MAP\[type\]" src/__tests__/test-utils/make-canvas-node.ts возвращает совпадение (D-08: color из map, не хардкод)
    - grep "import.*NODE_COLOR_MAP.*from.*node-color-map" src/__tests__/test-utils/make-canvas-node.ts возвращает совпадение
    - npm test -- --run завершается без ошибок TypeScript/import в новом файле
  </acceptance_criteria>
  <done>Хелпер makeCanvasNode создан. Автоматически получает color из NODE_COLOR_MAP. Файл компилируется без ошибок.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Production code | Тесты читают NODE_COLOR_MAP напрямую — импорт из production module. Нет внешних данных. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-00-01 | Tampering | canvas-write-back.test.ts | accept | Тест-файл, не production. Изменения только в тестах — нет runtime риска. |
| T-28-00-02 | Information Disclosure | make-canvas-node.ts | accept | Только в тестах, нет чувствительных данных. Palette strings "1"-"6" не являются секретами. |
</threat_model>

<verification>
После Wave 0:
- `npm test -- --run` завершается без синтаксических ошибок
- Новые тесты на color injection существуют в canvas-write-back.test.ts
- Старый тест "color never written" удалён
- Хелпер makeCanvasNode экспортирован и использует NODE_COLOR_MAP
- Ожидается: 3-5 новых тестов будут RED (failing) до Wave 1 — это нормально
</verification>

<success_criteria>
- `src/__tests__/test-utils/make-canvas-node.ts` создан, экспортирует `makeCanvasNode`
- `canvas-write-back.test.ts` не содержит строку "color are never written"
- `canvas-write-back.test.ts` содержит тесты для: type-change color, field-only color, overwrite color, unknown type, live-save с color
- `npm test -- --run` завершается без ошибок компилятора TypeScript
</success_criteria>

<output>
После завершения создать `.planning/phases/28-auto-node-coloring/28-00-SUMMARY.md`
</output>
