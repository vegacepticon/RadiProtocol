---
phase: 77
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
autonomous: true
requirements: [LINT-01]
must_haves:
  truths:
    - ".planning/** is excluded from lint surface (D-01)"
    - "src/__tests__/** and src/__mocks__/** receive a relaxed override block (D-02) covering @typescript-eslint/no-explicit-any and @typescript-eslint/no-unused-vars with _underscore-pattern"
    - "Single commit message contains BOTH D-01 and D-02 written rationales per ROADMAP SC#3"
    - "After this plan, npm run lint reports ~75 src-only findings (down from 523)"
    - "npm run build still exits 0"
    - "npm test still exits 0 (818 tests baseline preserved)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: ".planning/** ignore + tests/mocks override block"
      contains_after: "src/__tests__/**/*.ts"
  key_links:
    - from: "eslint.config.mjs"
      to: "src/__tests__/** + src/__mocks__/**"
      via: "files glob in override block"
      pattern: "files:\\s*\\[\\s*'src/__tests__/\\*\\*/\\*\\.ts'"
    - from: "eslint.config.mjs"
      to: ".planning/archive/**/*.mjs (parsing-error sources)"
      via: "ignores array"
      pattern: "'\\.planning/\\*\\*'"
---

<objective>
Establish the ESLint baseline for Phase 77 by adding `.planning/**` to ignores (D-01) and inserting a tests/mocks override block (D-02). This single Wave 1 plan resets the lint finding count from 523 → ~75 src-only findings, freeing Wave 2 plans to migrate static-styles violations one file at a time without sifting through ~440 noise findings in tests/mocks.

Purpose: per ROADMAP SC#3, both override rationales live in **one** commit message. Per ROADMAP SC#1, we are gating on `npm run lint` exit 0 — this plan is the first step toward that gate; subsequent plans clear the residual ~75 findings.
Output: amended `eslint.config.mjs`; commit with both D-01 and D-02 rationales verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@eslint.config.mjs

<interfaces>
<!-- ESLint flat config shape; the existing file uses tseslint.config(...) returning the array.
     Last-match semantics: per-file overrides MUST come AFTER the broader src/**/*.ts config and
     BEFORE the ignores block, so the narrower glob wins for tests/mocks. -->

Current eslint.config.mjs structure (verified by Read on 2026-04-30):
- Line 6: `tseslint.config(` opens
- Lines 7-9: base recommended configs
- Lines 11-23: global languageOptions block (browser + node globals)
- Lines 25-80: main src/**/*.ts config (23 obsidianmd rules + TS rules + general rules)
- Lines 82-92: ignores block (current contents: node_modules/**, main.js, eslint.config.mjs, esbuild.config.mjs, version-bump.mjs, vitest.config.ts)
- Line 93: closing `)`

D-02 override block target shape:
```javascript
{
  files: ['src/__tests__/**/*.ts', 'src/__mocks__/**/*.ts'],
  rules: {
    '@typescript-eslint/no-explicit-any': 'off',
    '@typescript-eslint/no-unused-vars': ['error', {
      argsIgnorePattern: '^_',
      varsIgnorePattern: '^_',
      caughtErrorsIgnorePattern: '^_',
    }],
  },
},
```
Insertion point: after line 80 (closing `}` of main src config), before line 82 (`{ ignores: [...] }`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .planning/** to ignores AND insert tests/mocks override block</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — 93 lines)
    - .planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md (D-01, D-02 verbatim rationale)
    - .planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md §«Pattern 4: Eslint config delta (D-01 + D-02)»
    - .planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md §1 «eslint.config.mjs — D-01 + D-02 config edits»
    - CLAUDE.md (root) — confirm «only modify code relevant to current phase»
  </read_first>
  <action>
    Make exactly two edits to `eslint.config.mjs`. Do NOT touch any other rule, plugin import, or block.

    **Edit 1 — append `.planning/**` to ignores array (D-01):**
    Locate the existing ignores array at lines 84-91:
    ```
    ignores: [
      'node_modules/**',
      'main.js',
      'eslint.config.mjs',
      'esbuild.config.mjs',
      'version-bump.mjs',
      'vitest.config.ts',
    ],
    ```
    Append a new entry as the LAST line of the array (before the closing `]`):
    ```
      '.planning/**',
    ```
    Result:
    ```
    ignores: [
      'node_modules/**',
      'main.js',
      'eslint.config.mjs',
      'esbuild.config.mjs',
      'version-bump.mjs',
      'vitest.config.ts',
      '.planning/**',
    ],
    ```

    **Edit 2 — insert tests/mocks override block (D-02):**
    INSERT a new top-level config object IMMEDIATELY AFTER the closing `},` of the «Main source files config» block (currently line 80) and IMMEDIATELY BEFORE the `// Ignore patterns` comment (currently line 82). Last-match semantics in flat-config require the narrower-glob override to come after the broader src/**/*.ts config.

    The block to insert verbatim (preserve indentation, comma at end so the next block follows cleanly):
    ```javascript

      // Phase 77 D-02: tests/mocks override —
      //   * @typescript-eslint/no-explicit-any: off — Obsidian API mocks need flexible
      //     shapes (see src/__mocks__/obsidian.ts where every method takes `unknown`);
      //     full typing duplicates production types or breaks tests.
      //   * @typescript-eslint/no-unused-vars: error w/ _underscore-pattern — already
      //     project convention per CONVENTIONS.md §Naming Patterns. Mock callbacks
      //     intentionally accept (_evt, _text, _cb) to match upstream signatures.
      {
        files: ['src/__tests__/**/*.ts', 'src/__mocks__/**/*.ts'],
        rules: {
          '@typescript-eslint/no-explicit-any': 'off',
          '@typescript-eslint/no-unused-vars': ['error', {
            argsIgnorePattern: '^_',
            varsIgnorePattern: '^_',
            caughtErrorsIgnorePattern: '^_',
          }],
        },
      },
    ```

    Do NOT add any other rules to this override block. Per D-03, residual test errors (no-this-alias × 3, no-floating-promises × 1, sentence-case × 2, ban-ts-comment × 1, prefer-const × 1) are fixed manually in Wave 3 — they are NOT relaxed via override.

    After the file is saved, run `node -c eslint.config.mjs 2>&1 || true` is not applicable (the file is ESM and uses dynamic imports), so instead validate by running the lint command itself in the verify step.

    **Commit message (single commit per D-11 stage 1):**
    Use the title `chore(lint): ignore .planning/ + add test/mock overrides` and a body that quotes BOTH D-01 and D-02 rationales verbatim per ROADMAP SC#3:
    ```
    chore(lint): ignore .planning/ + add test/mock overrides

    D-01 — .planning/** in ignores:
    .planning/archive/v1.11-phases/72.../73.../build/*.mjs are archived
    canvas-builder scripts of the completed v1.11 milestone, not active
    source code. CONCERNS.md MEDIUM-3 confirms archive status. Removes
    9 parsing errors from the lint surface.

    D-02 — src/__tests__/** + src/__mocks__/** override:
    * @typescript-eslint/no-explicit-any: off — Obsidian API mocks need
      flexible shapes (see src/__mocks__/obsidian.ts where every method
      takes `unknown`). Full typing duplicates production types or breaks
      tests.
    * @typescript-eslint/no-unused-vars: error w/ _underscore-pattern —
      already project convention per CONVENTIONS.md §Naming Patterns.
      Mock callbacks intentionally accept (_evt, _text, _cb) to match
      upstream signatures.

    Removes ~503 errors in tests/mocks; residual src+test findings
    (~75 expected) addressed in Wave 2/3. Per ROADMAP SC#3 both rule
    relaxations have written justification in this commit.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 | grep -E '([0-9]+) problems' | awk '{print $2}'</automated>
    <!-- Expect a number around 70-90 (was 523). Spec: must be ≤ 100. -->
  </verify>
  <acceptance_criteria>
    - `eslint.config.mjs` contains the literal string `'.planning/**'` inside the ignores array — verify with `grep -c "'.planning/\*\*'" eslint.config.mjs` returning 1.
    - `eslint.config.mjs` contains the override block — verify with `grep -c "files: \['src/__tests__/\*\*/\*.ts'" eslint.config.mjs` returning 1.
    - The override block contains `'@typescript-eslint/no-explicit-any': 'off'` — verify with `grep -c "'@typescript-eslint/no-explicit-any': 'off'" eslint.config.mjs` returning 1.
    - The override block contains the `_`-pattern unused-vars rule — verify with `grep -c "argsIgnorePattern: '\\^_'" eslint.config.mjs` returning 1.
    - `npm run lint 2>&1 | tail -2 | grep -oE '[0-9]+ problems' | head -1` shows a count ≤ 100 (was 523).
    - `npm run build` exits 0 (esbuild + tsc still happy).
    - `npm test` exits 0 (vitest 818 tests still pass — config-only change cannot affect runtime behaviour).
    - `git log -1 --format=%B` shows commit message containing BOTH "D-01" and "D-02" string tokens (verifies ROADMAP SC#3 «written justification» mandate).
  </acceptance_criteria>
  <done>
    `eslint.config.mjs` has both edits; one commit landed; `npm run lint` finding count is ≤ 100 (down from 523); `npm run build` and `npm test` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→eslint config | Developer authors flat-config rule changes; downstream `npm run lint` consumes them. The boundary is between locked decision (D-01/D-02) and the executor's textual edit. |
| (no new runtime trust boundaries) | Phase 77 is a refactor / lint baseline; no new attack surfaces are introduced. |

## STRIDE Threat Register (canonical for Phase 77; later plans reference this block)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering / behavior regression | UI rendering across runner-view, inline-runner-modal, editor-panel-view, snippet-* views | mitigate | Existing vitest suite (818 tests pre-Phase-77) is the primary gate. Each Wave 2 plan acceptance MUST run the focused-feature vitest plus `npm run build`. CSS append-only contract per CLAUDE.md prevents silent rule deletion (Pitfall 1 in RESEARCH.md). Manual visual smoke per VALIDATION.md §«Manual-Only Verifications» backstops the automated gate. |
| T-77-02 | UX-semantic drift via prefer-file-manager-trash-file | snippet-service.ts:240,283 | accept (document out-of-scope) | Per D-13 + ROADMAP SC#5: `vault.trash` → `app.fileManager.trashFile` changes UX (vault `.trash/` vs OS Trash vs Permanent). Behaviour change requires a UX decision phase outside LINT-01 scope. Documented in `77-VERIFICATION.md` per ROADMAP SC#5; the 2 warnings remain (warnings do not fail lint). |
| T-77-03 | Spoofing via per-line eslint-disable directive misuse | snippet-service.ts:488 (no-control-regex), inline-runner-position.test.ts:216-217 (no-static-styles-assignment) | mitigate | Per ROADMAP SC#3: per-line disables MUST carry a written rationale in both the inline comment and the commit message. Reviewer can audit `grep -n "eslint-disable" src/` post-phase. |
| T-77-04 | Lint config tampering reducing future coverage | eslint.config.mjs override block scope | mitigate | D-02 override is bounded to two specific globs (`src/__tests__/**`, `src/__mocks__/**`); D-03 explicitly prohibits extending overrides to other rules (no-this-alias, sentence-case, etc.) — those remain enforced in tests too. |

**ASVS Level:** L1 (refactor phase; no new endpoints, no new auth surface, no new data persistence).

**Block on:** high. Phase 77 has no high-severity threats — T-77-01 is medium; T-77-02..04 are low. Phase proceeds.

**Cross-reference:** Plans 02-13 reference this block as `threat_model_ref: 77-01-PLAN.md` instead of duplicating the table.
</threat_model>

<verification>
**Plan-level verification:**
1. `git diff main HEAD -- eslint.config.mjs` shows ONLY two additions (`.planning/**` line + the new override block) — no other lines changed.
2. `npm run lint 2>&1 | tail -2 | grep -E '[0-9]+ problems'` reports ≤ 100 problems (was 523).
3. `npm run build && npm test` exits 0 — config-only change, runtime behaviour unchanged.
4. `git log -1 --format=%B` includes the literal strings `D-01` and `D-02` in the body (ROADMAP SC#3 audit).
</verification>

<success_criteria>
- ROADMAP SC#3 satisfied for D-01 + D-02 — both rationales in one commit message.
- Lint finding count drops from 523 to ~75 src-only (D-01 removes 9 parsing errors; D-02 removes ~503 test/mock errors).
- `npm run build` exits 0.
- `npm test` exits 0 (vitest 818 tests).
- `eslint.config.mjs` change is purely additive — no existing rule is modified or removed.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-01-SUMMARY.md` with:
- The two edits applied (file:line of insertion).
- Pre/post lint counts (`523 → N`).
- Confirmation that `npm run build` and `npm test` exit 0.
- Confirmation commit message includes D-01 and D-02 rationales.
</output>
</content>
</invoke>