---
phase: 77
plan: 12
type: execute
wave: 7
depends_on: ["01", "05", "07", "11"]
files_modified:
  - src/main.ts
  - src/settings.ts
  - src/views/editor-panel-view.ts
  - src/views/snippet-chip-editor.ts
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "All 26 src-side `obsidianmd/ui/sentence-case` violations are resolved (Title Case strings rewritten to first-letter-uppercase only)"
    - "Russian-language strings unchanged (rule applies to English text per camelCase enforcement)"
    - "User-visible UI strings retain their semantic meaning — only casing changes"
    - "vitest tests assertion strings updated where they test exact UI text (in lock-step with production)"
  artifacts:
    - path: "src/main.ts"
      provides: "Sentence-case English UI strings"
    - path: "src/settings.ts"
      provides: "Sentence-case settings labels"
    - path: "src/views/editor-panel-view.ts"
      provides: "Sentence-case form field labels"
    - path: "src/views/snippet-chip-editor.ts"
      provides: "Sentence-case button text"
  key_links: []
---

<objective>
Clear all 26 `obsidianmd/ui/sentence-case` errors in production source. Per RESEARCH.md aggregate, the rule fires on:
- `src/main.ts` — command names, ribbon icon labels, Notice strings
- `src/settings.ts` — settings tab field labels, button captions
- `src/views/editor-panel-view.ts` — form field labels, header text
- `src/views/snippet-chip-editor.ts` — button captions

The rule's `enforceCamelCaseLower: true` config means `Save Settings` becomes `Save settings` (first-letter capital, rest lowercase), `New Snippet` becomes `New snippet`, etc. Russian text is unaffected (regex only matches words with multi-Capital pattern).

Plan 12 runs in Wave 7 AFTER Plans 05 (snippet-editor-modal), 07 (editor-panel-view auto-grow), 11 (TFile guards) which also touch some of these files. Sequencing avoids merge churn in shared files.

Per CLAUDE.md, the diff to each file is bounded strictly to UI-string-edit lines (typically `text:`/`name:` props of `createEl`, `addCommand`, `Notice`, button captions). Do NOT refactor any logic.

**Caveat — vitest assertions:** Some tests assert exact UI strings (e.g. `expect(button.textContent).toBe('Save Settings')`). When a string changes in production, EVERY corresponding test assertion must change in lock-step. The executor MUST run `git grep -F "Save Settings"` (substituting the actual changed string) BEFORE editing to find all sites; rename them ALL together in the same commit.

Purpose: ROADMAP SC#1 (lint exit 0).
Output: ~26 string edits across 4 production files + matching test assertion updates; vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/main.ts
@src/settings.ts
@src/views/editor-panel-view.ts
@src/views/snippet-chip-editor.ts

<interfaces>
<!-- Rule semantics (verified by reading the rule source per RESEARCH.md):
     `obsidianmd/ui/sentence-case` with `enforceCamelCaseLower: true` flags
     strings that have multiple capitalized words (Title Case). Examples:

     'Save Settings'    → flagged → 'Save settings'
     'New Snippet'      → flagged → 'New snippet'
     'Delete Folder'    → flagged → 'Delete folder'
     'Open Canvas File' → flagged → 'Open canvas file'

     Acronyms and proper nouns can stay capitalized; the rule has heuristics.
     Single-word strings ('Save', 'Settings') are unaffected.

     Russian text isn't matched by the rule (Cyrillic doesn't trip the
     all-Capital regex).

     CRITICAL workflow: BEFORE editing any string,
     `git grep -F "<original string>"` to find ALL references — if vitest
     tests or other source files reference the same string, they must
     ALL change together in this commit. -->

Files affected (per RESEARCH.md):
- main.ts: ~5-8 strings
- settings.ts: ~10-12 strings
- editor-panel-view.ts: ~3-5 strings
- snippet-chip-editor.ts: ~2-4 strings

Total: 26 (per RESEARCH.md aggregate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused lint to enumerate every sentence-case violation by line</name>
  <files>(read-only — produces working list)</files>
  <read_first>
    - eslint output for the 4 target files
  </read_first>
  <action>
    Run focused lint:
    ```bash
    npx eslint src/main.ts src/settings.ts src/views/editor-panel-view.ts src/views/snippet-chip-editor.ts 2>&1 | grep "sentence-case"
    ```

    Capture the file:line:column list of all 26 violations.

    For each violation, Read the line and identify:
    - Original Title-Case string.
    - Sentence-case rewrite (lowercase all words after the first; keep proper nouns/acronyms intact).
    - Whether the string is an outward-facing UI label (high probability — that's why the rule fires) — Russian fallback is OK if string is Russian.

    Also for each unique string:
    ```bash
    git grep -F "<original string>" -- src/
    ```
    to find any vitest tests that assert against the exact text — those must change in lock-step.

    Build a mapping table:
    ```
    file:line  | before                   | after                   | also-occurs-in
    main.ts:N  | 'Save Settings'          | 'Save settings'         | __tests__/foo.test.ts:M
    settings.ts:N | 'Default Separator'  | 'Default separator'     | (no test refs)
    ...
    ```

    No file output yet.
  </action>
  <verify>
    <automated>echo "Read-only mapping task; verification is the executor's confirmation that all 26 violations are mapped to before/after strings."</automated>
  </verify>
  <acceptance_criteria>
    - Mapping table covers all ~26 violations with file:line, before, after, and any test cross-references.
    - Russian-language strings explicitly noted (rule shouldn't be flagging them; if it does, investigate before changing).
  </acceptance_criteria>
  <done>
    Mapping ready; ready to apply in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply sentence-case rewrites in production + matching test updates</name>
  <files>
    src/main.ts
    src/settings.ts
    src/views/editor-panel-view.ts
    src/views/snippet-chip-editor.ts
    (any test files identified in Task 1 mapping as cross-referencing changed strings)
  </files>
  <read_first>
    - (mapping from Task 1)
  </read_first>
  <action>
    For each entry in the Task 1 mapping table:
    1. Replace the production string with its sentence-case rewrite.
    2. Replace ANY corresponding test-assertion string in lock-step.
    3. Verify with `git grep -F "<original string>"` that no instance is left.

    Per CLAUDE.md «only modify code relevant to current phase» — diff is bounded strictly to the changed string lines plus their immediate punctuation. NO refactoring of surrounding logic.

    **If the test files surface in this plan's `files_modified` list** (mapping reveals cross-references): list them in the SUMMARY but the diff stays minimal — only string updates.

    **Commit message:**
    ```
    refactor(ui): apply sentence-case to 26 user-visible strings

    Resolves obsidianmd/ui/sentence-case errors across:
    - src/main.ts (ribbon, commands, Notice text)
    - src/settings.ts (field labels, button captions)
    - src/views/editor-panel-view.ts (form field labels)
    - src/views/snippet-chip-editor.ts (button text)

    Rule applies enforceCamelCaseLower: true — Title Case → first-cap-only.
    Russian-language strings unaffected (rule skips Cyrillic).

    Test assertions referencing changed strings updated in lock-step
    (verified via `git grep -F` for each unique original string).

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/main.ts src/settings.ts src/views/editor-panel-view.ts src/views/snippet-chip-editor.ts 2>&1 | grep -c "sentence-case"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `npx eslint src/main.ts src/settings.ts src/views/editor-panel-view.ts src/views/snippet-chip-editor.ts 2>&1 | grep -c "sentence-case"` returns 0.
    - For every original string changed, `git grep -F "<original>"` returns 0 matches across `src/` (no stragglers in tests).
    - `npm test` exits 0 — assertion strings updated in lock-step.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    All 26 sentence-case violations cleared; tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/main.ts src/settings.ts src/views/editor-panel-view.ts src/views/snippet-chip-editor.ts` reports 0 `obsidianmd/ui/sentence-case` errors.
2. `npm run lint` total decreases by ~26.
3. `npm run build && npm test` green.
4. `git grep` confirms no stragglers (no test file asserts the old strings).
</verification>

<success_criteria>
- 26 sentence-case violations cleared.
- UI semantics preserved (only casing changes).
- Test assertions updated in lock-step.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-12-SUMMARY.md` with:
- Mapping table (file:line → before → after).
- List of test files updated in lock-step.
- Confirmation 0 sentence-case errors remain in src/.
</output>
</content>
</invoke>