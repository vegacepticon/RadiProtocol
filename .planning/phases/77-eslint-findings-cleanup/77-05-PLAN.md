---
phase: 77
plan: 05
type: execute
wave: 4
depends_on: ["01", "04"]
files_modified:
  - src/views/snippet-editor-modal.ts
  - src/styles/snippet-manager.css
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "snippet-editor-modal.ts no longer contains literal style assignments at lines 146, 199, 200, 217, 218, 374, 375, 518, 525, 637"
    - "Banner show/hide uses el.toggleClass('rp-snippet-banner-hidden', !visible)"
    - "Locked-state visual disabled (pointerEvents:none + opacity:0.5) uses el.toggleClass('rp-snippet-form-locked', isLocked)"
    - "Modal max-width:800px is permanent class addClass('rp-snippet-editor-modal')"
    - "Save-error color uses permanent class addClass('rp-snippet-editor-save-error')"
    - "Unused eslint-disable directive at line 143 is REMOVED (D-13 warning fix)"
    - "src/styles/snippet-manager.css gains a Phase 77 block at the END (third Phase 77 block in this file)"
    - "Existing snippet-editor-modal vitest tests stay green"
  artifacts:
    - path: "src/views/snippet-editor-modal.ts"
      provides: "Class-based modal max-width, banner toggle, save-error color, locked state"
    - path: "src/styles/snippet-manager.css"
      provides: "Phase 77 block — modal sizing, banner-hidden, save-error, form-locked"
      contains: "rp-snippet-editor-modal"
  key_links:
    - from: "src/views/snippet-editor-modal.ts"
      to: "src/styles/snippet-manager.css"
      via: "rp-snippet-editor-* and rp-snippet-banner-* classes"
      pattern: "rp-snippet-editor-(modal|save-error|form-locked)|rp-snippet-banner-hidden"
---

<objective>
Migrate the 10 `obsidianmd/no-static-styles-assignment` violations + 1 unused-eslint-disable warning in `src/views/snippet-editor-modal.ts` per D-04 + D-13. The file's flagged sites split into:

- **Permanent classes:** line 146 (`maxWidth = '800px'`), line 200 (`color = 'var(--text-error)'`).
- **Banner show/hide pairs:** lines 199, 217, 218, 374, 375, 637 (display = 'none'/'').
- **Locked-state pair:** lines 518, 525 (`pointerEvents = 'none'` + `opacity = '0.5'` ↔ resets).
- **D-13 warning:** line 143 unused `// eslint-disable-next-line @typescript-eslint/no-explicit-any` directive — DELETE.

Plan 05 runs in Wave 4 (sequential after Plan 04 because both append to `snippet-manager.css`).

Purpose: ROADMAP SC#2 (no-static-styles cleanup) + ROADMAP SC#5 (warning resolution). Follows in-file precedent at `snippet-editor-modal.ts:350` (`folderUnsavedDotEl.toggleClass('is-visible', diff)`).
Output: edited `snippet-editor-modal.ts`, third Phase 77 block in `snippet-manager.css`, regenerated `styles.css`, line 143 directive removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/views/snippet-editor-modal.ts
@src/styles/snippet-manager.css

<interfaces>
<!-- 10 flagged style props per RESEARCH.md table:
     - 146: maxWidth = '800px'  (permanent → addClass)
     - 199, 217, 374, 637: display = 'none' or '' (banner show/hide)
     - 200: color = 'var(--text-error)' (permanent → addClass on save-error elem)
     - 218, 375: display = 'none' or '' (banner sibling toggles)
     - 518: pointerEvents = 'none' (locked state)
     - 525: opacity = '0.5' (locked state)

     Class names per D-05:
     - rp-snippet-editor-modal — permanent on the modal root, max-width: 800px
     - rp-snippet-editor-save-error — permanent on the save-error el, color: var(--text-error)
     - rp-snippet-banner-hidden — toggled to hide validation banner
     - rp-snippet-form-locked — toggled when form is read-only / locked

     EXISTING CSS RULES (verified 2026-04-30):
     - `is-visible` and `is-hidden` precedents from earlier phases EXIST in this file (line 350).
       Per D-05 «existing is-* precedents are kept where present (don't rename)». Plan 05 does
       NOT rename those — only adds new rp-* classes for new toggle sites.
     - Pre-existing rule `.radi-snippet-editor-validation-banner` at `src/styles/snippet-manager.css:491`
       (Phase 52 — uses LEGACY `radi-` prefix, not `rp-`). This is the validation-banner BASE rule
       providing background/color/padding/border-left. Plan 05 does NOT edit this rule and does NOT
       rename it. The new `.rp-snippet-banner-hidden` class is INDEPENDENT — it is added to the
       same banner element ALONGSIDE the legacy `.radi-snippet-editor-validation-banner` class to
       toggle the `display: none` state. Both classes coexist on the banner element.
       (WARNING 4 fix from previous revision iteration: earlier plan narrative misnamed the
       legacy selector as «.rp-snippet-banner» — it is actually «.radi-snippet-editor-validation-banner»
       per the file content at line 491.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read snippet-editor-modal.ts at flagged lines + map each to a strategy</name>
  <files>(read-only)</files>
  <read_first>
    - src/views/snippet-editor-modal.ts (read 140-220, 370-380, 510-530, 630-640 — regions of all 10 flagged lines + line 143 directive)
    - src/styles/snippet-manager.css (grep for existing `rp-snippet-banner`, `rp-snippet-editor-modal`, `rp-snippet-editor-save-error`, `rp-snippet-form-locked`, `radi-snippet-editor-validation-banner` to confirm no name collisions AND to confirm the legacy selector exists at line 491)
    - .planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md §5 «snippet-editor-modal.ts banner + locked state + max-width»
    - CLAUDE.md
  </read_first>
  <action>
    For each flagged line, identify:
    - Targeted DOM element.
    - Show↔hide pair vs permanent value.
    - State name per D-05 template `rp-{component}-{state}` (NOT `is-*`).

    Build mapping. Confirm:
    - Line 143 directive: read its context (line 144-150) to confirm no `any` violation in the suppressed block. If genuinely unused → DELETE in Task 2. If still suppressing a real `any` → STOP and reconcile with research.
    - For each new class name, verify it does NOT already exist in `src/styles/snippet-manager.css` via `grep -c "<classname>" src/styles/snippet-manager.css`.
    - Verify legacy banner selector — `grep -n "radi-snippet-editor-validation-banner" src/styles/snippet-manager.css` — should report ≥ 1 hit at line ~491. This confirms the legacy `radi-` prefix base rule exists and clarifies it is NOT to be edited.

    Mapping skeleton:
    ```
    Line 146: this.modal.modalEl.style.maxWidth = '800px' → permanent addClass('rp-snippet-editor-modal') at construction
    Line 199: <bannerEl>.style.display = 'none' → toggleClass('rp-snippet-banner-hidden', true) — call site at hide
              (the banner element ALREADY has class `radi-snippet-editor-validation-banner` from
              Phase 52 — plan 05 ADDS `rp-snippet-banner-hidden` toggle ALONGSIDE it; both coexist)
    Line 200: <bannerEl>.style.color = 'var(--text-error)' → permanent addClass('rp-snippet-editor-save-error') at element creation
    Line 217: <X>.style.display = '<value>' → toggleClass('<state>', <expr>)
    Line 218: <X>.style.display = '<value>' → toggleClass(...)
    Line 374: ...
    Line 375: ...
    Line 518: <formEl>.style.pointerEvents = 'none' → toggleClass('rp-snippet-form-locked', true)
    Line 525: <formEl>.style.opacity = '0.5' → already covered by rp-snippet-form-locked rule
    Line 637: <X>.style.display = '<value>' → toggleClass(...)
    ```
    Line 525 may merge with line 518 via a single `toggleClass('rp-snippet-form-locked', isLocked)` call site if both prop sets are applied to the same element together.

    No file output yet.
  </action>
  <verify>
    <automated>echo "Read-only; verification is the executor's mapping confirmation."</automated>
  </verify>
  <acceptance_criteria>
    - Mapping covers all 10 flagged lines + line 143 directive.
    - For each new class name, executor's mapping notes confirm `grep` over `src/styles/snippet-manager.css` shows no pre-existing rule (avoids collision with Phase 52 banner block etc.).
    - Mapping notes confirm `grep -n "radi-snippet-editor-validation-banner" src/styles/snippet-manager.css` returns ≥ 1 hit (legacy `radi-` prefix base rule confirmed; will NOT be edited).
    - Line 143 directive flagged as «remove» if context-confirmed unused, or «retain with rationale» if still suppressing a real `any` (in that case, raise as planning issue — D-13 narrative said it was unused).
  </acceptance_criteria>
  <done>
    Mapping ready; all 10 sites + the directive accounted for; legacy `.radi-snippet-editor-validation-banner` confirmed as untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply class migration + remove unused eslint-disable + append CSS rules</name>
  <files>
    src/views/snippet-editor-modal.ts
    src/styles/snippet-manager.css
  </files>
  <read_first>
    - (mapping from Task 1)
    - src/styles/snippet-manager.css — confirm Plans 03 and 04 Phase 77 blocks already at END; this plan adds a THIRD Phase 77 block AFTER them.
    - src/styles/snippet-manager.css:491 — re-confirm `.radi-snippet-editor-validation-banner` rule body BEFORE editing; this rule MUST be byte-for-byte unchanged after this plan's commit.
  </read_first>
  <action>
    **Edit 1 — `src/views/snippet-editor-modal.ts`:**
    Apply the mapping from Task 1:
    - Line 146 `style.maxWidth = '800px'` → REMOVE that line; ADD `this.modal.modalEl.addClass('rp-snippet-editor-modal')` at construction (early in `onOpen` or constructor — matching where line 146 currently sits).
    - Line 200 `style.color = 'var(--text-error)'` → REMOVE; ADD `<el>.addClass('rp-snippet-editor-save-error')` at the element-creation site.
    - Banner show/hide pairs (lines 199, 217, 374, 637 + 218, 375 partners) → REMOVE both partners, REPLACE with single `<el>.toggleClass('rp-snippet-banner-hidden', !visible)` at toggle site.
      - The banner element retains its existing class `radi-snippet-editor-validation-banner` (legacy `radi-` prefix from Phase 52 — supplied by `addClass` or `cls:` option elsewhere in the modal, NOT touched by Plan 05). The new `rp-snippet-banner-hidden` toggle ADDS a hide-state class alongside the legacy class; both coexist on the same DOM element.
    - Locked-state pair (lines 518, 525) → REMOVE both, REPLACE with single `<el>.toggleClass('rp-snippet-form-locked', isLocked)` at the toggle site.
    - **Line 143 directive: DELETE the entire `// eslint-disable-next-line @typescript-eslint/no-explicit-any` line.** This is the D-13 unused-disable warning fix (1 of the 6 warnings).

    Per CLAUDE.md «only modify code relevant to current phase» — bound the diff to lines 140-220, 370-380, 510-530, 630-640, plus line 143 deletion. Do NOT refactor any other section of this 600+ LOC file.

    **Edit 2 — `src/styles/snippet-manager.css`:**
    Append at the END (third Phase 77 block in this file):
    ```css

    /* Phase 77: snippet-editor-modal — modal max-width, save-error color, banner toggle, form-locked state (LINT-01).
       The banner element retains its Phase 52 base rule `.radi-snippet-editor-validation-banner` at line 491
       (legacy `radi-` prefix); the new `.rp-snippet-banner-hidden` class is INDEPENDENT — both coexist on
       the banner element to toggle the display:none state without touching the legacy rule. */
    .rp-snippet-editor-modal {
      max-width: 800px;
    }
    .rp-snippet-editor-save-error {
      color: var(--text-error);
    }
    .rp-snippet-banner-hidden {
      display: none;
    }
    .rp-snippet-form-locked {
      pointer-events: none;
      opacity: 0.5;
    }
    ```
    Adapt rule list to match exact classes Task 1 introduced. NEVER edit existing rules above (especially the Phase 52 `.radi-snippet-editor-validation-banner` rule near line 491).

    **Edit 3 — `npm run build`** regenerates `styles.css`.

    **Commit message:**
    ```
    refactor(snippet-editor-modal): replace 10 inline styles with rp-snippet-* classes; remove unused eslint-disable

    Migrates obsidianmd/no-static-styles-assignment violations at lines
    146, 199, 200, 217, 218, 374, 375, 518, 525, 637 per D-04/D-05:
    - rp-snippet-editor-modal — permanent max-width: 800px on modalEl
    - rp-snippet-editor-save-error — permanent color: var(--text-error)
      on the save-error message element
    - rp-snippet-banner-hidden — toggle for validation banner show/hide.
      Banner element retains legacy `.radi-snippet-editor-validation-banner`
      base rule from Phase 52 (snippet-manager.css:491, `radi-` prefix);
      new `.rp-snippet-banner-hidden` class coexists ALONGSIDE the legacy
      class on the same DOM element — Phase 52 base rule UNCHANGED.
    - rp-snippet-form-locked — combined pointer-events:none + opacity:0.5
      for read-only/locked form state

    D-13 warning fix: removes unused
    `// eslint-disable-next-line @typescript-eslint/no-explicit-any`
    directive at line 143 — no `any` violation in the block below.
    1 of the 6 warnings cleared.

    CSS rules appended to src/styles/snippet-manager.css per D-07 with
    /* Phase 77 */ header. Phase 52 .radi-snippet-editor-validation-banner
    base rule UNCHANGED per CLAUDE.md append-only contract.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/views/snippet-editor-modal.ts 2>&1 | grep -cE "no-static-styles-assignment|eslint-disable"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `grep -cE "style\\.maxWidth\\s*=\\s*'800px'" src/views/snippet-editor-modal.ts` returns 0.
    - `grep -cE "style\\.color\\s*=\\s*'var\\(--text-error\\)'" src/views/snippet-editor-modal.ts` returns 0.
    - `grep -cE "style\\.pointerEvents\\s*=\\s*'none'" src/views/snippet-editor-modal.ts` returns 0.
    - `grep -cE "style\\.opacity\\s*=\\s*'0.5'" src/views/snippet-editor-modal.ts` returns 0.
    - `grep -cE "rp-snippet-editor-modal" src/views/snippet-editor-modal.ts` returns ≥ 1.
    - `grep -cE "rp-snippet-banner-hidden" src/views/snippet-editor-modal.ts` returns ≥ 1.
    - `grep -cE "rp-snippet-form-locked" src/views/snippet-editor-modal.ts` returns ≥ 1.
    - `sed -n '143p' src/views/snippet-editor-modal.ts | grep -c "eslint-disable-next-line @typescript-eslint/no-explicit-any"` returns 0 (line 143's content shifted; the disable directive is GONE).
    - `grep -c "rp-snippet-editor-modal" src/styles/snippet-manager.css` returns ≥ 1.
    - `grep -c "rp-snippet-form-locked" src/styles/snippet-manager.css` returns ≥ 1.
    - `grep -c "/\\* Phase 77:" src/styles/snippet-manager.css` returns ≥ 3 (Plan 03 + Plan 04 + this Plan 05's blocks).
    - `grep -c "radi-snippet-editor-validation-banner" src/styles/snippet-manager.css` returns ≥ 1 (Phase 52 BASE RULE INTACT — the actual selector with `radi-` prefix, NOT `rp-`).
    - `git diff src/styles/snippet-manager.css` shows ZERO modifications inside the `.radi-snippet-editor-validation-banner` block at line 491 (only ADDITIONS at the end of the file).
    - `npx eslint src/views/snippet-editor-modal.ts 2>&1 | grep -c "no-static-styles-assignment"` returns 0.
    - `npx eslint src/views/snippet-editor-modal.ts 2>&1 | grep -c "eslint-disable.*\\bunused"` returns 0 (warning cleared).
    - `npm run build` exits 0; `grep -c "rp-snippet-editor-modal" styles.css` returns ≥ 1.
    - `npm test -- src/__tests__/views/snippet-editor-modal*.test.ts` exits 0 (existing banner tests pass).
    - `git diff src/styles/snippet-manager.css` shows ONLY additions (zero `-` lines).
  </acceptance_criteria>
  <done>
    10 static-styles violations + 1 unused-disable warning cleared in `snippet-editor-modal.ts`; CSS rules appended; build + tests green; Phase 52 `.radi-snippet-editor-validation-banner` base rule preserved.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/views/snippet-editor-modal.ts` reports 0 `obsidianmd/no-static-styles-assignment` errors AND 0 unused `eslint-disable` warnings.
2. `npm run lint` total count strictly decreases by 11 (10 errors + 1 warning).
3. `npm run build` exits 0; `styles.css` includes the new rules.
4. `npm test` exits 0.
5. CSS file contains 3 separate Phase 77 blocks (Plans 03/04/05); Phase 52 `.radi-snippet-editor-validation-banner` rule (legacy `radi-` prefix at line 491) untouched.
</verification>

<success_criteria>
- 10 static-styles errors + 1 warning cleared.
- D-05 naming `rp-snippet-editor-{state}` and `rp-snippet-banner-hidden`, `rp-snippet-form-locked` followed.
- Existing `is-visible` precedent at line 350 left untouched.
- Phase 52 `.radi-snippet-editor-validation-banner` base rule (legacy `radi-` prefix) untouched.
- New `.rp-snippet-banner-hidden` class coexists with the legacy `.radi-snippet-editor-validation-banner` class on the banner element.
- CSS append-only contract held.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-05-SUMMARY.md` with:
- Mapping (10 lines + line 143 directive removal).
- Confirmation Phase 52 BASE RULE untouched — name the actual selector verbatim: `.radi-snippet-editor-validation-banner` (legacy `radi-` prefix), NOT `.rp-snippet-banner`. `grep -c` evidence + `git diff` block-evidence around line 491.
- New Phase 77 CSS block contents.
- `npx eslint <file>` before/after counts.
</output>
</content>
</invoke>