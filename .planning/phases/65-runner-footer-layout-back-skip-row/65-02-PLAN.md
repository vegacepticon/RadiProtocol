---
phase: 65-runner-footer-layout-back-skip-row
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - src/views/runner-view.ts
  - src/views/inline-runner-modal.ts
  - src/styles/runner-view.css
  - styles.css
autonomous: false
requirements:
  - RUNNER-02
must_haves:
  truths:
    - "The primary undo button reads `back` in sidebar, tab, and inline runner modes."
    - "Skip is a labeled `skip` button in the footer controls row immediately to the right of Back when both controls are present."
    - "Mixed answer+snippet questions render all answer buttons first, all snippet buttons second, and footer controls last."
    - "The footer controls row remains usable at narrow widths via flex wrapping."
  artifacts:
    - path: "src/views/runner-view.ts"
      provides: "Sidebar/tab RunnerView footer-control rendering and handlers"
      contains: "rp-runner-footer-actions"
    - path: "src/views/inline-runner-modal.ts"
      provides: "Inline runner footer-control rendering and handlers"
      contains: "rp-runner-footer-actions"
    - path: "src/styles/runner-view.css"
      provides: "Append-only Phase 65 footer row styles"
      contains: "Phase 65: Runner footer Back/Skip row"
    - path: "styles.css"
      provides: "Generated CSS bundle after `npm run build`"
      contains: "rp-runner-footer-actions"
  key_links:
    - from: "src/views/runner-view.ts"
      to: "src/styles/runner-view.css"
      via: "footer class consumed by CSS"
      pattern: "rp-runner-footer-actions"
    - from: "src/views/inline-runner-modal.ts"
      to: "src/styles/runner-view.css"
      via: "shared runner classes in inline mode"
      pattern: "rp-runner-footer-actions"
    - from: "src/views/runner-view.ts"
      to: "src/runner/protocol-runner.ts"
      via: "existing skip/stepBack calls preserved"
      pattern: "runner\.(skip|stepBack)"
---

<objective>
Implement the RUNNER-02 Back/Skip footer layout in RunnerView and InlineRunnerModal, then validate with automated tests/build and a human visual checkpoint.

Purpose: Remove the Phase 53 icon-only Skip placement regression and make runner controls predictable across sidebar, tab, and inline modes without changing runner state-machine behavior.
Output: Updated runner render code, append-only runner CSS, regenerated `styles.css`, and passing RUNNER-02 tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-runner-footer-layout-back-skip-row/65-01-SUMMARY.md
@src/views/runner-view.ts
@src/views/inline-runner-modal.ts
@src/styles/runner-view.css
@src/__tests__/views/runner-footer-layout.test.ts

<interfaces>
Relevant existing contracts:

From `src/views/runner-view.ts`:
```typescript
this.capturePendingTextareaScroll();
this.runner.syncManualEdit(this.previewTextarea?.value ?? '');
this.runner.skip();
void this.autoSaveSession();
void this.renderAsync();

this.runner.stepBack();
void this.autoSaveSession();
this.render();
```

From `src/views/inline-runner-modal.ts`:
```typescript
this.runner.skip();
this.render();

this.runner.stepBack();
this.render();
```

CSS rules are append-only per CLAUDE.md. Edit only `src/styles/runner-view.css`, add a Phase 65 section at the bottom, and run `npm run build` so generated `styles.css` is updated by esbuild.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor RunnerView Back/Skip into footer controls</name>
  <files>src/views/runner-view.ts, src/__tests__/views/runner-footer-layout.test.ts</files>
  <behavior>
    - Back button visible text is exactly `back` everywhere RunnerView currently renders step-back: at-node question, awaiting-loop-pick, and awaiting-snippet-pick.
    - Skip button visible text is exactly `skip` and is created only inside `.rp-runner-footer-actions` after answer and snippet branch lists have been rendered.
    - Mixed branch ordering is: `.rp-answer-list` → `.rp-snippet-branch-list` → `.rp-runner-footer-actions`.
    - Skip click behavior preserves the existing RUNFIX-02/BUG-01/SESSION-01 prologue and does not alter `ProtocolRunner.skip()` semantics.
  </behavior>
  <action>In `src/views/runner-view.ts`, add a private footer-rendering helper that creates `questionZone.createDiv({ cls: 'rp-runner-footer-actions' })` only when Back and/or Skip should be rendered. The helper must append Back first (`cls: 'rp-step-back-btn'`, `text: 'back'`) and Skip second (`cls: 'rp-skip-btn'`, `text: 'skip'`). Replace the current inline Skip creation inside the question branch with a footer call placed after the snippet branch block; replace each `text: 'Step back'` with the helper. Do not call `setIcon()` for Skip in Phase 65; RUNNER-02 requires a labeled button, not icon-only. Do not modify `ProtocolRunner`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts</automated>
  </verify>
  <done>RunnerView passes its RUNNER-02 tests: Back text is `back`, Skip text is `skip`, and Skip is not between answer/snippet branch groups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same footer contract to InlineRunnerModal</name>
  <files>src/views/inline-runner-modal.ts, src/__tests__/views/runner-footer-layout.test.ts</files>
  <behavior>
    - Inline mixed answer+snippet questions render a `.rp-runner-footer-actions` row after branch lists.
    - Inline Back text is `back` in at-node, loop picker, and snippet picker contexts.
    - Inline Skip text is `skip`, not an icon-only control.
  </behavior>
  <action>In `src/views/inline-runner-modal.ts`, mirror the RunnerView footer helper locally using normal `addEventListener` handlers. Move the existing inline Skip creation out of the question branch body and into the footer call after snippet branches. Replace all inline `text: 'Step back'` step-back buttons in the Phase 65 scope with footer-rendered `back` buttons. Preserve existing inline handler behavior (`runner.skip(); render();` and `runner.stepBack(); render();`) exactly; do not add sidebar-only session autosave calls to inline mode.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts</automated>
  </verify>
  <done>InlineRunnerModal satisfies the same RUNNER-02 footer text/order contract as RunnerView.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Style, build, and visually verify the footer row</name>
  <files>src/styles/runner-view.css, styles.css</files>
  <action>Append a `/* Phase 65: Runner footer Back/Skip row */` section to `src/styles/runner-view.css`. Style `.rp-runner-footer-actions` as a horizontal flex row with gap, top margin, and `flex-wrap: wrap`; keep buttons visible at narrow widths. Adjust `.rp-step-back-btn` and `.rp-skip-btn` only in the Phase 65 section as needed so both are labeled neutral controls and neither is clipped. Run `npm run build` to regenerate `styles.css`; never hand-edit `styles.css`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts && npm test && npm run build</automated>
  </verify>
  <done>Focused tests, full test suite, and production build pass; generated `styles.css` contains the Phase 65 footer styles.</done>
  <what-built>Runner footer Back+Skip row in sidebar/tab RunnerView and InlineRunnerModal.</what-built>
  <how-to-verify>
    1. Open Obsidian with the built plugin and run a protocol containing a question with both answer branches and snippet branches.
    2. Confirm answer buttons render first and snippet branch buttons render next.
    3. Confirm the footer row below those branches contains `back` and `skip` in that order; Skip must not appear between answer and snippet branches.
    4. Repeat in the normal runner view, a tab-hosted runner view if available, and the inline runner command.
    5. Narrow the sidebar/inline modal and confirm Back+Skip wrap instead of clipping off-screen.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/layout issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → runner state machine | Button clicks mutate local protocol state through existing `skip()` and `stepBack()` APIs. |
| renderer → DOM/CSS | Render methods create Obsidian DOM elements styled by generated CSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-02-01 | Tampering | `RunnerView` / `InlineRunnerModal` click handlers | mitigate | Preserve existing skip and stepBack handler bodies exactly except for DOM placement/text; focused tests assert click calls. |
| T-65-02-02 | Denial of Service | narrow runner widths | mitigate | Use flex-wrap in `.rp-runner-footer-actions` so controls remain reachable rather than clipped. |
| T-65-02-03 | Spoofing | icon-only Skip ambiguity | mitigate | Replace icon-only Skip with visible text `skip` and preserve aria/title labels where useful. |
| T-65-02-04 | Information Disclosure | generated CSS/build | accept | No secrets or external data; local UI-only change. |
</threat_model>

<verification>
Overall phase verification:
- `npx vitest run src/__tests__/views/runner-footer-layout.test.ts`
- `npm test`
- `npm run build`
- Human visual verification from the checkpoint confirms sidebar/tab/inline footer layout and narrow-width wrapping.
</verification>

<success_criteria>
- All RUNNER-02 source-audit items are implemented.
- `Step back` no longer appears in RunnerView or InlineRunnerModal user-facing button text for the runner footer controls.
- Skip no longer uses `setIcon(skipBtn, 'skip-forward')` as an icon-only button in RunnerView or InlineRunnerModal.
- Mixed answer+snippet questions render footer controls after both branch groups.
- `styles.css` is regenerated by `npm run build`.
</success_criteria>

<output>
After completion, create `.planning/phases/65-runner-footer-layout-back-skip-row/65-02-SUMMARY.md`.
</output>
