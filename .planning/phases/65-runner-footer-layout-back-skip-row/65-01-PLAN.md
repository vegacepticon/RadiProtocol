---
phase: 65-runner-footer-layout-back-skip-row
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/views/runner-footer-layout.test.ts
autonomous: true
requirements:
  - RUNNER-02
must_haves:
  truths:
    - "Runner users see a labeled back control, not 'Step back', whenever step-back is available."
    - "Runner users see a labeled skip control in the footer controls row, not between answer and snippet branches."
    - "Sidebar/tab RunnerView and InlineRunnerModal share the same Back+Skip ordering contract."
  artifacts:
    - path: "src/__tests__/views/runner-footer-layout.test.ts"
      provides: "Regression coverage for Back/Skip footer ordering and labels across RunnerView + InlineRunnerModal"
      contains: "RUNNER-02"
  key_links:
    - from: "src/__tests__/views/runner-footer-layout.test.ts"
      to: "src/views/runner-view.ts"
      via: "RunnerView render harness"
      pattern: "rp-runner-footer-actions"
    - from: "src/__tests__/views/runner-footer-layout.test.ts"
      to: "src/views/inline-runner-modal.ts"
      via: "InlineRunnerModal render harness or source-backed regression guard"
      pattern: "rp-runner-footer-actions"
---

<objective>
Create RED regression tests for the Phase 65 Runner footer layout contract.

Purpose: Lock RUNNER-02 before implementation so the executor cannot satisfy the feature by only changing button text while leaving Skip between mixed answer/snippet branches.
Output: A focused Vitest file that fails on the current implementation and passes only when Back and Skip render as labeled footer controls in sidebar/tab and inline modes.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/views/runner-view.ts
@src/views/inline-runner-modal.ts
@src/styles/runner-view.css
@src/__tests__/views/runner-snippet-sibling-button.test.ts

<interfaces>
Existing render classes and methods the tests must target:

From `src/views/runner-view.ts`:
```typescript
export class RunnerView extends ItemView {
  private render(): void;
  private renderSnippetPicker(state, questionZone): Promise<void>;
}
```

Current failing implementation details to assert against:
- `RunnerView` creates `.rp-skip-btn` in the question branch before `.rp-snippet-branch-list`.
- `RunnerView` and `InlineRunnerModal` currently use button text `Step back`.
- `InlineRunnerModal` currently calls `setIcon(skipBtn, 'skip-forward')` and gives Skip no visible text.

Use the fake DOM/harness style already present in `src/__tests__/views/runner-snippet-sibling-button.test.ts`; do not introduce jsdom.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RunnerView footer layout RED tests</name>
  <files>src/__tests__/views/runner-footer-layout.test.ts</files>
  <behavior>
    - Test 1: At an at-node question with answer neighbors, snippet neighbors, and `canStepBack: true`, `RunnerView.render()` creates one `.rp-runner-footer-actions` after `.rp-answer-list` and `.rp-snippet-branch-list` in DOM order.
    - Test 2: The footer row contains a `.rp-step-back-btn` whose visible text is exactly `back` and a `.rp-skip-btn` whose visible text is exactly `skip`, in that order.
    - Test 3: No `.rp-skip-btn` is inserted between `.rp-answer-list` and `.rp-snippet-branch-list`; Skip only appears inside `.rp-runner-footer-actions`.
    - Test 4: Clicking footer Skip preserves the Phase 53/56 click prologue: capturePendingTextareaScroll, syncManualEdit, skip, auto-save/render; clicking footer Back calls stepBack and render/save as before.
  </behavior>
  <action>Create `src/__tests__/views/runner-footer-layout.test.ts` using explicit Vitest imports and `vi.mock('obsidian')`. Reuse the FakeNode, `findByClass`, graph factory, and RunnerView private-method harness pattern from `runner-snippet-sibling-button.test.ts`, but keep this file focused on RUNNER-02. The tests must be written so they fail against the current code because the Skip button is icon-only and rendered before snippet branches while Back reads `Step back`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts</automated>
  </verify>
  <done>The new RunnerView tests fail RED on the current implementation for label/order/footer-class reasons, not because of harness errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add InlineRunnerModal footer layout RED tests</name>
  <files>src/__tests__/views/runner-footer-layout.test.ts</files>
  <behavior>
    - Test 5: Inline at-node mixed answer+snippet render places Skip in `.rp-runner-footer-actions` after Back, with visible text `skip`.
    - Test 6: Inline loop picker and snippet picker step-back controls use visible text `back`, not `Step back`.
  </behavior>
  <action>Extend the same test file to cover `src/views/inline-runner-modal.ts`. Prefer a lightweight render harness with fake DOM methods if existing inline modal tests expose a reusable pattern; otherwise use a source-backed regression guard that reads `inline-runner-modal.ts` and asserts the implementation contains a footer helper/class and does not contain `text: 'Step back'` or `setIcon(skipBtn, 'skip-forward')` in the Skip path. Keep the assertions tied to RUNNER-02 acceptance criteria, not broad snapshots.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts</automated>
  </verify>
  <done>The test file covers both RunnerView and InlineRunnerModal and fails RED against the current implementation for the intended RUNNER-02 gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → private render methods | Tests exercise private view rendering through controlled fakes; no user data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | Tampering | `src/__tests__/views/runner-footer-layout.test.ts` | mitigate | Assert explicit DOM class/order/text contracts so future changes cannot silently reinsert Skip between mixed branch lists. |
| T-65-01-02 | Repudiation | RUNNER-02 verification | mitigate | Include `RUNNER-02` in describe/test names and summary so the requirement has traceable automated evidence. |
| T-65-01-03 | Information Disclosure | test fixtures | accept | Tests use synthetic labels only and read source files locally; no vault/user protocol content is exposed. |
</threat_model>

<verification>
Run `npx vitest run src/__tests__/views/runner-footer-layout.test.ts` and confirm failures are expectation failures tied to the current Back/Skip labels/order, not TypeScript or harness setup failures.
</verification>

<success_criteria>
- `src/__tests__/views/runner-footer-layout.test.ts` exists.
- The test file asserts RUNNER-02 for mixed answer+snippet questions, visible `back`, visible `skip`, footer row order, and inline parity.
- The focused test command reaches RED for the current implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/65-runner-footer-layout-back-skip-row/65-01-SUMMARY.md`.
</output>
