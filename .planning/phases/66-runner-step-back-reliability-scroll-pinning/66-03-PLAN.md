---
phase: 66-runner-step-back-reliability-scroll-pinning
plan: 03
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified:
  - src/views/runner-view.ts
  - src/views/inline-runner-modal.ts
  - src/__tests__/views/runner-footer-layout.test.ts
autonomous: true
requirements:
  - RUNNER-03
must_haves:
  truths:
    - "After a Back click in RunnerView, the Back button's `disabled` attribute is set to true synchronously, before the runner state machine is invoked."
    - "After a Back click in InlineRunnerModal, the Back button's `disabled` attribute is set to true synchronously, before the runner state machine is invoked."
    - "A double-click on Back invokes ProtocolRunner.stepBack() exactly once visually (the second invocation is no-op'd by the in-flight guard from Plan 01) and sets disabled=true visually after the first click."
    - "renderRunnerFooter in both RunnerView and InlineRunnerModal accepts a `guarded` parameter (or has the guard built in for Back) so the click-binding for Back has a single shared shape."
  artifacts:
    - path: "src/views/runner-view.ts"
      provides: "renderRunnerFooter with built-in Back-click disable + onBack delegation"
      contains: "backBtn.disabled = true"
    - path: "src/views/inline-runner-modal.ts"
      provides: "renderRunnerFooter with built-in Back-click disable + onBack delegation"
      contains: "backBtn.disabled = true"
    - path: "src/__tests__/views/runner-footer-layout.test.ts"
      provides: "Tests asserting Back disable-on-click behavior in sidebar/tab/inline"
      contains: "disabled"
  key_links:
    - from: "src/views/runner-view.ts renderRunnerFooter"
      to: "src/runner/protocol-runner.ts ProtocolRunner.stepBack"
      via: "onBack callback wraps stepBack with disable-first"
      pattern: "backBtn\\.disabled\\s*=\\s*true"
    - from: "src/views/inline-runner-modal.ts renderRunnerFooter"
      to: "src/runner/protocol-runner.ts ProtocolRunner.stepBack"
      via: "onBack callback wraps stepBack with disable-first"
      pattern: "backBtn\\.disabled\\s*=\\s*true"
---

<objective>
Add the visual half of the double-click guard (D-01 + D-02 + D-03): when the user clicks Back in RunnerView OR InlineRunnerModal, the Back button is `disabled = true` immediately, before any runner mutation. The runner state machine half of the guard already shipped in Plan 01 (`_stepBackInFlight`); together they make Back a single-click guarantee in every state of every runner mode (RUNNER-03 SC1, SC2).

Purpose: D-03 mandates that the Back click-binding lives in a shared helper so RunnerView and InlineRunnerModal cannot drift again. Phase 65 already shipped a `renderRunnerFooter` helper in BOTH files (with the same signature). Phase 66 D-03 extends that helper to wrap `onBack` with a "disable button → invoke callback" prologue. No new CSS (D-02 — standard HTML `button.disabled` attribute is sufficient).

Output:
- `RunnerView.renderRunnerFooter` (currently :737-768) wraps the Back click handler so `backBtn.disabled = true` runs BEFORE `options.onBack()` is invoked.
- `InlineRunnerModal.renderRunnerFooter` (currently :507-537) does the same.
- Existing call-sites of `renderRunnerFooter` (sidebar/tab :579, :671, :856 in `runner-view.ts`; inline :397, :463, :962 in `inline-runner-modal.ts`) require NO changes — the helper does the work internally.
- New tests in `runner-footer-layout.test.ts` assert the Back button gets `disabled = true` after a click in sidebar and inline harnesses.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-CONTEXT.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-01-SUMMARY.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-02-SUMMARY.md
@.planning/phases/65-runner-footer-layout-back-skip-row/65-CONTEXT.md
@CLAUDE.md

@src/views/runner-view.ts
@src/views/inline-runner-modal.ts
@src/__tests__/views/runner-footer-layout.test.ts

<interfaces>
Existing helper signatures that this plan extends in-place. Both files have an identical signature today:

```typescript
private renderRunnerFooter(
  zone: HTMLElement,
  options: {
    showBack: boolean;
    onBack: () => void;
    showSkip?: boolean;
    onSkip?: () => void;
  },
): void { ... }
```

After this plan the public signature is UNCHANGED — the disable-on-click behaviour is built into the Back branch internally, so all current call-sites continue to work. We deliberately avoid adding a new `guarded?: boolean` parameter (D-03 explicitly allows the planner to pick between "extend signature" and "wrap onBack internally"; we pick the simpler internal wrap because it covers every existing call-site without modification and matches the "Skip / Answer / Snippet-branch / Loop-branch is out of scope" boundary in D-04).

The Phase 65 footer helper currently uses:
- RunnerView: `this.registerDomEvent(backBtn, 'click', options.onBack);` (currently at line 757; line 757 is approximate — match by call signature, not line number)
- InlineRunnerModal: `backBtn.addEventListener('click', options.onBack);` (line ~526)

Both must change to invoke a small wrapper that sets `backBtn.disabled = true` before calling `options.onBack()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap Back click in renderRunnerFooter (RunnerView + InlineRunnerModal) with disable-first prologue</name>
  <files>src/views/runner-view.ts, src/views/inline-runner-modal.ts, src/__tests__/views/runner-footer-layout.test.ts</files>
  <read_first>
    - src/views/runner-view.ts (renderRunnerFooter at :737-768, call-sites at :579, :671, :856)
    - src/views/inline-runner-modal.ts (renderRunnerFooter at :507-537, call-sites at :397, :463, :962)
    - src/__tests__/views/runner-footer-layout.test.ts (existing footer tests; particularly the Back-click handler invocation tests around :336)
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/65-runner-footer-layout-back-skip-row/65-CONTEXT.md (D-12, D-13 — original shared helper rationale)
  </read_first>
  <behavior>
    Concrete TDD checkpoints to drive Task 1:
    - Test F (RunnerView Back disable-on-click): Render RunnerView in `at-node` with `canStepBack: true`. Locate the Back button via `findByClass(footerRow, 'rp-step-back-btn')[0]`. Synchronously fire two clicks (`btn._clickHandler?.(); btn._clickHandler?.();`). Assert `btn.disabled === true` after the first click. Assert `runnerStepBackSpy` was called at most twice (the runner-side `_stepBackInFlight` from Plan 01 will collapse the second to a no-op). The visible button-disabled assertion is the proof that the view-side guard (this plan) is in place — independently of the runner-side guard.
    - Test G (InlineRunnerModal Back disable-on-click): Same shape as F but on inline modal. Fire two clicks; assert `disabled === true`.
    - Test H (Back disable does NOT propagate to Skip): On a question with both Back and Skip in the footer, click Back. Assert Back is `disabled = true` AND Skip is `disabled = false`. Conversely, clicking Skip must NOT disable Back. This proves D-04 (guard scope is Back only).
  </behavior>
  <action>
    1. In `src/views/runner-view.ts` `renderRunnerFooter` (lines :737-768), locate the `registerDomEvent(backBtn, 'click', options.onBack)` call (currently at line 757; line 757 is approximate — match by call signature, not line number) and replace the `this.registerDomEvent(backBtn, 'click', options.onBack);` line with:
    ```typescript
    // Phase 66 D-01 + D-02 + D-03: visual half of the double-click guard.
    // Disable Back synchronously on first click; the runner-side `_stepBackInFlight`
    // (Plan 01) silences any second invocation that slipped through. No new CSS — D-02.
    this.registerDomEvent(backBtn, 'click', () => {
      backBtn.disabled = true;
      options.onBack();
    });
    ```
    Do NOT touch the Skip branch (D-04). Do NOT add a new `guarded?: boolean` parameter — the wrap is unconditional for Back (this matches D-03 ergonomics; all current callers want the guard).

    2. In `src/views/inline-runner-modal.ts` `renderRunnerFooter` (lines :507-537), make the symmetric change. Replace `backBtn.addEventListener('click', options.onBack);` with:
    ```typescript
    // Phase 66 D-01 + D-02 + D-03: visual half of the double-click guard.
    backBtn.addEventListener('click', () => {
      backBtn.disabled = true;
      options.onBack();
    });
    ```
    The asymmetry between `registerDomEvent` (RunnerView, an Obsidian ItemView with lifecycle-managed event registration) and `addEventListener` (InlineRunnerModal, a plain Modal subclass) is preserved — both helpers were already using their respective binding mechanisms in Phase 65.

    3. Per CLAUDE.md never-remove-rule: do NOT touch any other line in these helpers (Skip branch, aria-labels, titles, the early `return` at the top, the `rp-runner-footer-row` div creation, any other render path, any other event handler). Only the single Back click-binding line in each file changes.

    4. In `src/__tests__/views/runner-footer-layout.test.ts`, append three new tests inside the existing top-level `describe(...)` block (or a new sibling describe `Phase 66 D-01/D-02/D-03 — Back disable-on-click guard`):
       - Test F: RunnerView at-node + canStepBack=true. Render. Find Back button. Fire `_clickHandler?.()` twice synchronously. Assert `backBtn.disabled === true` after the first click. Use the existing harness `makeView(...)` pattern and the fake-DOM helpers already in this file (`findByClass`, `hasClass`).
       - Test G: InlineRunnerModal at-node + canStepBack=true. Use the existing harness (or extend it) to render the inline modal's footer row. Find Back button. Fire two clicks. Assert `backBtn.disabled === true`.
       - Test H: question state with both Back and Skip rendered. Click Back; assert `backBtn.disabled === true` AND `skipBtn.disabled === false`. Reset (re-render). Click Skip; assert `skipBtn.disabled === false` (Skip click does NOT disable Skip — Plan 03 only guards Back, D-04) and `backBtn.disabled === false`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/views/runner-footer-layout.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/views/runner-view.ts` `renderRunnerFooter` body contains the literal substring `backBtn.disabled = true` (note: the existing file does NOT have this string; verify before/after).
    - `src/views/inline-runner-modal.ts` `renderRunnerFooter` body contains the literal substring `backBtn.disabled = true`.
    - `src/views/runner-view.ts` `renderRunnerFooter` Skip branch does NOT contain `disabled = true` (Skip is not guarded — D-04). `grep -A 10 "options.showSkip === true" src/views/runner-view.ts | grep -c "disabled = true"` returns 0.
    - `src/views/inline-runner-modal.ts` Skip branch does NOT contain `disabled = true`.
    - `src/__tests__/views/runner-footer-layout.test.ts` contains at least three new `it(` blocks whose names contain `Phase 66` AND one of {`Back disable`, `disabled`, `D-01`, `D-02`, `D-03`}.
    - `npx vitest run src/__tests__/views/runner-footer-layout.test.ts` exits 0 with the three new tests passing.
    - `npm test` exits 0 (full suite — sanity check; the click-binding changes must not break any existing test).
  </acceptance_criteria>
  <done>Both runner modes share the same Back-disable-on-click prologue inside their `renderRunnerFooter` helper; the asymmetric event-registration mechanisms (`registerDomEvent` vs `addEventListener`) are preserved; new tests exercise the disable-on-click contract and the D-04 boundary (Skip not affected); full suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build sanity check</name>
  <files>styles.css</files>
  <read_first>
    - CLAUDE.md (CSS architecture — esbuild concatenates `src/styles/*.css`; `npm run build` regenerates `styles.css`; never hand-edit `styles.css`)
  </read_first>
  <action>
    Per D-02, Plan 03 introduces NO new CSS. Per CLAUDE.md, source CSS lives in `src/styles/*.css` and `styles.css` at root is generated. After the TypeScript edits in Task 1, run `npm run build` to confirm the project compiles cleanly and the generated bundle is consistent. If `styles.css` is regenerated identically (because no CSS source files changed), git will show no diff for `styles.css` — that is correct.

    Do NOT add any new entry to `src/styles/` for Phase 66. Do NOT hand-edit `styles.css`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `git status --porcelain src/styles/` shows no new files.
    - If `git diff --stat styles.css` is non-empty, the diff is exclusively whitespace/ordering noise consistent with esbuild's concatenation, NOT new selectors. Inspect with `git diff styles.css | head -50` and confirm.
  </acceptance_criteria>
  <done>Production build is clean; no Phase 66 CSS sources were introduced; `styles.css` shows no content diff caused by Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click event → renderRunnerFooter wrapper | The wrapper is the new visual gate; it must not be bypassable from a re-render that creates a new button without the wrapper. |
| RunnerView vs InlineRunnerModal helper drift | Two parallel implementations of `renderRunnerFooter` introduce a drift risk; the only mitigation is that BOTH must be edited together in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03-01 | Tampering | Back button click handler | mitigate | Disable-first prologue runs synchronously inside the same click event before `options.onBack()` is invoked; the runner-side `_stepBackInFlight` (Plan 01) is the second layer. |
| T-66-03-02 | Tampering | helper drift between view/modal | mitigate | Two-file edit in the same task; acceptance criteria grep both files for `backBtn.disabled = true`. |
| T-66-03-03 | Spoofing | Skip button incorrectly disabled | mitigate | Test H asserts D-04 boundary explicitly — clicking Back disables Back only; Skip remains enabled. |
| T-66-03-04 | Information Disclosure | re-enabling Back across renders | accept | Each render creates a new `backBtn` element (existing helper contract); the previous disabled button is replaced. No state leak. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/views/runner-footer-layout.test.ts` exits 0.
- `npm test` exits 0 (full suite).
- `npm run build` exits 0.
- `grep -c "backBtn.disabled = true" src/views/runner-view.ts` returns ≥1.
- `grep -c "backBtn.disabled = true" src/views/inline-runner-modal.ts` returns ≥1.
</verification>

<success_criteria>
- D-01: View-side half of the double-click guard is in place in both runner modes.
- D-02: Standard HTML `button.disabled` attribute is the only mechanism — no new CSS.
- D-03: Both helpers (RunnerView + InlineRunnerModal) wrap the Back click identically; signatures unchanged so existing call-sites still work.
- D-04: Skip / answer / snippet-branch / loop-branch click handlers are NOT modified.
- Three new tests (F, G, H) exercise the contract and the D-04 boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-03-SUMMARY.md`.
</output>
</content>
</invoke>