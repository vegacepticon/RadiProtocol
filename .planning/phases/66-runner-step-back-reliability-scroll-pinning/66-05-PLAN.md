---
phase: 66-runner-step-back-reliability-scroll-pinning
plan: 05
type: execute
wave: 4
depends_on:
  - 66-02
  - 66-03
  - 66-04
files_modified:
  - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md
autonomous: false
requirements:
  - RUNNER-04
  - RUNNER-03
must_haves:
  truths:
    - "66-UAT.md exists and contains a Manual-Only Verifications section with at least three scripted scenarios for RUNNER-04 (file-bound snippet insert pinning to bottom; Back pinning to bottom; tab-mode parity)."
    - "66-UAT.md contains explicit RUNNER-03 visual checks for the double-click guard (Back disables on click) in sidebar and inline modes."
    - "Each scenario specifies the canvas to load, the action sequence, and the expected visual outcome."
    - "The user has visually run all scenarios in a real Obsidian vault and approved or returned with discrepancies."
  artifacts:
    - path: ".planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md"
      provides: "Scripted RUNNER-03/RUNNER-04 manual verification checklist"
      contains: "Manual-Only Verifications"
  key_links:
    - from: ".planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md"
      to: "src/views/runner-view.ts (renderPreviewZone scroll-pin) + renderRunnerFooter (Back disable)"
      via: "manual verification of automated changes from Plans 02 and 03"
      pattern: "RUNNER-04|RUNNER-03"
---

<objective>
Per D-16, write a scripted RUNNER-03 + RUNNER-04 manual verification checklist (`66-UAT.md`) and run it in a real Obsidian vault, then capture sign-off. The checklist mirrors the Phase 63 `63-VALIDATION.md` "Manual-Only Verifications" pattern. Every scenario is concrete enough that the user (or a future verifier) can execute it without ambiguity.

Purpose: Plans 01-04 are entirely automated — but RUNNER-04 (scroll pinning) and the visual half of RUNNER-03's double-click guard live in DOM behaviour that jsdom does not reliably reproduce (`scrollHeight`, real button-disable animation, real Obsidian textarea sizing). The only honest verification is human-run in real Obsidian.

Output:
- `66-UAT.md` with at minimum: (a) frontmatter summarizing source plans, (b) a Tests section with scripted scenarios per D-16 minimum 3 + Phase 66 RUNNER-03 visual checks, (c) a Summary block with pending counts to be flipped on sign-off, (d) a Gaps section (initially empty).
- A human-verify checkpoint task that the user runs and approves.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-CONTEXT.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-01-SUMMARY.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-02-SUMMARY.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-03-SUMMARY.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-04-SUMMARY.md
@.planning/phases/63-bidirectional-canvas-node-editor-sync/63-UAT.md

<interfaces>
The Phase 63 UAT shape (mirrored here) uses YAML frontmatter and a `## Tests` section with `### N. <name>` headings, each containing `expected:` and `result:` keys. The Summary block has `total / passed / issues / pending / skipped` counts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 66-UAT.md scripted checklist (D-16)</name>
  <files>.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md</files>
  <read_first>
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-UAT.md (frontmatter + Tests + Summary + Gaps shape — copy structure)
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-CONTEXT.md (D-16 minimum scenarios verbatim)
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-01-SUMMARY.md (what shipped — for accuracy)
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-02-SUMMARY.md
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-03-SUMMARY.md
  </read_first>
  <action>
    Create `.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md` with the following structure. All scenarios are pending (`result: pending`) until the human-verify checkpoint flips them.

    ```markdown
    ---
    status: pending
    phase: 66-runner-step-back-reliability-scroll-pinning
    source:
      - 66-01-SUMMARY.md
      - 66-02-SUMMARY.md
      - 66-03-SUMMARY.md
      - 66-04-SUMMARY.md
    requirements:
      - RUNNER-03
      - RUNNER-04
    started: <ISO date when checkpoint runs>
    ---

    ## Manual-Only Verifications

    These scenarios cover the parts of RUNNER-03 (visible Back disable-on-click) and RUNNER-04 (preview textarea scroll pinning) that jsdom cannot faithfully reproduce. Run all in a real Obsidian vault built from the latest `npm run build` output.

    ## Current Test

    [pending start]

    ## Tests

    ### 1. Sidebar — file-bound snippet insert pins preview to bottom (RUNNER-04 SC4, D-09, D-12)
    expected: |
      Open a canvas containing a Question node with at least one file-bound Snippet branch (📄). Drive the runner forward through enough answers to fill the preview textarea past one screen height of accumulated text (you should see a vertical scrollbar inside the preview). Click the file-bound Snippet branch button. After the runner re-renders, the preview textarea must be scrolled to the BOTTOM — the most recently inserted snippet content is visible at the bottom edge of the textarea, with no upward jump.
    result: pending

    ### 2. Sidebar — Back from any state pins preview to bottom (RUNNER-04 SC5, D-09)
    expected: |
      Continuing from scenario 1 (or starting fresh and accumulating enough text to scroll), click the Back button in the runner footer. After the re-render, the preview textarea must be scrolled to the BOTTOM. Verify in three sub-states: (a) Back from at-node after an Answer click, (b) Back from awaiting-snippet-pick (after clicking a directory-bound Snippet branch), (c) Back from awaiting-loop-pick after returning from a body iteration. In all three, the preview must end scrolled to the bottom.
    result: pending

    ### 3. Tab-mode runner — file-bound snippet insert + Back both pin to bottom (RUNNER-04 SC4 + SC5, D-12)
    expected: |
      Open the runner in a tab leaf instead of the sidebar (right-click the runner tab header → "Move to new pane" or similar Obsidian gesture, OR use the existing tab-host mechanism). Repeat scenarios 1 and 2. Both must produce the same scroll-to-bottom behaviour as the sidebar mode. The visible preview is the same `<textarea class="rp-preview-textarea">` in both hosts.
    result: pending

    ### 4. Sidebar — Back is single-click guaranteed (RUNNER-03 SC1, D-01 + D-02)
    expected: |
      With the runner advanced past at least one user action (so Back is enabled), perform a quick double-click on the Back button. Visually confirm: the Back button visibly transitions to a disabled state on the first click (mouse cursor changes, button greyed out per the browser's standard disabled-button visual). The runner advances backwards by exactly ONE step — not two. Repeat at five different runner states (at-node post-Answer, at-node post-Skip, awaiting-loop-pick, awaiting-snippet-pick, after a chooseLoopBranch). In every state, double-click → exactly one step back.
    result: pending

    ### 5. Sidebar — `Processing...` placeholder is never visible (RUNNER-03 SC2, D-07)
    expected: |
      Drive the runner through every reachable state (idle, at-node question, at-node post-Answer transition, awaiting-snippet-pick, awaiting-snippet-fill, awaiting-loop-pick, complete, error if a broken canvas is loaded). At no point during forward advance OR Back should the literal text "Processing..." appear in the question zone. If you see it, capture a screenshot and the canvas filename — that's a regression.
    result: pending

    ### 6. Inline runner — Back is single-click guaranteed (RUNNER-03 SC1, D-01 + D-02)
    expected: |
      Open a canvas via the "Run protocol in inline" command. Advance past at least one user action so Back is enabled. Double-click Back. The button visibly disables on the first click; the runner advances backwards by exactly one step. Verify in at-node and awaiting-loop-pick inline states.
    result: pending

    ### 7. Sidebar — repeated Back across loop boundaries does not corrupt accumulated text (RUNNER-03 SC3, D-08)
    expected: |
      Open `src/__tests__/fixtures/unified-loop-valid.canvas` (or an equivalent loop-bearing canvas in your test vault). Drive forward: pick the body branch, answer the question inside the body, return to the loop picker (now iter=2), pick the body branch again, answer again, exit via «выход». Now click Back six times in a row (slowly, single-click each time). At each step the preview text and runner state must move back by exactly one step — no jumps, no duplicate text, no missing text. Final state after six Backs: awaiting-loop-pick at the loop node, accumulated text is empty.
    result: pending

    ## Summary

    total: 7
    passed: 0
    issues: 0
    pending: 7
    skipped: 0

    ## Gaps

    [none yet — populated only if a scenario fails]
    ```

    Notes:
    - Use the EXACT scenario numbering above so the human-verify checkpoint can reference scenarios by number.
    - The `started:` field should be filled in by the executor at the time of writing (today's date, ISO 8601).
    - This task creates the file in the `pending` state. The next task (the checkpoint) is when scenarios are actually executed and results filled in.
  </action>
  <verify>
    <automated>test -f .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md && grep -c "Manual-Only Verifications" .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md` exists.
    - File contains the literal substring `Manual-Only Verifications`.
    - File contains at least 7 `### ` test headings (count: `grep -c "^### " 66-UAT.md` ≥ 7).
    - File contains at least 3 RUNNER-04-tagged scenarios — `grep -c "RUNNER-04" 66-UAT.md` ≥ 3 (covering D-16's minimum 3 visual scroll checks).
    - File contains at least 2 RUNNER-03-tagged scenarios — `grep -c "RUNNER-03" 66-UAT.md` ≥ 2.
    - YAML frontmatter `status: pending` is present.
    - Summary block lists total: 7 (or whatever count was authored), pending: equal to total, passed: 0.
  </acceptance_criteria>
  <done>UAT scripted checklist is committed, all scenarios `pending`, ready for human walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the 66-UAT.md scenarios in real Obsidian (D-16)</name>
  <files>.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md</files>
  <read_first>
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md (the checklist authored in Task 1)
  </read_first>
  <action>
    Pause for the user to run the seven scenarios in 66-UAT.md against the latest `npm run build` output, loaded into a real Obsidian vault. Do NOT make any source code changes during this checkpoint unless the user reports a failed scenario. After the user reports back:
    - For each PASSED scenario: flip `result: pending` → `result: pass` in 66-UAT.md.
    - For each FAILED scenario: flip `result: pending` → `result: fail`, append the user's notes under a `notes:` key, and add an entry to the `## Gaps` section with `truth`, `status: open`, `severity`, `test: <scenario number>`, and a brief `reason`.
    - Update the Summary block to reflect pass/fail/pending counts.
    - If ANY scenario fails, the orchestrator should subsequently route to gap closure (`/gsd-plan-phase 66 --gaps`). Do not pretend a fail is a pass.
  </action>
  <verify>
    <manual>Follow the scenarios in 66-UAT.md against the latest build in a real Obsidian vault.</manual>
  </verify>
  <acceptance_criteria>
    - 66-UAT.md `status:` frontmatter changed from `pending` to either `complete` (all pass) or `complete-with-gaps` (some fail).
    - For every scenario, `result:` is either `pass` or `fail` — none remain `pending`.
    - Summary block counts add up to total.
    - If any scenario failed, the Gaps section contains a corresponding entry.
  </acceptance_criteria>
  <done>User has reported back with pass/fail per scenario; UAT file reflects the actual state of the build; downstream gap closure (if any) is unblocked.</done>
  <what-built>
    Phase 66 ships:
    - Plan 01: ProtocolRunner restoreStatus + _stepBackInFlight + Processing-text removal in RunnerView.
    - Plan 02: RunnerView renderPreviewZone unconditional scroll-to-bottom, Phase 47 RUNFIX-02 mechanism removed.
    - Plan 03: Back-disable-on-click prologue inside renderRunnerFooter (sidebar + inline).
    - Plan 04: Property roundtrip suite + four scripted loop-boundary scenarios.
    - Plan 05: 66-UAT.md scripted checklist (this checkpoint).
  </what-built>
  <how-to-verify>
    1. Build the plugin: `npm run build`. Reload the plugin in your Obsidian test vault (Settings → Community Plugins → reload, or restart Obsidian).
    2. Open `66-UAT.md` in your editor or in Obsidian.
    3. For each scenario 1-7 in order:
       a. Open the relevant canvas (or set up the runner state described).
       b. Perform the action sequence.
       c. Visually confirm the expected outcome.
       d. Note pass / fail and any anomalies.
    4. Report results back to the assistant. The assistant flips the `result:` keys and updates the Summary.
    5. If anything fails, describe what you saw — screenshot if useful — so the gap is captured precisely.
  </how-to-verify>
  <resume-signal>Type "approved — all 7 pass", OR "scenario N failed: <description>" for each fail, OR "approved with notes: <notes>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human verifier → 66-UAT.md state | Only the user's report flips a scenario from pending to pass/fail; the assistant does not auto-pass. |
| Obsidian vault → built plugin | The user runs scenarios against `main.js` + `styles.css` produced by `npm run build`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05-01 | Spoofing | UAT pass without actual run | mitigate | Checkpoint is gated by user's textual approval; the resume-signal pattern requires the user to report results, not just type "ok". |
| T-66-05-02 | Information Disclosure | UAT screenshots | accept | If user shares screenshots, they may show vault paths or note content; user decides what to share. |
| T-66-05-03 | Tampering | premature status flip | mitigate | Task 1 creates the file in `pending`; Task 2 is the only writer that flips results, and only based on user input. |
</threat_model>

<verification>
- 66-UAT.md exists with frontmatter `status: pending` after Task 1.
- After Task 2, frontmatter `status:` is `complete` or `complete-with-gaps` and no scenario remains `pending`.
- `grep -c "result: pending" .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md` returns 0 after Task 2.
</verification>

<success_criteria>
- D-16 visual scroll verification scenarios (≥ 3 for RUNNER-04) shipped in 66-UAT.md and run by the user.
- RUNNER-03 visual scenarios (Back disable + no Processing + repeated-Back-across-loop-boundaries) shipped and run.
- User has signed off — either approved fully, or filed gaps for downstream closure.
</success_criteria>

<output>
After completion, create `.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-05-SUMMARY.md`.
</output>
