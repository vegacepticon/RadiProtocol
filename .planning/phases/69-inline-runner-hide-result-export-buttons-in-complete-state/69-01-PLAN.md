---
phase: 69-inline-runner-hide-result-export-buttons-in-complete-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - INLINE-CLEAN-01

must_haves:
  truths:
    - "REQUIREMENTS.md INLINE-CLEAN-01 line states the buttons are no longer rendered in any Inline Runner state (not only the complete state)"
    - "ROADMAP.md §Phase 69 Goal mentions all six Inline Runner states (idle, at-node, awaiting-snippet-pick, awaiting-loop-pick, awaiting-snippet-fill, complete) or equivalent 'every Inline Runner state' / 'in any Inline Runner state' wording"
    - "ROADMAP.md §Phase 69 SC#1 asserts the three button class names (rp-copy-btn, rp-save-btn, rp-insert-btn) are absent from the DOM across all Inline states"
    - "ROADMAP.md §Phase 69 SC#2 and SC#3 (sidebar / tab cross-mode regression) remain unchanged"
    - "REQUIREMENTS.md sentences about 'The Inline Runner already appends every answer/snippet directly to the active note' and 'The sidebar Runner View and the tab Runner View continue to show all three buttons unchanged' are preserved verbatim"
    - "REQUIREMENTS.md Out-of-Scope table row 'Removing buttons from sidebar/tab Runner views | INLINE-CLEAN-01 is Inline-mode-only by design' is preserved unchanged"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Re-worded INLINE-CLEAN-01 line covering all 6 Inline states"
      contains: "INLINE-CLEAN-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 69 Goal + SC#1 expanded to all Inline states; SC#2/#3 untouched"
      contains: "Phase 69: Inline Runner — Hide Result-Export Buttons"
  key_links:
    - from: ".planning/REQUIREMENTS.md INLINE-CLEAN-01"
      to: ".planning/ROADMAP.md §Phase 69 Success Criteria"
      via: "shared scope wording — both must mention all 6 Inline states (or equivalent 'in any Inline Runner state')"
      pattern: "Inline Runner state|any Inline Runner state|all (six|6) Inline"
    - from: ".planning/ROADMAP.md §Phase 69 Goal"
      to: "Phase 69 Plan 02 code deletion"
      via: "scope contract — code in Plan 02 deletes outputToolbar from all 6 cases per the rewritten Goal"
      pattern: "all (six|6) Inline|every Inline Runner state|in any Inline Runner state"
---

<objective>
Amend `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` so the locked Phase 69 scope (CONTEXT.md D-01: hide buttons in **all** six Inline Runner states, not only `complete`) is reflected in the source-of-truth requirement text and roadmap success criteria — BEFORE any code is written in Plan 02.

This mirrors the Phase 67 D-13 precedent (commit `c523b45`): when `/gsd-discuss-phase` widens the scope of a requirement bullet, the first plan of the phase rewrites the requirement and roadmap so the SC ↔ code traceability stays in sync for the v1.11 milestone audit.

Purpose: Lock written scope before code lands. Eliminates the gap where milestone-end audits would find code that deletes the toolbar from `idle`/`at-node`/`awaiting-snippet-*`/`awaiting-loop-pick` while the requirement still reads "when the Inline Runner reaches the protocol-complete state".

Output: Two updated planning docs; commit `docs(69-01): amend INLINE-CLEAN-01 + ROADMAP §Phase 69 to cover all 6 Inline states (D-02)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-CONTEXT.md
@.planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite INLINE-CLEAN-01 in REQUIREMENTS.md (D-02)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — read all of it; INLINE-CLEAN-01 is line 11; the Out-of-Scope table near line 78 contains a related row that MUST stay untouched; Traceability table near line 86 lists "INLINE-CLEAN-01 | Phase 69 | Open" and stays untouched)
    - .planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-CONTEXT.md (D-01 widens scope to all 6 states; D-02 mandates this re-wording)
    - .planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-PATTERNS.md (.planning/REQUIREMENTS.md section — exact pattern for the rewrite)
  </read_first>
  <action>
Replace line 11 of `.planning/REQUIREMENTS.md` (the entire INLINE-CLEAN-01 bullet) with the rewritten wording below. Use the Edit tool with the existing line as the `old_string` (verbatim) and the new text as the `new_string`.

**Existing line 11 (current text — copy verbatim as `old_string`):**

```markdown
- [ ] **INLINE-CLEAN-01**: When the Inline Runner reaches the protocol-complete state, the Insert / Copy to clipboard / Save to note buttons are no longer rendered — only the Close (and Run Again, where applicable) controls remain. The Inline Runner already appends every answer/snippet directly to the active note as the protocol runs, so the result-export buttons are redundant in this mode. The sidebar Runner View and the tab Runner View continue to show all three buttons unchanged — this requirement is Inline-mode-only.
```

**Replacement text (new text — use verbatim as `new_string`):**

```markdown
- [ ] **INLINE-CLEAN-01**: In every Inline Runner state (`idle`, `at-node`, `awaiting-snippet-pick`, `awaiting-loop-pick`, `awaiting-snippet-fill`, `complete`), the Insert / Copy to clipboard / Save to note buttons are no longer rendered — only the Close control in the modal header remains. The Inline Runner already appends every answer/snippet directly to the active note as the protocol runs, so the result-export buttons are redundant in this mode. The sidebar Runner View and the tab Runner View continue to show all three buttons unchanged — this requirement is Inline-mode-only.
```

**Diff summary:**
- "When the Inline Runner reaches the protocol-complete state" → "In every Inline Runner state (`idle`, `at-node`, `awaiting-snippet-pick`, `awaiting-loop-pick`, `awaiting-snippet-fill`, `complete`)"
- "only the Close (and Run Again, where applicable) controls remain" → "only the Close control in the modal header remains" (drops the "Run Again where applicable" parenthetical per CONTEXT.md D-06: Run Again is NOT added in Phase 69 for Inline)
- Sentences 2 and 3 (about the active-note append contract and the sidebar/tab unchanged guarantee) are preserved **verbatim** — they remain accurate.

**Out of scope for this task — DO NOT MODIFY any of these:**
- The Out-of-Scope table row "Removing buttons from sidebar/tab Runner views | INLINE-CLEAN-01 is Inline-mode-only by design" near line 78. The rewritten bullet is still Inline-mode-only — that table row stays correct.
- The Traceability table near line 86 (`| INLINE-CLEAN-01 | Phase 69 | Open |`).
- Every other requirement bullet (LOOP-EXIT-01, DONATE-01, CANVAS-LIB-01..08, BRAT-03).
- The "Future Requirements (Deferred)" list and the trailing `*Requirements defined: 2026-04-29*` footer.

DO NOT modify any other file in this task.
  </action>
  <verify>
    <automated>grep -n "INLINE-CLEAN-01" .planning/REQUIREMENTS.md | head -5 && grep -c "In every Inline Runner state" .planning/REQUIREMENTS.md && grep -c "When the Inline Runner reaches the protocol-complete state" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "In every Inline Runner state" .planning/REQUIREMENTS.md` returns 1 (the new wording is present)
    - `grep -c "When the Inline Runner reaches the protocol-complete state" .planning/REQUIREMENTS.md` returns 0 (the old wording is gone)
    - `grep -c "awaiting-snippet-pick" .planning/REQUIREMENTS.md` returns at least 1 (one of the 6 state names cited)
    - `grep -c "awaiting-snippet-fill" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "awaiting-loop-pick" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "INLINE-CLEAN-01" .planning/REQUIREMENTS.md` returns at least 2 (one in the bullet, one in the Traceability table — unchanged from before)
    - `grep -c "INLINE-CLEAN-01 is Inline-mode-only by design" .planning/REQUIREMENTS.md` returns 1 (Out-of-Scope table row preserved)
    - `grep -c "The Inline Runner already appends every answer/snippet" .planning/REQUIREMENTS.md` returns 1 (sentence 2 preserved verbatim)
    - `grep -c "The sidebar Runner View and the tab Runner View continue to show all three buttons unchanged" .planning/REQUIREMENTS.md` returns 1 (sentence 3 preserved verbatim)
    - `grep -c "Run Again, where applicable" .planning/REQUIREMENTS.md` returns 0 (the "Run Again, where applicable" parenthetical is gone from INLINE-CLEAN-01 per D-06)
    - No other requirement bullet was touched: `grep -c "LOOP-EXIT-01" .planning/REQUIREMENTS.md`, `grep -c "DONATE-01" .planning/REQUIREMENTS.md`, `grep -c "BRAT-03" .planning/REQUIREMENTS.md` all still return their pre-edit counts (each ≥ 2)
  </acceptance_criteria>
  <done>INLINE-CLEAN-01 line in `.planning/REQUIREMENTS.md` re-worded to cover all six Inline states; sentences 2 and 3 preserved verbatim; Out-of-Scope and Traceability tables untouched; no other bullets modified.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md §Phase 69 Goal + SC#1 (D-02)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the file being modified — Phase 69 entry is at lines 182-191; the §Phases summary list at line 30 also mentions Phase 69 by short title and stays untouched; the Progress table near line 269 lists Phase 69 by row and stays untouched)
    - .planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-CONTEXT.md (D-02 mandates Goal + SC#1 expansion; D-06 confirms no Run Again)
    - .planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-PATTERNS.md (.planning/ROADMAP.md section — exact pattern for the rewrite)
  </read_first>
  <action>
Use the Edit tool to replace the Goal line (line 183) and Success Criteria #1 (line 187) of `.planning/ROADMAP.md`. Make TWO separate Edits to limit blast radius.

**Edit 1 — replace the Goal line.**

`old_string` (verbatim from line 183):
```markdown
**Goal**: When the Inline Runner reaches the protocol-complete state, the user no longer sees the redundant Insert / Copy to clipboard / Save to note buttons — only Close (and Run Again, where applicable) remains. Sidebar Runner View and tab Runner View are unaffected.
```

`new_string`:
```markdown
**Goal**: In every Inline Runner state (`idle`, `at-node`, `awaiting-snippet-pick`, `awaiting-loop-pick`, `awaiting-snippet-fill`, `complete`), the user no longer sees the redundant Insert / Copy to clipboard / Save to note buttons — only the Close control in the modal header remains. Sidebar Runner View and tab Runner View are unaffected.
```

**Edit 2 — replace Success Criteria #1.**

`old_string` (verbatim from line 187):
```markdown
  1. Running a protocol to completion in Inline mode renders a footer/toolbar with Close (and Run Again where applicable) — Insert, Copy to clipboard, and Save to note buttons are absent from the DOM (INLINE-CLEAN-01)
```

`new_string`:
```markdown
  1. In every Inline Runner state (`idle`, `at-node`, `awaiting-snippet-pick`, `awaiting-loop-pick`, `awaiting-snippet-fill`, `complete`), the `.rp-copy-btn`, `.rp-save-btn`, and `.rp-insert-btn` button classes are absent from the DOM, and the `.rp-output-toolbar` container is also absent inside `.rp-inline-runner-content`; only the Close control in the modal header remains as a way to exit the modal (INLINE-CLEAN-01)
```

**DO NOT touch:**
- Line 184 (`**Depends on**: Nothing (Inline-mode-only render-time conditional in `InlineRunnerModal`; isolated from sidebar/tab runner code paths)`)
- Line 185 (`**Requirements**: INLINE-CLEAN-01`)
- Line 186 (`**Success Criteria** (what must be TRUE):`)
- Lines 188-189 (SC#2 and SC#3 about sidebar / tab runner — these MUST stay verbatim, they remain accurate)
- Line 190 (SC#4 about active-note append contract — MUST stay verbatim, it remains accurate)
- Line 191 (`**Plans**: TBD`) — left for the orchestrator's later plan-list update step
- Line 192 (`**UI hint**: yes`)
- The Phase summary list at line 30 (Phase 69 short-title bullet)
- The Progress table near line 269 (Phase 69 row)
- Every other phase's section (Phases 70-74) and every archived milestone block

DO NOT modify any other file in this task.
  </action>
  <verify>
    <automated>grep -n "Phase 69" .planning/ROADMAP.md | head -5 && grep -c "In every Inline Runner state" .planning/ROADMAP.md && grep -c "When the Inline Runner reaches the protocol-complete state" .planning/ROADMAP.md && grep -c "rp-copy-btn" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "In every Inline Runner state" .planning/ROADMAP.md` returns 2 (Goal + SC#1)
    - `grep -c "When the Inline Runner reaches the protocol-complete state" .planning/ROADMAP.md` returns 0 (old Goal wording gone)
    - `grep -c "Running a protocol to completion in Inline mode renders a footer/toolbar" .planning/ROADMAP.md` returns 0 (old SC#1 wording gone)
    - `grep -c "\.rp-copy-btn" .planning/ROADMAP.md` returns at least 1 (SC#1 now mentions the button class explicitly)
    - `grep -c "\.rp-save-btn" .planning/ROADMAP.md` returns at least 1
    - `grep -c "\.rp-insert-btn" .planning/ROADMAP.md` returns at least 1
    - `grep -c "Running the same protocol to completion in sidebar Runner View shows all three result-export buttons exactly as before" .planning/ROADMAP.md` returns 1 (SC#2 preserved verbatim)
    - `grep -c "Running the same protocol to completion in tab Runner View shows all three result-export buttons exactly as before" .planning/ROADMAP.md` returns 1 (SC#3 preserved verbatim)
    - `grep -c "The active note continues to receive answer/snippet text appended in real time" .planning/ROADMAP.md` returns 1 (SC#4 preserved verbatim)
    - `grep -c "Run Again, where applicable" .planning/ROADMAP.md` returns 0 (parenthetical gone from Phase 69 Goal per D-06; no other phase used that exact phrase)
    - `grep -c "INLINE-CLEAN-01" .planning/ROADMAP.md` returns at least 5 (4 SC mentions + 1 Requirements line — same count as before the edit)
    - `grep -c "**Depends on**: Nothing (Inline-mode-only render-time conditional" .planning/ROADMAP.md` returns 1 (line 184 untouched)
    - `grep -c "Phase 70: Loop-Exit Picker Visual Hint" .planning/ROADMAP.md` returns at least 2 (Phase 70 entry untouched — once in §Phases summary, once in §Phase Details)
  </acceptance_criteria>
  <done>ROADMAP.md §Phase 69 Goal and SC#1 widened to all six Inline states with explicit `.rp-copy-btn` / `.rp-save-btn` / `.rp-insert-btn` / `.rp-output-toolbar` references; SC#2/#3/#4 preserved verbatim; Depends on / Requirements / Plans / UI hint untouched; no other phase or table row modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure planning-document edit. No process boundary, no code path, no input from any external source crosses any boundary. The two files are markdown documents on the local filesystem committed to git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` | accept | These are project planning docs under git. Any unauthorized edit is caught by `git diff` review at commit time. No runtime trust placed on contents. |
| T-69-02 | Information Disclosure | n/a | accept | Re-worded text contains no secrets, no addresses, no credentials. The wallet addresses in DONATE-01 are in a separate requirement and are not touched by Plan 01. |

Phase 69 Plan 01 surface: zero. No new code, no input handling, no auth, no storage write outside git-tracked planning docs.
</threat_model>

<verification>
- Both file edits applied via the Edit tool (exact `old_string` matches above).
- All 11 + 13 acceptance criteria across the two tasks pass.
- `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows ONLY the lines specified above and no other lines (verify visually before commit).
- No source files (`src/**`) modified by this plan.
- No test files modified by this plan.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` INLINE-CLEAN-01 bullet states all 6 Inline states; sentences 2 and 3 preserved verbatim.
- `.planning/ROADMAP.md` §Phase 69 Goal and SC#1 expanded to all 6 Inline states with explicit button-class references; SC#2/#3/#4 preserved verbatim.
- `git diff` shows exactly two edited files, exactly the lines listed above, no others.
- Plan 02 can now safely delete the toolbar from all 6 cases without creating a SC↔code drift in the v1.11 milestone audit.
</success_criteria>

<output>
After completion, create `.planning/phases/69-inline-runner-hide-result-export-buttons-in-complete-state/69-01-SUMMARY.md` with: files modified, exact old/new text snippets, the D-02 / D-06 decision IDs cited, and the verification grep counts that passed.
</output>
