---
phase: 73
plan: 01
plan_id: 73-01-PLAN
title: ОГК short — single-trunk linear canvas, no fan-out, no loops
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas (author's vault — outside repo working tree)"
autonomous: false
requirements: [CANVAS-LIB-06]
tags: [canvas-authoring, content, obsidian, ogk, short]
must_haves:
  truths:
    - "An `ОГК short 1.0.0.canvas` file exists at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` and is valid JSON parseable by Obsidian"
    - "The canvas runs end-to-end in the Protocol Runner without throwing — author walks 4 sections through to terminal Заключение/Рекомендации"
    - "Each of the 4 organ-system sections (ЛЕГКИЕ, ТРАХЕЯ И БРОНХИ, СРЕДОСТЕНИЕ, ПЛЕВРАЛЬНЫЕ ПОЛОСТИ) presents its own «— норма?» Question with Норма (verbatim norm text) and «Опишу вручную» (==напишу что не так с …== chip) branches per D-01 + D-02"
    - "There are zero `loop` nodes in the canvas per D-04 (short = no multi-finding scenarios)"
    - "There is no Контраст fan-out and no procedure-preamble text-block — `start` connects directly to the first section header per Pattern S5"
    - "The terminal text-block contains the verbatim string `## Заключение\\nРКТ-признаки ==диагноз==\\n\\n## Рекомендации\\nКонсультация направившего специалиста.` per D-03 + D-13"
    - "All 4 norm-text strings appear verbatim from `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` lines 4-7 (byte-equivalent in муж file)"
    - "`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs` exits 0 and prints `[ОГК short 1.0.0.canvas] N pass, 0 fail` covering inherited Phase 72 invariants I1–I10 (D-17): I1 single start, I2/I3 vacuously satisfied (no loops per D-04), I4 each snippet node has subfolderPath XOR snippetPath, I5 each section header text-block present, I6 Заключение anchor `РКТ-признаки` present, I7 Рекомендации `Консультация направившего специалиста.` present, I8 edge referential integrity, I9 BFS reachability, I10 `==…==` chip well-formedness"
  decisions:
    - "D-01: per-organ-system text-blocks (NOT monolithic) — 4 separate sections"
    - "D-02: per-system Норма? gate — Да-branch verbatim norm / Нет-branch ==напишу что не так== chip"
    - "D-03: terminal `## Заключение` + `## Рекомендации` text-block"
    - "D-04: NO loops in short canvases"
    - "D-08, D-09: snippet-node strategy reusing Phase 72's `SNIPPETS\\ОГК\\` subfolders (`ОГК/ЛЕГКИЕ`, `ОГК/СРЕДОСТЕНИЕ`, `ОГК/ПЛЕВРАЛЬНЫЕ ПОЛОСТИ`)"
    - "D-10: do NOT pre-create snippet files; embed paths in nodes only"
    - "D-12, D-13: Phase-27 ==fill-in== chip on Заключение line (`==диагноз==`)"
    - "D-14: chip well-formedness invariant I10 enforced"
    - "D-15: programmatic per-canvas .mjs builder reusing Phase 72 `canvas-builder.mjs` via relative import"
    - "D-16: builder writes to `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (outside repo working tree)"
    - "D-17: I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged; builder prints invariant pass count"
    - "D-18: file name `ОГК short 1.0.0.canvas`"
    - "D-19: [informational] vault state observation — `ОГК short 1.0.0.canvas` coexists alongside the existing `ОГК.canvas` reference and Phase 72's six 1.0.0 canvases at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (no plan task implements this; vault target documented for context)"
    - "D-20: static layer — builder prints I1-I10 invariant pass count; no .canvas committed to repo"
    - "D-21: manual layer — Task 3 (`checkpoint:human-verify`) is the human_verification entry mirroring Phase 72 VERIFICATION.md structure; author walks ОГК short ×1 primary branch in Obsidian, confirms structural match with `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` ОГК section"
    - "D-22: per-canvas `73-01-SUMMARY.md` committed to `.planning/phases/73-…/`"
    - "PATTERNS.md: ID prefix `'6'` for ОГК short Canvas constructor (avoiding Phase 72 prefixes 1-5)"
  artifacts:
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs"
      provides: "Programmatic builder for ОГК short canvas"
      contains: "exactly one `new Canvas('6')`; 4 calls to `section({…})`; one terminal text-block with `## Заключение\\nРКТ-признаки ==диагноз==`; verify+write tail with `verifyInvariants` + `reportFindings` + `writeFileSync`"
    - path: "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas"
      provides: "Author-vault ОГК short canvas (NOT committed to repo per D-16/D-20)"
      contains: "1 start node; 0 loops; 4 section header text-blocks each beginning with `\\n`; 1 terminal text-block with verbatim Заключение/Рекомендации; well-formed valid JSON tab-indented"
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md"
      provides: "Per-plan summary mirroring Phase 72 SUMMARY format (D-22)"
      contains: "frontmatter with `phase: 73`, `plan: 01`, `requires: [CANVAS-LIB-06]`, `verification: static passed N/N, manual pending`; topology table; static invariant results; manual verification checklist"
  key_links:
    - from: "start node"
      to: "first section header (`\\nЛЕГКИЕ:`) text-block"
      via: "single edge"
      pattern: "no procedure preamble per Pattern S5 — short templates have no preamble line above the first organ-system sentence"
    - from: "each section's branches array (`norm`, `free`, optional `snip`)"
      to: "next section's header text-block (or terminal text-block for the last section)"
      via: "convergent edges via `converge(branches, target)` helper"
      pattern: "linear single-trunk top-to-bottom, no fan-out"
    - from: "last section (ПЛЕВРАЛЬНЫЕ ПОЛОСТИ) branches"
      to: "terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение line wraps `РКТ-признаки ` from source MD as `РКТ-признаки ==диагноз==` per D-13"
---

<objective>
Author the ОГК (органы грудной клетки) short-version algorithmic canvas in the author's local vault. Single-trunk linear canvas with 4 organ-system sections + terminal Заключение/Рекомендации, NO loops, NO Контраст fan-out, NO procedure preamble. Each organ-system has its own «— норма?» Question with verbatim normal-finding text on the Норма branch and a ==напишу что не так с …== fill-in chip on the «Опишу вручную» branch (D-01, D-02). When run end-to-end, the assembled output structurally matches `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 (the ОГК section is byte-equivalent in the муж file).

Purpose: deliver CANVAS-LIB-06.
Output: `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` (author's vault — NOT committed to repo per D-16, D-20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan is **programmatic** (D-15) — the executor runs a Node.js builder script that emits canvas JSON via the existing Phase 72 `canvas-builder.mjs` helper. There is no `npm run build`, no `vitest`, no `src/` change. The builder reuses Phase 72 helper via relative import (per 73-PATTERNS.md «Decision: Reuse vs Fork»):

```javascript
import { Canvas, verifyInvariants, reportFindings } from '../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs';
```

The end-to-end runner walkthrough in Obsidian (Task 3) is `checkpoint:human-verify` — Obsidian Protocol Runner cannot be driven headlessly from this session.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md
@./CLAUDE.md

<source_template>
Verbatim text source (must match byte-for-byte): `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 (ОГК section is byte-equivalent in the муж file at the same line range):

```
В легких свежих очаговых и инфильтративных изменений не выявлено.
Трахея и главные бронхи проходимы, не деформированы, обычного диаметра.
Лимфоузлы средостения не увеличены.
Плевральные полости и полость перикарда свободны.
```

Terminal block source (lines 19-26 of either short MD file — the trailing-space `РКТ-признаки ` becomes `РКТ-признаки ==диагноз==` per D-13):

```
## Заключение
РКТ-признаки 
## Рекомендации
Консультация направившего специалиста.
```
</source_template>

<interfaces>
<!-- Helper API exported from `.planning/phases/72-…/build/canvas-builder.mjs`. The executor reuses these directly via relative import — do NOT redefine. -->

```javascript
export class Canvas {
  constructor(prefix) { /* prefix is a single hex char namespacing IDs */ }
  start({ x, y, width = 250, height = 60 }) { /* returns id */ }
  textBlock({ text, x, y, width = 250, height = 60 }) { /* returns id */ }
  question({ text, x, y, width = 250, height = 60 }) { /* returns id */ }
  answer({ text, displayLabel, x, y, width = 250, height = 60 }) { /* returns id */ }
  loop({ text, headerText, x, y, width = 250, height = 80 }) { /* returns id (NOT used in Phase 73) */ }
  snippetFolder({ subfolderPath, snippetLabel, text, x, y, width = 250, height = 60 }) { /* returns id */ }
  snippetFile({ snippetPath, snippetLabel, text, x, y, width = 250, height = 60 }) { /* returns id (NOT used in Phase 73) */ }
  edge({ from, to, fromSide = 'bottom', toSide = 'top', label }) { /* returns id */ }
  toJSON() { /* returns { nodes, edges } */ }
}

export function verifyInvariants(canvasJSON, spec) {
  // spec: { sectionHeaders: string[], zaklyuchenie: string, rekomendatsii: string }
  // Returns array of { inv, status, msg } — checks I1-I10
}

export function reportFindings(canvasName, findings) {
  // Prints `[canvasName] P pass, F fail (N checks)` and per-fail lines
  // Returns true if zero failures
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build-ogk-short.mjs (programmatic ОГК short canvas builder)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs</files>
  <read_first>
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md (D-01..D-22)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md (S1-S5 + ОГК-short specific section)
    - Z:\projects\references\short - ОГК ОБП ОМТ жен.md (verbatim norm text source — lines 4-7)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs (single-trunk analog with section() + converge() helpers — read entire file before authoring)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs (helper API + verifyInvariants logic)
    - ./CLAUDE.md (never delete existing code; CSS not applicable here since no src/ changes)
  </read_first>
  <action>
Create the file `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs` (and the parent `build/` directory if it does not exist yet) with this exact structure (DO NOT add a procedure preamble text-block — short templates have no preamble per Pattern S5; DO NOT call `c.loop()` per D-04):

1. **Header comment + imports** (verbatim from PATTERNS.md):
   ```javascript
   // Build ОГК short 1.0.0.canvas per 73-01-PLAN.md.
   // Source template: Z:\projects\references\short - ОГК ОБП ОМТ жен.md (lines 4-7; ОГК section byte-equivalent in муж file).
   // Reference shape: Phase 72 build-pkop.mjs (linear single-trunk, no fan-out, no loops).

   import { writeFileSync } from 'node:fs';
   import { Canvas, verifyInvariants, reportFindings } from '../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs';

   const c = new Canvas('6'); // ОГК short → prefix '6'
   ```

2. **Layout constants** (verbatim from `build-pkop.mjs` lines 12-17):
   ```javascript
   const X = 0;
   const X_NORM = -340;
   const X_FREE = 340;
   const X_SNIP = 720;
   const STEP = 160;
   ```

3. **`section()` factory + `converge()` helper** — copy verbatim from `build-pkop.mjs` lines 31-53. Do NOT modify the helper bodies.

4. **Start node** (NO procedure preamble per D-04 + Pattern S5):
   ```javascript
   let y = -200;
   const start = c.start({ x: X, y });
   y += STEP;
   ```

5. **Section 1 — ЛЕГКИЕ** (verbatim norm text from short-жен MD line 4):
   ```javascript
   const sec1 = section({
     headerText: 'ЛЕГКИЕ:',
     normText: 'В легких свежих очаговых и инфильтративных изменений не выявлено.',
     sectionName: 'легкими',
     snippetSubfolder: 'ОГК/ЛЕГКИЕ',
     y,
   });
   c.edge({ from: start, to: sec1.header });
   y += STEP * 2 + 200;
   ```

6. **Section 2 — ТРАХЕЯ И БРОНХИ** (verbatim norm text from short-жен MD line 5; per PATTERNS.md table, snippet folder is optional for this section — omit `snippetSubfolder` to match the existing reference ОГК 1.10.0 vocabulary):
   ```javascript
   const sec2 = section({
     headerText: 'ТРАХЕЯ И БРОНХИ:',
     normText: 'Трахея и главные бронхи проходимы, не деформированы, обычного диаметра.',
     sectionName: 'трахеей и бронхами',
     y,
   });
   converge(sec1.branches, sec2.header);
   y += STEP * 2 + 200;
   ```

7. **Section 3 — СРЕДОСТЕНИЕ** (verbatim norm text from short-жен MD line 6):
   ```javascript
   const sec3 = section({
     headerText: 'СРЕДОСТЕНИЕ:',
     normText: 'Лимфоузлы средостения не увеличены.',
     sectionName: 'средостением',
     snippetSubfolder: 'ОГК/СРЕДОСТЕНИЕ',
     y,
   });
   converge(sec2.branches, sec3.header);
   y += STEP * 2 + 200;
   ```

8. **Section 4 — ПЛЕВРАЛЬНЫЕ ПОЛОСТИ** (verbatim norm text from short-жен MD line 7):
   ```javascript
   const sec4 = section({
     headerText: 'ПЛЕВРАЛЬНЫЕ ПОЛОСТИ:',
     normText: 'Плевральные полости и полость перикарда свободны.',
     sectionName: 'плевральными полостями',
     snippetSubfolder: 'ОГК/ПЛЕВРАЛЬНЫЕ ПОЛОСТИ',
     y,
   });
   converge(sec3.branches, sec4.header);
   y += STEP * 2 + 200;
   ```

9. **Terminal Заключение/Рекомендации text-block** (verbatim from short MD lines 19-26 with the trailing-space `РКТ-признаки ` wrapped as `РКТ-признаки ==диагноз==` per D-13):
   ```javascript
   const terminal = c.textBlock({
     text: '\n## Заключение\nРКТ-признаки ==диагноз==\n\n## Рекомендации\nКонсультация направившего специалиста.',
     x: X, y, height: 220,
   });
   converge(sec4.branches, terminal);
   ```

10. **Verify + write tail** (model on `build-pkop.mjs` lines 196-215 verbatim, adjust paths and section list):
    ```javascript
    const json = c.toJSON();
    const findings = verifyInvariants(json, {
      sectionHeaders: ['ЛЕГКИЕ:', 'ТРАХЕЯ И БРОНХИ:', 'СРЕДОСТЕНИЕ:', 'ПЛЕВРАЛЬНЫЕ ПОЛОСТИ:'],
      zaklyuchenie: 'РКТ-признаки',
      rekomendatsii: 'Консультация направившего специалиста.',
    });
    const ok = reportFindings('ОГК short 1.0.0.canvas', findings);

    const VAULT_PATH = 'Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas';
    writeFileSync(VAULT_PATH, JSON.stringify(json, null, '\t'), 'utf8');
    console.log(`\n→ wrote ${VAULT_PATH}`);
    console.log(`  nodes: ${json.nodes.length}, edges: ${json.edges.length}`);
    console.log(`  loops: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'loop').length}, snippets: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'snippet').length}`);
    process.exit(ok ? 0 : 1);
    ```

Then RUN the builder:
```bash
node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs
```

Confirm exit code 0 and the printed line `[ОГК short 1.0.0.canvas] N pass, 0 fail` (N is roughly 11-14 — I1, I4×3 snippets, I5×4 sections, I6, I7, I8, I9, I10; I2/I3 are vacuously satisfied because there are no loops). Confirm `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` is written and is valid JSON.
  </action>
  <verify>
    <automated>node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs` exists.
    - The builder file imports from `'../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs'` (verifiable via grep).
    - The builder file constructs `new Canvas('6')` (verifiable via grep — distinct from Phase 72 prefixes 1-5).
    - The builder file does NOT contain `c.loop(` (per D-04 — verifiable via grep returning zero matches).
    - The builder file contains the four verbatim norm strings from short-жен MD lines 4-7: `'В легких свежих очаговых и инфильтративных изменений не выявлено.'`, `'Трахея и главные бронхи проходимы, не деформированы, обычного диаметра.'`, `'Лимфоузлы средостения не увеличены.'`, `'Плевральные полости и полость перикарда свободны.'` (each verifiable via grep -F).
    - The builder file contains the terminal-block string `'\n## Заключение\nРКТ-признаки ==диагноз==\n\n## Рекомендации\nКонсультация направившего специалиста.'` (verifiable via grep -F).
    - `node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs` exits with code 0.
    - The builder stdout contains the line matching the regex `^\[ОГК short 1\.0\.0\.canvas\] \d+ pass, 0 fail \(\d+ checks\)$` (no FAIL lines).
    - The builder stdout contains a line `→ wrote Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas`.
    - The builder stdout shows `loops: 0` (per D-04).
    - File `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` exists and is valid JSON parseable by `node -e "JSON.parse(require('node:fs').readFileSync('Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas','utf8'))"`.
    - The written canvas JSON contains the substring `"radiprotocol_nodeType":"start"` exactly once (I1 — verifiable via `grep -c '"radiprotocol_nodeType":"start"' "Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas"` returning `1`).
    - The written canvas JSON contains zero `"radiprotocol_nodeType":"loop"` substrings (per D-04).
  </acceptance_criteria>
  <done>
    `build-ogk-short.mjs` exists, runs cleanly, prints all-pass invariants, writes `ОГК short 1.0.0.canvas` to author's vault. Static layer of CANVAS-LIB-06 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 73-01-SUMMARY.md (per-canvas summary mirroring Phase 72 SUMMARY format)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md (template format — frontmatter shape + topology table + verification disposition)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-PLAN.md (this plan — for the must_haves and decisions list)
    - The just-written canvas file `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` (extract node/edge counts for the topology table)
    - The just-emitted Task 1 build log (extract `N pass, 0 fail` line for the static-invariant section)
  </read_first>
  <action>
Author `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md` mirroring the Phase 72 SUMMARY format (D-22). Use frontmatter exactly like `72-05-SUMMARY.md` but with these field overrides:

```yaml
# (frontmatter fence shown above for SUMMARY format reference)
phase: 73-canvas-library-short-algorithmic-canvases
plan: 01
id: 73-01-ogk-short-canvas
subsystem: content-authoring (Obsidian Canvas JSON, no plugin code touched)
tags:
  - canvas-authoring
  - content
  - obsidian
  - ogk
  - short
requires:
  - CANVAS-LIB-06
provides:
  - ОГК (органы грудной клетки) short-version algorithmic canvas
  - 4 linear sections per D-01 (ЛЕГКИЕ, ТРАХЕЯ И БРОНХИ, СРЕДОСТЕНИЕ, ПЛЕВРАЛЬНЫЕ ПОЛОСТИ)
  - Per-system Норма? gate with verbatim normal text (D-02)
  - Terminal Заключение with `==диагноз==` Phase-27 chip + Рекомендации (D-03, D-13)
  - Zero loops (D-04), zero contrast fan-out, zero procedure preamble
affects:
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas (author's vault — not in repo per D-16/D-20)"
tech-stack:
  added: []
patterns:
  - Single linear trunk top-to-bottom — no fan-out, no loops
  - section() + converge() helpers reused from Phase 72 build-pkop.mjs
  - Phase-27 ==fill-in== chip on Заключение line for clinical diagnosis substitution
key-files:
  created:
    - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОГК short 1.0.0.canvas"
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs
  modified: []
key-decisions:
  - "D-01..D-22 (per-decision list — reuse the must_haves.decisions array from this PLAN.md)"
requirements-completed:
  - CANVAS-LIB-06
verification:
  static: passed (N/N invariant checks I1-I10; replace N with the actual builder output count)
  manual: pending (Task 3 — author runs in Obsidian)
# (frontmatter fence shown above for SUMMARY format reference)
```

After the frontmatter, write these sections:

1. **`## Summary`** — one paragraph stating that the ОГК short single-trunk linear canvas was authored programmatically; that it has zero loops and zero fan-out per CONTEXT.md D-04; that the verbatim norm text comes from `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 (byte-equivalent in муж file).

2. **`## Topology`** — Markdown table extracted from the canvas JSON written by Task 1:
   | Metric | Value |
   |---|---|
   | Nodes | (count from JSON) |
   | Edges | (count from JSON) |
   | Sections | 4 (ЛЕГКИЕ, ТРАХЕЯ И БРОНХИ, СРЕДОСТЕНИЕ, ПЛЕВРАЛЬНЫЕ ПОЛОСТИ) |
   | Loops | 0 (per D-04) |
   | Snippet nodes | (count from JSON) |
   | Free-text `==…==` chips | (count of distinct chips: at minimum 4 ==напишу что не так с …== chips + 1 ==диагноз== chip) |
   | Start nodes | 1 |

3. **`## Snippet Paths Used`** — table listing the 3 snippet folder paths used (`ОГК/ЛЕГКИЕ`, `ОГК/СРЕДОСТЕНИЕ`, `ОГК/ПЛЕВРАЛЬНЫЕ ПОЛОСТИ`), confirming no cross-canvas leakage (no `ОБП`, `ОМТ`, `ГМ`, `ОЗП`, `ПОЗВОНОЧНИК`, `КОСТИ` paths used).

4. **`## Static Invariant Results (I1-I10)`** — paste the relevant `[ОГК short 1.0.0.canvas] N pass, 0 fail` line from the Task 1 builder output, then enumerate which invariants apply (I1, I4×{snippet count}, I5×4 sections, I6, I7, I8, I9, I10) and note that I2/I3 are vacuously satisfied (no loops per D-04).

5. **`## Manual Verification (Task 3)`** — checklist of steps the author follows in Obsidian (mirror Phase 72 SUMMARY.md format):
   1. Open canvas in Obsidian. Click "Run from start".
   2. Walk all 4 sections (ЛЕГКИЕ → ТРАХЕЯ И БРОНХИ → СРЕДОСТЕНИЕ → ПЛЕВРАЛЬНЫЕ ПОЛОСТИ), picking norm/freetext/snippet at each.
   3. Confirm terminal Заключение/Рекомендации text-block emits and the `==диагноз==` chip is clickable.
   4. Visual diff output against `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 + 19-26.
   5. Type "approved" to mark CANVAS-LIB-06.

6. **`## Build Reproducibility`** — single command line: `Re-run: \`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-ogk-short.mjs\`.`
  </action>
  <verify>
    <automated>test -f .planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md && grep -q "CANVAS-LIB-06" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md && grep -q "loops: 0\|Loops | 0\|Loops: 0" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md` exists.
    - The frontmatter contains `phase: 73-canvas-library-short-algorithmic-canvases` (verifiable via grep).
    - The frontmatter contains `plan: 01` (verifiable via grep).
    - The frontmatter `requires:` list contains `CANVAS-LIB-06` (verifiable via grep -F).
    - The body contains a `## Topology` table.
    - The body asserts `Loops | 0` or equivalent zero-loop value (per D-04).
    - The body contains the substring `pass, 0 fail` (the static-invariant pass line copied from the builder output).
    - The body lists the snippet paths `ОГК/ЛЕГКИЕ`, `ОГК/СРЕДОСТЕНИЕ`, `ОГК/ПЛЕВРАЛЬНЫЕ ПОЛОСТИ` and confirms no cross-canvas leakage (no `ОБП`, `ОМТ`, `ГМ`, `ОЗП`, `ПОЗВОНОЧНИК` paths in the snippet table).
    - The body contains a `## Manual Verification` section with at least 5 numbered steps.
  </acceptance_criteria>
  <done>
    73-01-SUMMARY.md exists with full frontmatter, topology table, snippet-path audit, static-invariant results, and manual-verification checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author runs ОГК short canvas end-to-end in Obsidian and confirms output matches short-MD reference</name>
  <files>ОГК short 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. See the <how-to-verify> block below for the step-by-step. The action is: open the generated canvas in Obsidian Protocol Runner, run from start, walk every applicable branch, visually diff the assembled output against the corresponding section of the source short MD template at Z:/projects/references/.</action>
  <verify><automated>echo "manual checkpoint — see how-to-verify block; resume with author approval"</automated></verify>
  <done>Author types "approved" after confirming the canvas runs end-to-end in Obsidian Protocol Runner, output matches the source short MD reference, and CANVAS-LIB-06 is satisfied.</done>
  <what-built>
    `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` — single-trunk linear canvas with 4 organ-system sections + terminal Заключение/Рекомендации, zero loops, zero fan-out, programmatically generated with all I1-I10 static invariants passing.
  </what-built>
  <how-to-verify>
    1. Open Obsidian on the `TEST-BASE` vault.
    2. Open `Protocols/ОГК short 1.0.0.canvas`.
    3. From the Protocol Runner sidebar/inline, click "Run from start".
    4. At each of the 4 organ-system Question prompts («ЛЕГКИЕ — норма?», «ТРАХЕЯ И БРОНХИ — норма?», «СРЕДОСТЕНИЕ — норма?», «ПЛЕВРАЛЬНЫЕ ПОЛОСТИ — норма?»):
       - Pick **Норма** on at least one walkthrough — confirm the corresponding verbatim norm text from `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` line 4/5/6/7 appears in the running output.
       - On a second walkthrough, pick **Опишу вручную** at one section — confirm the `==напишу что не так с …==` chip is rendered and is clickable (Phase 27 fill-in modal opens).
       - On a third walkthrough, pick **Выберу шаблон** at sections that have snippet pickers — confirm the snippet picker shows entries from `SNIPPETS\ОГК\ЛЕГКИЕ\`, `SNIPPETS\ОГК\СРЕДОСТЕНИЕ\`, or `SNIPPETS\ОГК\ПЛЕВРАЛЬНЫЕ ПОЛОСТИ\` (and ONLY those — no cross-canvas leakage from `ОБП`/`ОМТ`/etc. per D-09 + Pitfall 9).
    5. Confirm the runner walks through to a terminal Заключение/Рекомендации output that contains:
       - `## Заключение` heading
       - `РКТ-признаки [some text — fill the ==диагноз== chip]` (chip is clickable per Phase 27)
       - `## Рекомендации` heading
       - `Консультация направившего специалиста.`
    6. Visual diff the runner's emitted output against `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 + 19-26 — every sentence from the source MD must be present in some path of the canvas walkthrough.
    7. Confirm zero parser errors, zero deadlocks, zero unreachable nodes.
    8. Confirm Phase 70 `+`-prefix loop-exit visual hint is NOT present anywhere (no loops in this canvas per D-04).
  </how-to-verify>
  <resume-signal>Type "approved" to mark CANVAS-LIB-06 as manually verified, OR describe issues (e.g., "section X norm text differs from MD source", "snippet picker shows ОБП folder entries — cross-canvas leakage", "terminal Заключение chip not clickable").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File-system writes | Builder writes a `.canvas` file outside the repo working tree to `Z:\documents\vaults\TEST-BASE\Protocols\` — author-controlled local path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01-01 | T | build-ogk-short.mjs `writeFileSync` to author vault | accept | Author-controlled local file write to a fixed absolute path; no untrusted input feeds the path; `.canvas` files are not bundled with plugin distribution per D-16/D-20 |
| T-73-01-02 | I | Canvas content (Russian medical text strings) | accept | All content is verbatim from `Z:\projects\references\short - ОГК ОБП ОМТ {жен,муж}.md` (author's own reference templates); no PII, no external data, no untrusted input |

(Phase 73 is content-authoring with no plugin code change — security surface is minimal. STRIDE register documented for completeness per `security_enforcement` default.)
</threat_model>

<verification>
- All three tasks above pass their `<acceptance_criteria>`.
- Static layer (Tasks 1-2): `node …/build-ogk-short.mjs` exits 0, prints `N pass, 0 fail`, writes valid JSON to author's vault; SUMMARY.md committed.
- Manual layer (Task 3): author confirms end-to-end runner walkthrough matches `short - ОГК ОБП ОМТ жен.md` reference and types "approved".
- I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged per D-17 — builder prints invariant pass count.
</verification>

<success_criteria>
- `Z:\documents\vaults\TEST-BASE\Protocols\ОГК short 1.0.0.canvas` exists, valid JSON, runs end-to-end in Obsidian Protocol Runner.
- All static invariants I1-I10 pass (verifiable via builder script exit code + stdout).
- Author confirms manual walkthrough produces output structurally matching `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 4-7 + 19-26.
- 73-01-SUMMARY.md committed to `.planning/phases/73-canvas-library-short-algorithmic-canvases/` per D-22.
- Zero cross-canvas snippet leakage (only `ОГК/*` paths used).
- CANVAS-LIB-06 marked complete in REQUIREMENTS.md after author approval.
</success_criteria>

<output>
After completion, create `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-01-SUMMARY.md` (Task 2 explicitly produces this file).
</output>
