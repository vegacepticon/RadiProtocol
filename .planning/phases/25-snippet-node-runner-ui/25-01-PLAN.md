---
phase: 25-snippet-node-runner-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/graph-model.ts
  - src/graph/canvas-parser.ts
  - src/graph/graph-validator.ts
  - src/runner/runner-state.ts
  - src/runner/protocol-runner.ts
autonomous: true
requirements:
  - SNIPPET-02
must_haves:
  truths:
    - "SnippetNode interface is exported from graph-model.ts with folderPath? и buttonLabel? полями"
    - "RPNodeKind union включает 'snippet'"
    - "canvas-parser.ts парсит ноды с radiprotocol_nodeType='snippet' в SnippetNode объекты"
    - "graph-validator.ts не падает и не выдаёт ошибок для snippet-ноды с одним входящим и одним исходящим ребром"
    - "AtNodeState.isAtSnippetNode?: boolean добавлен в runner-state.ts"
    - "ProtocolRunner.advanceThrough() останавливается на snippet-ноде в состоянии 'at-node'"
    - "ProtocolRunner.getState() возвращает isAtSnippetNode: true когда текущая нода — snippet"
    - "ProtocolRunner.completeSnippetFile(text, nodeId) добавляет текст через defaultSeparator и продвигает runner"
    - "npx tsc --noEmit проходит без ошибок"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "SnippetNode interface, RPNodeKind union с 'snippet'"
      contains: "export interface SnippetNode"
    - path: "src/runner/runner-state.ts"
      provides: "AtNodeState.isAtSnippetNode?: boolean"
      contains: "isAtSnippetNode"
    - path: "src/runner/protocol-runner.ts"
      provides: "completeSnippetFile() метод, halt в advanceThrough()"
      contains: "completeSnippetFile"
  key_links:
    - from: "src/runner/protocol-runner.ts advanceThrough()"
      to: "at-node state"
      via: "case 'snippet': halt"
      pattern: "case 'snippet'"
    - from: "src/runner/protocol-runner.ts getState()"
      to: "AtNodeState.isAtSnippetNode"
      via: "graph.nodes.get(currentNodeId)?.kind === 'snippet'"
      pattern: "isAtSnippetNode"
---

<objective>
Добавить весь graph/runner layer для snippet-ноды: модель, парсер, валидатор, halt-логика runner'а, метод advance.

Purpose: Wave 2 (UI) невозможна без этого layer — нужны типы SnippetNode и isAtSnippetNode, halt в advanceThrough, и метод completeSnippetFile для завершения выбора файла.

Output: Пять изменённых файлов; `npx tsc --noEmit` чистый; runner останавливается на snippet-ноде.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-snippet-node-runner-ui/25-CONTEXT.md
@.planning/phases/25-snippet-node-runner-ui/25-RESEARCH.md

<interfaces>
<!-- Текущее состояние файлов которые будут изменены. Executor читает их перед правками. -->

Из src/graph/graph-model.ts (текущий HEAD):
```typescript
export type RPNodeKind =
  | 'start' | 'question' | 'answer' | 'free-text-input'
  | 'text-block' | 'loop-start' | 'loop-end';
// SnippetNode ОТСУТСТВУЕТ — нужно добавить

export type RPNode =
  | StartNode | QuestionNode | AnswerNode | FreeTextInputNode
  | TextBlockNode | LoopStartNode | LoopEndNode;
// SnippetNode нужно добавить в union
```

Из src/runner/runner-state.ts (текущий HEAD):
```typescript
export interface AtNodeState {
  status: 'at-node';
  currentNodeId: string;
  accumulatedText: string;
  canStepBack: boolean;
  loopIterationLabel?: string;
  isAtLoopEnd?: boolean;
  // isAtSnippetNode?: boolean — ОТСУТСТВУЕТ, нужно добавить
}
```

Из src/runner/protocol-runner.ts — сигнатура resolveSeparator:
```typescript
private resolveSeparator(
  node: AnswerNode | FreeTextInputNode | TextBlockNode,
): 'newline' | 'space' {
  return node.radiprotocol_separator ?? this.defaultSeparator;
}
// SnippetNode не имеет radiprotocol_separator — использовать this.defaultSeparator напрямую
```

Из src/runner/protocol-runner.ts — паттерн halt в advanceThrough (loop-end для справки):
```typescript
case 'loop-end': {
  this.currentNodeId = cursor;
  this.runnerStatus = 'at-node';
  return;
}
```

Из src/runner/protocol-runner.ts — паттерн completeSnippet для справки:
```typescript
completeSnippet(renderedText: string): void {
  if (this.runnerStatus !== 'awaiting-snippet-fill') return;
  if (this.graph === null || this.snippetNodeId === null) return;
  const pendingNodeId = this.snippetNodeId;
  const snippetNode = this.graph.nodes.get(pendingNodeId);
  const snippetSep = (snippetNode?.kind === 'text-block')
    ? this.resolveSeparator(snippetNode)
    : this.defaultSeparator;
  this.accumulator.appendWithSeparator(renderedText, snippetSep);
  // ... advance through neighbors
}
```

Из src/graph/canvas-parser.ts — validKinds массив который нужно расширить:
```typescript
const validKinds: RPNodeKind[] = [
  'start', 'question', 'answer', 'free-text-input',
  'text-block', 'loop-start', 'loop-end',
];
// Добавить: 'snippet'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить SnippetNode в graph-model, canvas-parser, graph-validator</name>
  <files>src/graph/graph-model.ts, src/graph/canvas-parser.ts, src/graph/graph-validator.ts</files>
  <read_first>
    - src/graph/graph-model.ts — текущее состояние RPNodeKind union и RPNode union
    - src/graph/canvas-parser.ts — validKinds массив, parseNode switch, существующие случаи (loop-end для паттерна)
    - src/graph/graph-validator.ts — validate() метод, nodeLabel() хелпер
  </read_first>
  <action>
**graph-model.ts:**

1. Добавить `'snippet'` в `RPNodeKind` union после `'loop-end'`:
```typescript
export type RPNodeKind =
  | 'start'
  | 'question'
  | 'answer'
  | 'free-text-input'
  | 'text-block'
  | 'loop-start'
  | 'loop-end'
  | 'snippet';
```

2. Добавить `SnippetNode` интерфейс после `LoopEndNode`:
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  /** Per-node folder override (Phase 25). Overrides global snippetNodeFolderPath setting. */
  folderPath?: string;
  /** Button label in runner. Fallback chain: buttonLabel ?? canvasNodeText ?? "Select file" (D-09). */
  buttonLabel?: string;
}
```

3. Добавить `SnippetNode` в `RPNode` union:
```typescript
export type RPNode =
  | StartNode
  | QuestionNode
  | AnswerNode
  | FreeTextInputNode
  | TextBlockNode
  | LoopStartNode
  | LoopEndNode
  | SnippetNode;
```

**canvas-parser.ts:**

1. Добавить импорт `SnippetNode` из `'./graph-model'` (добавить к существующим импортам):
```typescript
import type {
  RPNode, RPNodeKind, RPEdge, ProtocolGraph, ParseResult,
  StartNode, QuestionNode, AnswerNode, FreeTextInputNode,
  TextBlockNode, LoopStartNode, LoopEndNode,
  SnippetNode,  // ДОБАВИТЬ
} from './graph-model';
```

2. Добавить `'snippet'` в `validKinds` массив:
```typescript
const validKinds: RPNodeKind[] = [
  'start', 'question', 'answer', 'free-text-input',
  'text-block', 'loop-start', 'loop-end', 'snippet',
];
```

3. Добавить `case 'snippet':` в switch внутри `parseNode()`, после `case 'loop-end':`:
```typescript
case 'snippet': {
  const node: SnippetNode = {
    ...base,
    kind: 'snippet',
    folderPath: typeof props['radiprotocol_folderPath'] === 'string' && props['radiprotocol_folderPath'] !== ''
      ? getString(props, 'radiprotocol_folderPath')
      : undefined,
    // D-09: fallback chain — radiprotocol_buttonLabel ?? raw.text (canvas node text) ?? undefined
    // Runner UI then does: buttonLabel ?? "Select file"
    buttonLabel: (typeof props['radiprotocol_buttonLabel'] === 'string' && props['radiprotocol_buttonLabel'] !== ''
      ? getString(props, 'radiprotocol_buttonLabel')
      : (raw.text ?? '')) || undefined,
  };
  return node;
}
```

**graph-validator.ts:**

Snippet-нода является терминирующей точкой взаимодействия (не question и не loop-end), поэтому валидатор не должен помечать её как "dead-end". Проверить существующие правила:
- Check 5 (dead-end questions) — касается только `kind === 'question'`, snippet не затрагивается.
- Check 6 (orphaned loop-end) — не затрагивает snippet.
- Validator не имеет exhaustive-switch по kind — изменений не требуется.

Убедиться, что `nodeLabel()` хелпер корректно обрабатывает `SnippetNode` (он использует `(node as QuestionNode).questionText` — если такого поля нет, вернёт `node.id`). Если `nodeLabel()` содержит exhaustive switch — добавить `case 'snippet': return node.id;`. Если это просто if-else без exhaustive check — изменений не нужно.
  </action>
  <verify>
    <automated>cd /z/projects/RadiProtocolObsidian && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/graph/graph-model.ts содержит строку `| 'snippet';` в RPNodeKind union
    - src/graph/graph-model.ts содержит `export interface SnippetNode extends RPNodeBase`
    - src/graph/graph-model.ts содержит `SnippetNode` в RPNode union
    - src/graph/canvas-parser.ts содержит `'snippet'` в validKinds массиве
    - src/graph/canvas-parser.ts содержит `case 'snippet':` в parseNode switch
    - src/graph/canvas-parser.ts импортирует `SnippetNode` из graph-model
    - `npx tsc --noEmit` завершается с кодом 0 (без ошибок)
  </acceptance_criteria>
  <done>graph layer для snippet-ноды добавлен; TypeScript компилируется без ошибок</done>
</task>

<task type="auto">
  <name>Task 2: Добавить halt в runner и completeSnippetFile(), isAtSnippetNode в state</name>
  <files>src/runner/runner-state.ts, src/runner/protocol-runner.ts</files>
  <read_first>
    - src/runner/runner-state.ts — текущий AtNodeState интерфейс
    - src/runner/protocol-runner.ts — advanceThrough() switch (особенно case 'loop-end'), getState() case 'at-node', completeSnippet() метод, resolveSeparator() сигнатура
  </read_first>
  <action>
**runner-state.ts:**

Добавить `isAtSnippetNode?: boolean` в `AtNodeState` интерфейс, после `isAtLoopEnd`:
```typescript
export interface AtNodeState {
  status: 'at-node';
  currentNodeId: string;
  accumulatedText: string;
  canStepBack: boolean;
  loopIterationLabel?: string;
  isAtLoopEnd?: boolean;
  /** true when currentNodeId refers to a snippet node — drives file-picker UI (SNIPPET-02). */
  isAtSnippetNode?: boolean;
}
```

**protocol-runner.ts:**

**Изменение 1 — импорт SnippetNode:**
Добавить `SnippetNode` к импорту из `'../graph/graph-model'`:
```typescript
import type { ProtocolGraph, LoopContext, SnippetNode } from '../graph/graph-model';
```

**Изменение 2 — halt в advanceThrough():**
В switch внутри `advanceThrough()`, ПЕРЕД `default:` блоком, добавить `case 'snippet':`:
```typescript
case 'snippet': {
  // Halt — RunnerView renders file-picker button (SNIPPET-02)
  this.currentNodeId = cursor;
  this.runnerStatus = 'at-node';
  return;
}
```

**Изменение 3 — isAtSnippetNode в getState():**
В `case 'at-node':` внутри `getState()`, добавить `isAtSnippetNode` рядом с `isAtLoopEnd`:
```typescript
return {
  status: 'at-node',
  currentNodeId: this.currentNodeId ?? '',
  accumulatedText: this.accumulator.current,
  canStepBack: this.undoStack.length > 0,
  loopIterationLabel,
  isAtLoopEnd: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'loop-end',
  isAtSnippetNode: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'snippet',
};
```

**Изменение 4 — новый публичный метод completeSnippetFile():**
Добавить после метода `completeSnippet()` в секции Public API:
```typescript
/**
 * Called by RunnerView after user selects a file for the snippet node (SNIPPET-04, SNIPPET-05).
 * Appends text using the runner's defaultSeparator and advances past the snippet node.
 * Only valid in at-node state with the current node being a snippet node (D-08).
 * Pushes UndoEntry BEFORE mutation (BUG-01 pattern).
 *
 * @param text - rendered file content to append
 * @param snippetNodeId - the ID of the snippet node being completed
 */
completeSnippetFile(text: string, snippetNodeId: string): void {
  if (this.runnerStatus !== 'at-node') return;
  if (this.graph === null) return;

  const snippetNode = this.graph.nodes.get(snippetNodeId);
  if (snippetNode === undefined || snippetNode.kind !== 'snippet') return;

  // Push undo entry BEFORE mutation (BUG-01 / D-08)
  this.undoStack.push({
    nodeId: snippetNodeId,
    textSnapshot: this.accumulator.snapshot(),
    loopContextStack: [...this.loopContextStack],
  });

  // SnippetNode does not have radiprotocol_separator — use defaultSeparator (RESEARCH.md Pattern 6)
  this.accumulator.appendWithSeparator(text, this.defaultSeparator);

  // Advance past the snippet node
  const neighbors = this.graph.adjacency.get(snippetNodeId);
  const next = neighbors !== undefined ? neighbors[0] : undefined;
  if (next === undefined) {
    this.transitionToComplete();
    return;
  }
  this.advanceThrough(next);
}
```

**Важно:** Неиспользуемый импорт `SnippetNode` нужен для TypeScript exhaustive-check в advanceThrough — убедиться, что он используется (хотя бы в JSDoc комментарии или как тип переменной в case 'snippet' если нужно явно). Если lint выдаёт предупреждение об unused import — удалить импорт SnippetNode (он не обязателен, т.к. в case 'snippet' типа нода уже определена через node: RPNode).
  </action>
  <verify>
    <automated>cd /z/projects/RadiProtocolObsidian && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/runner/runner-state.ts содержит `isAtSnippetNode?: boolean` в AtNodeState
    - src/runner/protocol-runner.ts содержит `case 'snippet':` в advanceThrough() switch
    - src/runner/protocol-runner.ts содержит `isAtSnippetNode:` в getState() case 'at-node'
    - src/runner/protocol-runner.ts содержит `completeSnippetFile(text: string, snippetNodeId: string): void`
    - src/runner/protocol-runner.ts в completeSnippetFile содержит `this.undoStack.push(` (BUG-01 паттерн)
    - src/runner/protocol-runner.ts в completeSnippetFile содержит `this.accumulator.appendWithSeparator(text, this.defaultSeparator)`
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>Runner layer полностью добавлен: halt на snippet, isAtSnippetNode в state, completeSnippetFile для advance</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas JSON → parser | Недоверенные данные из .canvas файла; парсер уже валидирует строки/типы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | canvas-parser.ts parseNode | accept | folderPath/buttonLabel читаются из radiprotocol_* полей canvas файла, который является локальным vault файлом пользователя; getString() sanitizes to empty string on non-string values |
</threat_model>

<verification>
После обоих задач: `npx tsc --noEmit` должен завершиться с кодом 0.

Structural checks (grep):
- `grep -n "snippet" src/graph/graph-model.ts` — должен показать SnippetNode, RPNodeKind union
- `grep -n "isAtSnippetNode" src/runner/runner-state.ts src/runner/protocol-runner.ts` — минимум 2 вхождения
- `grep -n "completeSnippetFile" src/runner/protocol-runner.ts` — минимум 1 вхождение
</verification>

<success_criteria>
- SnippetNode полностью определён в graph layer (model, parser, validator)
- Runner останавливается на snippet-ноде в at-node состоянии
- getState() возвращает isAtSnippetNode: true для snippet-ноды
- completeSnippetFile() корректно добавляет текст и продвигает runner
- npx tsc --noEmit зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/25-snippet-node-runner-ui/25-01-SUMMARY.md`
</output>
