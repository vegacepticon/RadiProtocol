---
phase: 25-snippet-node-runner-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/views/snippet-file-picker-modal.ts
  - src/views/runner-view.ts
autonomous: true
requirements:
  - SNIPPET-02
  - SNIPPET-03
  - SNIPPET-04
  - SNIPPET-05
  - SNIPPET-07
must_haves:
  truths:
    - "В Protocol Runner на snippet-ноде отображается кнопка с классом rp-answer-btn"
    - "Кнопка открывает FuzzySuggestModal только с .md и .json файлами из настроенной папки (рекурсивно)"
    - "Выбор .md файла добавляет его plain-text содержимое в textarea и продвигает runner"
    - "Выбор .json файла (валидный SnippetFile) открывает SnippetFillInModal; заполненный текст добавляется и runner продвигается"
    - "Отмена picker'а (Esc) или SnippetFillInModal оставляет runner на snippet-ноде без изменений"
    - "per-node folderPath имеет приоритет над global snippetNodeFolderPath"
    - "Если папка не настроена — Notice и runner остаётся"
    - "Если папка пуста (нет .md/.json) — Notice и runner остаётся"
    - "Невалидный .json файл — Notice и runner остаётся"
    - "npx tsc --noEmit проходит без ошибок"
  artifacts:
    - path: "src/views/snippet-file-picker-modal.ts"
      provides: "SnippetFilePickerModal — FuzzySuggestModal<TFile> subclass"
      exports: ["SnippetFilePickerModal"]
      contains: "extends FuzzySuggestModal"
    - path: "src/views/runner-view.ts"
      provides: "case 'snippet': branch в at-node switch, handleSnippetFilePick() метод"
      contains: "case 'snippet':"
  key_links:
    - from: "runner-view.ts case 'snippet': кнопка"
      to: "SnippetFilePickerModal.open()"
      via: "registerDomEvent click handler"
      pattern: "SnippetFilePickerModal"
    - from: "runner-view.ts handleSnippetFilePick()"
      to: "runner.completeSnippetFile(text, nodeId)"
      via: "vault.read() для .md; SnippetFillInModal для .json"
      pattern: "completeSnippetFile"
---

<objective>
Реализовать UI слой snippet-ноды: SnippetFilePickerModal и ветку рендеринга в runner-view.

Purpose: Пользователь получает рабочий file picker для snippet-ноды — нажимает кнопку, выбирает файл, текст вставляется в протокол.

Output: Новый файл SnippetFilePickerModal + изменения runner-view.ts; все 5 требований фазы выполнены.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-snippet-node-runner-ui/25-CONTEXT.md
@.planning/phases/25-snippet-node-runner-ui/25-UI-SPEC.md
@.planning/phases/25-snippet-node-runner-ui/25-01-SUMMARY.md

<interfaces>
<!-- Контракты из Wave 1 и существующего кода. Executor не должен исследовать кодовую базу. -->

Из src/runner/runner-state.ts (после Wave 1):
```typescript
export interface AtNodeState {
  status: 'at-node';
  currentNodeId: string;
  accumulatedText: string;
  canStepBack: boolean;
  loopIterationLabel?: string;
  isAtLoopEnd?: boolean;
  isAtSnippetNode?: boolean;  // ДОБАВЛЕНО в Wave 1
}
```

Из src/runner/protocol-runner.ts (после Wave 1):
```typescript
// Новый публичный метод:
completeSnippetFile(text: string, snippetNodeId: string): void;
// Сигнатура syncManualEdit (BUG-01 pattern, нужен перед advance):
syncManualEdit(text: string): void;
```

Из src/graph/graph-model.ts (после Wave 1):
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  folderPath?: string;   // per-node override
  buttonLabel?: string;  // fallback: buttonLabel ?? canvasNodeText ?? "Select file"
}
```

Из src/settings.ts (уже существует):
```typescript
export interface RadiProtocolSettings {
  snippetNodeFolderPath: string;  // '' = not configured (Phase 24)
  // ...
}
```

Из src/views/node-picker-modal.ts (паттерн для нового модального):
```typescript
// SuggestModal pattern — для справки; SnippetFilePickerModal использует FuzzySuggestModal
export class NodePickerModal extends SuggestModal<NodeOption> {
  constructor(app: App, options: NodeOption[], onChoose: (option: NodeOption) => void) {
    super(app);
    this.setPlaceholder('Search nodes by label\u2026');
  }
  getSuggestions(query: string): NodeOption[] { ... }
  renderSuggestion(option: NodeOption, el: HTMLElement): void { ... }
  onChooseSuggestion(option: NodeOption, _evt: MouseEvent | KeyboardEvent): void { ... }
}
```

Из src/views/snippet-fill-in-modal.ts (существующий, для .json dispatch):
```typescript
// SnippetFillInModal принимает SnippetFile и возвращает promise с rendered text или null (cancel)
// Паттерн использования в runner-view.ts:
const modal = new SnippetFillInModal(this.app, snippet);
modal.open();
const rendered = await modal.result;
if (rendered !== null) { ... }
```

Из src/views/runner-view.ts — паттерн рендеринга кнопок ответа (question branch):
```typescript
case 'question': {
  // ...
  const btn = answerList.createEl('button', {
    cls: 'rp-answer-btn',
    text: answerNode.displayLabel ?? answerNode.answerText,
  });
  this.registerDomEvent(btn, 'click', () => {
    this.runner.syncManualEdit(this.previewTextarea?.value ?? '');  // BUG-01
    this.runner.chooseAnswer(answerNode.id);
    void this.autoSaveSession();
    void this.renderAsync();
  });
  break;
}
```

Из src/views/runner-view.ts — паттерн async handler (awaiting-snippet-fill branch):
```typescript
case 'awaiting-snippet-fill': {
  questionZone.createEl('p', { text: 'Loading snippet...', cls: 'rp-empty-state-body' });
  // ...
  void this.handleSnippetFill(state.snippetId, questionZone);
  break;
}

private async handleSnippetFill(snippetId: string, questionZone: HTMLElement): Promise<void> {
  // ...
}
```

Из src/views/runner-view.ts — импорты (для справки, какие уже есть):
```typescript
import { ItemView, WorkspaceLeaf, Notice, TFile, TFolder, MarkdownView } from 'obsidian';
import { SnippetFillInModal } from './snippet-fill-in-modal';
```

Тип SnippetFile из ARCHITECTURE.md (для .json валидации D-06):
```typescript
// SnippetFile schema — поля которые должны присутствовать:
{
  id: string;
  name: string;
  template: string;
  placeholders?: Array<{ id: string; label: string; defaultValue?: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать SnippetFilePickerModal (FuzzySuggestModal subclass)</name>
  <files>src/views/snippet-file-picker-modal.ts</files>
  <read_first>
    - src/views/node-picker-modal.ts — существующий SuggestModal паттерн в кодовой базе
    - .planning/phases/25-snippet-node-runner-ui/25-RESEARCH.md §Pattern 2 — пример реализации FuzzySuggestModal
  </read_first>
  <action>
Создать новый файл `src/views/snippet-file-picker-modal.ts`:

```typescript
// views/snippet-file-picker-modal.ts
// FuzzySuggestModal for selecting a snippet file from a scoped folder (SNIPPET-03, D-01, D-02)
import { App, FuzzySuggestModal, TFile } from 'obsidian';

/**
 * File picker modal for snippet nodes in the Protocol Runner.
 *
 * Accepts a pre-filtered list of TFile objects (scoped to the configured folder).
 * Displays only filename (D-01 — no path prefix).
 * Calls onChoose callback when a file is selected.
 * On cancel (Esc), closes without calling the callback (D-03 — runner stays on node).
 */
export class SnippetFilePickerModal extends FuzzySuggestModal<TFile> {
  private readonly files: TFile[];
  private readonly onChooseCb: (file: TFile) => void;
  private resolved = false;

  constructor(app: App, files: TFile[], onChoose: (file: TFile) => void) {
    super(app);
    this.files = files;
    this.onChooseCb = onChoose;
    this.setPlaceholder('Search snippet files\u2026');
  }

  getItems(): TFile[] {
    return this.files;
  }

  getItemText(file: TFile): string {
    return file.name;  // D-01: filename only, no path prefix
  }

  onChooseItem(file: TFile, _evt: MouseEvent | KeyboardEvent): void {
    this.resolved = true;
    this.onChooseCb(file);
  }

  onClose(): void {
    // D-03: Esc (or any dismiss without selection) = stay on node.
    // resolved guard prevents double-call if onChooseItem already fired.
    // No action needed — runner-view's click handler awaits the callback; silence = cancel.
    super.onClose();
  }
}
```

**Примечания:**
- `FuzzySuggestModal<T>` требует `getItems()` и `getItemText()` — НЕ `getSuggestions()` и `renderSuggestion()` (те используются в SuggestModal<T>).
- `onChooseItem` — правильное название для FuzzySuggestModal (не `onChooseSuggestion`).
- Файл не должен импортировать ничего из Obsidian кроме `App, FuzzySuggestModal, TFile`.
  </action>
  <verify>
    <automated>cd /z/projects/RadiProtocolObsidian && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/views/snippet-file-picker-modal.ts существует
    - Файл содержит `export class SnippetFilePickerModal extends FuzzySuggestModal`
    - Файл содержит `getItems(): TFile[]`
    - Файл содержит `getItemText(file: TFile): string`
    - Файл содержит `return file.name` (D-01: только имя файла)
    - Файл содержит `onChooseItem(file: TFile`
    - Файл содержит `setPlaceholder('Search snippet files`
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>SnippetFilePickerModal создан и компилируется</done>
</task>

<task type="auto">
  <name>Task 2: Добавить case 'snippet' и handleSnippetFilePick() в runner-view.ts</name>
  <files>src/views/runner-view.ts</files>
  <read_first>
    - src/views/runner-view.ts — весь файл (критично: at-node switch, импорты, autoSaveSession вызов, handleSnippetFill паттерн)
    - src/views/snippet-fill-in-modal.ts — конструктор и result property (для .json dispatch)
    - src/graph/graph-model.ts — SnippetNode интерфейс (добавлен в Wave 1)
  </read_first>
  <action>
**Изменение 1 — Добавить импорт SnippetFilePickerModal:**
В список импортов добавить:
```typescript
import { SnippetFilePickerModal } from './snippet-file-picker-modal';
```
Также добавить `SnippetNode` в импорт из graph-model (если ещё не импортирован):
```typescript
import type { ProtocolGraph, SnippetNode } from '../graph/graph-model';
```

**Изменение 2 — Добавить case 'snippet': в at-node switch:**

В `render()` метод, в `switch (node.kind)`, ПЕРЕД `default:` кейсом, добавить:

```typescript
case 'snippet': {
  const snippetNode = node as SnippetNode;
  // D-09: button label fallback chain: buttonLabel ?? canvas node text ?? "Select file"
  // Note: RPNodeBase does not have a 'text' field; canvas node text is stored in
  // radiprotocol_buttonLabel or as buttonLabel on SnippetNode itself.
  const resolvedLabel = snippetNode.buttonLabel ?? 'Select file';

  const snippetBtn = questionZone.createEl('button', {
    cls: 'rp-answer-btn',
    text: resolvedLabel,
  });
  this.registerDomEvent(snippetBtn, 'click', () => {
    this.runner.syncManualEdit(this.previewTextarea?.value ?? '');  // BUG-01 pattern
    void this.handleSnippetFilePick(snippetNode, state.currentNodeId);
  });
  break;
}
```

**Изменение 3 — Добавить приватный метод handleSnippetFilePick():**

Добавить после `handleSnippetFill()` метода:

```typescript
/**
 * Resolves folder, filters files, opens SnippetFilePickerModal, dispatches .md/.json (SNIPPET-03..07).
 * Cancel at any step leaves runner on the snippet node (D-03, D-07).
 */
private async handleSnippetFilePick(node: SnippetNode, nodeId: string): Promise<void> {
  // SNIPPET-07 / D-05: resolve effective folder (per-node override > global setting)
  const rawFolder = node.folderPath?.trim() ?? '';
  const globalFolder = this.plugin.settings.snippetNodeFolderPath.trim();
  const folderPath = rawFolder !== '' ? rawFolder : globalFolder;

  if (folderPath === '') {
    // D-05: no folder configured at all
    new Notice('Snippet folder not configured. Set a path in plugin Settings \u2192 Storage.');
    return;  // runner stays on node
  }

  // D-02: recursively filter vault files by folder prefix (.md and .json only)
  const files = this.app.vault.getFiles().filter(f =>
    (f.extension === 'md' || f.extension === 'json') &&
    f.path.startsWith(folderPath + '/')
  );

  if (files.length === 0) {
    // D-04: folder exists but no eligible files
    new Notice(`No files found in ${folderPath}`);
    return;  // runner stays on node
  }

  // Open picker — await callback via promise wrapper (D-03: cancel = null)
  const chosenFile = await new Promise<TFile | null>((resolve) => {
    new SnippetFilePickerModal(this.app, files, (file) => {
      resolve(file);
    }).open();
    // Note: cancel (Esc) never calls the callback; we rely on the modal closing
    // without resolving. To handle this cleanly, we resolve null on close via onClose override.
    // SnippetFilePickerModal does NOT resolve on close — so this promise may hang.
    // Fix: pass a resolve-null callback that fires on close.
  });

  if (chosenFile === null) {
    return;  // D-03: cancel — runner stays on node
  }

  // Dispatch: .md vs .json
  if (chosenFile.extension === 'md') {
    // SNIPPET-04: read raw text and complete
    const content = await this.app.vault.read(chosenFile);
    this.runner.completeSnippetFile(content, nodeId);
    void this.autoSaveSession();
    this.render();
  } else {
    // SNIPPET-05: validate JSON, open SnippetFillInModal
    await this.handleSnippetJsonFile(chosenFile, nodeId);
  }
}

/** D-06: validate .json as SnippetFile and open fill-in modal. */
private async handleSnippetJsonFile(file: TFile, nodeId: string): Promise<void> {
  let content: string;
  try {
    content = await this.app.vault.read(file);
  } catch {
    new Notice('Could not read file.');
    return;  // runner stays on node
  }

  let parsed: unknown;
  try {
    parsed = JSON.parse(content);
  } catch {
    new Notice('Not a valid snippet file');
    return;  // D-06: invalid JSON
  }

  // D-06: structural validation — must have id, name, template as strings
  if (
    typeof parsed !== 'object' || parsed === null ||
    typeof (parsed as Record<string, unknown>)['id'] !== 'string' ||
    typeof (parsed as Record<string, unknown>)['name'] !== 'string' ||
    typeof (parsed as Record<string, unknown>)['template'] !== 'string'
  ) {
    new Notice('Not a valid snippet file');
    return;  // D-06: missing required fields
  }

  const snippetFile = parsed as import('../snippets/snippet-model').SnippetFile;

  // SNIPPET-05: open SnippetFillInModal
  const modal = new SnippetFillInModal(this.app, snippetFile);
  modal.open();
  const rendered = await modal.result;

  if (rendered === null) {
    // D-07: user cancelled SnippetFillInModal — runner stays on node
    return;
  }

  this.runner.completeSnippetFile(rendered, nodeId);
  void this.autoSaveSession();
  this.render();
}
```

**ВАЖНО — проблема с Promise/cancel в SnippetFilePickerModal:**

Метод выше использует `new Promise<TFile | null>()` но callback в SnippetFilePickerModal вызывается только при выборе файла, а при отмене — нет. Это значит promise зависнет при cancel.

**Исправление:** Переделать SnippetFilePickerModal чтобы принимать `onClose` callback, ИЛИ переписать handleSnippetFilePick без Promise wrapper — использовать inline callback паттерн (как в handleSnippetFill):

**Рекомендуемый подход — callback-based (без async/await для picker):**

```typescript
private async handleSnippetFilePick(node: SnippetNode, nodeId: string): Promise<void> {
  const rawFolder = node.folderPath?.trim() ?? '';
  const globalFolder = this.plugin.settings.snippetNodeFolderPath.trim();
  const folderPath = rawFolder !== '' ? rawFolder : globalFolder;

  if (folderPath === '') {
    new Notice('Snippet folder not configured. Set a path in plugin Settings \u2192 Storage.');
    return;
  }

  const files = this.app.vault.getFiles().filter(f =>
    (f.extension === 'md' || f.extension === 'json') &&
    f.path.startsWith(folderPath + '/')
  );

  if (files.length === 0) {
    new Notice(`No files found in ${folderPath}`);
    return;
  }

  // D-03: cancel = onChoose never called = runner stays on node (no render() call)
  new SnippetFilePickerModal(this.app, files, (chosenFile) => {
    void this.handleChosenSnippetFile(chosenFile, nodeId);
  }).open();
}

private async handleChosenSnippetFile(file: TFile, nodeId: string): Promise<void> {
  if (file.extension === 'md') {
    const content = await this.app.vault.read(file);
    this.runner.completeSnippetFile(content, nodeId);
    void this.autoSaveSession();
    this.render();
  } else {
    await this.handleSnippetJsonFile(file, nodeId);
  }
}
```

Этот подход корректен: callback срабатывает только при выборе; Esc закрывает модальное без вызова callback → runner остаётся на ноде (D-03).

**Использовать callback-based подход как основной.** Убрать Promise wrapper версию.

**Проверить импорт SnippetFile:** Если `snippet-model.ts` экспортирует `SnippetFile` — использовать `import type { SnippetFile } from '../snippets/snippet-model'`. Если такого файла нет — определить inline type для валидации:
```typescript
interface SnippetFileShape {
  id: string;
  name: string;
  template: string;
}
```
И кастить к `SnippetFileShape` для валидации, а к типу который ожидает `SnippetFillInModal` для вызова модального.
  </action>
  <verify>
    <automated>cd /z/projects/RadiProtocolObsidian && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/views/runner-view.ts содержит `import { SnippetFilePickerModal }` строку
    - src/views/runner-view.ts содержит `case 'snippet':` в at-node switch (в switch(node.kind))
    - src/views/runner-view.ts содержит `cls: 'rp-answer-btn'` внутри case 'snippet': (SC-1)
    - src/views/runner-view.ts содержит `handleSnippetFilePick` метод
    - src/views/runner-view.ts содержит `handleSnippetJsonFile` метод
    - src/views/runner-view.ts содержит `f.path.startsWith(folderPath + '/')` (D-02: recursive scope)
    - src/views/runner-view.ts содержит `'Snippet folder not configured` (D-05 notice)
    - src/views/runner-view.ts содержит `No files found in` (D-04 notice)
    - src/views/runner-view.ts содержит `'Not a valid snippet file'` (D-06 notice)
    - src/views/runner-view.ts содержит `completeSnippetFile(` (вызов нового метода runner'а)
    - src/views/runner-view.ts содержит `syncManualEdit(this.previewTextarea?.value ?? '')` в case 'snippet' click handler (BUG-01)
    - `npx tsc --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>UI layer полностью реализован: кнопка, picker, dispatch .md/.json, все error states, D-03..D-09 выполнены</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault file → app.vault.read() | Содержимое .md/.json файлов из vault; trusted source (локальный vault пользователя) |
| JSON.parse() → SnippetFillInModal | Произвольный JSON файл из папки пользователя |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Information Disclosure | handleSnippetJsonFile JSON.parse | mitigate | D-06 validation: проверяем typeof для id/name/template до открытия modal; невалидные JSON показывают Notice и не открывают modal |
| T-25-02-02 | Denial of Service | app.vault.getFiles() filter | accept | Фильтрация по path prefix безопасна; все файлы уже в vault пользователя; DoS нерелевантен для локального плагина |
| T-25-02-03 | Tampering | folder path injection via per-node folderPath | accept | folderPath — строка из canvas файла пользователя; .startsWith() ограничивает scope до под-дерева папки; нет shell injection рисков в Obsidian API |
</threat_model>

<verification>
После обоих задач запустить:
```bash
npx tsc --noEmit
```
Должен завершиться с кодом 0.

Ручная проверка (Obsidian):
1. Открыть Protocol Runner с canvas, содержащим snippet-ноду
2. Убедиться что кнопка отображается (SC-1 / SNIPPET-02)
3. Настроить snippetNodeFolderPath в Settings → Storage
4. Нажать кнопку — должен открыться fuzzy picker (SC-2 / SNIPPET-03)
5. Выбрать .md файл — текст должен добавиться в textarea, runner продвинуться (SC-3 / SNIPPET-04)
6. Выбрать .json snippet файл — открывается SnippetFillInModal, после fill — текст добавляется (SC-4 / SNIPPET-05)
7. Задать per-node folderPath в ноде, убедиться что он имеет приоритет (SC-5 / SNIPPET-07)
</verification>

<success_criteria>
- SC-1: Кнопка с классом rp-answer-btn отображается на snippet-ноде (SNIPPET-02)
- SC-2: Picker показывает только файлы из настроенной папки (SNIPPET-03)
- SC-3: .md файл добавляет текст и продвигает runner (SNIPPET-04)
- SC-4: .json snippet файл открывает SnippetFillInModal, filled text добавляется (SNIPPET-05)
- SC-5: per-node folderPath имеет приоритет над global setting (SNIPPET-07)
- npx tsc --noEmit зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/25-snippet-node-runner-ui/25-02-SUMMARY.md`
</output>
