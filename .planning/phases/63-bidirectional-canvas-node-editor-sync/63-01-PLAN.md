---
phase: 63-bidirectional-canvas-node-editor-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/edge-label-reconciler.ts
  - src/__tests__/edge-label-reconciler.test.ts
  - src/__tests__/canvas-write-back.test.ts
  - src/__tests__/fixtures/snippet-cold-open-migration.canvas
  - src/__tests__/fixtures/branching-snippet-multi-incoming.canvas
autonomous: true
requirements:
  - EDITOR-03
must_haves:
  truths:
    - "Pure reconciler treats Question→Snippet incoming edges with the same edge-wins rule as Question→Answer (per D-01, D-02, D-04)."
    - "Cold-open canvases that have radiprotocol_snippetLabel on the node but no label on the incoming edge migrate automatically on the first reconcile pass (per D-03)."
    - "EdgeLabelDiff carries a kind discriminator so downstream writers know whether a node-side change targets radiprotocol_displayLabel or radiprotocol_snippetLabel."
    - "saveLiveBatch routes snippet-kind nodeChanges to radiprotocol_snippetLabel and continues to write atomically with edgeEdits in one setData call (Phase 50 D-14 invariant preserved)."
    - "D-07 idempotency short-circuit still self-terminates the modify-event loop when nothing diverges across BOTH answer and snippet arms."
    - "Phase 50 Answer-arm tests in edge-label-reconciler.test.ts remain green; canvas-write-back.test.ts Phase 50 describe block remains green."
  artifacts:
    - path: "src/graph/edge-label-reconciler.ts"
      provides: "Snippet edge-wins arm + discriminated EdgeLabelDiff/ReconcileResult shape"
      contains: "kind: 'snippet'"
    - path: "src/__tests__/edge-label-reconciler.test.ts"
      provides: "Snippet describe block mirroring all Answer cases + cold-open + multi-incoming + mixed Answer+Snippet + idempotency"
      contains: "Phase 63"
    - path: "src/__tests__/canvas-write-back.test.ts"
      provides: "saveLiveBatch coverage routing snippet-kind nodeChanges to radiprotocol_snippetLabel + atomic node+edge write"
      contains: "snippet"
    - path: "src/__tests__/fixtures/snippet-cold-open-migration.canvas"
      provides: "Fixture: snippetLabel on node, edge.label undefined → cold-open D-03 migration target"
      contains: "radiprotocol_snippetLabel"
    - path: "src/__tests__/fixtures/branching-snippet-multi-incoming.canvas"
      provides: "Fixture: two Question nodes both incoming on one Snippet node with different labels → edge-wins resync"
      contains: "n-snip-shared"
  key_links:
    - from: "src/graph/edge-label-reconciler.ts"
      to: "src/graph/graph-model.ts (SnippetNode.snippetLabel) + src/graph/node-label.ts (isLabeledEdge)"
      via: "import + direct field read on RPNode"
      pattern: "node.kind === 'snippet'"
    - from: "src/__tests__/edge-label-reconciler.test.ts"
      to: "src/__tests__/fixtures/snippet-cold-open-migration.canvas + branching-snippet-multi-incoming.canvas"
      via: "loadFixture(...) + new CanvasParser().parse(...)"
      pattern: "loadFixture\\('(snippet-cold-open-migration|branching-snippet-multi-incoming)\\.canvas'\\)"
---

<objective>
Extend the pure Phase 50 reconciler to a parallel Snippet arm so that incoming Question→Snippet edges share a single label with the SnippetNode's snippetLabel under the same edge-wins rule that already governs Question→Answer edges.

Purpose: This is the load-bearing pure-function layer that Plans 02 and 03 will build on. With this in place, the existing Phase 50 `EdgeLabelSyncService` write pipeline can be extended in Plan 02 with zero re-architecture, and the new dispatch event in Plan 02 will already have correctly-typed snippet-kind diffs to broadcast.

Output:
- `src/graph/edge-label-reconciler.ts` — extended with snippet edge-wins arm + discriminated `EdgeLabelDiff` + discriminated nodeChanges shape (D-04).
- `src/__tests__/edge-label-reconciler.test.ts` — extended with a `describe('reconcileEdgeLabels — snippet edge-wins (Phase 63)')` block plus a mixed-kind test and an idempotency test.
- `src/__tests__/canvas-write-back.test.ts` — extended with one `it('saveLiveBatch routes snippet-kind nodeChanges to radiprotocol_snippetLabel')` plus an undefined-strip variant.
- `src/__tests__/fixtures/snippet-cold-open-migration.canvas` — NEW fixture for D-03.
- `src/__tests__/fixtures/branching-snippet-multi-incoming.canvas` — NEW fixture for multi-incoming edge-wins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-bidirectional-canvas-node-editor-sync/63-CONTEXT.md
@.planning/phases/63-bidirectional-canvas-node-editor-sync/63-RESEARCH.md
@.planning/phases/63-bidirectional-canvas-node-editor-sync/63-PATTERNS.md
@.planning/phases/63-bidirectional-canvas-node-editor-sync/63-VALIDATION.md
@.planning/notes/answer-label-edge-sync.md
@.planning/milestones/v1.8-phases/50-answer-edge-label-sync/50-CONTEXT.md
@CLAUDE.md
@src/graph/edge-label-reconciler.ts
@src/graph/graph-model.ts
@src/graph/node-label.ts
@src/graph/canvas-parser.ts
@src/__tests__/edge-label-reconciler.test.ts
@src/__tests__/canvas-write-back.test.ts
@src/__tests__/fixtures/displayLabel-edge-mismatch.canvas
@src/__tests__/fixtures/branching-multi-incoming.canvas

<interfaces>
<!-- CRITICAL contracts the executor needs. Do NOT explore the codebase to discover these — use what is below. -->

Existing shape (`src/graph/edge-label-reconciler.ts:12-21`) — Phase 50:
```typescript
export interface EdgeLabelDiff {
  edgeId: string;
  currentLabel: string | undefined;
  targetLabel: string | undefined;
}

export interface ReconcileResult {
  diffs: EdgeLabelDiff[];
  newDisplayLabelByAnswerId: Map<string, string | undefined>;
}
```

Target shape after Plan 01 (D-04 discriminated union — single source of truth for both kinds):
```typescript
export interface EdgeLabelDiff {
  edgeId: string;
  currentLabel: string | undefined;
  targetLabel: string | undefined;
  kind: 'answer' | 'snippet';   // Phase 63 NEW
}

export interface NodeLabelChange {
  nodeId: string;
  newLabel: string | undefined;
  kind: 'answer' | 'snippet';
}

export interface ReconcileResult {
  diffs: EdgeLabelDiff[];
  nodeChanges: NodeLabelChange[];   // replaces newDisplayLabelByAnswerId
}
```

Snippet field source on the graph (`src/graph/graph-model.ts:83-95` — already exists, do NOT modify):
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  subfolderPath?: string;
  snippetLabel?: string;        // ← Plan 01 reads this
  // ...
}
```

Reused predicate (`src/graph/node-label.ts:40-42`) — whitespace-as-unlabeled, already imported by the reconciler:
```typescript
export function isLabeledEdge(e: RPEdge): boolean {
  return typeof e.label === 'string' && e.label.trim().length > 0;
}
```

Edge-wins core to mirror 1:1 (`src/graph/edge-label-reconciler.ts:53-89`) — the snippet arm uses the SAME shape with three substitutions: `node.kind === 'snippet'`, read `snippet.snippetLabel?.trim()` instead of `displayLabel`, stamp every emitted diff/nodeChange with `kind: 'snippet'`.

`saveLiveBatch` already accepts arbitrary edits (no source change in `canvas-live-editor.ts:148-205`). The Phase 50 writer in `edge-label-sync-service.ts:102-112` (touched by Plan 02, NOT this plan) routes the discriminated nodeChanges by kind:
```typescript
const nodeEdits = nodeChanges.map(c => ({
  nodeId: c.nodeId,
  edits: c.kind === 'answer'
    ? { radiprotocol_displayLabel: c.newLabel }
    : { radiprotocol_snippetLabel: c.newLabel },
}));
```

NOTE: this plan changes the return shape of `reconcileEdgeLabels`. After Plan 01 lands the existing `edge-label-sync-service.ts:96, 99, 102-108` will fail to compile. That is intentional — Plan 02 is the next wave and will update those call sites. Run only the unit tests for this plan during the executor session; the build will not be green until Plan 02 lands.

Field-key vocabulary (verified — see `editor-panel-view.ts:519-728`, `canvas-parser.ts:198-286`):

| Form key | Reconciler reads from RPNode |
|----------|------------------------------|
| `radiprotocol_displayLabel` | `node.displayLabel` (Phase 50, untouched) |
| `radiprotocol_snippetLabel` | `node.snippetLabel` (Phase 63 NEW) |

`radiprotocol_content` is the canonical text-block key (NOT `radiprotocol_text`) — confirmed at `editor-panel-view.ts:600` and `canvas-parser.ts:220`. This plan does NOT touch text-block; it is informational for the snapshot work in Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixtures + extend reconciler unit tests with RED snippet describe block</name>
  <files>src/__tests__/fixtures/snippet-cold-open-migration.canvas, src/__tests__/fixtures/branching-snippet-multi-incoming.canvas, src/__tests__/edge-label-reconciler.test.ts</files>
  <read_first>
    - src/__tests__/edge-label-reconciler.test.ts (entire 260 lines — describe-block structure + makeGraph helper + loadFixture)
    - src/__tests__/fixtures/displayLabel-edge-mismatch.canvas (analog for cold-open fixture)
    - src/__tests__/fixtures/branching-multi-incoming.canvas (analog for multi-incoming fixture)
    - src/graph/edge-label-reconciler.ts (verify current shape and JSDoc you must NOT delete)
    - src/graph/graph-model.ts (lines 83-95 — SnippetNode shape; line 130 — reverseAdjacency)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-CONTEXT.md (D-01, D-02, D-03, D-04 — locked decisions)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-RESEARCH.md ("Cold-open migration mechanics" + "Phase 50 re-use surface" + "Test harness conventions")
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-PATTERNS.md ("EXTEND src/__tests__/edge-label-reconciler.test.ts" + the two NEW fixture sections at the bottom)
  </read_first>
  <behavior>
    - Snippet edge-wins picks first non-empty incoming label (mirrors existing Answer test "picks first non-empty incoming label when displayLabel differs").
    - Cold-open: a SnippetNode with `snippetLabel = "Брюшной отдел"` and a single incoming edge with no `label` → reconciler emits one `EdgeLabelDiff` with `targetLabel: "Брюшной отдел"`, `kind: 'snippet'`, AND zero entries in `nodeChanges` for the snippet (because `snippetLabel` already matches the picked value). Per D-03.
    - Multi-incoming: two Question nodes both pointing at one Snippet node with edge labels "Вариант X" and "Вариант Y" → first labeled wins ("Вариант X"); reconciler emits exactly ONE `EdgeLabelDiff` (for the second edge) with `targetLabel: "Вариант X"`, `kind: 'snippet'`, AND one `NodeLabelChange` for the snippet with `newLabel: "Вариант X"`, `kind: 'snippet'` (since fixture has `snippetLabel: "Старое"` which diverges).
    - Mixed Answer+Snippet: one Question fans out to one Answer + one Snippet, both with diverging incoming labels — both kinds reconcile in one pass, both diff entries carry the correct `kind` discriminator.
    - Idempotency: a graph where every snippet edge already matches its node's `snippetLabel` produces a structurally empty result (`diffs.length === 0 && nodeChanges.length === 0`).
    - Existing Phase 50 Answer tests remain green AFTER they are mechanically updated for the new return-shape (see Action below).
  </behavior>
  <action>
1. Create the two fixtures verbatim from 63-PATTERNS.md "NEW src/__tests__/fixtures/snippet-cold-open-migration.canvas" and "NEW src/__tests__/fixtures/branching-snippet-multi-incoming.canvas" sections. Use the exact JSON from those sections — do not add or rename properties.

2. Open `src/__tests__/edge-label-reconciler.test.ts`. Extend the `MakeGraphParams` type (lines 14-19) with an optional `snippets?: Array<{ id: string; snippetLabel?: string; subfolderPath?: string }>` field. Extend the `makeGraph` body (lines 21-70) so that for every entry in `snippets`, it pushes a node with `kind: 'snippet'`, `id: s.id`, `snippetLabel: s.snippetLabel`, `subfolderPath: s.subfolderPath` into the `nodes` Map. Do NOT remove or rename the existing `answers` / `questions` parameters.

3. Mechanically update existing Phase 50 Answer tests for the new return-shape (per <interfaces> block above). Every assertion that reads `result.newDisplayLabelByAnswerId.get(answerId)` becomes `result.nodeChanges.find(c => c.nodeId === answerId && c.kind === 'answer')?.newLabel`. Every assertion that reads `result.newDisplayLabelByAnswerId.size` becomes `result.nodeChanges.filter(c => c.kind === 'answer').length`. Every existing diff assertion gains an additional `expect(diff.kind).toBe('answer')` check. Mark the touched lines with a `// Phase 63: kind discriminator` comment per CLAUDE.md "never remove code you didn't add" — preserve every existing assertion semantically.

4. Append a new `describe('reconcileEdgeLabels — snippet edge-wins (Phase 63)')` block AFTER the last existing describe block (after line 226), containing exactly five tests matching the five behaviors listed above. Use the verbatim test bodies from 63-PATTERNS.md "Snippet describe-block to add" — no new behaviors, no skipped tests.

5. Tests are RED at this point because `reconcileEdgeLabels` does not yet have a snippet arm and does not yet expose `nodeChanges`. That is correct for Wave 0.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/edge-label-reconciler.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `[ -f src/__tests__/fixtures/snippet-cold-open-migration.canvas ]` returns 0
    - File exists: `[ -f src/__tests__/fixtures/branching-snippet-multi-incoming.canvas ]` returns 0
    - `grep -c "Брюшной отдел" src/__tests__/fixtures/snippet-cold-open-migration.canvas` returns at least 1
    - `grep -c "n-snip-shared" src/__tests__/fixtures/branching-snippet-multi-incoming.canvas` returns at least 1
    - `grep -n "describe('reconcileEdgeLabels — snippet edge-wins" src/__tests__/edge-label-reconciler.test.ts` returns at least one match
    - `grep -nE "snippets\\?:\\s*Array" src/__tests__/edge-label-reconciler.test.ts` returns at least one match (proving makeGraph extension)
    - `grep -c "kind: 'snippet'" src/__tests__/edge-label-reconciler.test.ts` returns at least 4 (one per snippet describe-block test that asserts the discriminator)
    - `npm test -- --run src/__tests__/edge-label-reconciler.test.ts` exits NON-zero (RED expected at this point — snippet arm not implemented)
  </acceptance_criteria>
  <done>RED tests in place: fixture files exist, makeGraph extended for snippet nodes, Phase 50 Answer assertions migrated to the discriminated shape, Phase 63 snippet describe block exists with 5 failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement snippet arm in pure reconciler + discriminated return shape</name>
  <files>src/graph/edge-label-reconciler.ts</files>
  <read_first>
    - src/graph/edge-label-reconciler.ts (entire 92 lines — current Phase 50 shape and JSDoc that must be PRESERVED, not deleted)
    - src/graph/graph-model.ts (lines 55-95 — RPNode union and SnippetNode shape)
    - src/graph/node-label.ts (lines 40-42 — isLabeledEdge predicate already imported)
    - src/__tests__/edge-label-reconciler.test.ts (the post-Task-1 RED tests — they define the contract)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-CONTEXT.md (D-01, D-02, D-04)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-RESEARCH.md ("reconcileEdgeLabels (`src/graph/edge-label-reconciler.ts:49-92`)" subsection)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-PATTERNS.md ("MODIFY src/graph/edge-label-reconciler.ts" — full target shape)
    - CLAUDE.md (the "never remove existing code you didn't add" rule applies in full to this shared accumulated file)
  </read_first>
  <behavior>
    - `reconcileEdgeLabels(graph)` returns `{ diffs: EdgeLabelDiff[], nodeChanges: NodeLabelChange[] }` — the legacy `newDisplayLabelByAnswerId: Map<...>` field is GONE; every Phase 50 Answer-side change becomes a `NodeLabelChange` with `kind: 'answer'`.
    - For every `node` whose `kind === 'answer'`: same edge-wins as before, but every emitted diff carries `kind: 'answer'` and every emitted node change goes into `nodeChanges` with `kind: 'answer'`.
    - For every `node` whose `kind === 'snippet'`: same edge-wins shape — read `node.snippetLabel`, fallback `pickedLabel = edgePick ?? snippetLabel.trim() || undefined`, propagate divergence to `nodeChanges` with `kind: 'snippet'`, push `EdgeLabelDiff` for every diverging incoming edge with `kind: 'snippet'`.
    - Iteration order: do not change the order of the existing `for (const node of graph.nodes.values())` loop. The snippet arm runs in the SAME loop iteration on snippet nodes (drop the `if (node.kind !== 'answer') continue` early return; replace it with two `if (node.kind === ...)` branches OR a switch on `node.kind`).
    - All Task 1 RED tests turn GREEN.
    - All previously-green Phase 50 Answer-arm tests in the same file remain GREEN.
    - File still has zero Obsidian imports (D-18 invariant from Phase 50 — pure module).
  </behavior>
  <action>
1. Edit `src/graph/edge-label-reconciler.ts`. Preserve the entire existing JSDoc block at lines 23-48 verbatim — append a new paragraph at the bottom of that JSDoc explaining the Phase 63 D-04 snippet pass: "Phase 63 D-04: the same edge-wins rule extends to Question→Snippet incoming edges. SnippetNode.snippetLabel is the single source of truth for every incoming Question→Snippet edge label. Diffs and node changes carry a `kind: 'answer' | 'snippet'` discriminator so the caller can route node-side writes to either `radiprotocol_displayLabel` or `radiprotocol_snippetLabel`."

2. Replace `EdgeLabelDiff` (lines 12-16) with the discriminated form from <interfaces> above. Replace `ReconcileResult` (lines 18-21) with the new shape using `nodeChanges: NodeLabelChange[]`. Add the `NodeLabelChange` interface declaration between them. Mark each new line with `// Phase 63: discriminated union` comment.

3. Inside the existing for-loop (lines 53-89), replace `if (node.kind !== 'answer') continue;` with a `switch (node.kind)` block (or two parallel `if` branches — Discretion). The Answer branch is byte-identical to the current code EXCEPT every `diffs.push(...)` gains `kind: 'answer'` and the `newDisplayLabelByAnswerId.set(...)` becomes `nodeChanges.push({ nodeId: answer.id, newLabel: pickedLabel, kind: 'answer' })`. The Snippet branch is the byte-identical mirror, reading `node.snippetLabel?.trim() || undefined` instead of `displayLabel?.trim() || undefined`, and emitting `kind: 'snippet'` on both diff and nodeChange.

4. Replace the local accumulator `const newDisplayLabelByAnswerId = new Map<string, string | undefined>();` (line 51) with `const nodeChanges: NodeLabelChange[] = [];`. Update the return statement (line 91) to `return { diffs, nodeChanges };`.

5. Run the test suite for this file. All Task 1 RED tests (5 snippet tests + the migrated Answer tests) must turn GREEN.

6. NEVER delete or reorder any existing comment, blank line, or import that you did not author in this task. Mark every new addition with `// Phase 63: ...`.

7. Acceptable: `edge-label-sync-service.ts` (and Plan 02's territory) will not compile after this change because it still references the removed `newDisplayLabelByAnswerId`. Plan 02 will fix it. Do NOT touch `edge-label-sync-service.ts` in this plan.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/edge-label-reconciler.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind: 'snippet'" src/graph/edge-label-reconciler.ts` returns at least 2 matches
    - `grep -n "kind: 'answer'" src/graph/edge-label-reconciler.ts` returns at least 2 matches
    - `grep -n "NodeLabelChange" src/graph/edge-label-reconciler.ts` returns at least 3 matches (interface decl + accumulator type + return type)
    - `grep -n "newDisplayLabelByAnswerId" src/graph/edge-label-reconciler.ts` returns ZERO matches (replaced by nodeChanges)
    - `grep -nE "^import.*from 'obsidian'" src/graph/edge-label-reconciler.ts` returns ZERO matches (D-18 pure-module invariant preserved)
    - `grep -n "Phase 63" src/graph/edge-label-reconciler.ts` returns at least 3 matches (per CLAUDE.md "mark new additions" rule)
    - `npm test -- --run src/__tests__/edge-label-reconciler.test.ts` exits 0 (all 5 new snippet tests + all migrated Answer tests GREEN)
  </acceptance_criteria>
  <done>Reconciler implements both arms with the discriminated return shape; all Phase 50 Answer-arm tests still pass; all 5 new Phase 63 snippet-arm tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend canvas-write-back tests with snippet-kind saveLiveBatch coverage</name>
  <files>src/__tests__/canvas-write-back.test.ts</files>
  <read_first>
    - src/__tests__/canvas-write-back.test.ts (lines 1-50 for imports + 202-327 for the entire PHASE-50 CanvasLiveEditor edge writes describe block — the template you mirror)
    - src/canvas/canvas-live-editor.ts (lines 148-234 — saveLiveBatch signature; confirms it accepts arbitrary `edits` Record so snippet keys work without source change)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-PATTERNS.md ("EXTEND src/__tests__/canvas-write-back.test.ts" — verbatim test bodies)
    - .planning/phases/63-bidirectional-canvas-node-editor-sync/63-VALIDATION.md (row 63-01-03)
  </read_first>
  <behavior>
    - When `saveLiveBatch` is called with `nodeEdits = [{ nodeId: 'n-snip', edits: { radiprotocol_snippetLabel: 'Новое' } }]` and `edgeEdits = [{ edgeId: 'e1', label: 'Новое' }]`, the resulting `setData` payload contains:
      • a node where `id === 'n-snip'` has `radiprotocol_snippetLabel: 'Новое'`
      • an edge where `id === 'e1'` has `label: 'Новое'`
      • exactly ONE `setData` call (D-14 atomicity preserved)
    - When `saveLiveBatch` is called with `radiprotocol_snippetLabel: undefined`, the resulting payload has the key DELETED from the node (D-08 strip-key symmetry).
    - All existing Phase 50 tests in lines 202-326 remain GREEN — their `radiprotocol_displayLabel` assertions are NOT touched.
  </behavior>
  <action>
1. Open `src/__tests__/canvas-write-back.test.ts`. Locate the `describe` block that opens around line 202 (PHASE-50 CanvasLiveEditor edge writes — the exact opener may use a different string; find by `grep -n "PHASE-50" src/__tests__/canvas-write-back.test.ts` first).

2. Append two new `it(...)` blocks AFTER the last existing `it(...)` in that describe block (just before its closing `});`). Use the verbatim test bodies from 63-PATTERNS.md "EXTEND src/__tests__/canvas-write-back.test.ts" — both `it('saveLiveBatch routes snippet-kind nodeChanges to radiprotocol_snippetLabel (Phase 63 D-04)', ...)` and `it('saveLiveBatch strips radiprotocol_snippetLabel when value is undefined (D-08 symmetry)', ...)`.

3. The strip-undefined test mirrors the existing `radiprotocol_displayLabel` strip test that lives in the Phase 50 describe block — find it via `grep -n "radiprotocol_displayLabel" src/__tests__/canvas-write-back.test.ts | grep -i "undefined\|strip"` and copy the assertion shape, swapping the field key to `radiprotocol_snippetLabel`.

4. Mark each new `it` block with a leading `// Phase 63: snippet-kind nodeEdits routing` comment.

5. Tests should turn GREEN immediately because Plan 01 Task 2 did NOT change `canvas-live-editor.ts`. The saveLiveBatch implementation is field-key-agnostic — it iterates `Object.entries(edits)` (line 148 of canvas-live-editor.ts) and applies any key including `radiprotocol_snippetLabel`. The test verifies that contract holds for the new key.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/canvas-write-back.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "radiprotocol_snippetLabel" src/__tests__/canvas-write-back.test.ts` returns at least 2 matches (one per new test)
    - `grep -n "Phase 63" src/__tests__/canvas-write-back.test.ts` returns at least 2 matches
    - `grep -c "saveLiveBatch routes snippet-kind nodeChanges" src/__tests__/canvas-write-back.test.ts` returns 1
    - `grep -c "strips radiprotocol_snippetLabel" src/__tests__/canvas-write-back.test.ts` returns 1
    - `npm test -- --run src/__tests__/canvas-write-back.test.ts` exits 0 (new tests GREEN, all Phase 50 tests still GREEN)
  </acceptance_criteria>
  <done>canvas-write-back.test.ts has two new GREEN tests proving saveLiveBatch handles `radiprotocol_snippetLabel` writes and strip-key symmetry; the entire Phase 50 describe block remains GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.canvas` JSON file → CanvasParser | Untrusted on-disk state crosses into the pure reconciler. The file may be malformed (manually edited), partially written (mid-Obsidian-save), or corrupted. |
| pure reconciler → consumer (sync service in Plan 02) | The reconciler returns a contract that downstream code trusts to be well-formed; if reconciler throws, the service-level catch in Plan 02 must swallow gracefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering / DoS | `reconcileEdgeLabels` reading from a malformed canvas JSON via the parser | mitigate | (a) The parser already produces a `parsed.success === false` result on malformed JSON — the consumer (Plan 02) bails before calling reconciler, so reconciler always sees a typed `ProtocolGraph`. (b) Inside the reconciler, every field read uses optional chaining (`node.snippetLabel?.trim()`, `e.label?.trim()`) — never assumes a field is present. (c) The reconciler must NEVER throw — even an unexpected node shape is silently skipped (the existing Answer arm sets that precedent at line 54). Document this invariant in the JSDoc you append in Task 2. |
| T-03 | Denial of Service / livelock | Infinite reconcile loop if D-07 idempotency check is broken (write-modify-write-modify…) | mitigate | The Phase 50 D-07 short-circuit (`edge-label-sync-service.ts:99`) is preserved by the discriminated return shape: a structurally empty `{ diffs: [], nodeChanges: [] }` still triggers the short-circuit in Plan 02. Task 1 includes an explicit idempotency test ("snippet kind contributes to D-07 short-circuit") that asserts the empty case for the snippet arm. If the snippet arm ever pushes a spurious diff for an already-matching edge, this test fails RED before any infinite loop can land in production. |

(T-02 / T-04 / T-05 are out of scope for Plan 01 — they live in Plans 02 and 03.)
</threat_model>

<verification>
After all three tasks land, run:

```bash
npm test -- --run src/__tests__/edge-label-reconciler.test.ts src/__tests__/canvas-write-back.test.ts
```

Expected: both files GREEN, with at least:
- 5 new tests in the snippet describe block (Task 1 + Task 2)
- All migrated Phase 50 Answer-arm tests still GREEN (Task 2)
- 2 new tests in canvas-write-back (Task 3)
- All Phase 50 canvas-write-back tests still GREEN (Task 3)

Do NOT run `npm run build` or the full test suite at the end of this plan — `edge-label-sync-service.ts` will not compile because it still references the removed `newDisplayLabelByAnswerId`. Plan 02 will fix that.
</verification>

<success_criteria>
- `src/graph/edge-label-reconciler.ts` exports the discriminated `EdgeLabelDiff`, `NodeLabelChange`, and `ReconcileResult` shapes from <interfaces> above.
- `reconcileEdgeLabels(graph)` produces correct snippet edge-wins diffs and nodeChanges per D-01/D-02/D-04 in the cold-open, multi-incoming, mixed Answer+Snippet, and idempotency cases.
- `src/__tests__/edge-label-reconciler.test.ts` contains a `describe('reconcileEdgeLabels — snippet edge-wins (Phase 63)')` block with 5 GREEN tests; all Phase 50 Answer-arm tests are migrated to the discriminated shape and remain GREEN.
- `src/__tests__/canvas-write-back.test.ts` contains 2 new GREEN tests proving `saveLiveBatch` handles snippet-kind nodeEdits and strip-key undefined symmetry.
- The two new fixture files exist with the verbatim contents from 63-PATTERNS.md.
- The pure-module invariant is preserved: zero Obsidian imports in `edge-label-reconciler.ts`.
- Every new addition is annotated `// Phase 63: ...` per CLAUDE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/63-bidirectional-canvas-node-editor-sync/63-01-SUMMARY.md`.
</output>
