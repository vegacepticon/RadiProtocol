---
phase: 62-brat-release-v1.9.0
plan: 03
type: execute
wave: 3
depends_on: [62-02]
files_modified:
  - .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md
autonomous: true
requirements: [BRAT-02]
must_haves:
  truths:
    - "A runbook file exists at `.planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md`"
    - "Runbook OPENS with a `## Pre-publish check — Phase 60 UAT gate` section (D10) that instructs the user to grep `.planning/ROADMAP.md` for Phase 60's status and STOP if it is not `Complete`"
    - "Runbook contains a `## Preflight (automated)` section invoking `bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh` verbatim"
    - "Runbook contains a `## Push the tag` section with the exact `git push origin 1.9.0` command"
    - "Runbook contains a `## Create the GitHub Release (web UI)` section with step-by-step instructions (tag selection, title `1.9.0`, 3 loose assets drag-and-drop, pre-release UNCHECKED, latest CHECKED, Publish)"
    - "Runbook contains a `## Release Notes` section with a ready-to-paste markdown block grouped per D7 (Runner — Inline, Settings UX, Distribution) — text copied verbatim from 62-CONTEXT.md D7"
    - "Release Notes section starts with the A3 guardrail instructing the user to TRIM bullets for phases not yet marked Complete in ROADMAP.md (especially Phase 60)"
    - "Runbook contains a `## Post-release verification` section with a fresh-vault BRAT smoke test identical in shape to Phase 55's SC-3 check"
    - "Runbook contains a `## Final verdict` section with success/failure marking instructions"
    - "Runbook references the BRAT identifier `vegacepticon/RadiProtocol` verbatim (at least twice — push-tag URL section and BRAT Add-plugin step)"
    - "Runbook references tag `1.9.0` (unprefixed) in every tag-related instruction — never `v1.9.0`"
  artifacts:
    - path: ".planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md"
      provides: "User-facing, copy-pasteable playbook for the D10 UAT gate, web-UI Release step, and post-publish verification"
      contains: "## Release Notes"
  key_links:
    - from: "runbook §Pre-publish check"
      to: "ROADMAP.md Phase 60 status"
      via: "grep for `Phase 60` + `Complete` vs. `Awaiting human UAT`"
      pattern: "grep.*Phase 60.*ROADMAP"
    - from: "runbook §Preflight"
      to: "scripts/release-preflight.sh (from Plan 02)"
      via: "bash invocation with full path"
      pattern: "bash \\.planning/phases/62-brat-release-v1\\.9\\.0/scripts/release-preflight\\.sh"
    - from: "runbook §Push the tag"
      to: "git tag 1.9.0 (from Plan 02)"
      via: "exact `git push origin 1.9.0` command"
      pattern: "git push origin 1\\.9\\.0"
    - from: "runbook §Post-release verification"
      to: "BRAT install smoke test in fresh vault"
      via: "identifier `vegacepticon/RadiProtocol` pasted into BRAT dialog"
      pattern: "vegacepticon/RadiProtocol"
---

<objective>
Create the user-facing release runbook — the single document the repository owner follows once to publish RadiProtocol v1.9.0 to GitHub so BRAT users can install it.

Purpose: Closes SC-2 (user creates the GitHub Release via web UI per D6) and frames SC-3 (BRAT install smoke test as the final human verification). All local preparation is already done by Plans 01 + 02; this runbook hands the user a deterministic, copy-pasteable sequence — with ONE structural delta vs. Phase 55: a new opening `Pre-publish check — Phase 60 UAT gate` section (D10) that halts the release if Phase 60 has not signed off in ROADMAP.md at publish time.

Output: ONE markdown file in the phase directory, containing the D10 gate + five required sections (Preflight, Push tag, Web-UI Release, Release Notes, Post-release verification) + Final verdict, with exact commands, exact UI steps, and the D7 curated v1.9 changelog block ready to paste into the Release description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/62-brat-release-v1.9.0/62-CONTEXT.md
@.planning/phases/62-brat-release-v1.9.0/62-01-PLAN.md
@.planning/phases/62-brat-release-v1.9.0/62-02-PLAN.md
@.planning/milestones/v1.8-phases/55-brat-distribution-readiness/55-CONTEXT.md
@.planning/milestones/v1.8-phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
@.planning/milestones/v1.8-phases/55-brat-distribution-readiness/55-04-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 62-03-01: Author 62-RELEASE-RUNBOOK.md — the v1.9.0 user playbook with D10 Phase 60 UAT gate</name>
  <files>.planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md</files>
  <read_first>
    - .planning/milestones/v1.8-phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md (direct template — read in full; adapt version + D10 gate)
    - .planning/phases/62-brat-release-v1.9.0/62-CONTEXT.md (D1 tag shape, D6 web-UI path, D7 verbatim changelog bullets, D8 Latest flag conditional on D10, D10 Phase 60 UAT gate — read the D7 block entirely so bullets are copied verbatim)
    - .planning/milestones/v1.8-phases/55-brat-distribution-readiness/55-04-PLAN.md (Task 55-04-01 — analog authoring task; note: Phase 55 used a heredoc for the write; this plan uses the Write tool per CLAUDE.md bash-tool guidance)
    - .planning/ROADMAP.md lines 137–144 (current v1.9 phase status — sanity-check that Phase 60 status is still "Awaiting human UAT" at runbook-authoring time; if it has flipped to "Complete" by the time the executor runs, the D10 gate instruction remains valid but the user will pass through it)
  </read_first>
  <action>
Use the **Write tool** (NOT heredoc/cat) to create `.planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` with the EXACT content below. The file must have 8 top-level sections in this order:

1. Frontmatter (YAML)
2. `# Phase 62 — v1.9.0 Release Runbook` (title) + intro blockquote
3. `## Pre-publish check — Phase 60 UAT gate` (D10 — NEW vs. Phase 55)
4. `## Preflight (automated)`
5. `## Push the tag`
6. `## Create the GitHub Release (web UI)`
7. `## Release Notes` (with A3 guardrail at top)
8. `## Post-release verification` (with `### Final verdict` subsection)
9. `## Anti-patterns (do NOT do any of these)`

Exact file content to write (preserve indentation, code fences, and blockquote markers exactly):

````markdown
---
phase: 62-brat-release-v1.9.0
type: release-runbook
status: ready
performed_on: <YYYY-MM-DD — user fills at publish time>
final_verdict: pending
source:
  - 62-CONTEXT.md
  - 55-RELEASE-RUNBOOK.md (template)
  - ROADMAP.md (D10 gate reference)
created: 2026-04-24
---

# Phase 62 — v1.9.0 Release Runbook

> Step-by-step guide for publishing RadiProtocol v1.9.0 to GitHub so users can install via BRAT with identifier `vegacepticon/RadiProtocol`.
>
> Everything local (manifest bump, versions.json append, build, commit, tag, preflight) has already been performed by Plans 01–02. Your job as the repository owner: confirm the Phase 60 UAT gate, run the preflight, push the tag, create the Release in the web UI, then verify.
>
> This runbook mirrors Phase 55's v1.8.0 runbook with one structural addition: the opening **Pre-publish check — Phase 60 UAT gate** section, which blocks publishing until Phase 60 is marked Complete in `ROADMAP.md`.

---

## Pre-publish check — Phase 60 UAT gate

**Do this FIRST, before any other step.** The release asset must reflect the full v1.9 shippable build; Phase 60's UAT status gates this release.

From the repo root, run:

```bash
grep -E '^\- \[.\] \*\*Phase 60:' .planning/ROADMAP.md
```

Interpret the output:

- If the line reads `- [x] **Phase 60: ...` **AND** ends with `— completed <DATE>` (or similar Complete marker) → Phase 60 UAT has signed off. Proceed to **Preflight (automated)** below.
- If the line reads `- [ ] **Phase 60: ...` **OR** the status note says `Awaiting human UAT` (or anything other than Complete) → **STOP**. Do not push the tag. Do not publish the Release. Local Plans 01–02 have already run (bump + build + preflight + tag all exist locally), which is safe. Complete Phase 60's UAT sign-off first, update ROADMAP.md, then return here.

Double-check the Progress table near the end of `.planning/ROADMAP.md`:

```bash
grep -E 'Phase 60' .planning/ROADMAP.md | grep -E 'Complete|Awaiting'
```

If the Progress-table row for Phase 60 reads `Awaiting human UAT`, the gate is NOT passed — STOP.

**Why this gate exists (D10 from 62-CONTEXT.md):** shipping v1.9.0 without Phase 60 UAT would either bundle un-validated Inline Runner layout/position code into a "Latest" Release (user-facing risk) or publish release notes that describe features not actually tested. Both are unacceptable. The gate is the single decision point; everything downstream assumes it has passed.

---

## Preflight (automated)

Once the D10 gate passes, run:

```bash
bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh
```

Expected final line of stdout:

```
SC-1 local verification: PASS
```

The script asserts:
- `manifest.json.version === "1.9.0"`
- `versions.json` keys are exactly `1.8.0,1.9.0` (both mapping to `1.5.7` — D4 preserves the `1.8.0` entry)
- `package.json.version === "1.9.0"`
- `author` / `authorUrl` / `fundingUrl` unchanged from Phase 55 (D9)
- `minAppVersion` remains `1.5.7` (D3)
- Tag `1.9.0` exists AND no `v1.9.0` collision
- `styles.css` and `main.js` are NOT in the git index and ARE matched by `.gitignore`
- Fresh `npm run build` produces non-empty `main.js` + `styles.css`

If the script exits non-zero, STOP — do not push, do not publish. Read the failing section header in stdout and fix (re-run Plan 01 or Plan 02 as needed) before continuing.

---

## Push the tag

The release-prep commit and annotated tag `1.9.0` already exist locally (Plan 02). Push them:

```bash
git push origin main
git push origin 1.9.0
```

(Only the tag push is strictly required for the Release step below — the branch push is recommended to keep `origin/main` in sync with the commit the tag points at.)

Verify the tag is visible on GitHub:
- Visit <https://github.com/vegacepticon/RadiProtocol/tags>
- Confirm `1.9.0` (no `v` prefix) appears at the top.
- The existing `1.8.0` tag (Phase 55) should still be present below it.
- If you see `v1.9.0` instead of `1.9.0`, STOP — a prefixed tag was created by mistake. Delete it locally and on origin, then return to Plan 02 Task 62-02-04.

---

## Create the GitHub Release (web UI)

BRAT does not read bare tags — it reads GitHub **Release** objects. A tag without a Release is invisible to BRAT (Phase 55 Research Pitfall 4).

1. Open <https://github.com/vegacepticon/RadiProtocol/releases/new>
2. **Choose a tag**: select `1.9.0` from the dropdown (already pushed in the previous section).
3. **Release title**: type exactly `1.9.0` — no `v` prefix, no suffix, no extra whitespace. (Phase 55 Pitfall 5 — tag name, release title, and `manifest.version` must all byte-match.)
4. **Description**: paste the entire contents of the [Release Notes](#release-notes) section below into the description field. Before pasting, read the A3 guardrail at the top of that section and trim any bullets for phases that are NOT yet marked Complete in `.planning/ROADMAP.md` (most relevant: Phase 60 — if the D10 gate above required you to wait until it flipped to Complete, its bullets are safe to keep; if a previous gate exception was ever invoked, trim accordingly).
5. **Attach binaries**: drag-and-drop these three files from your repo root, ONE AT A TIME (or all at once, but as individual loose files — DO NOT zip them per Phase 55 Pitfall 6):
   - `manifest.json`
   - `main.js`
   - `styles.css`
   Confirm the "Assets" list shows three separate downloadable entries. If it shows a single `.zip`, remove the zip and re-upload the three loose files.
6. **Pre-release flag**: leave `Set as a pre-release` UNCHECKED (per D8 — the D10 gate above ensures all three v1.9 phases passed UAT by publish time, so a normal Release is the honest signal). If for any reason you have intentionally bypassed the D10 gate (STRONGLY discouraged), CHECK this box instead.
7. **Latest release**: `Set as the latest release` must be CHECKED (default — BRAT selects by latest SemVer).
8. Click **Publish release**.

Immediately after publishing, verify in the browser:
- URL redirects to `https://github.com/vegacepticon/RadiProtocol/releases/tag/1.9.0`
- "Latest" green badge visible next to the release title
- Assets section shows three separate entries: `manifest.json`, `main.js`, `styles.css`
- Click each asset link — each downloads the individual file (not a zip, not a 404)

Optional API smoke-check (works without `gh` CLI):

```bash
curl -s https://api.github.com/repos/vegacepticon/RadiProtocol/releases/latest \
  | node -e 'const d=JSON.parse(require("fs").readFileSync(0,"utf8")); const names=(d.assets||[]).map(a=>a.name).sort().join(","); console.log("tag:", d.tag_name, "name:", d.name, "assets:", names); process.exit(d.tag_name==="1.9.0" && names==="main.js,manifest.json,styles.css" ? 0 : 1)'
```

Expected stdout:
```
tag: 1.9.0 name: 1.9.0 assets: main.js,manifest.json,styles.css
```

---

## Release Notes

> **A3 guardrail (read before pasting):** Some phases below may not be marked Complete in `.planning/ROADMAP.md` at the moment you publish. Before pasting this block into the Release description, open the ROADMAP Progress table and TRIM any bullet whose phase is not yet marked Complete. Do not ship a changelog that mentions features that were not actually included in the build. This is especially important for Phase 60 — if the D10 gate above instructed you to wait for its UAT, its bullets are valid; if you are publishing with any exception, trim accordingly.

### Runner (Inline mode)

**Phase 59 — Feature parity fixes**
- Nested `templates/ALGO`-style path resolution in inline mode (parity with the snippet resolver). INLINE-FIX-01.
- Snippet separator honoured when inserting from inline (space/newline between text and snippet — matches sidebar-runner). INLINE-FIX-04.
- JSON snippet fill-in modal now opens from inline mode (no z-index conflict with the floating runner). INLINE-FIX-05.

**Phase 60 — Layout & position persistence**
- Inline modal position persists across tab switches and plugin reload (saved in workspace state, clamped to viewport bounds so the modal never lands off-screen). INLINE-FIX-02.
- Compact default layout — the inline modal no longer overlaps note text at default Obsidian window size. INLINE-FIX-03.

### Settings UX

**Phase 61 — Folder autocomplete**
- Folder autocomplete (`FolderSuggest` built on Obsidian's `AbstractInputSuggest`) now shows a dropdown of existing vault folders on all three settings path fields: Protocols, Snippets, Output. SETTINGS-01.

### Distribution

- BRAT install remains at `vegacepticon/RadiProtocol`. v1.9.0 ships `manifest.json`, `main.js`, and `styles.css` as loose assets on this Release. BRAT-02.

### Install via BRAT

1. Install the **BRAT** plugin from Obsidian's Community Plugins list (if not already installed).
2. Open Command Palette → `BRAT: Add a beta plugin for testing`.
3. Paste `vegacepticon/RadiProtocol` into the dialog.
4. Click `Add Plugin`.
5. Once BRAT reports the plugin installed, enable it in Settings → Community Plugins.
6. Open Command Palette → `RadiProtocol: Open Runner view`.

---

## Post-release verification

### SC-3 — Fresh-vault BRAT install smoke test (manual, 5 minutes)

1. Create a new empty vault in Obsidian (`File → New vault`).
2. Enable Community Plugins in Settings (disable Restricted Mode if prompted).
3. Install **BRAT** from the Community Plugins browser and enable it.
4. Open Command Palette → `BRAT: Add a beta plugin for testing`.
5. Paste `vegacepticon/RadiProtocol` and click `Add Plugin`.
6. Wait for BRAT's "plugin installed" notice (downloads the three loose assets from the 1.9.0 Release).
7. Go to Settings → Community Plugins → find **RadiProtocol** in the installed list → toggle ON.
8. Open Command Palette → `RadiProtocol: Open Runner view`.
9. Confirm:
   - Plugin appears in the Community Plugins list with author `vegacepticon` and version `1.9.0`.
   - Plugin enables without errors in the Developer Console.
   - Runner view opens in the workspace (selector placeholder visible: `Select a protocol…`).
   - Open Settings → RadiProtocol: confirm all three path fields (Protocols, Snippets, Output) show folder-autocomplete dropdowns on focus (Phase 61 smoke verification).
   - Run the `Run protocol in inline` command on a note with any canvas configured: confirm the inline modal opens in the compact default layout and remembers its position after a tab switch (Phase 60 smoke verification).
10. If any step fails, open DevTools (`Ctrl+Shift+I`) → Console tab → copy the red error messages → return to this runbook's §Create the GitHub Release and verify the three assets uploaded correctly.

### Final verdict

- ✅ If all three checks pass (D10 gate, preflight, web-UI Release with 3 loose assets, fresh-vault BRAT install): mark `final_verdict: success` in this file's frontmatter and commit.
- ❌ If any check fails: mark `final_verdict: failed`, capture the specific failure, and return to the relevant plan (01/02) or this runbook's §Create the GitHub Release.

---

## Anti-patterns (do NOT do any of these)

- `git tag v1.9.0` — breaks BRAT's byte-match rule (Phase 55 Pitfall 5). Use `1.9.0` without prefix.
- Zipping the three assets into a single `.zip` — BRAT cannot extract it (Phase 55 Pitfall 6).
- Checking "Set as a pre-release" when all three prerequisite phases (59, 60, 61) are Complete — the D10 gate exists specifically so Latest is the honest signal. Pre-release is only valid if you have intentionally bypassed the D10 gate, which should never be routine.
- Editing the Release title to add a descriptive suffix (e.g., `1.9.0 — Inline Polish`) — the tag, release name, and `manifest.version` must all read exactly `1.9.0`. Descriptive text goes in the description body, not the title.
- Running `gh release create …` — `gh` CLI is not installed in this environment and D6 locks the web UI path. Do not install `gh` just for this step.
- Removing the `1.8.0` entry from `versions.json` — D4 PRESERVE. The preflight script will fail loudly if this regresses, but do not try to "tidy" the file manually.
- Re-tracking `styles.css` or `main.js` — D5 state was locked by Phase 55 and this phase does not touch `.gitignore`. The preflight asserts both paths remain ignored.
````

After writing the file, run this acceptance sweep to prove every required element is present:

```bash
RUNBOOK=.planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md
grep -Fxq '## Pre-publish check — Phase 60 UAT gate' "$RUNBOOK"
grep -Fxq '## Preflight (automated)' "$RUNBOOK"
grep -Fxq '## Push the tag' "$RUNBOOK"
grep -Fxq '## Create the GitHub Release (web UI)' "$RUNBOOK"
grep -Fxq '## Release Notes' "$RUNBOOK"
grep -Fxq '## Post-release verification' "$RUNBOOK"
grep -Fxq '## Anti-patterns (do NOT do any of these)' "$RUNBOOK"
grep -Fxq '### Final verdict' "$RUNBOOK"
grep -Fq 'bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh' "$RUNBOOK"
grep -Fq 'git push origin 1.9.0' "$RUNBOOK"
grep -Fq 'vegacepticon/RadiProtocol' "$RUNBOOK"
grep -Fq 'INLINE-FIX-01' "$RUNBOOK"
grep -Fq 'INLINE-FIX-02' "$RUNBOOK"
grep -Fq 'INLINE-FIX-03' "$RUNBOOK"
grep -Fq 'INLINE-FIX-04' "$RUNBOOK"
grep -Fq 'INLINE-FIX-05' "$RUNBOOK"
grep -Fq 'SETTINGS-01' "$RUNBOOK"
grep -Fq 'BRAT-02' "$RUNBOOK"
grep -Fq 'final_verdict: pending' "$RUNBOOK"
grep -Fq 'A3 guardrail' "$RUNBOOK"
grep -Fq 'trim' "$RUNBOOK"
```

All 21 greps must exit 0.

Then commit the runbook using a HEREDOC for the commit message (per project git convention, even though Write is used for the file):

```bash
git add .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md
git commit -m "$(cat <<'EOF'
docs(62): add v1.9.0 release runbook with Phase 60 UAT gate

User-facing copy-pasteable playbook covering the D10 Phase 60 UAT gate,
preflight, tag push, web-UI Release creation (per D6), curated v1.9
changelog (per D7), and post-publish BRAT smoke test (SC-3). Includes
A3 guardrail reminding the user to trim bullets for not-yet-Complete
phases before pasting. Mirrors Phase 55 runbook with the D10 gate as
the new structural addition.

BRAT-02

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>test -f .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Pre-publish check — Phase 60 UAT gate' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Preflight (automated)' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Push the tag' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Create the GitHub Release (web UI)' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Release Notes' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fxq '## Post-release verification' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fq 'bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fq 'git push origin 1.9.0' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && grep -Fq 'vegacepticon/RadiProtocol' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md && git log -1 --format='%s' | grep -q '^docs(62): add v1.9.0 release runbook' && echo 'runbook OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '## Pre-publish check — Phase 60 UAT gate' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (D10 gate section present — the key Phase 62 delta)
    - `grep -Fxq '## Preflight (automated)' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '## Push the tag' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '## Create the GitHub Release (web UI)' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '## Release Notes' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '## Post-release verification' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fxq '### Final verdict' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -Fq 'bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (preflight invocation literal)
    - `grep -Fq 'git push origin 1.9.0' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (tag-push command, unprefixed)
    - `grep -Fq 'git push origin main' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (branch push command)
    - `grep -cF 'vegacepticon/RadiProtocol' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` returns a number `>= 2` (BRAT identifier referenced at least twice)
    - `grep -cF '1.9.0' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` returns a number `>= 10` (version referenced throughout)
    - All five v1.9 requirement IDs present: `grep -Fq 'INLINE-FIX-01' ...` through `INLINE-FIX-05`, `SETTINGS-01`, `BRAT-02` — all exit 0
    - `grep -Fq 'A3 guardrail' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0
    - `grep -qi 'trim' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (guardrail instruction to trim)
    - `grep -Fq 'final_verdict: pending' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (frontmatter awaiting user)
    - `grep -Fq 'Phase 60' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (D10 gate mentions Phase 60 by name)
    - `grep -Fq 'Awaiting human UAT' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` exits 0 (D10 gate instructs on the specific ROADMAP status string)
    - No `v1.9.0` prefixed tag reference except as an anti-pattern: `grep -Fq 'git tag v1.9.0' .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` should exit 0 only inside the anti-patterns section (one occurrence expected — hard to assert precisely in grep, but acceptance is satisfied by presence of anti-patterns section)
    - Commit titled `docs(62): add v1.9.0 release runbook with Phase 60 UAT gate` is on HEAD: `git log -1 --format='%s' | grep -q '^docs(62): add v1.9.0 release runbook'` exits 0
    - `git log --oneline -1 -- .planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md` returns the just-created commit
  </acceptance_criteria>
  <done>62-RELEASE-RUNBOOK.md exists with the D10 Phase 60 UAT gate as the opening section plus the five required downstream sections, the Final verdict subsection, the A3 guardrail, the D7 changelog bullets verbatim, all v1.9 requirement IDs, and the anti-patterns section; committed with a `docs(62): add v1.9.0 release runbook with Phase 60 UAT gate` commit on HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook document → human operator | The runbook is the ONLY instruction set the user follows at publish time. Incorrect or missing instructions become production bugs. |
| ROADMAP.md → D10 gate grep | The gate check reads ROADMAP.md status strings. A false-negative ("Awaiting human UAT" present but Phase 60 is actually Complete) blocks a valid release; a false-positive (gate passes but UAT not actually complete) ships unreviewed code. |
| Release title string → BRAT resolver | The user types the title free-hand in the web UI. A stray `v` or suffix silently breaks BRAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-01 | T (Tampering — asset shipped without UAT) | Release artifact bundling Phase 60 code | mitigate | D10 gate is the runbook's FIRST operational section. It provides two independent greps (`- [x] **Phase 60`/`- [ ] **Phase 60` AND Progress-table row) and instructs STOP on any indicator other than Complete. Gate is worded as blocking, not advisory. |
| T-62-03-02 | S (Spoofing — tag/title/manifest mismatch) | Release title typed in web UI | mitigate | Step 3 of §Create the GitHub Release instructs typing exactly `1.9.0` — no prefix, no suffix. Anti-patterns section explicitly lists `git tag v1.9.0` and descriptive-suffix titles as forbidden. Optional curl-based API smoke-check verifies `tag_name === "1.9.0"` post-publish. |
| T-62-03-03 | D (Denial of service — zipped assets break BRAT) | Binary attach step | mitigate | Step 5 explicitly says "DO NOT zip them", lists three files by name to drag loose, and adds a "confirm three separate entries / if zip appears, remove and re-upload" instruction. Anti-patterns section repeats. |
| T-62-03-04 | I (Information disclosure — unreviewed feature in changelog) | Release Notes pasted block | mitigate | A3 guardrail paragraph at top of §Release Notes instructs the user to trim bullets for not-yet-Complete phases. D10 gate above mostly makes this moot (gate blocks publish until all three v1.9 phases are Complete), but A3 remains as a defence-in-depth. |
| T-62-03-05 | T (Tampering — versions.json regression missed) | Post-publish state of versions.json | mitigate | Anti-patterns section explicitly warns "Removing the 1.8.0 entry from versions.json — D4 PRESERVE." Preflight script (Plan 02) re-asserts on every run; runbook instructs running preflight BEFORE push, so any pre-commit drift is caught here. |

All threats MITIGATED. No credential handling in the runbook text itself (user logs into GitHub web UI under their own session). ASVS L1 sufficient.
</threat_model>

<verification>
After the task lands, these assertions must ALL pass:

```bash
RUNBOOK=.planning/phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md

# File exists
test -f "$RUNBOOK"

# All required sections
grep -Fxq '## Pre-publish check — Phase 60 UAT gate' "$RUNBOOK"
grep -Fxq '## Preflight (automated)' "$RUNBOOK"
grep -Fxq '## Push the tag' "$RUNBOOK"
grep -Fxq '## Create the GitHub Release (web UI)' "$RUNBOOK"
grep -Fxq '## Release Notes' "$RUNBOOK"
grep -Fxq '## Post-release verification' "$RUNBOOK"
grep -Fxq '### Final verdict' "$RUNBOOK"

# Required literal strings
grep -Fq 'bash .planning/phases/62-brat-release-v1.9.0/scripts/release-preflight.sh' "$RUNBOOK"
grep -Fq 'git push origin 1.9.0' "$RUNBOOK"
grep -Fq 'vegacepticon/RadiProtocol' "$RUNBOOK"
grep -Fq 'Phase 60' "$RUNBOOK"
grep -Fq 'Awaiting human UAT' "$RUNBOOK"
grep -Fq 'A3 guardrail' "$RUNBOOK"

# v1.9 requirement IDs from D7
grep -Fq 'INLINE-FIX-01' "$RUNBOOK"
grep -Fq 'INLINE-FIX-02' "$RUNBOOK"
grep -Fq 'INLINE-FIX-03' "$RUNBOOK"
grep -Fq 'INLINE-FIX-04' "$RUNBOOK"
grep -Fq 'INLINE-FIX-05' "$RUNBOOK"
grep -Fq 'SETTINGS-01' "$RUNBOOK"
grep -Fq 'BRAT-02' "$RUNBOOK"

# Frontmatter
grep -Fq 'final_verdict: pending' "$RUNBOOK"

# Commit
git log -1 --format='%s' | grep -q '^docs(62): add v1.9.0 release runbook'
```

All 22 must exit 0.
</verification>

<success_criteria>
- `62-RELEASE-RUNBOOK.md` exists in the phase directory with all required sections in order: D10 Pre-publish check → Preflight → Push the tag → Create the Release → Release Notes → Post-release verification → Anti-patterns
- D10 Phase 60 UAT gate is the FIRST operational section (the Phase 62 structural delta vs. Phase 55)
- Preflight invocation references Plan 02's script path verbatim
- Exact `git push origin 1.9.0` command present (unprefixed per D1)
- Release title instruction specifies typing exactly `1.9.0` (no v, no suffix)
- BRAT identifier `vegacepticon/RadiProtocol` referenced at least twice
- D7 changelog text from 62-CONTEXT.md pasted verbatim with all 7 v1.9 requirement IDs (INLINE-FIX-01..05, SETTINGS-01, BRAT-02) visible
- A3 guardrail paragraph present at top of Release Notes (instructs user to trim not-yet-Complete bullets)
- Pre-release flag instruction is UNCHECKED by default, conditional on D10 (per D8)
- Post-release verification includes SC-3 fresh-vault BRAT smoke test with Phase 60 + Phase 61 smoke hooks added
- Anti-patterns section re-emphasises the four Phase 55 pitfalls + the D4 preserve rule
- Runbook committed with `docs(62): add v1.9.0 release runbook with Phase 60 UAT gate` on HEAD
- Phase is now fully ready for the user to execute the D10 gate + web-UI Release
</success_criteria>

<output>
After completion, create `.planning/phases/62-brat-release-v1.9.0/62-03-SUMMARY.md` documenting: the runbook file path, the eight section headings verified, the list of v1.9 requirement IDs rendered, confirmation that the D10 gate section is present as the first operational section, and the commit SHA. The summary must explicitly state that SC-2 and SC-3 are now blocked only on (a) Phase 60 UAT signing off in ROADMAP.md and (b) user action (web-UI Release + fresh-vault install) — not on planner/executor work.
</output>
</content>
</invoke>