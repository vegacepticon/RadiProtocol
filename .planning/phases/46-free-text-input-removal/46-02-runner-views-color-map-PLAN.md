---
phase: 46-free-text-input-removal
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - src/canvas/node-color-map.ts
  - src/runner/protocol-runner.ts
  - src/runner/runner-state.ts
  - src/views/runner-view.ts
  - src/views/editor-panel-view.ts
  - src/views/node-picker-modal.ts
  - src/styles/runner-view.css
  - styles.css
  - src/styles.css
autonomous: true
requirements: [CLEAN-03]
tags: [removal, runner, views, color-map, css]

must_haves:
  truths:
    - "The Node Editor type dropdown does not offer 'Free-text input' as a choice"
    - "NODE_COLOR_MAP has no 'free-text-input' key; the TS Record<RPNodeKind,string> type enforces this"
    - "ProtocolRunner has no `enterFreeText()` method, no `case 'free-text-input':` arm, and no reference to the kind in any comment"
    - "RunnerView has no render arm for 'free-text-input'; the exhaustive switch over RunnerState / RPNode compiles clean after removal"
    - "EditorPanelView has no 'free-text-input' case in buildKindForm(); the form dispatcher is exhaustive over the current RPNodeKind"
    - "NodePickerModal's exclusion comment no longer names 'free-text-input' as an excluded kind (the kind no longer exists so the comment is stale)"
    - "CSS `.rp-free-text-input` class is deleted from src/styles/runner-view.css; styles.css + src/styles.css are regenerated by `npm run build`"
    - "`npx tsc --noEmit --skipLibCheck` exits 0 — all downstream exhaustiveness errors from Plan 46-01 are closed"
    - "npm run build exits 0"
  artifacts:
    - path: "src/canvas/node-color-map.ts"
      provides: "Record<RPNodeKind,string> without 'free-text-input' arm"
      contains_not: "free-text-input"
    - path: "src/runner/protocol-runner.ts"
      provides: "runtime with no free-text-input handling — enterFreeText method deleted, case arm in advanceThrough deleted, FreeTextInputNode import removed from resolveSeparator"
      contains_not: "free-text-input\\|FreeTextInputNode\\|enterFreeText"
    - path: "src/runner/runner-state.ts"
      provides: "AtNodeState JSDoc no longer mentions free-text-input or enterFreeText"
      contains_not: "free-text-input\\|enterFreeText"
    - path: "src/views/runner-view.ts"
      provides: "render() without the free-text-input arm; textarea with class rp-free-text-input removed"
      contains_not: "free-text-input"
    - path: "src/views/editor-panel-view.ts"
      provides: "buildKindForm without free-text-input case; dropdown comment refreshed to note CLEAN-01 removal"
      contains_not: "'free-text-input'"
    - path: "src/views/node-picker-modal.ts"
      provides: "exclusion comment trimmed — no longer names free-text-input as excluded (kind no longer exists, so it cannot be a member of the exclusion set)"
      contains_not: "free-text-input"
    - path: "src/styles/runner-view.css"
      provides: "no .rp-free-text-input rule; surrounding Phase 5/RUN-04 rules preserved byte-identically per CLAUDE.md"
      contains_not: "rp-free-text-input"
    - path: "styles.css"
      provides: "regenerated by npm run build — mirrors src/styles/*.css concatenation"
      contains_not: "rp-free-text-input"
  key_links:
    - from: "TypeScript exhaustiveness over RPNodeKind"
      to: "each consumer file in files_modified"
      via: "compile-time forcing function — every switch on RPNodeKind errors until the free-text-input arm is deleted"
      pattern: "case 'free-text-input'"
    - from: "src/styles/runner-view.css"
      to: "styles.css + src/styles.css"
      via: "npm run build esbuild CSS_FILES concatenation (esbuild.config.mjs line 31)"
      pattern: "npm run build"
---

<objective>
Close CLEAN-03 by removing every remaining `free-text-input` reference from the runner, views, color map, and CSS. Use TypeScript exhaustiveness over the shrunken `RPNodeKind` union (delivered by Plan 46-01) as the compile-time grep for stragglers.

Purpose: After Plan 46-01 red-lights the downstream build, this plan turns it green by deleting every exhaustive switch arm, the `enterFreeText()` runtime method, the runner-view render arm, the editor-panel form branch, the NODE_COLOR_MAP entry, the CSS class, and the stale comment references. No replacement behavior — this is pure excision.

Output:
- 7 source files with deletions (no additions aside from a one-line comment refresh in editor-panel-view.ts dropdown comment)
- `styles.css` + `src/styles.css` regenerated by `npm run build`
- Build green: `npx tsc --noEmit --skipLibCheck` exits 0 AND `npm run build` exits 0
- Tests NOT yet green — Plan 46-03 (wave 2 parallel) closes the test-layer breakage in `protocol-runner.test.ts` + `node-picker-modal.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/46-free-text-input-removal/46-01-graph-model-parser-validator-PLAN.md

<interfaces>
<!-- Key contracts the executor must respect or delete. Extracted from codebase. -->

From src/canvas/node-color-map.ts (line 16 — DELETE this line, surrounding arms preserved byte-identically):
```typescript
'free-text-input': '2',  // orange — user input action (same family as answer)
```
TypeScript `Record<RPNodeKind, string>` exhaustiveness enforces this after Plan 46-01 shrinks the union. The `@deprecated` `loop-start` / `loop-end` arms are UNTOUCHED (Phase 43 kept them in the union; this phase does not).

From src/runner/protocol-runner.ts — FOUR deletion sites:
- Line 20 JSDoc: `*   enterFreeText(text)       — user submits free-text input` — DELETE this line from the class JSDoc block
- Lines 214-252 — entire `enterFreeText(text: string): void` public method including its JSDoc comment header (counts ≈ 38 lines; read the file and confirm)
- Line 257 JSDoc of stepBack: `* chooseAnswer() or enterFreeText() call (D-03).` — reword to drop `or enterFreeText()` (keep `chooseAnswer()`)
- Line 344-345 JSDoc of syncManualEdit: `* Must be called BEFORE chooseAnswer() / enterFreeText() / chooseLoopBranch() so that` — reword to drop `/ enterFreeText()`
- Lines 513-517 `resolveSeparator` parameter type union: delete `| import('../graph/graph-model').FreeTextInputNode` line (line 515); preserve the other three union arms (`AnswerNode`, `TextBlockNode`, `SnippetNode`) byte-identically
- Line 529 JSDoc of advanceThrough: `* halts at nodes that require input (question, free-text-input) or terminal conditions.` — reword to `(question)` only
- Lines 594-600 — the fused `case 'question': case 'free-text-input': { ... }` arm — REMOVE `case 'free-text-input':` line so it becomes just `case 'question': { ... }` (preserve the body — halt-at-node logic is question-only now)

From src/runner/runner-state.ts — TWO deletion sites:
- Lines 12-16 AtNodeState JSDoc comment block — reword to drop `or a free-text-input node (awaiting enterFreeText)`. New text: `The node is a question (awaiting chooseAnswer).`
- Line 92 UndoEntry JSDoc: `* Captured BEFORE any mutation inside chooseAnswer() or enterFreeText().` — reword to drop `or enterFreeText()`

From src/views/runner-view.ts — ONE deletion site:
- Lines 399-413 — entire `case 'free-text-input': { ... break; }` block (15 lines including textarea with `cls: 'rp-free-text-input'`, submit button, and `enterFreeText` call). Delete the block in full. Surrounding arms (`case 'question':` above it and `default:` below it) preserved byte-identically.

From src/views/editor-panel-view.ts — TWO deletion sites + ONE comment refresh:
- Lines 463-513 — entire `case 'free-text-input': { ... break; }` block (51 lines including Setting constructors for prompt label, prefix, suffix, separator dropdown). Delete in full. The immediately preceding `case 'answer':` block (lines 421-461) and the immediately following `case 'text-block':` block (lines 515+) preserved byte-identically.
- Lines 346-348 dropdown comment: `// Phase 44 UAT-fix: legacy kinds (free-text-input, loop-start, loop-end) are NOT \n// offered here — ...` — REFRESH to: `// Phase 46 CLEAN-03: free-text-input excised entirely. Phase 44 UAT-fix: legacy\n// kinds (loop-start, loop-end) are NOT offered here — ...` (drops the `free-text-input` literal from the comma-list). Exact replacement text specified below in Task 2.
- (No dropdown .addOption to delete — verify by reading lines 339-345; the dropdown only had `start | question | answer | text-block | snippet | loop` plus `— unset —` and did NOT expose `free-text-input` even before this phase. Confirm this in read_first and record the finding.)

From src/views/node-picker-modal.ts — TWO deletion sites:
- Line 49 JSDoc: `*   - free-text-input (scheduled for removal in Phase 46, CLEAN-01..04)` — DELETE entire line (the scheduled removal has now happened)
- Line 77 inline comment: `// answer, start, free-text-input, loop-start, loop-end — сознательно исключены (D-06)` — REFRESH to drop `free-text-input`: `// answer, start, loop-start, loop-end — сознательно исключены (D-06)`

From src/styles/runner-view.css — ONE deletion:
- Lines 97-104 — the entire `.rp-free-text-input { ... }` rule block (8 lines). Delete including the trailing blank line separator. Surrounding rules (`.rp-answer-btn` above, `.rp-step-back-btn` below) preserved byte-identically.

From styles.css + src/styles.css — REGENERATED by `npm run build`, not hand-edited. `npm run build` must run at the end of this plan (mandatory per CLAUDE.md "After any CSS change").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TS exhaustiveness sweep — delete free-text-input arms from color map, runner, runner-state, editor-panel, runner-view, node-picker-modal</name>
  <files>
    - src/canvas/node-color-map.ts
    - src/runner/protocol-runner.ts
    - src/runner/runner-state.ts
    - src/views/runner-view.ts
    - src/views/editor-panel-view.ts
    - src/views/node-picker-modal.ts
  </files>
  <read_first>
    - src/canvas/node-color-map.ts (full file — confirm line 16 is the only free-text-input arm and Record<RPNodeKind,string> enforces exhaustiveness)
    - src/runner/protocol-runner.ts (lines 1-30 class JSDoc, lines 210-260 enterFreeText method, lines 250-280 stepBack JSDoc, lines 340-360 syncManualEdit JSDoc, lines 510-525 resolveSeparator type, lines 525-605 advanceThrough method with the case arms)
    - src/runner/runner-state.ts (full file — small, ~110 lines — confirm JSDoc mentions at lines 12-16 and line 92)
    - src/views/runner-view.ts (lines 315-430 render() dispatcher — confirm the `case 'free-text-input'` block boundaries)
    - src/views/editor-panel-view.ts (lines 330-370 dropdown + comment region, lines 420-520 form dispatcher — confirm buildKindForm case boundaries)
    - src/views/node-picker-modal.ts (full file — small, ~130 lines — confirm comment sites at lines 49 and 77)
    - .planning/phases/46-free-text-input-removal/46-01-graph-model-parser-validator-PLAN.md (D-46-01-A and D-46-01-D decisions — type has been hard-deleted, exhaustive switches must now follow)
    - .planning/phases/46-free-text-input-removal/46-01-graph-model-parser-validator-SUMMARY.md if exists (baseline error count from Plan 46-01)
  </read_first>
  <action>
    Per file, in the order below. Commit each file atomically only if the per-file tsc compile error count drops to 0 for THAT file's switch-site errors. If it still has errors, continue — the final tsc pass at the end of this task is what matters. Single commit at the end of Task 1 covering all six files.

    **File 1 — src/canvas/node-color-map.ts**:
    Delete line 16 exactly:
    ```
      'free-text-input': '2',  // orange — user input action (same family as answer)
    ```
    Preserve the surrounding `'answer'` line and `'text-block'` line byte-identically. Do NOT touch the `'loop-start'` / `'loop-end'` @deprecated arms (Phase 43 scope, not Phase 46).

    **File 2 — src/runner/runner-state.ts** (do this before protocol-runner.ts so the JSDoc mentions of enterFreeText are already cleaned up in the import target):
    - Lines 12-16 — replace the AtNodeState JSDoc:
      ```typescript
      /**
       * Runner is paused at a node awaiting user input.
       * The node is either a question (awaiting chooseAnswer) or a
       * free-text-input node (awaiting enterFreeText).
       */
      ```
      with:
      ```typescript
      /**
       * Runner is paused at a question node awaiting chooseAnswer.
       */
      ```
    - Line 92 — change:
      ```
       * Captured BEFORE any mutation inside chooseAnswer() or enterFreeText().
      ```
      to:
      ```
       * Captured BEFORE any mutation inside chooseAnswer().
      ```

    **File 3 — src/runner/protocol-runner.ts** (4 deletion sites + 2 JSDoc reword sites):
    - Line 20 JSDoc bullet `*   enterFreeText(text)       — user submits free-text input` — DELETE the entire line (leaving the `*   chooseAnswer(answerId)` bullet above it and `*   stepBack()` bullet below it byte-identical)
    - Lines 214-252 (the entire `enterFreeText(text: string): void` method including its `/** ... */` JSDoc header starting at line 214 and closing brace at line 252) — DELETE including one trailing blank-line separator. Read the file carefully BEFORE deleting to confirm exact line boundaries.
    - Line 257 (inside stepBack JSDoc) `chooseAnswer() or enterFreeText() call (D-03).` — replace with `chooseAnswer() call (D-03).`
    - Line 344-345 (syncManualEdit JSDoc) `Must be called BEFORE chooseAnswer() / enterFreeText() / chooseLoopBranch() so that` — replace with `Must be called BEFORE chooseAnswer() / chooseLoopBranch() so that`
    - Lines 513-517 (resolveSeparator parameter type) — delete `          | import('../graph/graph-model').FreeTextInputNode` line; result:
      ```typescript
        private resolveSeparator(
          node: import('../graph/graph-model').AnswerNode
                | import('../graph/graph-model').TextBlockNode
                | import('../graph/graph-model').SnippetNode,
        ): 'newline' | 'space' {
      ```
    - Line 529 (advanceThrough JSDoc) `halts at nodes that require input (question, free-text-input) or terminal conditions.` → `halts at nodes that require input (question) or terminal conditions.`
    - Lines 594-600 the `case 'question': case 'free-text-input': { ... }` fused arm — delete ONLY the `case 'free-text-input':` line (a single line) so the case becomes plain `case 'question': { ... }`. Preserve the body.

    **File 4 — src/views/runner-view.ts**:
    Delete the entire block spanning `case 'free-text-input': { ... break; }` (approximately lines 399-413, 15 lines). Surrounding arms untouched. Verify the deletion does NOT break the switch exhaustiveness — after removing `'free-text-input'` from RPNodeKind in Plan 46-01, the switch is exhaustive without this arm.

    **File 5 — src/views/editor-panel-view.ts** (2 deletion sites + 1 comment refresh):
    - Comment refresh at lines 346-348. Original text:
      ```typescript
              // Phase 44 UAT-fix: legacy kinds (free-text-input, loop-start, loop-end) are NOT
              // offered here — they remain in RPNodeKind only so the parser can emit MIGRATE-01
              // on legacy canvases (Phase 43 D-03). New nodes should use `loop` instead.
      ```
      Replace with:
      ```typescript
              // Phase 46 CLEAN-03: free-text-input excised entirely from RPNodeKind (46-01 D-46-01-A).
              // Phase 44 UAT-fix: legacy kinds (loop-start, loop-end) are NOT offered here — they
              // remain in RPNodeKind only so the parser can emit MIGRATE-01 on legacy canvases
              // (Phase 43 D-03). New nodes should use `loop` instead.
      ```
    - Delete the entire `case 'free-text-input': { ... break; }` block at lines 463-513 (51 lines). Preserve the preceding `case 'answer':` and the following `case 'text-block':` byte-identically.
    - Verify lines 339-345 do NOT contain an `.addOption('free-text-input', ...)` — they should already omit it per Phase 44 UAT-fix. Record confirmation in the commit body. If an `.addOption('free-text-input', ...)` IS present, delete it (contingency).

    **File 6 — src/views/node-picker-modal.ts** (2 comment refresh sites):
    - Line 49 JSDoc bullet: DELETE the entire line `*   - free-text-input (scheduled for removal in Phase 46, CLEAN-01..04)` (leaving the `*   - answer`, `*   - start`, `*   - loop-start, loop-end` surrounding bullets byte-identical)
    - Line 77 inline comment: replace `// answer, start, free-text-input, loop-start, loop-end — сознательно исключены (D-06)` with `// answer, start, loop-start, loop-end — сознательно исключены (D-06)` (drops one literal token from the list)

    After all six file edits, run:
    ```bash
    npx tsc --noEmit --skipLibCheck
    ```
    The ONLY remaining errors MUST be in `src/__tests__/runner/protocol-runner.test.ts` and `src/__tests__/node-picker-modal.test.ts` (and possibly any other test file that references the deleted type — Plan 46-03 will close these). Non-test production code MUST compile clean.

    Commit message: `feat(46-02): CLEAN-03 - excise free-text-input from color map, runner, views, picker (6 files)`.

    Do NOT modify CSS in this task — Task 2 handles CSS.
    Do NOT modify test files in this task — Plan 46-03 handles tests.
    Do NOT run `npm run build` in this task — Task 2 runs it after the CSS delete.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | grep -v "__tests__" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "free-text-input\|FreeTextInputNode\|enterFreeText" src/canvas/node-color-map.ts` returns 0
    - `grep -c "free-text-input\|FreeTextInputNode\|enterFreeText" src/runner/protocol-runner.ts` returns 0
    - `grep -c "free-text-input\|enterFreeText" src/runner/runner-state.ts` returns 0
    - `grep -c "free-text-input" src/views/runner-view.ts` returns 0
    - `grep -c "'free-text-input'" src/views/editor-panel-view.ts` returns 0
    - `grep -c "free-text-input" src/views/editor-panel-view.ts` returns exactly 1 (the refreshed comment at line 346 literally mentions "free-text-input excised entirely")
    - `grep -c "free-text-input" src/views/node-picker-modal.ts` returns 0
    - `npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | grep -v "__tests__" | wc -l` returns 0 (production code compiles clean; only test files may still error and are the Plan 46-03 scope)
    - `grep -c "enterFreeText" src/` recursive returns 0 in all non-__tests__ files (test files = Plan 46-03 scope)
    - `git log -1 --format=%s` matches `^feat\(46-02\): CLEAN-03`
  </acceptance_criteria>
  <done>All 6 production files scrubbed of free-text-input; TS production-code compile clean; test-file errors remain for Plan 46-03 to handle.</done>
</task>

<task type="auto">
  <name>Task 2: CSS delete + rebuild — remove .rp-free-text-input rule and regenerate styles.css</name>
  <files>
    - src/styles/runner-view.css
    - styles.css
    - src/styles.css
  </files>
  <read_first>
    - src/styles/runner-view.css (full file — small, ~200 lines — confirm `.rp-free-text-input` rule location at lines 97-104 and surrounding `.rp-answer-btn` above + `.rp-step-back-btn` below)
    - CLAUDE.md sections "CSS Architecture" and "After any CSS change" — re-read to reinforce: edit ONLY src/styles/runner-view.css, never styles.css; always run npm run build afterwards
    - esbuild.config.mjs CSS_FILES order (already verified: runner-view.css is the first entry per current CLAUDE.md table)
  </read_first>
  <action>
    **Step 1 — Edit src/styles/runner-view.css ONLY:**
    Delete the `.rp-free-text-input { ... }` rule block spanning lines 97-104 (the rule itself plus its trailing blank line separator). Do NOT modify any other rule. Per CLAUDE.md "Never remove existing code you didn't add" — this rule IS being removed in-scope because Phase 46 owns the removal (the rule was added for the free-text-input textarea in Phase 5 and has no other consumer; grep for `rp-free-text-input` in src/ confirms it is referenced ONLY from the runner-view.ts case arm which Task 1 deleted). Preserve `.rp-answer-btn` (line 86-95) and `.rp-step-back-btn` (line 106+) byte-identically. No new rules added.

    **Step 2 — Rebuild:**
    ```bash
    npm run build
    ```
    This regenerates both `styles.css` (repo root) and `src/styles.css` (source mirror) via esbuild's CSS_FILES concatenation plugin. Do NOT hand-edit either file.

    **Step 3 — Verify CSS regeneration:**
    - `grep -c "rp-free-text-input" styles.css` returns 0
    - `grep -c "rp-free-text-input" src/styles.css` returns 0
    - `grep -c "rp-free-text-input" src/styles/runner-view.css` returns 0
    - `grep -c "rp-answer-btn" styles.css` returns >= 1 (smoke test — unrelated rule still present)
    - `grep -c "rp-step-back-btn" styles.css` returns >= 1 (smoke test — unrelated rule still present)

    **Step 4 — Full production build + TS check:**
    ```bash
    npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | grep -v "__tests__" | wc -l
    ```
    Must return 0.

    Commit message: `style(46-02): remove .rp-free-text-input CSS rule and regenerate styles.css`.

    **Important:** styles.css and src/styles.css MUST be staged AS PART of this commit since they are generated artifacts committed to the repo (per repo convention — verify by running `git ls-files styles.css src/styles.css` to confirm both are tracked; if either is gitignored DO NOT stage it).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c "rp-free-text-input" styles.css src/styles.css src/styles/runner-view.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rp-free-text-input" src/styles/runner-view.css` returns 0
    - `grep -c "rp-free-text-input" styles.css` returns 0
    - `grep -c "rp-free-text-input" src/styles.css` returns 0
    - `grep -c "rp-answer-btn" styles.css` returns >= 1 (smoke — unrelated Phase 5 rule preserved)
    - `grep -c "rp-step-back-btn" styles.css` returns >= 1 (smoke — unrelated Phase 5 rule preserved)
    - `npm run build` exits 0
    - `npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | grep -v "__tests__" | wc -l` returns 0
    - `git log -1 --format=%s` matches `^style\(46-02\): remove \.rp-free-text-input`
    - `git diff HEAD~1 -- styles.css | wc -l` returns > 0 (proves the generated file was committed, not left stale)
  </acceptance_criteria>
  <done>CSS rule deleted from source, build regenerates both generated CSS files, production TS compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas-file → Node Editor dropdown | User-authored canvas JSON flows into the Node Editor form which mutates and writes back; dropdown must not expose a deleted type |
| protocol session → runner state | Persisted sessions (SESSION-01) may contain stale `free-text-input` node IDs; runner's missing case arm will not crash — validator/parser rejects BEFORE runner starts (handled by Plan 46-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | info | persisted session resume | accept | Sessions saved against canvases that previously had free-text-input nodes: on resume the Plan 46-01 parser rejects the canvas, RunnerView shows the parse error, user is NOT dropped into a half-broken runner — this is a clean failure mode. No action. |
| T-46-02-02 | T (tampering) | TypeScript exhaustiveness | mitigate | Every switch on RPNodeKind is enforced by TS to cover all union members after Plan 46-01 shrinks the union — no silent fall-through. Compile-time grep is the primary defense. |
| T-46-02-03 | D (DoS) | runner state machine | accept | Removal reduces state-space, not expands. No action. |
| T-46-02-04 | info | UI surprise | accept (documented) | Users who previously saw the "Free-text input" dropdown option in Node Editor no longer see it. This is the desired behavior per CLEAN-03. Dropdown already omitted the option (Phase 44 UAT-fix) — so most users will see NO behavioral change in the UI itself, only behind-the-scenes cleanup. |
| T-46-02-05 | info | CSS regression | mitigate | The `.rp-free-text-input` rule is deleted only because the `<textarea class="rp-free-text-input">` consumer is deleted in Task 1. Grep-verified: `grep -r "rp-free-text-input" src/` returns only the CSS file itself (the consumer in runner-view.ts is deleted in Task 1 of this plan, verified as pre-requisite). No orphan consumer can reference a deleted class. |
</threat_model>

<verification>
- `grep -rn "free-text-input\|FreeTextInputNode\|enterFreeText" src/ --include='*.ts' --include='*.css'` returns only matches in `src/__tests__/` (Plan 46-03 scope) AND one match in `src/views/editor-panel-view.ts` (the refreshed comment mentioning "free-text-input excised entirely")
- `npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | grep -v "__tests__" | wc -l` returns 0
- `npm run build` exits 0
- `grep -c "rp-free-text-input" styles.css src/styles.css src/styles/runner-view.css` returns three zeros
- Two commits on branch: Task 1 `feat(46-02): CLEAN-03 ...` + Task 2 `style(46-02): remove .rp-free-text-input ...`
</verification>

<success_criteria>
- Plan 46-02 closes CLEAN-03 at the runtime, view, color-map, and CSS layers.
- Production build (`npm run build`) green; `npx tsc --noEmit --skipLibCheck` green except for known `__tests__/` scope handled by Plan 46-03.
- Zero new files, zero new classes, zero new methods — pure excision per the Phase 46 goal.
- CLAUDE.md never-delete-others-code rule respected: every deletion is in-scope (free-text-input owned the rule/method/case); every surrounding rule/method/case preserved byte-identically.
</success_criteria>

<output>
After completion, create `.planning/phases/46-free-text-input-removal/46-02-runner-views-color-map-SUMMARY.md` documenting:
- Per-file deletion summary (lines removed, lines refreshed/reworded, any contingencies hit)
- Final `npx tsc --noEmit --skipLibCheck` error count (production = 0, test files = inherited from 46-01, Plan 46-03 scope)
- `npm run build` exit code
- Grep counts for `free-text-input`, `FreeTextInputNode`, `enterFreeText`, `rp-free-text-input` across src/ (expected pattern documented in verification above)
- Both commit hashes
- Confirmation that CSS regeneration committed both styles.css and src/styles.css
</output>
