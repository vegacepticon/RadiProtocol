---
phase: 72
plan: 05
plan_id: 72-05-PLAN
title: ПКОП (пояснично-крестцовый отдел позвоночника) authoring guide
type: execute
wave: 1
depends_on: []
files_modified:
  - "ПКОП 1.0.0.canvas (author's vault — not in repo)"
autonomous: false
requirements: [CANVAS-LIB-05]
tags: [canvas-authoring, content, obsidian]
must_haves:
  truths:
    - "A ПКОП canvas exists in the author's vault and runs end-to-end in the Protocol Runner without throwing"
    - "The canvas has no contrast question (spine MSCT in this template is non-contrast — RESEARCH §5)"
    - "Output contains every section header from `Z:\\projects\\references\\ПКОП остеохондроз.md`: АНОМАЛИИ РАЗВИТИЯ, СТАТИКА, ПОЗВОНКИ, МЕЖПОЗВОНКОВЫЕ ДИСКИ, ПОЗВОНОЧНЫЙ КАНАЛ, ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ, КРЕСТЕЦ, ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ"
    - "A vertebral-disc loop iterates per segment L1-L2 → L2-L3 → L3-L4 → L4-L5 → L5-S1 (5 segments per D-28; Approach A from RESEARCH §5)"
    - "Two loops total (ПОЗВОНКИ findings + МЕЖПОЗВОНКОВЫЕ ДИСКИ segments per D-10) each have exactly one `+`-prefix exit edge"
    - "The terminal output contains the verbatim 8-line Заключение and 2-line Рекомендации from `ПКОП остеохондроз.md` lines 56-68"
  artifacts:
    - path: "ПКОП 1.0.0.canvas"
      provides: "Standalone ПКОП algorithmic canvas (author's vault, not committed)"
      contains: "exactly one start; 2 loops (ПОЗВОНКИ + МЕЖПОЗВОНКОВЫЕ ДИСКИ); 8 section-header text-blocks each beginning with `\\n`; terminal Заключение text-block with verbatim multi-line conclusion"
  key_links:
    - from: "start node"
      to: "Procedure description text-block (МСКТ-исследование пояснично-крестцового отдела позвоночника...)"
      via: "single edge"
      pattern: "no contrast question for ПКОП per RESEARCH §5 (spine MSCT in this template is without contrast)"
    - from: "ПОЗВОНКИ loop and МЕЖПОЗВОНКОВЫЕ ДИСКИ loop"
      to: "next section's text-block"
      via: "loop exit edge with `+` prefix"
      pattern: "label `+Все указано, продолжаем` (Phase 50.1)"
    - from: "МЕЖПОЗВОНКОВЫЕ ДИСКИ loop body"
      to: "Per-segment fill-in template (L1-L2 / L2-L3 / L3-L4 / L4-L5 / L5-S1)"
      via: "5 body answers, one per vertebral segment, each containing per-segment text with `==…==` chips for sagittal sizes"
      pattern: "Approach A — single loop, level-as-iteration per D-28; loop iterates ≤5 times before exit"
    - from: "ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ section"
      to: "Terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение text inside terminal text-block.text matches ПКОП остеохондроз.md lines 56-63 verbatim"
---

<objective>
Author the ПКОП (пояснично-крестцовый отдел позвоночника) algorithmic canvas in the author's local vault. Single-template canvas (no contrast variant), 8 sections, 2 loops — one for vertebral-body findings (грыжи Шморля, листезы, спондилоартроз, костные разрастания) and one for the per-segment intervertebral disc enumeration L1-L2 → L5-S1 (D-10, D-28 — Approach A from RESEARCH §5: single loop iterating ≤5 segments). When run end-to-end, the assembled output structurally matches `Z:\projects\references\ПКОП остеохондроз.md`.

Purpose: deliver CANVAS-LIB-05. Recommended second-target authoring (after ГМ) per RESEARCH §"Authoring Workflow Recommendation" — vertebral-level loop is the dominant pattern.
Output: `ПКОП 1.0.0.canvas` in author's vault.
</objective>

<execution_context>
This plan is executed by the **author working manually in Obsidian Canvas UI** — not by an automated code-execution agent. There is no `npm run build`, no `vitest`, no commit to `src/`. Every task is a sequence of clicks/drags/edits in Obsidian's Canvas pane plus optional JSON inspection in a text editor for invariant verification.

`autonomous: false` for every task — each task pauses for the author.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md

External (author-side, not in repo):
- Z:\projects\references\ОГК 1.10.0.canvas (reference — read-only frozen template)
- Z:\projects\references\ПКОП остеохондроз.md (single template, 70 lines)
- Z:\projects\references\SNIPPETS\ПОЗВОНОЧНИК\ (5 ready snippets per RESEARCH §5: ОТНОСИТЕЛЬНЫЙ АНАТОМИЧЕСКИЙ СТЕНОЗ, ЭКСТРУЗИЯ, артроз атлантоосевого сустава [cervical — author may filter], без признаков грыж, состояние после СПОНДИЛОДЕЗА)
- Z:\projects\references\SNIPPETS\КОСТИ\ (used for КРЕСТЕЦ if author wants finger-on)
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Create ПКОП canvas skeleton (no contrast question)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — start node shape only
    - Z:\projects\references\ПКОП остеохондроз.md (line 4: «МСКТ-исследование пояснично-крестцового отдела позвоночника выполнено по стандартной программе с последующей мультипланарной реконструкцией.»)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§5 ПКОП" (lines 466-526) — section list and dominant loop pattern
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-05 (ПКОП standalone), D-10 (vertebral level loop), D-28 (5-iteration L1-L2..L5-S1)
  </read_first>
  <action>
    1. Create canvas `ПКОП 1.0.0.canvas` in author's working canvases folder.
    2. Drop a `start` node: `radiprotocol_nodeType:"start"`, `text:""`, `color:"4"`. Place at top.
    3. Drop a `text-block` node directly below: `text:"\nМСКТ-исследование пояснично-крестцового отдела позвоночника выполнено по стандартной программе с последующей мультипланарной реконструкцией."` (verbatim ПКОП остеохондроз.md line 4; leading `\n` per Pitfall 4).
    4. Wire start → procedure text-block. Per RESEARCH §5 spine MSCT in this template is non-contrast — no «Контраст вводился?» fan-out.
    5. Save canvas.
  </action>
  <verify>
    Manual + JSON: open `ПКОП 1.0.0.canvas` in a text editor; confirm exactly 1 `"radiprotocol_nodeType":"start"` (I1) and the procedure text-block whose `text` begins with `\nМСКТ-исследование пояснично-крестцового отдела позвоночника`.
  </verify>
  <acceptance_criteria>
    - File `ПКОП 1.0.0.canvas` exists in author's vault
    - JSON contains exactly 1 occurrence of `"radiprotocol_nodeType":"start"` (I1)
    - JSON contains a text-block whose `text` field starts with `\nМСКТ-исследование пояснично-крестцового отдела позвоночника`
    - JSON contains an edge from start node to procedure text-block (I8)
    - The start node's `color` is `"4"` (green)
    - No question node `text:"Контраст вводился?"` exists (ПКОП is single-template per RESEARCH §5)
  </acceptance_criteria>
  <done>Skeleton with start + procedure text-block exists; opens in Obsidian without errors.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Build the 6 linear sections (АНОМАЛИИ + СТАТИКА + ПОЗВОНОЧНЫЙ КАНАЛ + ИЛЕО-САКРАЛЬНЫЕ + КРЕСТЕЦ + ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ПКОП остеохондроз.md lines 6-22, 38-52 (linear sections; ПОЗВОНКИ + МЕЖПОЗВОНКОВЫЕ ДИСКИ are loops in Task 3)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§5 ПКОП — Section list" (lines 472-481)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-23
  </read_first>
  <action>
    For each linear section: drop a text-block header (`\n`-prefixed, Pitfall 4), a question, the verbatim normal-finding answer from ПКОП остеохондроз.md, and a `==напишу что не так==` free-text branch (D-23). Place ПОЗВОНКИ and МЕЖПОЗВОНКОВЫЕ ДИСКИ as placeholders for now (Task 3 wires their loops).

    Sections (each text-block's `text` MUST begin with `\n`):

    1. **АНОМАЛИИ РАЗВИТИЯ** — text-block: `\nАНОМАЛИИ РАЗВИТИЯ:`. Norm answer (verbatim ПКОП остеохондроз.md line 7): `Не выявлено.`
    2. **СТАТИКА** — text-block: `\nСТАТИКА:`. Norm answer (verbatim lines 10-11):
       ```
       Поясничный лордоз выражен обычно/ усилен/ сглажен.
       Ось позвоночника не нарушена/нарушена/ отклонена влево/ вправо, с вершиной угла на уровне L и углом наклона ХХ градусов (без нагрузки).
       ```
       (Per RESEARCH §5 row 2: small choice fan-out for лордоз variant + ось variant. Author may model as multiple answer branches OR keep as single norm with `==…==` chips. Recommended for first pass: single norm answer with `==лордоз вариант==` and `==ось вариант==` and `==L==` and `==угол==` chips at the choice slots.)
    3. **ПОЗВОНКИ** placeholder — text-block: `\nПОЗВОНКИ:`. Norm answer (verbatim lines 14-22):
       ```
       Форма тел поясничных позвонков обычная.
       Высота тел позвонков не изменена.
       Контуры тел позвонков ровные, целостность кортикальных слоев не нарушена.
       Костная структура позвонков не изменена.
       Внутрителовые узлы (грыжи) Шморля L.
       Соотношения между позвонками не нарушены. Анте/ ретролистез позвонка L до ХХ мм.
       Признаки спондилоартроза нерезко/умеренно выражены на уровнях L1-S1, проявляющиеся в неравномерном сужении суставных щелей, субхондральном склерозе, гипертрофии суставных фасеток, деформацией их остеофитами, наличии вакуум-включений.
       По передним, боковым и задним контурам тел позвонков L нерезко/ умеренно выражены костные разрастания.
       Межостистое расстояние не уменьшено/ уменьшено на уровнях L с формированием неоартрозов.
       ```
       (Loop is added in Task 3; placeholder for now. Convert literal `L` and `ХХ` placeholders to `==…==` chips in Task 5.)
    4. **МЕЖПОЗВОНКОВЫЕ ДИСКИ** placeholder — text-block: `\nМЕЖПОЗВОНКОВЫЕ ДИСКИ:`. Norm header text (verbatim lines 25-26):
       ```
       Субхондральный склероз замыкательных пластинок тел позвонков L1-S1 нерезко/ умеренно выражен.
       Неравномерно снижена высота межпозвонковых дисков L1-L2-L3-L4-L5-S1, больше выражено в сегменте L4-L5 с наименьшей высотой диска до 2-3 мм.
       ```
       (Per-segment text — Approach A loop in Task 3.)
    5. **ПОЗВОНОЧНЫЙ КАНАЛ** — text-block: `\nПОЗВОНОЧНЫЙ КАНАЛ:`. Norm (verbatim line 39): `Передне-задний размер на уровне тел позвонков до  мм (соответствует норме).` (with `==размер мм==` chip later in Task 5).
    6. **ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ** — text-block: `\nИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ:`. Norm (verbatim line 42): `Умеренно сужены, минимальным просветом до Х мм; со склерозом субхондральных слоев, с шиповидными оссификатами по передней поверхности.`
    7. **КРЕСТЕЦ** — text-block: `\nКРЕСТЕЦ:`. Norm (verbatim lines 45-49):
       ```
       Форма, размеры и структура крестца не изменены.
       Целостность кортикального слоя не нарушена.
       Патологического смещения крестцовых позвонков не выявлено.
       Крестцово-копчиковое сочленение сужено за счет спондилоартроза.
       Костно-деструктивных и костно-травматических изменений не выявлено.
       ```
    8. **ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ** — text-block: `\nПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ:`. Norm (verbatim line 52): `Не изменены.`

    Wiring: previous-section question's branches converge into the next section's text-block (mirror ОГК per RESEARCH §"Edge inventory"). Color attributes per RESEARCH §"Node-kind inventory": question=`"5"`, answer=`"2"`, text-block=`"3"`.

    Save canvas after each section.
  </action>
  <verify>
    Run canvas in Obsidian. Walks 8 sections in order (with sections 3 + 4 currently linear placeholders; loops wired in Task 3). Output contains all 8 section headers.
  </verify>
  <acceptance_criteria>
    - JSON contains exactly 8 text-block nodes whose `text` values match the 8 section headers (each beginning with `\n`): `АНОМАЛИИ РАЗВИТИЯ:`, `СТАТИКА:`, `ПОЗВОНКИ:`, `МЕЖПОЗВОНКОВЫЕ ДИСКИ:`, `ПОЗВОНОЧНЫЙ КАНАЛ:`, `ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ:`, `КРЕСТЕЦ:`, `ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ:` (I5)
    - Each section's normal-finding `answer` node carries the verbatim text from ПКОП остеохондроз.md (substring match on first 5 words)
    - Each section has a `==напишу что не так==` free-text answer (D-23) whose `text` contains `==`
    - All edges resolve (I8); BFS from start reaches all section nodes (I9 partial)
    - Running the canvas: walks all 8 sections in order without throwing
  </acceptance_criteria>
  <done>Canvas walks all 8 sections linearly; sections 3+4 wired but loop-shaping deferred to Task 3.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Wire the 2 loops — ПОЗВОНКИ findings + МЕЖПОЗВОНКОВЫЕ ДИСКИ per-segment iteration</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpt B in RESEARCH (loop with body branches and `+` exit, lines 162-177)
    - Z:\projects\references\ПКОП остеохондроз.md lines 28-36 (per-segment text for L1-L2 / L2-L3 / L3-L4 / L4-L5 / L5-S1)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Loop Modeling Pattern" (lines 218-260), § "§5 ПКОП — Loop modeling for МЕЖПОЗВОНКОВЫЕ ДИСКИ" (lines 483-490)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-10 (loop ✓ multiple vertebral levels), D-11 (`+` prefix), D-28 (5-iteration L1-L2..L5-S1; loop body re-used per level), D-12 (hybrid)
  </read_first>
  <action>
    Wire 2 loops. Per RESEARCH §5 + D-28: Approach A — single loop iterating per segment, body holds 5 segment-specific answers.

    1. **ПОЗВОНКИ findings loop** — sits AFTER the ПОЗВОНКИ norm answer; collects vertebral-body findings (грыжи Шморля, листезы, костные разрастания, спондилоартроз). Drop a `loop` node:
       - `radiprotocol_nodeType:"loop"`, `radiprotocol_headerText:"Есть изменения позвонков?"`, `text:"Есть изменения позвонков?"`, `color:"1"`.
       - Body A: answer `label:"Нет"`. Returns to header.
       - Body B: snippet folder picker `subfolderPath:"ПОЗВОНОЧНИК"`, `snippetLabel:"Выберу шаблон"`, `text:"ПОЗВОНОЧНИК"`, `color:"6"` (per RESEARCH §5 — 5 ready ПОЗВОНОЧНИК snippets). Returns to header.
       - Body C: answer `label:"Опишу вручную"`, text `==опишу изменение позвонков==`. Returns to header.
       - Exit edge: `label:"+Все указано, продолжаем"` → МЕЖПОЗВОНКОВЫЕ ДИСКИ text-block.

    2. **МЕЖПОЗВОНКОВЫЕ ДИСКИ per-segment loop (Approach A from RESEARCH §5 + D-28)** — sits AFTER the МЕЖПОЗВОНКОВЫЕ ДИСКИ norm header text; iterates per vertebral segment. Drop a `loop` node:
       - `radiprotocol_nodeType:"loop"`, `radiprotocol_headerText:"Опишу следующий сегмент?"`, `text:"Опишу следующий сегмент?"`, `color:"1"`.
       - 5 body answer branches — one per segment. Each body's edge `label` is the segment name; each body's answer node `radiprotocol_answerText` is the verbatim per-segment text from ПКОП остеохондроз.md with literal placeholder strings converted to `==…==` chips (Task 5 finalizes the chips):
         * Body L1-L2: edge `label:"L1-L2"`, answerText (verbatim line 28 with `==…==` chips substituted): `В сегменте L1-L2: диффузное симметричное выбухание диска дорсальным сагиттальным размером до ==мм== мм. Межпозвонковые отверстия на уровне диска ==степень сужения отверстий== сужены, в том числе за счет остеофитов/ элементов дугоотросчатых суставов. Просвет позвоночного канала на уровне диска ==степень сужения канала== сужен. Сагиттальный размер позвоночного канала на уровне диска до ==мм канала== мм.`
         * Body L2-L3: edge `label:"L2-L3"`, answerText (verbatim line 30): `В сегменте L2-L3: диффузное асимметричное ==сторона== выбухание диска дорсальным сагиттальным размером до ==мм== мм. Межпозвонковые отверстия ==степень сужения отверстий== сужены, больше ==сторона больше== в том числе за счет остеофитов/ элементов дугоотросчатых суставов. Просвет позвоночного канала ==степень сужения канала== сужен. Сагиттальный размер позвоночного канала до ==мм канала== мм.`
         * Body L3-L4: edge `label:"L3-L4"`, answerText (verbatim line 32, similar shape; protrusion-type per template).
         * Body L4-L5: edge `label:"L4-L5"`, answerText (verbatim line 34; экструзия type per template).
         * Body L5-S1: edge `label:"L5-S1"`, answerText (verbatim line 36; дискоостеофитный комплекс per template).
       - Each body branch returns to the loop header (Pitfall 2) — the user can pick a segment, complete its fill-ins, return to the picker, pick the next segment.
       - Exit edge: `label:"+Все сегменты описаны"` → ПОЗВОНОЧНЫЙ КАНАЛ text-block.

    Each loop has exactly 1 `+` exit (Pitfall 1, I2). Each body returns to header (Pitfall 2). The МЕЖПОЗВОНКОВЫЕ ДИСКИ loop's body answers contain segment-specific text with multiple `==…==` chips (Task 5 verifies well-formedness and adds remaining chips if missed).

    Save canvas.
  </action>
  <verify>
    Run canvas. At ПОЗВОНКИ, runner shows 4-option picker (Нет / Выберу шаблон / Опишу вручную / +). At МЕЖПОЗВОНКОВЫЕ ДИСКИ, runner shows 6-option picker (L1-L2 / L2-L3 / L3-L4 / L4-L5 / L5-S1 / +Все сегменты описаны). Picking each segment emits its verbatim per-segment text and returns to picker. Picking `+` advances to ПОЗВОНОЧНЫЙ КАНАЛ. Phase 70 visual hint marks `+` button.
  </verify>
  <acceptance_criteria>
    - JSON contains exactly 2 `"radiprotocol_nodeType":"loop"` nodes
    - The ПОЗВОНКИ loop has `radiprotocol_headerText:"Есть изменения позвонков?"` and exactly 1 `+`-prefix exit edge (I2; D-11; Pitfall 1)
    - The МЕЖПОЗВОНКОВЫЕ ДИСКИ loop has `radiprotocol_headerText:"Опишу следующий сегмент?"` and exactly 1 `+`-prefix exit edge whose label starts with `+` (e.g. `+Все сегменты описаны`)
    - The МЕЖПОЗВОНКОВЫЕ ДИСКИ loop has exactly 5 body edges with labels `L1-L2`, `L2-L3`, `L3-L4`, `L4-L5`, `L5-S1` (D-28; Pitfall 6 — labels match `displayLabel`)
    - The ПОЗВОНКИ loop has ≥3 body edges (I3); the МЕЖПОЗВОНКОВЫЕ ДИСКИ loop has ≥5 body edges (I3 + D-28)
    - Each body branch returns to its loop header (Pitfall 2)
    - The 5 segment answer texts each contain the verbatim segment marker (e.g. `В сегменте L1-L2:`) per ПКОП остеохондроз.md lines 28-36
    - The ПОЗВОНКИ snippet body carries `radiprotocol_subfolderPath:"ПОЗВОНОЧНИК"` AND NOT `radiprotocol_snippetPath` (I4)
    - Free-text body answers' `==…==` placeholders well-formed (I10)
    - Running the canvas: ПОЗВОНКИ loop cycles on body picks; МЕЖПОЗВОНКОВЫЕ ДИСКИ loop walks each of 5 segments before exit; both loops exit cleanly via `+`
  </acceptance_criteria>
  <done>Both loops cycle correctly; МЕЖПОЗВОНКОВЫЕ ДИСКИ loop emits 5 distinct per-segment outputs when all segments picked.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Insert snippet placeholder nodes for ПОЗВОНОЧНЫЙ КАНАЛ + ИЛЕО-САКРАЛЬНЫЕ + КРЕСТЕЦ + ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\SNIPPETS\ПОЗВОНОЧНИК\ folder listing (5 files per RESEARCH §5)
    - Z:\projects\references\SNIPPETS\КОСТИ\ (6 ready files for КРЕСТЕЦ if author wants finger-on)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§5 ПКОП — Snippet insertion points" (lines 492-502)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-13, D-14, D-15, D-16
  </read_first>
  <action>
    Add snippet folder pickers to non-loop sections that don't yet have them:

    | Section | Snippet attrs | Edge label |
    |---------|--------------|-----------|
    | ПОЗВОНОЧНЫЙ КАНАЛ | `subfolderPath:"ПОЗВОНОЧНИК"`, `snippetLabel:"Выберу шаблон"`, `text:"ПОЗВОНОЧНИК"`, `color:"6"` | `Выберу шаблон` (the file `ОТНОСИТЕЛЬНЫЙ АНАТОМИЧЕСКИЙ СТЕНОЗ позвоночного канала..md` matches this section per RESEARCH §5) |
    | ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ | `subfolderPath:"ПОЗВОНОЧНИК"` | `Выберу шаблон` (or skip — author's call; this section has variable text but no clean snippet match in current ПОЗВОНОЧНИК folder per RESEARCH §5) |
    | КРЕСТЕЦ | `subfolderPath:"КОСТИ"` | `Выберу шаблон по костям` (per RESEARCH §5: КОСТИ folder for болезнь Форестье / остеопороз) |
    | ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ | (skip — single-line section "Не изменены." — author may add later) | n/a |

    Per RESEARCH §5 ПОЗВОНОЧНИК folder includes: ОТНОСИТЕЛЬНЫЙ АНАТОМИЧЕСКИЙ СТЕНОЗ позвоночного канала, ЭКСТРУЗИЯ, без признаков грыж, состояние после СПОНДИЛОДЕЗА. Note: the cervical-only `артроз атлантоосевого сустава.md` may not apply to ПКОП — author can filter by snippet pick or refine path per D-14.

    All snippet nodes use folder picker (`subfolderPath`) — no file-bound `snippetPath` paths in this canvas (author's discretion per D-14/D-15). Each snippet carries exactly one of `subfolderPath` / `snippetPath` (I4). No cross-canvas snippet leakage (Pitfall 9): only `ПОЗВОНОЧНИК` and `КОСТИ` allowed.

    Save canvas.
  </action>
  <verify>
    Run canvas. Each non-loop section that received a snippet picker now offers a `Выберу шаблон` option. Picking it opens the snippet picker modal listing the available files.
  </verify>
  <acceptance_criteria>
    - JSON contains ≥2 additional snippet nodes (ПОЗВОНОЧНЫЙ КАНАЛ + КРЕСТЕЦ minimum; ИЛЕО-САКРАЛЬНЫЕ optional)
    - Total snippet nodes in canvas (incl. loop snippets from Task 3): ≥3
    - Every snippet node carries exactly one of `radiprotocol_subfolderPath` / `radiprotocol_snippetPath` (I4)
    - All `subfolderPath` values are one of `ПОЗВОНОЧНИК` or `КОСТИ`
    - No snippet path contains `ОБП`, `ОЗП`, `ОМТ`, `ОГК`, `ГМ`, `ШЕЯ` (Pitfall 9 / D-04 / D-05)
  </acceptance_criteria>
  <done>Sections offering snippet pickers do so; runner emits snippet contents when picked; no cross-canvas leakage.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Map literal placeholder strings to ==fill-in== chips (per-segment sizes, vertebral level letters, etc.)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ПКОП остеохондроз.md (search for `<% tp.file.cursor` — appears once at line 1; many literal placeholders `L`, `ХХ`, `Х`, `мм` throughout the body)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-29 (Templater + literal placeholders → `==fill-in==`)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§5 ПКОП — Note on placeholders in conclusion" (lines 517) — `L-L` and `L` are spine-level placeholders
  </read_first>
  <action>
    Per D-29: every Templater placeholder + every literal placeholder string (`L`, `ХХ`, `Х`, blank-mm slot) becomes a `==fill-in==` chip.

    1. Procedure text-block: optionally add `==тип исследования==` at the top (author's choice).
    2. СТАТИКА (Task 2 section 2) — replace literal variant-choice slots in the norm answer with chips:
       - `выражен обычно/ усилен/ сглажен` → `==лордоз вариант==`
       - `не нарушена/нарушена/ отклонена влево/ вправо` → `==ось вариант==`
       - `вершиной угла на уровне L` → `==уровень вершины==`
       - `углом наклона ХХ градусов` → `==угол градусов==`
    3. ПОЗВОНКИ norm answer (Task 2 section 3) — replace literal placeholders:
       - `грыжи) Шморля L.` → `==уровни Шморля==`
       - `позвонка L до ХХ мм` → `==позвонок==` and `==мм==`
       - `тел позвонков L нерезко/ умеренно` → `==L позвонков==` and `==степень==`
       - `на уровнях L с формированием` → `==уровни межостистого==`
    4. МЕЖПОЗВОНКОВЫЕ ДИСКИ per-segment answers (Task 3 5 body answers) — confirm each segment's text carries `==мм==`, `==степень сужения отверстий==`, `==степень сужения канала==`, `==мм канала==` chips per the Task 3 templates. Add `==сторона==` and `==сторона больше==` chips for asymmetric segments (L2-L3, L3-L4, L4-L5, L5-S1).
    5. ПОЗВОНОЧНЫЙ КАНАЛ norm — replace `до  мм` blank slot with `==размер канала мм==`.
    6. ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ norm — replace `до Х мм` with `==просвет мм==`.
    7. KРЕСТЕЦ norm — no literal placeholders to replace; verify all `==…==` from earlier tasks stay well-formed.
    8. JSON regex check: every `==` pairs on the same line (I10).

    Save canvas.
  </action>
  <verify>
    JSON regex: every `==` pairs. Run canvas; each chip is clickable in the Phase-27 fill-in modal. Output substitutes filled values correctly.
  </verify>
  <acceptance_criteria>
    - JSON contains no `<% tp.file.cursor` substring
    - JSON contains ≥15 distinct `==…==` placeholder chips (СТАТИКА ×4, ПОЗВОНКИ ×4, МЕЖПОЗВОНКОВЫЕ ДИСКИ per-segment ×~10 across 5 segments, ПОЗВОНОЧНЫЙ КАНАЛ ×1, ИЛЕО-САКРАЛЬНЫЕ ×1, plus loop free-text fills from Task 3)
    - Every `==` substring in the JSON pairs with a closing `==` on the same line (I10)
    - Running the canvas: each fill-in chip is clickable in Phase-27 modal and substitutes correctly
  </acceptance_criteria>
  <done>All Templater + literal placeholders replaced with `==fill-in==` chips; all chips well-formed.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Append terminal Заключение + Рекомендации text-block (verbatim 8-line conclusion + 2-line recommendations)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ПКОП остеохондроз.md lines 56-68 (Заключение: 8 lines; Рекомендации: 2 lines)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-19, D-22
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§5 ПКОП — Fixed Заключение" + § "Note on placeholders in conclusion" (lines 504-525)
  </read_first>
  <action>
    Drop a single terminal `text-block` node after ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ. `text` field MUST be exactly (verbatim ПКОП остеохондроз.md lines 56-68 — 8-line Заключение + 2-line Рекомендации; convert literal `L-L` and `L` placeholders to `==…==` chips per RESEARCH §5 line 517 + D-29):

    ```
    \n## Заключение\nРКТ-признаки нарушения статики поясничного отдела позвоночника.\nДегенеративные изменения межпозвонковых дисков, протрузии дисков ==уровни протрузий==.\nОстеохондроз пояснично-крестцового отдела позвоночника 2 степени.\nДеформирующий спондилоартроз.\nДеформирующий спондилез 2 степени.\nВнутрителовые узлы (грыжи) Шморля ==уровни Шморля заключение==.\nАртроз илеосакральных сочленений 2 степени.\nСпондилоартроз крестцово-копчикового сочленения.\n\n## Рекомендации\nКонсультация направившего специалиста.\nКонсультация невролога, нейрохирурга, ортопеда-травматолога по показаниям.
    ```

    Note: the literal `L-L` (line 57) becomes `==уровни протрузий==` and the literal `L` (line 61) becomes `==уровни Шморля заключение==` per D-29.

    Wire all ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ branches' terminal nodes (norm / `==напишу что не так==`) into this terminal text-block (single convergence point — Pitfall 7).

    Per D-03: optional «НА СКАНИРОВАННЫХ УРОВНЯХ» tail. Per D-05 ПКОП is standalone — strip cross-canvas snippets if D-03 included; only `ПОЗВОНОЧНИК`, `КОСТИ` paths allowed; never `ОБП`, `ОЗП`, `ОМТ`, `ОГК`, `ГМ`. Recommended: skip for first pass.

    Save canvas.
  </action>
  <verify>
    Run canvas to completion. Final assembled output's last lines contain the verbatim 8-line Заключение and 2-line Рекомендации (with `==…==` chips for the 2 literal placeholders).
  </verify>
  <acceptance_criteria>
    - JSON contains a `text-block` whose `text` value contains all 8 Заключение lines verbatim (I6); each substring of `РКТ-признаки нарушения статики`, `Остеохондроз пояснично-крестцового`, `Деформирующий спондилоартроз`, `Деформирующий спондилез 2 степени`, `Артроз илеосакральных сочленений 2 степени`, `Спондилоартроз крестцово-копчикового сочленения` present
    - JSON contains both Рекомендации lines verbatim (I7): `Консультация направившего специалиста.` AND `Консультация невролога, нейрохирурга, ортопеда-травматолога по показаниям.`
    - The 2 literal placeholders (`L-L`, `L`) in the conclusion are converted to `==…==` chips (well-formed; I10)
    - All ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ branches converge into this terminal text-block
    - Terminal text-block reachable from start by BFS over the edge DAG (I9)
    - Running the canvas to completion: assembled output's last block contains all Заключение and Рекомендации lines exactly
  </acceptance_criteria>
  <done>Terminal block reachable; output contains verbatim 8-line Заключение + 2-line Рекомендации with chips for L-L and L.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Static invariant check (verifier-runnable on the JSON)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md § "Structural Invariants" + § "Validation Sign-Off"
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls and Landmines"
  </read_first>
  <action>
    Walk I1-I10 against `ПКОП 1.0.0.canvas`:

    - **I1**: exactly 1 start node
    - **I2**: each loop has exactly 1 `+`-prefix exit edge (2 loops total — ПОЗВОНКИ + МЕЖПОЗВОНКОВЫЕ ДИСКИ)
    - **I3**: each loop has ≥1 body edge (ПОЗВОНКИ ≥3, МЕЖПОЗВОНКОВЫЕ ДИСКИ exactly 5 per D-28)
    - **I4**: every snippet has exactly one of subfolderPath/snippetPath (~3-4 snippet nodes)
    - **I5**: all 8 ПКОП section headers present as `\n`-prefixed text-block content: `АНОМАЛИИ РАЗВИТИЯ:`, `СТАТИКА:`, `ПОЗВОНКИ:`, `МЕЖПОЗВОНКОВЫЕ ДИСКИ:`, `ПОЗВОНОЧНЫЙ КАНАЛ:`, `ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ:`, `КРЕСТЕЦ:`, `ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ:`
    - **I6**: all 8 Заключение substrings (`РКТ-признаки нарушения статики поясничного отдела позвоночника.`, `Дегенеративные изменения межпозвонковых дисков`, `Остеохондроз пояснично-крестцового`, `Деформирующий спондилоартроз.`, `Деформирующий спондилез 2 степени.`, `Внутрителовые узлы (грыжи) Шморля`, `Артроз илеосакральных сочленений 2 степени.`, `Спондилоартроз крестцово-копчикового сочленения.`) present verbatim
    - **I7**: both Рекомендации lines present verbatim: `Консультация направившего специалиста.` AND `Консультация невролога, нейрохирурга, ортопеда-травматолога по показаниям.`
    - **I8**: all edges resolve
    - **I9**: BFS from start reaches every node
    - **I10**: all `==…==` well-formed; no `<% tp.file.cursor` substrings remaining

    Pitfall scan:
    - Pitfall 8: no chest/abdomen section headers leaked from copy-skeleton
    - Pitfall 9: no `ОБП`, `ОЗП`, `ОМТ`, `ОГК`, `ГМ`, `ШЕЯ` in any snippet path (per D-05 — ПКОП standalone)
    - D-28 specific: МЕЖПОЗВОНКОВЫЕ ДИСКИ loop has exactly 5 body labels matching `L1-L2`, `L2-L3`, `L3-L4`, `L4-L5`, `L5-S1` (no missing segment, no duplicate, no `L0-L1` or `S1-S2` extras)

    Fix any failures by re-opening the relevant earlier task.
  </action>
  <verify>
    All 10 invariants pass; D-28 segment-label scan finds exactly 5 segment labels.
  </verify>
  <acceptance_criteria>
    - All 10 invariants pass
    - BFS from start reaches every node
    - 5 disc-segment labels exactly match the L1-L2..L5-S1 sequence (D-28)
    - No leftover Templater syntax
    - No cross-canvas snippet leakage
    - No leftover ОГК-specific or ОБП-specific section headers (Pitfall 8)
  </acceptance_criteria>
  <done>All invariants pass; pitfall scan clean; 5 segment labels present and correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 8: Manual end-to-end verification — author runs ПКОП canvas in Obsidian (full disc-segment loop)</name>
  <files>ПКОП 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. The author manually walks the full disc-segment loop as described in the <how-to-verify> block below. The action is "open the canvas in Obsidian Protocol Runner, click Run from start, walk every applicable section, copy assembled output, visual-diff against the matching .md template at Z:\projectseferences\". See <how-to-verify> for the step-by-step.</action>
  <verify>Manual end-to-end verification per <how-to-verify> block. The runner walks start → all sections → terminal Заключение/Рекомендации without throwing; output structurally matches the matching .md template; both invariants AND runner walkthrough succeed.</verify>
  <done>Author types "approved" per <resume-signal> after confirming the runner walkthrough succeeded and output diff against the .md template is clean. CANVAS-LIB-05 satisfied.</done>
  <what-built>
    `ПКОП 1.0.0.canvas` in author's vault — start + procedure text + 8 sections + 2 loops (ПОЗВОНКИ + МЕЖПОЗВОНКОВЫЕ ДИСКИ per-segment) + ~3 snippet pickers + terminal 8-line Заключение / 2-line Рекомендации, derived verbatim from `Z:\projects\references\ПКОП остеохондроз.md`. Static invariants I1-I10 pass per Task 7.
  </what-built>
  <how-to-verify>
    Per VALIDATION.md sampling rate: ПКОП ×1 minimum (full disc-segment loop iteration, ideally per-segment). Run the canvas once, walking all 5 disc segments to verify D-28.

    1. Open Obsidian. Open `ПКОП 1.0.0.canvas`.
    2. Click "Run from start". Walk every section in order:
       - АНОМАЛИИ РАЗВИТИЯ: pick «Норма» → output adds `Не выявлено.`
       - СТАТИКА: pick «Норма» → output adds the lordoz/ось sentence with chips (fill in any clinical values via Phase-27 modal).
       - ПОЗВОНКИ: pick «Нет» (loop returns), then `+` exit. Output adds the verbatim 9-line ПОЗВОНКИ norm.
       - МЕЖПОЗВОНКОВЫЕ ДИСКИ: pick «L1-L2» → output adds the verbatim L1-L2 sentence (with chips). Loop returns to picker. Pick «L2-L3» → adds L2-L3 sentence. Continue through L3-L4, L4-L5, L5-S1. After all 5 segments emitted, pick `+Все сегменты описаны` → loop exits.
       - ПОЗВОНОЧНЫЙ КАНАЛ: pick norm → output adds with chip.
       - ИЛЕО-САКРАЛЬНЫЕ СОЧЛЕНЕНИЯ: pick norm.
       - КРЕСТЕЦ: pick norm → output adds 5-line block.
       - ПАРАВЕРТЕБРАЛЬНЫЕ МЯГКИЕ ТКАНИ: pick «Норма» → output adds `Не изменены.`
       - Terminal block emits 8-line Заключение + 2-line Рекомендации.
    3. Copy the assembled output. Visual diff against `Z:\projects\references\ПКОП остеохондроз.md` lines 4-68. All 5 disc segments must appear in the output in L1-L2 → L5-S1 order.
    4. Confirm no runner errors, no JS console errors, no stuck pickers.
    5. Per Phase 70: `+` exit buttons on the 2 loops are visually distinguishable.
    6. D-28 sanity check: confirm picking only 3 of 5 segments emits 3 segment lines (not 5, not 0) — the loop is segment-as-iteration, not segment-as-mandatory-prerequisite.
  </how-to-verify>
  <acceptance_criteria>
    - Runner walks start → 8 sections → terminal Заключение/Рекомендации without throwing (CANVAS-LIB-05 success criterion 5)
    - Every section header from ПКОП остеохондроз.md appears in the assembled output
    - All 5 disc segments (L1-L2, L2-L3, L3-L4, L4-L5, L5-S1) emit when picked (D-28)
    - The verbatim 8-line Заключение appears at the end (D-19, D-22)
    - The verbatim 2-line Рекомендации appears (`Консультация направившего специалиста.` + `Консультация невролога, нейрохирурга, ортопеда-травматолога по показаниям.`)
    - Both loops cycle on body picks and exit via `+` (D-11, Pitfall 1)
    - Fill-in chips clickable in Phase-27 modal and substituting correctly
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark CANVAS-LIB-05 satisfied. If runner threw, output mismatched, or any of the 5 disc segments missing/duplicated, describe the issue and re-open the relevant earlier task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase has no security surface. The canvas is a local file in the author's vault, edited via Obsidian UI, consumed by the existing Protocol Runner. No network, no untrusted input, no new IPC. The plugin's existing canvas-loading trust boundary (file system → plugin) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05-01 | n/a | Local vault canvas authoring | accept | No threat surface — local file authoring with no network, no new untrusted input, same trust boundary that the existing plugin canvas runner already operates within. |
</threat_model>

<verification>
**Static (verifier-runnable per VALIDATION.md):**
- All 10 structural invariants I1–I10 pass against `ПКОП 1.0.0.canvas` (Task 7)
- D-28 segment-label scan: МЕЖПОЗВОНКОВЫЕ ДИСКИ loop body labels match `L1-L2`, `L2-L3`, `L3-L4`, `L4-L5`, `L5-S1` exactly

**Manual-only (per VALIDATION.md "Manual-Only Verifications" row CANVAS-LIB-05):**
- Author runs ПКОП canvas end-to-end in Obsidian Protocol Runner; walks all 5 disc segments; copies output; visual-diffs against ПКОП остеохондроз.md (Task 8)
</verification>

<success_criteria>
**ROADMAP §"Phase 72" Success Criterion 5 (CANVAS-LIB-05):**
A ПКОП (пояснично-крестцовый отдел позвоночника) canvas exists and runs end-to-end with output matching the ПКОП `.md` text template.

**Per VALIDATION.md Sign-Off entries applicable to this plan:**
- [ ] Canvas passes I1–I10
- [ ] Canvas manually run end-to-end at least once with full disc-segment loop iteration (5 segments per D-28)
- [ ] Visual structural diff against ПКОП остеохондроз.md confirms every section heading appears
- [ ] Fixed 8-line Заключение and 2-line Рекомендации auto-emitted at terminus
- [ ] No `+`-prefix loop-exit edges missing (Pitfall 1)
- [ ] No body branch missing return-to-header edge (Pitfall 2)
- [ ] No snippet node with both subfolderPath and snippetPath (Pitfall 3 / I4)
- [ ] No cross-canvas snippet leakage (Pitfall 9 / D-04 / D-05 — ПКОП standalone)
</success_criteria>

<output>
After completion, create `.planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md` summarizing:
- Final canvas filename and vault path
- Node and edge count
- Snippet path values used (likely `ПОЗВОНОЧНИК` + `КОСТИ`)
- Approach used for МЕЖПОЗВОНКОВЫЕ ДИСКИ — A (single loop, level-as-iteration; recommended) or B (5 linear sub-sections)
- Per-segment fill-in chip count for the disc loop body answers
- Manual run-through outcome (passed / issues found and resolved)
</output>
