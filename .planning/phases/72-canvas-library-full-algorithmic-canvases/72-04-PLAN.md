---
phase: 72
plan: 04
plan_id: 72-04-PLAN
title: ОМТ full (органы малого таза) authoring guide
type: execute
wave: 1
depends_on: []
files_modified:
  - "ОМТ full 1.0.0.canvas (author's vault — not in repo)"
autonomous: false
requirements: [CANVAS-LIB-04]
tags: [canvas-authoring, content, obsidian]
must_haves:
  truths:
    - "An ОМТ full canvas exists in the author's vault and runs end-to-end in the Protocol Runner without throwing on all 4 sex×contrast paths"
    - "The canvas branches first on «Пол пациента?» (жен / муж — 2 answers per RESEARCH §4 author convenience tip), then on «Контраст вводился?» (3 answers per D-26) inside each gender branch — total 6 paths leading to 4 distinct .md template bodies"
    - "Жен branch reaches sections МОЧЕВОЙ ПУЗЫРЬ, МАТКА, ЯИЧНИКИ, КИШЕЧНИК, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, КОСТНЫЕ СТРУКТУРЫ; муж branch reaches МОЧЕВОЙ ПУЗЫРЬ, ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА, СЕМЕННЫЕ ПУЗЫРЬКИ, КИШЕЧНИК, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, КОСТНЫЕ СТРУКТУРЫ"
    - "Contrast paths additionally reach СОСУДЫ section; all 4 sex×contrast leaves additionally reach «Паховые каналы не расширены.» line per RESEARCH §4 row 10"
    - "Two loops (МАТКА + ЯИЧНИКИ — D-09, женские паттерны) each have exactly one `+`-prefix exit edge"
    - "The terminal output contains a verbatim Заключение from one of the 4 .md variants and Рекомендации `Консультация направившего специалиста.`"
  artifacts:
    - path: "ОМТ full 1.0.0.canvas"
      provides: "Standalone ОМТ full algorithmic canvas with sex × contrast fan-out (author's vault, not committed)"
      contains: "exactly one start; «Пол пациента?» 2-fan; nested «Контраст вводился?» 3-fan inside each gender; 2 loops in жен branch (МАТКА, ЯИЧНИКИ); ≥7 section-header text-blocks per gender branch each beginning with `\\n`"
  key_links:
    - from: "start node"
      to: "«Пол пациента?» question"
      via: "single edge"
      pattern: "gender first per RESEARCH §4 author convenience tip"
    - from: "Each of 2 gender answers"
      to: "Gender-specific «Контраст вводился?» question"
      via: "edge labeled `Жен` or `Муж`"
      pattern: "nested fan-out — 2×3=6 sub-paths total, leading to 4 distinct .md leaves"
    - from: "МАТКА + ЯИЧНИКИ loops (жен branch only)"
      to: "next section's text-block"
      via: "loop exit edge with `+` prefix"
      pattern: "label `+Все указано, продолжаем`"
    - from: "КОСТНЫЕ СТРУКТУРЫ section (each gender)"
      to: "Terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение text inside terminal text-block.text matches one of the 4 .md verbatim (or normalized per RESEARCH §4 recommendation)"
---

<objective>
Author the ОМТ (органы малого таза) full algorithmic canvas. Most complex Phase 72 canvas — branches twice at the top per D-26: «Пол пациента?» (жен/муж) → «Контраст вводился?» (3 answers per D-27 pattern) inside each gender, leading to 4 distinct .md template bodies (`ОМТ жен без КУ норма.md`, `ОМТ жен с КУ норма.md`, `ОМТ муж без контраста.md`, `ОМТ муж с контрастом.md`). When run end-to-end on each of the 4 sex×contrast leaves, the assembled output structurally matches the corresponding .md.

Purpose: deliver CANVAS-LIB-04. Highest fan-out and node count of the five Phase 72 canvases — recommended last per RESEARCH §"Authoring Workflow Recommendation".
Output: `ОМТ full 1.0.0.canvas` in author's vault.
</objective>

<execution_context>
This plan is executed by the **author working manually in Obsidian Canvas UI** — not by an automated code-execution agent. There is no `npm run build`, no `vitest`, no commit to `src/`. Every task is a sequence of clicks/drags/edits in Obsidian's Canvas pane plus optional JSON inspection in a text editor for invariant verification.

`autonomous: false` for every task — each task pauses for the author.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md

External (author-side, not in repo):
- Z:\projects\references\ОГК 1.10.0.canvas (reference — read-only frozen template)
- Z:\projects\references\ОМТ жен без КУ норма.md
- Z:\projects\references\ОМТ жен с КУ норма.md
- Z:\projects\references\ОМТ муж без контраста.md
- Z:\projects\references\ОМТ муж с контрастом.md
- Z:\projects\references\SNIPPETS\ОМТ\ (only 2 files: вероятно аднексит.md, придатки не визуализируются.md per RESEARCH §4 — author will expand later per D-14/D-15)
- Z:\projects\references\SNIPPETS\КОСТИ\
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Create ОМТ canvas skeleton with sex × contrast fan-out (2 × 3 = 6 sub-paths)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — first-after-start «Контраст вводился?» pattern (RESEARCH §"Conventions" rule 2)
    - All 4 ОМТ .md files line 4 each (procedure descriptions per gender × contrast)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§4 ОМТ full" (lines 414-462), specifically the "Author convenience tip" (line 424): model gender first then contrast inside each gender branch
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-26 (sex × contrast fan-out, 4 internal paths in single canvas)
  </read_first>
  <action>
    1. Create canvas `ОМТ full 1.0.0.canvas` in author's working canvases folder.
    2. Drop a `start` node: `radiprotocol_nodeType:"start"`, `text:""`, `color:"4"`.
    3. Drop a `question` node «Пол пациента?» (gender FIRST per RESEARCH §4 author convenience tip — halves visual fan-out vs contrast-first): `radiprotocol_nodeType:"question"`, `text:"Пол пациента?"`, `radiprotocol_headerText:"Пол пациента?"`, `color:"5"`. Connect start → question.
    4. Drop 2 `answer` nodes (gender):
       - Answer Жен: `displayLabel:"Жен"`, `answerText:""` (no procedure text yet — that comes after contrast). Edge `label:"Жен"`.
       - Answer Муж: `displayLabel:"Муж"`, `answerText:""`. Edge `label:"Муж"`.
    5. For EACH gender, drop a separate `question` node «Контраст вводился?» (per D-26 + RESEARCH §4 — contrast question is INSIDE each gender branch):
       - Жен-Контраст question: `text:"Контраст вводился?"`, `radiprotocol_headerText:"Контраст вводился?"`, `color:"5"`. Connect Жен answer → Жен-Контраст question.
       - Муж-Контраст question: `text:"Контраст вводился?"`, `color:"5"`. Connect Муж answer → Муж-Контраст question.
    6. For EACH of the 2 gender-Контраст questions, drop 3 `answer` nodes (Да-болюсно / Да-вручную / Нет) — total 6 contrast-disposition nodes. Procedure text per leaf:
       - **Жен × Да-болюсно** answer text (verbatim ОМТ жен с КУ норма.md line 4): `МСКТ-исследование органов малого таза выполнено по программе объемного сканирования с внутривенным болюсным контрастированием.`
       - **Жен × Да-вручную** answer text: `МСКТ-исследование органов малого таза выполнено по программе объемного сканирования с внутривенным контрастированием.` (manual contrast wording — the .md template covers boluse only; this is a reasonable variant for manual injection)
       - **Жен × Нет** answer text (verbatim ОМТ жен без КУ норма.md line 3): `МСКТ-исследование органов малого таза выполнено по программе объемного сканирования без внутривенного контрастирования.`
       - **Муж × Да-болюсно** answer text (verbatim ОМТ муж с контрастом.md line 4): `МСКТ-исследование органов малого таза выполнено по стандартному протоколу сканирования с внутривенным болюсным контрастированием.`
       - **Муж × Да-вручную** answer text: `МСКТ-исследование органов малого таза выполнено по стандартному протоколу сканирования с внутривенным контрастированием.`
       - **Муж × Нет** answer text (verbatim ОМТ муж без контраста.md line 4): `МСКТ-исследование органов малого таза выполнено по программе объемного сканирования без введения контрастного препарата.`
       Each answer's edge `label` matches its `displayLabel` (Pitfall 6).
    7. Tag each of the 6 contrast-disposition answers internally so Task 2 can route into 4 distinct section-trunks (the 6 leaves collapse to 4 .md bodies because manual-contrast and bolus-contrast share the same body text per .md template — the variant is the procedure description only).
    8. Save canvas.
  </action>
  <verify>
    Manual: open canvas. Run from start. The runner shows «Пол пациента?» picker with 2 buttons. After picking Жен or Муж, the runner shows «Контраст вводился?» with 3 buttons. Each combination emits the matching procedure description.
  </verify>
  <acceptance_criteria>
    - File `ОМТ full 1.0.0.canvas` exists in author's vault
    - Exactly 1 `"radiprotocol_nodeType":"start"` (I1)
    - 1 question node `text:"Пол пациента?"` with 2 answers (Жен / Муж)
    - 2 question nodes `text:"Контраст вводился?"` (one per gender), each with 3 answers
    - All 6 contrast-disposition answer nodes have `radiprotocol_displayLabel` matching their incoming edge `label` (Pitfall 6)
    - Running the canvas: each of 6 sex×contrast paths advances past the procedure description without throwing
    - Per Pitfall 10: all 4 distinct .md leaves are reachable from start by some path
  </acceptance_criteria>
  <done>Skeleton with sex (2-fan) × contrast (3-fan inside each gender) = 6 leaves; each emits its own procedure description.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Build the 4 trunk bodies (sex × contrast section structure)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - All 4 ОМТ .md files (full files — verbatim section text per leaf)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§4 ОМТ full — Section list" (lines 426-441) — gender-keyed table
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-23, D-26
  </read_first>
  <action>
    Build per-trunk linear sections. Per RESEARCH §4 each gender has its own gender-specific organs (МАТКА/ЯИЧНИКИ for жен, ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА/СЕМЕННЫЕ ПУЗЫРЬКИ for муж); both have shared organs (МОЧЕВОЙ ПУЗЫРЬ, КИШЕЧНИК, ЛИМФА, МЯГКИЕ ТКАНИ, КОСТНЫЕ СТРУКТУРЫ); СОСУДЫ is contrast-conditional within each gender. Loops on МАТКА + ЯИЧНИКИ (жен) are placeholders for now — Task 3 wires them.

    To reduce node count, model 4 trunks (Жен-Нет / Жен-Да / Муж-Нет / Муж-Да) collapsing the 2 contrast-yes leaves (бol/manual) into a single contrast-yes trunk per gender. Each Жен-Да and Муж-Да trunk receives BOTH «Да, болюсно» and «Да, вручную» converging edges from Task 1.

    **Жен-Нет trunk** — sections from `ОМТ жен без КУ норма.md`:
    1. text-block `\nМОЧЕВОЙ ПУЗЫРЬ:`. Norm answer (verbatim lines 6-8): `Хорошо наполнен, не деформирован. Стенки его не утолщены. Устья мочеточников без особенностей.\nСодержимое однородное жидкостное.\nТазовые сегменты мочеточников не расширены.`
    2. text-block `\nМАТКА:`. Norm answer (lines 11-12): `В положении anteflexio/retroflexio, не смещена, размеры тела матки -- мм, контуры четкие, ровные.\nПлотность не изменена. Полость матки не расширена. Шейка матки не деформирована, плотность не изменена.` (Loop placeholder for Task 3.)
    3. text-block `\nЯИЧНИКИ:`. Norm answer (lines 15-17): `В типичном месте на фоне петель кишечника не визуализируются.\nПравый яичник размерами ХХ мм, контуры четкие, ровные, плотность не изменена.\nЛевый яичник размерами ХХ мм, контуры четкие, ровные, плотность не изменена.` (Loop placeholder for Task 3.)
    4. text-block `\nКИШЕЧНИК:`. Norm answer (lines 20-25): `Прямая кишка обычного диаметра и просвета, стенка не утолщена. Сигмовидная кишка не удлинена, стенка не утолщена.\n\nПараректальная клетчатка обычной плотности.\nСедалищно-прямокишечная ямка симметрична с обеих сторон, без патологических включений.\nТазовая брюшина не утолщена.\nСкопления жидкости не выявлены.`
    5. text-block `\nЛИМФАТИЧЕСКИЕ УЗЛЫ:`. Norm: `Не увеличены.`
    6. text-block `\nМЯГКИЕ ТКАНИ:`. Norm: `Не изменены.`
    7. text-block `\nКОСТНЫЕ СТРУКТУРЫ:`. Norm: `Костно-травматических и костно-деструктивных изменений не выявлено.` (Note: ОМТ жен без КУ does NOT include the «Паховые каналы...» line; it appears only in the с-КУ and муж variants per RESEARCH §4 row 10. Author's call: include in all trunks for consistency or follow .md exactly.)

    **Жен-Да trunk** — sections from `ОМТ жен с КУ норма.md`. Same shape as Жен-Нет but with contrast-specific text and additional СОСУДЫ + Паховые каналы sections:
    1. МОЧЕВОЙ ПУЗЫРЬ — verbatim line 7: `Хорошо наполнен, не деформирован. Стенки его не утолщены, без патологического накопления контрастного препарата. Содержимое однородное жидкостное. Устья мочеточников без особенностей.\nТазовые сегменты мочеточников не расширены.`
    2. МАТКА — verbatim line 11: `В положении anteflexio/retroflexio, не смещена, размеры тела матки ХХ-ХХ-ХХ мм, контуры четкие, ровные. Плотность не изменена, без участков патологического накопления контрастного препарата. Полость матки не расширена. Шейка матки не деформирована, плотность не изменена.` (Loop placeholder.)
    3. ЯИЧНИКИ — verbatim lines 14-16. (Loop placeholder.)
    4. КИШЕЧНИК — verbatim lines 18-23.
    5. text-block `\nСОСУДЫ:` — verbatim line 26: `Контрастированы удовлетворительно, без дефектов.`
    6. ЛИМФАТИЧЕСКИЕ УЗЛЫ — verbatim line 29: `Не увеличены.`
    7. МЯГКИЕ ТКАНИ — verbatim line 32: `Не изменены.`
    8. text-block (no header — single line): `\nПаховые каналы не расширены.` (verbatim line 34).
    9. КОСТНЫЕ СТРУКТУРЫ — verbatim line 37.

    **Муж-Нет trunk** — sections from `ОМТ муж без контраста.md`:
    1. МОЧЕВОЙ ПУЗЫРЬ — verbatim lines 7-8.
    2. text-block `\nПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА:`. Norm answer (line 11): `Не увеличена, размерами ХХ-ХХ-ХХ мм. Контуры ее ровные и четкие. Структура однородная.` Linear (or short loop per RESEARCH §4 row 4 — author's call).
    3. text-block `\nСЕМЕННЫЕ ПУЗЫРЬКИ:`. Norm (line 14): `Нормальных размеров, симметричны. Угол между мочевым пузырем и семенными пузырьками четко выражен.` Linear.
    4. КИШЕЧНИК — verbatim lines 17-21.
    5. ЛИМФАТИЧЕСКИЕ УЗЛЫ — line 24.
    6. МЯГКИЕ ТКАНИ — line 27.
    7. text-block: `\nПаховые каналы не расширены.` (verbatim line 29).
    8. КОСТНЫЕ СТРУКТУРЫ — line 32.

    **Муж-Да trunk** — sections from `ОМТ муж с контрастом.md`. Same shape as Муж-Нет but with contrast-specific MOC.PUZ text + СОСУДЫ section:
    1. МОЧЕВОЙ ПУЗЫРЬ — verbatim line 7: `Достаточно наполнен, не деформирован. Стенки его не утолщены, без патологического накопления контрастного препарата. Содержимое однородное жидкостное. Устья мочеточников без особенностей.\nТазовые сегменты мочеточников не расширены.`
    2. ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА — verbatim line 11.
    3. СЕМЕННЫЕ ПУЗЫРЬКИ — verbatim line 14.
    4. КИШЕЧНИК — verbatim lines 17-21.
    5. text-block `\nСОСУДЫ:` — verbatim line 24: `Контрастированы удовлетворительно, без дефектов.`
    6. ЛИМФАТИЧЕСКИЕ УЗЛЫ — verbatim line 27.
    7. МЯГКИЕ ТКАНИ — verbatim line 30.
    8. text-block: `\nПаховые каналы не расширены.` (verbatim line 32).
    9. КОСТНЫЕ СТРУКТУРЫ — verbatim line 35.

    Each section's question allows a `==напишу что не так==` free-text branch (D-23). Loop wiring on МАТКА + ЯИЧНИКИ deferred to Task 3. Save canvas after each trunk's sections complete.
  </action>
  <verify>
    Run canvas on each of the 4 sex×contrast paths. Output for each path matches the corresponding .md template's section text.
  </verify>
  <acceptance_criteria>
    - JSON contains text-blocks (each `\n`-prefixed) for at minimum:
      - Both gender trunks: `МОЧЕВОЙ ПУЗЫРЬ:`, `КИШЕЧНИК:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:` (5 shared)
      - Жен-only: `МАТКА:`, `ЯИЧНИКИ:` (2)
      - Муж-only: `ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА:`, `СЕМЕННЫЕ ПУЗЫРЬКИ:` (2)
      - Contrast-only (each gender): `СОСУДЫ:` (2 — one per gender's contrast trunk)
      - Total ≥11 distinct section-header text-blocks (some may be duplicated across trunks per the parallel-trunk model)
    - Per RESEARCH §4 row 10 + Pitfall 10: all 4 .md template bodies are reachable from start by some path
    - The verbatim normal-finding text from each .md appears in the matching trunk's answer node (substring match on first 5 words)
    - Жен-only sections are NOT reachable from the Муж gender path (BFS confirms)
    - Муж-only sections are NOT reachable from the Жен gender path
    - СОСУДЫ in each gender trunk reachable ONLY from contrast-yes trunks (Жен-Да and Муж-Да)
    - Running each of the 4 sex×contrast leaves separately walks every applicable section without throwing
  </acceptance_criteria>
  <done>All 4 sex×contrast trunks walk their applicable linear sections; loops still pending Task 3.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Wire the 2 organ-discovery loops on the жен trunks (МАТКА + ЯИЧНИКИ — D-09)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpt B in RESEARCH (loop with `+` exit)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Loop Modeling Pattern", § "§4 ОМТ full" rows 2 and 3 (loops for миомы/изменения матки + кисты яичников)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-09 (ОМТ loops), D-11, D-12
  </read_first>
  <action>
    Per D-09: жен branch carries 2 loops — МАТКА (миомы/изменения) and ЯИЧНИКИ (cysts; left + right separately may both have a cyst). Each sits AFTER its section's norm answer; exit `+` advances. Loops apply on BOTH Жен-Нет and Жен-Да trunks; either duplicate one loop per trunk or share if trunks haven't been split (depends on Task 2's wiring choice).

    1. **МАТКА loop** — `radiprotocol_headerText:"Есть миома или другие изменения матки?"`. Body branches:
       - Body A: answer `label:"Нет"`. Returns to header.
       - Body B: snippet folder picker `subfolderPath:"ОМТ"`, `snippetLabel:"Выберу шаблон"` (per RESEARCH §4 — `SNIPPETS\ОМТ\` is sparse with only 2 files; author can refine path later per D-14). Returns to header.
       - Body C: answer `label:"Опишу вручную"`, text `==опишу изменение матки==`. Returns to header.
       - Exit: `label:"+Все указано, продолжаем"` → ЯИЧНИКИ text-block.

    2. **ЯИЧНИКИ loop** — `radiprotocol_headerText:"Есть киста или другое образование яичника?"`. Body identical shape; placeholder text `==опишу изменение яичника==`. Exit `+` → КИШЕЧНИК text-block.

    Each loop has exactly 1 `+` exit (Pitfall 1, I2). Each body returns to header (Pitfall 2). Each snippet carries `subfolderPath` only (I4). Free-text answers contain well-formed `==…==` (I10).

    The муж branch has NO loops per D-09 (the locked decision lists "ОМТ — loops ✓ (multiple fibroids/cysts/etc.)" referring to female-pelvis structures). However, RESEARCH §4 row 4 notes ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА may be modeled as a short loop for аденома/гиперплазия — author's call. Recommended: keep ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА linear in this plan (matches D-09 scope); author may add a loop later as enhancement.

    Save canvas.
  </action>
  <verify>
    Run canvas on Жен-Нет path. At МАТКА, runner shows the 4-option loop picker. Body picks loop back; `+` advances to ЯИЧНИКИ. Same at ЯИЧНИКИ. Phase 70 visual hint marks `+` button.
  </verify>
  <acceptance_criteria>
    - JSON contains ≥2 `"radiprotocol_nodeType":"loop"` nodes (or ≥4 if duplicated across Жен-Нет and Жен-Да trunks)
    - Each loop has exactly 1 `+`-prefix exit edge (I2 / D-11 / Pitfall 1)
    - Each loop has ≥3 body edges (I3)
    - Each loop's body snippet carries `radiprotocol_subfolderPath:"ОМТ"` AND NOT `radiprotocol_snippetPath` (I4)
    - Each loop body branch returns to header (Pitfall 2)
    - Loops are reachable ONLY from жен trunks; муж trunks NEVER traverse a loop node (BFS confirms)
    - Free-text body answers' `==…==` placeholders well-formed (I10)
    - Running the canvas on a жен path: loops cycle on body picks; `+` advances to next section
  </acceptance_criteria>
  <done>Both жен loops cycle and exit cleanly via `+`; муж paths remain loop-free.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Insert snippet placeholder nodes for shared sections</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\SNIPPETS\ОМТ\ folder listing (only 2 files: вероятно аднексит.md, придатки не визуализируются.md per RESEARCH §4 — sparse, author will expand)
    - Z:\projects\references\SNIPPETS\КОСТИ\ (6 ready files)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§4 ОМТ full — Snippet insertion points" (line 443)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <action>
    Add snippet folder pickers to non-loop sections. Because `SNIPPETS\ОМТ\` is sparse (2 files), most pickers reference `subfolderPath:"ОМТ"` and the runtime picker shows the available files. Author will add per-organ snippets later per D-14/D-15.

    | Section | Trunk | Snippet attrs | Edge label |
    |---------|-------|---------------|-----------|
    | МОЧЕВОЙ ПУЗЫРЬ | All 4 trunks | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА | Муж-Нет, Муж-Да | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | СЕМЕННЫЕ ПУЗЫРЬКИ | Муж-Нет, Муж-Да | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | КИШЕЧНИК | All 4 trunks | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | СОСУДЫ (contrast trunks only) | Жен-Да, Муж-Да | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | ЛИМФАТИЧЕСКИЕ УЗЛЫ | All 4 trunks | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | МЯГКИЕ ТКАНИ | All 4 trunks | `subfolderPath:"ОМТ"` | `Выберу шаблон` |
    | КОСТНЫЕ СТРУКТУРЫ | All 4 trunks | `subfolderPath:"КОСТИ"` | `Выберу шаблон по костям` |

    The МАТКА + ЯИЧНИКИ snippet pickers are already inside their loops from Task 3.

    Each snippet node carries exactly one of `subfolderPath` / `snippetPath` (I4). No cross-canvas snippet leakage (Pitfall 9): only `ОМТ` and `КОСТИ` paths allowed.

    Save canvas.
  </action>
  <verify>
    Run canvas on each of 4 sex×contrast paths. Each section offers a `Выберу шаблон` snippet picker. Picking it opens the snippet picker modal listing available files.
  </verify>
  <acceptance_criteria>
    - JSON contains snippet nodes covering each non-loop section across the 4 trunks
    - Every snippet node carries exactly one of `radiprotocol_subfolderPath` / `radiprotocol_snippetPath` (I4)
    - All `subfolderPath` values are one of `ОМТ` or `КОСТИ`
    - No snippet path contains `ОБП`, `ОЗП`, `ПКОП`, `ОГК`, `ШЕЯ` (Pitfall 9 / D-04)
  </acceptance_criteria>
  <done>All sections offer snippet pickers; runner emits snippet contents when picked.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Map Templater placeholders to ==fill-in== chips</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - All 4 ОМТ .md files (search `<% tp.file.cursor` — appears at line 1 in 3 of 4 templates; ОМТ жен без КУ норма.md does not have one at line 1 but has the literal `--` placeholder at line 11, and `ХХ` at line 16-17)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-29
  </read_first>
  <action>
    Per D-29:

    1. Replace any `<% tp.file.cursor() %>` Templater syntax in the procedure text-blocks with `==тип исследования==` chip (or omit; author's choice; recommend including in procedure text).
    2. Replace literal placeholder strings inside section bodies (per RESEARCH "Author's Discretion" + D-29 spirit): `--` and `ХХ-ХХ-ХХ` become `==размер матки мм==`, `ХХ` becomes `==размер яичника==`, `ХХ-ХХ-ХХ` for prostate becomes `==размер простаты==`.
       - Жен МАТКА answer: replace `--` with `==размер матки==` (or `==размер матки мм==`).
       - Жен ЯИЧНИКИ answer: each `ХХ мм` becomes `==размер яичника==`.
       - Жен с КУ МАТКА: `ХХ-ХХ-ХХ мм` becomes `==размер матки==`.
       - Муж ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА: `ХХ-ХХ-ХХ мм` becomes `==размер простаты==`.
    3. Verify all `==…==` placeholders previously inserted (Tasks 2-4) remain well-formed (I10).

    Save canvas.
  </action>
  <verify>
    JSON regex: every `==` pairs on the same line. Run canvas on each gender path; runner renders fill-in chips that are clickable and substitutable.
  </verify>
  <acceptance_criteria>
    - JSON contains no `<% tp.file.cursor` substring
    - JSON contains ≥6 `==…==` placeholder chips covering: размер матки, размер яичника (×2 sides) on жен trunks; размер простаты on муж trunks; plus loop free-text fills from Task 3
    - Every `==` substring pairs with a closing `==` on the same line (I10)
    - Running each gender path: fill-in chips are clickable and substitute correctly
  </acceptance_criteria>
  <done>All Templater syntax replaced; all `==fill-in==` chips well-formed.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Append terminal Заключение + Рекомендации (per RESEARCH §4 normalization recommendation)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - All 4 ОМТ .md files Заключение sections (line 38 in жен-без-КУ, line 41 in жен-с-КУ, line 36 in муж-без, line 39 in муж-с — 4 slightly different phrasings per RESEARCH §4 "Note for planner" line 454)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-19, D-22
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§4 ОМТ full — Fixed Заключение" table + recommendation (lines 446-454)
  </read_first>
  <action>
    Per RESEARCH §4 recommendation: normalize to a single Заключение across all 4 leaves. Pick the canonical phrasing from `ОМТ муж с контрастом.md` line 39 (which matches муж-без-контраста also): `РКТ-признаков патологических изменений в органах малого таза не выявлено.` This wording covers all 4 .md template variants per D-19 (each canvas ends with own Заключение — single conclusion per canvas is the locked decision).

    Drop a single terminal `text-block` node. `text` field MUST be exactly:

    ```
    \n## Заключение\nРКТ-признаков патологических изменений в органах малого таза не выявлено.\n\n## Рекомендации\nКонсультация направившего специалиста.
    ```

    Wire all 4 trunks' КОСТНЫЕ СТРУКТУРЫ branch terminal nodes (norm / snippet / free-text) into this single terminal text-block (single convergence point — Pitfall 7).

    Note: this normalization is per RESEARCH A5 assumption ("template wording differences are drift, not clinical intent"). If the author wants strict per-leaf Заключение wording, replace this single terminal text-block with 4 trunk-specific terminal text-blocks each carrying its leaf's verbatim Заключение from the matching .md. Recommended: stick with normalization per A5.

    Per D-03: optional «НА СКАНИРОВАННЫХ УРОВНЯХ» tail. If author includes, strip cross-canvas snippets (Pitfall 9) — only `ОМТ`, `КОСТИ` allowed; never `ОБП`, `ОЗП`, `ПКОП`, `ОГК`, `ГМ`. Recommended: skip for first pass.

    Save canvas.
  </action>
  <verify>
    Run canvas to completion on each of 4 sex×contrast paths. Each path's final assembled output's last lines contain the verbatim normalized Заключение and Рекомендации.
  </verify>
  <acceptance_criteria>
    - JSON contains a `text-block` whose `text` value contains both: `РКТ-признаков патологических изменений в органах малого таза не выявлено.` (I6) AND `Консультация направившего специалиста.` (I7)
    - Both substrings appear verbatim
    - All 4 КОСТНЫЕ СТРУКТУРЫ trunk terminals converge into this terminal text-block
    - Terminal text-block reachable from start by BFS on EACH of 4 sex×contrast paths (I9)
    - Running each of 4 paths to completion: output's last block contains the normalized fixed strings exactly
  </acceptance_criteria>
  <done>Terminal block reachable on all 4 paths with the normalized verbatim Заключение/Рекомендации.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Static invariant check (verifier-runnable on the JSON)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md § "Structural Invariants" + § "Validation Sign-Off"
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls and Landmines" (especially Pitfall 10 — sex×contrast 4-leaf reachability)
  </read_first>
  <action>
    Walk I1-I10 against `ОМТ full 1.0.0.canvas`:

    - **I1**: exactly 1 start
    - **I2**: each loop has exactly 1 `+` exit (≥2 loops, or ≥4 if duplicated per жен trunk)
    - **I3**: each loop has ≥1 body edge
    - **I4**: every snippet has exactly one of subfolderPath/snippetPath
    - **I5**: section headers present per gender×contrast trunk:
      - Shared: `МОЧЕВОЙ ПУЗЫРЬ:`, `КИШЕЧНИК:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:`
      - Жен: `МАТКА:`, `ЯИЧНИКИ:`
      - Муж: `ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА:`, `СЕМЕННЫЕ ПУЗЫРЬКИ:`
      - Contrast: `СОСУДЫ:` (×2, one per gender contrast trunk)
    - **I6**: `РКТ-признаков патологических изменений в органах малого таза не выявлено.` present verbatim
    - **I7**: `Консультация направившего специалиста.` present verbatim
    - **I8**: all edges resolve
    - **I9**: BFS from start reaches every node on EACH of 4 sex×contrast paths
    - **I10**: all `==…==` well-formed; no `<% tp.file.cursor` left

    Pitfall scan:
    - Pitfall 8: no chest section headers
    - Pitfall 9: no `ОБП`, `ОЗП`, `ПКОП`, `ОГК`, `ГМ` in any snippet path
    - **Pitfall 10 (ОМТ-specific)**: confirm all 4 .md template bodies are independently reachable from start. BFS the 4 sex×contrast leaves and confirm each leaf reaches its gender-and-contrast-specific section bodies. No collapsing 4 leaves into 2.

    Fix any failures by re-opening the relevant earlier task.
  </action>
  <verify>
    All 10 invariants pass on each of 4 paths. Pitfall 10 scan: 4 leaves confirmed reachable.
  </verify>
  <acceptance_criteria>
    - All 10 invariants pass
    - BFS from start reaches every node on each of the 4 sex×contrast paths
    - Pitfall 10 scan: 4 distinct .md template bodies (Жен-Нет, Жен-Да, Муж-Нет, Муж-Да) each independently reachable; gender-specific organs scoped to their gender; contrast-only sections scoped to contrast trunks
    - No leftover Templater syntax
    - No cross-canvas snippet leakage
  </acceptance_criteria>
  <done>All invariants pass; all 4 sex×contrast leaves verified reachable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 8: Manual end-to-end verification — author runs ОМТ canvas in Obsidian (all 4 sex×contrast paths)</name>
  <files>ОМТ full 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. The author manually walks the 4 sex×contrast paths as described in the <how-to-verify> block below. The action is "open the canvas in Obsidian Protocol Runner, click Run from start, walk every applicable section, copy assembled output, visual-diff against the matching .md template at Z:\projectseferences\". See <how-to-verify> for the step-by-step.</action>
  <verify>Manual end-to-end verification per <how-to-verify> block. The runner walks start → all sections → terminal Заключение/Рекомендации without throwing; output structurally matches the matching .md template; both invariants AND runner walkthrough succeed.</verify>
  <done>Author types "approved" per <resume-signal> after confirming the runner walkthrough succeeded and output diff against the .md template is clean. CANVAS-LIB-04 satisfied.</done>
  <what-built>
    `ОМТ full 1.0.0.canvas` in author's vault — start + «Пол пациента?» 2-fan + nested «Контраст вводился?» 3-fan inside each gender + 4 trunk bodies + 2 loops on жен trunks (МАТКА + ЯИЧНИКИ) + ~8 snippet pickers + terminal normalized Заключение/Рекомендации, derived verbatim from the 4 ОМТ .md templates. Static invariants I1-I10 pass per Task 7.
  </what-built>
  <how-to-verify>
    Per VALIDATION.md sampling rate: ОМТ ×4 (run all 4 sex×contrast combinations). Pitfall 10 — author MUST verify all 4 leaves work, not collapse to 2.

    1. Open Obsidian. Open `ОМТ full 1.0.0.canvas`.
    2. **Run 1 — Жен × Нет:**
       a. Pick Жен → Нет.
       b. Walk МОЧЕВОЙ ПУЗЫРЬ → МАТКА (loop, pick «Нет», then `+`) → ЯИЧНИКИ (loop, pick «Нет», then `+`) → КИШЕЧНИК → ЛИМФА → МЯГКИЕ ТКАНИ → КОСТНЫЕ СТРУКТУРЫ → terminal.
       c. Confirm СОСУДЫ section is SKIPPED.
       d. Visual diff against `Z:\projects\references\ОМТ жен без КУ норма.md` lines 3-43.
    3. **Run 2 — Жен × Да:**
       a. Pick Жен → Да, болюсно. Walk through. Confirm СОСУДЫ + Паховые каналы lines ARE visited.
       b. Visual diff against `Z:\projects\references\ОМТ жен с КУ норма.md` lines 4-46.
    4. **Run 3 — Муж × Нет:**
       a. Pick Муж → Нет. Walk through. Confirm ПРЕДСТАТЕЛЬНАЯ ЖЕЛЕЗА + СЕМЕННЫЕ ПУЗЫРЬКИ ARE visited; МАТКА/ЯИЧНИКИ are NOT visited; loops are NOT triggered.
       b. Visual diff against `Z:\projects\references\ОМТ муж без контраста.md` lines 4-41.
    5. **Run 4 — Муж × Да:**
       a. Pick Муж → Да, болюсно. Walk through. Confirm СОСУДЫ visited.
       b. Visual diff against `Z:\projects\references\ОМТ муж с контрастом.md` lines 4-44.
    6. Confirm no runner errors, no JS console errors, no stuck pickers on any of the 4 runs.
    7. Per Phase 70: confirm `+` exit buttons on the 2 жен loops are visually distinguishable.
    8. Pitfall 10 sanity check: confirm picking Муж + Да-болюсно vs Муж + Да-вручную emits different procedure descriptions but the same downstream sections (manual-vs-bolus collapses on body, not on procedure text).
  </how-to-verify>
  <acceptance_criteria>
    - All 4 sex×contrast paths run start → terminal without throwing (CANVAS-LIB-04 success criterion 4)
    - Every section header from the matching .md appears in the assembled output for each of the 4 paths
    - Pitfall 10 satisfied: gender-specific organs visited only inside their gender; contrast-only СОСУДЫ visited only on contrast trunks
    - Verbatim normalized Заключение `РКТ-признаков патологических изменений в органах малого таза не выявлено.` appears at the end of each path (per Task 6 normalization, A5)
    - Verbatim Рекомендации `Консультация направившего специалиста.` appears
    - Both жен loops cycle on body picks and exit via `+` (D-11, Pitfall 1); муж paths NEVER traverse a loop
    - Fill-in chips clickable and substituting correctly
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark CANVAS-LIB-04 satisfied. If issues — especially Pitfall 10 (any of the 4 leaves missing), describe and re-open the relevant earlier task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase has no security surface. The canvas is a local file in the author's vault, edited via Obsidian UI, consumed by the existing Protocol Runner. No network, no untrusted input, no new IPC. The plugin's existing canvas-loading trust boundary (file system → plugin) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04-01 | n/a | Local vault canvas authoring | accept | No threat surface — local file authoring with no network, no new untrusted input, same trust boundary that the existing plugin canvas runner already operates within. |
</threat_model>

<verification>
**Static (verifier-runnable per VALIDATION.md):**
- All 10 structural invariants I1–I10 pass against `ОМТ full 1.0.0.canvas` (Task 7)
- Pitfall 10 scan: all 4 sex×contrast .md template bodies independently reachable from start

**Manual-only (per VALIDATION.md "Manual-Only Verifications" row CANVAS-LIB-04):**
- Author runs all 4 sex×contrast paths end-to-end in Obsidian Protocol Runner; copies output; visual-diffs against the 4 .md templates (Task 8)
</verification>

<success_criteria>
**ROADMAP §"Phase 72" Success Criterion 4 (CANVAS-LIB-04):**
An ОМТ (органы малого таза — full version) canvas exists and runs end-to-end with output matching the full ОМТ `.md` text template.

**Per VALIDATION.md Sign-Off entries applicable to this plan:**
- [ ] Canvas passes I1–I10
- [ ] Manually run all 4 sex×contrast paths (4× total)
- [ ] Visual diff against all 4 ОМТ .md templates confirms every heading appears in the matching path's output
- [ ] Fixed normalized Заключение and Рекомендации auto-emitted at terminus on every path
- [ ] No `+`-prefix loop-exit edges missing (Pitfall 1)
- [ ] No body branch missing return-to-header edge (Pitfall 2)
- [ ] No snippet node with both subfolderPath and snippetPath (Pitfall 3 / I4)
- [ ] No cross-canvas snippet leakage (Pitfall 9 / D-04)
- [ ] Pitfall 10: 4 sex×contrast leaves all reachable, none collapsed
</success_criteria>

<output>
After completion, create `.planning/phases/72-canvas-library-full-algorithmic-canvases/72-04-SUMMARY.md` summarizing:
- Final canvas filename and vault path
- Node and edge count (expected highest of the 5 canvases)
- Whether trunks were duplicated per sex×contrast or shared via convergence nodes
- Snippet path values used (likely all `ОМТ` flat picker — author will refine per D-14)
- Whether Заключение was normalized to a single string (per A5) or kept per-leaf
- Manual run-through outcome for each of the 4 sex×contrast paths
</output>
