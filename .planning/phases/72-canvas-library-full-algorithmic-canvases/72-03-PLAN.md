---
phase: 72
plan: 03
plan_id: 72-03-PLAN
title: ОЗП (органы забрюшинного пространства) authoring guide
type: execute
wave: 1
depends_on: []
files_modified:
  - "ОЗП 1.0.0.canvas (author's vault — not in repo)"
autonomous: false
requirements: [CANVAS-LIB-03]
tags: [canvas-authoring, content, obsidian]
must_haves:
  truths:
    - "An ОЗП canvas exists in the author's vault and runs end-to-end in the Protocol Runner without throwing (both contrast branches)"
    - "The first question after start is «Контраст вводился?» with 3 answers (Да, болюсно / Да, вручную / Нет) per D-27"
    - "Output for the no-contrast branch contains every section header from `Z:\\projects\\references\\ОЗП без контраста.md` (НАДПОЧЕЧНИКИ, ПРАВАЯ ПОЧКА, ЛЕВАЯ ПОЧКА, МОЧЕТОЧНИКИ, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, КОСТНЫЕ СТРУКТУРЫ)"
    - "Output for the contrast branch additionally contains МОЧЕВОЙ ПУЗЫРЬ and СОСУДЫ section headers per `ОЗП с контрастом.md`"
    - "Three loops (НАДПОЧЕЧНИКИ + ПРАВАЯ ПОЧКА + ЛЕВАЯ ПОЧКА — D-08) each have exactly one `+`-prefix exit edge"
    - "The terminal output contains the verbatim Заключение `РКТ-признаков патологических изменений органов забрюшинного пространства не выявлено.` and Рекомендации `Консультация направившего специалиста.`"
  artifacts:
    - path: "ОЗП 1.0.0.canvas"
      provides: "Standalone ОЗП algorithmic canvas (author's vault, not committed)"
      contains: "exactly one start; one «Контраст вводился?» question with 3 answers; 3 loops (НАДПОЧЕЧНИКИ, ПРАВАЯ ПОЧКА, ЛЕВАЯ ПОЧКА); ≥7 section-header text-blocks (≥9 with МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ on contrast branch) each beginning with `\\n`"
  key_links:
    - from: "start node"
      to: "«Контраст вводился?» question"
      via: "single edge"
      pattern: "first-after-start question per ОГК reference convention rule 2"
    - from: "Each of the 3 loops"
      to: "next section's text-block"
      via: "loop exit edge with `+` prefix"
      pattern: "label `+Все указано, продолжаем` (Phase 50.1)"
    - from: "КОСТНЫЕ СТРУКТУРЫ section"
      to: "Terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение text inside terminal text-block.text matches the .md verbatim"
---

<objective>
Author the ОЗП (органы забрюшинного пространства) algorithmic canvas in the author's local vault. Modeled on `Z:\projects\references\ОГК 1.10.0.canvas` for shape; uses section lists from BOTH `Z:\projects\references\ОЗП без контраста.md` and `ОЗП с контрастом.md` (D-27 — single canvas with «Контраст вводился?» fan-out). When run end-to-end, the assembled output structurally matches the corresponding contrast-branch .md.

Purpose: deliver CANVAS-LIB-03. Three loops (kidneys × 2 + adrenals) plus contrast-conditional sections (МОЧЕВОЙ ПУЗЫРЬ, СОСУДЫ).
Output: `ОЗП 1.0.0.canvas` in author's vault.
</objective>

<execution_context>
This plan is executed by the **author working manually in Obsidian Canvas UI** — not by an automated code-execution agent. There is no `npm run build`, no `vitest`, no commit to `src/`. Every task is a sequence of clicks/drags/edits in Obsidian's Canvas pane plus optional JSON inspection in a text editor for invariant verification.

`autonomous: false` for every task — each task pauses for the author.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md

External (author-side, not in repo):
- Z:\projects\references\ОГК 1.10.0.canvas (reference — read-only frozen template)
- Z:\projects\references\ОЗП без контраста.md (без-КУ branch verbatim text)
- Z:\projects\references\ОЗП с контрастом.md (с-КУ branch verbatim text)
- Z:\projects\references\SNIPPETS\ОЗП\ (flat folder, 7 files: АНГИОМИОЛИПОМА, аденома надпочечника, киста КТА, киста НАТИВ, кисты почек КТА, кисты почек НАТИВ, неполное удвоение ЧЛС)
- Z:\projects\references\SNIPPETS\КОСТИ\
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Create ОЗП canvas skeleton with «Контраст вводился?» fan-out</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — first-after-start «Контраст вводился?» pattern (RESEARCH §"Conventions" rule 2)
    - Z:\projects\references\ОЗП без контраста.md line 4 (procedure: «МСКТ-исследование органов забрюшинного пространства выполнено по программе объемного сканирования с последующей мультипланарной реконструкцией.»)
    - Z:\projects\references\ОЗП с контрастом.md lines 4-5 (procedure with contrast: «...с внутривенным болюсным контрастированием. Исследование произведено в положении пациента лежа на спине.»)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§3 ОЗП" (lines 362-410)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-27
  </read_first>
  <action>
    1. Create canvas `ОЗП 1.0.0.canvas` in author's working canvases folder.
    2. Drop a `start` node: `radiprotocol_nodeType:"start"`, `text:""`, `color:"4"`.
    3. Drop a `question` node «Контраст вводился?»: `radiprotocol_nodeType:"question"`, `text:"Контраст вводился?"`, `radiprotocol_headerText:"Контраст вводился?"`, `color:"5"`. Connect start → question.
    4. Drop 3 `answer` nodes per D-27:
       - Answer A: `displayLabel:"Да, болюсно"`, `answerText:"МСКТ-исследование органов забрюшинного пространства выполнено по программе объемного сканирования с внутривенным болюсным контрастированием."` (ОЗП с контрастом.md line 4). Edge `label:"Да, болюсно"`.
       - Answer B: `displayLabel:"Да, вручную"`, `answerText:"МСКТ-исследование органов забрюшинного пространства выполнено по программе объемного сканирования с внутривенным контрастированием."`. Edge `label:"Да, вручную"`.
       - Answer C: `displayLabel:"Нет"`, `answerText:"МСКТ-исследование органов забрюшинного пространства выполнено по программе объемного сканирования с последующей мультипланарной реконструкцией."` (ОЗП без контраста.md line 4). Edge `label:"Нет"`.
    5. Drop a `text-block` convergence node common to all branches: `text:"\nИсследование произведено в положении пациента лежа на спине."` (ОЗП с контрастом.md line 5; the no-contrast template omits this exact line, but the canvas can include it harmlessly OR the no-contrast Answer C may bypass it — author's call. Recommend: include for both, single shared text-block).
    6. Wire all 3 answers → convergence text-block (3 edges).
    7. Per RESEARCH §3 row 5: МОЧЕВОЙ ПУЗЫРЬ section is contrast-only; per row 6: СОСУДЫ on contrast or the «Жидкости в забрюшинном пространстве не выявлено.» single line on no-contrast. Plan two parallel trunks downstream of the convergence (or use a hidden state node) so contrast-conditional sections can be skipped/added correctly. Recommended: duplicate the trunk into 2 parallel paths after the convergence — one per contrast disposition — re-converging at ЛИМФАТИЧЕСКИЕ УЗЛЫ.
    8. Save canvas.
  </action>
  <verify>
    Manual: open canvas in Obsidian. Run from start; the picker «Контраст вводился?» shows 3 buttons. Pick each — output emits the matching procedure description.
  </verify>
  <acceptance_criteria>
    - File `ОЗП 1.0.0.canvas` exists in author's vault
    - Exactly one `"radiprotocol_nodeType":"start"` (I1)
    - Question node `text:"Контраст вводился?"` exists
    - 3 answer nodes with `radiprotocol_displayLabel` "Да, болюсно" / "Да, вручную" / "Нет" connected from the question
    - Edge labels match the `displayLabel` values (Pitfall 6)
    - 3 contrast-answer nodes converge into the procedure text-block (or into 2 parallel trunks, both reachable)
    - Running the canvas: each of 3 contrast paths advances past the convergence without throwing
  </acceptance_criteria>
  <done>Skeleton with start + 3-fan + procedure text-block convergence; both contrast and no-contrast trunks routable.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Build the ОЗП linear sections (НАДПОЧЕЧНИКИ + 2 kidneys + МОЧЕТОЧНИКИ + contrast-conditional + tail)</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОЗП без контраста.md (full file — section text per organ)
    - Z:\projects\references\ОЗП с контрастом.md (full file — contrast-branch section text)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§3 ОЗП — Section list" (lines 367-380)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-23
  </read_first>
  <action>
    For each section, drop a text-block header (`\n`-prefixed, Pitfall 4), a question, the verbatim norm answer (per-contrast where text differs), and a `==напишу что не так==` free-text branch (D-23). Loops on НАДПОЧЕЧНИКИ + ПРАВАЯ ПОЧКА + ЛЕВАЯ ПОЧКА are placeholders for now (Task 3).

    Sections (each section's header text begins with `\n`):

    1. **НАДПОЧЕЧНИКИ** — text-block: `\nНАДПОЧЕЧНИКИ:`. Norm answer (no-contrast, ОЗП без контраста.md line 7): `Расположены обычно, типичной инвертированной Y-образной формы, не увеличены. Контуры ровные, чёткие. Дополнительных образований в надпочечниках не выявлено. Окружающая жировая клетчатка не изменена.` Norm answer (contrast, ОЗП с контрастом.md line 8): `Расположены обычно, типичной инвертированной Y-образной формы, не увеличены. Контуры ровные, чёткие. Дополнительных образований и патологических фокусов накопления контрастного средства в обоих надпочечниках не выявлено. Окружающая жировая клетчатка не изменена.` Use 2 separate norm answers per branch.
    2. **ПРАВАЯ ПОЧКА** — text-block: `\nПРАВАЯ ПОЧКА:`. Norm (no-contrast, line 10): `Положение, форма и размеры почки обычные, до Х-Х-Х мм. Контуры ровные, чёткие. Паренхима не истончена. Структура почки однородная. Чашечно-лоханочная система почки не расширена, не деформирована. В почке рентгенопозитивных конкрементов не выявлено. Околопочечная клетчатка не изменена/тяжиста.` Norm (contrast, line 11) extends with `Структура почки однородная, без дополнительных патологических фокусов накопления контрастного средства. ... Функция почки не нарушена.`
    3. **ЛЕВАЯ ПОЧКА** — text-block: `\nЛЕВАЯ ПОЧКА:`. Same shape as ПРАВАЯ ПОЧКА per .md lines 13/14, separate norm answers per branch.
    4. **МОЧЕТОЧНИКИ** — text-block: `\nМОЧЕТОЧНИКИ:`. Norm (no-contrast line 16): `На видимом уровне без нарушения просвета, без патологического содержимого.` Norm (contrast line 17): `Прослеживаются на всем протяжении, не расширены, без патологического содержимого.` Linear.
    5. **МОЧЕВОЙ ПУЗЫРЬ (contrast only)** — text-block: `\nМОЧЕВОЙ ПУЗЫРЬ:`. Norm (contrast line 20): `Обычного наполнения, с четкими ровными контурами, стенка не изменена; без патологического рентгенопозитивного содержимого; без патологических фокусов накопления контрастного средства.` Linear; ONLY on contrast trunk.
    6. **СОСУДЫ (contrast only) OR «Жидкости...» single line (no-contrast)** — text-block (contrast): `\nСОСУДЫ:`. Norm (contrast lines 23-24): `Визуализируются удовлетворительно, дефектов контрастирования не выявлено. Аорта в брюшном отделе не расширена. Стенка аорты не изменена.\nЖидкости в забрюшинном пространстве не выявлено.` On no-contrast trunk: drop a single-line text-block `\nЖидкости в забрюшинном пространстве не выявлено.` (verbatim ОЗП без контраста.md line 18).
    7. **ЛИМФАТИЧЕСКИЕ УЗЛЫ** — text-block: `\nЛИМФАТИЧЕСКИЕ УЗЛЫ:`. Norm (both): `Не увеличены.` Linear (re-convergence point).
    8. **МЯГКИЕ ТКАНИ** — text-block: `\nМЯГКИЕ ТКАНИ:`. Norm (both): `Без патологических изменений.` Linear.
    9. **КОСТНЫЕ СТРУКТУРЫ** — text-block: `\nКОСТНЫЕ СТРУКТУРЫ:`. Norm (both): `Костно-деструктивные, костно-травматические изменения не определяются. Дегенеративно-дистрофические изменения позвоночника нерезко/умеренно выражены.` Linear (or short Умеренно/Нерезко dual-degeneration question per RESEARCH §3).

    Wiring: contrast trunk and no-contrast trunk each route through their own copies of sections 1-4, then split at section 5/6 (МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ vs the «Жидкости...» single line), then re-converge at ЛИМФАТИЧЕСКИЕ УЗЛЫ and continue linearly.

    Save after each section.
  </action>
  <verify>
    Run canvas on each contrast branch. Output for no-contrast branch matches ОЗП без контраста.md lines 4-27; output for contrast branch matches ОЗП с контрастом.md lines 4-33. Loops still pending Task 3.
  </verify>
  <acceptance_criteria>
    - JSON contains text-block headers for at least: `НАДПОЧЕЧНИКИ:`, `ПРАВАЯ ПОЧКА:`, `ЛЕВАЯ ПОЧКА:`, `МОЧЕТОЧНИКИ:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:` (no-contrast trunk; I5 partial)
    - JSON additionally contains `МОЧЕВОЙ ПУЗЫРЬ:` and `СОСУДЫ:` on the contrast trunk
    - Each section's norm answer carries the verbatim text from the matching .md
    - МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ are reachable ONLY from the contrast trunk; the «Жидкости...» single-line text-block is reachable ONLY from the no-contrast trunk (BFS confirms)
    - Both trunks re-converge at ЛИМФАТИЧЕСКИЕ УЗЛЫ
    - Running each contrast branch separately walks every applicable section without throwing
  </acceptance_criteria>
  <done>Both contrast trunks walk all applicable linear sections; loops still pending.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Wire the 3 organ-discovery loops (НАДПОЧЕЧНИКИ + ПРАВАЯ ПОЧКА + ЛЕВАЯ ПОЧКА)</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpt B in RESEARCH (loop with `+` exit)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Loop Modeling Pattern", § "§3 ОЗП — Insertion guidance" (lines 393-396)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-08, D-11, D-12
  </read_first>
  <action>
    Per D-08: НАДПОЧЕЧНИКИ + ПРАВАЯ ПОЧКА + ЛЕВАЯ ПОЧКА each may carry образования/lesions/конкременты — each gets its own loop. Each loop sits AFTER its section's norm answer; exit `+` advances to the next section. Per RESEARCH §3, snippet pickers in these loops use folder-picker `subfolderPath:"ОЗП"` (flat folder — runtime picker shows all 7 ОЗП snippets; author can refine path per D-14/D-15).

    1. **НАДПОЧЕЧНИКИ loop** — `radiprotocol_headerText:"Есть образование надпочечника?"`. Body branches:
       - Body A: answer `label:"Нет"`. Returns to header.
       - Body B: snippet folder picker `subfolderPath:"ОЗП"`, `snippetLabel:"Выберу готовый шаблон"`. Returns to header.
       - Body C: answer `label:"Опишу вручную"`, text `==опишу образование надпочечника==`. Returns to header.
       - Exit: `label:"+Все указано, продолжаем"` → ПРАВАЯ ПОЧКА text-block.

    2. **ПРАВАЯ ПОЧКА loop** — `radiprotocol_headerText:"Есть образование/конкремент в правой почке?"`. Body:
       - "Нет" / snippet folder `subfolderPath:"ОЗП"` / "Опишу вручную" with `==опишу изменение правой почки==`. Each body returns to header.
       - Exit `+` → ЛЕВАЯ ПОЧКА text-block.

    3. **ЛЕВАЯ ПОЧКА loop** — `radiprotocol_headerText:"Есть образование/конкремент в левой почке?"`. Body identical shape; placeholder text `==опишу изменение левой почки==`. Exit `+` → МОЧЕТОЧНИКИ text-block.

    Each loop has exactly one `+` exit (Pitfall 1, I2). Each body branch returns to its loop header (Pitfall 2). Each snippet node carries `subfolderPath:"ОЗП"` (NOT `snippetPath`) per I4. The free-text answers contain well-formed `==…==` (I10).

    The loops apply on BOTH contrast trunks. If trunks were duplicated in Task 1, then loops are duplicated — each duplicate must independently satisfy I2/I3.

    Save canvas.
  </action>
  <verify>
    Run canvas. At each of the 3 sections (НАДПОЧЕЧНИКИ, ПРАВАЯ ПОЧКА, ЛЕВАЯ ПОЧКА), the runner shows a 4-option picker (Нет / Выберу готовый шаблон / Опишу вручную / +Все указано, продолжаем). Body picks loop back; `+` advances to next section. Phase 70 visual hint marks the `+` button.
  </verify>
  <acceptance_criteria>
    - JSON contains ≥3 `"radiprotocol_nodeType":"loop"` nodes (or ≥6 if duplicated per trunk)
    - Each loop has exactly 1 `+`-prefix exit edge (I2 / D-11 / Pitfall 1)
    - Each loop has ≥3 body edges (I3)
    - Each loop body snippet carries `radiprotocol_subfolderPath:"ОЗП"` AND NOT `radiprotocol_snippetPath` (I4)
    - Each loop body branch returns to header (Pitfall 2)
    - All free-text body answers' `==…==` placeholders are well-formed (I10)
    - Running the canvas: loops cycle on body picks; `+` advances to next section
  </acceptance_criteria>
  <done>All 3 loops cycle and exit cleanly via `+` on both contrast branches.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Insert snippet placeholder nodes for the remaining sections (МОЧЕТОЧНИКИ optional, МОЧЕВОЙ ПУЗЫРЬ, СОСУДЫ, ЛИМФА, МЯГКИЕ, КОСТИ)</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\SNIPPETS\ОЗП\ folder listing (7 flat files per RESEARCH §3 — АНГИОМИОЛИПОМА, аденома надпочечника, киста КТА, киста НАТИВ, кисты почек КТА, кисты почек НАТИВ, неполное удвоение ЧЛС)
    - Z:\projects\references\SNIPPETS\КОСТИ\
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§3 ОЗП — Snippet insertion points" (lines 381-396)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <action>
    Add a snippet folder picker to each remaining section (sections without dedicated subfolders use the flat `ОЗП` folder picker, which the runtime picker enumerates per D-14/D-15):

    | Section | Snippet attrs | Edge label |
    |---------|--------------|-----------|
    | МОЧЕТОЧНИКИ | `radiprotocol_subfolderPath:"ОЗП"`, `radiprotocol_snippetLabel:"Выберу шаблон"`, `text:"ОЗП"`, `color:"6"` | `Выберу шаблон` (optional — author may skip if no мочеточник-specific snippets) |
    | МОЧЕВОЙ ПУЗЫРЬ (contrast trunk only) | `radiprotocol_subfolderPath:"ОЗП"` | `Выберу шаблон` |
    | СОСУДЫ (contrast trunk only) | `radiprotocol_subfolderPath:"ОЗП"` | `Выберу шаблон` (sparse — author may add file-bound paths later) |
    | ЛИМФАТИЧЕСКИЕ УЗЛЫ | `radiprotocol_subfolderPath:"ОЗП"` | `Выберу шаблон` |
    | МЯГКИЕ ТКАНИ | `radiprotocol_subfolderPath:"ОЗП"` | `Выберу шаблон` |
    | КОСТНЫЕ СТРУКТУРЫ | `radiprotocol_subfolderPath:"КОСТИ"` | `Выберу шаблон по костям` |

    Per RESEARCH §3 "Insertion guidance": the contrast-vs-native split for kidney/cyst snippets is reflected in the snippet filenames (`киста КТА.md` vs `киста НАТИВ.md`). Author may keep both visible via the flat `ОЗП` picker, or refine paths post-hoc per D-14.

    Each snippet node carries exactly one of `subfolderPath` / `snippetPath` (I4). No cross-canvas snippet leakage (Pitfall 9): only `ОЗП` and `КОСТИ` paths allowed.

    Save canvas.
  </action>
  <verify>
    Run canvas; each non-loop section now offers a `Выберу шаблон` snippet picker. Picking it opens the snippet picker modal listing the 7 ready ОЗП snippets (or the 6 KОСТИ snippets for the КОСТНЫЕ СТРУКТУРЫ section).
  </verify>
  <acceptance_criteria>
    - JSON contains ≥5 additional snippet nodes (one per the listed sections; total in canvas including loop snippets ≥8)
    - Every snippet node carries exactly one of `radiprotocol_subfolderPath` / `radiprotocol_snippetPath` (I4)
    - All `subfolderPath` values are one of `ОЗП` or `КОСТИ`
    - No snippet path contains `ОБП`, `ОМТ`, `ПКОП`, `ОГК`, `ШЕЯ` (Pitfall 9 / D-04)
  </acceptance_criteria>
  <done>All sections offer snippet pickers; runner emits snippet contents when picked; no cross-canvas leakage.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Map any Templater placeholders to ==fill-in== chips</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОЗП без контраста.md (search for `<% tp.file.cursor` — appears once at line 1)
    - Z:\projects\references\ОЗП с контрастом.md (search — line 1 only)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-29
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls 5"
  </read_first>
  <action>
    ОЗП templates have only the header `<% tp.file.cursor() %>` placeholder at line 1; no per-section size placeholders. Per D-29:

    1. Add a `==тип исследования==` fill-in chip at the top of the procedure text-block (or omit; author's choice).
    2. The kidney norm-answer texts contain «до Х-Х-Х мм» literal text — these are clinical placeholders the radiologist edits in-line; convert to `==размер почки мм==` chips inside both kidney norm-answer nodes (per D-29 spirit, Phase 27 chip is more usable than free-text «Х-Х-Х»).
    3. The МОЧЕТОЧНИКИ / aorta sections also have «до» / «не/нерезко/умеренно» variant text — author may convert to chips or keep as plain text. Recommend converting `Х-Х-Х` patterns and `Х мм` patterns to `==…==` chips for runtime substitution.
    4. Verify all `==…==` placeholders previously inserted (Tasks 2-4) remain well-formed (Pitfall 5 / I10).
    5. JSON regex: every `==` pairs on the same line; total count even.

    Save canvas.
  </action>
  <verify>
    JSON regex: every `==` pairs. Run canvas; runner renders fill-in chips that are clickable and substitutable.
  </verify>
  <acceptance_criteria>
    - JSON contains no occurrences of `<% tp.file.cursor` (D-29)
    - JSON contains ≥4 `==…==` placeholders (kidney size chips × 2, optional `==тип исследования==`, plus loop free-text fills from Task 3)
    - Every `==` substring pairs with a closing `==` on the same line (I10)
    - Running the canvas: fill-in chips are clickable and substitute correctly
  </acceptance_criteria>
  <done>Templater syntax replaced; all `==fill-in==` chips well-formed.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Append terminal Заключение + Рекомендации text-block</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОЗП без контраста.md lines 30-36 (Заключение and Рекомендации — verbatim, identical in both contrast variants per RESEARCH §3)
    - Z:\projects\references\ОЗП с контрастом.md lines 36-42 (confirm identical)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-19, D-22
  </read_first>
  <action>
    Drop a single terminal `text-block` node after КОСТНЫЕ СТРУКТУРЫ. `text` field MUST be exactly:

    ```
    \n## Заключение\nРКТ-признаков патологических изменений органов забрюшинного пространства не выявлено.\n\n## Рекомендации\nКонсультация направившего специалиста.
    ```

    Both contrast variants share this Заключение/Рекомендации text — single terminal text-block serves both branches. Wire all КОСТНЫЕ СТРУКТУРЫ branches' terminal nodes (norm / snippet picker return / free-text) into this terminal text-block (single convergence; Pitfall 7).

    Per D-03: optional «НА СКАНИРОВАННЫХ УРОВНЯХ» tail. If author includes, strip cross-canvas snippet leakage (Pitfall 9) — only `ОЗП`, `КОСТИ`, possibly `ШЕЯ` (lower-neck adjacency) allowed; never `ОБП`, `ОМТ`, `ПКОП`, `ОГК`. Recommended: skip for first pass.

    Save canvas.
  </action>
  <verify>
    Run canvas to completion on either contrast branch. Final assembled output's last lines contain the verbatim Заключение and Рекомендации.
  </verify>
  <acceptance_criteria>
    - JSON contains a `text-block` whose `text` value contains both: `РКТ-признаков патологических изменений органов забрюшинного пространства не выявлено.` (I6) AND `Консультация направившего специалиста.` (I7)
    - Both substrings appear verbatim
    - All КОСТНЫЕ СТРУКТУРЫ branches converge into this terminal text-block
    - Terminal text-block reachable from start by BFS on BOTH contrast branches (I9)
    - Running each contrast branch to completion: assembled output's last block contains both fixed strings exactly
  </acceptance_criteria>
  <done>Terminal block reachable on both branches with verbatim Заключение/Рекомендации.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Static invariant check (verifier-runnable on the JSON)</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md § "Structural Invariants" + § "Validation Sign-Off"
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls and Landmines"
  </read_first>
  <action>
    Walk I1-I10 against `ОЗП 1.0.0.canvas`:

    - **I1**: exactly 1 start node
    - **I2**: each loop has exactly 1 `+`-prefix exit edge (≥3 loops, or ≥6 if duplicated per trunk)
    - **I3**: each loop has ≥1 body edge
    - **I4**: every snippet node has exactly one of subfolderPath/snippetPath
    - **I5**: section headers `НАДПОЧЕЧНИКИ:`, `ПРАВАЯ ПОЧКА:`, `ЛЕВАЯ ПОЧКА:`, `МОЧЕТОЧНИКИ:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:` present as `\n`-prefixed text-block content; on contrast trunk also `МОЧЕВОЙ ПУЗЫРЬ:`, `СОСУДЫ:`
    - **I6**: `РКТ-признаков патологических изменений органов забрюшинного пространства не выявлено.` present verbatim
    - **I7**: `Консультация направившего специалиста.` present verbatim
    - **I8**: all edges resolve
    - **I9**: BFS from start reaches every node on BOTH contrast trunks
    - **I10**: all `==…==` well-formed; no `<% tp.file.cursor` substrings

    Pitfall scan:
    - Pitfall 8: no chest section headers
    - Pitfall 9: no `ОБП`, `ОМТ`, `ПКОП`, `ОГК` in any snippet path

    Fix any failures by re-opening the relevant earlier task.
  </action>
  <verify>
    All 10 invariants pass on both contrast branches. Pitfall scan clean.
  </verify>
  <acceptance_criteria>
    - All 10 invariants pass
    - BFS from start reaches every node on each of the 2 contrast trunks
    - No leftover Templater syntax
    - No cross-canvas snippet leakage
  </acceptance_criteria>
  <done>All invariants pass; pitfall scan clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 8: Manual end-to-end verification — author runs ОЗП canvas in Obsidian (both contrast branches)</name>
  <files>ОЗП 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. The author manually walks the 2 contrast branches as described in the <how-to-verify> block below. The action is "open the canvas in Obsidian Protocol Runner, click Run from start, walk every applicable section, copy assembled output, visual-diff against the matching .md template at Z:\projectseferences\". See <how-to-verify> for the step-by-step.</action>
  <verify>Manual end-to-end verification per <how-to-verify> block. The runner walks start → all sections → terminal Заключение/Рекомендации without throwing; output structurally matches the matching .md template; both invariants AND runner walkthrough succeed.</verify>
  <done>Author types "approved" per <resume-signal> after confirming the runner walkthrough succeeded and output diff against the .md template is clean. CANVAS-LIB-03 satisfied.</done>
  <what-built>
    `ОЗП 1.0.0.canvas` in author's vault — start + «Контраст вводился?» 3-fan + 7-9 sections (depending on branch) + 3 loops (НАДПОЧЕЧНИКИ + ПРАВАЯ ПОЧКА + ЛЕВАЯ ПОЧКА) + ~6 snippet pickers + terminal Заключение/Рекомендации, derived verbatim from `Z:\projects\references\ОЗП без контраста.md` and `ОЗП с контрастом.md`. Static invariants I1-I10 pass per Task 7.
  </what-built>
  <how-to-verify>
    Per VALIDATION.md sampling rate: ОЗП ×2 (run both contrast branches).

    1. Open Obsidian. Open `ОЗП 1.0.0.canvas`.
    2. **Run 1 — no contrast branch:**
       a. Click "Run from start". Pick «Нет» at Контраст question.
       b. Walk every section. At each of 3 loops (NadPoch, ПР почка, ЛЕВ почка), pick "Нет" once → loop returns; then `+` exit.
       c. Confirm МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ are SKIPPED; the «Жидкости в забрюшинном пространстве не выявлено.» line appears instead.
       d. Walk to terminal. Copy assembled output. Visual diff against `Z:\projects\references\ОЗП без контраста.md` lines 4-36.
    3. **Run 2 — contrast branch:**
       a. Pick «Да, болюсно». Walk every section. Confirm МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ ARE visited.
       b. Confirm all 3 loops cycle and exit.
       c. Walk to terminal. Visual diff against `Z:\projects\references\ОЗП с контрастом.md` lines 4-42.
    4. No runner errors, no JS console errors, no stuck pickers.
    5. Phase 70 visual hint: confirm `+` exit buttons are visually distinguishable on each loop.
  </how-to-verify>
  <acceptance_criteria>
    - Both contrast paths run start → terminal without throwing (CANVAS-LIB-03 success criterion 3)
    - Every section header from the matching .md appears in the output for each branch
    - Verbatim Заключение `РКТ-признаков патологических изменений органов забрюшинного пространства не выявлено.` appears
    - Verbatim Рекомендации `Консультация направившего специалиста.` appears
    - МОЧЕВОЙ ПУЗЫРЬ + СОСУДЫ on contrast branch only; «Жидкости в забрюшинном пространстве не выявлено.» on no-contrast branch only
    - All 3 loops cycle on body picks and exit via `+` (D-11, Pitfall 1)
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark CANVAS-LIB-03 satisfied. If issues, describe them and re-open the relevant earlier task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase has no security surface. The canvas is a local file in the author's vault, edited via Obsidian UI, consumed by the existing Protocol Runner. No network, no untrusted input, no new IPC. The plugin's existing canvas-loading trust boundary (file system → plugin) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-01 | n/a | Local vault canvas authoring | accept | No threat surface — local file authoring with no network, no new untrusted input, same trust boundary that the existing plugin canvas runner already operates within. |
</threat_model>

<verification>
**Static (verifier-runnable per VALIDATION.md):**
- All 10 structural invariants I1–I10 pass against `ОЗП 1.0.0.canvas` (Task 7)

**Manual-only (per VALIDATION.md "Manual-Only Verifications" row CANVAS-LIB-03):**
- Author runs both contrast branches end-to-end in Obsidian Protocol Runner; copies output; visual-diffs against the matching .md (Task 8)
</verification>

<success_criteria>
**ROADMAP §"Phase 72" Success Criterion 3 (CANVAS-LIB-03):**
An ОЗП canvas exists and runs end-to-end with output matching the ОЗП `.md` text template.

**Per VALIDATION.md Sign-Off entries applicable to this plan:**
- [ ] Canvas passes I1–I10
- [ ] Manually run both contrast branches (2× total)
- [ ] Visual diff against ОЗП без контраста.md and ОЗП с контрастом.md confirms every heading appears
- [ ] Fixed Заключение and Рекомендации auto-emitted at terminus
- [ ] No `+`-prefix loop-exit edges missing (Pitfall 1)
- [ ] No body branch missing return-to-header edge (Pitfall 2)
- [ ] No snippet node with both subfolderPath and snippetPath (Pitfall 3 / I4)
- [ ] No cross-canvas snippet leakage (Pitfall 9 / D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/72-canvas-library-full-algorithmic-canvases/72-03-SUMMARY.md` summarizing:
- Final canvas filename and vault path
- Node and edge count
- Snippet path values used
- Whether trunks were duplicated per contrast disposition or single trunk with conditional sections
- Manual run-through outcome for each contrast branch
</output>
