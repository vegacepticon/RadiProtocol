---
phase: 70-loop-exit-picker-visual-hint
plan: 01
id: 70-01-loop-exit-css-hint
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/loop-support.css
  - styles.css
autonomous: false
requirements:
  - LOOP-EXIT-01
tags:
  - css
  - runner
  - loop
  - visual-hint

must_haves:
  truths:
    # Sourced 1:1 from the 4 ROADMAP §Phase 70 Success Criteria
    - "SC#1 (LOOP-EXIT-01): the loop-exit button (the picker entry whose edge label starts with '+' per Phase 50.1) renders with a visibly different background/accent than body-branch buttons in the picker, using existing Obsidian theme variables only (no new colour tokens)."
    - "SC#2 (LOOP-EXIT-01): the loop-exit button retains original shape, label text (with '+' stripped per Phase 50.1 stripExitPrefix convention), and position — only background/accent/text-color changes."
    - "SC#3 (LOOP-EXIT-01): body-branch buttons (.rp-loop-body-btn) render with the exact same styling as before this phase — no visual regression on non-exit entries; Phase 44 rule at loop-support.css:73-80 stays byte-for-byte."
    - "SC#4 (LOOP-EXIT-01): the visual hint applies in all three runner modes — sidebar Runner View, tab Runner View, and Inline Runner — verified by reaching a loop picker state in each mode."
    # Locked decision invariants from CONTEXT.md (D-01..D-12)
    - "D-01: exit gets solid `var(--color-green)` background; body stays on `var(--interactive-accent)` unchanged."
    - "D-02: Phase 44 .rp-loop-body-btn rule (loop-support.css:73-76) and its :hover (lines 78-80) are NOT modified — append-only per phase per CLAUDE.md."
    - "D-03: solid background + `color: var(--text-on-accent)` is the marker — no border, no ::before, no icon."
    - "D-04: `--text-on-accent` reused for text colour (no parented `--text-on-color-green` exists; pragmatic re-use; theme contrast covered by UAT)."
    - "D-05: hover state = `filter: brightness(0.92)` (no `--color-green-hover` token in Obsidian)."
    - "D-06: focus-visible NOT overridden — Obsidian default focus ring used."
    - "D-07: NO `:disabled` rule (loop picker always renders enabled buttons; runner-view.ts:647-657 / inline-runner-modal.ts:494-501 confirm)."
    - "D-08: ONE file (`src/styles/loop-support.css`), ONE append section. Selector `.rp-loop-exit-btn` (no mode-prefix) covers all 3 modes — DOM identical. `inline-runner.css:218-219` Phase 60 padding override stays untouched (orthogonal to colour)."
    - "D-09: section header is exactly `/* Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01) */` placed above the rules."
    - "D-10: after CSS edit run `npm run build`; commit `src/styles/loop-support.css` AND root `styles.css` atomically (esbuild CSS_FILES concatenation per esbuild.config.mjs)."
    - "D-11: manual UAT in 3 modes covers — colour hint visible, hover dim ~8%, Obsidian focus ring intact, no body regression, 3-colour visual rhythm (yellow iteration label + blue body + green exit)."
    - "D-12: existing class-presence regression tests in `src/__tests__/views/inline-runner-modal-loop-body-file-bound.test.ts` (lines 461-505) and `src/__tests__/views/runner-snippet-loop-body-file-bound.test.ts` (lines 267-311) MUST stay green; existing logic-level test `src/__tests__/runner/protocol-runner-loop-picker.test.ts` MUST stay green; NO new vitest tests written (CSS rules don't load in JSDOM)."
  artifacts:
    - path: "src/styles/loop-support.css"
      provides: "Phase 70 append-only section adding `.rp-loop-exit-btn { background: var(--color-green); color: var(--text-on-accent); } .rp-loop-exit-btn:hover { filter: brightness(0.92); }` below existing line 111"
      contains: "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)"
      min_lines: 117
    - path: "styles.css"
      provides: "Build artefact regenerated by esbuild CSS_FILES concatenation; contains the same Phase 70 section as loop-support.css"
      contains: "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)"
  key_links:
    - from: "src/views/runner-view.ts:648"
      to: "src/styles/loop-support.css (Phase 70 section)"
      via: "DOM class `rp-loop-exit-btn` assigned when isExitEdge(edge) === true"
      pattern: "cls: exit \\? 'rp-loop-exit-btn' : 'rp-loop-body-btn'"
    - from: "src/views/inline-runner-modal.ts:495"
      to: "src/styles/loop-support.css (Phase 70 section)"
      via: "DOM class `rp-loop-exit-btn` assigned when isExitEdge(edge) === true"
      pattern: "cls: exit \\? 'rp-loop-exit-btn' : 'rp-loop-body-btn'"
    - from: "src/styles/loop-support.css"
      to: "styles.css (root)"
      via: "esbuild CSS_FILES concat plugin (esbuild.config.mjs cssPlugin onEnd)"
      pattern: "Phase 70: Loop-exit picker visual hint"

deferred_items:
  - "Border-left stripe fallback variant (`border-left: 3px solid var(--color-green); background: var(--background-modifier-border); color: var(--text-normal);`) — ad-hoc rollback if UAT (Task 4) reports a contrast issue between `--color-green` and `--text-on-accent` in default Obsidian themes. NOT a separate plan; the executor applies it in-place ONLY if Task 4 fails the contrast checkpoint."
  - "Configurable accent token (Settings) — REQUIREMENTS.md Out-of-Scope row 'Visual-customisation tokens for the loop-exit highlight'."
  - "Node Editor visual hint for `+`-prefixed edges — Phase 50.1 deferred bucket; not v1.11."
  - "Animation/transition on background change — out-of-scope (REQUIREMENTS 'subtle ... without changing shape/label/position')."
  - "Custom focus-visible outline on exit button — D-06 relies on Obsidian default; deferred until accessibility audit."
  - "Multi-exit visual differentiation — Phase 50.1 D-01 locks exactly 1 exit edge per loop; not relevant until that constraint relaxes."
  - "Nyquist Dimension 8 (RESEARCH/VALIDATION absent) — Phase 70 ran without RESEARCH.md / VALIDATION.md artefacts as a deliberate user choice on the Nyquist gate (CSS-only phase, manual UAT covers visual verification per D-11). Soft warning acknowledged at planning time; not re-litigated during execution."

user_setup: []
---

<objective>
Append a CSS-only visual hint to `src/styles/loop-support.css` that paints the loop-picker exit button (`.rp-loop-exit-btn`) with a solid `--color-green` background + `--text-on-accent` text + `filter: brightness(0.92)` on hover, while leaving the Phase 44 body-button rule (`.rp-loop-body-btn`) byte-for-byte unchanged. Regenerate `styles.css` via `npm run build`, confirm the existing class-presence regression tests stay green, then perform the 7-checkpoint manual UAT in sidebar / tab / inline runner modes.

Purpose: satisfies LOOP-EXIT-01 — a radiologist running a protocol with a loop node sees at a glance which picker entry continues past the loop (green = finish) versus which entries continue iterating (blue = body branch), reusing the established Phase 44 yellow iteration-label colour for visual rhythm (yellow = current iteration, blue = body branch, green = exit) without introducing new colour tokens.

Output:
- `src/styles/loop-support.css` lines 112+ contain a single Phase 70 append section (header comment + 2 selector rules: base + :hover).
- `styles.css` (root) regenerated by `npm run build` containing the same section.
- Existing test surface (`runner-snippet-loop-body-file-bound.test.ts`, `inline-runner-modal-loop-body-file-bound.test.ts`, `protocol-runner-loop-picker.test.ts`) still passes — no new tests added.
- Manual UAT log appended at the bottom of this plan during Task 4 (or in a sibling `70-UAT.md`) recording PASS/FAIL on the 7 D-11 checkpoints.

Per CONTEXT.md D-08, Phase 70 is one plan covering: append CSS → build → test verification → UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-loop-exit-picker-visual-hint/70-CONTEXT.md
@src/styles/loop-support.css
@src/styles/inline-runner.css
@src/views/runner-view.ts
@src/views/inline-runner-modal.ts
@esbuild.config.mjs
@src/__tests__/runner/protocol-runner-loop-picker.test.ts
@src/__tests__/views/runner-snippet-loop-body-file-bound.test.ts
@src/__tests__/views/inline-runner-modal-loop-body-file-bound.test.ts

<interfaces>
<!-- DOM contract: `.rp-loop-exit-btn` is set by both render paths when isExitEdge(edge) === true. -->
<!-- Phase 70 attaches CSS rules to this hook — does NOT touch the TS render code. -->

From src/views/runner-view.ts (sidebar Runner View, lines 629-657):
```typescript
// One button per outgoing edge of the loop node.
const outgoing = this.graph.edges.filter(e => e.fromNodeId === state.nodeId);
const list = questionZone.createDiv({ cls: 'rp-loop-picker-list' });
for (const edge of outgoing) {
  const exit = isExitEdge(edge);                              // Phase 50.1 D-09 — '+'-prefix discrimination
  let caption: string;
  if (exit) {
    caption = stripExitPrefix(edge.label ?? '');              // strips '+' before display
  } else {
    const target = this.graph.nodes.get(edge.toNodeId);
    caption = target !== undefined ? nodeLabel(target) : edge.toNodeId;
  }
  const btn = list.createEl('button', {
    cls: exit ? 'rp-loop-exit-btn' : 'rp-loop-body-btn',     // ← THE CSS HOOK Phase 70 styles
    text: caption,
  });
  // ...click handler omitted...
}
```

From src/views/inline-runner-modal.ts (Inline Runner, lines 485-501):
```typescript
for (const edge of outgoing) {
  const exit = isExitEdge(edge);
  let caption: string;
  if (exit) {
    caption = stripExitPrefix(edge.label ?? '');
  } else {
    const target = this.graph.nodes.get(edge.toNodeId);
    caption = target !== undefined ? nodeLabel(target) : edge.toNodeId;
  }
  const btn = list.createEl('button', {
    cls: exit ? 'rp-loop-exit-btn' : 'rp-loop-body-btn',     // ← Same hook, identical class semantics
    text: caption,
  });
  btn.addEventListener('click', () => {
    void this.handleLoopBranchClick(edge, exit);
  });
}
```

From src/styles/loop-support.css (existing Phase 6 / 44 / 47 — DO NOT MODIFY, only append below line 111):
```css
/* Phase 44 body-button rule — STAYS BYTE-FOR-BYTE per D-02 */
.rp-loop-body-btn {                                /* line 73 */
  background: var(--interactive-accent);
  color: var(--text-on-accent);
}                                                  /* line 76 */
.rp-loop-body-btn:hover {                          /* line 78 */
  background: var(--interactive-accent-hover);
}                                                  /* line 80 */

/* Phase 44 exit-button rule — STAYS, but Phase 70 append section overrides background/color */
.rp-loop-exit-btn {                                /* line 82 */
  background: var(--background-modifier-border);   /* ← Phase 70 will override via cascade */
  color: var(--text-normal);                       /* ← Phase 70 will override via cascade */
}
.rp-loop-exit-btn:hover {                          /* line 87 */
  background: var(--background-modifier-border-hover, var(--background-modifier-border));
  opacity: 0.9;                                    /* ← Phase 70 will override via brightness filter cascade */
}

/* Phase 47 typography (lines 96-111) — STAYS, orthogonal to colour */
```

From src/styles/inline-runner.css (Phase 60 padding override — DO NOT MODIFY):
```css
.rp-inline-runner-content .rp-answer-btn,
.rp-inline-runner-content .rp-snippet-branch-btn,
.rp-inline-runner-content .rp-loop-body-btn,
.rp-inline-runner-content .rp-loop-exit-btn,                /* line 219 */
.rp-inline-runner-content .rp-step-back-btn,
.rp-inline-runner-content .rp-skip-btn {
  padding: var(--size-2-3) var(--size-4-2);                 /* orthogonal to colour — Phase 70 D-08 */
  min-height: 28px;
}
```

From esbuild.config.mjs (CSS_FILES concat — `loop-support` already listed at index 5):
```js
const CSS_FILES = [
  'runner-view',
  'canvas-selector',
  'editor-panel',
  'snippet-manager',
  'snippet-fill-modal',
  'loop-support',          // ← Phase 70 edits this file; NO change to CSS_FILES array
  'snippet-tree-picker',
  'inline-runner',
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Phase 70 visual-hint section to src/styles/loop-support.css</name>
  <files>src/styles/loop-support.css</files>

  <read_first>
    - `CLAUDE.md` — re-read "CSS Architecture", "Critical Rules for Editing Shared Files", "CSS files: append-only per phase", "After any CSS change". Phase 70 must obey: append-only, never delete existing rules, run `npm run build` after.
    - `.planning/phases/70-loop-exit-picker-visual-hint/70-CONTEXT.md` — re-read the `<decisions>` block in full (D-01..D-12). The exact CSS to write is dictated by D-01, D-03, D-04, D-05, D-09. The hover variant is mandated by D-05. Do NOT add `:focus-visible` (D-06) or `:disabled` (D-07).
    - `src/styles/loop-support.css` — confirm the current file ends at line 111 with the Phase 47 typography section. Confirm Phase 44 body-btn rule lives at lines 73-80 and is NOT touched. Confirm Phase 44 base exit-btn rule lives at lines 82-90 (Phase 70's new rule cascades over it via specificity-equal-but-source-order rule).
    - `src/styles/inline-runner.css` lines 218-224 — confirm Phase 60 padding override targets `.rp-inline-runner-content .rp-loop-exit-btn` (mode-prefixed selector, orthogonal to background/color). Phase 70 D-08 truth: this file is NOT touched.
  </read_first>

  <action>
    Open `src/styles/loop-support.css`. Move cursor to the end of file (line 111 is the closing `}` of Phase 47's `.rp-loop-body-btn, .rp-loop-exit-btn` typography rule).

    Append the following EXACT block — verbatim, no edits, no formatting changes — starting on a new line after line 111. Use Edit tool with `old_string` matching the closing brace of the Phase 47 rule (line 111 `}`) plus a trailing newline, and `new_string` adding the closing brace + newline + the Phase 70 block below. DO NOT delete or modify any byte of lines 1-111.

    Block to append (copy verbatim, including blank lines and comment text):

    ```
    
    /* Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01) ─────────────────── *
     * D-01/D-03: solid `--color-green` background marks the '+'-prefix exit
     *   button as the «finish/выход» control, distinct from body-branch
     *   buttons which keep the Phase 44 `--interactive-accent` blue.
     * D-04: `--text-on-accent` reused for text colour (no `--text-on-color-green`
     *   token exists in Obsidian; pragmatic re-use, contrast covered by UAT).
     * D-05: hover = `filter: brightness(0.92)` — no `--color-green-hover` token.
     * D-06/D-07: focus-visible relies on Obsidian default ring; no `:disabled`
     *   rule (loop picker always renders enabled buttons).
     * D-08: selector `.rp-loop-exit-btn` (unprefixed) covers sidebar / tab /
     *   inline runner modes — DOM is identical across the three. Phase 60
     *   inline padding override (`inline-runner.css:218-224`) is orthogonal
     *   and remains untouched. */
    .rp-loop-exit-btn {
      background: var(--color-green);
      color: var(--text-on-accent);
    }
    
    .rp-loop-exit-btn:hover {
      filter: brightness(0.92);
    }
    ```

    Notes for the executor:
    - The leading blank line is intentional — it separates the new section from the Phase 47 typography section above.
    - The header comment uses the `─` U+2500 box-drawing character matching the Phase 6 / 44 / 47 section dividers already in the file (Phase 6 header at line 1, Phase 44 header at line 47, Phase 47 header at line 92).
    - The trailing blank line between the base rule and the `:hover` rule mirrors the Phase 44 body-btn pattern (loop-support.css:76 → 77 → 78).
    - DO NOT add `:focus-visible` rules. DO NOT add `:disabled` rules. DO NOT add `transition`/`animation`. DO NOT touch the Phase 44 `.rp-loop-exit-btn` rule at lines 82-90 — Phase 70's later-source-order rule wins via cascade for `background`/`color`/`filter`.
    - DO NOT modify `esbuild.config.mjs` — `loop-support` is already in CSS_FILES.
    - DO NOT touch `src/styles/inline-runner.css`, `src/styles/runner-view.css`, `src/views/runner-view.ts`, `src/views/inline-runner-modal.ts`, or `src/graph/node-label.ts` — all explicitly out-of-scope per CONTEXT.md `<domain>` "Out of scope" list.
  </action>

  <acceptance_criteria>
    All MUST hold after the edit. Use the exact commands; no subjective judgement.

    1. Phase 70 header present exactly once:
       `grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" src/styles/loop-support.css` → output `1`.

    2. The new selector appears at least twice (base rule + :hover variant); confined to the appended section, no comment-only matches counted: filter out comment lines first.
       `grep -v '^[[:space:]]*\*' src/styles/loop-support.css | grep -v '^[[:space:]]*/\*' | grep -c '\.rp-loop-exit-btn'` → output is at least 6 (original count before edit was 4 — Phase 44 grouped selector line 64, Phase 44 base rule line 82, Phase 44 :hover line 87, Phase 47 grouped typography line 98; after Phase 70 append the count must be 6, delta +2 — Phase 70 base rule + Phase 70 :hover).

    3. Phase 70 properties are present exactly as specified:
       - `grep -c "background: var(--color-green);" src/styles/loop-support.css` → `1`
       - `grep -c "filter: brightness(0.92);" src/styles/loop-support.css` → `1`
       - `grep -c "color: var(--text-on-accent);" src/styles/loop-support.css` → `3` (one in Phase 6 `.rp-loop-again-btn` line 20, one in Phase 44 `.rp-loop-body-btn` line 75, one new in Phase 70 `.rp-loop-exit-btn`).

    4. Phase 44 body-button rule preserved BYTE-FOR-BYTE (D-02). Verify the exact string at the original line position is unchanged:
       `sed -n '73,80p' src/styles/loop-support.css` → output exactly:
       ```
       .rp-loop-body-btn {
         background: var(--interactive-accent);
         color: var(--text-on-accent);
       }
       
       .rp-loop-body-btn:hover {
         background: var(--interactive-accent-hover);
       }
       ```

    5. File line count grew by the expected amount (block = 1 leading blank + 14 comment lines + 4 base-rule lines + 1 blank + 3 :hover-rule lines = 23 lines). Original = 111, new MUST be 134:
       `wc -l < src/styles/loop-support.css` → output `134` (allow ±2 if executor formatting normalises blank lines / trailing newline; if it differs by more than 2, the block was modified — re-do verbatim).

    6. NO new `:focus-visible` or `:disabled` rules introduced (D-06/D-07):
       - `grep -c ':focus-visible' src/styles/loop-support.css` → `0`
       - `grep -c ':disabled' src/styles/loop-support.css` → `0`

    7. Out-of-scope adherence — verified via TWO complementary git diff scopes (split from the original AC#7 to tolerate the pre-existing carry-over modification of `src/styles.css` from a prior phase, which is OUTSIDE Phase 70's CONTEXT.md `<domain>` scope and MUST NOT be cleaned up by Phase 70's executor):

       **AC#7a — Phase 70 DID edit its target file:**
       `git diff --name-only -- src/styles/loop-support.css` → output MUST be EXACTLY one line:
       ```
       src/styles/loop-support.css
       ```
       (proves Phase 70 actually edited the target file in scope.)

       **AC#7b — Phase 70 did NOT edit any out-of-scope file under `src/`:**
       `git diff --name-only -- src/views/ src/graph/ src/runner/ src/__tests__/ src/styles/inline-runner.css src/styles/runner-view.css` → output MUST be empty (zero lines).

       (Proves Phase 70 did NOT touch any of the explicit "Out of scope" entries from CONTEXT.md `<domain>`: `src/views/runner-view.ts`, `src/views/inline-runner-modal.ts`, `src/graph/node-label.ts`, `src/runner/protocol-runner.ts`, `src/styles/inline-runner.css`, `src/styles/runner-view.css`, and any test under `src/__tests__/`. The pre-existing `M src/styles.css` carry-over from a prior phase is deliberately NOT covered by either assertion — it is outside Phase 70's scope and Phase 70 neither owns nor cleans it up. Note: the build artefact `src/styles.css` will be re-written by Task 2's `npm run build`; that is expected and orthogonal to Task 1's source-edit scope check.)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" src/styles/loop-support.css | grep -qx 1 &amp;&amp; grep -c "background: var(--color-green);" src/styles/loop-support.css | grep -qx 1 &amp;&amp; grep -c "filter: brightness(0.92);" src/styles/loop-support.css | grep -qx 1 &amp;&amp; [ "$(grep -c 'color: var(--text-on-accent);' src/styles/loop-support.css)" = "3" ] &amp;&amp; grep -c ":focus-visible" src/styles/loop-support.css | grep -qx 0 &amp;&amp; grep -c ":disabled" src/styles/loop-support.css | grep -qx 0 &amp;&amp; sed -n '73,80p' src/styles/loop-support.css | grep -qx ".rp-loop-body-btn {" &amp;&amp; [ "$(git diff --name-only -- src/styles/loop-support.css)" = "src/styles/loop-support.css" ] &amp;&amp; [ -z "$(git diff --name-only -- src/views/ src/graph/ src/runner/ src/__tests__/ src/styles/inline-runner.css src/styles/runner-view.css)" ] &amp;&amp; echo OK</automated>
  </verify>

  <done>
    `src/styles/loop-support.css` now contains exactly one new section at the bottom: a `/* Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01) */` comment followed by `.rp-loop-exit-btn { background: var(--color-green); color: var(--text-on-accent); }` and `.rp-loop-exit-btn:hover { filter: brightness(0.92); }`. Phase 6 (lines 1-45), Phase 44 (lines 47-90), and Phase 47 (lines 92-111) sections are byte-for-byte unchanged. No other source file in the repo has been modified (the pre-existing carry-over `M src/styles.css` from a prior phase is deliberately not touched by Phase 70).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate root styles.css via npm run build</name>
  <files>styles.css</files>

  <read_first>
    - `CLAUDE.md` "After any CSS change" — explicit instruction: run `npm run build` to regenerate `styles.css`; never edit `styles.css` directly.
    - `esbuild.config.mjs` lines 31-65 — confirm the `cssPlugin.setup(build).onEnd` callback concatenates `CSS_FILES` (which already includes `'loop-support'` at index 5) into root `styles.css` AND `src/styles.css`. Both writes happen per build; both should reflect the Phase 70 append.
    - `package.json` "scripts.build" → `tsc -noEmit -skipLibCheck && node esbuild.config.mjs production`. Production build runs typecheck first; since Phase 70 doesn't touch any `.ts`, typecheck should pass on a clean tree.
  </read_first>

  <action>
    From the repository root (`Z:\projects\RadiProtocolObsidian`), run the production build command exactly:

    ```
    npm run build
    ```

    This invokes `tsc -noEmit -skipLibCheck && node esbuild.config.mjs production` per `package.json`. The esbuild `cssPlugin.onEnd` callback concatenates `src/styles/{runner-view, canvas-selector, editor-panel, snippet-manager, snippet-fill-modal, loop-support, snippet-tree-picker, inline-runner}.css` (in that order) and writes the result to BOTH `styles.css` (root, the artefact Obsidian loads) and `src/styles.css` (convenience copy).

    Do NOT edit `styles.css` by hand. Do NOT edit `src/styles.css` by hand. If the build fails on typecheck, the failure is unrelated to Phase 70 (no `.ts` was modified) and indicates pre-existing tech debt — STOP and report the typecheck error to the user; do not attempt to "fix" unrelated TS code under the cover of Phase 70.

    Expected exit code: 0. Expected file mtimes updated: `main.js`, `styles.css`, `src/styles.css`. The Phase 70 section appended in Task 1 must now be present in `styles.css` because esbuild reads `src/styles/loop-support.css` from disk on each build.
  </action>

  <acceptance_criteria>
    1. Build exit code 0:
       `npm run build` → exits with status `0`.

    2. Root `styles.css` contains the Phase 70 header exactly once:
       `grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" styles.css` → output `1`.

    3. Root `styles.css` contains the Phase 70 background and hover rules:
       - `grep -c "background: var(--color-green);" styles.css` → `1`
       - `grep -c "filter: brightness(0.92);" styles.css` → `1`

    4. Convenience copy `src/styles.css` matches root `styles.css` (D-10 atomic regeneration via cssPlugin):
       `diff -q styles.css src/styles.css` → no output (files identical), exit code `0`.

    5. Existing Phase 6 / 44 / 47 rules still present in `styles.css` (sanity check that build didn't drop earlier sections):
       - `grep -c "Phase 6: Loop Support" styles.css` → `1`
       - `grep -c "Phase 44: Unified loop picker (RUN-01)" styles.css` → `1`
       - `grep -c "Phase 47: RUNFIX-03" styles.css` → `1`

    6. NO new files created beyond the build artefacts:
       `git status --short -- styles.css src/styles.css src/styles/loop-support.css` → output is EXACTLY 3 modified lines (`M` or `MM`):
       ```
        M src/styles/loop-support.css
        M styles.css
        M src/styles.css
       ```
       (`main.js` may also be modified — that's expected from the production build and is committed alongside per project convention. Do NOT untrack or revert `main.js` updates from this build.)
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" styles.css | grep -qx 1 &amp;&amp; grep -c "background: var(--color-green);" styles.css | grep -qx 1 &amp;&amp; grep -c "filter: brightness(0.92);" styles.css | grep -qx 1 &amp;&amp; diff -q styles.css src/styles.css &amp;&amp; grep -c "Phase 44: Unified loop picker" styles.css | grep -qx 1 &amp;&amp; echo OK</automated>
  </verify>

  <done>
    `npm run build` exited 0. `styles.css` (root) and `src/styles.css` are byte-identical and both contain the Phase 70 section concatenated in CSS_FILES order (loop-support is index 5 of 8). Phase 6 / 44 / 47 sections remain present. `main.js` may have been regenerated by esbuild; that's expected.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify existing class-presence regression tests stay green (D-12, no new tests)</name>
  <files></files>

  <read_first>
    - `.planning/phases/70-loop-exit-picker-visual-hint/70-CONTEXT.md` D-12 — locked: existing class-presence regression tests MUST stay green; NO new vitest tests added (CSS rules don't load in JSDOM, so computed-style assertions are not feasible).
    - `src/__tests__/runner/protocol-runner-loop-picker.test.ts` (whole file) — Phase 44 RUN-01..RUN-05 state-machine tests. No DOM class assertions, but exercises the same loop runner code path. Must stay green.
    - `src/__tests__/views/runner-snippet-loop-body-file-bound.test.ts` lines 263-313 — sidebar `RunnerView` class-presence assertions on `.rp-loop-body-btn` (the body-button class). Phase 70 does NOT touch the body-button styling, so the count assertions (`bodyBtns.length === 1` and caption text checks at lines 267-269 and 311-313) MUST stay green.
    - `src/__tests__/views/inline-runner-modal-loop-body-file-bound.test.ts` lines 458-507 — inline runner equivalent of the above. Same invariants.
    - `package.json` "scripts.test" → `vitest run`.
  </read_first>

  <action>
    From the repository root, run the full test suite once:

    ```
    npm test
    ```

    `npm test` resolves to `vitest run` per `package.json` (line 11). This executes the entire vitest suite under `src/__tests__/**/*.test.ts`. Phase 70 must not introduce any new test files (D-12) and must not modify any existing test file. Verify the suite exits 0 and that the four loop-related test files specifically pass.

    DO NOT add vitest tests asserting computed CSS values — JSDOM does not load `<link rel="stylesheet">` or `<style>` tags applied via the production build, so `getComputedStyle().backgroundColor` would always return the JSDOM default (typically empty string or `rgba(0,0,0,0)`). Phase 70 D-11 mandates manual UAT (Task 4) for the visual outcome instead.

    DO NOT modify any test file. DO NOT modify `src/__tests__/fixtures/`. If a test fails, the failure is unrelated to Phase 70 (CSS-only change cannot affect TypeScript test outcomes) and indicates pre-existing flake or unrelated breakage — STOP and report the failure with the failing test name and stderr to the user; do not attempt to "fix" tests under Phase 70.
  </action>

  <acceptance_criteria>
    1. Test suite exit code 0:
       `npm test` → exits `0`. Vitest summary line reports "Test Files: N passed (N)" and "Tests: M passed (M)" with no failures and no skipped suites.

    2. Specifically, the four loop-related test files all pass (zero failures within them):
       - `src/__tests__/runner/protocol-runner-loop-picker.test.ts` — all RUN-01..RUN-05 + B/I/W variants pass.
       - `src/__tests__/runner/protocol-runner-loop-body-file-bound-snippet.test.ts` — all tests pass.
       - `src/__tests__/views/runner-snippet-loop-body-file-bound.test.ts` — all D-15 class-presence assertions pass (`bodyBtns.length === 1` at lines 268, 312; caption assertions at 269, 313).
       - `src/__tests__/views/inline-runner-modal-loop-body-file-bound.test.ts` — all D-15 class-presence assertions pass (lines 461-505).

    3. NO new test files were added (D-12):
       `git status --short -- src/__tests__/` → output is empty (nothing modified, nothing added under `src/__tests__/`).

    4. NO existing test files were modified (D-12):
       `git diff --name-only -- 'src/__tests__/**/*.test.ts'` → output is empty.
  </acceptance_criteria>

  <verify>
    <automated>npm test &amp;&amp; test -z "$(git status --short -- src/__tests__/)" &amp;&amp; test -z "$(git diff --name-only -- 'src/__tests__/**/*.test.ts')" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    `npm test` exits 0. The four loop-related test files (`protocol-runner-loop-picker.test.ts`, `protocol-runner-loop-body-file-bound-snippet.test.ts`, `runner-snippet-loop-body-file-bound.test.ts`, `inline-runner-modal-loop-body-file-bound.test.ts`) all pass with zero failures. No file under `src/__tests__/` was modified or added — D-12 honoured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — 7 D-11 checkpoints across sidebar / tab / inline runner modes</name>
  <files></files>

  <read_first>
    - `.planning/phases/70-loop-exit-picker-visual-hint/70-CONTEXT.md` `<decisions>` D-11 — the seven UAT checkpoints. Re-read in full so the human verifier knows exactly what each step proves.
    - `.planning/phases/70-loop-exit-picker-visual-hint/70-CONTEXT.md` `<deferred>` first bullet — the border-left stripe fallback. If checkpoint 1 fails on a contrast issue with `--color-green` + `--text-on-accent` in a default Obsidian theme (light or dark), the rollback is documented there: edit Task 1's appended block to use `border-left: 3px solid var(--color-green); background: var(--background-modifier-border); color: var(--text-normal);` and re-run Tasks 2 + 4 (Task 3 stays green either way).
    - `src/views/runner-view.ts:629-657` and `src/views/inline-runner-modal.ts:485-501` — confirm understanding of how the loop picker renders so you can recognise it during UAT.
    - Any canvas in the dev vault that contains a `loop` node with at least one `+`-prefixed exit edge AND at least one body-branch edge. If `unified-loop-valid.canvas` (test fixture at `src/__tests__/fixtures/unified-loop-valid.canvas`) is not loaded in the dev vault, copy it there or hand-author a quick canvas with a Question → Loop → (body Question + `+exit` Text) shape.
  </read_first>

  <what-built>
    A CSS visual hint (Tasks 1 + 2) that paints the `+`-prefix loop-exit picker button with a solid `--color-green` background, `--text-on-accent` text, and a subtle `filter: brightness(0.92)` darkening on hover. The body-branch buttons next to it remain on `--interactive-accent` (Phase 44 styling), and the loop iteration label above remains `--color-yellow` (Phase 6 styling) — yielding a 3-colour visual rhythm: yellow iteration label → blue body branch button → green exit button. The hint applies in all three runner modes (sidebar Runner View, tab Runner View, Inline Runner) via the unprefixed `.rp-loop-exit-btn` selector.

    Build artefact `styles.css` regenerated and (per CLAUDE.md project convention) auto-copied into the dev vault by `esbuild.config.mjs`'s `devVaultCopyPlugin` if `OBSIDIAN_DEV_VAULT_PATH` is set in `.env`. Existing test surface (Task 3) confirmed the DOM contract is intact.
  </what-built>

  <how-to-verify>
    Open Obsidian on the dev vault. If the plugin's CSS hasn't reloaded automatically after the build, disable+re-enable the RadiProtocol plugin (Settings → Community Plugins toggle) to force a stylesheet refresh. Then walk through the seven checkpoints below. For each, record PASS/FAIL with a one-line note.

    **Setup:**
    Pick or create a canvas in the dev vault with a `loop` node that has at least:
      - one `+`-prefixed exit edge (e.g. label `+ выход`)
      - one body-branch edge (e.g. label `проверка` pointing to a Question node)
    The fixture canvas `src/__tests__/fixtures/unified-loop-valid.canvas` matches this shape; copy it into the dev vault as a working canvas if not already present.

    **Checkpoint 1 — Sidebar Runner View, picker colours:**
      1. Open the canvas in Obsidian Canvas view.
      2. Open the RadiProtocol Runner in the sidebar (right pane), select the canvas.
      3. Click "Run from start" (or equivalent start button) and answer questions until the loop picker is rendered.
      4. EXPECTED: the picker shows two buttons. The exit button (caption text without the `+`, per `stripExitPrefix`) has a solid GREEN background; the body button has a BLUE background. The text on the green button is readable in both light and dark Obsidian themes.
      RECORD: PASS / FAIL — if FAIL with a contrast/readability issue, see the deferred fallback in `read_first`.

    **Checkpoint 2 — Tab Runner View, picker colours:**
      1. With the same canvas open, open the Runner in a main editor tab (RadiProtocol command "Open Runner in tab" or equivalent).
      2. Run the protocol to the same loop picker state.
      3. EXPECTED: identical green-vs-blue treatment as in Checkpoint 1. The hint must not be sidebar-specific.
      RECORD: PASS / FAIL.

    **Checkpoint 3 — Inline Runner, picker colours:**
      1. Open a markdown note that will receive the protocol output.
      2. Trigger the Inline Runner (command palette → "RadiProtocol: Run protocol inline" or whichever command is configured, then pick the same canvas).
      3. Run to the loop picker state inside the inline modal.
      4. EXPECTED: identical green-vs-blue treatment as Checkpoints 1 & 2. The Phase 60 padding override (which compresses the buttons inside the modal) is still in effect, but the colours are the same as in the wider sidebar/tab modes.
      RECORD: PASS / FAIL.

    **Checkpoint 4 — Hover dim on exit button (any one mode is sufficient):**
      1. With the picker visible, hover the cursor over the green exit button (do not click).
      2. EXPECTED: the green darkens by roughly 8% (the `filter: brightness(0.92)` effect). The change is subtle but visible. Mouse-out restores the original brightness.
      RECORD: PASS / FAIL.

    **Checkpoint 5 — Focus ring intact on exit button (any one mode is sufficient):**
      1. With the picker visible, click somewhere neutral first to clear focus, then press Tab repeatedly until the green exit button receives keyboard focus.
      2. EXPECTED: Obsidian's default focus ring (typically a thin outline around the button — exact appearance depends on the active theme) is visible on the exit button. No custom outline introduced by Phase 70 — D-06 invariant.
      RECORD: PASS / FAIL.

    **Checkpoint 6 — No body-button regression (any one mode is sufficient):**
      1. Hover the body-branch button (the BLUE one) in the picker.
      2. EXPECTED: hover state matches the Phase 44 behaviour — background shifts to `--interactive-accent-hover`, no opacity/filter change. Identical to pre-Phase-70 behaviour.
      RECORD: PASS / FAIL.

    **Checkpoint 7 — 3-colour visual rhythm in the picker:**
      1. With the picker rendered, look at the whole picker block: the iteration label at the top (e.g. "Итерация 2") is YELLOW, the body button is BLUE, the exit button is GREEN.
      2. EXPECTED: three distinct colours, no collisions, the green exit reads as "the finish/выход button" at a glance without needing to read the label text.
      RECORD: PASS / FAIL.

    **After all 7 checkpoints:**
    Append (or include in your reply) a short UAT log in this format:
    ```
    Phase 70 UAT — 2026-04-29
    Checkpoint 1 (sidebar colours): PASS / FAIL — note
    Checkpoint 2 (tab colours):     PASS / FAIL — note
    Checkpoint 3 (inline colours):  PASS / FAIL — note
    Checkpoint 4 (hover dim):       PASS / FAIL — note
    Checkpoint 5 (focus ring):      PASS / FAIL — note
    Checkpoint 6 (body no regress): PASS / FAIL — note
    Checkpoint 7 (3-colour rhythm): PASS / FAIL — note
    Overall: APPROVED / NEEDS FALLBACK / NEEDS REWORK
    ```

    If Checkpoint 1, 2, or 3 reports a contrast/readability FAIL on the green button text, apply the deferred border-left stripe fallback (see `read_first` last bullet) and re-run Tasks 2 (`npm run build`) + Task 4 from Checkpoint 1. Tasks 1 and 3 do not need to re-run beyond editing the appended block in Task 1.
  </how-to-verify>

  <acceptance_criteria>
    All 7 checkpoints record PASS, OR Checkpoints 1-3 fail on contrast and the executor applies the documented border-left stripe fallback, re-runs Task 2 + Task 4, and on the second pass all 7 checkpoints record PASS. The user's resume-signal explicitly approves with one of the wording options below.
  </acceptance_criteria>

  <resume-signal>
    Reply with one of:
      - `approved` (or `одобрено`) — all 7 checkpoints PASS, no rework needed.
      - `fallback applied — approved` — used the border-left stripe fallback, re-built, all 7 PASS on second pass.
      - `needs rework: <description>` — describe what failed (which checkpoint, what was observed) and Claude will diagnose and propose a fix beyond the documented fallback.
  </resume-signal>
</task>

</tasks>

<threat_model>
Phase 70 is a CSS-only append to a single feature stylesheet that adds two static rules (background + filter on a class hook already present in the DOM). It introduces no new code paths, no new inputs, no new network/storage operations. Per the GSD planner instructions, the `<threat_model>` section is included for completeness when `security_enforcement` is enabled.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by Phase 70) | The phase adds CSS rules that style buttons rendered from already-trusted graph data (`isExitEdge` / `stripExitPrefix` from `src/graph/node-label.ts`). No new trust boundary is crossed; no untrusted input is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | `.rp-loop-exit-btn` text contrast in custom Obsidian themes | accept | `--text-on-accent` is paired with `--interactive-accent` in vanilla Obsidian, not with `--color-green`. In default light/dark themes contrast is acceptable (verified by D-11 UAT Checkpoint 1-3). In a heavily customised theme, contrast may degrade — accepted because: (a) the deferred border-left stripe fallback is documented and rollback-ready (Task 4 read_first + deferred_items), (b) the failure mode is "button text harder to read" (information disclosure of button identity is unaffected — the button is still visibly green and clickable), (c) custom-theme contrast guarantees are out-of-scope per REQUIREMENTS.md "Visual-customisation tokens for the loop-exit highlight" (deferred). |
| T-70-02 | Tampering | CSS file integrity (`loop-support.css` lines 1-111) | mitigate | CLAUDE.md "append-only per phase" rule is enforced procedurally by Task 1 acceptance criterion 4 (`sed -n '73,80p'` exact-match check on the Phase 44 body-button rule) and acceptance criterion 5 (line-count growth = +23 within ±2 tolerance). A planner agent silently rewriting earlier sections would fail criterion 4. |
| T-70-03 | Denial of Service | esbuild `cssPlugin` concat failure | accept | If the appended CSS contains syntax errors (unmatched braces, invalid token), esbuild logs to stderr and `result.errors.length > 0` short-circuits the concat (esbuild.config.mjs line 51). The build would still succeed for `main.js` but `styles.css` would not be regenerated — Task 2 acceptance criterion 1 (`npm run build` exit 0 AND grep finds Phase 70 header in styles.css) catches this. Accepted because the appended block is fixed verbatim text under strong specification (Task 1 action), not generated by user input. |

</threat_model>

<verification>
**Plan-level verification (after all 4 tasks complete):**

1. **Source artefact is correct:**
   - `grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" src/styles/loop-support.css` → `1`
   - `wc -l < src/styles/loop-support.css` → `134` (±2)
   - Phase 44 body-btn rule (lines 73-80) byte-for-byte unchanged.

2. **Build artefact is correct:**
   - `diff -q styles.css src/styles.css` → no output.
   - `grep -c "Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01)" styles.css` → `1`.
   - `grep -c "background: var(--color-green);" styles.css` → `1`.
   - `grep -c "filter: brightness(0.92);" styles.css` → `1`.

3. **Existing test surface stays green (D-12):**
   - `npm test` exits 0.
   - No file under `src/__tests__/` modified or added.

4. **Out-of-scope files untouched (split scope check, tolerates pre-existing carry-over `M src/styles.css` from a prior phase):**
   - `git diff --name-only -- src/styles/loop-support.css` lists EXACTLY: `src/styles/loop-support.css` (proves Phase 70 edited its target).
   - `git diff --name-only -- src/views/ src/graph/ src/runner/ src/__tests__/ src/styles/inline-runner.css src/styles/runner-view.css` is empty (proves Phase 70 did NOT touch any explicit CONTEXT.md `<domain>` "Out of scope" entry).
   - The pre-existing `M src/styles.css` carry-over from a prior phase is deliberately NOT covered by either assertion — it is outside Phase 70's scope and Phase 70 neither owns nor cleans it up. After Task 2's `npm run build` regenerates `src/styles.css` (build artefact, expected), it will continue to show as modified — that is correct per D-10.
   - `git diff --name-only` (whole tree) lists at most: `src/styles/loop-support.css`, `styles.css`, `src/styles.css`, `main.js`.

5. **All 4 ROADMAP Success Criteria covered:**
   - SC#1 (visible difference) — UAT Checkpoints 1-3.
   - SC#2 (shape/label/position invariant) — Task 1 action body explicitly avoids ::before/icon/border-radius/padding changes; UAT Checkpoint 7 confirms label is `stripExitPrefix(label)` (no `+`).
   - SC#3 (no body regression) — Task 1 acceptance criterion 4 + UAT Checkpoint 6.
   - SC#4 (3 modes coverage) — UAT Checkpoints 1, 2, 3 (sidebar / tab / inline).

6. **All 12 locked decisions honoured:**
   - D-01 → Task 1 background token.
   - D-02 → Task 1 acceptance criterion 4.
   - D-03 → Task 1 action (no border/icon/::before).
   - D-04 → Task 1 action (color: var(--text-on-accent)).
   - D-05 → Task 1 action + acceptance criterion 3.
   - D-06 → Task 1 acceptance criterion 6 (`grep :focus-visible == 0`).
   - D-07 → Task 1 acceptance criterion 6 (`grep :disabled == 0`).
   - D-08 → Task 1 files_modified list (one file); selector unprefixed.
   - D-09 → Task 1 action header text + acceptance criterion 1.
   - D-10 → Task 2 (npm run build + atomic commit guidance).
   - D-11 → Task 4 (7 checkpoints, 3 modes).
   - D-12 → Task 3 (no new tests, existing tests green).

**Deferred fallback note:** if UAT Task 4 Checkpoint 1, 2, or 3 reports a contrast/readability failure on the green button text in a default Obsidian theme, the executor applies the documented border-left stripe variant in-place (see `deferred_items` and Task 4 `read_first`):
```
.rp-loop-exit-btn {
  border-left: 3px solid var(--color-green);
  background: var(--background-modifier-border);
  color: var(--text-normal);
}
```
and re-runs Task 2 + Task 4. This is an ad-hoc rollback inside Phase 70, NOT a separate plan.
</verification>

<success_criteria>
Phase 70 ships when all of the following are true:

1. `src/styles/loop-support.css` line count = 134 (±2), header `/* Phase 70: Loop-exit picker visual hint (LOOP-EXIT-01) */` present exactly once at the bottom of the file, base rule `.rp-loop-exit-btn { background: var(--color-green); color: var(--text-on-accent); }` and `:hover { filter: brightness(0.92); }` both present and parseable. Phase 6 / 44 / 47 sections byte-for-byte unchanged. (Task 1)

2. `npm run build` exits 0; root `styles.css` and `src/styles.css` are byte-identical and both include the Phase 70 section. (Task 2)

3. `npm test` exits 0; the four loop-related test files (`protocol-runner-loop-picker.test.ts`, `protocol-runner-loop-body-file-bound-snippet.test.ts`, `runner-snippet-loop-body-file-bound.test.ts`, `inline-runner-modal-loop-body-file-bound.test.ts`) all pass; no file under `src/__tests__/` was modified or added. (Task 3 — D-12)

4. Manual UAT in 3 runner modes records PASS on all 7 D-11 checkpoints (or PASS on second pass after the documented border-left stripe fallback was applied). User's resume-signal is `approved`, `одобрено`, or `fallback applied — approved`. (Task 4 — D-11)

5. Out-of-scope guarantees (split scope check): `src/views/runner-view.ts`, `src/views/inline-runner-modal.ts`, `src/graph/node-label.ts`, `src/runner/protocol-runner.ts`, `src/styles/inline-runner.css`, `src/styles/runner-view.css`, `esbuild.config.mjs` (CSS_FILES array), and `src/styles/loop-support.css` lines 1-111 are all unchanged in `git diff`. No new files created in `src/styles/`, `src/views/`, `src/graph/`, or `src/__tests__/`. The pre-existing carry-over `M src/styles.css` from a prior phase is NOT cleaned up by Phase 70 (outside scope per CONTEXT.md `<domain>`); after Task 2's build it remains modified as the regenerated build artefact, which is expected per D-10.

6. The 4 ROADMAP §Phase 70 Success Criteria (SC#1..SC#4 — visibility, shape/label/position invariant, body no regression, 3-mode coverage) are observably met as recorded by the UAT checkpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/70-loop-exit-picker-visual-hint/70-01-SUMMARY.md` capturing:
  - Task 1 outcome (final loop-support.css line count, the verbatim appended block, grep counts).
  - Task 2 outcome (npm run build exit code, styles.css grep counts, mtimes of `main.js` / `styles.css` / `src/styles.css`).
  - Task 3 outcome (npm test exit code, vitest summary line, list of the four loop-related test files all passing).
  - Task 4 UAT log (7 checkpoints with PASS/FAIL + notes, fallback applied yes/no, user resume-signal text).
  - Final `git diff --name-only` to prove the only modified files are `src/styles/loop-support.css`, `styles.css`, `src/styles.css`, and `main.js`.
  - Confirmation that LOOP-EXIT-01 is satisfied and the 4 ROADMAP Success Criteria all observed PASS.

Then commit BOTH `src/styles/loop-support.css` AND `styles.css` (and `src/styles.css`, and `main.js` if regenerated) atomically per D-10. Suggested commit message:

```
feat(70-01): green accent on `+`-prefix loop-exit picker button (LOOP-EXIT-01)

Append a single Phase 70 section to src/styles/loop-support.css adding
`.rp-loop-exit-btn` background: var(--color-green) + color:
var(--text-on-accent) and a `:hover { filter: brightness(0.92); }`
variant. The unprefixed selector covers all three runner modes (sidebar
RunnerView, tab RunnerView, InlineRunnerModal) — DOM is identical
across the three (Phase 50.1 D-12 / Phase 44 D-18). Phase 44 body-button
rule (lines 73-80) preserved byte-for-byte per CLAUDE.md append-only
rule. Existing class-presence regression tests remain green (D-12).

UAT-verified in sidebar / tab / inline modes (D-11 — 7/7 PASS).

Refs: Phase 50.1 `+`-prefix convention, Phase 44 body-button styling.
```

Then update `.planning/STATE.md` "Current Position" / "stopped_at" / progress to reflect Phase 70 plan-01 complete and Phase 70 ready for milestone tracking, and tick the Phase 70 plan checkbox in `.planning/ROADMAP.md` Phase Details.
</output>
