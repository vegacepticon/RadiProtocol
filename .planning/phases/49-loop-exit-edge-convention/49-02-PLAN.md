---
phase: 49-loop-exit-edge-convention
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/graph/graph-validator.ts
  - src/__tests__/graph-validator.test.ts
autonomous: true
requirements: [EDGE-01]

must_haves:
  truths:
    - "GraphValidator emits D-01 error verbatim when a loop node has zero labeled outgoing edges"
    - "GraphValidator emits D-02 error verbatim (with comma-joined edge ids) when a loop node has ≥2 labeled outgoing edges"
    - "GraphValidator emits D-03 error verbatim when a loop node has zero unlabeled outgoing edges"
    - "GraphValidator no longer contains the literal string 'выход' inside the LOOP-04 check (lines 96-124)"
    - "GraphValidator.nodeLabel() delegates to the shared nodeLabel() from src/graph/node-label.ts (no duplicated switch body)"
  artifacts:
    - path: "src/graph/graph-validator.ts"
      provides: "LOOP-04 check rewired to isLabeledEdge() helper; error copy D-01/D-02/D-03"
      contains: "isLabeledEdge"
    - path: "src/__tests__/graph-validator.test.ts"
      provides: "Updated LOOP-04 assertions for new error text; new stray-body-label test"
      contains: "не имеет выхода"
  key_links:
    - from: "src/graph/graph-validator.ts"
      to: "src/graph/node-label.ts"
      via: "import { isLabeledEdge, nodeLabel }"
      pattern: "from './node-label'"
---

<objective>
Rewrite the LOOP-04 check in `src/graph/graph-validator.ts` (lines 91-124) to use the new `isLabeledEdge()` helper from `src/graph/node-label.ts` and emit the D-01/D-02/D-03 Russian error strings verbatim. Delegate `GraphValidator.nodeLabel()` to the shared module (D-13). Update assertions in `src/__tests__/graph-validator.test.ts` to match the new error copy. Add a new LOOP-04 test case for the stray-label-on-body-edge scenario (D-02).

Purpose: Validator is the authoritative enforcer of the Phase 49 convention (exactly one labeled outgoing edge = exit, all unlabeled = body). Error messages are the only repair guidance for authors — they must be the final locked Russian text.

Output: Validator emits new error copy; tests guard both the old-fixture happy path and the new stray-body-label failure case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-loop-exit-edge-convention/49-CONTEXT.md
@.planning/phases/49-loop-exit-edge-convention/49-PATTERNS.md
@CLAUDE.md
@src/graph/graph-validator.ts
@src/__tests__/graph-validator.test.ts

<interfaces>
<!-- The shared module created in Plan 01 (wave 1, depended-on). Import these three names only. -->

```typescript
// src/graph/node-label.ts (Plan 01)
export function nodeLabel(node: RPNode): string;
export function isLabeledEdge(edge: RPEdge): boolean;     // D-05: label != null && label.trim() !== ''
export const isExitEdge: typeof isLabeledEdge;            // D-07 alias
```
</interfaces>

<locked_error_copy>
<!-- COPY THESE STRINGS VERBATIM. Any deviation (including Russian quote style « » vs " ", a trailing space, or a different {placeholder} token) is a rejection. -->

**D-01 — zero labeled edges:**
```
Loop-узел "{label}" не имеет выхода. Пометьте ровно одно исходящее ребро — его метка станет подписью кнопки выхода.
```

**D-02 — ≥2 labeled edges:**
```
Loop-узел "{label}" имеет несколько помеченных исходящих рёбер: {edgeIds}. Должно быть ровно одно выходное ребро — снимите метки с остальных.
```

**D-03 — zero unlabeled (body) edges:**
```
Loop-узел "{label}" не имеет тела — добавьте исходящее ребро без метки.
```

Where:
- `{label}` = `nodeLabel(node)` via the shared helper (D-04 / D-13). Keep the surrounding double quotes `"..."` (straight ASCII quotes, same style as the existing code at line 105).
- `{edgeIds}` = `exitEdges.map(e => e.id).join(', ')` — same shape as the current line 111.
</locked_error_copy>

<fixture_contracts>
<!-- The test file in this plan REFERENCES fixtures that Plan 04 edits in Wave 2. Plan 05's build-test gate ties both plans together. For this plan, assume the post-Plan-04 fixture shape: -->

- `unified-loop-valid.canvas`: **post-Plan-04** — `e2` has NO label (body), `e3` has `"label": "выход"` (sole exit). Happy path — no LOOP-04 errors.
- `unified-loop-missing-exit.canvas`: unchanged — `n-loop` has one unlabeled outgoing edge (`e2 → n-q1`), zero labeled → triggers D-01.
- `unified-loop-duplicate-exit.canvas`: unchanged — `n-loop` has `e2 label=проверка`, `e3 label=выход`, `e4 label=выход` → three labeled edges (≥2) → triggers D-02 mentioning `e2, e3, e4`.
- `unified-loop-no-body.canvas`: unchanged — `n-loop` has one labeled edge (`e2 label=выход`), zero unlabeled → triggers D-03.
- `unified-loop-stray-body-label.canvas`: **created by Plan 04** — two labeled outgoing edges (`e2 label=проверка` on body-meant, `e3 label=выход` on exit) → triggers D-02 mentioning both `e2` and `e3`.
</fixture_contracts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas file → parser → ProtocolGraph → GraphValidator | Validator is the defensive boundary for author-written canvases. Phase 49 does not introduce a new boundary; it tightens an existing rule (exit-edge uniqueness) and surfaces a clearer error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Tampering / Denial-of-Service | LOOP-04 re-written check | mitigate | Uses `isLabeledEdge` (D-05 trim-based) so whitespace-only labels cannot sneak past the uniqueness rule. Edge-ids in D-02 come from parser-normalised `edge.id` strings — no unescaped interpolation issue (error target is stdout / RunnerView text node, not HTML; see adjacent code). |
| T-49-02-02 | Elevation of Privilege | n/a | accept | Validator is pure-function; returns string[]. No privilege surface. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite LOOP-04 check + delegate nodeLabel() to shared util</name>
  <files>src/graph/graph-validator.ts</files>
  <read_first>
    - src/graph/graph-validator.ts (full file — verify current lines 91-124 match the PATTERNS.md excerpt before editing)
    - src/graph/node-label.ts (the shared module from Plan 01 — verify exports are present)
    - .planning/phases/49-loop-exit-edge-convention/49-CONTEXT.md (D-01/D-02/D-03/D-04/D-05/D-09/D-13)
    - CLAUDE.md ("Never remove existing code you didn't add" — lines 36-89 of graph-validator.ts are NOT in scope)
  </read_first>
  <behavior>
    - For a graph with `unified-loop-valid.canvas` (post-Plan-04 shape: one unlabeled body edge + one labeled exit), `validate(graph)` returns no LOOP-04 error.
    - For `unified-loop-missing-exit.canvas` (zero labeled outgoing edges on loop node), `validate(graph)` includes a string starting `Loop-узел "Lesion loop" не имеет выхода.`.
    - For `unified-loop-duplicate-exit.canvas` (three labeled outgoing edges `e2, e3, e4`), `validate(graph)` includes a string starting `Loop-узел "Lesion loop" имеет несколько помеченных исходящих рёбер: ` and containing each of `e2`, `e3`, `e4`.
    - For `unified-loop-no-body.canvas` (zero unlabeled outgoing edges), `validate(graph)` includes a string starting `Loop-узел "Lesion loop" не имеет тела`.
    - For a graph with a loop node that has BOTH two labeled edges AND zero unlabeled edges, BOTH D-02 and D-03 errors are emitted (the two ifs are independent — same as the existing pattern at lines 103-123).
    - No substring `'выход'` remains inside the LOOP-04 check block (the migration-error at lines 36-52 is out of scope and retains its existing text).
  </behavior>
  <action>
1. **Add import at the top of `src/graph/graph-validator.ts`** (insert on a new line immediately after the existing `import type { ProtocolGraph, RPNode } from './graph-model';` at line 4):

```typescript
import { nodeLabel as sharedNodeLabel, isLabeledEdge } from './node-label';
```

2. **Replace the LOOP-04 block (current lines 91-124) with:**

```typescript
    // Check (LOOP-04): каждый unified loop-узел должен иметь (Phase 49 EDGE-01):
    //  1) ровно одно помеченное исходящее ребро (его метка = подпись кнопки выхода) — D-01/D-02
    //  2) хотя бы одно НЕ-помеченное исходящее ребро (body-ветвь) — D-03/D-09
    // «Помеченное» (labeled) — edge.label !== null/undefined И edge.label.trim() !== ''
    // (D-05, в shared src/graph/node-label.ts::isLabeledEdge). Пробельные метки считаются
    // непомеченными.
    for (const [id, node] of graph.nodes) {
      if (node.kind !== 'loop') continue;
      const outgoing = graph.edges.filter(e => e.fromNodeId === id);
      const exitEdges = outgoing.filter(e => isLabeledEdge(e));
      const bodyEdges = outgoing.filter(e => !isLabeledEdge(e));
      const label = this.nodeLabel(node);
      // D-01 — zero labeled edges
      if (exitEdges.length === 0) {
        errors.push(
          `Loop-узел "${label}" не имеет выхода. ` +
          `Пометьте ровно одно исходящее ребро — его метка станет подписью кнопки выхода.`
        );
      }
      // D-02 — ≥2 labeled edges
      if (exitEdges.length > 1) {
        const dupIds = exitEdges.map(e => e.id).join(', ');
        errors.push(
          `Loop-узел "${label}" имеет несколько помеченных исходящих рёбер: ${dupIds}. ` +
          `Должно быть ровно одно выходное ребро — снимите метки с остальных.`
        );
      }
      // D-03 — zero unlabeled (body) edges
      if (bodyEdges.length === 0) {
        errors.push(
          `Loop-узел "${label}" не имеет тела — добавьте исходящее ребро без метки.`
        );
      }
    }
```

3. **Delegate the private `nodeLabel()` method to the shared util** — replace the current body at lines 238-249:

```typescript
  /**
   * Returns a human-readable label for a node (for use in error messages).
   * Phase 49 D-13: delegates to the shared src/graph/node-label.ts so validator
   * error text and RunnerView loop-picker captions stay in lock-step.
   */
  private nodeLabel(node: RPNode): string {
    return sharedNodeLabel(node);
  }
```

Keep the private wrapper rather than deleting the method and changing every call site — minimal diff churn (D-13 "Claude's discretion"). All existing callers at `graph-validator.ts:42`, `:63`, `:101`, `:205` continue to call `this.nodeLabel(...)`.

4. **Do NOT touch:**
   - The migration-error block at lines 36-52 (D-15: "No migration pass" — even the `метка «выход»` literal inside the migration-error wording is out of scope; leaving it is explicitly safe because it describes the LEGACY v1.7 convention a legacy-canvas author used).
   - The cycle-detection block at lines 172-233.
   - The dead-end question check at lines 79-89.
   - The BFS reachability at lines 54-71 / 145-164.
   - Any comment block not inside the LOOP-04 section. (CLAUDE.md "Never remove existing code you didn't add".)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/graph-validator.test.ts</automated>
    <!-- Note: will only go fully green once Plan 04 ships the fixture edits. Until then, tests referencing unified-loop-valid.canvas may emit extra LOOP-04 errors. Plan 05 is the combined gate. -->
  </verify>
  <acceptance_criteria>
    - `grep -c "from './node-label'" src/graph/graph-validator.ts` → 1 (import added).
    - `grep -c "isLabeledEdge" src/graph/graph-validator.ts` → ≥2 (imported once, used twice in exitEdges/bodyEdges filters).
    - `grep -c "e.label === 'выход'" src/graph/graph-validator.ts` → 0 (literal removed from LOOP-04 block).
    - `grep -c "e.label !== 'выход'" src/graph/graph-validator.ts` → 0 (complementary literal removed).
    - `grep -c "не имеет выхода" src/graph/graph-validator.ts` → 1 (D-01 error present).
    - `grep -c "несколько помеченных исходящих рёбер" src/graph/graph-validator.ts` → 1 (D-02 error present).
    - `grep -c "не имеет тела" src/graph/graph-validator.ts` → 1 (D-03 error present).
    - `grep -c "не имеет ребра «выход»" src/graph/graph-validator.ts` → 0 (old D-08.1 wording gone from LOOP-04 block; migration-error line 48 may retain its own text referencing the legacy convention — verify via context that only the LOOP-04 block was touched).
    - `grep -c "return sharedNodeLabel(node);" src/graph/graph-validator.ts` → 1 (delegation present).
    - Line count of the file is approximately the same ±8 lines — major structural unrelated changes rejected.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>LOOP-04 block uses the shared helper; three new Russian error strings match D-01/D-02/D-03 verbatim; private nodeLabel() delegates to the shared util; no adjacent code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Update graph-validator.test.ts LOOP-04 assertions + add stray-body-label test</name>
  <files>src/__tests__/graph-validator.test.ts</files>
  <read_first>
    - src/__tests__/graph-validator.test.ts (specifically lines 196-309 — the Phase 43 LOOP-04 / MIGRATE-01 / D-09 describe block)
    - .planning/phases/49-loop-exit-edge-convention/49-CONTEXT.md (D-01/D-02/D-03 error text)
    - .planning/phases/49-loop-exit-edge-convention/49-PATTERNS.md (the test-diff recipe at §src/__tests__/graph-validator.test.ts)
  </read_first>
  <behavior>
    - The happy-path test for `unified-loop-valid.canvas` asserts NONE of the three new error substrings appear (positive assertion that the fixture post-Plan-04 passes the rewritten LOOP-04).
    - The missing-exit test asserts the D-01 error substring (`не имеет выхода`) is present.
    - The duplicate-exit test asserts the D-02 error substring (`несколько помеченных исходящих рёбер`) is present AND the error text contains BOTH `e2` AND `e3` AND `e4` (because Plan 04 does NOT modify duplicate-exit.canvas — it still has 3 labeled edges).
    - The no-body test asserts the D-03 error substring (`не имеет тела`) is present.
    - A NEW test case for `unified-loop-stray-body-label.canvas` (created by Plan 04) asserts the D-02 error AND that the error mentions both stray edges' ids.
    - The MIGRATE-01 tests (lines 253-296) and the D-09 cycle test (lines 298-320) remain untouched — even though they contain the literal `«выход»`, that literal belongs to the legacy migration error message at line 48 (out of scope per Task 1).
  </behavior>
  <action>
1. **Replace the happy-path test body at lines 202-214** (inside `describe('GraphValidator — Phase 43: unified loop + migration...')`):

```typescript
  // ── LOOP-04 happy path (Phase 49 EDGE-01) ───────────────────────────────────
  it('unified-loop-valid.canvas passes LOOP-04 checks (no label/body errors under Phase 49 convention)', () => {
    // Fixture: один loop узел с ровно одним помеченным (exit) ребром + одним непомеченным body-ребром.
    // Post-Phase-49: «проверка»-метка на body-ребре снята (см. Plan 04 fixture audit).
    const graph = parseFixture('unified-loop-valid.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors.some(e => e.includes('не имеет выхода'))).toBe(false);
    expect(errors.some(e => e.includes('несколько помеченных исходящих рёбер'))).toBe(false);
    expect(errors.some(e => e.includes('не имеет тела'))).toBe(false);
    // И никаких migration-ошибок (в этом канвасе нет legacy узлов):
    expect(errors.some(e => e.includes('устаревшие узлы loop-start/loop-end'))).toBe(false);
  });
```

2. **Replace the missing-exit test body at lines 216-224:**

```typescript
  // ── LOOP-04 D-01 — missing labeled (exit) edge ──────────────────────────────
  it('unified-loop-missing-exit.canvas flags zero labeled outgoing edges (Phase 49 D-01)', () => {
    const graph = parseFixture('unified-loop-missing-exit.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors.some(e => e.includes('не имеет выхода'))).toBe(true);
    const err = errors.find(e => e.includes('не имеет выхода'));
    expect(err).toBeDefined();
    if (err === undefined) return;
    expect(err).toContain('Пометьте ровно одно исходящее ребро');
    expect(err).toContain('Lesion loop');
  });
```

3. **Replace the duplicate-exit test body at lines 226-240:**

```typescript
  // ── LOOP-04 D-02 — ≥2 labeled edges (with offending edge IDs) ───────────────
  it('unified-loop-duplicate-exit.canvas flags multiple labeled edges with edge IDs (Phase 49 D-02)', () => {
    // Fixture has three labeled edges: e2 (label "проверка"), e3 (label "выход"), e4 (label "выход")
    // — all three are "labeled" under D-05, so D-02 fires listing all three ids.
    const graph = parseFixture('unified-loop-duplicate-exit.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors.some(e => e.includes('несколько помеченных исходящих рёбер'))).toBe(true);
    const dupErr = errors.find(e => e.includes('несколько помеченных исходящих рёбер'));
    expect(dupErr).toBeDefined();
    if (dupErr === undefined) return;
    expect(dupErr).toContain('e2');
    expect(dupErr).toContain('e3');
    expect(dupErr).toContain('e4');
    expect(dupErr).toContain('снимите метки с остальных');
  });
```

4. **Replace the no-body test body at lines 242-250:**

```typescript
  // ── LOOP-04 D-03 — zero body (unlabeled) edges ──────────────────────────────
  it('unified-loop-no-body.canvas flags zero unlabeled outgoing edges (Phase 49 D-03)', () => {
    const graph = parseFixture('unified-loop-no-body.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors.some(e => e.includes('не имеет тела'))).toBe(true);
    const err = errors.find(e => e.includes('не имеет тела'));
    expect(err).toBeDefined();
    if (err === undefined) return;
    expect(err).toContain('добавьте исходящее ребро без метки');
  });
```

5. **Insert a NEW test case immediately after the no-body test (before the MIGRATE-01 block at current line 252):**

```typescript
  // ── LOOP-04 D-02 — stray label on what was meant to be a body edge ──────────
  it('unified-loop-stray-body-label.canvas flags a second labeled edge (Phase 49 D-02 + D-16)', () => {
    // Fixture (Plan 04): n-loop has e2 (label "проверка", meant as body) + e3 (label "выход", legit exit)
    // → two labeled edges → D-02 fires listing both e2 and e3.
    const graph = parseFixture('unified-loop-stray-body-label.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors.some(e => e.includes('несколько помеченных исходящих рёбер'))).toBe(true);
    const dupErr = errors.find(e => e.includes('несколько помеченных исходящих рёбер'));
    expect(dupErr).toBeDefined();
    if (dupErr === undefined) return;
    expect(dupErr).toContain('e2');
    expect(dupErr).toContain('e3');
  });
```

6. **Do NOT touch:**
   - The MIGRATE-01 tests at current lines 253-296 (they assert the legacy migration-error string that still contains `«выход»` — that string is the line-48 migration text, NOT the LOOP-04 text).
   - The D-09 cycle test at lines 298-309 — unchanged.
   - The D-09 negative control at lines 311+ — unchanged.
   - Any existing `describe('valid protocols'...)` / `describe('error detection (PARSE-07, PARSE-08)'...)` blocks above line 196.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/graph-validator.test.ts</automated>
    <!-- Some assertions depend on Plan 04's fixture edits; the full green happens in Plan 05. This task's acceptance is the assertion text + new test presence, not a green run in isolation. -->
  </verify>
  <acceptance_criteria>
    - `grep -c "не имеет выхода" src/__tests__/graph-validator.test.ts` → ≥2 (happy-path negative + D-01 positive).
    - `grep -c "несколько помеченных исходящих рёбер" src/__tests__/graph-validator.test.ts` → ≥4 (happy-path negative + D-02 duplicate + D-02 stray + possibly error-finder rebinding).
    - `grep -c "не имеет тела" src/__tests__/graph-validator.test.ts` → ≥2 (happy-path negative + D-03 positive).
    - `grep -c "unified-loop-stray-body-label.canvas" src/__tests__/graph-validator.test.ts` → 1 (new test references the new fixture).
    - `grep -c "не имеет ребра «выход»" src/__tests__/graph-validator.test.ts` → ≤1 (at most the MIGRATE-01 `D-CL-02 order` assertion at line 295 retains this literal because it asserts absence of that OLD error — prefer updating that assertion to the new wording OR leaving it as a strict "old wording gone" guard; document choice in summary).
    - The five LOOP-04 tests (happy + D-01 + D-02-duplicate + D-02-stray + D-03) all reference `parseFixture` and a `validator.validate(graph)` call.
    - MIGRATE-01 tests (lines 253-296) are still present: `grep -c "legacy loop-body.canvas returns migration-error" src/__tests__/graph-validator.test.ts` → 1.
  </acceptance_criteria>
  <done>Five LOOP-04 tests updated / added; MIGRATE-01 block preserved; assertion text reflects D-01/D-02/D-03 verbatim.</done>
</task>

</tasks>

<verification>
- After Plan 04 ships fixtures, `npx vitest run src/__tests__/graph-validator.test.ts` is green.
- After Task 1, `grep -n "выход" src/graph/graph-validator.ts` shows the literal ONLY on lines 48-49 (migration-error) and in comments near the check — no runtime `edge.label === 'выход'` comparisons.
- `npx tsc --noEmit` green after both tasks.
</verification>

<success_criteria>
Validator speaks the Phase 49 convention: label-state-based, not literal-string-based. Error copy is D-01/D-02/D-03 verbatim. Tests assert each case against a realistic fixture. The shared `nodeLabel()` is the single source of truth (validator delegates; no duplicated switch body).
</success_criteria>

<output>
After completion, create `.planning/phases/49-loop-exit-edge-convention/49-02-SUMMARY.md` with:
- The exact lines rewritten in graph-validator.ts
- Confirmation that the migration-error at lines 48-49 was left unchanged (D-15)
- Confirmation of the `this.nodeLabel()` delegation choice vs. full inline replacement
- Test counts: 5 LOOP-04 tests green (after Plan 04 lands), MIGRATE-01 block still green
</output>
