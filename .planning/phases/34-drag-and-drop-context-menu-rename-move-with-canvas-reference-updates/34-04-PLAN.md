---
phase: 34
plan: 04
type: execute
wave: 4
depends_on: [34-00, 34-01]
files_modified:
  - src/views/snippet-editor-modal.ts
  - src/__tests__/snippet-editor-modal.test.ts
autonomous: true
requirements: [MOVE-04]
must_haves:
  truths:
    - "Modal «Папка» field moves file via moveSnippet (atomic rename, not save+delete)"
    - "Placebo rewriteCanvasRefs call on file-path mapping is REMOVED"
    - "Misleading «Обновлено канвасов: N» Notice is REMOVED from file-only-move branch"
    - "Simple save (no move) and create flows are unchanged"
    - "Existing Phase 33 modal tests still pass (regression guard)"
  artifacts:
    - path: "src/views/snippet-editor-modal.ts"
      provides: "Corrected handleSave move-on-save pipeline using moveSnippet"
      contains: "moveSnippet"
  key_links:
    - from: "handleSave move-on-save branch"
      to: "snippetService.moveSnippet"
      via: "atomic rename replaces save+delete"
      pattern: "moveSnippet\\("
---

<objective>
Привести `SnippetEditorModal.handleSave` move-on-save pipeline в соответствие с новым service API и удалить placebo `rewriteCanvasRefs` + вводящий в заблуждение Notice (D-03 следствие 1, D-10).

Purpose: Фиксит Phase 33 долг. Файл-only move канвас-невидим — call не нужен, Notice врёт. Заодно переводим save+delete на атомарный `moveSnippet` — снижает окно рассогласования (если delete падает после save, остаются два файла).

Output:
- Переписанная move-on-save ветка в handleSave (только content changes, save flow без move не трогаем)
- Обновлённые / добавленные тесты в snippet-editor-modal.test.ts

**Scope guard:** плагин уже реализует MOVE-04 функционально через Phase 33 save+delete пайплайн. Этот план — чистый cleanup + правильный сервис-контракт. НЕ переписывает оставшийся modal flow, НЕ трогает unsaved-changes guard, НЕ трогает «Папка» UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-CONTEXT.md
@src/views/snippet-editor-modal.ts
@src/__tests__/snippet-editor-modal.test.ts
@CLAUDE.md

<interfaces>
From Plan 00:
```typescript
async moveSnippet(oldPath: string, newFolder: string): Promise<string>;
```

Current handleSave move branch (lines 446-475) to be replaced:
```typescript
await this.plugin.snippetService.save(draftToSave);
await this.plugin.snippetService.delete(oldPath);
const syncResult = await rewriteCanvasRefs(this.app, new Map([[oldPath, newPath]]));  // PLACEBO — D-03
new Notice('Сниппет перемещён. Обновлено канвасов: ' + syncResult.updated.length + ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placebo rewriteCanvasRefs with moveSnippet + clean Notice</name>
  <files>src/views/snippet-editor-modal.ts, src/__tests__/snippet-editor-modal.test.ts</files>
  <behavior>
    **handleSave when oldPath !== newPath (move case):**
    - If name/content changed in addition to folder: save the updated draft content FIRST (`snippetService.save(draftAtOldPath)` — writing to oldPath), then `moveSnippet(oldPath, newFolder)` (atomic rename).
    - If ONLY folder changed (name and content identical): skip save, just `moveSnippet`.
    - After move: Notice = «Сниппет перемещён.» — NO «Обновлено канвасов» suffix (file-only move is canvas-invisible per D-03).
    - NO call to rewriteCanvasRefs in this branch.
    - Resolve `{ saved: true, snippet: draftToSave, movedFrom: oldPath }` with draft.path = newPath returned from moveSnippet.
    - If moveSnippet throws (collision), show error Notice in Russian, leave modal open.

    **handleSave when oldPath === newPath OR create mode:** unchanged.

    **Import cleanup:** if `rewriteCanvasRefs` is no longer used anywhere in this file, remove the import. Otherwise leave it.
  </behavior>
  <action>
    1. Read `src/views/snippet-editor-modal.ts` lines 420-480. Confirm the current move-on-save block matches the snippet in 34-CONTEXT.md.
    2. Replace lines 446-475 with the new logic:
       ```typescript
       if (this.options.mode === 'create' || oldPath === null || oldPath === newPath) {
         // Simple save — unchanged
         await this.plugin.snippetService.save(draftToSave);
         this.safeResolve({ saved: true, snippet: draftToSave, movedFrom: null });
         super.close();
         return;
       }

       // Phase 34: atomic move via service API.
       // 1. Save any name/content changes to the OLD path first
       //    (draftAtOldPath keeps existing path so service.save writes in place).
       // 2. Then moveSnippet performs the atomic rename to the new folder.
       // 3. No canvas-ref-sync — SnippetNode.subfolderPath is a folder-only
       //    reference, so file moves are canvas-invisible (D-03 Phase 34).
       const draftAtOldPath: Snippet =
         this.draftKind === 'json'
           ? { ...(this.draft as JsonSnippet), path: oldPath }
           : { ...(this.draft as MdSnippet), path: oldPath };
       await this.plugin.snippetService.save(draftAtOldPath);

       const newFolder = newPath.slice(0, newPath.lastIndexOf('/'));
       const finalPath = await this.plugin.snippetService.moveSnippet(oldPath, newFolder);

       const finalDraft: Snippet =
         this.draftKind === 'json'
           ? { ...(this.draft as JsonSnippet), path: finalPath }
           : { ...(this.draft as MdSnippet), path: finalPath };

       new Notice('Сниппет перемещён.');
       this.safeResolve({ saved: true, snippet: finalDraft, movedFrom: oldPath });
       super.close();
       ```
    3. If `rewriteCanvasRefs` import is now unused, remove that import line. If still used elsewhere in this file, leave it.
    4. Update `src/__tests__/snippet-editor-modal.test.ts` (existing Phase 33 test file):
       - Find the existing test for move-on-save pipeline (D-09). Update mocks: it now expects `moveSnippet(oldPath, newFolder)` to be called, NOT `save + delete + rewriteCanvasRefs`.
       - Add an assertion that `rewriteCanvasRefs` is NOT called from this modal's move branch.
       - Add an assertion that the Notice text is exactly «Сниппет перемещён.» (no «Обновлено канвасов» suffix).
       - Add a regression test: if moveSnippet throws (collision), modal remains open and an error Notice is shown.
       - **DO NOT remove** any other existing test in this file — CLAUDE.md rule.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/snippet-editor-modal.test.ts --reporter=default</automated>
  </verify>
  <done>
    - handleSave move branch uses moveSnippet, not save+delete
    - No rewriteCanvasRefs call remains in snippet-editor-modal.ts move branch
    - Notice no longer references canvas count
    - Existing modal tests still green (plus new Plan 34 assertions)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/snippet-editor-modal.test.ts` green
- `grep "rewriteCanvasRefs" src/views/snippet-editor-modal.ts` returns no call sites (import may or may not remain depending on other usages)
- `grep "Обновлено канвасов" src/views/snippet-editor-modal.ts` returns nothing
</verification>

<success_criteria>
Advances Phase 34 Success Criterion #2 (modal «Папка» field — regression-only; Phase 33 already functionally shipped this, Phase 34 makes it honest: file-only moves no longer claim to rewrite canvases).
</success_criteria>

<output>
Create `.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-04-SUMMARY.md`
</output>
