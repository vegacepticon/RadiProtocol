---
phase: 22-snippet-node-graph-and-runner-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/graph/graph-model.ts
  - src/graph/canvas-parser.ts
  - src/graph/graph-validator.ts
  - src/runner/runner-state.ts
  - src/runner/protocol-runner.ts
  - src/sessions/session-model.ts
  - src/__tests__/canvas-parser.test.ts
  - src/__tests__/runner/protocol-runner.test.ts
  - src/__tests__/runner/protocol-runner-session.test.ts
autonomous: true
requirements:
  - SNIPPET-01

must_haves:
  truths:
    - "После удаления dead code команда npx vitest run проходит 138 тестов (3 pre-existing RED в runner-extensions.test.ts допустимы)"
    - "В кодовой базе нет ни одного упоминания awaiting-snippet-fill, enterFreeText, completeSnippet, snippetId, snippetNodeId за пределами комментариев или git-истории"
    - "RPNodeKind не содержит 'free-text-input'; DEPRECATED_KINDS set содержит 'free-text-input'"
    - "Тесты для DEPRECATED_KINDS silent-skip и для удалённых runner-методов написаны (GREEN — dead code уже удалён)"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "RPNodeKind без 'free-text-input', без FreeTextInputNode"
      contains: "DEPRECATED_KINDS"
    - path: "src/graph/canvas-parser.ts"
      provides: "DEPRECATED_KINDS set + silent-skip guard в parseNode()"
    - path: "src/runner/runner-state.ts"
      provides: "RunnerStatus без 'awaiting-snippet-fill', без AwaitingSnippetFillState"
    - path: "src/sessions/session-model.ts"
      provides: "PersistedSession.runnerStatus: 'at-node' only, без snippetId/snippetNodeId"
    - path: "src/runner/protocol-runner.ts"
      provides: "Без enterFreeText/completeSnippet/snippetId/snippetNodeId/awaiting-snippet-fill"
  key_links:
    - from: "src/graph/canvas-parser.ts DEPRECATED_KINDS"
      to: "parseNode() guard"
      via: "if (DEPRECATED_KINDS.has(kind)) return null;"
      pattern: "DEPRECATED_KINDS\\.has\\(kind\\)"
    - from: "src/sessions/session-model.ts PersistedSession"
      to: "src/runner/protocol-runner.ts restoreFrom()"
      via: "runnerStatus: 'at-node' type match"
      pattern: "runnerStatus.*at-node"
---

<objective>
Восстановить изменения Phase 20, утерянные при merge Phase 21 (D-08).

Purpose: Очистить кодовую базу от dead code (free-text-input, awaiting-snippet-fill, enterFreeText, completeSnippet, snippetId/snippetNodeId) перед добавлением нового node kind в Wave 1. Без этого TypeScript exhaustiveness-checks в Wave 1 будут давать ложные ошибки, а тесты — пересекаться со старыми путями.

Output: Шесть production-файлов без dead code, три test-файла с корректными тестами, зелёный vitest (138 pass, 3 pre-existing RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-CONTEXT.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-RESEARCH.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-VALIDATION.md
</context>

<interfaces>
<!-- Текущее состояние файлов (ВЕРИФИЦИРОВАНО прямым чтением) -->

Из src/graph/graph-model.ts (REGRESSION — подлежат удалению):
```typescript
// УДАЛИТЬ из RPNodeKind:
| 'free-text-input'

// УДАЛИТЬ интерфейс целиком:
export interface FreeTextInputNode extends RPNodeBase {
  kind: 'free-text-input';
  promptLabel: string;
  prefix?: string;
  suffix?: string;
  radiprotocol_separator?: 'newline' | 'space';
}

// УДАЛИТЬ из RPNode union:
| FreeTextInputNode
```

Из src/runner/runner-state.ts (REGRESSION — подлежат удалению):
```typescript
// УДАЛИТЬ из RunnerStatus:
| 'awaiting-snippet-fill'

// УДАЛИТЬ интерфейс целиком:
export interface AwaitingSnippetFillState { ... }

// УДАЛИТЬ из RunnerState union:
| AwaitingSnippetFillState
```

Из src/sessions/session-model.ts (REGRESSION — подлежат удалению):
```typescript
// ИЗМЕНИТЬ:
runnerStatus: 'at-node' | 'awaiting-snippet-fill';
// НА:
runnerStatus: 'at-node';

// УДАЛИТЬ поля:
snippetId: string | null;
snippetNodeId: string | null;
```

Из src/runner/protocol-runner.ts (REGRESSION — подлежат удалению):
- private snippetId и private snippetNodeId поля
- enterFreeText() метод
- completeSnippet() метод
- case 'awaiting-snippet-fill': в getState()
- snippetId/snippetNodeId в getSerializableState() возврате и параметре restoreFrom()

Из src/graph/canvas-parser.ts (REGRESSION):
- 'free-text-input' в validKinds[]
- case 'free-text-input': в parseNode() switch
- import FreeTextInputNode
- radiprotocol_snippetId строка в case 'text-block':
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Удалить dead code из production-файлов (D-08)</name>
  <files>
    src/graph/graph-model.ts
    src/graph/canvas-parser.ts
    src/graph/graph-validator.ts
    src/runner/runner-state.ts
    src/runner/protocol-runner.ts
    src/sessions/session-model.ts
  </files>
  <read_first>
    - src/graph/graph-model.ts — текущее состояние RPNodeKind union и FreeTextInputNode
    - src/graph/canvas-parser.ts — текущий validKinds[], parseNode() switch, импорты
    - src/graph/graph-validator.ts — nodeLabel() switch
    - src/runner/runner-state.ts — RunnerStatus union, AwaitingSnippetFillState, RunnerState union
    - src/runner/protocol-runner.ts — приватные поля, getState(), getSerializableState(), restoreFrom(), enterFreeText, completeSnippet
    - src/sessions/session-model.ts — PersistedSession shape
  </read_first>
  <action>
Применить следующие изменения точно, файл за файлом:

**src/graph/graph-model.ts:**
1. Из `RPNodeKind` union — удалить строку `| 'free-text-input'`
2. Удалить интерфейс `FreeTextInputNode` целиком (4 поля + extends RPNodeBase) — не оставлять даже комментарий (git history — достаточный tombstone)
3. Из `RPNode` union — удалить `| FreeTextInputNode`
4. Из `TextBlockNode` удалить поле `snippetId?: string` (оно относилось к старому Phase 5 механизму)

**src/graph/canvas-parser.ts:**
1. Из import-блока в начале файла — удалить `FreeTextInputNode` из деструктуризации
2. ПЕРЕД объявлением класса `CanvasParser` добавить:
```typescript
/** Node types that existed in older plugin versions but are no longer supported.
 * Nodes with these types are silently excluded from the ProtocolGraph (NTYPE-01, NTYPE-02). */
const DEPRECATED_KINDS = new Set<string>(['free-text-input']);
```
3. В методе `parseNode()`, сразу после блока `if (typeof kind !== 'string')`, добавить:
```typescript
if (DEPRECATED_KINDS.has(kind)) return null; // silently skip deprecated node type (NTYPE-01)
```
4. Из массива `validKinds` — удалить `'free-text-input'`
5. Удалить весь `case 'free-text-input':` блок в switch
6. В `case 'text-block':` удалить строку, читающую `radiprotocol_snippetId` (поле `snippetId` удалено из `TextBlockNode`)

**src/graph/graph-validator.ts:**
1. В методе `nodeLabel()` — удалить `case 'free-text-input': return node.promptLabel || node.id;`

**src/runner/runner-state.ts:**
1. Из `RunnerStatus` union — удалить `| 'awaiting-snippet-fill'`
2. Обновить комментарий "Five runner statuses" → "Four runner statuses"
3. Удалить интерфейс `AwaitingSnippetFillState` целиком (все поля)
4. Из `RunnerState` union — удалить `| AwaitingSnippetFillState`
5. В JSDoc `AtNodeState` — удалить ссылку на `enterFreeText` из описания ("or a free-text-input node (awaiting enterFreeText)")

**src/runner/protocol-runner.ts:**
1. Удалить поля `private snippetId: string | null = null;` и `private snippetNodeId: string | null = null;`
2. Удалить все присваивания этих полей в методах `start()` и `stepBack()`
3. Удалить метод `enterFreeText()` полностью
4. Удалить метод `completeSnippet()` полностью
5. В `getState()`: удалить `case 'awaiting-snippet-fill':` блок (со всем return statement)
6. В `getSerializableState()`: сузить тип return с `'at-node' | 'awaiting-snippet-fill'` до `'at-node'`; изменить guard `if (this.runnerStatus !== 'at-node' && this.runnerStatus !== 'awaiting-snippet-fill')` на `if (this.runnerStatus !== 'at-node')`; удалить поля `snippetId` и `snippetNodeId` из возвращаемого объекта
7. В `restoreFrom()`: сузить тип параметра — удалить `snippetId`/`snippetNodeId` из типа; удалить присваивания `this.snippetId`/`this.snippetNodeId` в теле
8. В `resolveSeparator()`: удалить `FreeTextInputNode` из union-проверки (если присутствует)
9. В JSDoc `syncManualEdit()`: удалить ссылку на `enterFreeText()`

**src/sessions/session-model.ts:**
1. Изменить поле `runnerStatus: 'at-node' | 'awaiting-snippet-fill'` → `runnerStatus: 'at-node'`
2. Обновить JSDoc над этим полем — убрать упоминание awaiting-snippet-fill как валидного resume state
3. Удалить поле `snippetId: string | null;` и его JSDoc
4. Удалить поле `snippetNodeId: string | null;` и его JSDoc
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run src/__tests__/canvas-parser.test.ts src/__tests__/runner/protocol-runner.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "awaiting-snippet-fill" src/ возвращает пустой результат
    - grep -r "enterFreeText\|completeSnippet" src/ возвращает пустой результат
    - grep -r "snippetId\|snippetNodeId" src/ возвращает пустой результат (кроме node-color-map.ts где нет таких строк)
    - grep "free-text-input" src/graph/graph-model.ts возвращает пустой результат
    - grep "DEPRECATED_KINDS" src/graph/canvas-parser.ts содержит "new Set&lt;string&gt;(['free-text-input'])"
    - src/graph/graph-validator.ts не содержит "free-text-input"
    - src/sessions/session-model.ts содержит "runnerStatus: 'at-node'" без "'awaiting-snippet-fill'"
  </acceptance_criteria>
  <done>Все шесть production-файлов не содержат dead code от Phase 5/20; TypeScript exhaustiveness-checks корректны для 6-членного RPNodeKind (без 'free-text-input')</done>
</task>

<task type="auto">
  <name>Task 2: Обновить тесты под очищенный API (D-08)</name>
  <files>
    src/__tests__/canvas-parser.test.ts
    src/__tests__/runner/protocol-runner.test.ts
    src/__tests__/runner/protocol-runner-session.test.ts
  </files>
  <read_first>
    - src/__tests__/canvas-parser.test.ts — текущие тесты (перед добавлением новых)
    - src/__tests__/runner/protocol-runner.test.ts — текущие тесты (поиск enterFreeText/completeSnippet/awaiting-snippet-fill)
    - src/__tests__/runner/protocol-runner-session.test.ts — текущие фикстуры (поиск snippetId/snippetNodeId)
  </read_first>
  <action>
**src/__tests__/canvas-parser.test.ts** — добавить в подходящую describe-группу:
```typescript
describe('DEPRECATED_KINDS silent skip', () => {
  it('silently skips a free-text-input node and drops its edges', () => {
    const json = JSON.stringify({
      nodes: [
        { id: 'n-start', type: 'text', text: 'Start', x: 0, y: 0, width: 200, height: 60, radiprotocol_nodeType: 'start' },
        { id: 'n-fti', type: 'text', text: 'Old', x: 0, y: 120, width: 200, height: 60, radiprotocol_nodeType: 'free-text-input', radiprotocol_promptLabel: 'Enter text' },
      ],
      edges: [
        { id: 'e1', fromNode: 'n-start', toNode: 'n-fti' },
      ],
    });
    const result = parser.parse(json, 'test.canvas');
    expect(result.success).toBe(true);
    if (!result.success) return;
    // free-text-input node silently excluded
    expect(result.graph.nodes.has('n-fti')).toBe(false);
    // only start node present
    expect(result.graph.nodes.size).toBe(1);
    // edge to dropped node also dropped
    expect(result.graph.edges.length).toBe(0);
  });
});
```

**src/__tests__/runner/protocol-runner.test.ts** — найти и удалить все тесты или строки, ссылающиеся на:
- `runner.enterFreeText(`
- `runner.completeSnippet(`
- `'awaiting-snippet-fill'` в ожиданиях
Если таких тестов нет (что вероятно — они были удалены в commit 9861db2), пропустить.

**src/__tests__/runner/protocol-runner-session.test.ts** — найти все fixture-объекты типа `PersistedSession` и удалить из них поля `snippetId` и `snippetNodeId`. Пример замены:
```typescript
// БЫЛО:
{ version: 1, canvasFilePath: '...', ..., snippetId: null, snippetNodeId: null }
// СТАЛО:
{ version: 1, canvasFilePath: '...', ... }
// (без snippetId и snippetNodeId)
```
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run выводит "138 passed" (или больше если были добавлены DEPRECATED_KINDS тесты) и "3 failed" только в runner-extensions.test.ts
    - src/__tests__/canvas-parser.test.ts содержит "DEPRECATED_KINDS silent skip"
    - grep -r "snippetId\|snippetNodeId" src/__tests__/ возвращает пустой результат
    - grep -r "enterFreeText\|completeSnippet\|awaiting-snippet-fill" src/__tests__/ возвращает пустой результат
  </acceptance_criteria>
  <done>Vitest проходит 138+ тестов, 3 pre-existing RED в runner-extensions.test.ts; кодовая база чиста от dead code Phase 5/20 — Wave 0 gate выполнен</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file edits | Все изменения — pure TypeScript в src/; нет I/O, нет внешних вызовов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | TypeScript exhaustiveness | accept | TypeScript compiler enforces exhaustiveness via `default: never`; удаление old cases автоматически ужесточает проверку |
| T-22-02 | Denial | Partial Wave 0 | mitigate | Gate: `npx vitest run` должен пройти перед переходом к Plan 02 |
</threat_model>

<verification>
После завершения обоих задач:
- `npx vitest run` — 138+ pass, только 3 RED в runner-extensions.test.ts
- `grep -r "awaiting-snippet-fill\|enterFreeText\|completeSnippet\|free-text-input" src/` — пусто (кроме DEPRECATED_KINDS)
- `grep "DEPRECATED_KINDS" src/graph/canvas-parser.ts` — присутствует
</verification>

<success_criteria>
Wave 0 gate ОТКРЫТ когда:
1. Vitest: 138+ pass, 3 pre-existing RED (runner-extensions.test.ts)
2. Ни один из 6 production-файлов не содержит: awaiting-snippet-fill, enterFreeText, completeSnippet, snippetId, snippetNodeId, FreeTextInputNode, free-text-input (кроме DEPRECATED_KINDS set)
3. DEPRECATED_KINDS set присутствует в canvas-parser.ts с silent-skip guard
</success_criteria>

<output>
После завершения создать `.planning/phases/22-snippet-node-graph-and-runner-layer/22-01-SUMMARY.md`
</output>
