---
phase: 22-snippet-node-graph-and-runner-layer
plan: 02
type: execute
wave: 1
depends_on:
  - 22-01-PLAN.md
files_modified:
  - src/graph/graph-model.ts
  - src/graph/canvas-parser.ts
  - src/graph/graph-validator.ts
  - src/__tests__/fixtures/snippet-node.canvas
  - src/__tests__/canvas-parser.test.ts
  - src/__tests__/graph-validator.test.ts
autonomous: true
requirements:
  - SNIPPET-01

must_haves:
  truths:
    - "SnippetNode присутствует в RPNodeKind union, SnippetNode interface, RPNode union"
    - "Canvas-парсер читает radiprotocol_snippetFolderPath → folderPath и radiprotocol_buttonLabel → buttonLabel"
    - "Валидатор не выдаёт dead-end ошибку для snippet-узла без исходящих рёбер"
    - "NODE_COLOR_MAP аннотирован как Record<RPNodeKind, string> и TypeScript не жалуется"
    - "Все новые тесты GREEN; vitest 139+ pass, 3 pre-existing RED"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "RPNodeKind с 'snippet', SnippetNode interface, RPNode union обновлён"
      contains: "SnippetNode"
    - path: "src/graph/canvas-parser.ts"
      provides: "'snippet' в validKinds, case 'snippet': в parseNode()"
      contains: "case 'snippet':"
    - path: "src/graph/graph-validator.ts"
      provides: "case 'snippet': в nodeLabel(), без dead-end check для snippet"
      contains: "case 'snippet':"
    - path: "src/__tests__/fixtures/snippet-node.canvas"
      provides: "Fixture с snippet terminal node"
    - path: "src/__tests__/canvas-parser.test.ts"
      provides: "Тесты парсинга snippet node: folderPath, buttonLabel, terminal"
    - path: "src/__tests__/graph-validator.test.ts"
      provides: "Тест: snippet без outgoing edges — не ошибка валидатора"
  key_links:
    - from: "src/graph/graph-model.ts RPNodeKind"
      to: "src/graph/canvas-parser.ts validKinds"
      via: "RPNodeKind[] type annotation"
      pattern: "'snippet'"
    - from: "src/canvas/node-color-map.ts"
      to: "src/graph/graph-model.ts RPNodeKind"
      via: "Record<RPNodeKind, string> annotation"
      pattern: "Record<RPNodeKind, string>"
---

<objective>
Добавить `snippet` node kind в граф: модель, парсер, валидатор; создать тестовый fixture и тесты.

Purpose: Распространить новый тип по чистой (после Wave 0) discriminated union. После этого плана граф знает о snippet-узлах — runner и UI-слой (Plans 03 и Phase 25) могут опираться на это.

Output: SnippetNode interface, обновлённый парсер, обновлённый валидатор (без dead-end для snippet), fixture, тесты GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-CONTEXT.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-RESEARCH.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-01-SUMMARY.md
</context>

<interfaces>
<!-- Целевое состояние после Wave 0 (из Plan 01) -->

src/graph/graph-model.ts — после Wave 0 RPNodeKind:
```typescript
export type RPNodeKind =
  | 'start'
  | 'question'
  | 'answer'
  | 'text-block'
  | 'loop-start'
  | 'loop-end';
  // 'free-text-input' удалён в Plan 01
```

Добавляемый SnippetNode (D-01, D-02 — точная форма):
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  /** Per-node override for the folder from which the file picker opens.
   *  undefined means use the global setting (Phase 24). */
  folderPath?: string;
  /** Label for the file-picker button rendered in Phase 25.
   *  Falls back to canvas node text, then to "Select file". */
  buttonLabel?: string;
}
```

src/graph/canvas-parser.ts — pattern для case 'snippet': (из RESEARCH.md Pattern 6):
```typescript
case 'snippet': {
  const node: SnippetNode = {
    ...base,
    kind: 'snippet',
    folderPath: props['radiprotocol_snippetFolderPath'] !== undefined
      ? getString(props, 'radiprotocol_snippetFolderPath')
      : undefined,
    buttonLabel: props['radiprotocol_buttonLabel'] !== undefined
      ? getString(props, 'radiprotocol_buttonLabel')
      : undefined,
  };
  return node;
}
```

src/canvas/node-color-map.ts — текущая аннотация (подлежит смене):
```typescript
// БЫЛО:
export const NODE_COLOR_MAP: Record<string, string> = { ... }
// СТАЛО (D-03):
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = { ... }
// + добавить import type { RPNodeKind } from '../graph/graph-model';
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить SnippetNode в graph-model, canvas-parser, graph-validator и node-color-map (D-01, D-02, D-03, D-04)</name>
  <files>
    src/graph/graph-model.ts
    src/graph/canvas-parser.ts
    src/graph/graph-validator.ts
    src/canvas/node-color-map.ts
  </files>
  <read_first>
    - src/graph/graph-model.ts — текущий RPNodeKind (после Wave 0: 6 членов без free-text-input)
    - src/graph/canvas-parser.ts — validKinds[], import-блок, parseNode() switch, паттерн case 'text-block': как шаблон
    - src/graph/graph-validator.ts — nodeLabel() switch (все case), Check 5 (dead-end questions логика — убедиться что она специфична для 'question', а не для всех типов)
    - src/canvas/node-color-map.ts — текущая аннотация Record<string, string> и список ключей
  </read_first>
  <behavior>
    - RPNodeKind содержит 'snippet' (8-й член union)
    - SnippetNode interface существует с полями kind: 'snippet', folderPath?: string, buttonLabel?: string
    - RPNode union содержит | SnippetNode
    - canvas-parser.ts: validKinds содержит 'snippet'; parseNode() имеет case 'snippet': читающий radiprotocol_snippetFolderPath → folderPath, radiprotocol_buttonLabel → buttonLabel
    - graph-validator.ts: nodeLabel() имеет case 'snippet': возвращающий node.buttonLabel || node.id
    - graph-validator.ts: Check 5 (dead-end) работает только для 'question' — snippet без outgoing edges НЕ вызывает ошибки
    - node-color-map.ts: аннотация Record<RPNodeKind, string> вместо Record<string, string>; ключ 'snippet' уже присутствует
  </behavior>
  <action>
**src/graph/graph-model.ts:**
1. Добавить `| 'snippet'` в RPNodeKind (после 'loop-end')
2. Добавить интерфейс SnippetNode ТОЧНО в этой форме:
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  /** Per-node override for the folder from which the file picker opens.
   *  undefined means use the global setting (Phase 24). */
  folderPath?: string;
  /** Label for the file-picker button rendered in Phase 25.
   *  Falls back to canvas node text, then to "Select file". */
  buttonLabel?: string;
}
```
3. Добавить `| SnippetNode` в RPNode union

**src/graph/canvas-parser.ts:**
1. Добавить `SnippetNode` в import list из './graph-model'
2. Добавить `'snippet'` в массив `validKinds`
3. Добавить в parseNode() switch перед `default:` новый case:
```typescript
case 'snippet': {
  const node: SnippetNode = {
    ...base,
    kind: 'snippet',
    folderPath: props['radiprotocol_snippetFolderPath'] !== undefined
      ? getString(props, 'radiprotocol_snippetFolderPath')
      : undefined,
    buttonLabel: props['radiprotocol_buttonLabel'] !== undefined
      ? getString(props, 'radiprotocol_buttonLabel')
      : undefined,
  };
  return node;
}
```

**src/graph/graph-validator.ts:**
1. В nodeLabel() switch добавить:
```typescript
case 'snippet': return (node as import('./graph-model').SnippetNode).buttonLabel || node.id;
```
   ВАЖНО: Для этого нужен импорт SnippetNode. Альтернатива — добавить SnippetNode в import type из './graph-model' в начале файла.
2. Убедиться что Check 5 (dead-end questions) НЕ изменён — он проверяет только `node.kind === 'question'`. Snippet dead-end — NOT an error (D-04).

**src/canvas/node-color-map.ts:**
1. Добавить в начало файла: `import type { RPNodeKind } from '../graph/graph-model';`
2. Изменить аннотацию константы: `Record<string, string>` → `Record<RPNodeKind, string>`
3. Ключ 'snippet' уже присутствует в объекте — логику не трогать
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run src/__tests__/canvas-parser.test.ts src/__tests__/graph-validator.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "'snippet'" src/graph/graph-model.ts выводит строку в RPNodeKind union
    - grep "SnippetNode" src/graph/graph-model.ts выводит interface объявление с folderPath и buttonLabel
    - grep "'snippet'" src/graph/canvas-parser.ts выводит строку в validKinds И case 'snippet':
    - grep "case 'snippet'" src/graph/graph-validator.ts выводит строку в nodeLabel()
    - grep "Record<RPNodeKind" src/canvas/node-color-map.ts выводит строку с новой аннотацией
    - npx vitest run src/__tests__/canvas-parser.test.ts — выходит с кодом 0
  </acceptance_criteria>
  <done>SnippetNode существует во всех четырёх файлах; TypeScript exhaustiveness-check в advanceThrough() потребует case 'snippet': (план 03 его добавит)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture и тесты парсера + валидатора для snippet (SNIPPET-01)</name>
  <files>
    src/__tests__/fixtures/snippet-node.canvas
    src/__tests__/canvas-parser.test.ts
    src/__tests__/graph-validator.test.ts
  </files>
  <read_first>
    - src/__tests__/fixtures/ — список существующих fixture-файлов (паттерн именования)
    - src/__tests__/canvas-parser.test.ts — существующие тесты (import-блок, describe-структура)
    - src/__tests__/graph-validator.test.ts — существующие тесты (import-блок, describe-структура)
  </read_first>
  <behavior>
    - Fixture-файл snippet-node.canvas существует с узлами: start → question → answer → snippet (terminal, без outgoing edge); snippet-узел имеет radiprotocol_buttonLabel: "Select template" и radiprotocol_snippetFolderPath: "Templates"
    - Тест: парсер возвращает success:true для канвы со snippet-узлом
    - Тест: node.kind === 'snippet' для распарсенного узла
    - Тест: folderPath === 'Templates' для распарсенного snippet-узла
    - Тест: buttonLabel === 'Select template' для распарсенного snippet-узла
    - Тест: snippet-узел без outgoing edges — валидатор возвращает [] (no errors)
  </behavior>
  <action>
**src/__tests__/fixtures/snippet-node.canvas** — создать файл:
```json
{
  "nodes": [
    { "id": "n-start", "type": "text", "text": "Start", "x": 0, "y": 0, "width": 200, "height": 60, "radiprotocol_nodeType": "start" },
    { "id": "n-q1", "type": "text", "text": "Q1", "x": 0, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "question", "radiprotocol_questionText": "Insert report?" },
    { "id": "n-a1", "type": "text", "text": "Yes", "x": 0, "y": 240, "width": 200, "height": 60, "radiprotocol_nodeType": "answer", "radiprotocol_answerText": "Yes" },
    { "id": "n-snip1", "type": "text", "text": "Select file", "x": 0, "y": 360, "width": 200, "height": 60, "radiprotocol_nodeType": "snippet", "radiprotocol_buttonLabel": "Select template", "radiprotocol_snippetFolderPath": "Templates" }
  ],
  "edges": [
    { "id": "e1", "fromNode": "n-start", "toNode": "n-q1" },
    { "id": "e2", "fromNode": "n-q1", "toNode": "n-a1" },
    { "id": "e3", "fromNode": "n-a1", "toNode": "n-snip1" }
  ]
}
```

**src/__tests__/canvas-parser.test.ts** — добавить describe-группу для snippet:
```typescript
describe('snippet node parsing', () => {
  const snippetJson = readFileSync(
    join(__dirname, 'fixtures/snippet-node.canvas'),
    'utf-8',
  );

  it('parses canvas with snippet node without errors', () => {
    const result = parser.parse(snippetJson, 'snippet-node.canvas');
    expect(result.success).toBe(true);
  });

  it('assigns kind "snippet" to snippet node', () => {
    const result = parser.parse(snippetJson, 'snippet-node.canvas');
    if (!result.success) throw new Error(result.error);
    const node = result.graph.nodes.get('n-snip1');
    expect(node?.kind).toBe('snippet');
  });

  it('maps radiprotocol_snippetFolderPath to folderPath', () => {
    const result = parser.parse(snippetJson, 'snippet-node.canvas');
    if (!result.success) throw new Error(result.error);
    const node = result.graph.nodes.get('n-snip1');
    if (node?.kind !== 'snippet') throw new Error('not a snippet node');
    expect(node.folderPath).toBe('Templates');
  });

  it('maps radiprotocol_buttonLabel to buttonLabel', () => {
    const result = parser.parse(snippetJson, 'snippet-node.canvas');
    if (!result.success) throw new Error(result.error);
    const node = result.graph.nodes.get('n-snip1');
    if (node?.kind !== 'snippet') throw new Error('not a snippet node');
    expect(node.buttonLabel).toBe('Select template');
  });

  it('parses snippet node with no optional fields as undefined', () => {
    const json = JSON.stringify({
      nodes: [
        { id: 'n-start', type: 'text', text: 'S', x: 0, y: 0, width: 200, height: 60, radiprotocol_nodeType: 'start' },
        { id: 'n-snip', type: 'text', text: 'Snip', x: 0, y: 120, width: 200, height: 60, radiprotocol_nodeType: 'snippet' },
      ],
      edges: [{ id: 'e1', fromNode: 'n-start', toNode: 'n-snip' }],
    });
    const result = parser.parse(json, 'test.canvas');
    if (!result.success) throw new Error(result.error);
    const node = result.graph.nodes.get('n-snip');
    if (node?.kind !== 'snippet') throw new Error('not a snippet node');
    expect(node.folderPath).toBeUndefined();
    expect(node.buttonLabel).toBeUndefined();
  });
});
```
Использовать `readFileSync` и `join` — проверить есть ли уже такой импорт в файле; если нет — добавить `import { readFileSync } from 'fs'; import { join } from 'path';` (или `import { dirname } from 'path'; const __dirname = ...` если файл использует ES modules с import.meta.url).

**src/__tests__/graph-validator.test.ts** — добавить describe-группу:
```typescript
describe('snippet node — dead-end is valid (D-04)', () => {
  it('does not error when snippet node has no outgoing edges', () => {
    // Build minimal graph: start → answer → snippet (terminal)
    const graph: ProtocolGraph = {
      canvasFilePath: 'test.canvas',
      startNodeId: 'n-start',
      nodes: new Map([
        ['n-start', { id: 'n-start', kind: 'start', x: 0, y: 0, width: 200, height: 60 }],
        ['n-a1', { id: 'n-a1', kind: 'answer', answerText: 'Yes', x: 0, y: 120, width: 200, height: 60 }],
        ['n-snip', { id: 'n-snip', kind: 'snippet', x: 0, y: 240, width: 200, height: 60 }],
      ]),
      edges: [
        { id: 'e1', fromNodeId: 'n-start', toNodeId: 'n-a1' },
        { id: 'e2', fromNodeId: 'n-a1', toNodeId: 'n-snip' },
      ],
      adjacency: new Map([
        ['n-start', ['n-a1']],
        ['n-a1', ['n-snip']],
      ]),
      reverseAdjacency: new Map([
        ['n-a1', ['n-start']],
        ['n-snip', ['n-a1']],
      ]),
    };
    const errors = validator.validate(graph);
    expect(errors).toHaveLength(0);
  });
});
```
Убедиться что `ProtocolGraph` импортирован в файле тестов (обычно уже есть).
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run src/__tests__/canvas-parser.test.ts src/__tests__/graph-validator.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/__tests__/fixtures/snippet-node.canvas существует и содержит "n-snip1" и "radiprotocol_buttonLabel"
    - npx vitest run src/__tests__/canvas-parser.test.ts — выходит с кодом 0; вывод содержит "snippet node parsing"
    - npx vitest run src/__tests__/graph-validator.test.ts — выходит с кодом 0; вывод содержит "dead-end is valid"
    - npx vitest run — показывает 143+ passed (138 базовых + 5 snippet parser + 1 validator + DEPRECATED_KINDS тесты)
  </acceptance_criteria>
  <done>Граф-слой полностью поддерживает snippet node; fixture существует; все тесты GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canvas JSON → ProtocolGraph | Парсер принимает произвольный JSON из файловой системы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | canvas-parser.ts parseNode | accept | Все поля из canvas JSON читаются через getString() с безопасным fallback; нет eval, нет innerHTML |
| T-22-04 | Information Disclosure | folderPath field | accept | folderPath хранится только в памяти ProtocolGraph; не сериализуется в session-model в Phase 22 |
</threat_model>

<verification>
После завершения плана:
- `npx vitest run` — 143+ pass, 3 pre-existing RED (runner-extensions.test.ts)
- `grep "RPNodeKind" src/canvas/node-color-map.ts` — присутствует Record&lt;RPNodeKind, string&gt;
- `grep "case 'snippet'" src/graph/graph-validator.ts` — присутствует
- `ls src/__tests__/fixtures/snippet-node.canvas` — файл существует
</verification>

<success_criteria>
Plan 02 завершён когда:
1. SnippetNode присутствует в graph-model.ts, canvas-parser.ts, graph-validator.ts
2. node-color-map.ts аннотирован как Record&lt;RPNodeKind, string&gt;
3. fixture snippet-node.canvas создан
4. Тесты парсера (5 шт.) и валидатора (1 шт.) GREEN
5. npx vitest run: 143+ pass, 3 RED в runner-extensions.test.ts
</success_criteria>

<output>
После завершения создать `.planning/phases/22-snippet-node-graph-and-runner-layer/22-02-SUMMARY.md`
</output>
