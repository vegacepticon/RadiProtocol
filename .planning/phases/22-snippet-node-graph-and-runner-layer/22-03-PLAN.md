---
phase: 22-snippet-node-graph-and-runner-layer
plan: 03
type: execute
wave: 1
depends_on:
  - 22-01-PLAN.md
files_modified:
  - src/runner/runner-state.ts
  - src/runner/protocol-runner.ts
  - src/__tests__/runner/protocol-runner.test.ts
autonomous: true
requirements:
  - SNIPPET-01

must_haves:
  truths:
    - "Runner halts at snippet node в 'at-node' state — не auto-advance, не error"
    - "getState() возвращает isAtSnippetNode: true когда currentNodeId — snippet node"
    - "canStepBack === false сразу после halt у snippet (undo-стек пуст — D-07)"
    - "tsc --noEmit чист на project source (exhaustiveness в advanceThrough() закрыта)"
    - "Виtest: все тесты GREEN (включая новые runner-тесты для snippet)"
  artifacts:
    - path: "src/runner/runner-state.ts"
      provides: "AtNodeState с isAtSnippetNode?: boolean"
      contains: "isAtSnippetNode"
    - path: "src/runner/protocol-runner.ts"
      provides: "case 'snippet': в advanceThrough(); isAtSnippetNode в getState()"
      contains: "case 'snippet':"
    - path: "src/__tests__/runner/protocol-runner.test.ts"
      provides: "Тесты halt + isAtSnippetNode + canStepBack"
  key_links:
    - from: "src/runner/protocol-runner.ts advanceThrough() case 'snippet':"
      to: "this.runnerStatus = 'at-node'"
      via: "halt pattern (no undo push)"
      pattern: "case 'snippet':.*at-node"
    - from: "src/runner/protocol-runner.ts getState() at-node branch"
      to: "isAtSnippetNode field"
      via: "this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'snippet'"
      pattern: "isAtSnippetNode"
---

<objective>
Добавить runner halt и AtNodeState signal для snippet node (D-05, D-06, D-07).

Purpose: После этого плана runner знает о snippet-узлах: останавливается на них в 'at-node' state, сигнализирует isAtSnippetNode: true в getState(). Phase 25 использует этот сигнал для рендеринга file-picker button. Undo-инвариант соблюдён (D-07).

Output: Обновлённые runner-state.ts и protocol-runner.ts; 3 новых теста GREEN; TypeScript exhaustiveness закрыта.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-CONTEXT.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-RESEARCH.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-01-SUMMARY.md
@.planning/phases/22-snippet-node-graph-and-runner-layer/22-02-SUMMARY.md
</context>

<interfaces>
<!-- Точные паттерны из RESEARCH.md (ВЕРИФИЦИРОВАНО прямым чтением кодовой базы) -->

Из src/runner/protocol-runner.ts — существующий case 'loop-end': (ШАБЛОН для case 'snippet':):
```typescript
case 'loop-end': {
  // Halt here — RunnerView will render "loop again / done" prompt (LOOP-02)
  this.currentNodeId = cursor;
  this.runnerStatus = 'at-node';
  return;
}
```
ВАЖНО: Нет push в undoStack — по той же причине нет и у snippet (D-07).

Из src/runner/protocol-runner.ts — существующий getState() at-node return (ШАБЛОН для isAtSnippetNode):
```typescript
return {
  status: 'at-node',
  currentNodeId: this.currentNodeId ?? '',
  accumulatedText: this.accumulator.current,
  canStepBack: this.undoStack.length > 0,
  loopIterationLabel,
  isAtLoopEnd: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'loop-end',
  // добавить рядом:
  isAtSnippetNode: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'snippet',
};
```

Из src/runner/runner-state.ts — существующий AtNodeState (ШАБЛОН для isAtSnippetNode):
```typescript
/** true when currentNodeId refers to a loop-end node — drives UI branch (LOOP-02) */
isAtLoopEnd?: boolean;
// добавить рядом:
/** true when currentNodeId refers to a snippet node — Phase 25 renders file-picker button */
isAtSnippetNode?: boolean;
```

После Wave 0 advanceThrough() switch содержит cases: start, question, answer, text-block, loop-start, loop-end, default: never.
После Plan 02 добавлен snippet в RPNodeKind — TypeScript требует case 'snippet': для exhaustiveness.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runner halt и AtNodeState.isAtSnippetNode (D-05, D-06, D-07)</name>
  <files>
    src/runner/runner-state.ts
    src/runner/protocol-runner.ts
  </files>
  <read_first>
    - src/runner/runner-state.ts — AtNodeState interface (текущие поля после Wave 0)
    - src/runner/protocol-runner.ts — advanceThrough() switch (все case, позиция default: never); getState() at-node return block (строка с isAtLoopEnd)
  </read_first>
  <behavior>
    - AtNodeState содержит isAtSnippetNode?: boolean рядом с isAtLoopEnd
    - advanceThrough() содержит case 'snippet': идентичный case 'loop-end': (halt, set currentNodeId, set runnerStatus='at-node', return) — БЕЗ push в undoStack
    - getState() at-node return содержит isAtSnippetNode: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'snippet'
    - TypeScript не жалуется на exhaustiveness (case 'snippet': закрывает union member)
  </behavior>
  <action>
**src/runner/runner-state.ts:**
1. В интерфейсе `AtNodeState`, после строки `isAtLoopEnd?: boolean;`, добавить:
```typescript
/** true when currentNodeId refers to a snippet node — Phase 25 renders file-picker button (D-05) */
isAtSnippetNode?: boolean;
```

**src/runner/protocol-runner.ts:**
1. В методе `advanceThrough()`, в switch по `node.kind`, ПЕРЕД блоком `default: never`, добавить:
```typescript
case 'snippet': {
  // Halt here — RunnerView will render file-picker button (Phase 25) (D-06)
  // No undo push — no mutation has occurred yet (D-07)
  this.currentNodeId = cursor;
  this.runnerStatus = 'at-node';
  return;
}
```
2. В методе `getState()`, в ветке `case 'at-node':`, в return-объект, РЯДОМ со строкой `isAtLoopEnd: ...`, добавить:
```typescript
isAtSnippetNode: this.graph?.nodes.get(this.currentNodeId ?? '')?.kind === 'snippet',
```
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run src/__tests__/runner/protocol-runner.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "isAtSnippetNode" src/runner/runner-state.ts выводит строку в AtNodeState interface
    - grep "case 'snippet'" src/runner/protocol-runner.ts выводит строку в advanceThrough()
    - grep "isAtSnippetNode" src/runner/protocol-runner.ts выводит строку в getState() return
    - grep -A5 "case 'snippet'" src/runner/protocol-runner.ts НЕ содержит "undoStack.push" (проверяет D-07)
    - npx vitest run src/__tests__/runner/protocol-runner.test.ts — выходит с кодом 0 (существующие тесты не сломаны)
  </acceptance_criteria>
  <done>Runner останавливается на snippet node в at-node state; getState() возвращает isAtSnippetNode: true; TypeScript exhaustiveness закрыта</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runner-тесты для snippet halt (SNIPPET-01)</name>
  <files>
    src/__tests__/runner/protocol-runner.test.ts
  </files>
  <read_first>
    - src/__tests__/runner/protocol-runner.test.ts — import-блок, describe-структура, паттерн создания тестовых graphs (как создаётся ProtocolGraph для тестов)
    - src/__tests__/fixtures/snippet-node.canvas — fixture для использования в тестах (создан в Plan 02)
  </read_first>
  <behavior>
    - Тест 1: runner.start(graph) где graph содержит snippet terminal → getState().status === 'at-node' И getState().isAtSnippetNode === true
    - Тест 2: сразу после halt у snippet → canStepBack === false (undo-стек пуст, т.к. D-07 — halt не пушит undo)
    - Тест 3 (optional): runner не переходит в 'error' state при встрече snippet-узла
  </behavior>
  <action>
В src/__tests__/runner/protocol-runner.test.ts добавить describe-группу. Использовать inline graph (не fixture файл), чтобы тест был самодостаточным:

```typescript
describe('snippet node — runner halt (D-06, D-07)', () => {
  function makeSnippetGraph(): ProtocolGraph {
    return {
      canvasFilePath: 'test.canvas',
      startNodeId: 'n-start',
      nodes: new Map([
        ['n-start', { id: 'n-start', kind: 'start', x: 0, y: 0, width: 200, height: 60 }],
        ['n-snip', { id: 'n-snip', kind: 'snippet', x: 0, y: 120, width: 200, height: 60 }],
      ]),
      edges: [{ id: 'e1', fromNodeId: 'n-start', toNodeId: 'n-snip' }],
      adjacency: new Map([['n-start', ['n-snip']]]),
      reverseAdjacency: new Map([['n-snip', ['n-start']]]),
    };
  }

  it('halts at snippet node in at-node state (D-06)', () => {
    const runner = new ProtocolRunner();
    runner.start(makeSnippetGraph());
    const state = runner.getState();
    expect(state.status).toBe('at-node');
  });

  it('sets isAtSnippetNode: true when halted at snippet (D-05)', () => {
    const runner = new ProtocolRunner();
    runner.start(makeSnippetGraph());
    const state = runner.getState();
    if (state.status !== 'at-node') throw new Error(`expected at-node, got ${state.status}`);
    expect(state.isAtSnippetNode).toBe(true);
  });

  it('canStepBack is false immediately after halt at snippet (D-07 — no undo push)', () => {
    const runner = new ProtocolRunner();
    runner.start(makeSnippetGraph());
    const state = runner.getState();
    if (state.status !== 'at-node') throw new Error(`expected at-node, got ${state.status}`);
    expect(state.canStepBack).toBe(false);
  });
});
```
Убедиться что `ProtocolRunner` и `ProtocolGraph` уже импортированы в файле (обычно да). Если `ProtocolGraph` не импортирован — добавить в import.
  </action>
  <verify>
    <automated>cd "Z:/projects/RadiProtocolObsidian" && npx vitest run src/__tests__/runner/protocol-runner.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/__tests__/runner/protocol-runner.test.ts — выходит с кодом 0
    - Вывод содержит "snippet node — runner halt"
    - Все 3 новых теста PASS: "halts at snippet", "isAtSnippetNode: true", "canStepBack is false"
    - npx vitest run — 146+ passed (базовые 138 + 5 parser + 1 validator + 3 runner + DEPRECATED_KINDS), 3 RED в runner-extensions.test.ts
  </acceptance_criteria>
  <done>Три теста для runner halt GREEN; вся phase suite зелёная; Phase 22 готова к /gsd-verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProtocolGraph → runner | graph.nodes.get() вызовы в getState() — граф уже верифицирован GraphValidator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Denial | advanceThrough() infinite loop | accept | snippet halt использует `return` немедленно — нет while-loop continuation; loop-cap не затронут |
| T-22-06 | Tampering | undo stack integrity | mitigate | case 'snippet': не вызывает undoStack.push() — проверяется acceptance_criteria grep |
</threat_model>

<verification>
После завершения плана — финальная фаза:
```
npx vitest run
```
Ожидаемый результат: 146+ pass, 3 RED (runner-extensions.test.ts).

Дополнительные проверки:
- `grep "isAtSnippetNode" src/runner/runner-state.ts` — присутствует
- `grep "case 'snippet'" src/runner/protocol-runner.ts` — присутствует (2 места: advanceThrough + ВОЗМОЖНО в resolveSeparator если там есть exhaustive switch)
- `grep "isAtSnippetNode" src/runner/protocol-runner.ts` — присутствует в getState()
</verification>

<success_criteria>
Phase 22 завершена (все 4 success criteria из ROADMAP):
1. tsc --noEmit проходит с нулём ошибок на project source (snippet в RPNodeKind union, exhaustiveness закрыта)
2. Canvas-файл со snippet-type node парсируется без ошибок и появляется в ProtocolGraph (тест в Plan 02)
3. Граф-валидатор принимает snippet node в валидном графе и не выдаёт dead-end ошибку (тест в Plan 02)
4. Runner останавливается на snippet node в 'at-node' state, не auto-advance, не error (тесты в Plan 03)
</success_criteria>

<output>
После завершения создать `.planning/phases/22-snippet-node-graph-and-runner-layer/22-03-SUMMARY.md`
</output>
