---
phase: 60-inline-runner-layout-position-persistence
plan: 01
type: execute
wave: 1
depends_on: [60-00]
files_modified:
  - src/settings.ts
  - src/main.ts
  - src/views/inline-runner-modal.ts
  - src/__tests__/views/inline-runner-position.test.ts
autonomous: true
requirements:
  - INLINE-FIX-02
must_haves:
  truths:
    - "Inline Runner has a durable last-position state that survives plugin reload."
    - "Restored coordinates are always finite numbers clamped into the current viewport."
  artifacts:
    - path: "src/settings.ts"
      provides: "Persisted inline runner position data shape and default"
      contains: "inlineRunnerPosition"
    - path: "src/main.ts"
      provides: "Plugin-owned persistence methods passed to InlineRunnerModal"
      contains: "saveInlineRunnerPosition"
    - path: "src/views/inline-runner-modal.ts"
      provides: "Position state contracts and clamp helper"
      contains: "clampInlineRunnerPosition"
  key_links:
    - from: "src/views/inline-runner-modal.ts"
      to: "src/main.ts"
      via: "plugin position getter/setter"
      pattern: "saveInlineRunnerPosition"
---

<objective>
Add the durable position-state contract and viewport-clamping helper for Inline Runner.

Purpose: This gives drag/restore code a safe persistence layer and prevents off-screen coordinates from reaching DOM styles.
Output: Settings/data shape, plugin persistence methods, exported or test-reachable clamp helper, and passing Phase 60 position-state tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-inline-runner-layout-position-persistence/60-CONTEXT.md
@src/settings.ts
@src/main.ts
@src/views/inline-runner-modal.ts
@src/__tests__/views/inline-runner-position.test.ts

<interfaces>
Create this contract per D-01/D-02:
```typescript
export interface InlineRunnerPosition {
  left: number;
  top: number;
}
```
Persist `inlineRunnerPosition: InlineRunnerPosition | null` in plugin data with `DEFAULT_SETTINGS.inlineRunnerPosition = null`. Do not add a Settings tab control.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01.1: Add persisted position data contract</name>
  <files>src/settings.ts, src/main.ts, src/__tests__/views/inline-runner-position.test.ts</files>
  <behavior>
    - Existing installs without `inlineRunnerPosition` merge to `null` via `Object.assign({}, DEFAULT_SETTINGS, await loadData())`.
    - Saving a finite `{left, top}` updates plugin data without exposing a user-facing setting.
    - Saving `null` is possible for defensive reset in tests, but no Settings UI is added.
  </behavior>
  <action>Add `InlineRunnerPosition` and `inlineRunnerPosition: InlineRunnerPosition | null` to `RadiProtocolSettings`; default it to `null`. Add `getInlineRunnerPosition()` and `saveInlineRunnerPosition(position: InlineRunnerPosition | null): Promise<void>` methods to `RadiProtocolPlugin`; `saveInlineRunnerPosition` must mutate `this.settings.inlineRunnerPosition` and call `saveSettings()`. This implements D-01. Do not add any visible setting or reset button (D-04).</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Tests can seed/read saved coordinates through plugin methods; default state is null on fresh settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01.2: Add clamp helper and restore calculation</name>
  <files>src/views/inline-runner-modal.ts, src/__tests__/views/inline-runner-position.test.ts</files>
  <behavior>
    - Given saved `{left, top}` and modal dimensions, clamp keeps at least the header/drag handle visible.
    - Negative or far-beyond-viewport coordinates clamp to visible bounds.
    - Non-finite values are ignored and fall back to compact default coordinates.
  </behavior>
  <action>In `inline-runner-modal.ts`, define a small position type and a `clampInlineRunnerPosition(position, viewport, size)` helper near the position code. Use finite-number guards (`Number.isFinite`) before applying values. The clamped left/top must keep the modal inside `0..viewportWidth - minVisibleWidth` and `0..viewportHeight - minVisibleHeaderHeight`; choose constants that leave the header draggable. Reference D-02 in comments. Export the helper if needed for direct unit tests; otherwise test through modal open.</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Out-of-range saved coordinates restore to visible on-screen positions and invalid coordinates do not reach `style.left/top`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin data→InlineRunnerModal | Previously persisted coordinates become DOM style values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | Tampering | `inlineRunnerPosition` | mitigate | Accept only finite numeric left/top and clamp before style application. |
| T-60-01-02 | DoS | off-screen restore | mitigate | Clamp to viewport and keep draggable header visible. |
</threat_model>

<verification>
Run `npm test -- src/__tests__/views/inline-runner-position.test.ts` and `npx tsc --noEmit --skipLibCheck`.
</verification>

<success_criteria>
Inline Runner has safe durable position state and clamping primitives; no visible Settings UI is added.
</success_criteria>

<output>
After completion, create `.planning/phases/60-inline-runner-layout-position-persistence/60-01-SUMMARY.md`
</output>
