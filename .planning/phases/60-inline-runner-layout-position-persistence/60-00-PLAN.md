---
phase: 60-inline-runner-layout-position-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/views/inline-runner-position.test.ts
  - src/__tests__/regression.test.ts
  - src/__mocks__/obsidian.ts
autonomous: true
requirements:
  - INLINE-FIX-02
  - INLINE-FIX-03
must_haves:
  truths:
    - "Automated tests describe persisted inline-runner drag coordinates and viewport clamping before implementation."
    - "Automated tests describe compact inline-runner CSS so generated agents cannot silently keep the Phase 54 bottom bar footprint."
  artifacts:
    - path: "src/__tests__/views/inline-runner-position.test.ts"
      provides: "RED tests for drag persistence, restore, and clamping"
      min_lines: 120
    - path: "src/__tests__/regression.test.ts"
      provides: "Source/CSS regression guard for compact inline runner rules"
      contains: "Phase 60"
    - path: "src/__mocks__/obsidian.ts"
      provides: "Mock workspace/saveData support needed by Phase 60 tests"
  key_links:
    - from: "src/__tests__/views/inline-runner-position.test.ts"
      to: "src/views/inline-runner-modal.ts"
      via: "InlineRunnerModal instantiated against fake workspace + fake target note"
      pattern: "InlineRunnerModal"
    - from: "src/__tests__/regression.test.ts"
      to: "src/styles/inline-runner.css"
      via: "source-string assertions"
      pattern: "rp-inline-runner-container"
---

<objective>
Create Phase 60 RED verification scaffolding for position persistence, viewport clamping, drag behavior, and compact layout.

Purpose: Phase 60 changes UI state and CSS where visual regressions are likely; tests must define the expected behavior before implementation.
Output: Focused Vitest files/mocks that fail until Plans 60-01 through 60-03 land.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-inline-runner-layout-position-persistence/60-CONTEXT.md
@.planning/milestones/v1.8-phases/54-inline-protocol-display-mode/54-CONTEXT.md
@src/views/inline-runner-modal.ts
@src/styles/inline-runner.css
@src/__mocks__/obsidian.ts
@src/__tests__/regression.test.ts

<interfaces>
From src/views/inline-runner-modal.ts:
```typescript
export class InlineRunnerModal {
  async open(): Promise<void>;
  close(): void;
}
```

From src/main.ts:
```typescript
export default class RadiProtocolPlugin extends Plugin {
  settings!: RadiProtocolSettings;
  async saveSettings(): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00.1: Add RED position persistence tests</name>
  <files>src/__tests__/views/inline-runner-position.test.ts, src/__mocks__/obsidian.ts</files>
  <behavior>
    - Test 1: first open without saved state places the inline runner at a compact default in the viewport.
    - Test 2: dragging the header updates `style.left`/`style.top`, clears bottom/right anchoring, and saves coordinates per D-01.
    - Test 3: reopening after saved coordinates restores them per D-01.
    - Test 4: saved coordinates outside the viewport are clamped to visible bounds per D-02.
    - Test 5: layout-change re-clamps a currently visible modal without erasing the saved user position.
  </behavior>
  <action>Create `src/__tests__/views/inline-runner-position.test.ts` using Vitest and the existing mock-DOM style from view tests. Mock only what InlineRunnerModal needs: vault read/modify, workspace events, active file, layout events, parser/validator success, and plugin `saveSettings` or equivalent persistence hook. If current `src/__mocks__/obsidian.ts` lacks event-ref or workspace helpers, add minimal stubs only; do not rewrite existing mocks. Reference D-01 and D-02 in describe names.</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Position tests exist, compile under Vitest, and fail only because Phase 60 production code is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00.2: Add RED compact-layout regression guards</name>
  <files>src/__tests__/regression.test.ts</files>
  <behavior>
    - Test 1: `src/styles/inline-runner.css` contains a Phase 60 compact layout section.
    - Test 2: inline runner container is no longer a full note-width bottom bar by default.
    - Test 3: compact rules bound width/height and reduce padding/spacing for `.rp-inline-runner-content` and question-zone descendants.
  </behavior>
  <action>Append Phase 60 regression tests to `src/__tests__/regression.test.ts`; use source-string assertions like existing regression guards. The assertions should require a `/* Phase 60: compact draggable inline runner */` CSS comment and compact selectors in `src/styles/inline-runner.css`. Do not assert pixel-perfect values; assert presence of bounded max-width/max-height and compact padding tokens.</action>
  <verify>
    <automated>npm test -- src/__tests__/regression.test.ts</automated>
  </verify>
  <done>Regression guards make it impossible to satisfy Phase 60 without CSS changes in `src/styles/inline-runner.css` per D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted plugin data→DOM style | Stored numeric coordinates become CSS positions. |
| pointer events→persisted state | User drag input updates durable position state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-00-01 | Tampering | position tests | mitigate | Tests require numeric finite-coordinate validation and viewport clamping before DOM style application. |
| T-60-00-02 | DoS | off-screen saved coordinates | mitigate | Tests cover out-of-bounds saved state and layout-change clamping. |
</threat_model>

<verification>
Run `npm test -- src/__tests__/views/inline-runner-position.test.ts src/__tests__/regression.test.ts`; RED failures should point at missing Phase 60 behavior, not TypeScript syntax or mock errors.
</verification>

<success_criteria>
Phase 60 has automated RED coverage for INLINE-FIX-02 and INLINE-FIX-03 before production code changes.
</success_criteria>

<output>
After completion, create `.planning/phases/60-inline-runner-layout-position-persistence/60-00-SUMMARY.md`
</output>
