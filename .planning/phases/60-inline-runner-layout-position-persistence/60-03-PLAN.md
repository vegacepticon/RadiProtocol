---
phase: 60-inline-runner-layout-position-persistence
plan: 03
type: execute
wave: 3
depends_on: [60-02]
files_modified:
  - src/styles/inline-runner.css
  - styles.css
  - src/styles.css
  - src/__tests__/regression.test.ts
autonomous: true
requirements:
  - INLINE-FIX-03
must_haves:
  truths:
    - "Inline Runner default footprint is compact and no longer spans the note width."
    - "Question/answer/snippet/toolbar spacing is tightened without deleting existing Phase 54 rules."
    - "Generated CSS is refreshed from source CSS."
  artifacts:
    - path: "src/styles/inline-runner.css"
      provides: "Phase 60 compact draggable inline runner CSS"
      contains: "Phase 60: compact draggable inline runner"
    - path: "styles.css"
      provides: "Generated root CSS after build"
    - path: "src/styles.css"
      provides: "Generated convenience CSS copy after build"
  key_links:
    - from: "src/styles/inline-runner.css"
      to: "styles.css"
      via: "npm run build CSS concat"
      pattern: "Phase 60: compact draggable inline runner"
---

<objective>
Apply the compact default Inline Runner layout and regenerate built CSS.

Purpose: The modal must stop covering the active note's editing area by default while preserving all existing runner controls.
Output: Append-only Phase 60 CSS overrides and regenerated CSS bundles.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-inline-runner-layout-position-persistence/60-CONTEXT.md
@CLAUDE.md
@src/styles/inline-runner.css
@src/__tests__/regression.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 03.1: Append compact inline runner CSS</name>
  <files>src/styles/inline-runner.css, src/__tests__/regression.test.ts</files>
  <action>Append a new section at the bottom of `src/styles/inline-runner.css` with comment `/* Phase 60: compact draggable inline runner */`. Do not delete or rewrite existing Phase 54 rules. Override `.rp-inline-runner-container` to use compact dimensions (bounded width, max-width, max-height, border radius, full border, right/bottom no longer authoritative once JS sets left/top). Tighten `.rp-inline-runner-header`, `.rp-inline-runner-content`, `.rp-question-text`, answer/snippet/loop lists, and `.rp-output-toolbar` spacing. Add `cursor: grab` / active dragging affordance for the header. Do not add resize handles or fade rules per D-04.</action>
  <verify>
    <automated>npm test -- src/__tests__/regression.test.ts</automated>
  </verify>
  <done>Regression tests find the Phase 60 compact CSS section and compact layout selectors.</done>
</task>

<task type="auto">
  <name>Task 03.2: Rebuild generated CSS artifacts</name>
  <files>styles.css, src/styles.css</files>
  <action>Run the production build so esbuild concatenates `src/styles/inline-runner.css` into root `styles.css` and `src/styles.css`. Do not hand-edit generated files. If the build surfaces TypeScript issues from previous Phase 60 plans, fix those source issues rather than editing generated outputs.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`npm run build` exits 0 and generated CSS contains the Phase 60 compact section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS layout→note editing area | Floating modal visual footprint can obscure user note editing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03-01 | DoS | compact CSS | mitigate | Bound max-width/max-height and leave modal draggable so note editing remains accessible. |
| T-60-03-02 | Tampering | generated CSS | mitigate | Regenerate via `npm run build`; do not hand-edit `styles.css`. |
</threat_model>

<verification>
Run `npm test -- src/__tests__/regression.test.ts` and `npm run build`.
</verification>

<success_criteria>
Inline Runner has compact default styling and generated CSS is in sync with source CSS.
</success_criteria>

<output>
After completion, create `.planning/phases/60-inline-runner-layout-position-persistence/60-03-SUMMARY.md`
</output>
