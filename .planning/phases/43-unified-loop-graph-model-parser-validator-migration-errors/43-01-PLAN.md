---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/graph-model.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
user_setup: []

must_haves:
  truths:
    - "RPNodeKind содержит литерал 'loop'"
    - "RPNode union включает LoopNode"
    - "LoopNode имеет поле headerText типа string"
    - "LoopContext использует поле loopNodeId (не loopStartId)"
    - "LoopStartNode и LoopEndNode остаются экспортируемыми (помечены @deprecated) для переживания parsing legacy-канвасов в Phase 43"
    - "TypeScript compile (tsc) проходит после изменения union — никаких broken imports"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "RPNodeKind union + LoopNode interface + LoopContext с loopNodeId + LegacyLoopStartNode/LegacyLoopEndNode"
      contains: "kind: 'loop'"
    - path: "src/graph/graph-model.ts"
      provides: "LoopNode interface"
      contains: "headerText: string"
  key_links:
    - from: "src/graph/graph-model.ts"
      to: "src/graph/canvas-parser.ts"
      via: "RPNodeKind type import + LoopNode interface"
      pattern: "import .*LoopNode.*from '\\./graph-model'"
    - from: "src/graph/graph-model.ts"
      to: "src/canvas/node-color-map.ts"
      via: "Record<RPNodeKind, string> exhaustiveness"
      pattern: "Record<RPNodeKind, string>"
---

<objective>
Обновить discriminated union `RPNodeKind` и набор node-interface'ов в `src/graph/graph-model.ts`, чтобы модель выражала единый `loop` узел (LOOP-01) с полем `headerText` (LOOP-02) вместо пары `loop-start`/`loop-end`. Это фундамент всей Phase 43 — все downstream файлы (parser, validator, node-color-map, session, runner, views) зависят от новой формы типов.

Purpose: Без обновления модели TypeScript-exhaustiveness не может пересобрать остальной codebase под unified-loop. Одна точка истины даёт нам автоматический «scavenger hunt» по всем switch-ам и Record'ам — TS находит их сам.

Output: Обновлённый `src/graph/graph-model.ts` с `'loop'` в RPNodeKind union, новым `LoopNode` interface, переименованным полем `loopNodeId` в `LoopContext`, и сохранёнными (но `@deprecated`) `LoopStartNode` / `LoopEndNode` для поддержки legacy parsing в D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Текущие типы в graph-model.ts (строки 1-120) — executor модифицирует именно их. -->
<!-- См. `src/graph/graph-model.ts` целиком; ниже — минимальный контракт контекста. -->

```ts
// Current (to be changed):
export type RPNodeKind =
  | 'start' | 'question' | 'answer' | 'free-text-input'
  | 'text-block' | 'loop-start' | 'loop-end' | 'snippet';

export interface LoopStartNode extends RPNodeBase { kind: 'loop-start'; loopLabel: string; exitLabel: string; maxIterations: number; }
export interface LoopEndNode extends RPNodeBase { kind: 'loop-end'; loopStartId: string; }

export interface LoopContext { loopStartId: string; iteration: number; textBeforeLoop: string; }

export type RPNode =
  | StartNode | QuestionNode | AnswerNode | FreeTextInputNode
  | TextBlockNode | LoopStartNode | LoopEndNode | SnippetNode;
```

```ts
// Pattern-analog для нового LoopNode — QuestionNode (graph-model.ts:28-31):
export interface QuestionNode extends RPNodeBase {
  kind: 'question';
  questionText: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Перестроить RPNodeKind + RPNode union, добавить LoopNode interface, переименовать LoopContext.loopStartId → loopNodeId, пометить LoopStartNode/LoopEndNode как @deprecated</name>

  <read_first>
    - `src/graph/graph-model.ts` (весь файл — 120 строк, прочитать целиком перед правкой)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-01/D-02/D-03/D-04/D-CL-05 (locked decisions по модели)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/graph/graph-model.ts` (model, transform)» + секция «Shared Patterns → Discriminated-union exhaustiveness»
    - `CLAUDE.md` правило «Never remove existing code you didn't add» — `FreeTextInputNode` (строки 40-46) НЕ трогать в этой фазе, Phase 46 его уберёт
  </read_first>

  <objective>
    Превратить `graph-model.ts` в новую «single source of truth» для unified loop. После этой задачи TypeScript compile будет красный по всему остальному codebase (это целевое поведение — exhaustiveness-check погонит нас через все use-sites), но ТОЛЬКО этот файл после правки должен быть внутренне самосогласован.
  </objective>

  <action>
  Последовательно внести СЛЕДУЮЩИЕ изменения в `src/graph/graph-model.ts`. Не удалять ничего вне этого списка (см. CLAUDE.md never-remove-existing-code).

  **1. RPNodeKind union (строки 4-12)** — добавить `'loop'` литерал в union и НЕ удалять `'loop-start'` / `'loop-end'` (они нужны для D-06 legacy-parsing; удаление — Phase 46/пост). Итог:

  ```ts
  // Phase 43 D-01: unified 'loop' kind добавлен; 'loop-start' / 'loop-end' сохраняются
  // как legacy parseable kinds до Phase 44 (runtime stub) и последующих фаз.
  export type RPNodeKind =
    | 'start'
    | 'question'
    | 'answer'
    | 'free-text-input'
    | 'text-block'
    | 'loop-start'      // @deprecated Phase 43 D-03 — legacy parseable for migration-error (D-07)
    | 'loop-end'        // @deprecated Phase 43 D-03 — legacy parseable for migration-error (D-07)
    | 'snippet'         // Phase 29
    | 'loop';           // Phase 43 D-01 — unified loop kind (see v1.7 LOOP-01, LOOP-02)
  ```

  **2. Новый `LoopNode` interface** — добавить ПОСЛЕ `TextBlockNode` (после строки 53), ПЕРЕД `LoopStartNode`. Shape зеркалит `QuestionNode` (один string payload, всегда defined):

  ```ts
  /**
   * Phase 43 D-02 — unified loop node (LOOP-01, LOOP-02).
   * Замена пары LoopStartNode/LoopEndNode. headerText — текст заголовка над picker'ом,
   * рендерится runtime в Phase 44. Отсутствие / undefined в canvas JSON нормализуется
   * парсером в пустую строку (D-05).
   */
  export interface LoopNode extends RPNodeBase {
    kind: 'loop';
    headerText: string;
  }
  ```

  **3. Существующие `LoopStartNode` / `LoopEndNode` (строки 55-65)** — ОСТАВИТЬ interface-объявления как есть (по D-CL-05 вариант (b) — сохранить имена), но добавить `@deprecated` JSDoc перед каждым:

  ```ts
  /**
   * @deprecated Phase 43 D-03, D-CL-05 — legacy parseable kind.
   * Canvas с этим узлом теперь отвергается GraphValidator'ом c migration-error (MIGRATE-01).
   * Удаление — в будущей фазе после того как все legacy-канвасы пересобраны.
   */
  export interface LoopStartNode extends RPNodeBase {
    kind: 'loop-start';
    loopLabel: string;
    exitLabel: string;
    maxIterations: number;
  }

  /**
   * @deprecated Phase 43 D-03, D-CL-05 — legacy parseable kind.
   * См. {@link LoopStartNode} для контекста.
   */
  export interface LoopEndNode extends RPNodeBase {
    kind: 'loop-end';
    loopStartId: string;
  }
  ```

  **4. `LoopContext` (строки 81-89)** — переименовать поле `loopStartId` → `loopNodeId` (D-04). JSDoc обновить:

  ```ts
  /**
   * One frame on the loop context stack.
   * Pushed when the runner enters a unified loop node (Phase 43 D-04).
   * Contains only primitives — shallow array copy is sufficient for snapshots (LOOP-05).
   */
  export interface LoopContext {
    /** Phase 43 D-04 — ID of the unified loop node that opened this frame.
     *  Renamed from loopStartId (v1.0..v1.6). */
    loopNodeId: string;
    /** 1-based iteration counter (starts at 1 on first entry) */
    iteration: number;
    /** Full text snapshot captured immediately before entering the loop body.
     *  Used to restore accumulated text if the user steps back past the loop entry point. */
    textBeforeLoop: string;
  }
  ```

  **5. `RPNode` discriminated union (строки 91-99)** — добавить `LoopNode`, сохранить `LoopStartNode` / `LoopEndNode` (D-CL-05 вариант (b)):

  ```ts
  export type RPNode =
    | StartNode
    | QuestionNode
    | AnswerNode
    | FreeTextInputNode
    | TextBlockNode
    | LoopStartNode     // @deprecated — legacy, см. interface JSDoc
    | LoopEndNode       // @deprecated — legacy, см. interface JSDoc
    | SnippetNode       // Phase 29
    | LoopNode;         // Phase 43 D-02 — unified loop (LOOP-01, LOOP-02)
  ```

  **НЕ ТРОГАТЬ:** строки 14-22 (`RPNodeBase`), 24-26 (`StartNode`), 28-31 (`QuestionNode`), 33-38 (`AnswerNode`), 40-46 (`FreeTextInputNode` — Phase 46), 48-53 (`TextBlockNode`), 67-74 (`SnippetNode` + Phase 29/31 комментарии), 101-119 (`RPEdge`, `ProtocolGraph`, `ParseResult`). Шапка-комментарий в строках 1-2 — оставить.

  **Почему `LoopStartNode.maxIterations` остаётся в interface-объявлении:** это часть legacy-формы, которую parser (D-06) читает из старого canvas JSON. Значение никуда не утекает в runtime (Phase 43 runner-stub не доходит до loop-логики). Удаление поля из interface произойдёт когда будут удалены сами interface'ы — после v1.7. Pitfall STATE.md #10 говорит «never carry maxIterations forward into LoopNode» — это соблюдено: в `LoopNode` поля `maxIterations` нет. Пункт #10 НЕ требует немедленного удаления из `LoopStartNode`.
  </action>

  <acceptance_criteria>
    - `npm run build` завершается exit 0 ТОЛЬКО для файла `graph-model.ts`; сборка всего проекта падает с TS-ошибками в downstream файлах (это ожидаемо — другие планы их починят). Проверить «здоровье» именно этого файла:
      ```bash
      npx tsc --noEmit --skipLibCheck src/graph/graph-model.ts 2>&1 | tee /tmp/model-tsc.log || true
      # В /tmp/model-tsc.log НЕ должно быть ошибок, упоминающих graph-model.ts
      grep -c "graph-model.ts" /tmp/model-tsc.log    # ожидается 0
      ```
    - Union содержит новый literal:
      ```bash
      grep -q "| 'loop';" src/graph/graph-model.ts     # Phase 43 D-01
      ```
    - `LoopNode` interface определён с полем `headerText: string`:
      ```bash
      grep -qzP "export interface LoopNode extends RPNodeBase \{\s*kind: 'loop';\s*headerText: string;\s*\}" src/graph/graph-model.ts
      ```
    - `LoopContext` использует `loopNodeId` (не `loopStartId`):
      ```bash
      grep -q "loopNodeId: string;" src/graph/graph-model.ts
      ! grep -q "loopStartId: string;" <(sed -n '/export interface LoopContext/,/^}/p' src/graph/graph-model.ts)
      ```
    - `LoopStartNode` / `LoopEndNode` сохранены и помечены `@deprecated`:
      ```bash
      grep -B1 "export interface LoopStartNode" src/graph/graph-model.ts | grep -q "@deprecated"
      grep -B1 "export interface LoopEndNode"   src/graph/graph-model.ts | grep -q "@deprecated"
      ```
    - `RPNode` union содержит `LoopNode`:
      ```bash
      grep -q "| LoopNode" src/graph/graph-model.ts
      ```
    - `FreeTextInputNode` interface нетронут (CLAUDE.md never-remove):
      ```bash
      grep -q "export interface FreeTextInputNode extends RPNodeBase" src/graph/graph-model.ts
      grep -q "kind: 'free-text-input';" src/graph/graph-model.ts
      ```
    - `SnippetNode` interface и комментарии Phase 29/31 нетронуты:
      ```bash
      grep -q "Phase 31 D-01" src/graph/graph-model.ts
      grep -q "Phase 31 D-04" src/graph/graph-model.ts
      ```
    - Phase-marker комментарий `Phase 43 D-01` или `Phase 43 D-02` присутствует где-то в файле:
      ```bash
      grep -q "Phase 43" src/graph/graph-model.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # 1. graph-model.ts внутренне валиден
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "graph-model.ts")
      # 2. union и типы на месте
      grep -q "| 'loop';" src/graph/graph-model.ts \
        && grep -q "export interface LoopNode extends RPNodeBase" src/graph/graph-model.ts \
        && grep -q "headerText: string;" src/graph/graph-model.ts \
        && grep -q "loopNodeId: string;" src/graph/graph-model.ts \
        && grep -q "| LoopNode" src/graph/graph-model.ts \
        && grep -q "@deprecated" src/graph/graph-model.ts
    </automated>
  </verify>

  <done>
    - RPNodeKind union содержит `'loop'`; `'loop-start'`/`'loop-end'` сохранены как legacy parseable
    - `LoopNode` interface экспортирован с `kind: 'loop'` + `headerText: string`
    - `LoopStartNode` / `LoopEndNode` interface'ы сохранены с `@deprecated` JSDoc
    - `LoopContext.loopStartId` переименован в `loopNodeId`
    - `RPNode` discriminated union включает `LoopNode`
    - `graph-model.ts` сам по себе компилируется без ошибок
    - `FreeTextInputNode`, `SnippetNode`, `RPNodeBase`, `RPEdge`, `ProtocolGraph`, `ParseResult` — нетронуты
  </done>
</task>

</tasks>

<verification>
  - Проверить grep-паттерны из `<acceptance_criteria>` — все проходят
  - Прочитать итоговый файл, убедиться что:
    - порядок kinds в `RPNodeKind` union имеет `'loop'` явно после `'snippet'` или в конце (по усмотрению, но не внутри legacy-группы)
    - JSDoc пометки `@deprecated` стоят ПЕРЕД interface-декларацией (не внутри тела)
    - phase-marker комментарий указывает на D-01/D-02 соответственно месту
  - Downstream-файлы (canvas-parser.ts, graph-validator.ts, node-color-map.ts, protocol-runner.ts, runner-state.ts, runner-view.ts, session-model.ts, session-service.ts) — ОЖИДАЕМО ломаются в TypeScript. Это целевое поведение wave 1; починят downstream-планы.
</verification>

<success_criteria>
  - `grep -q "| 'loop';" src/graph/graph-model.ts` → exit 0
  - `grep -q "export interface LoopNode extends RPNodeBase" src/graph/graph-model.ts` → exit 0
  - `grep -q "headerText: string;" src/graph/graph-model.ts` → exit 0
  - `grep -q "loopNodeId: string;" src/graph/graph-model.ts` → exit 0
  - `grep -q "| LoopNode" src/graph/graph-model.ts` → exit 0
  - `grep -cB1 "export interface LoopStartNode" src/graph/graph-model.ts` возвращает строку с `@deprecated`
  - `graph-model.ts` самостоятельно компилируется (нет ошибок в этом файле при tsc --noEmit)
  - `FreeTextInputNode` и `SnippetNode` нетронуты
  - Commit содержит ТОЛЬКО изменения в `src/graph/graph-model.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md`
</output>
