---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 04
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/runner/runner-state.ts
  - src/runner/protocol-runner.ts
  - src/views/runner-view.ts
autonomous: true
requirements:
  - LOOP-01
user_setup: []

must_haves:
  truths:
    - "advanceThrough() не содержит case 'loop-start' / case 'loop-end' в switch"
    - "advanceThrough() содержит case 'loop': с transitionToError-вызовом (или эквивалентным soft-stub'ом)"
    - "chooseLoopAction() метод сохранён как @deprecated stub с transitionToError (Surprise #2: тесты в .skip всё равно компилируются и ссылаются на него)"
    - "getState() больше не читает несуществующие поля loopStartId / loopLabel"
    - "runner-view.ts не содержит case 'loop-end': switch-arm (Surprise #1 — удалять, так как compile сломается без этого)"
    - "npm run build проходит exit 0 — весь проект компилируется с новой моделью (Plan 01) и unified-loop scaffolding"
  artifacts:
    - path: "src/runner/protocol-runner.ts"
      provides: "advanceThrough case 'loop' stub + chooseLoopAction @deprecated stub"
      contains: "case 'loop':"
    - path: "src/views/runner-view.ts"
      provides: "runner-view без case 'loop-end': switch-arm"
      contains: "switch (node.kind)"
  key_links:
    - from: "src/runner/protocol-runner.ts"
      to: "src/graph/graph-model.ts"
      via: "LoopContext.loopNodeId access"
      pattern: "loopNodeId"
    - from: "src/views/runner-view.ts"
      to: "src/runner/runner-state.ts"
      via: "AtNodeState discriminated union (switch на node.kind)"
      pattern: "switch \\(node\\.kind\\)"
---

<objective>
Минимальная хирургия в runner-слое и view-слое, чтобы `npm run build` стал зелёным после Plan 01. Цель — НЕ реализовывать Phase 44 runtime (это будущая фаза), а:

1. Удалить ветки `case 'loop-start'` / `case 'loop-end'` из `advanceThrough()` (они ссылаются на несуществующие типы/поля)
2. Добавить `case 'loop':` с soft-stub `transitionToError('Loop runtime implemented in Phase 44')` (D-14, D-CL-04 опция (b))
3. Оставить `chooseLoopAction()` как `@deprecated` stub-метод, который просто делает `transitionToError` — потому что .skip-ed тесты (D-18) всё равно компилируются и ссылаются на него (Surprise #2)
4. Обновить getState() чтобы не читать `loopStartNode.loopLabel` / `topFrame.loopStartId` (их больше нет)
5. Удалить case `'loop-end':` switch-arm из `runner-view.ts` (строки 402-444) — TS-compile падает без этого удаления (Surprise #1 — CONTEXT.md говорил «view не трогаем» про error-panel, но этот switch — другая точка)
6. Обновить `runner-state.ts`: `isAtLoopEnd` оставить как `@deprecated` (Phase 44 заменит)

Purpose: После этого плана проект собирается; все TypeScript-exhaustiveness-требования удовлетворены; loop-канвасы бросают soft-error в runtime ДО попытки runtime-исполнения (`advanceThrough case 'loop'` → transitionToError). Phase 44 позже перепишет этот stub в полноценный picker.

Output:
- `src/runner/runner-state.ts` с `@deprecated` JSDoc на `isAtLoopEnd` (поле сохраняется для compile-compat .skip тестов)
- `src/runner/protocol-runner.ts` без legacy cases, с `case 'loop':` stub, с `chooseLoopAction` как stub-метод
- `src/views/runner-view.ts` без `case 'loop-end':` switch-arm
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@src/graph/graph-model.ts
@src/runner/runner-state.ts
@src/runner/protocol-runner.ts
@src/views/runner-view.ts

<interfaces>
```ts
// Current transitionToError (protocol-runner.ts:~668-671):
private transitionToError(message: string): void {
  this.runnerStatus = 'error';
  this.errorMessage = message;
}

// Current chooseLoopAction signature (protocol-runner.ts:~295):
chooseLoopAction(action: 'again' | 'done'): void { /* ... полная реализация ... */ }

// Current runner-view switch-arm (runner-view.ts:402-444):
case 'loop-end': {
  // Reads node.loopStartId, matchingStart.loopLabel, matchingStart.exitLabel
  // Calls this.runner.chooseLoopAction('again') / ('done')
  // Uses CSS classes: rp-loop-btn-row, rp-loop-again-btn, rp-loop-done-btn, rp-loop-iteration-label
}
// CSS classes in src/styles/loop-support.css — НЕ трогать (Surprise #5, Phase 45 вернёт picker UI)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Пометить runner-state.ts AtNodeState.isAtLoopEnd как @deprecated (compile-compat для .skip тестов)</name>

  <read_first>
    - `src/runner/runner-state.ts` (весь файл — 99 строк)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-14
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/runner/runner-state.ts`» (рекомендация (c): оставить поле, пометить @deprecated)
  </read_first>

  <objective>
    Оставить поле `isAtLoopEnd?: boolean` в `AtNodeState`, но пометить `@deprecated` — Phase 44 заменит на полноценное `isAtLoopNode` или picker-state. Поле всё равно undefined в Phase 43 (runner-stub бросает error до его установки). `loopIterationLabel?: string` — тоже оставить нетронутым (Phase 44 переинтерпретирует).
  </objective>

  <action>
  В `src/runner/runner-state.ts` найти `AtNodeState` interface (строки 17-27) и обновить JSDoc полей `loopIterationLabel` и `isAtLoopEnd` — добавить `@deprecated Phase 43 D-14` пометку. Не удалять сами поля.

  ```ts
  export interface AtNodeState {
    status: 'at-node';
    currentNodeId: string;
    accumulatedText: string;
    /** true when undoStack is non-empty — avoids exposing the stack itself (D-02) */
    canStepBack: boolean;
    /**
     * @deprecated Phase 43 D-14 — семантика меняется в Phase 44 (unified loop picker, RUN-01).
     * В Phase 43 поле всегда undefined (runner-stub бросает error до достижения loop).
     */
    loopIterationLabel?: string;
    /**
     * @deprecated Phase 43 D-14 — поле теряет смысл: loop-end kind больше не существует.
     * Оставлено как undefined-only до Phase 44, которая заменит на полноценный picker-state.
     */
    isAtLoopEnd?: boolean;
  }
  ```

  **НЕ ТРОГАТЬ:** другие interface'ы (`IdleState`, `AwaitingSnippetPickState`, `AwaitingSnippetFillState`, `CompleteState`, `ErrorState`, `RunnerState` union, `UndoEntry` — последний типизируется через `LoopContext[]` из graph-model, который уже обновлён в Plan 01).
  </action>

  <acceptance_criteria>
    - Поле `isAtLoopEnd` сохранено, помечено `@deprecated`:
      ```bash
      grep -B2 "isAtLoopEnd?: boolean" src/runner/runner-state.ts | grep -q "@deprecated Phase 43 D-14"
      ```
    - Поле `loopIterationLabel` сохранено, помечено `@deprecated`:
      ```bash
      grep -B2 "loopIterationLabel?: string" src/runner/runner-state.ts | grep -q "@deprecated Phase 43 D-14"
      ```
    - Остальные interface'ы не модифицированы:
      ```bash
      grep -q "export interface IdleState" src/runner/runner-state.ts
      grep -q "export interface AwaitingSnippetFillState" src/runner/runner-state.ts
      grep -q "export interface UndoEntry" src/runner/runner-state.ts
      grep -q "Phase 31 D-08" src/runner/runner-state.ts
      ```
    - Компиляция файла:
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "runner-state.ts")
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      grep -B2 "isAtLoopEnd?: boolean" src/runner/runner-state.ts | grep -q "@deprecated Phase 43 D-14" \
        && grep -q "Phase 31 D-08" src/runner/runner-state.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "runner-state.ts")
    </automated>
  </verify>

  <done>
    - `isAtLoopEnd` и `loopIterationLabel` помечены `@deprecated Phase 43 D-14`
    - Остальные types нетронуты
    - Файл компилируется
  </done>
</task>

<task type="auto">
  <name>Task 2: Удалить ветки loop-start/loop-end в advanceThrough, добавить case 'loop' (soft stub), пометить chooseLoopAction как deprecated stub, обновить getState() reader</name>

  <read_first>
    - `src/runner/protocol-runner.ts` (весь файл — особенно `advanceThrough()` switch ~535-643, `chooseLoopAction()` ~295-347, `getState()` ~357-377, `transitionToError()` ~668-671)
    - `src/graph/graph-model.ts` (после Plan 01 — `LoopContext.loopNodeId`, `LoopNode.headerText`, `LoopStartNode` остаётся как @deprecated)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-14 + D-CL-04 (stub-выбор: опция (b) — transitionToError, soft)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/runner/protocol-runner.ts` (service, state machine)» — особенно параграфы «D-18 и `chooseLoopAction`», «D-04 getState assembly», «Surprise #2»
    - `CLAUDE.md` never-remove — `syncManualEdit`, `chooseSnippetBranch`, `pickSnippet`, `completeSnippet`, `stepBack`, `setGraph`, `restoreFrom`, `getSerializableState`, TextAccumulator usage, setGraph, options handling — НЕ трогать
  </read_first>

  <objective>
    Минимальная surgery: устранить все use-sites `loop-start`/`loop-end` полей, добавить unified-loop stub, сохранить метод `chooseLoopAction` (Surprise #2) как no-op error-trigger, чтобы .skip-тесты компилировались.
  </objective>

  <action>
  В `src/runner/protocol-runner.ts` внести СЛЕДУЮЩИЕ изменения:

  **1. `advanceThrough()` switch — заменить legacy cases на single unified stub.**

  Найти блок:
  ```ts
  case 'loop-start': {
    // ...push loopContextStack frame, follow 'continue' edge...
  }
  case 'loop-end': {
    // ...halt at loop-end...
  }
  ```

  Заменить НА:
  ```ts
  // Phase 43 D-14, D-CL-04 — unified loop runtime реализуется в Phase 44 (RUN-01..RUN-07).
  // Канвасы с loop-start/loop-end отвергаются GraphValidator'ом через migration-error (MIGRATE-01),
  // поэтому сюда legacy kinds не доходят. Unified 'loop' kind встречается только если канвас
  // прошёл validator — в Phase 43 это soft-error до имплементации runtime picker'а.
  case 'loop': {
    this.transitionToError(
      'Loop runtime ещё не реализован (запланировано в Phase 44 — см. ROADMAP v1.7).'
    );
    return;
  }
  ```

  Поскольку Plan 01 оставил `LoopStartNode` / `LoopEndNode` в `RPNode` union (D-CL-05 вариант b), TypeScript-exhaustiveness потребует закрыть эти ветки. Добавить отдельные legacy-stubs РЯДОМ с `case 'loop':`:

  ```ts
  case 'loop-start':
  case 'loop-end': {
    // Phase 43 D-14 — legacy kinds парсируются (D-06) для migration-error через GraphValidator.
    // В runtime не доходят: validator отвергает канвас до старта runner'а.
    this.transitionToError(
      'Обнаружен устаревший узел loop-start/loop-end. Канвас должен быть отклонён validator-ом; ' +
      'если вы видите это сообщение — это программная ошибка, сообщите автору плагина.'
    );
    return;
  }
  ```

  **2. `chooseLoopAction()` метод (строки ~291-347) — превратить в `@deprecated` stub.**

  Заменить тело метода на soft-error. НЕ удалять сигнатуру (Surprise #2: `.skip` тесты из D-18 всё равно компилируются и ссылаются на него через `runner.chooseLoopAction('again')`):

  ```ts
  /**
   * @deprecated Phase 43 D-14, D-18 — loop-end runtime удалён вместе с LoopEndNode;
   * unified loop runtime реализуется в Phase 44 (RUN-01..RUN-07).
   * Метод сохранён как stub, чтобы `.skip`-тесты (src/__tests__/runner/protocol-runner.test.ts,
   * protocol-runner-session.test.ts) продолжали TypeScript-компилироваться.
   * Не должен вызываться в runtime — если вызвали, переводим runner в error-state.
   */
  chooseLoopAction(action: 'again' | 'done'): void {
    void action;
    this.transitionToError(
      'chooseLoopAction устарел (Phase 43 D-18). Loop runtime реализуется в Phase 44.'
    );
  }
  ```

  **3. `getState()` `'at-node'` case (строки ~357-377) — убрать ссылки на удалённые поля.**

  Найти блок, где собирается `loopLabel` / `loopIterationLabel` / `isAtLoopEnd` с чтением `loopStartNode.loopLabel` / `topFrame.loopStartId` / `node.kind === 'loop-end'`:

  ```ts
  case 'at-node': {
    const topFrame = this.loopContextStack[this.loopContextStack.length - 1];
    const loopStartNode = topFrame !== undefined
      ? this.graph?.nodes.get(topFrame.loopStartId)        // ← loopStartId не существует
      : undefined;
    const loopLabel = loopStartNode?.kind === 'loop-start'
      ? loopStartNode.loopLabel                             // ← legacy
      : undefined;
    // ... isAtLoopEnd: ...kind === 'loop-end'
  }
  ```

  Заменить НА упрощённый вариант (Phase 44 перенапишет полноценный label):

  ```ts
  case 'at-node': {
    // Phase 43 D-14 — label assembly упрощён до undefined.
    // Phase 44 (RUN-01..RUN-07) реализует полный header + iteration counter поверх LoopNode.headerText.
    const loopIterationLabel: string | undefined = undefined;
    return {
      status: 'at-node',
      currentNodeId: this.currentNodeId ?? '',
      accumulatedText: this.accumulator.current,
      canStepBack: this.undoStack.length > 0,
      loopIterationLabel,
      isAtLoopEnd: undefined,
    };
  }
  ```

  (Точная форма зависит от текущей структуры метода — если там что-то ещё есть (spread, возврат ErrorState и т.п.), сохранять. Задача — убрать чтение `loopStartId` / `loopLabel` / `loop-end`.)

  **4. Любые остальные use-sites `.loopStartId`** — обновить на `.loopNodeId` (имя поля изменилось в Plan 01 для runtime `LoopContext`):

  ```bash
  # Перед правкой выполнить:
  grep -n "loopStartId" src/runner/protocol-runner.ts
  ```

  Каждое вхождение ДОЛЖНО быть одним из:
  - Deprecated access к `LoopStartNode.loopLabel` / `.loopStartId` → удаляется вместе с удалением legacy case'ов
  - Живое `frame.loopStartId` на `LoopContext` → заменить на `frame.loopNodeId`

  ВНИМАНИЕ: внутри `chooseLoopAction`-тела (которое теперь stub) никаких field access'ов НЕ должно остаться — метод теперь просто `transitionToError`. Исходное тело (с `loopStartNode.loopLabel` и т.п.) — удалить целиком.

  **5. `ProtocolRunnerOptions.maxIterations`** (строки ~8-9, ~44) — НЕ ТРОГАТЬ. Это общий cap auto-advance (используется в text-block chain), не loop-specific. См. тест `iteration cap (RUN-09)` в PATTERNS.md.

  **НЕ ТРОГАТЬ:**
  - Методы `syncManualEdit`, `chooseSnippetBranch`, `pickSnippet`, `completeSnippet`, `stepBack`, `setGraph`, `restoreFrom`, `getSerializableState`, `enterFreeText`, `chooseAnswer`, `start` — сохраняются
  - `TextAccumulator` usage — не модифицировать
  - `edgeByLabel` helper (строки ~657-662) — Phase 44 переиспользует, оставить
  - Любые не-loop-specific error-transitions и state-переходы
  </action>

  <acceptance_criteria>
    - `advanceThrough()` содержит `case 'loop':` с `transitionToError`:
      ```bash
      grep -Pzo "case 'loop': \{[^}]*transitionToError[^}]*\}" src/runner/protocol-runner.ts | grep -q "Phase 44"
      ```
    - `advanceThrough()` содержит объединённый legacy stub `case 'loop-start':` + `case 'loop-end':`:
      ```bash
      grep -q "case 'loop-start':" src/runner/protocol-runner.ts
      grep -q "case 'loop-end':" src/runner/protocol-runner.ts
      # оба case'а ведут в один блок с transitionToError (смежные cases без break):
      grep -B1 "case 'loop-end':" src/runner/protocol-runner.ts | grep -q "case 'loop-start':"
      ```
    - `chooseLoopAction` сохранён как stub-метод, тело — только transitionToError:
      ```bash
      grep -q "chooseLoopAction(action: 'again' | 'done'): void" src/runner/protocol-runner.ts
      grep -q "@deprecated Phase 43 D-14, D-18" src/runner/protocol-runner.ts
      ```
    - Legacy field-reads `loopStartId` / `loopLabel` исчезли из runtime (кроме поля `LegacyLoopStartNode.loopLabel`-сослались через kind-narrowing — но в новой версии tested в `chooseLoopAction` удалены целиком):
      ```bash
      ! grep "\.loopStartId" src/runner/protocol-runner.ts
      ! grep "loopStartNode\?\.loopLabel" src/runner/protocol-runner.ts
      ```
    - Phase-marker присутствует:
      ```bash
      grep -q "Phase 43 D-14" src/runner/protocol-runner.ts
      ```
    - Компиляция файла проходит (после Plan 01 + Task 1):
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "protocol-runner.ts")
      ```
    - Другие методы (syncManualEdit, chooseSnippetBranch, pickSnippet, completeSnippet, stepBack, setGraph) — не модифицированы:
      ```bash
      grep -q "syncManualEdit" src/runner/protocol-runner.ts
      grep -q "chooseSnippetBranch" src/runner/protocol-runner.ts
      grep -q "pickSnippet" src/runner/protocol-runner.ts
      grep -q "completeSnippet" src/runner/protocol-runner.ts
      grep -q "stepBack" src/runner/protocol-runner.ts
      grep -q "setGraph" src/runner/protocol-runner.ts
      grep -q "getSerializableState" src/runner/protocol-runner.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      ! grep "\.loopStartId" src/runner/protocol-runner.ts \
        && grep -q "case 'loop':" src/runner/protocol-runner.ts \
        && grep -q "chooseLoopAction(action: 'again' | 'done'): void" src/runner/protocol-runner.ts \
        && grep -q "@deprecated Phase 43 D-14, D-18" src/runner/protocol-runner.ts \
        && grep -q "Phase 44" src/runner/protocol-runner.ts \
        && grep -q "syncManualEdit" src/runner/protocol-runner.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "protocol-runner.ts")
    </automated>
  </verify>

  <done>
    - `advanceThrough` switch: новый `case 'loop':` (soft-error), merged-stub `case 'loop-start':` + `case 'loop-end':` (soft-error)
    - `chooseLoopAction` — `@deprecated` stub с telemetry-error
    - `getState()` 'at-node' branch не читает legacy поля
    - Все `.loopStartId` access заменены либо на `.loopNodeId`, либо удалены вместе с legacy chooseLoopAction body
    - Другие методы runner'а нетронуты
    - Файл компилируется
  </done>
</task>

<task type="auto">
  <name>Task 3: Удалить case 'loop-end': switch-arm из runner-view.ts — минимальная правка UI (Surprise #1)</name>

  <read_first>
    - `src/views/runner-view.ts` строки 395-460 (switch (node.kind) и окружение)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/views/runner-view.ts`» — особенно Surprise #1 и Notes
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-14 (подсказка что view правится минимально — CONTEXT-фраза «views не меняется» про error panel, не про discriminated switch)
    - `CLAUDE.md` never-remove — любые другие case'ы (`question`, `free-text-input`, `default`), chooseSnippetBranch, pickSnippet flow, error panel render, autoSaveSession — НЕ трогать
  </read_first>

  <objective>
    Закрыть TS-compile дыру в view: `case 'loop-end':` (строки 402-444) ссылается на `node.loopStartId` и `matchingStart.loopLabel` / `.exitLabel`, которые больше не существуют. TypeScript упадёт без удаления этого блока. Runner всё равно не доходит до этого UI (advanceThrough case 'loop' бросает error-state до render'а), поэтому UI-исчезновение — bit-perfect ожидаемое поведение на Phase 43.

    CSS классы `rp-loop-btn-row`, `rp-loop-again-btn`, `rp-loop-done-btn`, `rp-loop-iteration-label` в `src/styles/loop-support.css` — НЕ трогать (Surprise #5: Phase 45 вернёт picker UI, стили переиспользуются).
  </objective>

  <action>
  В `src/views/runner-view.ts` найти блок `case 'loop-end': {` (строки ~402-444) и удалить его целиком — от строки с `case 'loop-end': {` до закрывающей `break;` + `}` включительно.

  Добавить комментарий на место удаления (для читающего код):

  ```ts
  // Phase 43 D-14 — case 'loop-end' удалён вместе с LoopEndNode kind.
  // Unified 'loop' picker UI реализуется в Phase 44 (RUN-01). До тех пор
  // runtime просто transitionToError'ит на loop-узле (см. ProtocolRunner.advanceThrough).
  ```

  Логика после удаления: `switch (node.kind)` теперь покрывает `question`, `free-text-input`, а всё остальное (включая `'loop'`, `'start'`, `'answer'`, `'text-block'`, `'snippet'` в at-node state) пойдёт в `default:` clause (строки 446-454) с «Processing...» сообщением. Для 'loop' kind это безопасно — runner уже transitionToError'нул до render'а.

  **НЕ ТРОГАТЬ:**
  - `case 'question':` блок
  - `case 'free-text-input':` блок
  - `default:` блок (строки ~446-454)
  - `stepBackBtn` создание (строки ~457+)
  - Любые другие методы / хелперы в runner-view.ts: `chooseSnippetBranch`, `pickSnippet`, error panel render, `autoSaveSession`, `renderAsync`, `registerDomEvent` usage, lifecycle hooks
  - `src/styles/loop-support.css` — НЕ ТРОГАТЬ (ни одной правки; CSS классы переживают для Phase 45)
  </action>

  <acceptance_criteria>
    - Блок `case 'loop-end':` удалён:
      ```bash
      ! grep "case 'loop-end':" src/views/runner-view.ts
      ```
    - Ссылки на несуществующие поля удалены:
      ```bash
      ! grep "matchingStart\.loopLabel" src/views/runner-view.ts
      ! grep "matchingStart\.exitLabel" src/views/runner-view.ts
      ! grep "node\.loopStartId" src/views/runner-view.ts
      ! grep "chooseLoopAction" src/views/runner-view.ts
      ```
    - `case 'question':` и `case 'free-text-input':` сохранены:
      ```bash
      grep -q "case 'question':" src/views/runner-view.ts
      grep -q "case 'free-text-input':" src/views/runner-view.ts
      grep -q "default:" src/views/runner-view.ts
      ```
    - CSS в loop-support.css НЕ ТРОНУТ:
      ```bash
      # git diff на этот файл должен быть пустым
      git diff --quiet src/styles/loop-support.css
      ```
    - Phase-marker комментарий на месте:
      ```bash
      grep -q "Phase 43 D-14" src/views/runner-view.ts
      ```
    - Полный build проходит:
      ```bash
      npm run build
      # exit 0 ожидается — это финальная TS-починка, Plans 01/02/03 уже покрыли остальное
      ```
    - Существующие тесты компилируются (даже .skip-ed — `chooseLoopAction` сохранён как stub в Task 2):
      ```bash
      npx tsc --noEmit --skipLibCheck 2>&1 | tee /tmp/final-tsc.log
      # /tmp/final-tsc.log может иметь ошибки в test-файлах из-за inline loopStartId literals;
      # они будут починены в Plan 07. Source-файлы должны быть clean.
      SOURCE_ERRORS=$(grep -E "^src/(graph|runner|canvas|sessions|views|styles|utils|snippets)/" /tmp/final-tsc.log | wc -l)
      test "$SOURCE_ERRORS" -eq 0
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # Ключевые подтверждения
      ! grep "case 'loop-end':" src/views/runner-view.ts \
        && ! grep "node\.loopStartId" src/views/runner-view.ts \
        && ! grep "chooseLoopAction" src/views/runner-view.ts \
        && grep -q "case 'question':" src/views/runner-view.ts \
        && grep -q "Phase 43 D-14" src/views/runner-view.ts \
        && git diff --quiet src/styles/loop-support.css \
        && npm run build > /tmp/build.log 2>&1
    </automated>
  </verify>

  <done>
    - `case 'loop-end':` блок удалён целиком
    - Все ссылки на `.loopStartId`, `.loopLabel`, `.exitLabel`, `chooseLoopAction` в runner-view.ts удалены
    - `case 'question':`, `case 'free-text-input':`, `default:` сохранены
    - `src/styles/loop-support.css` НЕ ТРОНУТ
    - `npm run build` — exit 0 (весь проект компилируется с новой моделью)
    - Source-файлы (не test-файлы) — TS-compile clean
  </done>
</task>

</tasks>

<verification>
  - После всех трёх задач выполнить `npm run build` → exit 0 (основная проверка wave 2)
  - `npx tsc --noEmit --skipLibCheck` — source-файлы (`src/**` кроме `__tests__`) чисты; test-файлы могут падать из-за inline literals (Plan 07 починит)
  - grep проверки из acceptance-criteria проходят
  - Mental walkthrough: загрузить канвас с loop-node (валидный) → parser строит `LoopNode` → runner вошёл в advanceThrough с `node.kind === 'loop'` → соответствие case → `transitionToError` → RunnerView рендерит error panel с корректным сообщением «Loop runtime ещё не реализован...»
</verification>

<success_criteria>
  - `! grep "case 'loop-end':" src/views/runner-view.ts` → exit 0
  - `grep -q "case 'loop':" src/runner/protocol-runner.ts` → exit 0
  - `grep -q "@deprecated Phase 43 D-14, D-18" src/runner/protocol-runner.ts` → exit 0
  - `grep -B2 "isAtLoopEnd?: boolean" src/runner/runner-state.ts | grep -q "@deprecated"` → exit 0
  - `git diff --quiet src/styles/loop-support.css` → exit 0
  - `npm run build` → exit 0
  - Commit содержит ровно три файла: `src/runner/runner-state.ts`, `src/runner/protocol-runner.ts`, `src/views/runner-view.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-04-SUMMARY.md`
</output>
