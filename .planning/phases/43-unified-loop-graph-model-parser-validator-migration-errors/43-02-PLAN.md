---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/graph/canvas-parser.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
user_setup: []

must_haves:
  truths:
    - "Канвас с radiprotocol_nodeType='loop' парсится в LoopNode с headerText заполненным из radiprotocol_headerText"
    - "Канвас с radiprotocol_nodeType='loop-start' / 'loop-end' продолжает парситься (legacy parseable) — строит LoopStartNode/LoopEndNode узлы, которые validator затем отвергнет через migration-error"
    - "Неизвестный radiprotocol_nodeType даёт parseError (не silent ignore)"
    - "radiprotocol_headerText отсутствующий/undefined нормализуется в пустую строку"
  artifacts:
    - path: "src/graph/canvas-parser.ts"
      provides: "case 'loop' в parseNode() switch + 'loop' в validKinds"
      contains: "case 'loop':"
  key_links:
    - from: "src/graph/canvas-parser.ts"
      to: "src/graph/graph-model.ts"
      via: "import LoopNode"
      pattern: "import .*LoopNode.*from '\\./graph-model'"
    - from: "src/graph/canvas-parser.ts"
      to: "src/graph/graph-validator.ts"
      via: "ProtocolGraph с LoopNode / LegacyLoopStartNode в nodes Map"
      pattern: "graph\\.nodes\\.get.*kind === 'loop'"
---

<objective>
Научить canvas-parser распознавать `radiprotocol_nodeType = "loop"` и строить `LoopNode` c `headerText` из `radiprotocol_headerText` (LOOP-03). Одновременно СОХРАНИТЬ существующие cases `'loop-start'` / `'loop-end'` как parseable (D-06), чтобы validator мог давать точную migration-error, а не parseError.

Purpose: Parser — единственный мост между JSON-канвасом и типизированной моделью. Без case 'loop' новый формат канваса не доходит до validator'а; без сохранения legacy case'ов пользователь получает менее понятный parseError вместо migration-инструкции.

Output: Обновлённый `src/graph/canvas-parser.ts` с добавленным `'loop'` в `validKinds` list и новым `case 'loop':` в `parseNode()` switch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@src/graph/graph-model.ts

<interfaces>
```ts
// Pattern-analog: TextBlockNode parser case (canvas-parser.ts:225-238)
case 'text-block': {
  const node: TextBlockNode = {
    ...base,
    kind: 'text-block',
    content: getString(props, 'radiprotocol_content', raw.text ?? ''),
    // ... остальные поля
  };
  return node;
}

// Closer analog by shape: QuestionNode parser case (canvas-parser.ts:186-193)
case 'question': {
  const node: QuestionNode = {
    ...base,
    kind: 'question',
    questionText: getString(props, 'radiprotocol_questionText', raw.text ?? ''),
  };
  return node;
}

// Helper signature:
// getString(props: Record<string, unknown>, key: string, fallback?: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить 'loop' в validKinds и case 'loop' в parseNode switch</name>

  <read_first>
    - `src/graph/canvas-parser.ts` (весь файл — особенно строки 150-280: parseNode switch)
    - `src/graph/graph-model.ts` (после правок Plan 01 — убедиться что LoopNode импортируется)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-05 (парсер-поведение для `loop`), D-06 (сохранение legacy cases)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/graph/canvas-parser.ts` (parser, transform)»
    - `CLAUDE.md` правило never-remove — `case 'snippet'` (строки 261-280), legacy `case 'loop-start'` (239-248), legacy `case 'loop-end'` (249-260) НЕ удалять
  </read_first>

  <objective>
    После этой задачи canvas-parser строит `LoopNode` для `radiprotocol_nodeType = "loop"` и продолжает парсить legacy loop-start/loop-end как раньше. Базис для всех downstream проверок: validator (Plan 05) работает на parsed graph, fixtures (Plan 06) рассчитывают на этот парсинг.
  </objective>

  <action>
  Последовательно внести СЛЕДУЮЩИЕ изменения в `src/graph/canvas-parser.ts`.

  **1. Import block (вверху файла, строки 4-18)** — добавить `LoopNode` в импорт из `./graph-model`. Не удалять существующие импорты (`LoopStartNode`, `LoopEndNode` остаются — legacy cases всё ещё строят эти типы; D-CL-05 вариант (b)).

  Ожидаемый итог — в списке импортируемых из `./graph-model` типов должен появиться `LoopNode` рядом с `LoopStartNode` / `LoopEndNode`:

  ```ts
  import type {
    // ...существующие типы...
    LoopStartNode,
    LoopEndNode,
    LoopNode,           // Phase 43 D-05 — unified loop kind (LOOP-01, LOOP-02)
    // ...
  } from './graph-model';
  ```

  (Конкретная форма зависит от текущего стиля импорта — сохранить его. Если `import type { ... }` многострочный — добавить `LoopNode` в алфавитном порядке или в конец. Если один-в-строку — добавить рядом с LoopStartNode.)

  **2. validKinds array (строки 159-162)** — добавить `'loop'` как новый элемент. Порядок — в конце списка перед закрывающей скобкой (зеркалит pattern Phase 29, D-11 для `'snippet'`):

  ```ts
  const validKinds: RPNodeKind[] = [
    'start', 'question', 'answer', 'free-text-input',
    'text-block', 'loop-start', 'loop-end', 'snippet',  // Phase 29
    'loop',  // Phase 43 D-05 — unified loop (LOOP-01, LOOP-02)
  ];
  ```

  **3. Новый `case 'loop':` в parseNode() switch** — добавить ПОСЛЕ `case 'snippet':` блока (после строки 280, перед закрывающей `}` switch'а). Shape зеркалит `QuestionNode` — один string payload, всегда defined, нормализация через `getString` c fallback `''`:

  ```ts
  case 'loop': {
    // Phase 43 D-05 — unified loop node (LOOP-01, LOOP-02, LOOP-03).
    // headerText нормализуется в '' если radiprotocol_headerText отсутствует/undefined
    // (симметрия с TextBlockNode.content, НЕ SnippetNode.subfolderPath?: string | undefined).
    const node: LoopNode = {
      ...base,
      kind: 'loop',
      headerText: getString(props, 'radiprotocol_headerText', ''),
    };
    return node;
  }
  ```

  **Примечание по fallback:** использовать `getString(props, 'radiprotocol_headerText', '')` — БЕЗ `raw.text ?? ''` (в отличие от `QuestionNode.questionText` который читает `raw.text` если `radiprotocol_questionText` отсутствует). Причина: `headerText` не имеет fallback-семантики на нативный canvas-text; пустой header — легитимное состояние узла. Если в будущем понадобится fallback — можно добавить в Phase 45 (Node Editor form).

  **4. Case 'loop-start' (строки 239-248) и 'loop-end' (строки 249-260)** — НЕ удалять. Оставить как есть (D-06 требует legacy parseable). Никаких изменений в их телах — `LoopStartNode` и `LoopEndNode` interface'ы всё ещё существуют (Plan 01 их сохранил с `@deprecated`), парсер по-прежнему может их сконструировать.

  **НЕ ТРОГАТЬ:**
  - Case 'start' / 'question' / 'answer' / 'free-text-input' / 'text-block' / 'snippet' (строки 181-280) — не модифицировать
  - Legacy `case 'loop-start'` (239-248), `case 'loop-end'` (249-260) — оставить как есть
  - `parseEdges()`, `buildAdjacency()`, метод `parse()` в целом — не трогать
  - Helper функции `getString`, `getNumber` — не трогать
  - Импорты, не связанные с `LoopNode` — оставить
  </action>

  <acceptance_criteria>
    - `LoopNode` импортирован из `./graph-model`:
      ```bash
      grep -Pzo "import type \{[^}]*LoopNode[^}]*\} from '\./graph-model'" src/graph/canvas-parser.ts | grep -q "LoopNode"
      ```
    - `validKinds` содержит `'loop'`:
      ```bash
      grep -q "'loop'" src/graph/canvas-parser.ts
      grep -q "Phase 43 D-05" src/graph/canvas-parser.ts   # phase-marker
      ```
    - `case 'loop':` добавлен и создаёт `LoopNode` с `headerText`:
      ```bash
      grep -q "case 'loop':" src/graph/canvas-parser.ts
      grep -q "kind: 'loop'," src/graph/canvas-parser.ts
      grep -q "headerText: getString(props, 'radiprotocol_headerText'" src/graph/canvas-parser.ts
      ```
    - Legacy cases `'loop-start'` и `'loop-end'` ещё присутствуют:
      ```bash
      grep -q "case 'loop-start':" src/graph/canvas-parser.ts
      grep -q "case 'loop-end':" src/graph/canvas-parser.ts
      ```
    - Файл компилируется (ожидаемо — `LoopNode` теперь существует в модели после Plan 01):
      ```bash
      npx tsc --noEmit --skipLibCheck 2>&1 | grep "canvas-parser.ts" | wc -l     # ожидается 0
      ```
    - Unit-тест (после Plan 07) должен построить `LoopNode` из canvas-fixture (проверяется в Plan 07). В этом plan'е достаточно compile-green.
    - `case 'snippet'` тело нетронуто (CLAUDE.md never-remove):
      ```bash
      grep -q "Phase 31 D-01" src/graph/canvas-parser.ts
      grep -q "Phase 31 D-04" src/graph/canvas-parser.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # Parser компилируется без ошибок
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "canvas-parser.ts")
      # Ключевые правки на месте
      grep -q "case 'loop':" src/graph/canvas-parser.ts \
        && grep -q "kind: 'loop'," src/graph/canvas-parser.ts \
        && grep -q "headerText: getString(props, 'radiprotocol_headerText'" src/graph/canvas-parser.ts \
        && grep -q "case 'loop-start':" src/graph/canvas-parser.ts \
        && grep -q "case 'loop-end':" src/graph/canvas-parser.ts \
        && grep -q "Phase 43 D-05" src/graph/canvas-parser.ts
    </automated>
  </verify>

  <done>
    - `validKinds` включает `'loop'`
    - `case 'loop':` строит `LoopNode` с `headerText: string` (fallback `''`)
    - Legacy `case 'loop-start':` / `case 'loop-end':` неизменны
    - `canvas-parser.ts` компилируется без ошибок (Plan 01 foundation уже на месте)
    - Phase-marker `Phase 43 D-05` присутствует
  </done>
</task>

</tasks>

<verification>
  - grep-паттерны из acceptance-criteria проходят
  - Прочитать итоговый `canvas-parser.ts`: убедиться что `case 'loop':` возвращает `LoopNode` (не `LoopStartNode`/`LoopEndNode`), fallback у `getString` именно `''` (а не `raw.text ?? ''`)
  - Убедиться что никакой из существующих cases (start, question, answer, free-text-input, text-block, loop-start, loop-end, snippet) не был модифицирован
</verification>

<success_criteria>
  - `grep -q "case 'loop':" src/graph/canvas-parser.ts` → exit 0
  - `grep -q "headerText: getString(props, 'radiprotocol_headerText', '')" src/graph/canvas-parser.ts` → exit 0
  - `grep -q "'loop'" src/graph/canvas-parser.ts` (в validKinds) → exit 0
  - Legacy case 'loop-start' / 'loop-end' присутствуют
  - `canvas-parser.ts` компилируется без ошибок
  - Commit содержит ТОЛЬКО изменения в `src/graph/canvas-parser.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-02-SUMMARY.md`
</output>
