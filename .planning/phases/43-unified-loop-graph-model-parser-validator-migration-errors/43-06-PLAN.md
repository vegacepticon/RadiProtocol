---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 06
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/__tests__/fixtures/unified-loop-valid.canvas
  - src/__tests__/fixtures/unified-loop-missing-exit.canvas
  - src/__tests__/fixtures/unified-loop-duplicate-exit.canvas
  - src/__tests__/fixtures/unified-loop-no-body.canvas
autonomous: true
requirements:
  - LOOP-03
  - LOOP-04
user_setup: []

must_haves:
  truths:
    - "fixture unified-loop-valid.canvas содержит ровно один loop-узел с radiprotocol_headerText, одним ребром label=«выход» и ≥1 body-ребром"
    - "fixture unified-loop-missing-exit.canvas содержит loop-узел без ребра «выход» (только body-рёбра)"
    - "fixture unified-loop-duplicate-exit.canvas содержит loop-узел с 2+ рёбрами label=«выход»"
    - "fixture unified-loop-no-body.canvas содержит loop-узел только с ребром «выход», без body"
    - "все fixtures валидный JSON (jq parse) + содержат один start-узел (иначе validator валится на Check 1 до LOOP-04)"
    - "все fixtures используют кириллическое «выход» — не 'exit' / 'Exit' / 'выход ' (с trailing space)"
    - "существующие legacy fixtures loop-start.canvas и loop-body.canvas НЕ тронуты (D-16)"
  artifacts:
    - path: "src/__tests__/fixtures/unified-loop-valid.canvas"
      provides: "Happy-path unified loop fixture для LOOP-03 + LOOP-04 passing"
    - path: "src/__tests__/fixtures/unified-loop-missing-exit.canvas"
      provides: "Fixture для D-08.1 (missing «выход» edge)"
    - path: "src/__tests__/fixtures/unified-loop-duplicate-exit.canvas"
      provides: "Fixture для D-08.2 (duplicate «выход» edges)"
    - path: "src/__tests__/fixtures/unified-loop-no-body.canvas"
      provides: "Fixture для D-08.3 (no body branch)"
  key_links:
    - from: "src/__tests__/fixtures/*.canvas"
      to: "src/__tests__/graph-validator.test.ts"
      via: "parseFixture() helper"
      pattern: "parseFixture\\('unified-loop-"
---

<objective>
Создать четыре новых fixture-канваса для LOOP-03 (parsing) и LOOP-04 (validator sub-checks). Legacy fixtures `loop-start.canvas` / `loop-body.canvas` оставляются как есть (D-16) — Plan 07 добавит migration-error тесты против них.

Purpose: Unit-тесты в Plan 07 (D-19) — fixture-based (pattern `parseFixture('linear.canvas')`). Без fixtures тесты не могут проверить реальный parser+validator end-to-end для unified-loop канвасов.

Output: 4 новых `.canvas` файла с валидным JSON, корректной структурой для проверки каждого sub-check'а D-08.1/2/3 и happy-path LOOP-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@src/__tests__/fixtures/linear.canvas
@src/__tests__/fixtures/branching.canvas
@src/__tests__/fixtures/loop-body.canvas

<interfaces>
```json
// Canvas JSON shape reference (linear.canvas pattern):
{
  "nodes": [
    {
      "id": "n-start",
      "type": "text",
      "text": "Start",
      "x": 0, "y": 0, "width": 200, "height": 60,
      "radiprotocol_nodeType": "start"
    },
    {
      "id": "n-loop",
      "type": "text",
      "text": "Lesion loop",
      "x": 0, "y": 120, "width": 200, "height": 60,
      "radiprotocol_nodeType": "loop",
      "radiprotocol_headerText": "Lesion loop"
    }
    // ...
  ],
  "edges": [
    { "id": "e1", "fromNode": "n-start", "toNode": "n-loop" },
    { "id": "e2", "fromNode": "n-loop", "toNode": "n-q1", "label": "проверка" }
  ]
}
```

Requirements:
- `"выход"` — literal Cyrillic, six chars (U+0432 U+044B U+0445 U+043E U+0434 — wait: в-ы-х-о-д = 5 chars). Дословно: в, ы, х, о, д.
- Валидный JSON (двойные кавычки, запятые)
- UTF-8 кодировка файла
- Минимум один `start` узел в каждом fixture (иначе Check 1 в validator падает до LOOP-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать все 4 unified-loop fixture-файла</name>

  <read_first>
    - `src/__tests__/fixtures/linear.canvas` (reference — минимальный happy-path)
    - `src/__tests__/fixtures/branching.canvas` (reference — несколько outgoing edges + labels)
    - `src/__tests__/fixtures/loop-body.canvas` (reference — legacy loop-start/body/loop-end shape для ознакомления)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-17 (fixture requirements)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «Fixtures (new)» — pattern-references и D-17 структура
  </read_first>

  <objective>
    4 fixture-файла, каждый — минимально-достаточная канвас-структура для проверки ровно одного аспекта LOOP-04 / LOOP-03. Все fixtures должны пройти parser (radiprotocol_nodeType = "loop" → LoopNode), причём первый должен пройти validator (0 errors), остальные три — упасть с конкретными LOOP-04 ошибками.
  </objective>

  <action>

  **Важно про «выход»:** литерал `"выход"` внутри JSON string'а — это просто 5 кириллических символов между двойными кавычками. UTF-8 encoding файла ДОЛЖНО быть сохранено при создании (редактор может предложить BOM — его ставить не надо).

  **Fixture 1: `src/__tests__/fixtures/unified-loop-valid.canvas` (happy-path; LOOP-03 + LOOP-04 passes)**

  Структура:
  - `n-start` → `n-loop`
  - `n-loop` → `n-q1` (label: `"проверка"` — body branch), и `n-loop` → `n-end` (label: `"выход"`)
  - `n-q1` → `n-a1` (body walks one step; cycle-through-loop через back-edge)
  - `n-a1` → `n-loop` (замыкает цикл через loop node — detectUnintentionalCycles теперь не флагает (D-09))
  - `n-end` — terminal text-block

  Содержимое файла:

  ```json
  {
    "nodes": [
      { "id": "n-start", "type": "text", "text": "Start",       "x": 0,   "y": 0,   "width": 200, "height": 60, "radiprotocol_nodeType": "start" },
      { "id": "n-loop",  "type": "text", "text": "Lesion loop", "x": 0,   "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "loop",      "radiprotocol_headerText": "Lesion loop" },
      { "id": "n-q1",    "type": "text", "text": "Size?",       "x": 260, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "question",  "radiprotocol_questionText": "Size?" },
      { "id": "n-a1",    "type": "text", "text": "1 cm",        "x": 520, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "answer",    "radiprotocol_answerText": "1 cm" },
      { "id": "n-end",   "type": "text", "text": "Done",        "x": 0,   "y": 240, "width": 200, "height": 60, "radiprotocol_nodeType": "text-block","radiprotocol_content": "Done" }
    ],
    "edges": [
      { "id": "e1", "fromNode": "n-start", "toNode": "n-loop" },
      { "id": "e2", "fromNode": "n-loop",  "toNode": "n-q1",   "label": "проверка" },
      { "id": "e3", "fromNode": "n-loop",  "toNode": "n-end",  "label": "выход" },
      { "id": "e4", "fromNode": "n-q1",    "toNode": "n-a1" },
      { "id": "e5", "fromNode": "n-a1",    "toNode": "n-loop" }
    ]
  }
  ```

  **Fixture 2: `src/__tests__/fixtures/unified-loop-missing-exit.canvas` (D-08.1)**

  loop-узел с одним body-ребром, БЕЗ ребра `"выход"` → validator должен вернуть ошибку «не имеет ребра «выход»».

  ```json
  {
    "nodes": [
      { "id": "n-start", "type": "text", "text": "Start",       "x": 0,   "y": 0,   "width": 200, "height": 60, "radiprotocol_nodeType": "start" },
      { "id": "n-loop",  "type": "text", "text": "Lesion loop", "x": 0,   "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "loop",      "radiprotocol_headerText": "Lesion loop" },
      { "id": "n-q1",    "type": "text", "text": "Size?",       "x": 260, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "question",  "radiprotocol_questionText": "Size?" },
      { "id": "n-a1",    "type": "text", "text": "1 cm",        "x": 520, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "answer",    "radiprotocol_answerText": "1 cm" }
    ],
    "edges": [
      { "id": "e1", "fromNode": "n-start", "toNode": "n-loop" },
      { "id": "e2", "fromNode": "n-loop",  "toNode": "n-q1",   "label": "проверка" },
      { "id": "e3", "fromNode": "n-q1",    "toNode": "n-a1" },
      { "id": "e4", "fromNode": "n-a1",    "toNode": "n-loop" }
    ]
  }
  ```

  **Fixture 3: `src/__tests__/fixtures/unified-loop-duplicate-exit.canvas` (D-08.2)**

  loop-узел с ДВУМЯ рёбрами `label: "выход"` и минимум одним body-ребром (чтобы D-08.3 не срабатывал — мы таргетируем именно D-08.2).

  ```json
  {
    "nodes": [
      { "id": "n-start", "type": "text", "text": "Start",       "x": 0,   "y": 0,   "width": 200, "height": 60, "radiprotocol_nodeType": "start" },
      { "id": "n-loop",  "type": "text", "text": "Lesion loop", "x": 0,   "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "loop",      "radiprotocol_headerText": "Lesion loop" },
      { "id": "n-q1",    "type": "text", "text": "Size?",       "x": 260, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "question",  "radiprotocol_questionText": "Size?" },
      { "id": "n-a1",    "type": "text", "text": "1 cm",        "x": 520, "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "answer",    "radiprotocol_answerText": "1 cm" },
      { "id": "n-end1",  "type": "text", "text": "Exit 1",      "x": 0,   "y": 240, "width": 200, "height": 60, "radiprotocol_nodeType": "text-block","radiprotocol_content": "Exit 1" },
      { "id": "n-end2",  "type": "text", "text": "Exit 2",      "x": 260, "y": 240, "width": 200, "height": 60, "radiprotocol_nodeType": "text-block","radiprotocol_content": "Exit 2" }
    ],
    "edges": [
      { "id": "e1", "fromNode": "n-start", "toNode": "n-loop" },
      { "id": "e2", "fromNode": "n-loop",  "toNode": "n-q1",   "label": "проверка" },
      { "id": "e3", "fromNode": "n-loop",  "toNode": "n-end1", "label": "выход" },
      { "id": "e4", "fromNode": "n-loop",  "toNode": "n-end2", "label": "выход" },
      { "id": "e5", "fromNode": "n-q1",    "toNode": "n-a1" },
      { "id": "e6", "fromNode": "n-a1",    "toNode": "n-loop" }
    ]
  }
  ```

  **Fixture 4: `src/__tests__/fixtures/unified-loop-no-body.canvas` (D-08.3)**

  loop-узел с одним ребром `label: "выход"` и БЕЗ body-ветвей → validator должен вернуть ошибку «не имеет ни одной body-ветви». Обратите внимание: канвас валидный по остальным проверкам (reachability — все узлы достижимы; cycles — нет циклов).

  ```json
  {
    "nodes": [
      { "id": "n-start", "type": "text", "text": "Start",       "x": 0,   "y": 0,   "width": 200, "height": 60, "radiprotocol_nodeType": "start" },
      { "id": "n-loop",  "type": "text", "text": "Lesion loop", "x": 0,   "y": 120, "width": 200, "height": 60, "radiprotocol_nodeType": "loop",      "radiprotocol_headerText": "Lesion loop" },
      { "id": "n-end",   "type": "text", "text": "Done",        "x": 0,   "y": 240, "width": 200, "height": 60, "radiprotocol_nodeType": "text-block","radiprotocol_content": "Done" }
    ],
    "edges": [
      { "id": "e1", "fromNode": "n-start", "toNode": "n-loop" },
      { "id": "e2", "fromNode": "n-loop",  "toNode": "n-end",  "label": "выход" }
    ]
  }
  ```

  **ВАЖНО:** использовать Write tool (не heredoc/cat) для создания каждого файла, чтобы сохранить корректное UTF-8 кодирование без BOM.

  **НЕ ТРОГАТЬ:**
  - `src/__tests__/fixtures/loop-start.canvas` — D-16 сохраняет как migration-test fixture
  - `src/__tests__/fixtures/loop-body.canvas` — D-16 сохраняет как migration-test fixture
  - `linear.canvas`, `branching.canvas`, `cycle.canvas`, `dead-end.canvas`, `free-text.canvas`, `text-block.canvas`, `snippet-*.canvas`, `two-questions.canvas` — не модифицировать
  </action>

  <acceptance_criteria>
    - Все 4 файла существуют:
      ```bash
      test -f src/__tests__/fixtures/unified-loop-valid.canvas
      test -f src/__tests__/fixtures/unified-loop-missing-exit.canvas
      test -f src/__tests__/fixtures/unified-loop-duplicate-exit.canvas
      test -f src/__tests__/fixtures/unified-loop-no-body.canvas
      ```
    - Все 4 файла — валидный JSON (через `node -e` parse):
      ```bash
      for f in unified-loop-valid unified-loop-missing-exit unified-loop-duplicate-exit unified-loop-no-body; do
        node -e "JSON.parse(require('fs').readFileSync('src/__tests__/fixtures/${f}.canvas','utf8'))" || exit 1
      done
      ```
    - Каждый содержит ровно один `radiprotocol_nodeType: "start"` и ровно один `radiprotocol_nodeType: "loop"`:
      ```bash
      for f in src/__tests__/fixtures/unified-loop-*.canvas; do
        starts=$(grep -c '"radiprotocol_nodeType": "start"' "$f")
        loops=$(grep -c '"radiprotocol_nodeType": "loop"' "$f")
        test "$starts" = "1" && test "$loops" = "1" || { echo "FAIL: $f (starts=$starts loops=$loops)"; exit 1; }
      done
      ```
    - `radiprotocol_headerText` присутствует в каждом loop-узле:
      ```bash
      for f in src/__tests__/fixtures/unified-loop-*.canvas; do
        grep -q "radiprotocol_headerText" "$f" || { echo "FAIL: $f missing headerText"; exit 1; }
      done
      ```
    - Fixture 1 (`unified-loop-valid`) содержит ровно одно ребро с label «выход» и минимум одно без:
      ```bash
      count_exit=$(grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-valid.canvas)
      test "$count_exit" = "1"
      grep -q '"label": "проверка"' src/__tests__/fixtures/unified-loop-valid.canvas
      ```
    - Fixture 2 (`unified-loop-missing-exit`) НЕ содержит «выход»:
      ```bash
      ! grep '"label": "выход"' src/__tests__/fixtures/unified-loop-missing-exit.canvas
      ```
    - Fixture 3 (`unified-loop-duplicate-exit`) содержит ≥2 «выход»:
      ```bash
      count=$(grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-duplicate-exit.canvas)
      test "$count" -ge 2
      ```
    - Fixture 4 (`unified-loop-no-body`) содержит ровно одно «выход» и НЕ содержит других labels:
      ```bash
      count=$(grep -c '"label":' src/__tests__/fixtures/unified-loop-no-body.canvas)
      test "$count" = "1"
      grep -q '"label": "выход"' src/__tests__/fixtures/unified-loop-no-body.canvas
      ```
    - Legacy fixtures нетронуты:
      ```bash
      git diff --quiet src/__tests__/fixtures/loop-start.canvas
      git diff --quiet src/__tests__/fixtures/loop-body.canvas
      ```
    - UTF-8 validity (не ASCII-fallback, не CP1251):
      ```bash
      file --brief --mime-encoding src/__tests__/fixtures/unified-loop-valid.canvas | grep -qE "utf-8|ascii"
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # 1. Все 4 файла существуют и парсятся как JSON
      for f in unified-loop-valid unified-loop-missing-exit unified-loop-duplicate-exit unified-loop-no-body; do
        node -e "JSON.parse(require('fs').readFileSync('src/__tests__/fixtures/${f}.canvas','utf8'))" || exit 1
      done

      # 2. Структурные проверки
      test $(grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-valid.canvas) = "1" \
        && ! grep '"label": "выход"' src/__tests__/fixtures/unified-loop-missing-exit.canvas \
        && test $(grep -c '"label": "выход"' src/__tests__/fixtures/unified-loop-duplicate-exit.canvas) -ge 2 \
        && test $(grep -c '"label":' src/__tests__/fixtures/unified-loop-no-body.canvas) = "1" \
        && grep -q '"label": "выход"' src/__tests__/fixtures/unified-loop-no-body.canvas

      # 3. Legacy fixtures нетронуты
      git diff --quiet src/__tests__/fixtures/loop-start.canvas \
        && git diff --quiet src/__tests__/fixtures/loop-body.canvas
    </automated>
  </verify>

  <done>
    - 4 новых fixture-файла созданы
    - Каждый — валидный JSON
    - Каждый имеет один start-узел и один loop-узел с headerText
    - Структурно соответствует своему назначению (valid / missing-exit / duplicate-exit / no-body)
    - Кириллическое «выход» корректно закодировано
    - Legacy loop-start.canvas / loop-body.canvas нетронуты
  </done>
</task>

</tasks>

<verification>
  - Читатель фиксирует что каждый fixture отражает ровно один D-08 sub-check (для негативных) или D-08.1/2/3 passing (для valid)
  - Канвасы можно условно проверить через реальный parser (это сделает Plan 07), но в этом плане достаточно JSON-parse + grep-структуры
</verification>

<success_criteria>
  - Все 4 файла существуют и парсятся как JSON
  - Каждый fixture прошёл structural grep-check'и
  - Legacy fixtures нетронуты
  - Commit содержит ровно 4 новых файла (нет изменений существующих)
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-06-SUMMARY.md`
</output>
