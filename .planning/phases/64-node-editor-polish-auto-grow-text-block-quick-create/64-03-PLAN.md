---
phase: 64-node-editor-polish-auto-grow-text-block-quick-create
plan: 03
type: execute
wave: 3
depends_on:
  - 64-01
  - 64-02
files_modified:
  - src/views/editor-panel-view.ts
  - src/styles/editor-panel.css
  - src/__tests__/editor-panel-create.test.ts
  - styles.css
autonomous: false
requirements:
  - EDITOR-06
must_haves:
  truths:
    - "The Node Editor bottom toolbar includes a fifth create button labelled Create text block."
    - "Clicking Create text block creates a text-block node through CanvasNodeFactory and immediately renders the Text-block form from live node data."
    - "All create controls and Duplicate remain reachable in the bottom vertical toolbar at narrow sidebar widths."
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "Text-block quick-create union widening and toolbar button"
      contains: "Create text block"
    - path: "src/styles/editor-panel.css"
      provides: "Phase 64 append-only toolbar styling if needed"
      contains: "Phase 64"
    - path: "src/__tests__/editor-panel-create.test.ts"
      provides: "EDITOR-06 quick-create regression tests"
      contains: "text-block"
    - path: "styles.css"
      provides: "Generated CSS output rebuilt by npm run build"
      contains: "Phase 64"
  key_links:
    - from: "src/views/editor-panel-view.ts"
      to: "src/canvas/canvas-node-factory.ts"
      via: "this.plugin.canvasNodeFactory.createNode(canvasPath, kind, anchor)"
      pattern: "createNode\(\s*canvasPath,\s*kind"
    - from: "src/canvas/canvas-node-factory.ts"
      to: "src/canvas/node-color-map.ts"
      via: "NODE_COLOR_MAP[nodeKind]"
      pattern: "NODE_COLOR_MAP\[nodeKind\]"
---

<objective>
Add the Text block quick-create button and complete Phase 64 verification.

Purpose: Deliver EDITOR-06 through the existing creation path locked by D3 while preserving the Phase 48 bottom vertical toolbar locked by D4.
Output: Text-block quick-create UI, green targeted tests, green build, and a human visual/functional verification gate.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-CONTEXT.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-UI-SPEC.md
@src/views/editor-panel-view.ts
@src/canvas/canvas-node-factory.ts
@src/canvas/node-color-map.ts
@src/styles/editor-panel.css
@src/__tests__/editor-panel-create.test.ts

<interfaces>
Existing factory contract:
```typescript
createNode(canvasPath: string, nodeKind: RPNodeKind, anchorNodeId?: string): CreateNodeResult | null;
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = { 'text-block': '3', ... };
```

Existing quick-create render pattern:
```typescript
const btn = toolbar.createEl('button', { cls: 'rp-create-...-btn' });
btn.setAttribute('aria-label', 'Create ... node');
btn.setAttribute('title', 'Create ... node');
setIcon(btn.createSpan(), '...');
btn.appendText('Create ... node');
this.registerDomEvent(btn, 'click', () => { void this.onQuickCreate('...'); });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire text-block into quick-create union and toolbar</name>
  <files>src/views/editor-panel-view.ts, src/__tests__/editor-panel-create.test.ts</files>
  <behavior>
    - `onQuickCreate` accepts `'text-block'` in the same union as the existing create kinds per D3.
    - Toolbar renders a full-width peer button labelled `Create text block` with `aria-label` and `title` equal to `Create text block` per UI-SPEC.
    - The button uses a passive text/content icon such as `file-text` and calls `onQuickCreate('text-block')`.
    - Successful creation renders the Text-block form from `result.canvasNode.getData()` and sets `currentNodeId/currentFilePath` like other kinds.
  </behavior>
  <action>Modify only the quick-create-related code in `EditorPanelView`. Widen `onQuickCreate` to include `'text-block'`; do not change `CanvasNodeFactory` because it already accepts `RPNodeKind` and `NODE_COLOR_MAP['text-block']` exists. Add the new button to the existing bottom toolbar after Loop and before Duplicate, keeping Duplicate a separate utility button per UI-SPEC. Use `setIcon` and `registerDomEvent` like peer buttons. Make the Plan 01 create tests pass.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-create.test.ts</automated>
  </verify>
  <done>Targeted quick-create tests pass and prove the factory is called with `'text-block'` through the existing path.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve bottom vertical toolbar styling and run full automated verification</name>
  <files>src/styles/editor-panel.css, styles.css, src/__tests__/editor-panel-create.test.ts</files>
  <action>If the new text-block button needs CSS, append a `/* Phase 64: text block quick-create button */` section at the bottom of `src/styles/editor-panel.css`; otherwise leave CSS source unchanged except for any test-required Phase 64 assertion already satisfied by Plan 02. Ensure all toolbar buttons remain `width: 100%`, centered, and vertically stacked per D4/UI-SPEC. Run targeted tests for both Phase 64 files, then run the full suite and build. Do not hand-edit `styles.css`; regenerate it with `npm run build` if CSS changed.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-forms.test.ts src/__tests__/editor-panel-create.test.ts; if ($?) { npm test }; if ($?) { npm run build }</automated>
  </verify>
  <done>All automated tests and production build pass; generated CSS is in sync when CSS changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 64 Node Editor polish: all authored multiline fields auto-grow, and the bottom toolbar has Create text block alongside the existing create buttons and Duplicate.</what-built>
  <how-to-verify>
    1. Build/install the plugin in the dev Obsidian vault using the existing project workflow.
    2. Open a canvas and select each node kind: Question, Answer, Text block, Snippet, and Loop.
    3. Paste several lines into Question text, Answer text, Text block Content, Loop Header text, and Snippet Branch label. Expected: each field expands with no inner scrollbar.
    4. Delete the pasted content down to one short line. Expected: each field shrinks without flicker and focus/cursor stay in the field.
    5. In the Node Editor bottom toolbar, click `Create text block`. Expected: a yellow text-block canvas node is created below the current node, and the Node Editor immediately shows the Text-block form.
    6. Narrow the sidebar. Expected: the five create buttons plus Duplicate remain visible/reachable by vertical scrolling; no horizontal clipping.
  </how-to-verify>
  <resume-signal>Type "approved" if the behavior matches, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolbar click → Canvas internal API | User click invokes undocumented Canvas createTextNode through CanvasNodeFactory. |
| Canvas factory result → Node Editor form | Live in-memory canvas node data is trusted to render the newly-created form before disk save flush. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03-01 | Tampering | Quick-create kind | mitigate | Pass only the literal `'text-block'` through the existing `onQuickCreate`/`CanvasNodeFactory` path per D3; no alternate node creation pipeline. |
| T-64-03-02 | Denial of Service | Toolbar layout | mitigate | Preserve vertical full-width stack and panel scrolling per D4/UI-SPEC so added controls do not become unreachable at narrow widths. |
| T-64-03-03 | Elevation of Privilege | Canvas internal API | accept | Existing `CanvasNodeFactory` runtime probing/Notice failure path already gates unavailable Canvas API; this phase does not expand privileges. |
</threat_model>

<verification>
1. `npm test -- src/__tests__/editor-panel-forms.test.ts src/__tests__/editor-panel-create.test.ts`
2. `npm test`
3. `npm run build`
4. Human Obsidian UI verification checkpoint.
</verification>

<success_criteria>
- EDITOR-06 is satisfied: Create text block is visible, accessible, and calls `CanvasNodeFactory.createNode(canvasPath, 'text-block', anchor)`.
- Text-block nodes get existing factory behavior: `radiprotocol_nodeType = 'text-block'`, `NODE_COLOR_MAP['text-block']`, below-anchor placement, and immediate in-memory form load.
- Toolbar layout remains bottom, vertical, full-width, and reachable; Phase 64 does not redesign unrelated Node Editor UI.
</success_criteria>

<output>
After completion, create `.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-03-SUMMARY.md`.
</output>
