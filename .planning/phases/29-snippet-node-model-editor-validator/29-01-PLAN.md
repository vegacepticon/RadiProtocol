---
phase: 29-snippet-node-model-editor-validator
plan: 01
type: execute
wave: 1
depends_on:
  - 29-00
files_modified:
  - src/graph/graph-model.ts
  - src/canvas/node-color-map.ts
  - src/graph/canvas-parser.ts
  - src/graph/graph-validator.ts
autonomous: true
requirements:
  - SNIPPET-NODE-01
  - SNIPPET-NODE-08

must_haves:
  truths:
    - "TypeScript компилируется без ошибок после всех 4 файлов изменены"
    - "CanvasParser распознаёт radiprotocol_nodeType = 'snippet' и возвращает SnippetNode"
    - "CanvasParser парсит radiprotocol_subfolderPath как optional string"
    - "GraphValidator.nodeLabel() обрабатывает kind = 'snippet' без падения"
    - "NODE_COLOR_MAP содержит 'snippet': '6'"
  artifacts:
    - path: "src/graph/graph-model.ts"
      provides: "SnippetNode interface + 'snippet' в RPNodeKind + RPNode union"
      contains: "kind: 'snippet'"
    - path: "src/canvas/node-color-map.ts"
      provides: "NODE_COLOR_MAP['snippet'] = '6'"
      contains: "'snippet':.*'6'"
    - path: "src/graph/canvas-parser.ts"
      provides: "validKinds[] + case 'snippet' в parseNode switch"
      contains: "case 'snippet'"
    - path: "src/graph/graph-validator.ts"
      provides: "nodeLabel() case 'snippet'"
      contains: "case 'snippet': return"
  key_links:
    - from: "src/graph/graph-model.ts"
      to: "src/canvas/node-color-map.ts"
      via: "Record<RPNodeKind, string> — exhaustive type check"
      pattern: "RPNodeKind"
    - from: "src/graph/graph-model.ts"
      to: "src/graph/canvas-parser.ts"
      via: "import type { SnippetNode } from './graph-model'"
      pattern: "SnippetNode"
---

<objective>
Расширить граф-модель, парсер, color map и валидатор для поддержки 8-го типа узла `snippet`.

Purpose: Это фундамент всей фазы 29. После этого плана TypeScript знает о `SnippetNode`, парсер его читает, color map его красит, валидатор его описывает. TDD-тесты из Plan 00 перейдут из RED в GREEN.
Output: 4 файла изменены, `npm test` зелёный для всех тестов Phase 29.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-snippet-node-model-editor-validator/29-CONTEXT.md
@.planning/phases/29-snippet-node-model-editor-validator/29-RESEARCH.md
@.planning/phases/29-snippet-node-model-editor-validator/29-00-SUMMARY.md

<interfaces>
<!-- Текущее состояние файлов (прочитаны до планирования) -->

src/graph/graph-model.ts (строки 1-11, 81-88) — до изменений:
```typescript
export type RPNodeKind =
  | 'start'
  | 'question'
  | 'answer'
  | 'free-text-input'
  | 'text-block'
  | 'loop-start'
  | 'loop-end';
// ...
export type RPNode =
  | StartNode
  | QuestionNode
  | AnswerNode
  | FreeTextInputNode
  | TextBlockNode
  | LoopStartNode
  | LoopEndNode;
```

src/canvas/node-color-map.ts (строки 12-20) — до изменений:
```typescript
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
  'start':           '4',
  'question':        '5',
  'answer':          '2',
  'free-text-input': '2',
  'text-block':      '3',
  'loop-start':      '1',
  'loop-end':        '1',
};
```

src/graph/canvas-parser.ts (строки 158-165) — validKinds[] до изменений:
```typescript
const validKinds: RPNodeKind[] = [
  'start', 'question', 'answer', 'free-text-input',
  'text-block', 'loop-start', 'loop-end',
];
```

src/graph/graph-validator.ts (строки 192-202) — nodeLabel() до изменений:
```typescript
private nodeLabel(node: RPNode): string {
  switch (node.kind) {
    case 'start': return `start (${node.id})`;
    case 'question': return node.questionText || node.id;
    case 'answer': return (node.displayLabel ?? node.answerText) || node.id;
    case 'free-text-input': return node.promptLabel || node.id;
    case 'text-block': return node.content.slice(0, 30) || node.id;
    case 'loop-start': return node.loopLabel || node.id;
    case 'loop-end': return `loop-end (${node.id})`;
  }
}
```

Паттерн optional-поля в parseNode() — из case 'text-block':
```typescript
snippetId: props['radiprotocol_snippetId'] !== undefined
  ? getString(props, 'radiprotocol_snippetId')
  : undefined,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить graph-model.ts — SnippetNode + RPNodeKind + RPNode</name>
  <files>src/graph/graph-model.ts</files>
  <read_first>
    - src/graph/graph-model.ts (прочитать полностью — изменяем 3 места, нельзя потерять существующий код)
  </read_first>
  <action>
Внести три изменения в `src/graph/graph-model.ts`:

**Изменение 1** — добавить `'snippet'` в `RPNodeKind` union (строки 4-11). Результат:
```typescript
export type RPNodeKind =
  | 'start'
  | 'question'
  | 'answer'
  | 'free-text-input'
  | 'text-block'
  | 'loop-start'
  | 'loop-end'
  | 'snippet';  // Phase 29
```

**Изменение 2** — добавить `SnippetNode` interface после `LoopEndNode` (после строки ~64, до комментария LoopContext):
```typescript
export interface SnippetNode extends RPNodeBase {
  kind: 'snippet';
  subfolderPath?: string;  // отсутствие = корень .radiprotocol/snippets (D-02, D-03)
}
```

**Изменение 3** — добавить `SnippetNode` в `RPNode` union (строки 81-88). Результат:
```typescript
export type RPNode =
  | StartNode
  | QuestionNode
  | AnswerNode
  | FreeTextInputNode
  | TextBlockNode
  | LoopStartNode
  | LoopEndNode
  | SnippetNode;  // Phase 29
```

КРИТИЧНО: Не удалять комментарии, LoopContext interface и всё остальное в файле. Только точечные вставки.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error|snippet" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/graph/graph-model.ts` содержит строку `| 'snippet';` (grep: `grep "'snippet'" src/graph/graph-model.ts`)
    - `src/graph/graph-model.ts` содержит `export interface SnippetNode extends RPNodeBase` (grep: `grep "SnippetNode" src/graph/graph-model.ts`)
    - `src/graph/graph-model.ts` содержит `subfolderPath?: string` (grep: `grep "subfolderPath" src/graph/graph-model.ts`)
    - `npm run build` падает с ошибкой про `'snippet'` missing в `NODE_COLOR_MAP` — это ОЖИДАЕМО и подтверждает, что TS-проверка работает (ошибка исчезнет после Task 1b в node-color-map.ts)
  </acceptance_criteria>
  <done>SnippetNode interface существует, 'snippet' в RPNodeKind и RPNode; TS сигнализирует об отсутствующем ключе в NODE_COLOR_MAP (это ожидаемый Red-state)</done>
</task>

<task type="auto">
  <name>Task 2: Обновить node-color-map, canvas-parser, graph-validator</name>
  <files>src/canvas/node-color-map.ts, src/graph/canvas-parser.ts, src/graph/graph-validator.ts</files>
  <read_first>
    - src/canvas/node-color-map.ts (прочитать — добавляем одну строку, не трогаем остальное)
    - src/graph/canvas-parser.ts (прочитать полностью — добавляем в validKinds[] и в switch)
    - src/graph/graph-validator.ts (прочитать полностью — добавляем case в nodeLabel(); проверяем нет ли других exhaustive switch)
  </read_first>
  <action>
**Файл 1: src/canvas/node-color-map.ts**

Добавить запись `'snippet': '6'` в `NODE_COLOR_MAP`. Результат:
```typescript
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
  'start':           '4',  // green  — entry point ("go" semantics)
  'question':        '5',  // cyan   — information gathering
  'answer':          '2',  // orange — action / selection
  'free-text-input': '2',  // orange — user input action (same family as answer)
  'text-block':      '3',  // yellow — passive content
  'loop-start':      '1',  // red    — loop boundary
  'loop-end':        '1',  // red    — loop boundary (intentional share with loop-start, D-01)
  'snippet':         '6',  // purple — snippet node (Phase 29, D-11)
};
```

**Файл 2: src/graph/canvas-parser.ts**

Изменение 2a — добавить `'snippet'` в `validKinds[]` (строка ~158-162):
```typescript
const validKinds: RPNodeKind[] = [
  'start', 'question', 'answer', 'free-text-input',
  'text-block', 'loop-start', 'loop-end', 'snippet',  // Phase 29
];
```

Изменение 2b — добавить импорт `SnippetNode` в import type (строка ~4-17):
```typescript
import type {
  RPNode,
  RPNodeKind,
  RPEdge,
  ProtocolGraph,
  ParseResult,
  StartNode,
  QuestionNode,
  AnswerNode,
  FreeTextInputNode,
  TextBlockNode,
  LoopStartNode,
  LoopEndNode,
  SnippetNode,  // Phase 29
} from './graph-model';
```

Изменение 2c — добавить `case 'snippet'` в switch внутри `parseNode()` (после `case 'loop-end'`, перед закрывающей скобкой switch). Паттерн идентичен `case 'text-block'`:
```typescript
case 'snippet': {
  const node: SnippetNode = {
    ...base,
    kind: 'snippet',
    subfolderPath: props['radiprotocol_subfolderPath'] !== undefined
      ? getString(props, 'radiprotocol_subfolderPath')
      : undefined,
  };
  return node;
}
```

**Файл 3: src/graph/graph-validator.ts**

Добавить `case 'snippet'` в `nodeLabel()` switch (после `case 'loop-end'`):
```typescript
case 'snippet': return node.subfolderPath ? `snippet (${node.subfolderPath})` : 'snippet (root)';
```

КРИТИЧНО для graph-validator.ts: Файл содержит методы `validate()`, `bfsReachable()`, `detectUnintentionalCycles()`, `nodeLabel()` — изменяем только `nodeLabel()`. Не трогать ни один другой метод.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test -- canvas-parser 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` завершается без ошибок (exit 0 или "Build complete" / "Done in")
    - `grep "'snippet'" src/canvas/node-color-map.ts` выводит строку с `'6'`
    - `grep "case 'snippet'" src/graph/canvas-parser.ts` выводит непустой результат
    - `grep "SnippetNode" src/graph/canvas-parser.ts` — содержит импорт и использование в case
    - `grep "case 'snippet'" src/graph/graph-validator.ts` выводит непустой результат
    - `npm test -- canvas-parser` — новые тесты Phase 29 теперь GREEN (было RED в Wave 0)
    - `npm test -- graph-validator` — новый тест Phase 29 (D-12) GREEN
    - `npm test` — полный suite GREEN (нет регрессий)
  </acceptance_criteria>
  <done>npm run build и npm test завершаются без ошибок. Все 3 новых теста из Plan 00 GREEN. Все ранее существовавшие тесты остаются GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas JSON → CanvasParser | `radiprotocol_subfolderPath` поступает из canvas файла |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Information Disclosure | canvas-parser.ts parseNode | accept | `subfolderPath` — путь к внутренней vault-папке; не передаётся за пределы vault; данные source — Obsidian canvas файл |
| T-29-01-02 | Tampering | canvas-parser.ts subfolderPath | mitigate | `getString()` helper с fallback — только string-тип принимается; non-string значения игнорируются через `props['radiprotocol_subfolderPath'] !== undefined ? getString(...) : undefined` |
</threat_model>

<verification>
После Wave 1:
1. `npm run build` — OK (no TypeScript errors)
2. `npm test` — все тесты GREEN включая 3 новых из Phase 29
3. `grep "SnippetNode" src/graph/graph-model.ts` — находит interface
4. `grep "'snippet'" src/canvas/node-color-map.ts` — находит `'6'`
5. `grep "case 'snippet'" src/graph/canvas-parser.ts` — находит case
6. `grep "case 'snippet'" src/graph/graph-validator.ts` — находит case в nodeLabel()
</verification>

<success_criteria>
- `npm test` полностью GREEN
- SnippetNode interface экспортирован из graph-model.ts
- 'snippet' присутствует в RPNodeKind union
- NODE_COLOR_MAP содержит 'snippet': '6'
- CanvasParser парсит snippet-node.canvas и возвращает SnippetNode с subfolderPath = 'CT/adrenal'
- CanvasParser парсит snippet-node-no-path.canvas и возвращает SnippetNode с subfolderPath = undefined
- GraphValidator не генерирует ошибок для snippet-узла без subfolderPath (D-12)
</success_criteria>

<output>
После выполнения создать `.planning/phases/29-snippet-node-model-editor-validator/29-01-SUMMARY.md`
</output>
