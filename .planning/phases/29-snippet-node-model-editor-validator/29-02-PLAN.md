---
phase: 29-snippet-node-model-editor-validator
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/views/editor-panel-view.ts
autonomous: true
requirements:
  - SNIPPET-NODE-02

must_haves:
  truths:
    - "Node type dropdown в EditorPanel содержит опцию Snippet"
    - "При выборе типа snippet в kindFormSection появляется dropdown выбора подпапки"
    - "Первая опция dropdown — root (all snippets) — с value пустой строкой"
    - "При отсутствии подпапок dropdown показывает disabled-опцию No subfolders found"
    - "При выборе подпапки pendingEdits накапливает radiprotocol_subfolderPath + text"
    - "При выборе root radiprotocol_subfolderPath = undefined, text = пустая строка"
    - "npm run build завершается без ошибок"
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "EditorPanelView с case snippet в buildKindForm + listSnippetSubfolders helper"
      contains: "case 'snippet'"
  key_links:
    - from: "src/views/editor-panel-view.ts buildKindForm case snippet"
      to: "this.listSnippetSubfolders(basePath)"
      via: "void (async () => { ... })()"
      pattern: "listSnippetSubfolders"
    - from: "drop.onChange handler"
      to: "this.pendingEdits['radiprotocol_subfolderPath']"
      via: "v || undefined"
      pattern: "radiprotocol_subfolderPath"
---

<objective>
Добавить форму EditorPanel для snippet-узла: опция в dropdown типов + async dropdown выбора подпапки.

Purpose: Это единственный способ для автора протокола настроить snippet-узел — выбрать папку, из которой Runner будет предлагать сниппеты (Phase 30).
Output: editor-panel-view.ts с `case 'snippet'` в `buildKindForm` + приватный `listSnippetSubfolders()` метод.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-snippet-node-model-editor-validator/29-CONTEXT.md
@.planning/phases/29-snippet-node-model-editor-validator/29-RESEARCH.md
@.planning/phases/29-snippet-node-model-editor-validator/29-01-SUMMARY.md

<interfaces>
<!-- Состояние editor-panel-view.ts (прочитан до планирования) -->

Структура класса EditorPanelView:
- `private plugin: RadiProtocolPlugin` — доступ к vault через `this.plugin.app.vault`
- `private pendingEdits: Record<string, unknown>` — накапливает изменения
- `private scheduleAutoSave(): void` — 800ms debounce + saveNodeEdits
- `private buildKindForm(container, nodeRecord, kind): void` — СИНХРОННЫЙ метод
- Switch в buildKindForm: case 'start', 'question', 'answer', 'free-text-input', 'text-block', 'loop-start', 'loop-end'

Паттерн case в buildKindForm (из case 'loop-end'):
```typescript
case 'loop-end': {
  new Setting(container).setHeading().setName('Loop-end node');
  new Setting(container)
    .setName('Loop start node ID')
    .addText(t => {
      t.setValue(...)
        .onChange(v => {
          this.pendingEdits['radiprotocol_loopStartId'] = v;
          this.scheduleAutoSave();
        });
    });
  break;
}
```

Node type dropdown (renderForm, строки 300-328) — все 7 addOption вызовов заканчиваются:
```typescript
.addOption('loop-end', 'Loop end')
.setValue(currentKind ?? '')
```

Settings field: `this.plugin.settings.snippetFolderPath` → default value: `.radiprotocol/snippets`

Паттерн async-внутри-синхронного (из loadNode):
```typescript
void this.renderNodeForm(canvasFilePath, nodeId);
```

vault.adapter.list(path) возвращает `{ files: string[], folders: string[] }`
vault.adapter.exists(path) возвращает `Promise<boolean>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить snippet в dropdown + case snippet в buildKindForm + listSnippetSubfolders</name>
  <files>src/views/editor-panel-view.ts</files>
  <read_first>
    - src/views/editor-panel-view.ts (прочитать ПОЛНОСТЬЮ — файл большой, нельзя потерять ни одной строки из существующего кода)
    - src/snippets/snippet-service.ts (найти строки с vault.adapter.list — верифицировать формат возвращаемых путей)
  </read_first>
  <action>
Внести три точечных изменения в `src/views/editor-panel-view.ts`.

КРИТИЧНО: CLAUDE.md запрещает удалять любой существующий код. Только добавлять.

ИЗМЕНЕНИЕ 1 — Добавить опцию snippet в dropdown типов (renderForm, внутри addDropdown блока)

Найти строку:
```typescript
.addOption('loop-end', 'Loop end')
.setValue(currentKind ?? '')
```
Изменить на:
```typescript
.addOption('loop-end', 'Loop end')
.addOption('snippet', 'Snippet')         // Phase 29: D-06
.setValue(currentKind ?? '')
```

ИЗМЕНЕНИЕ 2 — Добавить case snippet в buildKindForm switch

Найти закрывающую скобку switch в конце buildKindForm (после case loop-end блока). Вставить перед закрывающей `}` switch новый case:

```typescript
      case 'snippet': {
        new Setting(container).setHeading().setName('Snippet node');
        const subfolderSetting = new Setting(container)
          .setName('Subfolder path')
          .setDesc(
            'Select the subfolder within .radiprotocol/snippets/ that this node offers to the runner. ' +
            'Leave as root to use all snippets from the top-level folder.'
          );

        // Async populate — buildKindForm is synchronous; use void IIFE pattern (per RESEARCH.md)
        void (async () => {
          try {
            const basePath = this.plugin.settings.snippetFolderPath;
            const subfolders = await this.listSnippetSubfolders(basePath);

            subfolderSetting.addDropdown(drop => {
              drop.addOption('', '\u2014 root (all snippets) \u2014');  // D-09

              if (subfolders.length === 0) {
                // D-08: empty state — disabled placeholder via DOM API (no innerHTML)
                const disabledOpt = drop.selectEl.createEl('option', {
                  text: 'No subfolders found',
                });
                disabledOpt.disabled = true;
              } else {
                for (const sub of subfolders) {
                  drop.addOption(sub, sub);
                }
              }

              const currentPath = (nodeRecord['radiprotocol_subfolderPath'] as string | undefined) ?? '';
              drop.setValue(currentPath);

              drop.onChange(v => {
                // D-09: empty value -> undefined (root fallback at runtime)
                this.pendingEdits['radiprotocol_subfolderPath'] = v || undefined;
                // D-10: text field mirrors the subfolder path value only
                this.pendingEdits['text'] = v;  // empty string when root selected
                this.scheduleAutoSave();
              });
            });
          } catch {
            // Pitfall 2: async errors silent in void IIFE — show fallback text
            subfolderSetting.setDesc('Could not load subfolders. Check that .radiprotocol/snippets/ exists.');
          }
        })();
        break;
      }
```

ИЗМЕНЕНИЕ 3 — Добавить приватный метод listSnippetSubfolders

Добавить после метода `renderError` (перед закрывающей `}` класса EditorPanelView):

```typescript
  /**
   * Recursively lists all subfolder paths (relative to basePath) within basePath.
   * Uses BFS via vault.adapter.list(). Returns [] if basePath does not exist.
   * Phase 29, D-07.
   */
  private async listSnippetSubfolders(basePath: string): Promise<string[]> {
    const exists = await this.plugin.app.vault.adapter.exists(basePath);
    if (!exists) return [];

    const results: string[] = [];
    const queue: string[] = [basePath];

    while (queue.length > 0) {
      const current = queue.shift()!;
      let listing: { files: string[]; folders: string[] };
      try {
        listing = await this.plugin.app.vault.adapter.list(current);
      } catch {
        continue; // Skip inaccessible directories silently
      }

      for (const folder of listing.folders) {
        // vault.adapter.list returns full vault-relative paths (e.g. .radiprotocol/snippets/CT/adrenal)
        // Compute relative path: strip basePath + '/' prefix (Pitfall 3 from RESEARCH.md)
        const rel = folder.slice(basePath.length + 1);
        if (rel) {
          results.push(rel);
        }
        queue.push(folder); // BFS: recurse into subfolder
      }
    }

    return results;
  }
```

После редактирования убедиться что в файле сохранились все существующие методы: attachCanvasListener, handleNodeClick, loadNode, saveNodeEdits, renderNodeForm, renderForm, buildKindForm с 7 существующими case, scheduleAutoSave, onTypeDropdownChange, showSavedIndicator, renderError.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` завершается без ошибок TypeScript (exit 0)
    - `grep "addOption('snippet'" src/views/editor-panel-view.ts` выводит строку с Snippet
    - `grep "case 'snippet'" src/views/editor-panel-view.ts` выводит непустой результат
    - `grep "listSnippetSubfolders" src/views/editor-panel-view.ts` находит и вызов и определение метода (минимум 2 вхождения)
    - `grep "radiprotocol_subfolderPath" src/views/editor-panel-view.ts` находит в onChange handler
    - `grep "pendingEdits\[.text.\] = v" src/views/editor-panel-view.ts` содержит строку (D-10: text mirrors path)
    - `grep -c "case '" src/views/editor-panel-view.ts` выводит 8 (было 7 + новый snippet case)
    - `npm test` проходит без регрессий — полный suite green
  </acceptance_criteria>
  <done>EditorPanel содержит Snippet в dropdown, case snippet в buildKindForm с async subfolder picker, метод listSnippetSubfolders добавлен. npm run build и npm test зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault.adapter.list() to dropdown | Subfolder paths поступают из vault filesystem в UI |
| drop.onChange() to pendingEdits | Значение из dropdown записывается в pendingEdits и затем в canvas JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | radiprotocol_subfolderPath в pendingEdits | mitigate | Значения ограничены результатами `vault.adapter.list(basePath)` — только реально существующие подпапки в пределах basePath. Прямого пользовательского ввода пути нет (V5: Input Validation через ограниченный dropdown) |
| T-29-02-02 | Denial of Service | listSnippetSubfolders BFS | accept | Vault-файловая система ограничена размером vault; BFS в памяти без ограничений итераций приемлем для практического размера vault |
| T-29-02-03 | Information Disclosure | listing.folders пути в dropdown | accept | Пути к подпапкам видны только пользователю vault; Obsidian однопользовательское приложение |
</threat_model>

<verification>
После Wave 2:
1. `npm run build` — OK (нет TS ошибок)
2. `npm test` — полный suite GREEN
3. Ручная проверка (если есть vault с .radiprotocol/snippets/ подпапками):
   - Открыть canvas со snippet-узлом, открыть EditorPanel, убедиться что dropdown показывает подпапки
   - Выбрать подпапку, подождать 800ms, проверить canvas JSON на наличие radiprotocol_subfolderPath
   - Выбрать root, canvas JSON не должен содержать radiprotocol_subfolderPath
</verification>

<success_criteria>
- Dropdown типов в EditorPanel содержит Snippet как 8-ю опцию
- При выборе snippet отображается форма с heading Snippet node и dropdown Subfolder path
- Первая опция dropdown — root (all snippets) — (value пустая строка)
- При наличии подпапок все рекурсивно найденные пути отображаются как опции
- При отсутствии подпапок показывается disabled-опция No subfolders found
- Выбор подпапки сохраняет radiprotocol_subfolderPath и text через pendingEdits и scheduleAutoSave
- Выбор root сохраняет radiprotocol_subfolderPath как undefined (ключ удалится через saveNodeEdits) и text как пустую строку
- npm run build без ошибок
- npm test без регрессий
</success_criteria>

<output>
После выполнения создать `.planning/phases/29-snippet-node-model-editor-validator/29-02-SUMMARY.md`
</output>
