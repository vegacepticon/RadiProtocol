# Phase 74 — Pattern Map

**Phase:** 74 — GitHub Release v1.11.0  
**Generated:** 2026-04-30  

---

## Files to Create / Modify

| File | Role | Closest Existing Analog | Notes |
|------|------|------------------------|-------|
| `manifest.json` | Version metadata | Current `manifest.json` (1.10.0) | Bump `version` field only |
| `package.json` | NPM metadata | Current `package.json` (1.10.0) | Bump `version` field only |
| `versions.json` | BRAT version map | Current `versions.json` | Append `1.11.0: "1.5.7"` entry |
| `main.js` | Build artifact | Current `main.js` | Generated by `npm run build` |
| `styles.css` | Build artifact | Current `styles.css` | Generated by esbuild CSS concat plugin |
| Git tag `1.11.0` | Release marker | Tags `1.8.0`, `1.9.0`, `1.10.0` | Unprefixed annotated tag |
| GitHub Release | Distribution | Releases 1.8.0, 1.9.0, 1.10.0 | 3 loose assets, prerelease=false |

---

## Release Runbook Pattern (from Phase 68)

Prior release phases (55, 62, 68) established this invariant sequence:

1. `npm version {X.Y.Z} --no-git-tag-version` → bumps package.json
2. `node version-bump.mjs` → bumps manifest.json + versions.json
3. `git add manifest.json versions.json package.json`
4. `git commit -m "release: v{X.Y.Z}"`
5. `npm run build` → verify clean build
6. `git tag -a {X.Y.Z} -m "Release {X.Y.Z}"`
7. `git push origin {X.Y.Z}`
8. Create GitHub Release with 3 assets
9. BRAT smoke test on clean vault

---

## Code Excerpts

### version-bump.mjs
```javascript
const targetVersion = process.env.npm_package_version;
const manifest = JSON.parse(readFileSync('manifest.json', 'utf8'));
const { minAppVersion } = manifest;
manifest.version = targetVersion;
writeFileSync('manifest.json', JSON.stringify(manifest, null, '\t'));
const versions = JSON.parse(readFileSync('versions.json', 'utf8'));
versions[targetVersion] = minAppVersion;
writeFileSync('versions.json', JSON.stringify(versions, null, '\t'));
```

### package.json scripts
```json
{
  "version": "node version-bump.mjs && git add manifest.json versions.json",
  "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
}
```

---

## PATTERN MAPPING COMPLETE
