---
phase: 18-css-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles.css
autonomous: false
requirements:
  - LAYOUT-03
  - SIDEBAR-01
  - RUNNER-01

must_haves:
  truths:
    - "Insert button renders at equal width to Copy and Save buttons in the output toolbar"
    - "Canvas selector widget (trigger, popover, rows) is visually styled in sidebar mode"
    - "Run Again button is styled with accent background and disabled state when no canvas is loaded"
  artifacts:
    - path: "src/styles.css"
      provides: "All three CSS gap fixes"
      contains: ".rp-insert-btn"
    - path: "src/styles.css"
      provides: "Canvas selector widget styling"
      contains: ".rp-selector-bar"
    - path: "src/styles.css"
      provides: "Run Again button styling"
      contains: ".rp-run-again-btn"
  key_links:
    - from: "src/views/runner-view.ts (line 568)"
      to: "src/styles.css (.rp-insert-btn)"
      via: "CSS class name match"
      pattern: "rp-insert-btn"
    - from: "src/views/canvas-selector-widget.ts"
      to: "src/styles.css (.rp-selector-bar through .rp-selector-empty-hint)"
      via: "CSS class name match"
      pattern: "rp-selector-"
    - from: "src/views/runner-view.ts (line 438)"
      to: "src/styles.css (.rp-run-again-btn)"
      via: "CSS class name match"
      pattern: "rp-run-again-btn"
---

<objective>
Close three CSS gaps identified by the v1.2 milestone audit. The TypeScript already emits
the correct class names; only the matching CSS rules are missing in src/styles.css.

Purpose: Without these rules, Insert button is narrower than Copy/Save, the canvas
selector widget renders unstyled, and the Run Again button has no visual treatment
(and no disabled state when canvasFilePath is null).

Output: Three targeted CSS additions to src/styles.css — no TypeScript changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-css-gap-fixes/18-CONTEXT.md
@.planning/phases/18-css-gap-fixes/18-RESEARCH.md
@.planning/phases/18-css-gap-fixes/18-UI-SPEC.md
@.planning/phases/18-css-gap-fixes/18-VALIDATION.md

<interfaces>
<!-- Key rules and insertion points. Extracted from src/styles.css. -->
<!-- Executor: read the file first — these are reference markers, not final state. -->

From src/styles.css lines 61–64 (LAYOUT-03 target — extend this selector):
```css
.rp-copy-btn,
.rp-save-btn {
  flex: 1;
}
```

From src/styles.css lines 151–155 (RUNNER-01 insertion point — add .rp-run-again-btn AFTER this):
```css
.rp-complete-heading {
  font-size: var(--font-ui-medium); /* 16px */
  font-weight: var(--font-semibold);
  margin-bottom: var(--size-4-1);
}
```

From src/styles.css line 157 (SIDEBAR-01 insertion point — add new section BEFORE this):
```css
/* Phase 4: EditorPanelView ─────────────────────────────────────────────── */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LAYOUT-03 — Extend flex-button selector to include .rp-insert-btn</name>
  <files>src/styles.css</files>

  <read_first>
    - src/styles.css — read lines 54–70 to confirm the current two-selector group at lines
      61–64 before editing; verify .rp-output-toolbar has display:flex at line 54
    - src/views/runner-view.ts — read lines 558–572 to confirm rp-insert-btn is emitted
      alongside rp-copy-btn and rp-save-btn
  </read_first>

  <action>
In src/styles.css, find the block at lines 61–64:

```css
.rp-copy-btn,
.rp-save-btn {
  flex: 1;
}
```

Replace it with a three-selector group:

```css
.rp-copy-btn,
.rp-save-btn,
.rp-insert-btn {
  flex: 1;
}
```

This is the ONLY change for this task. Do not touch any other rules. The output toolbar
(`.rp-output-toolbar`, line 54) already has `display: flex; flex-direction: row` — adding
`flex: 1` to `.rp-insert-btn` gives all three buttons equal share of available width.

Per D-01: single declaration for all three buttons, no separate rule.
  </action>

  <verify>
    <automated>grep -n "rp-insert-btn" src/styles.css</automated>
  </verify>

  <acceptance_criteria>
    - grep "rp-insert-btn" src/styles.css exits 0 (match found)
    - The matched line is the selector `.rp-insert-btn {` within the three-selector group
      (not a comment, not a TypeScript file)
    - src/styles.css contains the exact text `.rp-insert-btn` immediately after `.rp-save-btn,`
      within the same rule block (i.e. `.rp-copy-btn, .rp-save-btn, .rp-insert-btn { flex: 1; }`)
    - grep "rp-copy-btn" src/styles.css still exits 0 (existing selector preserved, not deleted)
    - The block still contains exactly `flex: 1;` as the only declaration
  </acceptance_criteria>

  <done>
    src/styles.css contains `.rp-insert-btn` in the three-selector flex group with `flex: 1`.
    All three toolbar buttons will render at equal width.
  </done>
</task>

<task type="auto">
  <name>Task 2: SIDEBAR-01 — Add canvas selector widget CSS section</name>
  <files>src/styles.css</files>

  <read_first>
    - src/styles.css — read lines 145–165 to confirm .rp-complete-heading block ends at
      line 155 and the Phase 4 comment appears at line 157; verify no rp-selector-* rules
      already exist (grep "rp-selector" src/styles.css should return no matches before edit)
    - src/views/canvas-selector-widget.ts — read to confirm all class names emitted:
      rp-selector-bar, rp-selector-wrapper, rp-selector-trigger, rp-selector-trigger-label,
      rp-selector-placeholder, rp-selector-chevron, rp-selector-popover, rp-selector-row,
      rp-selector-row-icon, rp-selector-row-label, rp-selector-row-arrow, rp-selector-empty-hint
  </read_first>

  <action>
In src/styles.css, insert the following CSS block BETWEEN the `.rp-complete-heading` rule
(line 155) and the `/* Phase 4: EditorPanelView */` comment (line 157). Insert after the
closing brace of `.rp-complete-heading` and before the Phase 4 comment, preserving one
blank line before the Phase 4 comment.

Insert this exact block (per D-02, D-03, D-04 — the `/* Phase 13 */` section header and
all listed selectors):

```css
/* Phase 13: CanvasSelectorWidget ─────────────────────────────────────────── */

.rp-selector-bar {
  flex: 0 0 auto;
  padding: var(--size-2-1) var(--size-4-2);
  border-bottom: 1px solid var(--background-modifier-border);
}

.rp-selector-wrapper {
  position: relative;
  width: 100%;
}

.rp-selector-trigger {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--size-2-1);
  padding: var(--size-2-1) var(--size-4-1);
  font-size: var(--font-text-size);
  text-align: left;
  background: var(--background-secondary);
  border: 1px solid var(--background-modifier-border);
  border-radius: var(--radius-s);
  cursor: pointer;
}

.rp-selector-trigger:hover {
  background: var(--background-modifier-hover);
}

.rp-selector-trigger-label {
  flex: 1 1 auto;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.rp-selector-placeholder {
  color: var(--text-muted);
}

.rp-selector-chevron {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
}

.rp-selector-popover {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  z-index: 100;
  background: var(--background-primary);
  border: 1px solid var(--background-modifier-border);
  border-radius: var(--radius-s);
  box-shadow: var(--shadow-s);
  max-height: 240px;
  overflow-y: auto;
  margin-top: 2px;
}

.rp-selector-row {
  display: flex;
  align-items: center;
  gap: var(--size-2-1);
  padding: var(--size-2-1) var(--size-4-1);
  min-height: 32px;
  cursor: pointer;
  font-size: var(--font-text-size);
}

.rp-selector-row:hover {
  background: var(--background-modifier-hover);
}

.rp-selector-row.is-selected {
  background: var(--background-modifier-active-hover);
  font-weight: var(--font-semibold);
}

.rp-selector-row-icon {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  color: var(--text-muted);
}

.rp-selector-row-label {
  flex: 1 1 auto;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.rp-selector-row-arrow {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  color: var(--text-muted);
}

.rp-selector-empty-hint {
  padding: var(--size-4-1) var(--size-4-2);
  color: var(--text-muted);
  font-size: var(--font-text-size);
  font-style: italic;
}
```

Constraints:
- NO hard-coded hex or rgb() values — all colours must use Obsidian CSS variables
- The `margin-top: 2px` on `.rp-selector-popover` is an intentional micro-adjustment
  (documented exception in 18-UI-SPEC.md) — keep as-is
- Do NOT edit src/styles.css at the root level (root styles.css is the build output)
  </action>

  <verify>
    <automated>grep -n "rp-selector-bar\|rp-selector-wrapper\|rp-selector-trigger\|rp-selector-popover\|rp-selector-row\|rp-selector-empty-hint" src/styles.css</automated>
  </verify>

  <acceptance_criteria>
    - grep "rp-selector-bar" src/styles.css exits 0
    - grep "rp-selector-wrapper" src/styles.css exits 0
    - grep "rp-selector-trigger" src/styles.css exits 0
    - grep "rp-selector-popover" src/styles.css exits 0
    - grep "rp-selector-row" src/styles.css exits 0
    - grep "rp-selector-empty-hint" src/styles.css exits 0
    - grep "rp-selector-placeholder" src/styles.css exits 0
    - grep "rp-selector-chevron" src/styles.css exits 0
    - grep "rp-selector-row-icon" src/styles.css exits 0
    - grep "rp-selector-row-label" src/styles.css exits 0
    - grep "rp-selector-row-arrow" src/styles.css exits 0
    - grep "Phase 13: CanvasSelectorWidget" src/styles.css exits 0
    - grep "#" src/styles.css does NOT introduce new hex color values (existing content
      preserved; any new hex match would be a violation — scan the inserted block only)
    - The `/* Phase 4: EditorPanelView` comment still exists in src/styles.css (not deleted)
  </acceptance_criteria>

  <done>
    src/styles.css contains a complete `/* Phase 13: CanvasSelectorWidget */` section with
    all 12 selector-widget rules. Canvas selector widget renders styled in sidebar mode.
  </done>
</task>

<task type="auto">
  <name>Task 3: RUNNER-01 — Add .rp-run-again-btn CSS rule</name>
  <files>src/styles.css</files>

  <read_first>
    - src/styles.css — read the area around .rp-complete-heading (now at approximately
      lines 151–155 before Task 2's insertions; after Task 2 it will be further down —
      search by selector name, not line number) to confirm insertion point
    - src/views/runner-view.ts — read lines 436–452 to confirm rp-run-again-btn is emitted
      and that `runAgainBtn.disabled = true` fires when `this.canvasFilePath === null`
    - src/styles.css — search for `.rp-loop-again-btn` (Phase 6 section) to confirm the
      accent-button reference pattern
  </read_first>

  <action>
In src/styles.css, locate the `.rp-complete-heading` rule (search by selector name —
line numbers shifted after Task 2's insertions). Insert the following block IMMEDIATELY
AFTER the closing brace `}` of `.rp-complete-heading`, before whatever rule follows it.

Insert this exact block (per D-05 and D-06):

```css
.rp-run-again-btn {
  margin-top: var(--size-4-1);
  background: var(--interactive-accent);
  color: var(--text-on-accent);
  border: none;
  border-radius: var(--radius-s);
  padding: var(--size-2-2) var(--size-4-2);
  cursor: pointer;
  font-size: var(--font-ui-small);
  width: 100%;
}

.rp-run-again-btn:hover {
  background: var(--interactive-accent-hover);
}

.rp-run-again-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```

Constraints:
- The `:disabled` rule is REQUIRED — runner-view.ts:441 sets `runAgainBtn.disabled = true`
  when `canvasFilePath === null`; without it the button looks active when it is not
- `width: 100%` is required — the button is alone in the complete zone and should fill it
- NO hard-coded hex or rgb() values
- Pattern matches `.rp-loop-again-btn` (accent colour, border: none, radius-s, padding)
  plus `width: 100%` and `:disabled` additions specific to this button
  </action>

  <verify>
    <automated>grep -n "rp-run-again-btn" src/styles.css</automated>
  </verify>

  <acceptance_criteria>
    - grep "rp-run-again-btn" src/styles.css exits 0
    - Output shows at least 3 matches: `.rp-run-again-btn {`, `.rp-run-again-btn:hover {`,
      `.rp-run-again-btn:disabled {`
    - grep "interactive-accent" src/styles.css includes a line under `.rp-run-again-btn`
      (the background declaration)
    - grep "width: 100%" src/styles.css includes a match in the `.rp-run-again-btn` block
    - grep "opacity: 0.5" src/styles.css includes a match (disabled state)
    - grep "cursor: not-allowed" src/styles.css includes a match (disabled state)
    - The `.rp-complete-heading` selector still exists in src/styles.css (not deleted)
  </acceptance_criteria>

  <done>
    src/styles.css contains `.rp-run-again-btn` with default, :hover, and :disabled rules.
    Run Again button renders styled with accent background and shows disabled state when
    no canvas is loaded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three CSS additions to src/styles.css:
    1. LAYOUT-03: .rp-insert-btn added to the three-selector flex group
    2. SIDEBAR-01: Full rp-selector-* section (12 rules) for the canvas selector widget
    3. RUNNER-01: .rp-run-again-btn with :hover and :disabled states
  </what-built>
  <how-to-verify>
    Run all three grep checks to confirm rule presence:

    ```
    grep -n "rp-insert-btn" src/styles.css
    grep -n "rp-selector-bar" src/styles.css
    grep -n "rp-run-again-btn" src/styles.css
    ```

    All three must return matches.

    Then visually verify in Obsidian developer mode (if available):
    1. Open the runner — verify the Insert button is the same width as Copy and Save
    2. Open the runner in sidebar mode — verify the canvas selector shows a styled dropdown
       (trigger button with border, rows with hover highlight)
    3. Complete a protocol — verify the Run Again button appears with accent background;
       if no canvas is loaded, verify it appears at 50% opacity (disabled state)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all three CSS gaps are closed, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase modifies only src/styles.css. There are no trust boundaries to document.

| Boundary | Description |
|----------|-------------|
| None | CSS-only phase — no inputs, no outputs, no server calls, no DOM manipulation |

## STRIDE Threat Register

This is a pure CSS file edit. No new JavaScript logic, no user inputs, no data
processing, no authentication, no cryptography, no server communication. All STRIDE
categories are not applicable.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | src/styles.css | accept | Class names are already emitted in the built main.js and visible to any user inspecting the DOM. CSS rules do not introduce new information disclosure. |
| T-18-02 | Tampering | src/styles.css | accept | CSS edits affect visual presentation only. No security-relevant logic, no input handling, no data access. |

All other STRIDE categories (Spoofing, Repudiation, Denial of Service, Elevation of
Privilege) are N/A for a CSS-only change with no runtime logic.
</threat_model>

<verification>
Full suite grep confirms all three gaps closed:

```bash
grep -E "rp-insert-btn|rp-selector-bar|rp-selector-wrapper|rp-selector-trigger|rp-selector-popover|rp-selector-row|rp-run-again-btn" src/styles.css
```

Expected: 10+ matches across all three gap areas.

Existing Vitest suite must remain green (no TypeScript changed):
```bash
npm test
```
</verification>

<success_criteria>
1. src/styles.css line contains `.rp-insert-btn` in a three-selector group with `flex: 1`
2. src/styles.css contains a `/* Phase 13: CanvasSelectorWidget */` section with all 12
   rp-selector-* rules
3. src/styles.css contains `.rp-run-again-btn` with default, :hover, and :disabled rules
4. No hard-coded hex or rgb() values introduced (all colours use Obsidian CSS variables)
5. src/styles.css at root is NOT modified (only src/styles.css is the source)
6. npm test passes (TypeScript unchanged, Vitest suite green)
7. Human UAT confirms visual rendering correct in Obsidian
</success_criteria>

<output>
After completion, create `.planning/phases/18-css-gap-fixes/18-01-SUMMARY.md` following
the standard summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.

Include:
- Which three CSS gaps were closed (LAYOUT-03, SIDEBAR-01, RUNNER-01)
- Exact line ranges modified/inserted in src/styles.css
- grep output confirming all rules present
- UAT outcome
</output>
