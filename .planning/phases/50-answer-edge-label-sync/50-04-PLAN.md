---
phase: 50-answer-edge-label-sync
plan: 04
type: execute
wave: 3
depends_on: ["50-01", "50-02", "50-03"]
files_modified:
  - src/canvas/edge-label-sync-service.ts
  - src/main.ts
  - src/views/editor-panel-view.ts
requirements: [EDGE-02]
autonomous: true

must_haves:
  truths:
    - "EdgeLabelSyncService owns the vault.on('modify') subscription for .canvas files via registerEvent (D-01)"
    - "Debounced (250ms) reconcile-per-filepath schedule never exceeds Obsidian's 500ms canvas requestSave (Claude's Discretion)"
    - "Reconcile pass reads live JSON first (canvasLiveEditor.getCanvasJSON) then disk fallback (BUG-02/03 avoidance — Shared Pattern C)"
    - "Reconcile: if reconcileEdgeLabels returns empty diffs + empty map → no disk write (D-07 loop guard; self-terminates modify chain)"
    - "Reconcile writes: Pattern B (saveLiveBatch with nodeEdits + edgeEdits) when live; Strategy A (single vault.modify mutating both nodes[] and edges[]) when disk"
    - "Node Editor Display label handler collects every incoming Question→Answer edgeId and submits ONE saveLiveBatch call (D-14) — never two writes"
    - "Display label clear (v → undefined) strips the 'label' key on every incoming edge (D-08 symmetry)"
    - "Shared helper collectIncomingEdgeEdits lives in edge-label-sync-service.ts (or alongside) and is consumed by both Node Editor handler AND reconciler"
    - "Code comments at reconciler entry + editor-panel-view.ts:442 Display label Setting cite .planning/notes/answer-label-edge-sync.md (D-10 + D-16)"
    - "No rules/functions/event-listeners from prior phases are deleted from main.ts or editor-panel-view.ts (Shared Pattern G)"
  artifacts:
    - path: "src/canvas/edge-label-sync-service.ts"
      provides: "Owner of vault.on('modify') subscription + debounced reconcile + Pattern B/Strategy A write dispatch"
      exports: ["EdgeLabelSyncService"]
      contains: "class EdgeLabelSyncService"
    - path: "src/main.ts"
      provides: "Instantiate EdgeLabelSyncService in onload; destroy in onunload"
      contains: "edgeLabelSyncService"
    - path: "src/views/editor-panel-view.ts"
      provides: "Display label handler collects incoming edges + uses saveLiveBatch (D-14)"
      contains: "Phase 50 D-14"
  key_links:
    - from: "src/main.ts"
      to: "src/canvas/edge-label-sync-service.ts"
      via: "new EdgeLabelSyncService(this.app, this); .register() in onload; .destroy() in onunload"
      pattern: "edgeLabelSyncService"
    - from: "src/canvas/edge-label-sync-service.ts"
      to: "src/graph/edge-label-reconciler.ts"
      via: "import { reconcileEdgeLabels } — pure reconciler drives all writes"
      pattern: "reconcileEdgeLabels"
    - from: "src/views/editor-panel-view.ts:442"
      to: ".planning/notes/answer-label-edge-sync.md"
      via: "code comment above Display label Setting citing canonical design source (D-10)"
      pattern: "answer-label-edge-sync\\.md"
---

<objective>
Связать всё вместе:
1. Создать `EdgeLabelSyncService` с vault.on('modify') подпиской, дебаунсом 250ms и dispatch в Pattern B / Strategy A write через reconciler.
2. Зарегистрировать сервис в `main.ts` (3 строки append-only: field, instantiate+register в onload, destroy в onunload).
3. Переписать Display label onChange handler в `editor-panel-view.ts:442-451` так, чтобы он вызвал `saveLiveBatch` с node+edges в одном write (D-14); добавить Strategy A fallback, мутирующий `canvasData.edges[]` в тот же `vault.modify()` call. Добавить code-comment ссылающийся на `.planning/notes/answer-label-edge-sync.md` (D-10 + D-16).

Purpose: финальный wire-up делает reconciler жизнеспособным — без этого pure-модуль из Plan 02 не вызывается на runtime, а `saveLiveEdges`/`saveLiveBatch` из Plan 03 — ни одним caller'ом.
Output: 3 модифицированных/созданных файла; подписка auto-detach'ится на unload; Node Editor Display label при изменении синхронизирует `displayLabel` И все incoming edges атомарно.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md
@.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md
@.planning/notes/answer-label-edge-sync.md
@.planning/phases/50-answer-edge-label-sync/50-01-SUMMARY.md
@.planning/phases/50-answer-edge-label-sync/50-02-SUMMARY.md
@.planning/phases/50-answer-edge-label-sync/50-03-SUMMARY.md

<interfaces>
<!-- EdgeLabelSyncService skeleton — PATTERNS.md §5 -->

<!-- registerEvent + vault.on('create') canonical pattern — runner-view.ts:200-206 -->
```typescript
this.registerEvent(
  this.app.vault.on('create', (file) => {
    if (file instanceof TFile && file.extension === 'canvas') {
      this.selector?.rebuildIfOpen();
    }
  })
);
```

<!-- Debounce-per-path pattern — snippet-manager-view.ts:170-180 + canvas-live-editor.ts:210-218 -->

<!-- Live-JSON-or-disk read — main.ts:318-335 (adapt: drop new Notice() calls; reconciler is background) -->
```typescript
let content: string;
const liveJson = this.canvasLiveEditor.getCanvasJSON(canvasPath);
if (liveJson !== null) {
  content = liveJson;
} else {
  const file = this.app.vault.getAbstractFileByPath(canvasPath);
  if (!(file instanceof TFile)) return;
  content = await this.app.vault.read(file);
}
```

<!-- Reconciler API (Plan 02) -->
```typescript
export interface EdgeLabelDiff { edgeId: string; currentLabel: string | undefined; targetLabel: string | undefined; }
export interface ReconcileResult { diffs: EdgeLabelDiff[]; newDisplayLabelByAnswerId: Map<string, string | undefined>; }
export function reconcileEdgeLabels(graph: ProtocolGraph): ReconcileResult;
```

<!-- CanvasLiveEditor API (Plan 03) -->
```typescript
async saveLiveEdges(filePath: string, edgeEdits: Array<{ edgeId: string; label: string | undefined }>): Promise<boolean>
async saveLiveBatch(
  filePath: string,
  nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }>,
  edgeEdits?: Array<{ edgeId: string; label: string | undefined }>
): Promise<boolean>
```

<!-- CanvasParser (pure) -->
```typescript
parser.parse(content: string, filePath: string): ParseResult   // { success: true, graph } | { success: false, error }
```

<!-- Existing Display label block — editor-panel-view.ts:442-451 -->
```typescript
new Setting(container)
  .setName('Display label (optional)')
  .setDesc('Short label shown in the runner button if set. Leave blank to use answer text.')
  .addText(t => {
    t.setValue((nodeRecord['radiprotocol_displayLabel'] as string | undefined) ?? '')
      .onChange(v => {
        this.pendingEdits['radiprotocol_displayLabel'] = v || undefined;
        this.scheduleAutoSave();
      });
  });
```

<!-- Existing saveNodeEdits Pattern B block — editor-panel-view.ts:185-199 -->
```typescript
try {
  const savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, enrichedEdits);
  if (savedLive) {
    return;
  }
} catch (err) {
  console.error('[RadiProtocol] saveLive threw — canvas state rolled back:', err);
  new Notice('Save failed — close the canvas and try again.');
  return;
}
```

<!-- Existing Strategy A node-mutation loop — editor-panel-view.ts:258-265 -->
```typescript
for (const [key, value] of Object.entries(enrichedEdits)) {
  if (PROTECTED_FIELDS.has(key)) continue;
  if (value === undefined) {
    delete node[key];
  } else {
    node[key] = value;
  }
}
```

<!-- main.ts wiring points — main.ts:23 (field), :41 (instantiate canvasLiveEditor), :44 (instantiate canvasNodeFactory), :134-138 (onunload destroys) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EdgeLabelSyncService (vault.on modify + debounced reconcile + write dispatch)</name>
  <files>src/canvas/edge-label-sync-service.ts</files>

  <read_first>
    - `src/views/runner-view.ts:195-230` — registerEvent + vault.on('create'/'delete'/'rename') canonical usage (pattern analog).
    - `src/views/snippet-manager-view.ts:130-185` — vault watcher with debounce + shouldHandle filter.
    - `src/main.ts:316-340` — `handleStartFromNode` live-JSON-or-disk read pattern (copy verbatim для reconcile read path; drop new Notice calls).
    - `src/canvas/canvas-live-editor.ts` (полностью, 231 строка в стайле pre-Plan-03; расширенный Plan 03 — строки 149-266 примерно).
    - `src/graph/edge-label-reconciler.ts` (Plan 02 output — 80-100 строк).
    - `src/graph/canvas-parser.ts:1-30` — для понимания импорта + signature `parse(content, filePath)`.
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §5 (полный EdgeLabelSyncService skeleton + Shared Patterns B, C, D, E, F, G, H).
    - `.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md` D-01, D-02, D-03, D-07, D-08, D-09, D-10, D-13, D-14.
  </read_first>

  <action>

1. Создать новый файл `src/canvas/edge-label-sync-service.ts`. Это новый файл → не применяется Shared Pattern G.

2. Содержимое файла (точная структура; адаптировать под актуальные пути импортов):

```typescript
// src/canvas/edge-label-sync-service.ts
// Phase 50 D-01: owns the vault.on('modify') subscription + debounced reconcile pass
// for bi-directional Answer.displayLabel ↔ incoming Question→Answer edge label sync.
//
// Lifecycle mirrors CanvasLiveEditor (main.ts:23, :41, :135): instantiated in onload,
// destroy() called in onunload. Subscription attached via this.plugin.registerEvent(...)
// so Obsidian auto-detaches on plugin unload (snippet-manager-view.ts:135 /
// runner-view.ts:200 canonical pattern).
//
// Debounce per filePath: 250ms — below Obsidian's own 500ms canvas requestSave cap
// (canvas-live-editor.ts:216) so keystrokes feel responsive. (Claude's Discretion
// per 50-CONTEXT.md; documented constant below.)
//
// D-07 loop guard: reconcileEdgeLabels returns empty result when nothing diverges;
// we short-circuit before any write so the plugin's own write (which triggers a
// follow-up modify event) self-terminates after one reentrant pass. No mutable
// suppress flags or in-flight counters.
//
// Design source: .planning/notes/answer-label-edge-sync.md (Phase 50 D-10 + D-16)

import { TFile, Notice } from 'obsidian';
import type { App, EventRef } from 'obsidian';
import type RadiProtocolPlugin from '../main';
import { CanvasParser } from '../graph/canvas-parser';
import { reconcileEdgeLabels } from '../graph/edge-label-reconciler';
import type { EdgeLabelDiff } from '../graph/edge-label-reconciler';
import type { ProtocolGraph } from '../graph/graph-model';

/** D-Claude: 250ms debounce < Obsidian's 500ms canvas requestSave (canvas-live-editor.ts:216). */
const RECONCILE_DEBOUNCE_MS = 250;

export class EdgeLabelSyncService {
  private readonly app: App;
  private readonly plugin: RadiProtocolPlugin;
  private readonly parser: CanvasParser;
  private readonly debounceTimers = new Map<string, ReturnType<typeof setTimeout>>();
  private eventRef: EventRef | null = null;

  constructor(app: App, plugin: RadiProtocolPlugin) {
    this.app = app;
    this.plugin = plugin;
    this.parser = new CanvasParser();
  }

  /** D-01: subscribe to vault 'modify' events for .canvas files. */
  register(): void {
    this.eventRef = this.plugin.registerEvent(
      this.app.vault.on('modify', (file) => {
        if (!(file instanceof TFile) || file.extension !== 'canvas') return;
        this.scheduleReconcile(file.path);
      })
    );
  }

  private scheduleReconcile(filePath: string): void {
    const existing = this.debounceTimers.get(filePath);
    if (existing !== undefined) clearTimeout(existing);
    const timer = setTimeout(() => {
      this.debounceTimers.delete(filePath);
      void this.reconcile(filePath).catch((err) => {
        // Debug-only telemetry (CONTEXT.md Claude's Discretion — no user-facing Notice).
        console.warn('[RadiProtocol] edge-label reconcile failed:', err);
      });
    }, RECONCILE_DEBOUNCE_MS);
    this.debounceTimers.set(filePath, timer);
  }

  /**
   * D-02: read live JSON (BUG-02/03 avoidance) or disk; parse; reconcile; dispatch write.
   * D-07: empty diffs + empty map → no write (self-terminates the modify-event loop).
   */
  private async reconcile(filePath: string): Promise<void> {
    // Shared Pattern C — live-JSON-or-disk read for background work.
    let content: string;
    const liveJson = this.plugin.canvasLiveEditor.getCanvasJSON(filePath);
    if (liveJson !== null) {
      content = liveJson;
    } else {
      const file = this.app.vault.getAbstractFileByPath(filePath);
      if (!(file instanceof TFile)) return;
      try {
        content = await this.app.vault.read(file);
      } catch {
        return; // silent — reconciler is background
      }
    }

    // Parse (pure)
    const parsed = this.parser.parse(content, filePath);
    if (!parsed.success) return; // broken canvas — nothing sensible to reconcile
    const graph: ProtocolGraph = parsed.graph;

    // Reconcile (pure)
    const { diffs, newDisplayLabelByAnswerId } = reconcileEdgeLabels(graph);

    // D-07 loop guard: nothing diverges → no write.
    if (diffs.length === 0 && newDisplayLabelByAnswerId.size === 0) return;

    // Dispatch write: Pattern B first (saveLiveBatch with node+edge atomicity), Strategy A fallback.
    const nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }> = [];
    for (const [answerId, newLabel] of newDisplayLabelByAnswerId) {
      nodeEdits.push({
        nodeId: answerId,
        edits: { radiprotocol_displayLabel: newLabel }, // undefined → D-08 strip-key symmetry
      });
    }
    const edgeEdits = diffs.map((d: EdgeLabelDiff) => ({
      edgeId: d.edgeId,
      label: d.targetLabel, // undefined → strip-key
    }));

    // Shared Pattern D — Pattern B / Strategy A fork. D-14 atomicity: one setData.
    try {
      const savedLive = await this.plugin.canvasLiveEditor.saveLiveBatch(
        filePath,
        nodeEdits,
        edgeEdits,
      );
      if (savedLive) return;
    } catch (err) {
      console.warn('[RadiProtocol] edge-label saveLiveBatch threw — canvas rolled back:', err);
      return;
    }

    // Strategy A: canvas closed or Pattern B returned false.
    const file = this.app.vault.getAbstractFileByPath(filePath);
    if (!(file instanceof TFile)) return;
    let raw: string;
    try {
      raw = await this.app.vault.read(file);
    } catch {
      return;
    }
    let canvasData: { nodes: Array<Record<string, unknown>>; edges: Array<Record<string, unknown>> };
    try {
      canvasData = JSON.parse(raw) as typeof canvasData;
    } catch {
      return;
    }

    // D-14: single vault.modify mutates both nodes AND edges before the one write.
    for (const { nodeId, edits } of nodeEdits) {
      const node = canvasData.nodes.find(n => n['id'] === nodeId);
      if (!node) continue;
      for (const [k, v] of Object.entries(edits)) {
        if (v === undefined) delete node[k]; // D-08 strip-key
        else node[k] = v;
      }
    }
    for (const { edgeId, label } of edgeEdits) {
      const edge = canvasData.edges.find(e => e['id'] === edgeId);
      if (!edge) continue;
      if (label === undefined) delete edge['label']; // D-08 strip-key
      else edge['label'] = label;
    }
    try {
      await this.app.vault.modify(file, JSON.stringify(canvasData, null, 2));
    } catch {
      // silent — reconciler background; user sees no Notice
    }
  }

  /** T-11-04 timer leak prevention — called from RadiProtocolPlugin.onunload(). */
  destroy(): void {
    for (const [, timer] of this.debounceTimers.entries()) clearTimeout(timer);
    this.debounceTimers.clear();
    // eventRef auto-detached by Plugin lifecycle (registerEvent contract)
  }
}

// Shared helper exposed for Node Editor Display-label outbound sync (D-06 / D-14).
// Given a live or on-disk canvas JSON + the edited Answer node id + the new label,
// enumerate every incoming Question→Answer edgeId and produce edgeEdits for
// saveLiveBatch. Parses with CanvasParser so reverseAdjacency is available.
export function collectIncomingEdgeEdits(
  parser: CanvasParser,
  canvasContent: string,
  canvasFilePath: string,
  answerId: string,
  newLabel: string | undefined,
): Array<{ edgeId: string; label: string | undefined }> {
  const parsed = parser.parse(canvasContent, canvasFilePath);
  if (!parsed.success) return [];
  const { graph } = parsed;
  const answerNode = graph.nodes.get(answerId);
  if (!answerNode || answerNode.kind !== 'answer') return [];
  const incoming = graph.edges.filter(e => e.toNodeId === answerId);
  return incoming.map(e => ({ edgeId: e.id, label: newLabel }));
}

// Suppress unused Notice import warning if the file doesn't use Notice elsewhere —
// imported defensively as future maintenance anchor. Remove the import if eslint
// complains in the build pipeline.
void Notice;
```

   **Пометка**: `void Notice;` внизу файла — намеренный приём, если eslint на `noUnusedImport` жалуется. Если в CI/типах нет такой проверки, можно удалить как import `Notice`, так и `void Notice;`. Наличие `Notice` не требуется семантикой сервиса (все ошибки тихие, console.warn only).

3. Проверить `npx tsc --noEmit --skipLibCheck` → exit 0. Если TS жалуется на `import type RadiProtocolPlugin from '../main'` — использовать circular-dep-safe вариант: `import type { default as RadiProtocolPlugin } from '../main';` или тайпкаст через минимальный interface `{ registerEvent(ref: EventRef): EventRef; canvasLiveEditor: CanvasLiveEditor; app: App }`. В случае сомнений — выбрать минимальный-interface вариант (избегаем circular type-only import).
  </action>

  <verify>
    <automated>npx tsc --noEmit --skipLibCheck</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `src/canvas/edge-label-sync-service.ts` существует.
    - [ ] `grep -c "export class EdgeLabelSyncService" src/canvas/edge-label-sync-service.ts` → 1.
    - [ ] `grep -c "vault.on('modify'" src/canvas/edge-label-sync-service.ts` → 1.
    - [ ] `grep -c "this.plugin.registerEvent" src/canvas/edge-label-sync-service.ts` → 1 (D-01 auto-detach).
    - [ ] `grep -c "RECONCILE_DEBOUNCE_MS = 250" src/canvas/edge-label-sync-service.ts` → 1 (250ms constant documented).
    - [ ] `grep -c "reconcileEdgeLabels" src/canvas/edge-label-sync-service.ts` → ≥1 (reconciler wired).
    - [ ] `grep -c "saveLiveBatch" src/canvas/edge-label-sync-service.ts` → 1 (Pattern B with edgeEdits — D-14).
    - [ ] `grep -c "diffs.length === 0 && newDisplayLabelByAnswerId.size === 0" src/canvas/edge-label-sync-service.ts` → 1 (D-07 loop guard).
    - [ ] `grep -c "delete node\[k\]" src/canvas/edge-label-sync-service.ts` → 1 (Strategy A D-08 strip-key for nodes).
    - [ ] `grep -c "delete edge\['label'\]" src/canvas/edge-label-sync-service.ts` → 1 (Strategy A D-08 strip-key for edges).
    - [ ] `grep -c "answer-label-edge-sync.md" src/canvas/edge-label-sync-service.ts` → ≥1 (D-10 + D-16 canonical ref).
    - [ ] `grep -c "export function collectIncomingEdgeEdits" src/canvas/edge-label-sync-service.ts` → 1 (shared helper для Task 3).
    - [ ] `grep -c "destroy()" src/canvas/edge-label-sync-service.ts` → 1 (timer cleanup).
    - [ ] `npx tsc --noEmit --skipLibCheck` exit 0.
    - [ ] `npm test` — все тесты Plans 01-03 остаются зелёными (никакие существующие тесты не сломались).
  </acceptance_criteria>

  <done>
    EdgeLabelSyncService существует и компилируется. Task 2 wire-up'ит его в main.ts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire EdgeLabelSyncService in main.ts (3 append-only edits)</name>
  <files>src/main.ts</files>

  <read_first>
    - `src/main.ts` (полный файл, строки 1-138 минимум — узнать точное расположение: имена instance-полей ~22-25, instantiate-ряд в onload ~41-44, onunload destroy-ряд 134-138).
    - `CLAUDE.md` Shared Pattern G — `main.ts` **прямо назван в CLAUDE.md**. Append-only, НЕ трогать: service instantiations, commands, view registrations, ribbon, settings tab, canvas:node-menu handler, onunload destroys для прошлых сервисов.
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §5 «main.ts wire-up — exactly 3 added lines (append-only, no deletions)».
  </read_first>

  <action>

1. **Открыть `src/main.ts` и прочитать полностью** (не только первые 150 строк — убедиться, что после `onunload` нет других методов, или знать структуру). Подтвердить актуальные номера строк для трёх правок.

2. **Правка 1: добавить import.** В начале файла среди существующих imports (вокруг строк 12-16) добавить одну строку:
   ```typescript
   import { EdgeLabelSyncService } from './canvas/edge-label-sync-service';
   ```
   Расположить в порядке соседних imports (например, после `import { CanvasLiveEditor } from './canvas/canvas-live-editor';`).

3. **Правка 2: добавить поле в class.** В блок полей class (вокруг строк 19-25, после `canvasNodeFactory!: CanvasNodeFactory;`) добавить одну строку:
   ```typescript
   edgeLabelSyncService!: EdgeLabelSyncService;
   ```

4. **Правка 3: instantiate + register в `onload`.** После существующей строки `this.canvasNodeFactory = new CanvasNodeFactory(this.app);` (~строка 44) добавить три строки:
   ```typescript

       // Phase 50 D-01: own the vault.on('modify') subscription for bi-directional
       // Answer.displayLabel ↔ incoming-edge label sync. Design source:
       // .planning/notes/answer-label-edge-sync.md
       this.edgeLabelSyncService = new EdgeLabelSyncService(this.app, this);
       this.edgeLabelSyncService.register();
   ```

5. **Правка 4: destroy в `onunload`.** В методе `onunload` (вокруг строк 134-138), внутри тела, после существующей строки `this.canvasNodeFactory.destroy();` и перед `console.debug(...)` добавить:
   ```typescript
       this.edgeLabelSyncService.destroy();
   ```

6. Проверки:
   - НЕ удалены: settings init (27-29), pure-modules init (31-32), services init (35-38), `canvasLiveEditor` init (40-41), `canvasNodeFactory` init (43-44), `addRibbonIcon` (46), все `addCommand` блоки (49-91), `registerView` блоки (64-70), settings tab (94), `canvas:node-menu` registerEvent блок (101-129), `console.debug('[RadiProtocol] Plugin loaded')` в конце onload, `canvasLiveEditor.destroy()` + `canvasNodeFactory.destroy()` в onunload, `console.debug('[RadiProtocol] Plugin unloaded')` в конце onunload.
   - Ни один метод ниже `onunload` не затронут (activateEditorPanelView, activateRunnerView, handleStartFromNode и т.д.).

7. Прогнать `npx tsc --noEmit --skipLibCheck` → exit 0.

8. Прогнать `npm run build` → exit 0 (эта правка трогает bundle entry point, поэтому стоит убедиться в build-success, даже если в Task 4 UAT-плана build запускается ещё раз).
  </action>

  <verify>
    <automated>npx tsc --noEmit --skipLibCheck && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -c "import { EdgeLabelSyncService }" src/main.ts` → 1.
    - [ ] `grep -c "edgeLabelSyncService!: EdgeLabelSyncService" src/main.ts` → 1.
    - [ ] `grep -c "new EdgeLabelSyncService" src/main.ts` → 1.
    - [ ] `grep -c "this.edgeLabelSyncService.register()" src/main.ts` → 1.
    - [ ] `grep -c "this.edgeLabelSyncService.destroy()" src/main.ts` → 1.
    - [ ] `grep -c "answer-label-edge-sync.md" src/main.ts` → ≥1 (canonical ref в комментарии перед instantiate).
    - [ ] `grep -c "this.canvasLiveEditor.destroy()" src/main.ts` → 1 (prior destroy preserved verbatim).
    - [ ] `grep -c "this.canvasNodeFactory.destroy()" src/main.ts` → 1 (prior destroy preserved verbatim).
    - [ ] `grep -c "console.debug\('\\[RadiProtocol\\] Plugin loaded'\)" src/main.ts` → 1 (prior log preserved).
    - [ ] `grep -c "console.debug\('\\[RadiProtocol\\] Plugin unloaded'\)" src/main.ts` → 1 (prior log preserved).
    - [ ] `grep -c "canvas:node-menu" src/main.ts` → 1 (Phase 4 handler preserved byte-for-byte).
    - [ ] `grep -c "handleStartFromNode" src/main.ts` → ≥1 (Phase 45 command preserved).
    - [ ] `npx tsc --noEmit --skipLibCheck` exit 0.
    - [ ] `npm run build` exit 0.
    - [ ] **Shared Pattern G**: diff `src/main.ts` показывает ТОЛЬКО: 1 новый import, 1 новое поле, 1 новый instantiate-блок (3-5 строк с комментарием), 1 новый destroy-ряд. Никакие rules/functions/event-listeners из предыдущих фаз не удалены (diff показывает только additions и targeted in-place правки, без unrelated deletions).
    - [ ] Никаких CSS-изменений.
  </acceptance_criteria>

  <done>
    Сервис зарегистрирован в plugin lifecycle. vault.on('modify') подписка работает; при закрытии плагина auto-detach через registerEvent + destroy timers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire editor-panel-view.ts Display label onChange for atomic node+edge write (D-06 + D-14)</name>
  <files>src/views/editor-panel-view.ts</files>

  <read_first>
    - `src/views/editor-panel-view.ts` — прочитать полностью или как минимум:
      * блок `saveNodeEdits` строки 165-274;
      * Display label Setting строки 442-451 (Phase 48 NODEUI-03 comment + onChange handler);
      * imports в начале файла (узнать: импортирован ли уже CanvasParser или нет).
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §4 (весь блок + Shared Patterns D/E/H) — **это самый ограниченный файл**: CLAUDE.md назван поимённо.
    - `.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md` D-06, D-08, D-13, D-14.
    - `src/canvas/edge-label-sync-service.ts` (Task 1 output — узнать API `collectIncomingEdgeEdits`).
    - `CLAUDE.md` Shared Pattern G applied to editor-panel-view.ts — НЕ ТРОГАТЬ: `attachCanvasListener`, `handleNodeClick`, `onQuickCreate`, `onDuplicate`, `renderToolbar`, `listSnippetSubfolders`, `buildKindForm` ветки `question`/`text-block`/`loop`/`snippet`/`loop-start`/`loop-end`/`start`, `renderForm` структурные каркасы, любой form-field handler КРОМЕ Display label. `saveNodeEdits` — разрешено расширять ТОЛЬКО веткой для displayLabel.
  </read_first>

  <action>

**Scope:** три точечных изменения в `editor-panel-view.ts`:

1. **Добавить import `collectIncomingEdgeEdits`** из edge-label-sync-service.
2. **Добавить code-comment со ссылкой на canonical source** над Display label `new Setting(...)` на строке 442.
3. **Расширить `saveNodeEdits`** — при наличии `radiprotocol_displayLabel` в edits использовать `saveLiveBatch(... , edgeEdits)` (Pattern B) или мутировать edges в том же `vault.modify()` payload (Strategy A).

### Шаг-за-шагом

**Шаг A — Imports.** В блоке imports в начале файла (найти последний import) добавить **одну строку**:
```typescript
// Phase 50 D-14: enumerates incoming Question→Answer edges for atomic Node Editor write
import { collectIncomingEdgeEdits } from '../canvas/edge-label-sync-service';
```

Если `CanvasParser` ещё не импортирован в editor-panel-view.ts — добавить **одну строку**:
```typescript
import { CanvasParser } from '../graph/canvas-parser';
```
(Иначе — не добавлять, использовать существующий import.)

**Шаг B — Code-comment у Display label Setting (D-10).** ПЕРЕД строкой 441 `// Phase 48 NODEUI-03: Display label renders BEFORE Answer text (swapped from original order).` добавить **новый** многострочный комментарий:
```typescript
// Phase 50 D-10: Answer.displayLabel is the single source of truth for every
// incoming Question→Answer edge label. Multi-incoming Answer nodes share ONE
// label across all incoming edges — per-edge override is explicitly out of scope
// for v1.8. Writes flow through saveNodeEdits (D-14 atomic batch): node
// radiprotocol_displayLabel + every incoming edge.label land in ONE saveLiveBatch
// or ONE vault.modify call. Undefined displayLabel strips the 'label' key on
// every incoming edge (D-08 symmetry with canvas-parser.ts:207-209).
// Design source: .planning/notes/answer-label-edge-sync.md
```

Существующие строки 442-451 (`new Setting(container).setName('Display label (optional)').../* onChange handler */`) — оставить **character-for-character идентичными**. `this.pendingEdits['radiprotocol_displayLabel'] = v || undefined;` — сохранить verbatim (это канонический undefined-deletes-key, PATTERNS.md §4 явно подтверждает).

`scheduleAutoSave()` — НЕ заменять на direct save. 800ms debounce в `scheduleAutoSave → saveNodeEdits` pipeline остаётся тем же.

**Шаг C — расширение `saveNodeEdits` для atomic node+edges write.**

Цель: когда `enrichedEdits` содержит ключ `'radiprotocol_displayLabel'`, обе ветки (Pattern B и Strategy A) должны обработать edges ВМЕСТЕ с node в одном write.

**Scope edit 1 — Pattern B branch.** В блоке строк 185-199 заменить **один вызов** `saveLive(...)` на раздвоенную ветку: если displayLabel в edits → `saveLiveBatch` с edgeEdits, иначе как сейчас. Точечная правка:

Текущий код (строки 187-191):
```typescript
    try {
      const savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, enrichedEdits);
      if (savedLive) {
        return;
      }
    } catch (err) {
```
Заменить **только эти 5 строк** на:
```typescript
    try {
      // Phase 50 D-14: when displayLabel is in edits, read live-or-disk canvas JSON
      // to enumerate incoming Question→Answer edges, then commit node + edges in
      // ONE saveLiveBatch call. Otherwise fall back to the Phase 28 saveLive path.
      const isDisplayLabelEdit = 'radiprotocol_displayLabel' in enrichedEdits;
      let savedLive: boolean;
      if (isDisplayLabelEdit) {
        const newLabel = enrichedEdits['radiprotocol_displayLabel'] as string | undefined;
        const liveJson = this.plugin.canvasLiveEditor.getCanvasJSON(filePath);
        const canvasContent = liveJson ?? await (async () => {
          const f = this.plugin.app.vault.getAbstractFileByPath(filePath);
          if (!(f instanceof TFile)) return '';
          try { return await this.plugin.app.vault.read(f); } catch { return ''; }
        })();
        const parser = new CanvasParser();
        const edgeEdits = collectIncomingEdgeEdits(parser, canvasContent, filePath, nodeId, newLabel);
        savedLive = await this.plugin.canvasLiveEditor.saveLiveBatch(
          filePath,
          [{ nodeId, edits: enrichedEdits }],
          edgeEdits,
        );
      } else {
        savedLive = await this.plugin.canvasLiveEditor.saveLive(filePath, nodeId, enrichedEdits);
      }
      if (savedLive) {
        return;
      }
    } catch (err) {
```

Остальная часть catch-блока (строки 192-197, включая `console.error`, `new Notice`, `return`) — сохранить verbatim.

**Scope edit 2 — Strategy A branch (D-13).** После существующей node-mutation loop (строки 258-266 `for (const [key, value] of Object.entries(enrichedEdits))`) и ПЕРЕД финальным `vault.modify` вызовом (строка 269-273), добавить блок мутации edges:

```typescript

    // Phase 50 D-13/D-14: when displayLabel is in edits, mutate every incoming
    // Question→Answer edge label in the SAME canvasData payload — one vault.modify
    // writes node + edges atomically (avoids WR-01 race). Symmetric to the node
    // mutation above: undefined ≡ delete 'label' key (D-08, canvas-parser.ts:207-209).
    if ('radiprotocol_displayLabel' in enrichedEdits) {
      const newLabel = enrichedEdits['radiprotocol_displayLabel'] as string | undefined;
      const parser = new CanvasParser();
      const edgeEdits = collectIncomingEdgeEdits(parser, raw, filePath, nodeId, newLabel);
      const incomingIds = new Set(edgeEdits.map(e => e.edgeId));
      for (const edge of canvasData.edges) {
        const edgeObj = edge as Record<string, unknown>;
        if (!incomingIds.has(edgeObj['id'] as string)) continue;
        if (newLabel === undefined) {
          delete edgeObj['label']; // D-08 strip-key
        } else {
          edgeObj['label'] = newLabel;
        }
      }
    }
```

**Важно**: `raw` здесь — тот же `raw`, что был прочитан через `vault.read(file)` в строке 209. Использовать именно его, чтобы edgeEdits вычислялись из того же снапшота, который сейчас парсится/мутируется (нет двойного чтения). `canvasData.edges` — уже typed as `unknown[]` в местном interface на строке 215, cast через `as Record<string, unknown>` — уже принятый паттерн в этом файле.

**НЕ трогать** никакой другой код в `saveNodeEdits` — isUnmarking branch (249-256), Phase 28 color-injection (177-183 + 229-240), PROTECTED_FIELDS loop (258-265), final `vault.modify` call (269-273). Все они preserved verbatim.

**Шаг D** — прогнать `npx tsc --noEmit --skipLibCheck` → exit 0.

**Шаг E** — прогнать `npm test` (полный набор). Ожидание: существующие тесты (включая `canvas-write-back.test.ts`'s 10 prior + Phase 50 appended) — все зелёные. **Важно**: если какие-то prior tests в `canvas-write-back.test.ts` пишут displayLabel БЕЗ incoming edges (что раньше шло через `saveLive`), они теперь пойдут через `saveLiveBatch` с `edgeEdits = []`. Это не должно их сломать, т.к. Plan 03 Task 1 прописал «empty edgeEdits → no-op» в `saveLiveBatch`. Но если конкретный prior test ожидает `saveLive` (не `saveLiveBatch`) вызов — этот тест теперь красный. В таком случае: **НЕ менять существующий тест**, а задокументировать в SUMMARY как expected semantic shift (Phase 50 D-14 replaces single-node displayLabel write with batch — backward-compatible but different mock-spy target). Если при этом тест использует fall-through в `vault.modify` (как `'undefined values delete the key from the node'` на строках 143-157), он должен продолжать работать, т.к. mock Pattern B возвращает false при отсутствии leaf. Проверить явно.
  </action>

  <verify>
    <automated>npx tsc --noEmit --skipLibCheck && npm test</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -c "from '../canvas/edge-label-sync-service'" src/views/editor-panel-view.ts` → 1.
    - [ ] `grep -c "collectIncomingEdgeEdits" src/views/editor-panel-view.ts` → ≥2 (import + ≥2 call-sites в Pattern B и Strategy A branches).
    - [ ] `grep -c "Phase 50 D-10" src/views/editor-panel-view.ts` → 1 (canonical ref comment над Display label Setting).
    - [ ] `grep -c "answer-label-edge-sync.md" src/views/editor-panel-view.ts` → 1 (в том же D-10 комментарии).
    - [ ] `grep -c "Phase 50 D-14" src/views/editor-panel-view.ts` → ≥2 (Pattern B branch + Strategy A branch).
    - [ ] `grep -c "isDisplayLabelEdit" src/views/editor-panel-view.ts` → ≥2 (declare + check).
    - [ ] `grep -c "saveLiveBatch" src/views/editor-panel-view.ts` → ≥1 (atomic node+edge Pattern B call).
    - [ ] `grep -c "delete edgeObj\['label'\]" src/views/editor-panel-view.ts` → 1 (Strategy A D-08 strip-key).
    - [ ] `grep -c "this.pendingEdits\['radiprotocol_displayLabel'\] = v || undefined" src/views/editor-panel-view.ts` → 1 (PRIOR onChange handler preserved verbatim — Phase 48 NODEUI-03 invariant).
    - [ ] `grep -c "Phase 48 NODEUI-03" src/views/editor-panel-view.ts` → 1 (prior Phase 48 comment preserved).
    - [ ] `grep -c "async saveLive(filePath, nodeId, enrichedEdits)" src/views/editor-panel-view.ts` → 0 OR 1 — если 1, значит saveLive остался для не-displayLabel edits (ожидаемый else-branch). Если 0, значит весь Pattern B вызов switched to saveLiveBatch, что тоже допустимо семантически (empty edgeEdits = no-op). Допустимые варианты обсудить в SUMMARY.
    - [ ] `grep -c "new Notice('Save failed" src/views/editor-panel-view.ts` → 1 (prior error-notice preserved).
    - [ ] `grep -c "vault.modify(file as TFile" src/views/editor-panel-view.ts` → 1 (prior Strategy A write preserved — ONE vault.modify call; D-14 atomicity).
    - [ ] `npx tsc --noEmit --skipLibCheck` exit 0.
    - [ ] `npm test` — все тесты зелёные (включая Phase 50 Plan 03 Task 2 и все prior).
    - [ ] **Shared Pattern G (CLAUDE.md — prime example)**: diff `src/views/editor-panel-view.ts` касается только двух блоков: `saveNodeEdits` (строки 165-274) и Display label Setting (442-451) + двух новых import-строк в начале. Никакая функция, listener или rule вне этих диапазонов НЕ модифицирована, НЕ переупорядочена, НЕ удалена. Никакие rules/functions/event-listeners из предыдущих фаз не удалены.
    - [ ] Никаких CSS-изменений.
  </acceptance_criteria>

  <done>
    Display label handler теперь пишет node.displayLabel + каждое incoming edge.label в одном atomic write (Pattern B: `saveLiveBatch`; Strategy A: single `vault.modify` с pre-mutated edges). D-14 invariant enforced. Code-comment закрепляет D-10 multi-incoming trade-off ссылкой на canonical source.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --skipLibCheck` exit 0.
- `npm run build` exit 0.
- `npm test` — все тесты passed (baseline Phase 49 + Phase 50 Plans 01-03 + ничего не сломано Plan 04 wire-up).
- Подписка на vault.on('modify') видна через `grep`: `grep -rn "vault.on('modify'" src/` → ровно 1 hit в `src/canvas/edge-label-sync-service.ts`.
- `grep -rn "edgeLabelSyncService" src/` → ≥4 hits (field, instantiate, register, destroy в main.ts).
- Shared Pattern G для ВСЕХ трёх shared files (`main.ts`, `editor-panel-view.ts`, и — косвенно — `canvas-live-editor.ts` из Plan 03): diff показывает ТОЛЬКО targeted additions / in-place правки в явно разрешённых scope, никаких unrelated deletions.
- `grep -c "answer-label-edge-sync.md"` across Phase 50 files (`edge-label-sync-service.ts`, `edge-label-reconciler.ts`, `main.ts`, `editor-panel-view.ts`, `types/canvas-internal.d.ts`, `canvas-live-editor.ts`) → ≥6 (canonical ref зацементирован по D-10/D-16).
</verification>

<success_criteria>
Bi-directional sync работает: (1) canvas-side edit label → vault.on('modify') → debounced reconcile → saveLiveBatch с edge-updates + displayLabel update; (2) Node Editor Display label change → saveNodeEdits → saveLiveBatch с node+edges в одном write. D-07 loop guard: follow-up modify event после plugin's own write находит diff=∅ и завершается без дальнейшей записи.
</success_criteria>

<output>
After completion, create `.planning/phases/50-answer-edge-label-sync/50-04-SUMMARY.md`
</output>
