---
phase: 50-answer-edge-label-sync
plan: 03
type: tdd
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/canvas/canvas-live-editor.ts
  - src/__tests__/canvas-write-back.test.ts
requirements: [EDGE-02]
autonomous: true

must_haves:
  truths:
    - "CanvasLiveEditor.saveLiveEdges(filePath, edgeEdits) exists and applies edge-label edits via Pattern B (getData → mutate → setData → debouncedRequestSave)"
    - "saveLiveBatch's signature accepts an optional 3rd param edgeEdits (back-compat: existing call-sites unchanged)"
    - "Undefined label deletes the 'label' key on the edge JSON (D-08 symmetry with canvas-parser.ts:207-209)"
    - "Rollback-on-throw: if setData throws, prior data is restored; rollback failure is console.error'd (Shared Pattern F)"
    - "First-pass-validate on saveLiveBatch: if any target edgeId is missing, the call bails with false BEFORE any mutation (mirrors existing node first-pass, line 161-165)"
    - "D-14 atomicity: saveLiveBatch with both nodeEdits + edgeEdits writes them inside ONE setData + ONE debouncedRequestSave call — never two writes"
  artifacts:
    - path: "src/canvas/canvas-live-editor.ts"
      provides: "saveLiveEdges method + saveLiveBatch extended with edgeEdits param"
      contains: "async saveLiveEdges("
    - path: "src/__tests__/canvas-write-back.test.ts"
      provides: "Round-trip tests for edge-label write (D-14 atomic + D-08 strip-key)"
      contains: "PHASE-50"
  key_links:
    - from: "src/canvas/canvas-live-editor.ts"
      to: "src/types/canvas-internal.d.ts (CanvasEdgeData)"
      via: "typed edge access via CanvasData.edges (now CanvasEdgeData[])"
      pattern: "updatedData\\.edges"
---

<objective>
Расширить `CanvasLiveEditor` двумя операциями, необходимыми для D-12 / D-14:

1. **Новый метод `saveLiveEdges(filePath, edgeEdits)`** — Pattern B для одних только edge-label изменений. Нужен reconciler'у (Plan 04) в случае, когда выявлен edge-diff без сопутствующего Answer.displayLabel change (или когда displayLabel handler не задействован).
2. **Расширение `saveLiveBatch`** — добавить optional 3-й параметр `edgeEdits`, чтобы Node Editor мог писать `radiprotocol_displayLabel` на Answer-ноде И label на каждом incoming edge в **одном** `setData + debouncedRequestSave` (D-14 atomicity — предотвращает WR-01 гонку).

Также расширить `canvas-write-back.test.ts` двумя интеграционными тестами (D-14 atomicity + D-08 strip-key symmetry).

Purpose: закрыть writer-сторону Волны 2 (prerequisite для Plan 04 wire-up). Без этого reconciler не сможет писать на диск.
Output: расширенный `canvas-live-editor.ts` (append-only: один новый метод + опциональный параметр у существующего метода) + 2 новых интеграционных теста.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md
@.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md

<interfaces>
<!-- Existing saveLive signature — src/canvas/canvas-live-editor.ts:75-79 -->
```typescript
async saveLive(
  filePath: string,
  nodeId: string,
  edits: Record<string, unknown>
): Promise<boolean>
```

<!-- Existing saveLiveBatch signature — src/canvas/canvas-live-editor.ts:149-152 -->
```typescript
async saveLiveBatch(
  filePath: string,
  nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }>
): Promise<boolean>
```

<!-- Rollback-on-throw template — src/canvas/canvas-live-editor.ts:120-132 (copy verbatim) -->
```typescript
try {
  view.canvas.setData(updatedData);
  this.debouncedRequestSave(filePath, view);
  return true;
} catch (err) {
  try {
    view.canvas.setData(originalData);
  } catch (rollbackErr) {
    console.error('[RadiProtocol] Canvas rollback failed — canvas may be in inconsistent state:', rollbackErr);
  }
  throw err;
}
```

<!-- debouncedRequestSave — src/canvas/canvas-live-editor.ts:210-218 (reuse as-is; 500ms per-file debounce) -->

<!-- CanvasData.edges (after Plan 01): CanvasEdgeData[] — so updatedData.edges[k].label is typed string | undefined -->
```typescript
export interface CanvasEdgeData {
  id: string;
  fromNode: string;
  toNode: string;
  label?: string;
  [key: string]: unknown;
}
```

<!-- Existing canvas-write-back.test.ts undefined-deletes-key pattern — lines 143-157 -->
```typescript
it('undefined values delete the key from the node', async () => {
  mockVaultRead.mockResolvedValue(
    makeCanvasJson({ radiprotocol_displayLabel: 'old-label' })
  );
  await view.saveNodeEdits('test.canvas', 'node-1', {
    radiprotocol_displayLabel: undefined,
  });
  expect(mockVaultModify).toHaveBeenCalled();
  const written = JSON.parse(mockVaultModify.mock.calls[0]![1] as string) as {
    nodes: Array<Record<string, unknown>>;
  };
  const node = written.nodes[0];
  expect(node).not.toHaveProperty('radiprotocol_displayLabel');
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add saveLiveEdges + extend saveLiveBatch with edgeEdits param</name>
  <files>src/canvas/canvas-live-editor.ts</files>

  <read_first>
    - `src/canvas/canvas-live-editor.ts` (полный файл, 231 строка — прочитать целиком; это shared file, KNOW ЧТО НЕ ТРОГАТЬ)
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §2 (verbatim signatures + rollback + batch first-pass-validate blocks)
    - `.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md` D-12, D-13, D-14
    - `src/types/canvas-internal.d.ts` (после Plan 01 — убедиться, что `CanvasEdgeData` доступен)
    - `CLAUDE.md` Shared Pattern G — `canvas-live-editor.ts` — **shared file**, append-only, НЕ переписывать `saveLive` / `saveLiveBatch` / `debouncedRequestSave` / `destroy` / `getCanvasView` / `isLiveAvailable` / `getCanvasJSON`. Единственная разрешённая in-place правка — добавление optional 3-го параметра к `saveLiveBatch` + обработка его внутри того же метода.
  </read_first>

  <behavior>
**New public method: `saveLiveEdges`**

Signature:
```typescript
async saveLiveEdges(
  filePath: string,
  edgeEdits: Array<{ edgeId: string; label: string | undefined }>
): Promise<boolean>
```

Contract:
1. Return `true` on success (view.canvas.setData succeeded, requestSave debounced).
2. Return `false` without mutation if:
   - Canvas view is not available (`getCanvasView(filePath) === undefined`), или
   - Any `edgeId` в `edgeEdits` не найден в `updatedData.edges`.
3. Empty `edgeEdits` array → return `true` immediately (no-op; mirrors `saveLiveBatch` line 155).
4. For each `{ edgeId, label }`:
   - Найти `edge = updatedData.edges.find(e => e.id === edgeId)` (now typed `CanvasEdgeData`). Если не найден → return `false`.
   - Если `label === undefined` → `delete (edge as Record<string, unknown>)['label']` (D-08 strip-key symmetry).
   - Иначе `(edge as Record<string, unknown>)['label'] = label`.
5. Apply first-pass-validate (bail before mutation) + second-pass-mutate pattern mirroring `saveLiveBatch:160-166`.
6. Rollback-on-throw: mirror Shared Pattern F (Shared Pattern F block in PATTERNS.md §2).

**Extended `saveLiveBatch` signature (back-compat):**
```typescript
async saveLiveBatch(
  filePath: string,
  nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }>,
  edgeEdits?: Array<{ edgeId: string; label: string | undefined }>   // NEW — Phase 50 D-14
): Promise<boolean>
```

Contract changes:
1. Signature gets optional 3rd param — existing call-sites unchanged (`saveLiveBatch(filePath, nodeEdits)` continues to work).
2. If `edgeEdits === undefined` OR `edgeEdits.length === 0` → поведение бит-в-бит идентично предыдущему (node-only code-path работает as-is).
3. Иначе: first-pass validates BOTH node targets AND edge targets. Если ЛЮБОЙ edgeId не найден → return false без мутации (до `setData`).
4. Если и nodeEdits и edgeEdits пустые (`nodeEdits.length === 0 && (edgeEdits === undefined || edgeEdits.length === 0)`) — возвращаем `true` сразу (no-op).
5. Second-pass: сначала применяется mutation на ноды (как сейчас, строки 168-191), потом на edges (new loop):
   ```typescript
   // Phase 50 D-14: apply edge mutations in the SAME updatedData copy so the
   // single setData call below commits node + edge changes atomically.
   if (edgeEdits) {
     for (const { edgeId, label } of edgeEdits) {
       const edge = updatedData.edges.find((e) => e.id === edgeId);
       if (!edge) continue; // first-pass already validated — defensive no-op
       if (label === undefined) {
         delete (edge as Record<string, unknown>)['label']; // D-08 strip-key
       } else {
         (edge as Record<string, unknown>)['label'] = label;
       }
     }
   }
   ```
6. setData + rollback-on-throw block at 193-204 stays character-for-character identical — ONE setData, ONE debouncedRequestSave ≡ D-14 atomicity.
  </behavior>

  <action>

1. **Открыть `src/canvas/canvas-live-editor.ts` и прочитать полностью** (231 строка). Подтвердить Shared Pattern G touchpoints:
   - `getCanvasView` (29-47) — НЕ трогать.
   - `isLiveAvailable` (52-54) — НЕ трогать.
   - `getCanvasJSON` (64-68) — НЕ трогать.
   - `saveLive` (75-133) — НЕ трогать.
   - `saveLiveBatch` (149-205) — ТОЛЬКО добавить 3-й optional параметр и блок обработки edges в second-pass.
   - `debouncedRequestSave` (210-218) — НЕ трогать.
   - `destroy` (224-229) — НЕ трогать.

2. **Правка 1: расширить `saveLiveBatch` сигнатуру.**

   **Точечно заменить** только строки 149-152 — сигнатуру:
   ```typescript
   async saveLiveBatch(
     filePath: string,
     nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }>
   ): Promise<boolean> {
   ```
   на:
   ```typescript
   async saveLiveBatch(
     filePath: string,
     nodeEdits: Array<{ nodeId: string; edits: Record<string, unknown> }>,
     // Phase 50 D-14: optional edge-label edits applied in the SAME Pattern B
     // write so node + incoming-edge updates land in one setData/requestSave
     // cycle. undefined label ≡ delete the 'label' key (D-08 symmetry with
     // canvas-parser.ts:207-209). Design source: .planning/notes/answer-label-edge-sync.md
     edgeEdits?: Array<{ edgeId: string; label: string | undefined }>
   ): Promise<boolean> {
   ```

3. **Правка 2: no-op short-circuit для пустых edits.**

   Текущая строка 155 — `if (nodeEdits.length === 0) return true;`. Заменить **одну эту строку** на:
   ```typescript
   if (nodeEdits.length === 0 && (!edgeEdits || edgeEdits.length === 0)) return true;
   ```
   (Это одна in-place правка с теми же семантиками для старых callers — edgeEdits по умолчанию undefined, поэтому `!edgeEdits` = true, т.е. поведение строго идентично при node-only вызове.)

4. **Правка 3: first-pass-validate edges** — добавить блок СРАЗУ ПОСЛЕ существующего first-pass node-loop (после строки 166, перед «Second pass: apply edits…» комментарием):
   ```typescript

   // Phase 50 D-14: first-pass validate edge targets — bail without mutating
   // if any edgeId is missing (mirrors node-target validation above).
   if (edgeEdits) {
     for (const { edgeId } of edgeEdits) {
       const edge = updatedData.edges.find((e) => e.id === edgeId);
       if (!edge) return false;
     }
   }
   ```

5. **Правка 4: second-pass mutate edges** — добавить блок ПОСЛЕ существующего second-pass node-loop (после строки 191 — перед try-блоком на 193):
   ```typescript

   // Phase 50 D-14: apply edge mutations to updatedData. Same setData call
   // below commits node + edge changes atomically — NEVER split into two
   // setData/requestSave cycles (WR-01 doc-comment lines 138-148).
   if (edgeEdits) {
     for (const { edgeId, label } of edgeEdits) {
       const edge = updatedData.edges.find((e) => e.id === edgeId);
       if (!edge) continue; // first-pass already rejected; defensive
       if (label === undefined) {
         delete (edge as Record<string, unknown>)['label']; // D-08 strip-key
       } else {
         (edge as Record<string, unknown>)['label'] = label;
       }
     }
   }
   ```

6. **Правка 5: добавить новый метод `saveLiveEdges` после `saveLiveBatch` и перед `debouncedRequestSave`.** Место — между закрывающей `}` метода `saveLiveBatch` (строка 205) и открывающим doc-комментарием `debouncedRequestSave` (строка 207). Вставить:

   ```typescript

   /**
    * Phase 50 D-12: Pattern B write for edge-label-only changes.
    *
    * Same getData → mutate → setData → debouncedRequestSave pattern as saveLive
    * (lines 75-133) and saveLiveBatch (lines 149-205). Used by the modify-event
    * reconciler (EdgeLabelSyncService) when a canvas-side edge edit has to be
    * re-synced onto OTHER incoming edges of the same Answer, without touching
    * any Answer node's radiprotocol_* fields.
    *
    * When an Answer's radiprotocol_displayLabel also needs updating, callers
    * MUST use saveLiveBatch with edgeEdits instead — D-14 atomicity.
    *
    * undefined label ≡ delete 'label' key (D-08 symmetry with
    * canvas-parser.ts:207-209).
    *
    * Design source: .planning/notes/answer-label-edge-sync.md
    */
   async saveLiveEdges(
     filePath: string,
     edgeEdits: Array<{ edgeId: string; label: string | undefined }>
   ): Promise<boolean> {
     const view = this.getCanvasView(filePath);
     if (!view) return false;
     if (edgeEdits.length === 0) return true;

     const originalData: CanvasData = view.canvas.getData();
     const updatedData: CanvasData = view.canvas.getData();

     // First pass: locate every target edge; bail without mutating if any missing
     for (const { edgeId } of edgeEdits) {
       const edge = updatedData.edges.find((e) => e.id === edgeId);
       if (!edge) return false;
     }

     // Second pass: apply label edits
     for (const { edgeId, label } of edgeEdits) {
       const edge = updatedData.edges.find((e) => e.id === edgeId);
       if (!edge) continue; // defensive — first-pass already validated
       if (label === undefined) {
         delete (edge as Record<string, unknown>)['label']; // D-08 strip-key
       } else {
         (edge as Record<string, unknown>)['label'] = label;
       }
     }

     try {
       view.canvas.setData(updatedData);
       this.debouncedRequestSave(filePath, view);
       return true;
     } catch (err) {
       try {
         view.canvas.setData(originalData);
       } catch (rollbackErr) {
         console.error('[RadiProtocol] Canvas rollback failed — canvas may be in inconsistent state:', rollbackErr);
       }
       throw err;
     }
   }
   ```

7. Прогнать `npx tsc --noEmit --skipLibCheck`. Должен пройти без ошибок. Если есть ошибка типа на `updatedData.edges.find((e) => e.id === edgeId)` — убедиться, что Plan 01 выполнен и `CanvasEdgeData[]` сделал `e` типизированным (иначе TS может не вывести `e.id`).

8. Прогнать `npm test`. Ожидание: **не хуже baseline** (475 + Task 4 из Plan 02 = 477; Task 2 этого плана добавит ещё тесты).
  </action>

  <verify>
    <automated>npx tsc --noEmit --skipLibCheck</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -c "async saveLiveEdges(" src/canvas/canvas-live-editor.ts` → 1.
    - [ ] `grep -c "edgeEdits?: Array<{ edgeId: string; label: string | undefined }>" src/canvas/canvas-live-editor.ts` → 1 (в сигнатуре `saveLiveBatch`).
    - [ ] `grep -c "Phase 50 D-12" src/canvas/canvas-live-editor.ts` → ≥1 (doc-комментарий у `saveLiveEdges`).
    - [ ] `grep -c "Phase 50 D-14" src/canvas/canvas-live-editor.ts` → ≥2 (в сигнатуре `saveLiveBatch` + в блоке apply edge mutations).
    - [ ] `grep -c "D-08 strip-key" src/canvas/canvas-live-editor.ts` → ≥2 (новое в saveLiveEdges + новое в saveLiveBatch edge-branch).
    - [ ] `grep -c "answer-label-edge-sync.md" src/canvas/canvas-live-editor.ts` → ≥1 (canonical ref comment у `saveLiveEdges`).
    - [ ] `grep -c "console.error.*Canvas rollback failed" src/canvas/canvas-live-editor.ts` → 3 (было 2 в saveLive + saveLiveBatch, теперь +1 в saveLiveEdges).
    - [ ] `npx tsc --noEmit --skipLibCheck` exit 0.
    - [ ] **Shared Pattern G (CLAUDE.md append-only)**: diff `src/canvas/canvas-live-editor.ts` показывает:
      - Ноль изменений в `getCanvasView`, `isLiveAvailable`, `getCanvasJSON`, `saveLive`, `debouncedRequestSave`, `destroy`.
      - Три targeted in-place правки в `saveLiveBatch` (сигнатура +1 параметр; no-op условие; добавлены два блока first-pass-validate-edges и second-pass-mutate-edges).
      - Один новый метод `saveLiveEdges` appended между `saveLiveBatch` и `debouncedRequestSave`.
      - Никакие rules/functions/event-listeners из предыдущих фаз не удалены (diff показывает только additions и targeted in-place правки, без unrelated deletions).
    - [ ] `PROTECTED_FIELDS` constant (line 14) не изменился.
    - [ ] Никаких CSS-изменений.
  </acceptance_criteria>

  <done>
    CanvasLiveEditor расширен — D-12 (`saveLiveEdges`) и D-14 (`saveLiveBatch` с edgeEdits) shipped. Reconciler и Display-label handler могут вызывать эти методы в Plan 04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write-back integration tests — D-14 atomic + D-08 strip-key</name>
  <files>src/__tests__/canvas-write-back.test.ts</files>

  <read_first>
    - `src/__tests__/canvas-write-back.test.ts` (весь файл — узнать `makeCanvasJson` helper на строках 7-13, полный `saveNodeEdits`-mock setup, и точный паттерн теста «undefined values delete the key from the node» на строках 143-157)
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §8 (verbatim pattern for append)
    - `CLAUDE.md` Shared Pattern G — `canvas-write-back.test.ts` — **shared file**, append-only.
  </read_first>

  <behavior>
  - Test 1 (Phase 50): `'PHASE-50 D-14 атомарность: saveNodeEdits с radiprotocol_displayLabel НЕ вызывает vault.modify отдельно от edge-write'` — **ВАЖНО**: в этом плане мы НЕ трогаем `editor-panel-view.ts`. Этот тест вызывается против `CanvasLiveEditor` (mock Pattern B) И fallback `saveNodeEdits` Strategy A; поскольку wire-up в `editor-panel-view.ts` ещё не сделан (это Plan 04), фокус теста — ПРОСТО pure-CanvasLiveEditor.saveLiveBatch: один setData на node+edges вместе. То есть тест направлен на `CanvasLiveEditor`, не на `saveNodeEdits`. Формулировка теста должна быть про `saveLiveBatch`.
    Переформулируем: **Test 1 — `'CanvasLiveEditor.saveLiveBatch applies node + edge edits in a single view.canvas.setData call (D-14)'`**. Использовать mock `CanvasViewInternal` с `setData` spy, проверить, что `setData` вызван **ровно 1 раз**, а переданный payload содержит и обновлённую ноду, и обновлённое ребро.
  - Test 2 (Phase 50): `'CanvasLiveEditor.saveLiveEdges with label=undefined deletes the "label" key from the edge JSON (D-08)'` — сконструировать `CanvasViewInternal` mock с edges содержащим `{ id: 'e1', fromNode: 'q', toNode: 'a', label: 'old' }`; вызвать `saveLiveEdges('test.canvas', [{ edgeId: 'e1', label: undefined }])`; проверить, что в payload, переданном в `setData`, edge НЕ имеет свойства `label`.
  - Test 3 (Phase 50): `'CanvasLiveEditor.saveLiveEdges with label="new" sets the "label" key on the edge (D-04 forward direction)'` — аналогично, но с не-undefined label.
  - Test 4 (Phase 50): `'CanvasLiveEditor.saveLiveBatch first-pass-validate rejects ENTIRE batch if any edgeId is missing (no partial mutation)'` — конструируется mock с известной node-id `'n-a'` и edges `[{id:'e1',...}]`; вызов `saveLiveBatch('test.canvas', [{nodeId:'n-a',edits:{...}}], [{edgeId:'e-missing', label:'X'}])` → должен вернуть `false`; `setData` НЕ должен быть вызван.
  </behavior>

  <action>

1. Прочитать `src/__tests__/canvas-write-back.test.ts` полностью (~193 строки). Понять mock-структуру (mockVaultRead, mockVaultModify, view instance). Определить, доступен ли там mock `CanvasLiveEditor` — если нет, новые тесты должны инстанцировать `CanvasLiveEditor` напрямую с mock `app.workspace.getLeavesOfType('canvas')` возвращающим leaf с mock `CanvasViewInternal`.

2. **Решение по структуре**: **новые тесты ЖИВУТ в новом describe-блоке в конце `canvas-write-back.test.ts`**. Они не обязаны использовать существующий `view` / `mockVaultRead` / `mockVaultModify` — они могут инстанцировать свой mini-mock. Ключевое правило (CLAUDE.md Shared Pattern G): НЕ изменять существующие тесты (строки 15-193 — preserve verbatim); appended-only.

3. **Append** в конец файла новый describe (после последней закрывающей `})`):
```typescript

// Phase 50: CanvasLiveEditor edge-write surface (saveLiveEdges + saveLiveBatch with edgeEdits).
// Round-trip tests asserting D-08 strip-key semantics and D-14 single-setData atomicity.
// Uses a hand-rolled CanvasViewInternal mock (no full EditorPanelView needed because
// the wire-up of displayLabel → incoming edges lives in Plan 04).
describe('PHASE-50 CanvasLiveEditor edge writes (D-08 / D-12 / D-14)', () => {
  // Minimal CanvasViewInternal-compatible mock. Mirrors the shape consumed by
  // CanvasLiveEditor.getCanvasView (src/canvas/canvas-live-editor.ts:29-47).
  function buildMockLiveEditor(params: {
    filePath: string;
    nodes: Array<Record<string, unknown>>;
    edges: Array<Record<string, unknown>>;
  }): { editor: CanvasLiveEditor; setDataSpy: ReturnType<typeof vi.fn>; requestSaveSpy: ReturnType<typeof vi.fn> } {
    // Lazy imports inside the helper so the top-of-file import surface does not bloat.
    // CanvasLiveEditor expects app.workspace.getLeavesOfType('canvas') → array of leaves whose
    // view.file.path matches filePath AND view.canvas.getData returns { nodes, edges }.
    const data = { nodes: [...params.nodes], edges: [...params.edges] };
    const setDataSpy = vi.fn((next: { nodes: unknown[]; edges: unknown[] }) => {
      data.nodes = next.nodes as Array<Record<string, unknown>>;
      data.edges = next.edges as Array<Record<string, unknown>>;
    });
    const requestSaveSpy = vi.fn();
    const mockView = {
      file: { path: params.filePath },
      canvas: {
        getData: () => ({
          nodes: data.nodes.map(n => ({ ...n })), // deep-enough copy for Pattern B semantics
          edges: data.edges.map(e => ({ ...e })),
        }),
        setData: setDataSpy,
        requestSave: requestSaveSpy,
        nodes: new Map(),
      },
    };
    const mockApp = {
      workspace: {
        getLeavesOfType: (t: string) => (t === 'canvas' ? [{ view: mockView }] : []),
      },
    };
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const editor = new CanvasLiveEditor(mockApp as any);
    return { editor, setDataSpy, requestSaveSpy };
  }

  it('saveLiveEdges deletes the "label" key when label === undefined (D-08)', async () => {
    const { editor, setDataSpy } = buildMockLiveEditor({
      filePath: 'test.canvas',
      nodes: [{ id: 'n-q' }, { id: 'n-a' }],
      edges: [{ id: 'e1', fromNode: 'n-q', toNode: 'n-a', label: 'old' }],
    });
    const ok = await editor.saveLiveEdges('test.canvas', [{ edgeId: 'e1', label: undefined }]);
    expect(ok).toBe(true);
    expect(setDataSpy).toHaveBeenCalledTimes(1);
    const written = setDataSpy.mock.calls[0]![0] as { edges: Array<Record<string, unknown>> };
    expect(written.edges[0]).not.toHaveProperty('label');
  });

  it('saveLiveEdges sets the "label" key when label is a string (D-04 forward)', async () => {
    const { editor, setDataSpy } = buildMockLiveEditor({
      filePath: 'test.canvas',
      nodes: [{ id: 'n-q' }, { id: 'n-a' }],
      edges: [{ id: 'e1', fromNode: 'n-q', toNode: 'n-a' }],
    });
    const ok = await editor.saveLiveEdges('test.canvas', [{ edgeId: 'e1', label: 'Новое' }]);
    expect(ok).toBe(true);
    const written = setDataSpy.mock.calls[0]![0] as { edges: Array<Record<string, unknown>> };
    expect(written.edges[0]!['label']).toBe('Новое');
  });

  it('saveLiveBatch writes node edits + edge edits in ONE setData call (D-14 atomicity)', async () => {
    const { editor, setDataSpy } = buildMockLiveEditor({
      filePath: 'test.canvas',
      nodes: [
        { id: 'n-q', radiprotocol_nodeType: 'question' },
        { id: 'n-a', radiprotocol_nodeType: 'answer', radiprotocol_displayLabel: 'Старое' },
      ],
      edges: [{ id: 'e1', fromNode: 'n-q', toNode: 'n-a', label: 'Старое' }],
    });
    const ok = await editor.saveLiveBatch(
      'test.canvas',
      [{ nodeId: 'n-a', edits: { radiprotocol_displayLabel: 'Новое' } }],
      [{ edgeId: 'e1', label: 'Новое' }],
    );
    expect(ok).toBe(true);
    expect(setDataSpy).toHaveBeenCalledTimes(1); // D-14: atomic single write — never two setData
    const written = setDataSpy.mock.calls[0]![0] as {
      nodes: Array<Record<string, unknown>>;
      edges: Array<Record<string, unknown>>;
    };
    const answer = written.nodes.find(n => n['id'] === 'n-a');
    const edge = written.edges.find(e => e['id'] === 'e1');
    expect(answer!['radiprotocol_displayLabel']).toBe('Новое');
    expect(edge!['label']).toBe('Новое');
  });

  it('saveLiveBatch first-pass-validate rejects entire batch if an edgeId is missing (no partial mutation)', async () => {
    const { editor, setDataSpy } = buildMockLiveEditor({
      filePath: 'test.canvas',
      nodes: [{ id: 'n-a' }],
      edges: [{ id: 'e1', fromNode: 'q', toNode: 'n-a' }],
    });
    const ok = await editor.saveLiveBatch(
      'test.canvas',
      [{ nodeId: 'n-a', edits: { radiprotocol_displayLabel: 'X' } }],
      [{ edgeId: 'e-missing', label: 'X' }],
    );
    expect(ok).toBe(false);
    expect(setDataSpy).not.toHaveBeenCalled();
  });

  it('saveLiveBatch called with only nodeEdits (no edgeEdits) keeps prior single-arg behaviour (back-compat)', async () => {
    const { editor, setDataSpy } = buildMockLiveEditor({
      filePath: 'test.canvas',
      nodes: [{ id: 'n-a', radiprotocol_nodeType: 'answer' }],
      edges: [],
    });
    const ok = await editor.saveLiveBatch(
      'test.canvas',
      [{ nodeId: 'n-a', edits: { radiprotocol_displayLabel: 'X' } }],
    );
    expect(ok).toBe(true);
    expect(setDataSpy).toHaveBeenCalledTimes(1);
    const written = setDataSpy.mock.calls[0]![0] as { nodes: Array<Record<string, unknown>> };
    expect(written.nodes[0]!['radiprotocol_displayLabel']).toBe('X');
  });
});
```

**Примечание по imports**: убедиться, что в начале `canvas-write-back.test.ts` уже импортированы `vi` из `'vitest'` и `CanvasLiveEditor` из `'../canvas/canvas-live-editor'`. Если `vi` не импортирован — добавить его в **существующий** import-stmt из `'vitest'` (не плодить дубль import). Если `CanvasLiveEditor` не импортирован — добавить новую строку import в конце блока импортов. Это единственное допустимое in-place изменение верхней части файла.

4. Прогнать `npx vitest run src/__tests__/canvas-write-back.test.ts` — все 5 новых тестов должны быть зелёными, существующие 10 — тоже зелёные.

5. Прогнать `npm test` full. Ожидание: **≥ (baseline после Plan 02 Task 1-3) + 5 passed / 1 skipped / 0 failed**. Итоговый счёт после Plans 01-02-03 должен быть примерно 466 + 9 + 2 + 5 = 482 passed.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/canvas-write-back.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -c "PHASE-50 CanvasLiveEditor edge writes" src/__tests__/canvas-write-back.test.ts` → 1.
    - [ ] `grep -c "saveLiveEdges" src/__tests__/canvas-write-back.test.ts` → ≥3 (импорт уже покрыт саббордом + ≥3 теста на метод).
    - [ ] `grep -c "saveLiveBatch" src/__tests__/canvas-write-back.test.ts` → ≥3 (3 теста на batch).
    - [ ] `grep -c "setDataSpy).toHaveBeenCalledTimes(1)" src/__tests__/canvas-write-back.test.ts` → ≥2 (D-14 atomicity proof).
    - [ ] `grep -c "setDataSpy).not.toHaveBeenCalled()" src/__tests__/canvas-write-back.test.ts` → 1 (first-pass reject proof).
    - [ ] Все 5 новых тестов passed.
    - [ ] `npx vitest run src/__tests__/canvas-write-back.test.ts` exit 0.
    - [ ] **Shared Pattern G**: diff `src/__tests__/canvas-write-back.test.ts` показывает только (а) при необходимости одно расширение существующего `import from 'vitest'` добавлением `vi` и (б) appended-only новый describe-блок в конце файла. Все prior tests (lines 15-193) побайтово неизменны. Никакие rules/functions/event-listeners из предыдущих фаз не удалены.
    - [ ] Никаких CSS-изменений.
  </acceptance_criteria>

  <done>
    Write-back тесты доказывают: (а) saveLiveEdges правильно strip'ит label-ключ (D-08); (б) saveLiveBatch атомарно пишет node + edges в одном setData (D-14); (в) first-pass reject сохраняет canvas неизменным при ошибке.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --skipLibCheck` exit 0.
- `grep -c "async saveLiveEdges(" src/canvas/canvas-live-editor.ts` → 1.
- `grep -c "edgeEdits" src/canvas/canvas-live-editor.ts` → ≥6 (сигнатура `saveLiveBatch` + doc-комментарий + first-pass-validate loop + second-pass-mutate loop + сигнатура `saveLiveEdges` + call-sites).
- `grep -c "setData).toHaveBeenCalledTimes(1)" src/__tests__/canvas-write-back.test.ts` — old + new (в Phase 50 блоке ≥2).
- Все Plan 01 и Plan 02 acceptance criteria остаются true (type-lift не откатывается, reconciler и его тесты не трогаются).
- Shared Pattern G для обоих shared-файлов: diff ТОЛЬКО appended / targeted edits.
</verification>

<success_criteria>
Writer-слой готов. Reconciler (Plan 02) и Display-label handler (Plan 04) имеют pattern-B API для edge-label записи с atomic node+edge semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/50-answer-edge-label-sync/50-03-SUMMARY.md`
</output>
