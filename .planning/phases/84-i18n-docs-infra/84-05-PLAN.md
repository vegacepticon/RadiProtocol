---
phase: 84
plan: 05
title: "Write docs/PROTOCOL-AUTHORING.md and docs/CONTRIBUTING.md (DOC-02)"
status: pending
requirements: [DOC-02]
depends_on: []
wave: 3
risk: low
---

# Plan 84-05 — Write docs/PROTOCOL-AUTHORING.md and docs/CONTRIBUTING.md

## Objective

Create the `docs/` folder with two foundational documents.

## Scope

Create:
- `docs/PROTOCOL-AUTHORING.md` — how to build a protocol canvas:
  - Node kinds (question, answer, text-block, snippet, loop, start).
  - Edge semantics (labels, "+" prefix for loop exit).
  - Loop construction with unified loop node.
  - Snippet binding (file vs folder).
- `docs/CONTRIBUTING.md` — dev setup:
  - `npm install`, `npm run dev`.
  - Test command (`npm test`).
  - Lint gate (`npm run lint`).
  - Pre-commit hook (reference `.githooks/pre-commit` from Phase 78).
  - Branch naming (`dev/<task-slug>`).
  - PR process (no explicit PR template required).

## Constraints

- Both documents in English (primary) with a note that Russian translation is future work.
- Do not duplicate README content.

## Verification

```bash
npm run build && npm run lint && npm test
```

## Success criteria

- `docs/` folder exists with both files.
- Content covers all required topics.
- No Russian strings.
