---
phase: 72
plan: 02
plan_id: 72-02-PLAN
title: ОБП full (органы брюшной полости) authoring guide
type: execute
wave: 1
depends_on: []
files_modified:
  - "ОБП full 1.0.0.canvas (author's vault — not in repo)"
autonomous: false
requirements: [CANVAS-LIB-02]
tags: [canvas-authoring, content, obsidian]
must_haves:
  truths:
    - "An ОБП full canvas exists in the author's vault and runs end-to-end in the Protocol Runner without throwing (both contrast branches)"
    - "The first question after start is «Контраст вводился?» with 3 answers (Да, болюсно / Да, вручную / Нет) fanning into a single shared «Исследование произведено в положении пациента лежа на спине.» convergence (per ОГК pattern, D-27)"
    - "The output produced by running each contrast branch contains every section header from `Z:\\projects\\references\\ОБП без контраста.md` (when «Нет» picked) or `Z:\\projects\\references\\ОБП с контрастом.md` (when «Да, ...» picked) — ПЕЧЕНЬ, ЖЕЛЧНЫЙ ПУЗЫРЬ, ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА, СЕЛЕЗЕНКА, КИШЕЧНИК, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, КОСТНЫЕ СТРУКТУРЫ; plus СОСУДЫ in the contrast branch"
    - "Two loops (ПЕЧЕНЬ + ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА — D-07) each have exactly one `+`-prefix exit edge"
    - "The terminal output contains the verbatim Заключение `РКТ-признаки патологических изменений органов брюшной полости не выявлены.` and Рекомендации `Консультация направившего специалиста.`"
    - "All `==fill-in==` placeholders mapped from `<% tp.file.cursor(N) %>` (D-29) are well-formed"
  decisions:
    - "D-01: Mixed structure (linear sections + ПЕЧЕНЬ/ПОДЖЕЛУДОЧНАЯ loops) as in ОГК 1.10.0"
    - "D-02: Standalone canvas with own conclusion (ОБП independent)"
    - "D-03: Optional 'НА СКАНИРОВАННЫХ УРОВНЯХ' tail — author's call"
    - "D-04: No cross-canvas snippet links (Pitfall 9 acceptance check)"
    - "D-07: ОБП liver + pancreas loops; spleen, kidneys, GI, lymph nodes, aorta linear"
    - "D-11: '+' prefix on loop exit edges per Phase 50.1"
    - "D-12: Hybrid linear + nested-loop modeling per ОГК"
    - "D-13: Snippet nodes inserted as template placeholders"
    - "D-14: Snippet paths configured manually by author later"
    - "D-15: No pre-created snippet folders"
    - "D-16: Snippet format (.json placeholder vs .md verbatim) per author choice"
    - "D-17: Standalone ОБП report"
    - "D-18: No cross-canvas references"
    - "D-19: Terminal Заключение + Рекомендации auto-output"
    - "D-20: End-to-end run without errors (both contrast branches)"
    - "D-21: All sections from matching .md template present in output"
    - "D-22: Fixed Заключение/Рекомендации blocks auto-output"
    - "D-23: '==напишу что не так==' free-text branches valid"
    - "D-24: Fill-in placeholders considered filled after user completes"
    - "D-25: Visual comparison vs ОБП без контраста.md / ОБП с контрастом.md"
    - "D-27: Single ОБП canvas with «Контраст вводился?» fan-out (КУ / безКУ)"
    - "D-29: Templater '<% tp.file.cursor() %>' → '==fill-in==' chips"
  artifacts:
    - path: "ОБП full 1.0.0.canvas"
      provides: "Standalone ОБП full algorithmic canvas (author's vault, not committed)"
      contains: "exactly one start; one «Контраст вводился?» question with 3 answers; 2 loops (ПЕЧЕНЬ, ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА); ≥9 text-block section headers each beginning with `\\n`"
  key_links:
    - from: "start node"
      to: "«Контраст вводился?» question"
      via: "single edge"
      pattern: "first-after-start question per ОГК reference convention rule 2"
    - from: "3 contrast-answer nodes"
      to: "shared «Исследование произведено в положении пациента лежа на спине.» text-block"
      via: "3 convergent edges"
      pattern: "convergence node receives 3 incoming edges (mirror ОГК)"
    - from: "ПЕЧЕНЬ loop and ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА loop"
      to: "next section's text-block"
      via: "loop exit edge with `+` prefix"
      pattern: "label like `+Все указано, продолжаем` (Phase 50.1)"
    - from: "КОСТНЫЕ СТРУКТУРЫ section"
      to: "Terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение text inside terminal text-block.text matches the .md verbatim"
---

<objective>
Author the ОБП (органы брюшной полости) full algorithmic canvas in the author's local vault. The canvas mirrors the structure of `Z:\projects\references\ОГК 1.10.0.canvas` and uses section lists from BOTH `Z:\projects\references\ОБП без контраста.md` and `ОБП с контрастом.md` (D-27 — single canvas with «Контраст вводился?» fan-out). When run end-to-end, the assembled output structurally matches the corresponding contrast-branch .md template.

Purpose: deliver CANVAS-LIB-02. Highest section count of the five Phase 72 canvases.
Output: `ОБП full 1.0.0.canvas` in author's vault (per RESEARCH "Filename and Vault Path Convention"; not committed per phase scope).
</objective>

<execution_context>
This plan is executed by the **author working manually in Obsidian Canvas UI** — not by an automated code-execution agent. There is no `npm run build`, no `vitest`, no commit to `src/`. Every task is a sequence of clicks/drags/edits in Obsidian's Canvas pane plus optional JSON inspection in a text editor for invariant verification.

`autonomous: false` for every task — each task pauses for the author.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md

External (author-side, not in repo):
- Z:\projects\references\ОГК 1.10.0.canvas (reference — read-only frozen template, used for «Контраст вводился?» fan-out shape)
- Z:\projects\references\ОБП без контраста.md (без-КУ branch section list + verbatim text)
- Z:\projects\references\ОБП с контрастом.md (с-КУ branch section list + verbatim text)
- Z:\projects\references\SNIPPETS\ОБП\ (8 subfolders: ЖЕЛЧНЫЙ ПУЗЫРЬ, КИШЕЧНИК, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, ПЕЧЕНЬ, ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА, СЕЛЕЗЕНКА, СОСУДЫ)
- Z:\projects\references\SNIPPETS\КОСТИ\
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Create ОБП canvas skeleton with «Контраст вводился?» fan-out</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — first-after-start «Контраст вводился?» question + 3 answers + convergent text-block (this is the canonical pattern per RESEARCH §"Conventions" rule 2)
    - Z:\projects\references\ОБП без контраста.md (line 4: «МСКТ-исследование органов брюшной полости выполнено по программе объемного сканирования без внутривенного контрастирования...») and `ОБП с контрастом.md` (line 4: «...с внутривенным болюсным контрастированием»)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§2 ОБП full" (lines 309-359), § "Conventions" rule 2
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-27 (single canvas, «Контраст вводился?» fan-out)
  </read_first>
  <action>
    1. Open Obsidian, create canvas named exactly `ОБП full 1.0.0.canvas` in the author's working canvases folder (NOT in `Z:\projects\references\`).
    2. Drop a `start` node at the top: `radiprotocol_nodeType:"start"`, `text:""`, `color:"4"`.
    3. Drop a `question` node below: `radiprotocol_nodeType:"question"`, `text:"Контраст вводился?"`, `radiprotocol_headerText:"Контраст вводился?"`, `color:"5"`. Connect start → question.
    4. Drop 3 `answer` nodes (each `radiprotocol_nodeType:"answer"`, `color:"2"`). Per D-27 + RESEARCH §"Conventions" rule 2 fan 3 ways:
       - Answer A: `radiprotocol_displayLabel:"Да, болюсно"`, `radiprotocol_answerText:"МСКТ-исследование органов брюшной полости выполнено по программе объемного сканирования с внутривенным болюсным контрастированием."` (verbatim from ОБП с контрастом.md line 4). Edge `label:"Да, болюсно"`.
       - Answer B: `radiprotocol_displayLabel:"Да, вручную"`, `radiprotocol_answerText:"МСКТ-исследование органов брюшной полости выполнено по программе объемного сканирования с внутривенным контрастированием."`. Edge `label:"Да, вручную"`.
       - Answer C: `radiprotocol_displayLabel:"Нет"`, `radiprotocol_answerText:"МСКТ-исследование органов брюшной полости выполнено по программе объемного сканирования без внутривенного контрастирования с последующей мультипланарной реконструкцией."` (verbatim from ОБП без контраста.md line 4). Edge `label:"Нет"`.
    5. Drop a SHARED convergence `text-block` node with `text:"\nИсследование произведено в положении пациента лежа на спине.\n\nНаддиафрагмальные отделы легких не изменены.\n\nСвободной жидкости в брюшной полости не выявлено."` (verbatim from ОБП без контраста.md lines 5-9 — these three lines are shared between both contrast variants).
    6. Wire all 3 contrast-answer nodes (top side) into the convergence text-block (top side) — 3 edges into the same node (per RESEARCH §"Edge inventory" — convergence is widespread; per Pitfall 7, ensure each contrast-answer node has only ONE outgoing edge to the convergence node).
    7. Save canvas.

    Tag the contrast-answer nodes A/B/C internally so later tasks can branch contrast-conditional sections (СОСУДЫ shows only on contrast branches per RESEARCH §2 row 6). The branching is achieved by the canvas DAG: contrast answers A and B route through СОСУДЫ; answer C routes around it. This implies that DOWNSTREAM of the convergence text-block, the canvas needs separate routing for «есть контраст» vs «нет контраста». Practical approach: the convergence text-block has 2 outgoing edges — one per contrast disposition. Add a secondary minimal `question` node «Был ли контраст?» (or use a hidden state-carrying answer) right after the convergence — OR model the conditional routing via the existing 3 contrast-answer nodes: instead of converging into one text-block, route the contrast (A,B) answers and the no-contrast (C) answer into TWO different text-blocks each carrying the same shared text. Recommended: the simpler "carry state via two parallel paths" — duplicate the convergence text-block into 2 (one for contrast, one for no-contrast) so each path can later route through its own per-contrast sections (СОСУДЫ vs the «Аорта в брюшном отделе не расширена» linear text). This costs ~3 extra nodes vs forking later, but it cleanly avoids state-tracking gymnastics.
  </action>
  <verify>
    Manual: open the canvas in Obsidian. Run from start. The runner shows the picker «Контраст вводился?» with 3 buttons. Pick each — output emits the matching procedure description line and proceeds.
    JSON: confirm exactly one start node (I1), exactly one «Контраст вводился?» question node, exactly 3 answer nodes connected to it, and the convergence text-block(s) connected from all 3 answers.
  </verify>
  <acceptance_criteria>
    - File `ОБП full 1.0.0.canvas` exists in author's vault
    - JSON contains exactly one `"radiprotocol_nodeType":"start"` (I1)
    - JSON contains a question node whose `text` is `"Контраст вводился?"`
    - JSON contains 3 answer nodes with `radiprotocol_displayLabel` values "Да, болюсно", "Да, вручную", "Нет" — each connected as the target of an outgoing edge from the contrast question (with matching edge `label`)
    - The contrast question's outgoing edges have labels exactly: "Да, болюсно", "Да, вручную", "Нет" (Pitfall 6 — `displayLabel` ↔ edge `label` aligned)
    - Each of the 3 contrast-answer nodes has at least one outgoing edge into the next-step convergence (or per-disposition) text-block (no orphans — I8/I9)
    - Running the canvas: each of the 3 contrast paths advances past the convergence node into Task 2's section-1 territory without throwing
  </acceptance_criteria>
  <done>Skeleton with start + «Контраст вводился?» fan-out + procedure-description convergence exists; both contrast and no-contrast paths are independently routable.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Build the ОБП section structure (organ-by-organ linear sections, no loops yet)</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОБП без контраста.md (full file — verbatim section text per organ; Templater placeholders `<% tp.file.cursor(N) %>` will be mapped in Task 5)
    - Z:\projects\references\ОБП с контрастом.md (full file — verbatim contrast-branch text per organ)
    - Z:\projects\references\ОГК 1.10.0.canvas — per-organ pattern shape (text-block header → question → 2-3 answers → optional snippet picker → optional `==пишу что не так==`)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§2 ОБП full — Section list" (lines 313-329) and § "Conventions" rules 3-4
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-23 (==напишу что не так== branches valid), D-27
  </read_first>
  <action>
    For each section below, drop a text-block header (text begins with `\n` per Pitfall 4), a question, the verbatim normal-finding answer from the matching .md file (preserve `<% tp.file.cursor(N) %>` in the answer text for now — Task 5 maps them to `==fill-in==`), and a `==напишу что не так==` free-text branch (D-23). PEченъ and ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА are placeholders here — their loops are added in Task 3.

    Section list (each section's normal answer is the verbatim block from `ОБП без контраста.md` for the no-contrast path / from `ОБП с контрастом.md` for the contrast path; sections marked "shared" use the no-contrast text since both files agree there):

    1. **ПЕЧЕНЬ** — text-block: `\nПЕЧЕНЬ:`. Norm answer (no-contrast, ОБП без контраста.md lines 12-14):
       ```
       Не увеличена, кранио-каудальный размер правой доли по среднеключичной линии - <% tp.file.cursor(1) %> мм (норма меньше 150 мм), левой доли - <% tp.file.cursor(2) %> мм (норма меньше 100 мм), с четкими ровными контурами. Плотность паренхимы не снижена, среднее значение +<% tp.file.cursor(3) %> НU (норма +55...+65 HU). Структура однородная.
       Внутри- и внепечёночные протоки не расширены.
       Дополнительных образований в условиях нативного исследования не выявлено.
       ```
       Norm answer (contrast, ОБП с контрастом.md lines 12-13):
       ```
       Не увеличена, кранио-каудальный размер правой доли по среднеключичной линии - <% tp.file.cursor(1) %> мм (норма меньше 150 мм), левой доли - <% tp.file.cursor(2) %> мм (норма меньше 100 мм), с четкими ровными контурами. Плотность паренхимы не снижена, среднее значение +<% tp.file.cursor(3) %> НU (норма +55...+65 HU). Структура однородная.
       Внутри- и внепечёночные протоки не расширены. Дополнительных образований и патологических фокусов накопления контрастного средства в печени не выявлено.
       ```
       Use 2 separate norm answer nodes — one per contrast branch — connected from the matching contrast-disposition path created in Task 1. (Loop is added in Task 3.)
    2. **ЖЕЛЧНЫЙ ПУЗЫРЬ** — text-block: `\nЖЕЛЧНЫЙ ПУЗЫРЬ:`. Per-contrast norm answers (ОБП без контраста.md lines 17-18 vs ОБП с контрастом.md lines 16-17). Linear.
    3. **ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА** — text-block: `\nПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА:`. Per-contrast norm (ОБП без контраста.md line 21 vs ОБП с контрастом.md line 20). Linear placeholder; loop added in Task 3.
    4. **СЕЛЕЗЕНКА** — text-block: `\nСЕЛЕЗЕНКА:`. Per-contrast norm (ОБП без контраста.md line 24 vs ОБП с контрастом.md line 23). Linear.
    5. **КИШЕЧНИК** — text-block: `\nКИШЕЧНИК:`. Norm: `Положение петель кишечника обычное, патологическое утолщение стенки кишки и нарушение просвета не выявлено.` (verbatim, identical in both .md files). Linear.
    6. **СОСУДЫ branch (contrast only)** — text-block: `\nСОСУДЫ:`. Norm answer (ОБП с контрастом.md lines 32-33): `Визуализируются удовлетворительно, дефектов контрастирования не выявлено. Аорта в брюшном отделе не расширена. Стенка аорты не изменена.\nВоротная вена  мм, селезёночная вена  мм.` Linear; ONLY on contrast path.
    7. **«Аорта...» branch (no contrast only)** — text-block (no header — single line): `\nАорта в брюшном отделе не расширена. Стенка аорты не изменена.` (verbatim ОБП без контраста.md line 29). ONLY on no-contrast path.
    8. **ЛИМФАТИЧЕСКИЕ УЗЛЫ** — text-block: `\nЛИМФАТИЧЕСКИЕ УЗЛЫ:`. Norm: `Не увеличены.` Linear.
    9. **МЯГКИЕ ТКАНИ** — text-block: `\nМЯГКИЕ ТКАНИ:`. Norm: `Не изменены.` (no-contrast) / `Без патологических изменений.` (contrast) — use 2 per-branch norm answers. Linear.
    10. **КОСТНЫЕ СТРУКТУРЫ** — text-block: `\nКОСТНЫЕ СТРУКТУРЫ:`. Norm (verbatim ОБП без контраста.md line 38, identical in both branches): `Костно-деструктивные, костно-травматические изменения не определяются. Дегенеративно-дистрофические изменения позвоночника нерезко/умеренно выражены.` Linear (or short Умеренно/Нерезко dual-degeneration question per RESEARCH §2 row 9 — author's call).

    Wiring: contrast and no-contrast paths from Task 1 each route through their own copy of sections 1-5 (or share where the .md text matches), then split at section 6/7 (СОСУДЫ vs the linear «Аорта...» line), then re-converge at section 8 (ЛИМФАТИЧЕСКИЕ УЗЛЫ — identical text on both branches), and run linearly to section 10. Per RESEARCH §"Conventions" rule 4 each section's question allows a `==напишу что не так==` free-text branch.

    Save canvas after each section.
  </action>
  <verify>
    Run canvas in Obsidian on each contrast branch. The runner walks all sections in order. Output for the no-contrast branch matches `ОБП без контраста.md` lines 4-39; output for the contrast branch matches `ОБП с контрастом.md` lines 4-40 (excluding the loops in ПЕЧЕНЬ + ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА — those are still linear placeholders here).
  </verify>
  <acceptance_criteria>
    - JSON contains text-block nodes whose `text` values match (begin with `\n` and contain): `ПЕЧЕНЬ:`, `ЖЕЛЧНЫЙ ПУЗЫРЬ:`, `ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА:`, `СЕЛЕЗЕНКА:`, `КИШЕЧНИК:`, `СОСУДЫ:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:` (I5 — 9 expected section headers; «Аорта...» not a header but a sentence)
    - The verbatim sentences from each .md file's normal-finding paragraphs appear in answer node `radiprotocol_answerText` or `text` fields (substring match on first 5 words of each)
    - The СОСУДЫ section is reachable ONLY from the contrast-disposition path (BFS from no-contrast path NEVER visits the СОСУДЫ text-block)
    - The «Аорта в брюшном отделе...» single-line text-block is reachable ONLY from the no-contrast path
    - All edges resolve (I8); BFS from start reaches all section nodes (I9 partial — full I9 confirmed in Task 7)
    - Running the canvas on each contrast branch separately walks every applicable section without throwing
  </acceptance_criteria>
  <done>Both contrast branches walk all applicable linear sections; СОСУДЫ appears only on contrast; loops still pending Task 3.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Wire the 2 organ-discovery loops (ПЕЧЕНЬ + ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА)</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpt B and Excerpt E from RESEARCH §"JSON excerpts" — sequential-loop-chain idiom (lines 200-216)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Loop Modeling Pattern" (lines 218-260), § "§2 ОБП full" rows 1 and 3 (loop notes)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-07 (loop for liver + pancreas), D-11 (`+`-prefix), D-12 (hybrid)
  </read_first>
  <action>
    Add a loop after each of ПЕЧЕНЬ and ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА's normal-finding answer (per D-07: liver and pancreas may carry multiple findings — focal lesions, гемангиомы, кисты, образования). Each loop matches the pattern from RESEARCH §"Loop Modeling Pattern" — body branches + `+`-prefix exit.

    1. **ПЕЧЕНЬ loop** — drop a `loop` node after the ПЕЧЕНЬ norm answer, before advancing to ЖЕЛЧНЫЙ ПУЗЫРЬ. Attributes: `radiprotocol_nodeType:"loop"`, `radiprotocol_headerText:"Есть очаговые изменения в печени?"`, `text:"Есть очаговые изменения в печени?"`, `color:"1"`. Body branches:
       - Body A: answer node, edge `label:"Нет"`, answerText empty (already covered by ПЕЧЕНЬ norm sentence). Returns to loop header.
       - Body B: snippet node (folder picker), edge `label:"Выберу готовый шаблон"`. Snippet attrs: `radiprotocol_nodeType:"snippet"`, `radiprotocol_subfolderPath:"ОБП/ПЕЧЕНЬ"` (per RESEARCH §2 — 5 ready snippets: КИСТА, ПЕЧЕНЬ УВЕЛИЧЕНА, ФЛЕШ-ГЕМАНГИОМА, гемангиома, стеатоз), `radiprotocol_snippetLabel:"Выберу готовый шаблон"`, `text:"ОБП/ПЕЧЕНЬ"`, `color:"6"`. Returns to loop header.
       - Body C: answer node, edge `label:"Опишу вручную"`, answerText: `==опишу очаг печени==` (D-29 free-text fill-in). Returns to loop header.
       - Exit edge: `label:"+Все указано, продолжаем"` from loop → next text-block (ЖЕЛЧНЫЙ ПУЗЫРЬ).

    2. **ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА loop** — drop a `loop` node after the ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА norm answer, before advancing to СЕЛЕЗЕНКА. Attributes: `radiprotocol_headerText:"Есть образование/изменения поджелудочной железы?"`, `text:"Есть образование/изменения поджелудочной железы?"`, `color:"1"`. Body branches:
       - Body A: answer "Нет". Returns to loop header.
       - Body B: snippet folder picker `radiprotocol_subfolderPath:"ОБП/ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА"`. Returns to loop header.
       - Body C: answer "Опишу вручную", text `==опишу изменение поджелудочной железы==`. Returns to loop header.
       - Exit edge: `label:"+Все указано, продолжаем"` from loop → next text-block (СЕЛЕЗЕНКА).

    Each body branch's terminal node MUST have a return edge back to the loop header (Pitfall 2). Each loop has exactly one `+`-prefix exit edge (Pitfall 1, I2). Save canvas.

    Both loops apply on BOTH contrast branches (the loops sit on the shared trunk — D-07 isn't contrast-specific, just organ-specific). If during Task 1 the canvas was split into 2 parallel trunks per contrast disposition, then the loops are duplicated — one per trunk — and each duplicate must independently satisfy I2/I3.
  </action>
  <verify>
    Run canvas. At ПЕЧЕНЬ, runner shows the loop picker «Есть очаговые изменения в печени?» with 4 options (Нет / Выберу готовый шаблон / Опишу вручную / +Все указано, продолжаем). Pick «Нет» → returns to picker. Pick `+` → advances to ЖЕЛЧНЫЙ ПУЗЫРЬ. Same for ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА.
  </verify>
  <acceptance_criteria>
    - JSON contains ≥2 `"radiprotocol_nodeType":"loop"` nodes (or ≥4 if loops were duplicated per contrast trunk)
    - Each loop has exactly one outgoing edge with `label` starting with `"+"` (I2; D-11; Pitfall 1)
    - Each loop has ≥3 body edges (no `+` prefix) (I3)
    - Each loop's body snippet node carries `radiprotocol_subfolderPath` (one of `"ОБП/ПЕЧЕНЬ"` or `"ОБП/ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА"`) AND NOT `radiprotocol_snippetPath` (I4)
    - Each loop body branch has a return edge from terminal node back to the loop header (Pitfall 2)
    - The free-text body answers contain well-formed `==…==` placeholders (I10)
    - Running the canvas: cycling through loop body branches works; `+` exit advances to the next section
  </acceptance_criteria>
  <done>Both organ-discovery loops cycle correctly and exit cleanly via `+`; runner output captures multiple findings per organ when body branches are picked multiple times.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Insert snippet placeholder nodes for the remaining organs</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\SNIPPETS\ОБП\ folder listing (8 subfolders per RESEARCH §2 — ЖЕЛЧНЫЙ ПУЗЫРЬ, КИШЕЧНИК, ЛИМФАТИЧЕСКИЕ УЗЛЫ, МЯГКИЕ ТКАНИ, ПЕЧЕНЬ, ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА, СЕЛЕЗЕНКА, СОСУДЫ)
    - Z:\projects\references\SNIPPETS\КОСТИ\ folder listing (6 snippets — for КОСТНЫЕ СТРУКТУРЫ)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§2 ОБП full — Snippet insertion points" (lines 331-345)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <action>
    Per RESEARCH §2 snippet table — every organ has a dedicated subfolder. Add a snippet folder picker node to each section's question (alongside norm answer + free-text), one per organ:

    | Section | Snippet node attrs | Edge label |
    |---------|---------------------|-----------|
    | ЖЕЛЧНЫЙ ПУЗЫРЬ | `radiprotocol_subfolderPath:"ОБП/ЖЕЛЧНЫЙ ПУЗЫРЬ"`, `text:"ОБП/ЖЕЛЧНЫЙ ПУЗЫРЬ"`, `color:"6"` | `Выберу готовый шаблон` |
    | СЕЛЕЗЕНКА | `radiprotocol_subfolderPath:"ОБП/СЕЛЕЗЕНКА"` | `Выберу готовый шаблон` |
    | КИШЕЧНИК | `radiprotocol_subfolderPath:"ОБП/КИШЕЧНИК"` | `Выберу готовый шаблон` |
    | СОСУДЫ (contrast branch) | `radiprotocol_subfolderPath:"ОБП/СОСУДЫ"` | `Выберу готовый шаблон` |
    | ЛИМФАТИЧЕСКИЕ УЗЛЫ | `radiprotocol_subfolderPath:"ОБП/ЛИМФАТИЧЕСКИЕ УЗЛЫ"` | `Выберу готовый шаблон` |
    | МЯГКИЕ ТКАНИ | `radiprotocol_subfolderPath:"ОБП/МЯГКИЕ ТКАНИ"` | `Выберу готовый шаблон` |
    | КОСТНЫЕ СТРУКТУРЫ | `radiprotocol_subfolderPath:"КОСТИ"` | `Выберу шаблон по костям` |

    Each snippet node carries `radiprotocol_subfolderPath` AND NOT `radiprotocol_snippetPath` (I4). All paths start with `ОБП/` or are the shared `КОСТИ` — no cross-canvas leakage (Pitfall 9 — no `ОЗП`/`ОМТ`/`ПКОП`/`ОГК` paths).

    The ПЕЧЕНЬ + ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА snippet pickers are already inside their loops from Task 3 (no additional snippets needed there).

    Save canvas.
  </action>
  <verify>
    Run canvas on either contrast branch. Each non-loop section's picker now shows 3 options (Норма / Выберу готовый шаблон / ==напишу что не так==). Picking the snippet option opens the snippet picker modal listing the available snippets in the matching folder.
  </verify>
  <acceptance_criteria>
    - JSON contains 7 additional snippet nodes (1 per the 7 sections above; total in canvas = 7 + 2 loop snippets from Task 3 = 9)
    - Every snippet node carries exactly one of `radiprotocol_subfolderPath` or `radiprotocol_snippetPath` (I4) — verifier scans all snippet nodes
    - All snippet `subfolderPath` values are one of: `ОБП/ЖЕЛЧНЫЙ ПУЗЫРЬ`, `ОБП/КИШЕЧНИК`, `ОБП/ЛИМФАТИЧЕСКИЕ УЗЛЫ`, `ОБП/МЯГКИЕ ТКАНИ`, `ОБП/ПЕЧЕНЬ`, `ОБП/ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА`, `ОБП/СЕЛЕЗЕНКА`, `ОБП/СОСУДЫ`, `КОСТИ`
    - No snippet path contains `ОЗП`, `ОМТ`, `ПКОП`, `ОГК`, `ШЕЯ` (Pitfall 9 / D-04)
  </acceptance_criteria>
  <done>All organ sections offer a snippet picker; runner emits snippet contents when picked; no cross-canvas leakage.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Map Templater placeholders `<% tp.file.cursor(N) %>` to ==fill-in== chips</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОБП без контраста.md (search for `<% tp.file.cursor` — appears 11 times: cursor(1)..cursor(11) at lines 1, 12, 21, 24)
    - Z:\projects\references\ОБП с контрастом.md (same set of cursor placeholders)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-29 (Templater → `==fill-in==`)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls 5" (==…== well-formedness)
  </read_first>
  <action>
    Per D-29, every `<% tp.file.cursor(N) %>` Templater placeholder in the .md template becomes a Phase-27 `==fill-in==` chip in the canvas's text. ОБП templates use 11 numbered placeholders. Map them:

    | .md placeholder | Section | Replacement in canvas answer text |
    |-----------------|---------|------------------------------------|
    | `<% tp.file.cursor() %>` (line 1) | header | `==тип исследования==` (or omit — author's choice; recommend including in the procedure text-block) |
    | `<% tp.file.cursor(1) %>` | ПЕЧЕНЬ кранио-каудальный размер правой доли | `==размер правой доли мм==` |
    | `<% tp.file.cursor(2) %>` | ПЕЧЕНЬ левой доли | `==размер левой доли мм==` |
    | `<% tp.file.cursor(3) %>` | ПЕЧЕНЬ плотность | `==плотность HU==` |
    | `<% tp.file.cursor(4) %>` | ПЖ размер головки | `==размер головки==` |
    | `<% tp.file.cursor(5) %>` | ПЖ размер тела | `==размер тела==` |
    | `<% tp.file.cursor(6) %>` | ПЖ размер хвоста | `==размер хвоста==` |
    | `<% tp.file.cursor(7) %>` | ПЖ плотность | `==плотность ПЖ HU==` |
    | `<% tp.file.cursor(8) %>` | СЕЛЕЗЕНКА длина | `==длина селезенки==` |
    | `<% tp.file.cursor(9) %>` | СЕЛЕЗЕНКА толщина | `==толщина селезенки==` |
    | `<% tp.file.cursor(10) %>` | СЕЛЕЗЕНКА высота | `==высота селезенки==` |
    | `<% tp.file.cursor(11) %>` | СЕЛЕЗЕНКА индекс | `==индекс селезенки==` |

    Edit each affected answer node's `radiprotocol_answerText` (or `text` for free-text branches): replace the Templater syntax with the matching `==…==` chip text. Verify each `==…==` is well-formed (matched, no embedded `==` inside) per I10.

    Verify all `==…==` placeholders previously inserted in Tasks 2 (`==напишу что не так==`) and 3 (`==опишу очаг печени==`, `==опишу изменение поджелудочной железы==`) remain well-formed.

    Save canvas.
  </action>
  <verify>
    JSON regex: every `==` pairs on the same line; total count of `==` is even. Run canvas — runner renders fill-in chips that are clickable; submitting a value substitutes that value into the output.
  </verify>
  <acceptance_criteria>
    - JSON contains no occurrences of the substring `<% tp.file.cursor` (all Templater syntax removed per D-29)
    - JSON contains ≥11 `==…==` placeholders matching the cursor(1)..cursor(11) replacements (search for distinct `==` chip text values)
    - Every `==` substring in the JSON pairs with a closing `==` on the same line (I10)
    - Running the canvas: each fill-in chip is clickable, opens Phase-27 modal, and substitutes correctly
  </acceptance_criteria>
  <done>All Templater placeholders replaced with `==fill-in==` chips; all chips well-formed; runner shows clickable chips at each placeholder position.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Append terminal Заключение + Рекомендации text-block</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОБП без контраста.md lines 41-47 (Заключение and Рекомендации — verbatim, identical in both contrast variants per RESEARCH §2)
    - Z:\projects\references\ОБП с контрастом.md lines 42-48 (confirm identical wording)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-19, D-22
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Conventions" rule 8
  </read_first>
  <action>
    Drop a single terminal `text-block` node after КОСТНЫЕ СТРУКТУРЫ. Its `text` field MUST be exactly (verbatim from `ОБП без контраста.md` lines 41-47):

    ```
    \n## Заключение\nРКТ-признаки патологических изменений органов брюшной полости не выявлены.\n\n## Рекомендации\nКонсультация направившего специалиста.
    ```

    Per RESEARCH §2: both contrast variants share the SAME Заключение and Рекомендации text — single terminal text-block serves both branches.

    Wire all branches converging from КОСТНЫЕ СТРУКТУРЫ (norm / snippet / free-text) into this terminal text-block (single convergence point — Pitfall 7: ensure no duplicate edges from same parent).

    Per D-03 — author may optionally prepend a `\nНА СКАНИРОВАННЫХ УРОВНЯХ:` block. If included, strip cross-canvas snippet leakage (Pitfall 9) — only `ОБП/`, `КОСТИ`, `ШЕЯ` (lower neck adjacency) paths allowed; never `ОМТ` / `ОЗП` / `ПКОП` / `ОГК`. Recommended: skip for first pass; author adds later.

    Save canvas.
  </action>
  <verify>
    Run canvas to completion on either contrast branch. The runner's final assembled output's last lines contain the verbatim Заключение and Рекомендации.
  </verify>
  <acceptance_criteria>
    - JSON contains a `text-block` node whose `text` value contains both: `РКТ-признаки патологических изменений органов брюшной полости не выявлены.` (I6) AND `Консультация направившего специалиста.` (I7)
    - Both substrings appear verbatim
    - All КОСТНЫЕ СТРУКТУРЫ branches' terminal nodes have an outgoing edge into this terminal text-block
    - The terminal text-block is reachable from start by BFS over the edge DAG on BOTH contrast branches (I9)
    - Running the canvas to completion on EACH contrast branch: the assembled output's last block contains both fixed strings exactly
  </acceptance_criteria>
  <done>Terminal block reachable on both branches; output contains verbatim Заключение/Рекомендации.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Static invariant check (verifier-runnable on the JSON)</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md § "Structural Invariants" I1-I10 + § "Validation Sign-Off"
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls and Landmines"
  </read_first>
  <action>
    Walk I1-I10 against `ОБП full 1.0.0.canvas`:

    - **I1**: exactly 1 start node
    - **I2**: each loop has ≥1 `+`-prefix exit edge (expected: 2 loops × exactly 1 `+`-edge each, or 4 if duplicated per trunk)
    - **I3**: each loop has ≥1 body edge
    - **I4**: every snippet node has exactly one of subfolderPath/snippetPath (~9 snippet nodes)
    - **I5**: all 9 ОБП section headers appear as `\n`-prefixed text-block content: `ПЕЧЕНЬ:`, `ЖЕЛЧНЫЙ ПУЗЫРЬ:`, `ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА:`, `СЕЛЕЗЕНКА:`, `КИШЕЧНИК:`, `СОСУДЫ:`, `ЛИМФАТИЧЕСКИЕ УЗЛЫ:`, `МЯГКИЕ ТКАНИ:`, `КОСТНЫЕ СТРУКТУРЫ:`
    - **I6**: `РКТ-признаки патологических изменений органов брюшной полости не выявлены.` present verbatim
    - **I7**: `Консультация направившего специалиста.` present verbatim
    - **I8**: all edges resolve
    - **I9**: BFS from start reaches every node — on BOTH contrast paths
    - **I10**: all `==…==` placeholders well-formed; no `<% tp.file.cursor` substrings remaining

    Pitfall scan:
    - Pitfall 8: no chest section headers (ЛЕГКИЕ, ПЛЕВРА, СРЕДОСТЕНИЕ) — would be Russian-named with `:` suffix
    - Pitfall 9: no `ОЗП`, `ОМТ`, `ПКОП`, `ОГК`, `ШЕЯ`-prefixed paths in any snippet path (other than `ШЕЯ` if the optional D-03 scanned-levels block was added)
    - Pitfall 10 N/A (this is ОБП, not ОМТ)

    Fix any failures by re-opening the relevant earlier task.
  </action>
  <verify>
    All 10 invariants pass on both contrast branches. Pitfall scan clean.
  </verify>
  <acceptance_criteria>
    - All 10 invariants I1-I10 pass against the JSON
    - BFS from start reaches every node on each of the 2 contrast paths
    - No Templater syntax (`<% tp.file.cursor`) remains
    - No cross-canvas snippet leakage
    - No leftover ОГК-specific section headers from a copy-skeleton (Pitfall 8)
  </acceptance_criteria>
  <done>All invariants pass; pitfall scan clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 8: Manual end-to-end verification — author runs ОБП canvas in Obsidian (both contrast branches)</name>
  <files>ОБП full 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. The author manually walks the 2 contrast branches as described in the <how-to-verify> block below. The action is "open the canvas in Obsidian Protocol Runner, click Run from start, walk every applicable section, copy assembled output, visual-diff against the matching .md template at Z:\projectseferences\". See <how-to-verify> for the step-by-step.</action>
  <verify>Manual end-to-end verification per <how-to-verify> block. The runner walks start → all sections → terminal Заключение/Рекомендации without throwing; output structurally matches the matching .md template; both invariants AND runner walkthrough succeed.</verify>
  <done>Author types "approved" per <resume-signal> after confirming the runner walkthrough succeeded and output diff against the .md template is clean. CANVAS-LIB-02 satisfied.</done>
  <what-built>
    `ОБП full 1.0.0.canvas` in author's vault — start + «Контраст вводился?» 3-fan + 9 sections + 2 loops (ПЕЧЕНЬ + ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА) + ~9 snippet pickers + terminal Заключение/Рекомендации, derived verbatim from `Z:\projects\references\ОБП без контраста.md` and `ОБП с контрастом.md`. Static invariants I1-I10 pass per Task 7.
  </what-built>
  <how-to-verify>
    Per VALIDATION.md sampling rate: ОБП ×2 (run both contrast branches).

    1. Open Obsidian. Open `ОБП full 1.0.0.canvas`.
    2. **Run 1 — no contrast branch:**
       a. Click "Run from start". Pick «Нет» at the Контраст question.
       b. Walk every section. At ПЕЧЕНЬ loop, pick "Нет" once (loop returns), then `+` exit. Same at ПОДЖЕЛУДОЧНАЯ ЖЕЛЕЗА loop.
       c. Confirm СОСУДЫ section is SKIPPED on this branch (the «Аорта в брюшном отделе не расширена» linear sentence appears instead).
       d. Walk to terminal. Copy assembled output. Visual diff against `Z:\projects\references\ОБП без контраста.md` lines 4-46. Every section header from .md must appear; Заключение and Рекомендации verbatim.
    3. **Run 2 — contrast branch:**
       a. Click "Run from start". Pick «Да, болюсно» at the Контраст question.
       b. Walk every section. Confirm СОСУДЫ section IS visited; the «Аорта...» linear sentence is NOT visited.
       c. Confirm both loops cycle and exit.
       d. Walk to terminal. Visual diff against `Z:\projects\references\ОБП с контрастом.md` lines 4-47.
    4. Confirm no runner errors, no JS console errors, no stuck pickers on either run.
    5. Per Phase 70 visual hint: confirm `+` exit buttons on the two loops are visually distinguishable.
  </how-to-verify>
  <acceptance_criteria>
    - Both contrast paths run start → terminal without throwing (CANVAS-LIB-02 success criterion 2)
    - Every section header from the matching .md file appears in the assembled output for each branch
    - The verbatim Заключение `РКТ-признаки патологических изменений органов брюшной полости не выявлены.` appears (D-19, D-22)
    - The verbatim Рекомендации `Консультация направившего специалиста.` appears
    - СОСУДЫ section appears on contrast branch only; «Аорта...» linear sentence appears on no-contrast branch only
    - Both loops cycle on body picks and exit via `+` (D-11, Pitfall 1)
    - Fill-in chips clickable and substituting correctly
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark CANVAS-LIB-02 satisfied. If runner threw, output mismatched, or any invariant from VALIDATION.md "Validation Sign-Off" failed, describe the issue and re-open the relevant earlier task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase has no security surface. The canvas is a local file in the author's vault, edited via Obsidian UI, consumed by the existing Protocol Runner. No network, no untrusted input, no new IPC. The plugin's existing canvas-loading trust boundary (file system → plugin) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | n/a | Local vault canvas authoring | accept | No threat surface — local file authoring with no network, no new untrusted input, same trust boundary that the existing plugin canvas runner already operates within. |
</threat_model>

<verification>
**Static (verifier-runnable per VALIDATION.md):**
- All 10 structural invariants I1–I10 pass against `ОБП full 1.0.0.canvas` (Task 7)

**Manual-only (per VALIDATION.md "Manual-Only Verifications" row CANVAS-LIB-02):**
- Author runs both contrast branches end-to-end in Obsidian Protocol Runner; copies assembled output; visual-diffs against the matching .md template (Task 8)
</verification>

<success_criteria>
**ROADMAP §"Phase 72" Success Criterion 2 (CANVAS-LIB-02):**
An ОБП (органы брюшной полости — full version) canvas exists and runs end-to-end with output matching the full ОБП `.md` text template.

**Per VALIDATION.md Sign-Off entries applicable to this plan:**
- [ ] Canvas passes I1–I10
- [ ] Manually run end-to-end both contrast branches (2× total)
- [ ] Visual diff against ОБП без контраста.md and ОБП с контрастом.md confirms every heading appears
- [ ] Fixed Заключение and Рекомендации auto-emitted at terminus
- [ ] No `+`-prefix loop-exit edges missing (Pitfall 1)
- [ ] No body branch missing return-to-header edge (Pitfall 2)
- [ ] No snippet node with both subfolderPath and snippetPath (Pitfall 3 / I4)
- [ ] No cross-canvas snippet leakage (Pitfall 9 / D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/72-canvas-library-full-algorithmic-canvases/72-02-SUMMARY.md` summarizing:
- Final canvas filename and vault path
- Node and edge count
- Snippet path values used
- Whether the canvas was modeled with 2 parallel contrast trunks or single trunk with conditional branches at СОСУДЫ
- Manual run-through outcome for each contrast branch (passed / issues found and resolved)
</output>
