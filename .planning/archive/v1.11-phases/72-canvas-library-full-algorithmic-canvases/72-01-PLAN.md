---
phase: 72
plan: 01
plan_id: 72-01-PLAN
title: ГМ (головной мозг) authoring guide
type: execute
wave: 1
depends_on: []
files_modified:
  - "ГМ 1.0.0.canvas (author's vault — not in repo)"
autonomous: false
requirements: [CANVAS-LIB-01]
tags: [canvas-authoring, content, obsidian]
must_haves:
  truths:
    - "A ГМ canvas exists in the author's vault and runs end-to-end in the Protocol Runner without throwing"
    - "The output produced by the runner contains every section header from `Z:\\projects\\references\\ГМ.md` (СРЕДИННЫЕ СТРУКТУРЫ, ВЕЩЕСТВО ГОЛОВНОГО МОЗГА, ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА, СЕЛЛЯРНАЯ ОБЛАСТЬ, ОКОЛОНОСОВЫЕ ПАЗУХИ, ОРБИТЫ, КОСТНЫЕ СТРУКТУРЫ)"
    - "The terminal output contains the verbatim Заключение `РКТ-признаков патологических изменений в веществе головного мозга не выявлено.` and the verbatim Рекомендации `Консультация направившего специалиста.`"
    - "The focal-lesion loop in ВЕЩЕСТВО ГОЛОВНОГО МОЗГА exits cleanly via a `+`-prefix edge"
    - "All `==fill-in==` placeholders inserted in answer/text-block/snippet text are well-formed (matched `==`)"
  decisions:
    - "D-01: Mixed structure (linear sections + focal-lesions loop) as in ОГК 1.10.0"
    - "D-02: Standalone canvas with own conclusion (ГМ independent)"
    - "D-03: Optional 'НА СКАНИРОВАННЫХ УРОВНЯХ' tail — author's call (recommended skip first pass)"
    - "D-04: No cross-canvas snippet links (Pitfall 9 acceptance check)"
    - "D-06: ГМ focal-lesions loop inside ВЕЩЕСТВО ГОЛОВНОГО МОЗГА section"
    - "D-11: '+' prefix on loop exit edge per Phase 50.1"
    - "D-12: Hybrid linear + nested-loop modeling per ОГК"
    - "D-13: Snippet nodes inserted as template placeholders"
    - "D-14: Snippet paths configured manually by author later"
    - "D-15: No pre-created snippet folders"
    - "D-16: Snippet format (.json placeholder vs .md verbatim) per author choice"
    - "D-17: Standalone ГМ report"
    - "D-18: No cross-canvas references"
    - "D-19: Terminal Заключение + Рекомендации auto-output"
    - "D-20: End-to-end run without errors"
    - "D-21: All ГМ.md sections present in output"
    - "D-22: Fixed Заключение/Рекомендации blocks auto-output"
    - "D-23: '==напишу что не так==' free-text branches valid"
    - "D-24: Fill-in placeholders considered filled after user completes"
    - "D-25: Visual comparison vs ГМ.md template"
    - "D-29: Templater '<% tp.file.cursor() %>' → '==fill-in==' chips"
  artifacts:
    - path: "ГМ 1.0.0.canvas"
      provides: "Standalone ГМ algorithmic canvas (author's vault, not committed)"
      contains: "exactly one node with radiprotocol_nodeType:start; one loop in ВЕЩЕСТВО ГОЛОВНОГО МОЗГА section; ≥7 text-block section headers each beginning with `\\n`"
  key_links:
    - from: "start node"
      to: "Section 1 question/text-block (СРЕДИННЫЕ СТРУКТУРЫ)"
      via: "outgoing edge from start"
      pattern: "no contrast question for ГМ — proceed directly into the first section per template (head CT is without contrast)"
    - from: "ВЕЩЕСТВО ГОЛОВНОГО МОЗГА loop"
      to: "ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА section"
      via: "loop exit edge whose label starts with `+`"
      pattern: "label like `+Все указано, продолжаем` (Phase 50.1 convention)"
    - from: "КОСТНЫЕ СТРУКТУРЫ section"
      to: "Terminal Заключение/Рекомендации text-block"
      via: "linear edge"
      pattern: "Заключение text inside terminal text-block.text matches the .md verbatim"
---

<objective>
Author the ГМ (головной мозг) algorithmic canvas in the author's local vault. The canvas mirrors the structure of `Z:\projects\references\ОГК 1.10.0.canvas` but uses the section list and fixed text from `Z:\projects\references\ГМ.md` (D-01, D-02, D-06, D-19, D-25). When run end-to-end in the Protocol Runner, the assembled output structurally matches `ГМ.md`.

Purpose: deliver CANVAS-LIB-01 — author's reference canvas for head-CT reports. Lowest-complexity of the five Phase 72 canvases, recommended starting point per RESEARCH.md "Authoring Workflow Recommendation".
Output: `ГМ 1.0.0.canvas` in author's vault (filename per RESEARCH.md "Filename and Vault Path Convention"; not committed to this repository per phase scope and D-04).
</objective>

<execution_context>
This plan is executed by the **author working manually in Obsidian Canvas UI** — not by an automated code-execution agent. There is no `npm run build`, no `vitest`, no commit to `src/`. Every task is a sequence of clicks/drags/edits in Obsidian's Canvas pane plus optional JSON inspection in a text editor for invariant verification.

`autonomous: false` for every task — each task pauses for the author.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md

External (author-side, not in repo):
- Z:\projects\references\ОГК 1.10.0.canvas (reference — read-only frozen template)
- Z:\projects\references\ГМ.md (section list + fixed Заключение/Рекомендации)
- Z:\projects\references\SNIPPETS\ГМ\ (16 ready snippets per RESEARCH §1)
- Z:\projects\references\SNIPPETS\КОСТИ\ (6 ready snippets — used by КОСТНЫЕ СТРУКТУРЫ)
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Create ГМ canvas skeleton</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — copy the start-node shape only
    - Z:\projects\references\ГМ.md — confirm head CT has no contrast question; the canvas starts directly into section 1 (per RESEARCH §1: "single file, 44 lines, no contrast variant")
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Per-Canvas Blueprints — §1 ГМ" (lines ~266-306)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md decisions D-01, D-02, D-03 (optional scanned-levels block)
  </read_first>
  <action>
    1. Open Obsidian, create a new canvas named exactly `ГМ 1.0.0.canvas` in the author's working canvases folder (NOT in `Z:\projects\references\` — that folder is the read-only template area per RESEARCH "Filename and Vault Path Convention"). Use the same `<area> <semver>.canvas` naming pattern as `ОГК 1.10.0.canvas`; this is the first authored canvas so version `1.0.0`.
    2. Drag a `Start` node from the RadiProtocol Node Editor onto the canvas. Verify in the Node Editor panel that its attributes are: `radiprotocol_nodeType:"start"`, `text:""`, `color:"4"` (green). Place it at the top of the canvas (per D-01: mirror ОГК shape — start at top of vertical spine).
    3. Drop a `text-block` node directly below the start node with text `\nМСКТ-исследование выполнено по стандартной программе с последующей мультипланарной реконструкцией.` (verbatim from `ГМ.md` line 4 — this is the procedure description that prefixes the report; the leading `\n` produces a blank-line separator per Pitfall 4). Note: ГМ has no «Контраст вводился?» fan-out (head CT in this template is non-contrast — RESEARCH §1 line 268), so the start node connects directly into this procedure text-block.
    4. Draw a single edge from the start node (bottom side) to the procedure text-block (top side). No edge label needed.
    5. Save the canvas (Ctrl+S). Do NOT yet build any sections — that is Task 2.
  </action>
  <verify>
    Manual + JSON inspection. Open `ГМ 1.0.0.canvas` in a text editor; confirm exactly one `"radiprotocol_nodeType":"start"` substring (I1) and one `text-block` node whose `text` field begins with `\nМСКТ-исследование выполнено по стандартной программе`.
  </verify>
  <acceptance_criteria>
    - File `ГМ 1.0.0.canvas` exists in author's vault (NOT under `Z:\projects\references\`)
    - JSON contains exactly one occurrence of `"radiprotocol_nodeType":"start"` (I1)
    - JSON contains a node with `"radiprotocol_nodeType":"text-block"` whose `text` field starts with `\nМСКТ-исследование выполнено по стандартной программе`
    - JSON contains an edge whose `fromNode` resolves to the start node and `toNode` resolves to the procedure text-block (I8)
    - The start node's `color` is `"4"` (green) per RESEARCH §"Conventions" rule 1
  </acceptance_criteria>
  <done>Canvas file exists with start node + procedure text-block connected by one edge; opens in Obsidian without errors; runner Run-from-start command completes the first node without throwing.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Build the 7 linear sections from ГМ.md (no loops yet)</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ГМ.md (section list lines 6-35; copy normal-finding text verbatim per section)
    - Z:\projects\references\ОГК 1.10.0.canvas — for the per-organ pattern shape (text-block header → question → 2-3 answers → optional snippet picker → optional `==пишу что не так==`)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§1 ГМ — Section list" (lines 270-282) and § "Conventions the 5 new canvases must follow" rules 3-4
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-01, D-23 (==напишу что не так== free-text branches valid)
  </read_first>
  <action>
    For each of the 7 sections below, drop (a) one `text-block` node as the section header, (b) one `question` node asking «Норма?» / «Есть изменения?», (c) one `answer` node carrying the verbatim normal-finding text from `ГМ.md`, and (d) one `==напишу что не так==` free-text answer node (per D-23). Wire them: previous-section terminus → text-block → question → branches. Section 2 (ВЕЩЕСТВО ГОЛОВНОГО МОЗГА) is a placeholder for now — the loop is added in Task 3.

    Sections (each text-block's `text` MUST begin with `\n` — Pitfall 4):

    1. **СРЕДИННЫЕ СТРУКТУРЫ** — text-block text: `\nСРЕДИННЫЕ СТРУКТУРЫ:`. Norm answer text (verbatim ГМ.md line 7): `Не смещены.`
    2. **ВЕЩЕСТВО ГОЛОВНОГО МОЗГА** — text-block text: `\nВЕЩЕСТВО ГОЛОВНОГО МОЗГА:`. Norm answer text (verbatim ГМ.md lines 10-13):
       ```
       Белое и серое вещество больших полушарий развиты типично, обычной плотности.
       Мозолистое тело, зрительный бугор, ствол мозга и мозжечок нормальной плотности.
       Миндалины мозжечка выше уровня линии Мак-Рея.
       Очагов патологической плотности, объемных образований в веществе головного мозга и по ходу оболочек не выявлено.
       ```
       (Task 3 will replace the last sentence with a loop — leave a placeholder edge for now.)
    3. **ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА** — text-block text: `\nЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА:`. Norm answer text (verbatim ГМ.md lines 16-19):
       ```
       Субарахноидальное конвекситальное пространство и цистерны мозга не расширены.
       Желудочки головного мозга обычной формы, не изменены.
       Боковые желудочки симметричны, не расширены.
       Ширина III-его желудочка до 5 мм.
       ```
    4. **СЕЛЛЯРНАЯ ОБЛАСТЬ** — text-block text: `\nСЕЛЛЯРНАЯ ОБЛАСТЬ:`. Norm answer text (verbatim ГМ.md lines 22-24):
       ```
       Турецкое седло обычной формы и величины.
       Размеры, плотность и структура гипофиза в норме.
       Параселлярные структуры обычной плотности и локализации.
       ```
    5. **ОКОЛОНОСОВЫЕ ПАЗУХИ** — text-block text: `\nОКОЛОНОСОВЫЕ ПАЗУХИ:`. Norm answer text (verbatim ГМ.md line 27): `Без нарушения развития и пневматизации.`
    6. **ОРБИТЫ** — text-block text: `\nОРБИТЫ:`. Norm answer text (verbatim ГМ.md line 30): `Расположены симметрично, нормальных размеров с обычным развитием орбитальных конусов. Стенки орбит не изменены.`
    7. **КОСТНЫЕ СТРУКТУРЫ** — text-block text: `\nКОСТНЫЕ СТРУКТУРЫ:`. Norm answer text (verbatim ГМ.md lines 33-35):
       ```
       Краниовертебральный переход без особенностей.
       Сосцевидные отростки и пирамиды височных костей пневматизированы.
       Кости свода и основания черепа без деструктивных, травматических изменений.
       ```

    Wiring rule: previous section's question's branches all converge at the next section's text-block (mirroring ОГК's "answer→shared-text-block" convergence pattern, RESEARCH §"Excerpt A" + §"Edge inventory"). Color attributes per RESEARCH §"Node-kind inventory": question=`"5"` (blue), answer=`"2"` (red), text-block=`"3"` (yellow).

    Save canvas after each section so progress survives a crash.
  </action>
  <verify>
    Manual: open the canvas in Obsidian and run from start. The runner should walk through 7 sections in order (with section 2 currently linear — looping is Task 3). Output must contain all 7 section header strings (`СРЕДИННЫЕ СТРУКТУРЫ:`, `ВЕЩЕСТВО ГОЛОВНОГО МОЗГА:`, etc.).
    JSON: search for each section header text — every entry must be a `"radiprotocol_nodeType":"text-block"` whose `text` value starts with `\n` followed by the verbatim header text.
  </verify>
  <acceptance_criteria>
    - JSON contains exactly 7 text-block nodes whose `text` value matches the 7 section headers above (each beginning with `\n`) — invariant I5 partially satisfied; full I5 satisfied after Tasks 6 (terminal block)
    - Each section's normal-finding `answer` node carries the verbatim text from `ГМ.md` (search the JSON for the first 5 words of each normal text)
    - Each section has at least one `==напишу что не так==` free-text answer (D-23) whose `text` field contains the substring `==`
    - All edges resolve (I8): no `fromNode` or `toNode` value points to a non-existent node id
    - Running the canvas from the start node in Obsidian Protocol Runner walks through all 7 sections in order without the runner throwing
  </acceptance_criteria>
  <done>Canvas walks all 7 sections linearly in the runner; output contains every section header in document order; section 2 is wired but not yet loop-shaped.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Wire the focal-lesions loop in ВЕЩЕСТВО ГОЛОВНОГО МОЗГА</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpt B in 72-RESEARCH.md (loop with body branches and `+`-prefix exit, lines 162-177); look at ОГК's «Видны ли очаговые, интерстициальные изменения?» loop in the lung section as the canonical body-branch shape
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Loop Modeling Pattern" (lines 218-260) — Author-facing checklist for any loop
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-06 (ГМ loop for multiple focal lesions), D-11 (`+`-prefix exit per Phase 50.1), D-12 (hybrid as in ОГК)
  </read_first>
  <action>
    Rewire section 2 (ВЕЩЕСТВО ГОЛОВНОГО МОЗГА) so the last sentence "Очагов патологической плотности..." is replaced by a loop that captures multiple focal lesions per D-06.

    1. Drop a `loop` node with attributes `radiprotocol_nodeType:"loop"`, `radiprotocol_headerText:"Видны ли очаги патологической плотности?"`, `text:"Видны ли очаги патологической плотности?"`, `color:"1"` (per RESEARCH §"Node-kind inventory" loop row).
    2. Wire 3 body branches outgoing from the loop node (each carries an edge `label`, none starting with `+`):
       - Body branch A — answer node, edge `label:"Нет"`, answer text (verbatim from ГМ.md line 13): `Очагов патологической плотности, объемных образований в веществе головного мозга и по ходу оболочек не выявлено.`
       - Body branch B — snippet node (folder picker), edge `label:"Выберу готовый шаблон очага"`, snippet attributes: `radiprotocol_nodeType:"snippet"`, `radiprotocol_subfolderPath:"ГМ"`, `radiprotocol_snippetLabel:"Выберу готовый шаблон очага"`, `text:"ГМ"`, `color:"6"` (per RESEARCH §"Excerpt C" snippet shape; folder per RESEARCH §1 snippet table — "ГМ/" 16 ready snippets per D-13/D-14 author may refine the path later).
       - Body branch C — answer node, edge `label:"Опишу вручную"`, answer text: `==опишу очаг==` (free-text placeholder per D-23, D-29; well-formed `==…==` per I10).
    3. Each body branch's terminal node MUST have a return edge back to the loop header (Pitfall 2). Draw three return edges: from each body answer/snippet node back to the loop node.
    4. Draw exactly one `+`-prefix exit edge from the loop node to the next section's text-block (`\nЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА:`). Edge `label:"+Все указано, продолжаем"` (verbatim Phase 50.1 convention text from ОГК reference, RESEARCH §"Excerpt B").
    5. Remove the old linear "Очагов..." answer node that was placed in Task 2 — it is now subsumed by body branch A. Verify no orphaned edges remain (I8: every edge's `fromNode`/`toNode` resolves).
    6. Save canvas.
  </action>
  <verify>
    Run the canvas from the start in Obsidian. At ВЕЩЕСТВО ГОЛОВНОГО МОЗГА, the runner shows a picker with 4 options: "Нет" / "Выберу готовый шаблон очага" / "Опишу вручную" / "Все указано, продолжаем". Per Phase 70 visual hint, the exit option has a distinct background. Pick "Нет" — runner returns to the same picker (loop cycle). Pick "Все указано, продолжаем" — runner advances to ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА section.
  </verify>
  <acceptance_criteria>
    - JSON contains exactly one `"radiprotocol_nodeType":"loop"` node whose `radiprotocol_headerText` is `"Видны ли очаги патологической плотности?"`
    - The loop node has at least one outgoing edge whose `label` starts with `"+"` (I2) and exactly one such `+`-prefix edge per single-loop-exit rule (D-11)
    - The loop node has at least 3 body edges (no `+` prefix) — covering the "Нет" / snippet / "Опишу вручную" branches (I3)
    - The snippet node body branch carries `radiprotocol_subfolderPath:"ГМ"` AND does NOT carry `radiprotocol_snippetPath` (I4 — exactly one of the two keys)
    - The free-text answer's `text` contains a well-formed `==…==` substring (I10) — match `==[^=]+==` once
    - Each of the 3 body branches has a return-edge from the body's terminal node back to the loop header node (no orphaned body branches — Pitfall 2)
    - Running the canvas: picking the `+` option advances to ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА; picking any body option returns to the loop picker
  </acceptance_criteria>
  <done>Loop runs cyclically in the runner, exits cleanly via `+Все указано, продолжаем`, and the assembled output contains all picked body-branch contributions plus the loop continues to the next section.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Insert snippet placeholder nodes for ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА, ОРБИТЫ, КОСТНЫЕ СТРУКТУРЫ</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ОГК 1.10.0.canvas — Excerpts C and D in 72-RESEARCH.md (subfolderPath vs snippetPath shapes, lines 179-198)
    - Z:\projects\references\SNIPPETS\ГМ\ folder listing (16 snippets including ЖЕЛУДОЧКИ - расширенное описание.md, РАСШИРЕНИЕ ликоврных пространств.md, ФТИЗИС глазного яблока.md, ДЕФОРМАЦИЯ медиальной стенки глазницы.md per RESEARCH §1 snippet table)
    - Z:\projects\references\SNIPPETS\КОСТИ\ folder listing (6 snippets)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "§1 ГМ — Snippet insertion points" (lines ~285-291)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-13, D-14, D-15, D-16
  </read_first>
  <action>
    Add snippet picker entries to three sections per RESEARCH §1 snippet table. Author's discretion (D-14, D-15) per which exact snippet path — recommended values below match the existing `SNIPPETS\` folder layout at `Z:\projects\references\` and may be refined later.

    1. **ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА** — add two file-bound snippet branches off the section's question node (alongside the norm answer and the `==напишу что не так==` already created in Task 2):
       - Snippet node A: `radiprotocol_nodeType:"snippet"`, `radiprotocol_snippetPath:"ГМ/ЖЕЛУДОЧКИ - расширенное описание.md"`, `radiprotocol_snippetLabel:"Расширенное описание желудочков"`, `text:"ЖЕЛУДОЧКИ - расширенное описание"`, `color:"6"`. Edge label: `Расширенное описание желудочков`.
       - Snippet node B: `radiprotocol_snippetPath:"ГМ/РАСШИРЕНИЕ ликоврных пространств.md"`, `radiprotocol_snippetLabel:"Расширение ликворных пространств"`, `text:"РАСШИРЕНИЕ ликворных пространств"`, `color:"6"`. Edge label: `Расширение ликворных пространств`.
       Each snippet node carries `radiprotocol_snippetPath` AND NOT `radiprotocol_subfolderPath` (I4).

    2. **ОРБИТЫ** — add two file-bound snippet branches off the section's question node:
       - Snippet node A: `radiprotocol_snippetPath:"ГМ/ФТИЗИС глазного яблока.md"`, `radiprotocol_snippetLabel:"Фтизис глазного яблока"`, `text:"ФТИЗИС глазного яблока"`, `color:"6"`. Edge label: `Фтизис глазного яблока`.
       - Snippet node B: `radiprotocol_snippetPath:"ГМ/ДЕФОРМАЦИЯ медиальной стенки глазницы.md"`, `radiprotocol_snippetLabel:"Деформация медиальной стенки"`, `text:"ДЕФОРМАЦИЯ медиальной стенки глазницы"`, `color:"6"`. Edge label: `Деформация медиальной стенки`.

    3. **КОСТНЫЕ СТРУКТУРЫ** — add one folder-picker snippet branch off the section's question node:
       - Snippet node: `radiprotocol_subfolderPath:"КОСТИ"`, `radiprotocol_snippetLabel:"Выберу шаблон по костям"`, `text:"КОСТИ"`, `color:"6"` (per RESEARCH §1 snippet table — folder picker for the 6 ready KОСТИ snippets). Edge label: `Выберу шаблон по костям`. Carries `radiprotocol_subfolderPath` AND NOT `radiprotocol_snippetPath` (I4).

    All snippet nodes converge back into the section's "next-section" path (the same way the norm and free-text answers do). No cross-canvas snippet leakage (Pitfall 9, D-04, D-18) — the only paths used are `ГМ/...` and `КОСТИ/` (the latter is a shared kostnaya-system folder, not another canvas).

    Save canvas.
  </action>
  <verify>
    Run canvas; at each of the 3 sections the runner picker should list extra snippet entries alongside "Норма?" / "==напишу что не так==". Picking a file-bound snippet (e.g., "Фтизис глазного яблока") inserts the snippet's verbatim contents from the file at `SNIPPETS\ГМ\ФТИЗИС глазного яблока.md`. Picking the КОСТИ folder picker opens the snippet picker modal listing the 6 available КОСТИ snippets.
  </verify>
  <acceptance_criteria>
    - JSON contains 5 snippet nodes total in this canvas (2 in ЛИКВОРОСОДЕРЖАЩИЕ + 2 in ОРБИТЫ + 1 in КОСТНЫЕ СТРУКТУРЫ) plus the 1 already added in Task 3 (ВЕЩЕСТВО ГОЛОВНОГО МОЗГА focal-lesion folder picker) = 6 snippet nodes total
    - Every snippet node carries exactly one of `radiprotocol_subfolderPath` or `radiprotocol_snippetPath` (I4) — verifier scans all 6 nodes
    - No snippet path value contains `ОБП`, `ОЗП`, `ОМТ`, `ПКОП`, `ОГК` (Pitfall 9 / D-04 — cross-canvas snippet leakage check)
    - The 4 file-bound snippets reference paths that exist under `Z:\projects\references\SNIPPETS\ГМ\` (author's discretion to swap to author's own SNIPPETS folder per D-14/D-15)
    - Running the canvas: each of the 3 sections shows the new snippet picker entries; picking each one produces non-empty output (snippet contents)
  </acceptance_criteria>
  <done>All 3 snippet-bearing sections offer their snippet picker entries; runner emits snippet contents when picked; no cross-canvas snippet leakage.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Map Templater placeholders to ==fill-in== chips</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ГМ.md — search for all `<% tp.file.cursor() %>` (D-29; ГМ.md has 1 occurrence at line 1)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-29 (Templater → `==fill-in==`)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls 5" (lines 660-664) on `==…==` well-formedness
  </read_first>
  <action>
    Per D-29, every `<% tp.file.cursor() %>` Templater placeholder in `ГМ.md` becomes a Phase-27 `==fill-in==` chip in the canvas's text. ГМ.md has the cursor placeholder only at line 1 (the report header — typically a "study type" line filled at note creation). For ГМ this maps to a fill-in chip at the very top of the procedure text-block created in Task 1, OR it may be omitted (the procedure description text itself stands alone).

    1. Edit the procedure text-block (created in Task 1) to read: `\n==тип исследования==\nМСКТ-исследование выполнено по стандартной программе с последующей мультипланарной реконструкцией.` (the first `==тип исследования==` is a fill-in placeholder for the optional study-type prefix). Verify the placeholder is well-formed (matched `==`).
    2. Verify all `==…==` placeholders previously inserted in Task 2 free-text answers (`==напишу что не так==` and similar) and Task 3 (`==опишу очаг==`) remain well-formed. Use a regex search in the .canvas JSON: every `==` must pair with a closing `==` on the same line.
    3. Save canvas.

    No other Templater placeholders need mapping — ГМ.md has only the single header `<% tp.file.cursor() %>` (D-29 + RESEARCH §1 confirms ГМ has no per-section `tp.file.cursor(N)` size placeholders, unlike ОБП).
  </action>
  <verify>
    JSON regex check: every occurrence of `==` is followed by a matching `==` before any newline (I10). Run the canvas; confirm the runner renders fill-in chips that are clickable to open the Phase-27 fill-in modal.
  </verify>
  <acceptance_criteria>
    - JSON contains the procedure text-block whose `text` field includes a well-formed `==тип исследования==` placeholder
    - Every `==` substring in the JSON pairs with a closing `==` on the same line (I10) — verifier counts: total `==` occurrences must be even, and `==[^=\n]+==` regex must match every pair
    - Running the canvas in Obsidian: each fill-in chip is clickable and opens the Phase-27 fill-in modal; submitting a value substitutes that value into the output
  </acceptance_criteria>
  <done>All `==fill-in==` placeholders are well-formed; runner shows clickable chips at each placeholder position.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Append terminal Заключение + Рекомендации text-block</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - Z:\projects\references\ГМ.md lines 38-44 (Заключение and Рекомендации blocks — verbatim)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-CONTEXT.md D-19, D-22 (each canvas ends with own Заключение + Рекомендации)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Conventions" rule 8 (line 144) — terminal text-block holds verbatim conclusion text
  </read_first>
  <action>
    Drop a single terminal `text-block` node at the end of the canvas (after section 7 КОСТНЫЕ СТРУКТУРЫ branches converge). Its `text` field MUST be exactly (verbatim from `ГМ.md` lines 38-44):

    ```
    \n## Заключение\nРКТ-признаков патологических изменений в веществе головного мозга не выявлено.\n\n## Рекомендации\nКонсультация направившего специалиста.
    ```

    (Use literal `\n` newline characters in the JSON. The leading `\n` provides blank-line separation per Pitfall 4. The Markdown `## Заключение` and `## Рекомендации` headers from the .md template are preserved verbatim per D-22.)

    Wire all 3 outgoing branches from the КОСТНЫЕ СТРУКТУРЫ section's question (norm answer / `==напишу что не так==` / KОСТИ snippet picker return) into this single terminal text-block node — convergence pattern per RESEARCH §"Edge inventory" / Excerpt A.

    Optional (per D-03 — author's call): if the author wants to capture findings outside the head, prepend a `\nНА СКАНИРОВАННЫХ УРОВНЯХ:` text-block + a small loop before the terminal Заключение. Per RESEARCH §"Shared sub-structures" — strip cross-canvas snippets (no `ОБП`, `ОЗП` snippet pickers), since ГМ is a brain CT. Recommended: skip this optional block for ГМ first pass (D-03 marks it optional). Author may add later.

    Save canvas.
  </action>
  <verify>
    Run canvas from start through every section to the terminal node. The runner's final assembled output's last lines must contain the verbatim Заключение and Рекомендации text from ГМ.md.
  </verify>
  <acceptance_criteria>
    - JSON contains a `text-block` node whose `text` value contains both substrings: `РКТ-признаков патологических изменений в веществе головного мозга не выявлено.` (I6) AND `Консультация направившего специалиста.` (I7)
    - Both substrings appear verbatim (no extra spaces, no typos) — direct grep against ГМ.md lines 39 and 43
    - All 3 KОСТНЫЕ СТРУКТУРЫ branches' terminal nodes have an outgoing edge to this terminal text-block (single convergence point)
    - The terminal text-block is reachable from the start node by BFS over the edge DAG (I9)
    - Running the canvas to completion: the assembled output's last block contains both fixed strings exactly as in ГМ.md
  </acceptance_criteria>
  <done>Canvas has a single terminal Заключение/Рекомендации text-block with verbatim text from ГМ.md; all section paths converge into it.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Static invariant check (verifier-runnable on the JSON)</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md § "Structural Invariants" (I1-I10) and § "Validation Sign-Off" checklist
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-RESEARCH.md § "Pitfalls and Landmines" (lines 627-697) — all 10 pitfalls
  </read_first>
  <action>
    Open `ГМ 1.0.0.canvas` in a text editor (or run the verifier via `gsd-verify-work`). Walk the I1-I10 invariants from VALIDATION.md against the JSON. Each MUST hold:

    - **I1**: `grep -c '"radiprotocol_nodeType":"start"' "ГМ 1.0.0.canvas"` returns exactly `1`
    - **I2**: For every node whose `radiprotocol_nodeType` is `"loop"` (1 node — the focal-lesion loop), at least one outgoing edge in the canvas `edges` array has a `label` starting with `"+"`. Count of `+`-prefix edges per loop should be exactly 1.
    - **I3**: Same loop has at least one body edge (no label OR label not starting with `"+"`). Expected: 3 body edges (Нет / snippet / opishu vruchnuyu).
    - **I4**: For every node whose `radiprotocol_nodeType` is `"snippet"` (6 nodes), check exactly one of `radiprotocol_subfolderPath` / `radiprotocol_snippetPath` is present.
    - **I5**: All 7 section headers (`СРЕДИННЫЕ СТРУКТУРЫ:`, `ВЕЩЕСТВО ГОЛОВНОГО МОЗГА:`, `ЛИКВОРОСОДЕРЖАЩИЕ ПРОСТРАНСТВА:`, `СЕЛЛЯРНАЯ ОБЛАСТЬ:`, `ОКОЛОНОСОВЫЕ ПАЗУХИ:`, `ОРБИТЫ:`, `КОСТНЫЕ СТРУКТУРЫ:`) appear as the trailing portion of a `text-block.text` value, each preceded by `\n`.
    - **I6**: `grep -c 'РКТ-признаков патологических изменений в веществе головного мозга не выявлено.' "ГМ 1.0.0.canvas"` returns ≥1.
    - **I7**: `grep -c 'Консультация направившего специалиста.' "ГМ 1.0.0.canvas"` returns ≥1.
    - **I8**: For every edge in the `edges` array, both `fromNode` and `toNode` resolve to a node id present in the `nodes` array.
    - **I9**: BFS from the start node over outgoing edges reaches every other node — no orphans.
    - **I10**: Every `==` substring in any `text` field pairs with a closing `==` on the same line; no unmatched `==`.

    Pitfall scan also:
    - Pitfall 8: confirm no section header from ОГК (`ЛЕГКИЕ:`, `ПЛЕВРАЛЬНЫЕ ПОЛОСТИ:`, `СЕРДЦЕ:`, etc.) leaked into the canvas
    - Pitfall 9: confirm no snippet path contains `ОБП`, `ОЗП`, `ОМТ`, `ПКОП`, `ОГК`

    If any invariant fails, return to the relevant earlier task and fix.
  </action>
  <verify>
    All 10 invariants pass. Pitfall scan finds no leaks.
  </verify>
  <acceptance_criteria>
    - I1 holds: exactly 1 start node
    - I2 holds: focal-lesion loop has exactly 1 `+`-prefix exit edge
    - I3 holds: focal-lesion loop has 3 body edges
    - I4 holds: all 6 snippet nodes carry exactly one of subfolderPath/snippetPath
    - I5 holds: all 7 ГМ section headers present as text-block content prefixed by `\n`
    - I6 holds: Заключение string present verbatim
    - I7 holds: Рекомендации string present verbatim
    - I8 holds: no dangling edge references
    - I9 holds: BFS from start reaches every node (no orphans)
    - I10 holds: all `==…==` placeholders well-formed
    - Pitfall 8 scan: no chest section headers (ЛЕГКИЕ, СРЕДОСТЕНИЕ, ПЛЕВРА) appear in JSON
    - Pitfall 9 scan: no `ОБП`/`ОЗП`/`ОМТ`/`ПКОП`/`ОГК` appears in any `radiprotocol_subfolderPath` or `radiprotocol_snippetPath`
  </acceptance_criteria>
  <done>All 10 invariants pass; pitfall scan clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" autonomous="false">
  <name>Task 8: Manual end-to-end verification — author runs ГМ canvas in Obsidian</name>
  <files>ГМ 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. The author manually walks the 1 primary path as described in the <how-to-verify> block below. The action is "open the canvas in Obsidian Protocol Runner, click Run from start, walk every applicable section, copy assembled output, visual-diff against the matching .md template at Z:\projectseferences\". See <how-to-verify> for the step-by-step.</action>
  <verify>Manual end-to-end verification per <how-to-verify> block. The runner walks start → all sections → terminal Заключение/Рекомендации without throwing; output structurally matches the matching .md template; both invariants AND runner walkthrough succeed.</verify>
  <done>Author types "approved" per <resume-signal> after confirming the runner walkthrough succeeded and output diff against the .md template is clean. CANVAS-LIB-01 satisfied.</done>
  <what-built>
    `ГМ 1.0.0.canvas` in author's vault — 7 sections + 1 focal-lesion loop + 6 snippet pickers + terminal Заключение/Рекомендации, all derived verbatim from `Z:\projects\references\ГМ.md`. Static invariants I1-I10 pass per Task 7.
  </what-built>
  <how-to-verify>
    1. Open Obsidian. Open `ГМ 1.0.0.canvas` from the author's vault.
    2. Open the Protocol Runner sidebar/inline. Click "Run from start" on the canvas.
    3. Walk every section from start to terminal (1 primary path per VALIDATION.md sampling rate "ГМ ×1"):
       - Section 1 СРЕДИННЫЕ СТРУКТУРЫ: pick "Норма" → output adds `Не смещены.`
       - Section 2 ВЕЩЕСТВО ГОЛОВНОГО МОЗГА: at the loop picker, pick "Нет" once (loop returns), then pick "+Все указано, продолжаем" → output adds the verbatim normal text + advances. Confirm Phase 70 visual hint marks the `+` exit button.
       - Sections 3-7: pick the norm answer for each → output adds verbatim text.
       - Terminal block emits Заключение + Рекомендации.
    4. Copy the assembled output. Visual diff against `Z:\projects\references\ГМ.md` lines 4-44 (the procedure description through the Рекомендации). Every section header and every normal-finding sentence must appear verbatim.
    5. Confirm no runner errors, no JS console errors, no stuck pickers.
    6. Per Pitfall 1 sanity check: confirm the `+` exit edge is present and visually distinguishable in the runner picker.
  </how-to-verify>
  <acceptance_criteria>
    - Runner walks start → 7 sections → terminal Заключение/Рекомендации without throwing
    - Every section header from ГМ.md appears in the assembled output (CANVAS-LIB-01 success criterion 1)
    - The verbatim Заключение `РКТ-признаков патологических изменений в веществе головного мозга не выявлено.` appears as the conclusion (D-19, D-22)
    - The verbatim Рекомендации `Консультация направившего специалиста.` appears immediately after Заключение (D-19, D-22)
    - The focal-lesion loop cycles when body branches are picked and exits via `+` (D-11, Pitfall 1)
    - At least one fill-in chip is clickable and opens Phase-27 fill-in modal
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark CANVAS-LIB-01 satisfied. If runner threw, output mismatched, or any invariant from VALIDATION.md "Validation Sign-Off" failed, describe the issue and re-open the relevant earlier task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase has no security surface. The canvas is a local file in the author's vault, edited via Obsidian UI, consumed by the existing Protocol Runner. No network, no untrusted input, no new IPC. The plugin's existing canvas-loading trust boundary (file system → plugin) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | n/a | Local vault canvas authoring | accept | No threat surface — local file authoring with no network, no new untrusted input, same trust boundary that the existing plugin canvas runner already operates within. Per CONTEXT.md and STATE.md, no `src/` is modified, no new code paths introduced. |
</threat_model>

<verification>
**Static (verifier-runnable on the JSON, per VALIDATION.md):**
- All 10 structural invariants I1–I10 pass against `ГМ 1.0.0.canvas` (Task 7)

**Manual-only (per VALIDATION.md "Manual-Only Verifications" row CANVAS-LIB-01):**
- Author runs `ГМ 1.0.0.canvas` end-to-end in Obsidian Protocol Runner, walks 1 primary path, copies assembled output, visual-diffs against `Z:\projects\references\ГМ.md` (Task 8)
- Output structurally matches ГМ.md: every section header present, Заключение and Рекомендации verbatim
</verification>

<success_criteria>
**ROADMAP §"Phase 72" Success Criterion 1 (CANVAS-LIB-01):**
A ГМ (головной мозг) canvas exists in the author's vault and runs end-to-end in the Protocol Runner using the existing graph node kinds (Question / Answer / Text block / Snippet / Loop), producing a structurally complete ГМ report matching `Z:\projects\references\ГМ.md`.

**Per VALIDATION.md Sign-Off entries applicable to this plan:**
- [ ] Canvas passes static invariants I1–I10
- [ ] Canvas manually run end-to-end at least once (1× primary branch)
- [ ] Visual structural diff against ГМ.md confirms every section heading appears in output
- [ ] Fixed Заключение and Рекомендации auto-emitted at canvas terminus
- [ ] No `+`-prefix loop-exit edges missing (Pitfall 1)
- [ ] No body branch missing return-to-header edge (Pitfall 2)
- [ ] No snippet node with both `subfolderPath` and `snippetPath` (Pitfall 3 / I4)
- [ ] No cross-canvas snippet leakage (Pitfall 9 / D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/72-canvas-library-full-algorithmic-canvases/72-01-SUMMARY.md` summarizing:
- Final canvas filename and vault path (relative path from vault root)
- Node count and edge count
- Snippet path values used (so the author can later re-point per D-14)
- Any deviations from the recommended snippet folders
- Manual run-through outcome (passed / issues found and resolved)
</output>
