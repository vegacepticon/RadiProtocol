---
phase: 73
plan: 02
plan_id: 73-02-PLAN
title: ОБП short — single-trunk linear canvas, no fan-out, no loops
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas (author's vault — outside repo working tree)"
autonomous: false
requirements: [CANVAS-LIB-07]
tags: [canvas-authoring, content, obsidian, obp, short]
must_haves:
  truths:
    - "An `ОБП short 1.0.0.canvas` file exists at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` and is valid JSON parseable by Obsidian"
    - "The canvas runs end-to-end in the Protocol Runner without throwing — author walks 3 sections through to terminal Заключение/Рекомендации"
    - "Each of the 3 organ-system sections (ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ, ЖЕЛЧНЫЙ ПУЗЫРЬ, ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ) presents its own «— норма?» Question with Норма (verbatim norm text) and «Опишу вручную» (==напишу что не так с …== chip) branches per D-01 + D-02"
    - "There are zero `loop` nodes in the canvas per D-04 (short = no multi-finding scenarios)"
    - "There is no Контраст fan-out and no procedure-preamble text-block — `start` connects directly to the first section header per Pattern S5"
    - "The terminal text-block contains the verbatim string `## Заключение\\nРКТ-признаки ==диагноз==\\n\\n## Рекомендации\\nКонсультация направившего специалиста.` per D-03 + D-13"
    - "All 3 norm-text strings appear verbatim from `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` lines 10-12 (functionally equivalent in муж file lines 10-14, normalized to one canonical form per CONTEXT.md «specifics»)"
    - "`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs` exits 0 and prints `[ОБП short 1.0.0.canvas] N pass, 0 fail` covering inherited Phase 72 invariants I1–I10 (D-17): I1 single start, I2/I3 vacuously satisfied (no loops per D-04), I4 each snippet node has subfolderPath XOR snippetPath, I5 each section header text-block present, I6 Заключение anchor `РКТ-признаки` present, I7 Рекомендации present, I8 edge referential integrity, I9 BFS reachability, I10 `==…==` chip well-formedness"
  decisions:
    - "D-01: per-organ-system text-blocks (NOT monolithic) — 3 separate sections"
    - "D-02: per-system Норма? gate — Да-branch verbatim norm / Нет-branch ==напишу что не так== chip"
    - "D-03: terminal `## Заключение` + `## Рекомендации` text-block"
    - "D-04: NO loops in short canvases"
    - "D-08, D-09: snippet-node strategy reusing Phase 72's `SNIPPETS\\ОБП\\` subfolder (single top-level mirror of Phase 72 ОБП full)"
    - "D-10: do NOT pre-create snippet files; embed paths in nodes only"
    - "D-12, D-13: Phase-27 ==fill-in== chip on Заключение line (`==диагноз==`)"
    - "D-14: chip well-formedness invariant I10 enforced"
    - "D-15: programmatic per-canvas .mjs builder reusing Phase 72 `canvas-builder.mjs` via relative import"
    - "D-16: builder writes to `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (outside repo working tree)"
    - "D-17: I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged; builder prints invariant pass count"
    - "D-18: file name `ОБП short 1.0.0.canvas`"
    - "D-19: [informational] vault state observation — `ОБП short 1.0.0.canvas` coexists alongside the existing `ОБП full 1.0.0.canvas` and Phase 72's other canvases at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (no plan task implements this; vault target documented for context)"
    - "D-20: static layer — builder prints I1-I10 invariant pass count; no .canvas committed to repo"
    - "D-21: manual layer — Task 3 (`checkpoint:human-verify`) is the human_verification entry mirroring Phase 72 VERIFICATION.md structure; author walks ОБП short ×1 primary branch in Obsidian, confirms structural match with `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` ОБП section"
    - "D-22: per-canvas `73-02-SUMMARY.md` committed to `.planning/phases/73-…/`"
    - "PATTERNS.md: ID prefix `'7'` for ОБП short Canvas constructor"
  artifacts:
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs"
      provides: "Programmatic builder for ОБП short canvas"
      contains: "exactly one `new Canvas('7')`; 3 calls to `section({…})`; one terminal text-block with `## Заключение\\nРКТ-признаки ==диагноз==`; verify+write tail"
    - path: "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas"
      provides: "Author-vault ОБП short canvas (NOT committed to repo per D-16/D-20)"
      contains: "1 start node; 0 loops; 3 section header text-blocks each beginning with `\\n`; 1 terminal text-block with verbatim Заключение/Рекомендации"
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md"
      provides: "Per-plan summary mirroring Phase 72 SUMMARY format (D-22)"
      contains: "frontmatter with `phase: 73`, `plan: 02`, `requires: [CANVAS-LIB-07]`; topology table; static invariant results; manual verification checklist"
  key_links:
    - from: "start node"
      to: "first section header (`\\nПАРЕНХИМАТОЗНЫЕ ОРГАНЫ:`) text-block"
      via: "single edge"
      pattern: "no procedure preamble per Pattern S5; ОБП short does NOT include the Контраст fan-out used in Phase 72 ОБП full (short templates do not differentiate contrast)"
    - from: "each section's branches array"
      to: "next section's header text-block (or terminal text-block for the last section)"
      via: "convergent edges via `converge(branches, target)` helper"
      pattern: "linear single-trunk top-to-bottom, no fan-out"
    - from: "last section (ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ) branches"
      to: "terminal Заключение/Рекомендации text-block"
      via: "convergent edges"
      pattern: "Заключение line wraps `РКТ-признаки ` from source MD as `РКТ-признаки ==диагноз==` per D-13"
---

<objective>
Author the ОБП (органы брюшной полости) short-version algorithmic canvas in the author's local vault. Single-trunk linear canvas with 3 organ-system sections + terminal Заключение/Рекомендации, NO loops, NO Контраст fan-out (D-06 spirit — short templates do not differentiate contrast), NO procedure preamble. Each organ-system has its own «— норма?» Question with verbatim normal-finding text on the Норма branch and a ==напишу что не так с …== fill-in chip on the «Опишу вручную» branch (D-01, D-02). When run end-to-end, the assembled output structurally matches `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12 (functionally identical to муж file lines 10-14, with the минор blank-line formatting normalized).

Purpose: deliver CANVAS-LIB-07.
Output: `Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas` (author's vault — NOT committed to repo per D-16, D-20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan is **programmatic** (D-15) — the executor runs a Node.js builder script that emits canvas JSON via the existing Phase 72 `canvas-builder.mjs` helper. There is no `npm run build`, no `vitest`, no `src/` change. The builder reuses Phase 72 helper via relative import.

The end-to-end runner walkthrough in Obsidian (Task 3) is `checkpoint:human-verify`.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md
@./CLAUDE.md

<source_template>
Verbatim text source: `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12. Per CONTEXT.md «specifics», this section is **functionally identical** to муж lines 10-14 (только minor formatting — blank lines between sentences in муж version). Generator normalizes to one canonical form (the жен variant — adjacent sentences without blank-line separators).

```
Патологических образований (очагов патологической плотности) в паренхиме печени и почек, селезенки, надпочечников, поджелудочной железы не выявлено.
Желчный пузырь не увеличен, R-контрастных включений в нем не выявлено.
Забрюшинные лимфоузлы не увеличены.
```

Per CONTEXT.md «Author's Discretion», section names below are suggested — the executor may pick alternative consistent Russian wording, but ALL three verbatim sentences above MUST appear in the output (one per section's normText). Recommended section header → norm-text mapping (from PATTERNS.md):

| Section name | Norm text (verbatim from source) | Snippet subfolder |
|---|---|---|
| `ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ:` | `Патологических образований (очагов патологической плотности) в паренхиме печени и почек, селезенки, надпочечников, поджелудочной железы не выявлено.` | `ОБП` |
| `ЖЕЛЧНЫЙ ПУЗЫРЬ:` | `Желчный пузырь не увеличен, R-контрастных включений в нем не выявлено.` | `ОБП` |
| `ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ:` | `Забрюшинные лимфоузлы не увеличены.` | `ОБП` |

Terminal block source (lines 19-26 of either short MD file — the trailing-space `РКТ-признаки ` becomes `РКТ-признаки ==диагноз==` per D-13):

```
## Заключение
РКТ-признаки 
## Рекомендации
Консультация направившего специалиста.
```
</source_template>

<interfaces>
<!-- Helper API exported from `.planning/phases/72-…/build/canvas-builder.mjs`. The executor reuses these directly via relative import — do NOT redefine. -->

```javascript
export class Canvas {
  constructor(prefix) { /* prefix is a single hex char namespacing IDs */ }
  start({ x, y, width, height }) { /* returns id */ }
  textBlock({ text, x, y, width, height }) { /* returns id */ }
  question({ text, x, y, width, height }) { /* returns id */ }
  answer({ text, displayLabel, x, y, width, height }) { /* returns id */ }
  snippetFolder({ subfolderPath, snippetLabel, text, x, y, width, height }) { /* returns id */ }
  edge({ from, to, fromSide = 'bottom', toSide = 'top', label }) { /* returns id */ }
  toJSON() { /* returns { nodes, edges } */ }
}

export function verifyInvariants(canvasJSON, spec) {
  // spec: { sectionHeaders: string[], zaklyuchenie: string, rekomendatsii: string }
  // Returns array of { inv, status, msg } — checks I1-I10
}

export function reportFindings(canvasName, findings) {
  // Prints `[canvasName] P pass, F fail (N checks)` and per-fail lines
  // Returns true if zero failures
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build-obp-short.mjs (programmatic ОБП short canvas builder)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs</files>
  <read_first>
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md (D-01..D-22)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md (S1-S5 + ОБП-short specific section)
    - Z:\projects\references\short - ОГК ОБП ОМТ жен.md (verbatim norm text source — lines 10-12)
    - Z:\projects\references\short - ОГК ОБП ОМТ муж.md (verify functional equivalence with жен ОБП block lines 10-14; normalize to жен canonical form)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs (single-trunk analog with section() + converge() helpers)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs (helper API + verifyInvariants logic)
    - ./CLAUDE.md (never delete existing code)
  </read_first>
  <action>
Create `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs` with this exact structure (DO NOT add a procedure preamble; DO NOT call `c.loop()`; DO NOT add a Контраст fan-out — short ОБП is single-trunk, not contrast-differentiated, per D-04 + Pattern S5):

1. **Header comment + imports**:
   ```javascript
   // Build ОБП short 1.0.0.canvas per 73-02-PLAN.md.
   // Source template: Z:\projects\references\short - ОГК ОБП ОМТ жен.md (lines 10-12; муж file functionally identical lines 10-14, normalized to жен canonical form).
   // Reference shape: Phase 72 build-pkop.mjs (linear single-trunk, no fan-out, no loops).

   import { writeFileSync } from 'node:fs';
   import { Canvas, verifyInvariants, reportFindings } from '../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs';

   const c = new Canvas('7'); // ОБП short → prefix '7'
   ```

2. **Layout constants** (verbatim from `build-pkop.mjs`):
   ```javascript
   const X = 0;
   const X_NORM = -340;
   const X_FREE = 340;
   const X_SNIP = 720;
   const STEP = 160;
   ```

3. **`section()` factory + `converge()` helper** — copy verbatim from `build-pkop.mjs` lines 31-53.

4. **Start node** (NO procedure preamble per D-04 + Pattern S5):
   ```javascript
   let y = -200;
   const start = c.start({ x: X, y });
   y += STEP;
   ```

5. **Section 1 — ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ** (verbatim norm text from short-жен MD line 10):
   ```javascript
   const sec1 = section({
     headerText: 'ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ:',
     normText: 'Патологических образований (очагов патологической плотности) в паренхиме печени и почек, селезенки, надпочечников, поджелудочной железы не выявлено.',
     normHeight: 100,
     sectionName: 'паренхиматозными органами',
     snippetSubfolder: 'ОБП',
     y,
   });
   c.edge({ from: start, to: sec1.header });
   y += STEP * 2 + 240;
   ```

6. **Section 2 — ЖЕЛЧНЫЙ ПУЗЫРЬ** (verbatim norm text from short-жен MD line 11):
   ```javascript
   const sec2 = section({
     headerText: 'ЖЕЛЧНЫЙ ПУЗЫРЬ:',
     normText: 'Желчный пузырь не увеличен, R-контрастных включений в нем не выявлено.',
     sectionName: 'желчным пузырём',
     snippetSubfolder: 'ОБП',
     y,
   });
   converge(sec1.branches, sec2.header);
   y += STEP * 2 + 200;
   ```

7. **Section 3 — ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ** (verbatim norm text from short-жен MD line 12):
   ```javascript
   const sec3 = section({
     headerText: 'ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ:',
     normText: 'Забрюшинные лимфоузлы не увеличены.',
     sectionName: 'забрюшинными лимфоузлами',
     snippetSubfolder: 'ОБП',
     y,
   });
   converge(sec2.branches, sec3.header);
   y += STEP * 2 + 200;
   ```

8. **Terminal Заключение/Рекомендации text-block** (verbatim from short MD lines 19-26 with `==диагноз==` chip per D-13):
   ```javascript
   const terminal = c.textBlock({
     text: '\n## Заключение\nРКТ-признаки ==диагноз==\n\n## Рекомендации\nКонсультация направившего специалиста.',
     x: X, y, height: 220,
   });
   converge(sec3.branches, terminal);
   ```

9. **Verify + write tail**:
   ```javascript
   const json = c.toJSON();
   const findings = verifyInvariants(json, {
     sectionHeaders: ['ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ:', 'ЖЕЛЧНЫЙ ПУЗЫРЬ:', 'ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ:'],
     zaklyuchenie: 'РКТ-признаки',
     rekomendatsii: 'Консультация направившего специалиста.',
   });
   const ok = reportFindings('ОБП short 1.0.0.canvas', findings);

   const VAULT_PATH = 'Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas';
   writeFileSync(VAULT_PATH, JSON.stringify(json, null, '\t'), 'utf8');
   console.log(`\n→ wrote ${VAULT_PATH}`);
   console.log(`  nodes: ${json.nodes.length}, edges: ${json.edges.length}`);
   console.log(`  loops: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'loop').length}, snippets: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'snippet').length}`);
   process.exit(ok ? 0 : 1);
   ```

Then RUN the builder:
```bash
node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs
```

Confirm exit code 0 and the printed line `[ОБП short 1.0.0.canvas] N pass, 0 fail` (N is roughly 11-13 — I1, I4×3 snippets, I5×3 sections, I6, I7, I8, I9, I10; I2/I3 vacuously satisfied).
  </action>
  <verify>
    <automated>node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs` exists.
    - The builder file imports from `'../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs'` (verifiable via grep).
    - The builder file constructs `new Canvas('7')` (verifiable via grep — distinct from Phase 72 prefixes 1-5 and from 73-01's `'6'`).
    - The builder file does NOT contain `c.loop(` (per D-04 — verifiable via grep returning zero matches).
    - The builder file does NOT contain the substring `Контраст` (per D-06 spirit — short ОБП does not differentiate contrast).
    - The builder file contains the three verbatim norm strings from short-жен MD lines 10-12: `'Патологических образований (очагов патологической плотности) в паренхиме печени и почек, селезенки, надпочечников, поджелудочной железы не выявлено.'`, `'Желчный пузырь не увеличен, R-контрастных включений в нем не выявлено.'`, `'Забрюшинные лимфоузлы не увеличены.'` (each verifiable via grep -F).
    - The builder file contains the terminal-block string `'\n## Заключение\nРКТ-признаки ==диагноз==\n\n## Рекомендации\nКонсультация направившего специалиста.'` (verifiable via grep -F).
    - `node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs` exits with code 0.
    - The builder stdout contains a line matching `^\[ОБП short 1\.0\.0\.canvas\] \d+ pass, 0 fail \(\d+ checks\)$` (no FAIL lines).
    - The builder stdout contains a line `→ wrote Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas`.
    - The builder stdout shows `loops: 0` (per D-04).
    - File `Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas` exists and is valid JSON parseable by `node -e "JSON.parse(require('node:fs').readFileSync('Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas','utf8'))"`.
    - The written canvas JSON contains the substring `"radiprotocol_nodeType":"start"` exactly once (`grep -c` returns `1`).
    - The written canvas JSON contains zero `"radiprotocol_nodeType":"loop"` substrings.
  </acceptance_criteria>
  <done>
    `build-obp-short.mjs` exists, runs cleanly, prints all-pass invariants, writes `ОБП short 1.0.0.canvas` to author's vault. Static layer of CANVAS-LIB-07 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 73-02-SUMMARY.md (per-canvas summary mirroring Phase 72 SUMMARY format)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md (template format)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-PLAN.md (this plan)
    - The just-written canvas file `Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas` (extract node/edge counts)
    - The Task 1 build log (extract `N pass, 0 fail` line)
  </read_first>
  <action>
Author `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md` mirroring 72-05-SUMMARY.md format. Frontmatter:

```yaml
# (frontmatter fence shown above for SUMMARY format reference)
phase: 73-canvas-library-short-algorithmic-canvases
plan: 02
id: 73-02-obp-short-canvas
subsystem: content-authoring (Obsidian Canvas JSON, no plugin code touched)
tags: [canvas-authoring, content, obsidian, obp, short]
requires: [CANVAS-LIB-07]
provides:
  - ОБП (органы брюшной полости) short-version algorithmic canvas
  - 3 linear sections per D-01 (ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ, ЖЕЛЧНЫЙ ПУЗЫРЬ, ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ)
  - Per-system Норма? gate with verbatim normal text (D-02)
  - Terminal Заключение with `==диагноз==` Phase-27 chip + Рекомендации (D-03, D-13)
  - Zero loops (D-04), zero contrast fan-out (D-06 spirit), zero procedure preamble
affects:
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas (author's vault — not in repo per D-16/D-20)"
tech-stack:
  added: []
patterns:
  - Single linear trunk top-to-bottom — no fan-out, no loops, no Контраст question
  - section() + converge() helpers reused from Phase 72 build-pkop.mjs
  - Phase-27 ==fill-in== chip on Заключение line for clinical diagnosis substitution
key-files:
  created:
    - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОБП short 1.0.0.canvas"
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs
  modified: []
key-decisions:
  - "D-01..D-22 (per-decision list — reuse the must_haves.decisions array from this PLAN.md)"
requirements-completed:
  - CANVAS-LIB-07
verification:
  static: passed (N/N invariant checks I1-I10; replace N with builder output)
  manual: pending (Task 3 — author runs in Obsidian)
# (frontmatter fence shown above for SUMMARY format reference)
```

Body sections (mirror 73-01-SUMMARY.md but with ОБП-specific values):

1. `## Summary` — single paragraph: programmatic single-trunk linear canvas; zero loops + zero Контраст fan-out per D-04 + D-06 spirit; verbatim norm text from `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12 (functionally identical to муж file lines 10-14, normalized).

2. `## Topology` table:
   | Metric | Value |
   |---|---|
   | Nodes | (count from JSON) |
   | Edges | (count from JSON) |
   | Sections | 3 (ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ, ЖЕЛЧНЫЙ ПУЗЫРЬ, ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ) |
   | Loops | 0 (per D-04) |
   | Snippet nodes | (count from JSON, expected 3) |
   | Free-text `==…==` chips | (count: 3 ==напишу что не так с …== chips + 1 ==диагноз== chip) |
   | Start nodes | 1 |

3. `## Snippet Paths Used` — table listing the snippet paths used (`ОБП` ×3) and confirming no cross-canvas leakage (no `ОГК`, `ОМТ`, `ГМ`, `ОЗП`, `ПОЗВОНОЧНИК`, `КОСТИ` paths).

4. `## Static Invariant Results (I1-I10)` — paste the relevant `[ОБП short 1.0.0.canvas] N pass, 0 fail` line and enumerate the applicable invariants (I1, I4×3, I5×3, I6, I7, I8, I9, I10; I2/I3 vacuously satisfied).

5. `## Manual Verification (Task 3)` — checklist:
   1. Open canvas in Obsidian. Click "Run from start".
   2. Walk all 3 sections (ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ → ЖЕЛЧНЫЙ ПУЗЫРЬ → ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ), picking norm/freetext/snippet at each.
   3. Confirm terminal Заключение/Рекомендации emits and `==диагноз==` chip is clickable.
   4. Visual diff vs `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12 + 19-26.
   5. Type "approved" to mark CANVAS-LIB-07.

6. `## Build Reproducibility` — `Re-run: \`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-obp-short.mjs\`.`
  </action>
  <verify>
    <automated>test -f .planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md && grep -q "CANVAS-LIB-07" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md && grep -q "Loops | 0\|loops: 0\|Loops: 0" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md` exists.
    - The frontmatter contains `phase: 73-canvas-library-short-algorithmic-canvases` and `plan: 02` (verifiable via grep).
    - The frontmatter `requires:` list contains `CANVAS-LIB-07` (grep -F).
    - The body asserts `Loops | 0` or equivalent zero-loop value (per D-04).
    - The body contains the substring `pass, 0 fail`.
    - The body lists `ОБП` as the snippet path used (×3) and confirms no cross-canvas leakage.
    - The body contains a `## Manual Verification` section with at least 5 numbered steps.
  </acceptance_criteria>
  <done>
    73-02-SUMMARY.md exists with full frontmatter, topology, snippet-path audit, static-invariant results, and manual-verification checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author runs ОБП short canvas end-to-end in Obsidian and confirms output matches short-MD reference</name>
  <files>ОБП short 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. See the <how-to-verify> block below for the step-by-step. The action is: open the generated canvas in Obsidian Protocol Runner, run from start, walk every applicable branch, visually diff the assembled output against the corresponding section of the source short MD template at Z:/projects/references/.</action>
  <verify><automated>echo "manual checkpoint — see how-to-verify block; resume with author approval"</automated></verify>
  <done>Author types "approved" after confirming the canvas runs end-to-end in Obsidian Protocol Runner, output matches the source short MD reference, and CANVAS-LIB-07 is satisfied.</done>
  <what-built>
    `Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas` — single-trunk linear canvas with 3 organ-system sections + terminal Заключение/Рекомендации, zero loops, zero contrast fan-out, programmatically generated with all I1-I10 static invariants passing.
  </what-built>
  <how-to-verify>
    1. Open Obsidian on the `TEST-BASE` vault.
    2. Open `Protocols/ОБП short 1.0.0.canvas`.
    3. From the Protocol Runner, click "Run from start".
    4. At each of the 3 organ-system Question prompts («ПАРЕНХИМАТОЗНЫЕ ОРГАНЫ — норма?», «ЖЕЛЧНЫЙ ПУЗЫРЬ — норма?», «ЗАБРЮШИННЫЕ ЛИМФОУЗЛЫ — норма?»):
       - Pick **Норма** on at least one walkthrough — confirm the corresponding verbatim norm text from `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` line 10/11/12 appears.
       - On a second walkthrough, pick **Опишу вручную** at one section — confirm the `==напишу что не так с …==` chip is rendered and clickable.
       - On a third walkthrough, pick **Выберу шаблон** at any section — confirm the snippet picker shows entries from `SNIPPETS\ОБП\` and ONLY those (no cross-canvas leakage from `ОГК`/`ОМТ`/etc. per D-09 + Pitfall 9).
    5. Confirm the runner walks through to a terminal Заключение/Рекомендации output with `## Заключение`, `РКТ-признаки [some text — fill the ==диагноз== chip]`, `## Рекомендации`, `Консультация направившего специалиста.`.
    6. Visual diff the runner's emitted output against `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12 + 19-26.
    7. Confirm zero parser errors, zero deadlocks, zero unreachable nodes.
    8. Confirm Phase 70 `+`-prefix loop-exit visual hint is NOT present anywhere (no loops in this canvas per D-04).
  </how-to-verify>
  <resume-signal>Type "approved" to mark CANVAS-LIB-07 as manually verified, OR describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File-system writes | Builder writes a `.canvas` file outside the repo working tree to `Z:\documents\vaults\TEST-BASE\Protocols\` — author-controlled local path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02-01 | T | build-obp-short.mjs `writeFileSync` to author vault | accept | Author-controlled local file write to a fixed absolute path; no untrusted input |
| T-73-02-02 | I | Canvas content (Russian medical text strings) | accept | All content is verbatim from author's reference templates; no PII, no external data |
</threat_model>

<verification>
- All three tasks above pass their `<acceptance_criteria>`.
- Static layer (Tasks 1-2): `node …/build-obp-short.mjs` exits 0, prints `N pass, 0 fail`, writes valid JSON to author's vault; SUMMARY.md committed.
- Manual layer (Task 3): author confirms end-to-end runner walkthrough matches `short - ОГК ОБП ОМТ жен.md` reference and types "approved".
- I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged per D-17.
</verification>

<success_criteria>
- `Z:\documents\vaults\TEST-BASE\Protocols\ОБП short 1.0.0.canvas` exists, valid JSON, runs end-to-end in Obsidian Protocol Runner.
- All static invariants I1-I10 pass.
- Author confirms manual walkthrough produces output structurally matching `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 10-12 + 19-26.
- 73-02-SUMMARY.md committed to `.planning/phases/73-…/` per D-22.
- Zero cross-canvas snippet leakage (only `ОБП` paths used).
- CANVAS-LIB-07 marked complete in REQUIREMENTS.md after author approval.
</success_criteria>

<output>
After completion, create `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-02-SUMMARY.md` (Task 2 explicitly produces this file).
</output>
