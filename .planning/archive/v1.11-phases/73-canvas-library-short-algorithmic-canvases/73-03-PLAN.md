---
phase: 73
plan: 03
plan_id: 73-03-PLAN
title: ОМТ short — sex 2-way fan-out (Жен/Муж), no contrast, no loops
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs
  - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas (author's vault — outside repo working tree)"
autonomous: false
requirements: [CANVAS-LIB-08]
tags: [canvas-authoring, content, obsidian, omt, short, sex-fanout]
must_haves:
  truths:
    - "An `ОМТ short 1.0.0.canvas` file exists at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` and is valid JSON parseable by Obsidian"
    - "The canvas runs end-to-end in the Protocol Runner without throwing — author walks BOTH sex paths (Жен AND Муж) through to the SHARED terminal Заключение/Рекомендации"
    - "ОМТ short is ONE canvas with sex 2-way fan-out at the top: initial Question «Пол пациента?» → Жен sub-flow + Муж sub-flow per D-05"
    - "ОМТ short does NOT include the «Контраст вводился?» fan-out used in Phase 72 ОМТ full per D-06 (short templates do not differentiate contrast — only sex matters)"
    - "Per D-07 both sex sub-trunks converge to ONE shared terminal Заключение + Рекомендации pair (no per-sex terminal divergence)"
    - "Each sub-flow's organ-system section presents its own «— норма?» Question with Норма (verbatim norm text) and «Опишу вручную» (==напишу что не так с …== chip) branches per D-01 + D-02"
    - "There are zero `loop` nodes in the canvas per D-04"
    - "The Жен sub-flow contains the verbatim sentences from `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` lines 15-17 (МАТКА-ПРИДАТКИ-ПОДВЗДОШНЫЕ-КОСТНО)"
    - "The Муж sub-flow contains the verbatim sentences from `Z:\\projects\\references\\short - ОГК ОБП ОМТ муж.md` lines 17-21 (ПРОСТАТА-СЕМ.ПУЗ-ПОДВЗДОШНЫЕ-КОСТНО)"
    - "Phase-27 ==fill-in== chips per D-12: МАТКА section has `==размер тела матки мм==` and `==размер шейки матки мм==`; ПРИДАТКИ section has `==придатки справа==` and `==придатки слева==`; ПРОСТАТА section has `==размер простаты мм==`"
    - "The terminal text-block contains the verbatim string `## Заключение\\nРКТ-признаки ==диагноз==\\n\\n## Рекомендации\\nКонсультация направившего специалиста.` per D-03 + D-13"
    - "`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs` exits 0 and prints `[ОМТ short 1.0.0.canvas] N pass, 0 fail` covering inherited Phase 72 invariants I1–I10 (D-17)"
  decisions:
    - "D-01: per-organ-system text-blocks (NOT monolithic) — per-sex sub-flows"
    - "D-02: per-system Норма? gate"
    - "D-03: terminal `## Заключение` + `## Рекомендации` text-block (D-07: shared single terminal)"
    - "D-04: NO loops"
    - "D-05: ОМТ short = ONE canvas with sex 2-way fan-out at top"
    - "D-06: NO Контраст fan-out (short templates do not differentiate contrast)"
    - "D-07: After per-sex sub-trunks, both converge to ONE shared terminal Заключение + Рекомендации"
    - "D-08, D-09: snippet-node strategy reusing Phase 72's `SNIPPETS\\ОМТ\\` subfolder"
    - "D-10: do NOT pre-create snippet files; embed paths in nodes only"
    - "D-11: «Костно-деструктивных изменений не выявлено.» line — generator's choice between hardcoded text-block and snippet under `КОСТИ`. Recommendation: hardcoded section per sub-trunk (one КОСТНЫЕ СТРУКТУРЫ section terminating each sex sub-flow before convergence to shared terminal) — simpler than introducing a КОСТИ snippet picker."
    - "D-12: Phase-27 ==fill-in== chips for measurement gaps (МАТКА сizes, ПРИДАТКИ left/right, ПРОСТАТА size)"
    - "D-13: Phase-27 ==диагноз== chip on Заключение line"
    - "D-14: chip well-formedness invariant I10 enforced"
    - "D-15: programmatic per-canvas .mjs builder reusing Phase 72 `canvas-builder.mjs` via relative import"
    - "D-16: builder writes to `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (outside repo working tree)"
    - "D-17: I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged; builder prints invariant pass count"
    - "D-18: file name `ОМТ short 1.0.0.canvas`"
    - "D-19: [informational] vault state observation — `ОМТ short 1.0.0.canvas` coexists alongside the existing `ОМТ full 1.0.0.canvas` and Phase 72's other canvases at `Z:\\documents\\vaults\\TEST-BASE\\Protocols\\` (no plan task implements this; vault target documented for context)"
    - "D-20: static layer — builder prints I1-I10 invariant pass count; no .canvas committed to repo"
    - "D-21: manual layer — Task 3 (`checkpoint:human-verify`) is the human_verification entry mirroring Phase 72 VERIFICATION.md structure; author walks ОМТ short ×2 primary branches (Жен и Муж) in Obsidian, confirms structural match with `Z:\\projects\\references\\short - ОГК ОБП ОМТ жен.md` ОМТ-Жен section AND `Z:\\projects\\references\\short - ОГК ОБП ОМТ муж.md` ОМТ-Муж section"
    - "D-22: per-canvas `73-03-SUMMARY.md` committed to `.planning/phases/73-…/`"
    - "PATTERNS.md: ID prefix `'8'` for ОМТ short Canvas constructor"
  artifacts:
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs"
      provides: "Programmatic builder for ОМТ short canvas"
      contains: "exactly one `new Canvas('8')`; sex fan-out at top (`Пол пациента?` Question with Жен/Муж answer fan-out); per-sex linear section() chains; ONE shared terminal text-block both sub-trunks converge into"
    - path: "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas"
      provides: "Author-vault ОМТ short canvas (NOT committed to repo per D-16/D-20)"
      contains: "1 start node; 0 loops; sex Question with 2 answer fan-out; 4 sections per sex sub-flow; 1 shared terminal text-block"
    - path: ".planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md"
      provides: "Per-plan summary mirroring Phase 72 SUMMARY format (D-22)"
      contains: "frontmatter with `phase: 73`, `plan: 03`, `requires: [CANVAS-LIB-08]`; topology table; static invariant results; manual verification checklist for BOTH sex paths"
  key_links:
    - from: "start node"
      to: "sex Question («Пол пациента?»)"
      via: "single edge"
      pattern: "no procedure preamble per Pattern S5"
    - from: "sex Question"
      to: "Жен answer + Муж answer (2-way fan-out per D-05)"
      via: "two labeled edges (`Жен`, `Муж`)"
      pattern: "Phase 72 build-omt.mjs sex fan-out skeleton minus the contrast inner fan-out per D-06"
    - from: "Жен answer"
      to: "Жен sub-flow first section header (МАТКА:)"
      via: "single edge"
      pattern: "linear sub-trunk top-to-bottom on X_FEM trunk"
    - from: "Муж answer"
      to: "Муж sub-flow first section header (ПРОСТАТА:)"
      via: "single edge"
      pattern: "linear sub-trunk top-to-bottom on X_MUZH trunk"
    - from: "Жен sub-flow last section branches AND Муж sub-flow last section branches"
      to: "ONE shared terminal Заключение/Рекомендации text-block"
      via: "convergent edges from both sub-trunks per D-07"
      pattern: "shared single terminal — no per-sex terminal divergence"
---

<objective>
Author the ОМТ (органы малого таза) short-version algorithmic canvas in the author's local vault. ONE canvas with sex 2-way fan-out at the top per D-05: initial Question «Пол пациента?» branches into Жен and Муж sub-flows. Each sub-flow is a linear sequence of per-organ-system sections (D-01) with «— норма?» gates (D-02), with NO loops (D-04) and NO Контраст fan-out (D-06). Both sub-flows converge to ONE shared terminal Заключение + Рекомендации text-block (D-07). Phase-27 `==fill-in==` chips embed all measurement gaps from the source MD per D-12. When run end-to-end, the assembled output structurally matches `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17 (Жен path) OR `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21 (Муж path).

Purpose: deliver CANVAS-LIB-08.
Output: `Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas` (author's vault — NOT committed to repo per D-16, D-20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan is **programmatic** (D-15) — the executor runs a Node.js builder script that emits canvas JSON via the Phase 72 `canvas-builder.mjs` helper. The builder reuses Phase 72 helper via relative import.

The end-to-end runner walkthrough in Obsidian (Task 3) is `checkpoint:human-verify` — and requires walking BOTH sex paths (Жен AND Муж) per CANVAS-LIB-08 success criteria mirroring Phase 72 ОМТ-full's sex × contrast walkthrough scope, scaled down to 2 paths.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md
@.planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-VALIDATION.md
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-omt.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs
@.planning/phases/72-canvas-library-full-algorithmic-canvases/72-04-SUMMARY.md
@./CLAUDE.md

<source_template>
**Жен sub-flow source — `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17:**
```
Тело матки в поперечнике до  мм. Шейка матки до  мм. Придатки: справа - , слева - .
Подвздошные лимфоузлы не увеличены.
Костно-деструктивных изменений не выявлено.
```

**Муж sub-flow source — `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21:**
```
Простата в поперечнике до  мм. Семенные пузырьки интактны. Окружающая клетчатка не изменена.

Подвздошные лимфоузлы не увеличены.

Костно-деструктивных изменений не выявлено.
```

Per CONTEXT.md «specifics» + D-12, the empty measurement gaps in the source (`до  мм.`, `справа - , слева - .`, etc.) are wrapped as Phase-27 ==fill-in== chips with descriptive Russian labels. PATTERNS.md provides the canonical chip wording — use it verbatim:

**Жен sub-flow — section header → norm-text → snippet folder mapping:**

| Section name | Norm text (verbatim with ==fill-in== chips per D-12) | Snippet subfolder |
|---|---|---|
| `МАТКА:` | `Тело матки в поперечнике до ==размер тела матки мм== мм. Шейка матки до ==размер шейки матки мм== мм.` | `ОМТ` |
| `ПРИДАТКИ:` | `Придатки: справа - ==придатки справа==, слева - ==придатки слева==.` | `ОМТ` |
| `ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:` | `Подвздошные лимфоузлы не увеличены.` | `ОМТ` |
| `КОСТНЫЕ СТРУКТУРЫ:` | `Костно-деструктивных изменений не выявлено.` | (omit — hardcoded text-block per D-11 recommended) |

**Муж sub-flow — section header → norm-text → snippet folder mapping:**

| Section name | Norm text (verbatim with ==fill-in== chip per D-12) | Snippet subfolder |
|---|---|---|
| `ПРОСТАТА:` | `Простата в поперечнике до ==размер простаты мм== мм.` | `ОМТ` |
| `СЕМЕННЫЕ ПУЗЫРЬКИ:` | `Семенные пузырьки интактны. Окружающая клетчатка не изменена.` | `ОМТ` |
| `ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:` | `Подвздошные лимфоузлы не увеличены.` | `ОМТ` |
| `КОСТНЫЕ СТРУКТУРЫ:` | `Костно-деструктивных изменений не выявлено.` | (omit — hardcoded text-block per D-11) |

**Terminal block source (verbatim from short MD lines 19-26 — `РКТ-признаки ` becomes `РКТ-признаки ==диагноз==` per D-13):**
```
## Заключение
РКТ-признаки ==диагноз==

## Рекомендации
Консультация направившего специалиста.
```

**ID-uniqueness note:** Phase 73 must NOT collide with the four ПОДВЗДОШНЫЕ-and-КОСТНЫЕ section headers between sex sub-trunks. Both sub-flows include their own `ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:` and `КОСТНЫЕ СТРУКТУРЫ:` sections — that's fine because each is a different node (different IDs), and the I5 invariant `n.text.includes(h)` is satisfied by the existence of any text-block that contains the header substring. The verifier sectionHeaders list passed to `verifyInvariants` should contain each unique header once (so `'ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:'` appears once in the spec list, even though two text-blocks contain it).
</source_template>

<interfaces>
<!-- Helper API exported from `.planning/phases/72-…/build/canvas-builder.mjs`. Reused via relative import. -->

```javascript
export class Canvas {
  constructor(prefix) { /* prefix is a single hex char namespacing IDs */ }
  start({ x, y, width, height }) { /* returns id */ }
  textBlock({ text, x, y, width, height }) { /* returns id */ }
  question({ text, x, y, width, height }) { /* returns id */ }
  answer({ text, displayLabel, x, y, width, height }) { /* returns id */ }
  snippetFolder({ subfolderPath, snippetLabel, text, x, y, width, height }) { /* returns id */ }
  edge({ from, to, fromSide = 'bottom', toSide = 'top', label }) { /* returns id */ }
  toJSON() { /* returns { nodes, edges } */ }
}

export function verifyInvariants(canvasJSON, spec) {
  // spec: { sectionHeaders: string[], zaklyuchenie: string, rekomendatsii: string }
  // Returns array of { inv, status, msg } — checks I1-I10
}

export function reportFindings(canvasName, findings) {
  // Prints `[canvasName] P pass, F fail (N checks)` and per-fail lines
  // Returns true if zero failures
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build-omt-short.mjs (programmatic ОМТ short canvas builder with sex 2-way fan-out)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs</files>
  <read_first>
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-CONTEXT.md (D-01..D-22, especially D-05, D-06, D-07)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-PATTERNS.md (S1-S5 + ОМТ-short specific section)
    - Z:\projects\references\short - ОГК ОБП ОМТ жен.md (verbatim Жен sub-flow source — lines 15-17)
    - Z:\projects\references\short - ОГК ОБП ОМТ муж.md (verbatim Муж sub-flow source — lines 17-21)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-omt.mjs (sex fan-out analog — read first 70 lines for the sex Question + 2-way answer pattern; SKIP the Контраст inner fan-out and the loop helpers — Phase 73 omits those per D-04 + D-06)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/build-pkop.mjs (single-trunk linear pattern for the per-sex sub-flows — read entire file for section() + converge() helpers)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs (helper API + verifyInvariants logic)
    - ./CLAUDE.md (never delete existing code)
  </read_first>
  <action>
Create `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs` with this structure (DO NOT call `c.loop()` per D-04; DO NOT add Контраст question per D-06; DO NOT add a procedure preamble per Pattern S5; create exactly ONE shared terminal text-block both sex sub-trunks converge into per D-07):

1. **Header comment + imports**:
   ```javascript
   // Build ОМТ short 1.0.0.canvas per 73-03-PLAN.md.
   // Source templates:
   //   - Z:\projects\references\short - ОГК ОБП ОМТ жен.md lines 15-17 (Жен sub-flow)
   //   - Z:\projects\references\short - ОГК ОБП ОМТ муж.md lines 17-21 (Муж sub-flow)
   // Reference shape: Phase 72 build-omt.mjs sex fan-out skeleton minus the Контраст inner fan-out (D-06).
   //   - 2-way (Жен/Муж) instead of 4-way (sex × contrast)
   //   - Per-sex sub-flows are linear section() chains (no loops per D-04)
   //   - Both sub-flows converge to ONE shared terminal Заключение + Рекомендации (D-07)

   import { writeFileSync } from 'node:fs';
   import { Canvas, verifyInvariants, reportFindings } from '../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs';

   const c = new Canvas('8'); // ОМТ short → prefix '8'
   ```

2. **Layout constants** (simplified from `build-omt.mjs` keeping only the two surviving trunks):
   ```javascript
   const X_FEM = -1500;     // female trunk x-anchor
   const X_MUZH = 1500;     // male trunk x-anchor
   const NORM_DX = -340;
   const FREE_DX = 340;
   const SNIP_DX = 720;
   const STEP = 160;
   ```

3. **Trunk-aware `section(c, …)` factory + `converge(c, …)` helper** — copy verbatim from `build-omt.mjs` lines 22-58 (the 3-arg form taking `c` as first param plus the trunkX-aware `section`). DO NOT modify the helper bodies.

4. **Sex fan-out at top** (modeled on `build-omt.mjs` lines 60-70 but ONLY the sex layer; SKIP the Контраст layer entirely per D-06):
   ```javascript
   const start = c.start({ x: 0, y: -400 });
   const qSex = c.question({ text: 'Пол пациента?', x: 0, y: -240 });
   c.edge({ from: start, to: qSex });

   const aFem = c.answer({ text: '', displayLabel: 'Жен', x: X_FEM, y: -80 });
   const aMuzh = c.answer({ text: '', displayLabel: 'Муж', x: X_MUZH, y: -80 });
   c.edge({ from: qSex, to: aFem, label: 'Жен' });
   c.edge({ from: qSex, to: aMuzh, label: 'Муж' });
   ```

5. **Жен sub-flow on `X_FEM` trunk** — 4 sections per the source-template table above (each with verbatim norm text including the ==fill-in== chips per D-12):
   ```javascript
   let yFem = 200;
   const fem_uterus = section(c, {
     trunkX: X_FEM,
     headerText: 'МАТКА:',
     normText: 'Тело матки в поперечнике до ==размер тела матки мм== мм. Шейка матки до ==размер шейки матки мм== мм.',
     normHeight: 80,
     sectionName: 'маткой',
     snippetSubfolder: 'ОМТ',
     y: yFem,
   });
   c.edge({ from: aFem, to: fem_uterus.header });
   yFem += STEP * 2 + 200;

   const fem_adnexa = section(c, {
     trunkX: X_FEM,
     headerText: 'ПРИДАТКИ:',
     normText: 'Придатки: справа - ==придатки справа==, слева - ==придатки слева==.',
     sectionName: 'придатками',
     snippetSubfolder: 'ОМТ',
     y: yFem,
   });
   converge(c, fem_uterus.branches, fem_adnexa.header);
   yFem += STEP * 2 + 200;

   const fem_lymph = section(c, {
     trunkX: X_FEM,
     headerText: 'ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:',
     normText: 'Подвздошные лимфоузлы не увеличены.',
     sectionName: 'подвздошными лимфоузлами',
     snippetSubfolder: 'ОМТ',
     y: yFem,
   });
   converge(c, fem_adnexa.branches, fem_lymph.header);
   yFem += STEP * 2 + 200;

   const fem_bones = section(c, {
     trunkX: X_FEM,
     headerText: 'КОСТНЫЕ СТРУКТУРЫ:',
     normText: 'Костно-деструктивных изменений не выявлено.',
     sectionName: 'костными структурами',
     y: yFem,
   });
   converge(c, fem_lymph.branches, fem_bones.header);
   yFem += STEP * 2 + 200;
   ```

6. **Муж sub-flow on `X_MUZH` trunk** — 4 sections from муж source:
   ```javascript
   let yMuzh = 200;
   const muzh_prostate = section(c, {
     trunkX: X_MUZH,
     headerText: 'ПРОСТАТА:',
     normText: 'Простата в поперечнике до ==размер простаты мм== мм.',
     sectionName: 'простатой',
     snippetSubfolder: 'ОМТ',
     y: yMuzh,
   });
   c.edge({ from: aMuzh, to: muzh_prostate.header });
   yMuzh += STEP * 2 + 200;

   const muzh_seminal = section(c, {
     trunkX: X_MUZH,
     headerText: 'СЕМЕННЫЕ ПУЗЫРЬКИ:',
     normText: 'Семенные пузырьки интактны. Окружающая клетчатка не изменена.',
     sectionName: 'семенными пузырьками',
     snippetSubfolder: 'ОМТ',
     y: yMuzh,
   });
   converge(c, muzh_prostate.branches, muzh_seminal.header);
   yMuzh += STEP * 2 + 200;

   const muzh_lymph = section(c, {
     trunkX: X_MUZH,
     headerText: 'ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:',
     normText: 'Подвздошные лимфоузлы не увеличены.',
     sectionName: 'подвздошными лимфоузлами',
     snippetSubfolder: 'ОМТ',
     y: yMuzh,
   });
   converge(c, muzh_seminal.branches, muzh_lymph.header);
   yMuzh += STEP * 2 + 200;

   const muzh_bones = section(c, {
     trunkX: X_MUZH,
     headerText: 'КОСТНЫЕ СТРУКТУРЫ:',
     normText: 'Костно-деструктивных изменений не выявлено.',
     sectionName: 'костными структурами',
     y: yMuzh,
   });
   converge(c, muzh_lymph.branches, muzh_bones.header);
   yMuzh += STEP * 2 + 200;
   ```

7. **Shared terminal Заключение/Рекомендации text-block — both sub-trunks converge here per D-07** (positioned at x=0 vertically below the deeper of the two sub-trunks):
   ```javascript
   const yTerm = Math.max(yFem, yMuzh) + 200;
   const terminal = c.textBlock({
     text: '\n## Заключение\nРКТ-признаки ==диагноз==\n\n## Рекомендации\nКонсультация направившего специалиста.',
     x: 0, y: yTerm, height: 240,
   });
   converge(c, fem_bones.branches, terminal);
   converge(c, muzh_bones.branches, terminal);
   ```

8. **Verify + write tail** (the sectionHeaders list contains each UNIQUE header once — `ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:` and `КОСТНЫЕ СТРУКТУРЫ:` appear in both sub-flows but the I5 invariant only requires the substring be findable in some text-block):
   ```javascript
   const json = c.toJSON();
   const findings = verifyInvariants(json, {
     sectionHeaders: [
       'МАТКА:', 'ПРИДАТКИ:', 'ПРОСТАТА:', 'СЕМЕННЫЕ ПУЗЫРЬКИ:',
       'ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:', 'КОСТНЫЕ СТРУКТУРЫ:',
     ],
     zaklyuchenie: 'РКТ-признаки',
     rekomendatsii: 'Консультация направившего специалиста.',
   });
   const ok = reportFindings('ОМТ short 1.0.0.canvas', findings);

   const VAULT_PATH = 'Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas';
   writeFileSync(VAULT_PATH, JSON.stringify(json, null, '\t'), 'utf8');
   console.log(`\n→ wrote ${VAULT_PATH}`);
   console.log(`  nodes: ${json.nodes.length}, edges: ${json.edges.length}`);
   console.log(`  loops: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'loop').length}, snippets: ${json.nodes.filter(n => n.radiprotocol_nodeType === 'snippet').length}`);
   process.exit(ok ? 0 : 1);
   ```

Then RUN the builder:
```bash
node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs
```

Confirm exit code 0 and the printed line `[ОМТ short 1.0.0.canvas] N pass, 0 fail` (N is roughly 18-22 — I1, I4×6 snippets, I5×6 sections, I6, I7, I8, I9, I10).
  </action>
  <verify>
    <automated>node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs` exists.
    - The builder file imports from `'../../72-canvas-library-full-algorithmic-canvases/build/canvas-builder.mjs'` (verifiable via grep).
    - The builder file constructs `new Canvas('8')` (verifiable via grep — distinct from Phase 72 prefixes 1-5 and from 73-01/73-02 prefixes 6-7).
    - The builder file does NOT contain `c.loop(` (per D-04 — verifiable via grep returning zero matches).
    - The builder file does NOT contain the substring `Контраст вводился` (per D-06 — verifiable via grep returning zero matches).
    - The builder file constructs a sex Question with text `'Пол пациента?'` and TWO answer fan-out children with `displayLabel: 'Жен'` and `displayLabel: 'Муж'` (verifiable via grep -F).
    - The builder file contains the verbatim Жен norm strings: `'Тело матки в поперечнике до ==размер тела матки мм== мм. Шейка матки до ==размер шейки матки мм== мм.'`, `'Придатки: справа - ==придатки справа==, слева - ==придатки слева==.'`, `'Подвздошные лимфоузлы не увеличены.'`, `'Костно-деструктивных изменений не выявлено.'` (each verifiable via grep -F).
    - The builder file contains the verbatim Муж norm strings: `'Простата в поперечнике до ==размер простаты мм== мм.'`, `'Семенные пузырьки интактны. Окружающая клетчатка не изменена.'` (verifiable via grep -F).
    - The builder file contains exactly ONE terminal text-block whose `text` includes `'## Заключение\nРКТ-признаки ==диагноз=='` (verifiable via grep -F count = 1).
    - The builder file contains exactly TWO `converge(c, …, terminal)` invocations (one for `fem_bones.branches`, one for `muzh_bones.branches` — both sub-trunks merge to the ONE shared terminal per D-07; verifiable via grep -c).
    - `node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs` exits with code 0.
    - The builder stdout contains a line matching `^\[ОМТ short 1\.0\.0\.canvas\] \d+ pass, 0 fail \(\d+ checks\)$` (no FAIL lines).
    - The builder stdout contains a line `→ wrote Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas`.
    - The builder stdout shows `loops: 0` (per D-04).
    - File `Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas` exists and is valid JSON parseable by `node -e "JSON.parse(require('node:fs').readFileSync('Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas','utf8'))"`.
    - The written canvas JSON contains `"radiprotocol_nodeType":"start"` exactly once (`grep -c` = 1).
    - The written canvas JSON contains zero `"radiprotocol_nodeType":"loop"` substrings.
    - The written canvas JSON contains the substring `"text":"Пол пациента?"` (the sex fan-out Question).
  </acceptance_criteria>
  <done>
    `build-omt-short.mjs` exists, runs cleanly, prints all-pass invariants, writes `ОМТ short 1.0.0.canvas` to author's vault with sex 2-way fan-out and shared terminal. Static layer of CANVAS-LIB-08 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 73-03-SUMMARY.md (per-canvas summary mirroring Phase 72 SUMMARY format)</name>
  <files>.planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-04-SUMMARY.md (Phase 72 ОМТ-full SUMMARY — closest analog for sex fan-out canvas)
    - .planning/phases/72-canvas-library-full-algorithmic-canvases/72-05-SUMMARY.md (alt template format reference)
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-PLAN.md (this plan)
    - The just-written canvas file `Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas` (extract node/edge counts)
    - The Task 1 build log (extract `N pass, 0 fail` line)
  </read_first>
  <action>
Author `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md` mirroring Phase 72 SUMMARY format. Frontmatter:

```yaml
# (frontmatter fence shown above for SUMMARY format reference)
phase: 73-canvas-library-short-algorithmic-canvases
plan: 03
id: 73-03-omt-short-canvas
subsystem: content-authoring (Obsidian Canvas JSON, no plugin code touched)
tags: [canvas-authoring, content, obsidian, omt, short, sex-fanout]
requires: [CANVAS-LIB-08]
provides:
  - ОМТ (органы малого таза) short-version algorithmic canvas
  - Single canvas with sex 2-way fan-out at the top per D-05 (Жен / Муж)
  - 4 sections per Жен sub-flow (МАТКА, ПРИДАТКИ, ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ, КОСТНЫЕ СТРУКТУРЫ)
  - 4 sections per Муж sub-flow (ПРОСТАТА, СЕМЕННЫЕ ПУЗЫРЬКИ, ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ, КОСТНЫЕ СТРУКТУРЫ)
  - Shared terminal Заключение + Рекомендации (D-07)
  - Phase-27 ==fill-in== chips for measurement gaps (D-12)
  - Zero loops (D-04), zero contrast fan-out (D-06)
affects:
  - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas (author's vault — not in repo per D-16/D-20)"
tech-stack:
  added: []
patterns:
  - Sex 2-way fan-out (Phase 72 build-omt.mjs sex layer minus contrast inner fan-out)
  - Linear per-sex sub-flow with section() + converge() helpers from Phase 72 build-pkop.mjs
  - Shared terminal text-block both sub-trunks converge into per D-07
  - Phase-27 ==fill-in== chips embedded in answer norm-text and terminal Заключение
key-files:
  created:
    - "Z:\\documents\\vaults\\TEST-BASE\\Protocols\\ОМТ short 1.0.0.canvas"
    - .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs
  modified: []
key-decisions:
  - "D-01..D-22 (per-decision list — reuse the must_haves.decisions array from this PLAN.md)"
requirements-completed:
  - CANVAS-LIB-08
verification:
  static: passed (N/N invariant checks I1-I10; replace N with builder output)
  manual: pending (Task 3 — author runs BOTH sex paths in Obsidian)
# (frontmatter fence shown above for SUMMARY format reference)
```

Body sections:

1. `## Summary` — paragraph: ОМТ short single-canvas with sex 2-way fan-out per D-05; verbatim Жен sub-flow from `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17; verbatim Муж sub-flow from `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21; ONE shared terminal per D-07; zero loops + zero contrast fan-out per D-04 + D-06.

2. `## Topology` table:
   | Metric | Value |
   |---|---|
   | Nodes | (count from JSON) |
   | Edges | (count from JSON) |
   | Sections (Жен) | 4 (МАТКА, ПРИДАТКИ, ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ, КОСТНЫЕ СТРУКТУРЫ) |
   | Sections (Муж) | 4 (ПРОСТАТА, СЕМЕННЫЕ ПУЗЫРЬКИ, ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ, КОСТНЫЕ СТРУКТУРЫ) |
   | Sex fan-out | 2-way (Жен/Муж) per D-05 |
   | Loops | 0 (per D-04) |
   | Contrast fan-out | 0 (per D-06) |
   | Snippet nodes | (count from JSON, expected 6) |
   | Free-text `==…==` chips | (count: 6 measurement chips per D-12 + 6 ==напишу что не так с …== chips + 1 ==диагноз== chip) |
   | Terminal text-blocks | 1 (shared, per D-07) |
   | Start nodes | 1 |

3. `## Snippet Paths Used` — table listing the snippet paths used (`ОМТ` ×6 across both sub-flows) and confirming no cross-canvas leakage (no `ОГК`, `ОБП`, `ГМ`, `ОЗП`, `ПОЗВОНОЧНИК`, `КОСТИ` paths since per D-11 КОСТНЫЕ СТРУКТУРЫ is hardcoded text-block, not a snippet picker).

4. `## Sex Fan-Out Modeling` — short subsection explaining: ОМТ short uses the Phase 72 build-omt.mjs sex fan-out skeleton minus the Контраст inner fan-out. Two surviving x-trunks (`X_FEM=-1500`, `X_MUZH=1500`). Both sub-flows are linear `section()` chains. Both terminate by converging into ONE shared terminal text-block at `x=0` per D-07 — not per-sex divergent terminals.

5. `## Static Invariant Results (I1-I10)` — paste `[ОМТ short 1.0.0.canvas] N pass, 0 fail` line and enumerate (I1, I4×6, I5×6, I6, I7, I8, I9, I10; I2/I3 vacuously satisfied — no loops). Note that I5 spec list contains 6 unique headers — `ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ:` and `КОСТНЫЕ СТРУКТУРЫ:` appear in both sub-flows but only need to be findable once in any text-block to satisfy the substring check.

6. `## Manual Verification (Task 3)` — checklist:
   1. Open canvas in Obsidian. Click "Run from start".
   2. **Path 1 (Жен):** At the «Пол пациента?» Question, pick «Жен». Walk all 4 Жен sections. Confirm МАТКА section shows `==размер тела матки мм==` and `==размер шейки матки мм==` chips clickable; ПРИДАТКИ shows `==придатки справа==` and `==придатки слева==`; ПОДВЗДОШНЫЕ + КОСТНЫЕ СТРУКТУРЫ verbatim. Reach shared terminal Заключение/Рекомендации.
   3. **Path 2 (Муж):** Re-run. At sex Question pick «Муж». Walk all 4 Муж sections. Confirm ПРОСТАТА shows `==размер простаты мм==` chip; СЕМЕННЫЕ ПУЗЫРЬКИ verbatim; ПОДВЗДОШНЫЕ + КОСТНЫЕ СТРУКТУРЫ verbatim. Reach SAME shared terminal as Path 1 (D-07 — verify the Заключение/Рекомендации node id is identical between both walkthroughs).
   4. Visual diff Жен output against `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17 + 19-26.
   5. Visual diff Муж output against `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21 + 23-30.
   6. Confirm ОМТ snippet picker shows entries from `SNIPPETS\ОМТ\` ONLY (no cross-canvas leakage).
   7. Type "approved" to mark CANVAS-LIB-08.

7. `## Build Reproducibility` — `Re-run: \`node .planning/phases/73-canvas-library-short-algorithmic-canvases/build/build-omt-short.mjs\`.`
  </action>
  <verify>
    <automated>test -f .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md && grep -q "CANVAS-LIB-08" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md && grep -q "Loops | 0\|loops: 0\|Loops: 0" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md && grep -q "sex.fan.out\|Sex.[Ff]an.[Oo]ut\|2-way" .planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md` exists.
    - The frontmatter contains `phase: 73-canvas-library-short-algorithmic-canvases` and `plan: 03` (verifiable via grep).
    - The frontmatter `requires:` list contains `CANVAS-LIB-08` (grep -F).
    - The body asserts `Loops | 0` or equivalent zero-loop value (per D-04).
    - The body asserts `Contrast fan-out | 0` or equivalent zero-contrast value (per D-06).
    - The body asserts `Sex fan-out | 2-way` or equivalent 2-way value (per D-05).
    - The body contains the substring `pass, 0 fail`.
    - The body lists `ОМТ` as the snippet path used (×6 across both sub-flows).
    - The body confirms no cross-canvas leakage in the snippet-paths table.
    - The body contains a `## Manual Verification` section with steps for BOTH sex paths (Жен AND Муж — at least 6 numbered steps total).
    - The body asserts that both sex sub-trunks converge to ONE shared terminal per D-07.
  </acceptance_criteria>
  <done>
    73-03-SUMMARY.md exists with full frontmatter, topology, sex-fan-out documentation, snippet-path audit, static-invariant results, and dual-path manual verification checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author runs ОМТ short canvas BOTH sex paths in Obsidian and confirms output matches short-MD references</name>
  <files>ОМТ short 1.0.0.canvas (author's vault — not in repo)</files>
  <action>This is a human-verification checkpoint task. See the <how-to-verify> block below for the step-by-step. The action is: open the generated canvas in Obsidian Protocol Runner, run from start, walk every applicable branch, visually diff the assembled output against the corresponding section of the source short MD template at Z:/projects/references/.</action>
  <verify><automated>echo "manual checkpoint — see how-to-verify block; resume with author approval"</automated></verify>
  <done>Author types "approved" after confirming the canvas runs end-to-end in Obsidian Protocol Runner, output matches the source short MD reference, and CANVAS-LIB-08 is satisfied.</done>
  <what-built>
    `Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas` — single canvas with sex 2-way fan-out (Жен/Муж) at the top, 4 linear sections per sex sub-flow, ONE shared terminal Заключение/Рекомендации per D-07, zero loops, zero contrast fan-out. Programmatically generated with all I1-I10 static invariants passing.
  </what-built>
  <how-to-verify>
    1. Open Obsidian on the `TEST-BASE` vault.
    2. Open `Protocols/ОМТ short 1.0.0.canvas`.
    3. **Walkthrough 1 — Жен path:**
       - Click "Run from start".
       - At the «Пол пациента?» Question, pick «Жен».
       - At each of 4 Жен Question prompts («МАТКА — норма?», «ПРИДАТКИ — норма?», «ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ — норма?», «КОСТНЫЕ СТРУКТУРЫ — норма?»):
         - On at least one section, pick **Норма** — confirm verbatim norm text appears.
         - For МАТКА section, click on the `==размер тела матки мм==` and `==размер шейки матки мм==` chips, type a value, confirm substitution.
         - For ПРИДАТКИ section, click on `==придатки справа==` and `==придатки слева==` chips, type values, confirm substitution.
         - On at least one section, pick **Опишу вручную** — confirm the `==напишу что не так с …==` chip is clickable.
         - On a section with snippet picker (МАТКА/ПРИДАТКИ/ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ), pick **Выберу шаблон** — confirm the picker shows entries from `SNIPPETS\ОМТ\` ONLY (no cross-canvas leakage from `ОГК`/`ОБП`/`ГМ`/`ОЗП`/etc. per D-09 + Pitfall 9).
       - Confirm runner reaches shared terminal Заключение/Рекомендации — the `==диагноз==` chip is clickable.
       - Visual diff output against `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17 + 19-26.
    4. **Walkthrough 2 — Муж path:**
       - Re-run the canvas.
       - At the sex Question pick «Муж».
       - At each of 4 Муж Question prompts («ПРОСТАТА — норма?», «СЕМЕННЫЕ ПУЗЫРЬКИ — норма?», «ПОДВЗДОШНЫЕ ЛИМФОУЗЛЫ — норма?», «КОСТНЫЕ СТРУКТУРЫ — норма?»):
         - For ПРОСТАТА section, click on `==размер простаты мм==` chip, type a value, confirm substitution.
         - On at least one section, pick **Норма** — confirm verbatim norm text appears (especially СЕМЕННЫЕ ПУЗЫРЬКИ: «Семенные пузырьки интактны. Окружающая клетчатка не изменена.»).
       - Confirm runner reaches the SAME shared terminal as Walkthrough 1 (D-07 — single terminal node, not per-sex divergent terminals).
       - Visual diff output against `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21 + 23-30.
    5. **Cross-walkthrough check:** confirm both walkthroughs ended at the same terminal Заключение/Рекомендации text-block (verify by re-opening the canvas and visually inspecting that there's only ONE terminal text-block on canvas, not two).
    6. Confirm zero parser errors, zero deadlocks, zero unreachable nodes across both walkthroughs.
    7. Confirm Phase 70 `+`-prefix loop-exit visual hint is NOT present anywhere (no loops in this canvas per D-04).
  </how-to-verify>
  <resume-signal>Type "approved" to mark CANVAS-LIB-08 as manually verified, OR describe issues (e.g., "fill-in chip ==размер простаты мм== not clickable", "Жен and Муж paths reach DIFFERENT terminals — D-07 violated", "ОМТ snippet picker shows ОБП folder entries — cross-canvas leakage").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File-system writes | Builder writes a `.canvas` file outside the repo working tree to `Z:\documents\vaults\TEST-BASE\Protocols\` — author-controlled local path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03-01 | T | build-omt-short.mjs `writeFileSync` to author vault | accept | Author-controlled local file write to a fixed absolute path; no untrusted input |
| T-73-03-02 | I | Canvas content (Russian medical text strings + sex-specific anatomy) | accept | All content is verbatim from author's reference templates; no PII, no external data, no untrusted input. Sex fan-out routes purely on radiologist-selected pol of the patient at runtime |
</threat_model>

<verification>
- All three tasks above pass their `<acceptance_criteria>`.
- Static layer (Tasks 1-2): `node …/build-omt-short.mjs` exits 0, prints `N pass, 0 fail`, writes valid JSON to author's vault; SUMMARY.md committed.
- Manual layer (Task 3): author confirms BOTH sex-path walkthroughs (Жен AND Муж) match their respective short MD references AND converge to ONE shared terminal per D-07.
- I1-I10 invariants inherited from Phase 72 VALIDATION.md unchanged per D-17.
</verification>

<success_criteria>
- `Z:\documents\vaults\TEST-BASE\Protocols\ОМТ short 1.0.0.canvas` exists, valid JSON, runs end-to-end in Obsidian Protocol Runner on BOTH sex paths.
- All static invariants I1-I10 pass.
- Author confirms Жен walkthrough produces output structurally matching `Z:\projects\references\short - ОГК ОБП ОМТ жен.md` lines 15-17 + 19-26.
- Author confirms Муж walkthrough produces output structurally matching `Z:\projects\references\short - ОГК ОБП ОМТ муж.md` lines 17-21 + 23-30.
- Author confirms both walkthroughs converge to ONE shared terminal text-block per D-07 (no per-sex terminal divergence).
- 73-03-SUMMARY.md committed to `.planning/phases/73-…/` per D-22.
- Zero cross-canvas snippet leakage (only `ОМТ` paths used).
- CANVAS-LIB-08 marked complete in REQUIREMENTS.md after author approval.
</success_criteria>

<output>
After completion, create `.planning/phases/73-canvas-library-short-algorithmic-canvases/73-03-SUMMARY.md` (Task 2 explicitly produces this file).
</output>
