---
phase: 71-settings-donate-section
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/donate-section.css
  - esbuild.config.mjs
autonomous: true
requirements: [DONATE-01]

must_haves:
  truths:
    - "A new per-feature CSS file `src/styles/donate-section.css` exists with the Phase 71 marker and the minimal rules needed by the donate section"
    - "esbuild.config.mjs CSS_FILES array contains the entry 'donate-section' appended after the existing 8 entries"
    - "All 8 existing CSS_FILES entries are preserved in their original order (CLAUDE.md never-delete-others-code rule)"
    - "`npm run build` regenerates `styles.css` with the donate-section rules included"
  artifacts:
    - path: "src/styles/donate-section.css"
      provides: "Per-feature CSS for the donate section: word-break, display:block, margin-top on the address element"
      contains: "/* Phase 71: Donate section (DONATE-01) */"
      min_lines: 10
    - path: "esbuild.config.mjs"
      provides: "CSS build registration of donate-section.css"
      contains: "'donate-section'"
  key_links:
    - from: "esbuild.config.mjs CSS_FILES"
      to: "src/styles/donate-section.css"
      via: "concat in cssPlugin onEnd → styles.css"
      pattern: "donate-section"
    - from: "src/styles/donate-section.css"
      to: "styles.css (generated)"
      via: "esbuild concat"
      pattern: "rp-donate-address"
---

<objective>
Create the per-feature stylesheet `src/styles/donate-section.css` for the donate section and register it in the `esbuild.config.mjs` `CSS_FILES` array. After this plan, `npm run build` regenerates `styles.css` with the donate rules concatenated at the end.

Purpose: Implements VIS-D-02 (new feature file in `src/styles/`, registered in `CSS_FILES` per CLAUDE.md "CSS Architecture") and VIS-D-03 (minimal CSS — word-break, display:block, small margin-top on `.rp-donate-address`; rely on Obsidian's default `<code>` styling for everything else). Implements Claude's-Discretion choice on the class names: `.rp-donate-address` for the `<code>` element holding the wallet address, `.rp-donate-intro` for the invitation paragraph.

Output: `src/styles/donate-section.css` (new file, append-only-ready), updated `esbuild.config.mjs` with one line added to `CSS_FILES`, regenerated `styles.css` containing the new rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/71-settings-donate-section/71-CONTEXT.md
@.planning/phases/71-settings-donate-section/71-PATTERNS.md
@src/styles/canvas-selector.css
@src/styles/inline-runner.css
@esbuild.config.mjs

<interfaces>
<!-- Class names that the Plan 03 settings.ts edit will produce. Defined here so the CSS targets the correct selectors. -->

CSS hooks the donate section will emit (Plan 03 will create these DOM nodes):
- `.rp-donate-intro` — `<p>` invitation paragraph (one element, immediately after the section heading)
- `.rp-donate-address` — `<code>` element holding the full wallet address (one per row, inside `setting.descEl`)

Existing CSS_FILES order in esbuild.config.mjs:31-40 (ALL must be preserved):
```javascript
const CSS_FILES = [
  'runner-view',
  'canvas-selector',
  'editor-panel',
  'snippet-manager',
  'snippet-fill-modal',
  'loop-support',
  'snippet-tree-picker',
  'inline-runner',
];
```

After this plan it must be:
```javascript
const CSS_FILES = [
  'runner-view',
  'canvas-selector',
  'editor-panel',
  'snippet-manager',
  'snippet-fill-modal',
  'loop-support',
  'snippet-tree-picker',
  'inline-runner',
  'donate-section',
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/styles/donate-section.css with the Phase 71 rules</name>
  <files>src/styles/donate-section.css</files>
  <read_first>
    - .planning/phases/71-settings-donate-section/71-CONTEXT.md (decisions VIS-D-01, VIS-D-02, VIS-D-03 — minimal CSS, Obsidian default `<code>` is theme-aware)
    - .planning/phases/71-settings-donate-section/71-PATTERNS.md (lines 134-173 — analog files canvas-selector.css and inline-runner.css, header-comment + `rp-`-prefix conventions)
    - CLAUDE.md (CSS Architecture + Critical Rules: per-feature file in src/styles/, append-only per phase, register in esbuild.config.mjs CSS_FILES)
    - src/styles/canvas-selector.css (lines 1-20 — header comment + .rp-selector-* selector style)
    - src/styles/inline-runner.css (lines 1-15 — alternative header style + Obsidian theme-token usage like var(--size-4-2))
  </read_first>
  <action>
Create the new file `src/styles/donate-section.css` with the following content. The file is being created from scratch, so the entire content is the Phase 71 chunk; future phases will add new rules below this block with their own `/* Phase N: ... */` markers per CLAUDE.md "append-only per phase".

Required content:

```css
/* Phase 71: Donate section (DONATE-01) ─────────────────────────────────────── */

/* Wallet-address `<code>` element inside each donate row's setting-item-description.
 * - display: block         → place the address on its own line under the network list
 * - word-break: break-all  → long crypto addresses must wrap inside narrow setting columns
 * - margin-top             → small breathing room beneath the EVM network list / row name
 * Obsidian's default <code> styling (theme-aware monospace font, tinted background,
 * 4px radius) is intentionally inherited — see VIS-D-03. */
.rp-donate-address {
  display: block;
  word-break: break-all;
  margin-top: var(--size-2-1);
}

/* Invitation paragraph rendered between the section heading and the first wallet row.
 * No custom font-size or color — inherits Obsidian's body text styling. The small
 * bottom margin keeps the paragraph from sitting flush against the first Setting row. */
.rp-donate-intro {
  margin-bottom: var(--size-4-2);
}
```

Use Obsidian theme tokens (`var(--size-2-1)`, `var(--size-4-2)`) rather than hard-coded pixel values — this matches `src/styles/inline-runner.css:5-9` precedent and stays theme-aware (VIS-D-03).

Do NOT add any other selectors, animations, colors, borders, or background overrides — VIS-D-03 explicitly defers all visual styling beyond word-break/display:block/margin to Obsidian's defaults.
  </action>
  <verify>
    <automated>test -f src/styles/donate-section.css && grep -q "Phase 71: Donate section (DONATE-01)" src/styles/donate-section.css && grep -q "\.rp-donate-address" src/styles/donate-section.css && grep -q "word-break: break-all" src/styles/donate-section.css && grep -q "\.rp-donate-intro" src/styles/donate-section.css</automated>
  </verify>
  <acceptance_criteria>
    - File `src/styles/donate-section.css` exists.
    - Contains the literal header comment `/* Phase 71: Donate section (DONATE-01) ─` (the marker required by CLAUDE.md "CSS append-only per phase").
    - Contains the literal selector `.rp-donate-address`.
    - Contains the literal selector `.rp-donate-intro`.
    - Contains the literal property `display: block`.
    - Contains the literal property `word-break: break-all`.
    - Contains at least one `var(--size-` Obsidian theme token reference (no hard-coded pixel-only values).
    - Does NOT contain any color/background/border/font-size override (grep for `color:`, `background:`, `border:`, `font-size:` returns no match — Obsidian defaults are inherited per VIS-D-03).
  </acceptance_criteria>
  <done>
    The CSS file is created and ready to be picked up by the esbuild concat plugin in Task 2 of this plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register donate-section in esbuild.config.mjs CSS_FILES and rebuild</name>
  <files>esbuild.config.mjs</files>
  <read_first>
    - esbuild.config.mjs (full file — the CSS_FILES array is at lines 31-40, the cssPlugin at lines 47-65)
    - CLAUDE.md (Critical Rules — never delete code you didn't add; the 8 existing CSS_FILES entries must be preserved verbatim in their original order)
    - .planning/phases/71-settings-donate-section/71-PATTERNS.md (lines 177-208 — the exact append-to-end pattern)
    - src/styles/donate-section.css (the file just created in Task 1 — its base name is `donate-section`, no `.css` extension goes into the array)
  </read_first>
  <action>
Edit `esbuild.config.mjs` and add the single string `'donate-section'` as a new array entry at the END of the `CSS_FILES` array (after `'inline-runner'`). Preserve the comma-trailing style used by the existing entries.

Before edit (esbuild.config.mjs:31-40):
```javascript
const CSS_FILES = [
  'runner-view',
  'canvas-selector',
  'editor-panel',
  'snippet-manager',
  'snippet-fill-modal',
  'loop-support',
  'snippet-tree-picker',
  'inline-runner',
];
```

After edit:
```javascript
const CSS_FILES = [
  'runner-view',
  'canvas-selector',
  'editor-panel',
  'snippet-manager',
  'snippet-fill-modal',
  'loop-support',
  'snippet-tree-picker',
  'inline-runner',
  'donate-section',
];
```

Critical: do NOT reorder, rename, or delete any of the 8 existing entries — CLAUDE.md "never delete code you didn't add" is binding here. Do NOT touch the surrounding doc-comment, the `cssPlugin` definition, or the `devVaultCopyPlugin`. The only change is one new line inserted before the closing bracket.

After the edit, run `npm run build` to confirm:
- esbuild compiles successfully.
- The `cssPlugin.onEnd` callback runs, reads `src/styles/donate-section.css`, and concatenates it into `styles.css` at the project root and `src/styles.css` (per the existing dual-write logic in esbuild.config.mjs:58-59).
- The regenerated `styles.css` contains the Phase 71 marker and the `.rp-donate-address` selector.
  </action>
  <verify>
    <automated>npm run build && grep -q "'donate-section'" esbuild.config.mjs && grep -q "Phase 71: Donate section (DONATE-01)" styles.css && grep -q "\.rp-donate-address" styles.css && grep -c "'runner-view'\|'canvas-selector'\|'editor-panel'\|'snippet-manager'\|'snippet-fill-modal'\|'loop-support'\|'snippet-tree-picker'\|'inline-runner'" esbuild.config.mjs | grep -q "^8$"</automated>
  </verify>
  <acceptance_criteria>
    - `esbuild.config.mjs` contains the literal string `'donate-section'`.
    - `esbuild.config.mjs` still contains all 8 prior entries: `'runner-view'`, `'canvas-selector'`, `'editor-panel'`, `'snippet-manager'`, `'snippet-fill-modal'`, `'loop-support'`, `'snippet-tree-picker'`, `'inline-runner'` (verified by an aggregate grep counting 8 matches).
    - `'donate-section'` appears in `esbuild.config.mjs` AFTER `'inline-runner'` (verified by `grep -n` showing `'donate-section'` line number greater than `'inline-runner'` line number).
    - `npm run build` exits 0.
    - The regenerated `styles.css` (project root) contains the literal string `Phase 71: Donate section (DONATE-01)`.
    - The regenerated `styles.css` contains the literal selector `.rp-donate-address`.
    - The regenerated `styles.css` contains the literal selector `.rp-donate-intro`.
    - The regenerated `styles.css` contains all prior feature CSS too (e.g. `.rp-selector-bar` from canvas-selector.css, `.rp-inline-runner` from inline-runner.css — confirms no existing rules were dropped).
  </acceptance_criteria>
  <done>
    `donate-section.css` is wired into the build pipeline. The next time any plan runs `npm run build`, the Phase 71 styles are emitted into `styles.css` automatically. Plan 03 (settings.ts integration) can now safely emit `<code class="rp-donate-address">` and `<p class="rp-donate-intro">` knowing the styles will land.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config → bundle | `esbuild.config.mjs` is the gateway controlling what CSS reaches the user's vault. A wrong entry in `CSS_FILES` (typo in filename, wrong order, duplicate) corrupts the bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Tampering | `esbuild.config.mjs` `CSS_FILES` array | mitigate | Acceptance criterion verifies all 8 prior entries remain present after the edit (aggregate grep counting matches against the pre-edit set). The CLAUDE.md "never delete code you didn't add" rule is the standing safeguard against an editor agent silently dropping an entry. |
| T-71-04 | Denial of service | `src/styles/donate-section.css` (rule scope) | accept | Donate section CSS targets only `.rp-donate-*` classes. No global selectors, no `*` resets. Worst-case fallout from a buggy rule is limited to the donate section itself; the runner / editor / snippet UIs are unaffected because they use disjoint class prefixes (`rp-runner-*`, `rp-edit-*`, `rp-stp-*`). |
</threat_model>

<verification>
1. `test -f src/styles/donate-section.css` exits 0.
2. `grep -c "Phase 71: Donate section (DONATE-01)" src/styles/donate-section.css` returns `1`.
3. `grep -c "'donate-section'" esbuild.config.mjs` returns `1`.
4. `npm run build` exits 0.
5. `grep -c "Phase 71: Donate section (DONATE-01)" styles.css` returns `1` (regenerated bundle).
6. `grep -c "\.rp-donate-address" styles.css` returns `1`.
7. `grep -c "\.rp-donate-intro" styles.css` returns `1`.
8. The 8 pre-existing CSS_FILES entries remain in `esbuild.config.mjs` (no deletions).
</verification>

<success_criteria>
- New per-feature CSS file `src/styles/donate-section.css` exists with the Phase 71 header marker and minimal rules per VIS-D-03.
- `esbuild.config.mjs` `CSS_FILES` array now contains 9 entries — the 8 originals (untouched) plus `'donate-section'` appended at the end.
- `npm run build` regenerates `styles.css` containing all donate-section rules.
- No existing CSS rules were deleted from the build output (verified by spot-checking selectors from earlier feature files).
</success_criteria>

<output>
After completion, create `.planning/phases/71-settings-donate-section/71-02-SUMMARY.md` documenting:
- The path of the new CSS file.
- The exact one-line addition to `esbuild.config.mjs`.
- Confirmation that `styles.css` was regenerated and contains the donate rules.
- A reminder that the next plan (Plan 03) will produce DOM nodes with class names `rp-donate-intro` and `rp-donate-address`.
</output>
