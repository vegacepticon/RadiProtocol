---
phase: 44-unified-loop-runtime
plan: 03
subsystem: runner-view-picker
tags: [ui, runner-view, css, session-roundtrip, wave-3]

# Dependency graph
requires:
  - plan: 44-02a (state machine) — provides AwaitingLoopPickState, chooseLoopBranch(edgeId), getSerializableState/restoreFrom widening, the awaiting-loop-pick exhaustiveness stub arm in RunnerView (this plan replaces it with the real picker render)
  - plan: 44-02b (runner picker tests) — RUN-01..RUN-05 + W4 already pinned green at the state-machine layer; UI work cannot regress runtime contract
  - plan: 44-04 (legacy maxIterations excision) — Settings/parser/editor cleanup independent of UI; no shared files
  - phase: 43 — provides LoopNode.headerText field, GraphValidator LOOP-04, literal Cyrillic «выход», unified-loop-valid.canvas fixture
provides:
  - "RunnerView case 'awaiting-loop-pick' — real picker render arm (between snippet-pick and snippet-fill) replacing Plan 02a Rule 3 placeholder"
  - "RunnerView handleSelectorSelect — needsConfirmation now includes 'awaiting-loop-pick' (canvas-switch confirmation parity with other picker states)"
  - "src/styles/loop-support.css — Phase 44 picker CSS classes appended under /* Phase 44 */ marker (rp-loop-header-text, rp-loop-picker-list, rp-loop-body-btn, rp-loop-exit-btn) without touching the Phase 6 block"
  - "Regenerated styles.css + src/styles.css containing Phase 44 classes (esbuild concatenation)"
  - "src/__tests__/runner/protocol-runner-session.test.ts — 7 new RUN-06 tests against unified-loop-valid.canvas; all 6 it.skip + 1 describe.skip blocks deleted along with their TODO Phase 44 comment headers"
affects:
  - Phase 44 verifier — all 7 RUN requirements now satisfied across phase 44 (RUN-01 picker UI, RUN-02..05 state machine, RUN-06 session round-trip both type and integration halves, RUN-07 legacy excision)
  - Phase 45 (LOOP-05 / LOOP-06) — RunnerView picker is the runtime contract Phase 45 Node Editor form for loop kind must produce data for; CSS classes already styled means Phase 45 only owns editor form

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Picker render skeleton mirrors awaiting-snippet-pick arm shape (halt-state preview zone + toolbar + picker delegation) but inlines the picker rather than delegating to a helper — picker is small enough (≤25 lines) that an inline implementation reads cleaner than a separate renderLoopPicker() method"
    - "Edge filter over adjacency (Pitfall 4): graph.edges.filter(e => e.fromNodeId === state.nodeId) — one button per outgoing edge with edge.label as text, dispatch via runner.chooseLoopBranch(edge.id). adjacency.get() loses labels and would force a second lookup"
    - "Click handler tail (BUG-01 + Pattern 3): syncManualEdit(preview.value ?? '') BEFORE chooseLoopBranch(edge.id), then void autoSaveSession() + void renderAsync() — exact same rhythm as chooseAnswer/chooseSnippetBranch"
    - "Empty-describe-block guard (Rule 3): vitest fails a suite that contains no tests; when bulk-deleting it.skip blocks one must also delete or merge any describe wrappers that become empty as a result. Plan deletions left two such empty wrappers; collapsed both into the SESSION-01 describe and let the new RUN-06 describe stand on its own"
    - "CSS append-only marker convention (CLAUDE.md): new picker classes land under a /* Phase 44: Unified loop picker (RUN-01) */ delimiter at the bottom of loop-support.css — Phase 6 block above stays byte-identical (the four .rp-loop-iteration-label / .rp-loop-btn-row / .rp-loop-again-btn / .rp-loop-done-btn rules are now technically unused by Phase 44 but preserved per never-remove rule)"

key-files:
  created: []
  modified:
    - src/views/runner-view.ts (+62 / -12 net) — replaced Plan 02a placeholder arm with real picker render (53 lines: graph guard + node lookup + headerText render + edge button loop + step-back); positioned between awaiting-snippet-pick and awaiting-snippet-fill; needsConfirmation extended with awaiting-loop-pick
    - src/styles/loop-support.css (+44 / 0) — appended Phase 44 picker CSS under marker; Phase 6 block preserved verbatim
    - styles.css (regenerated by npm run build — esbuild concatenation of CSS_FILES)
    - src/styles.css (regenerated copy)
    - src/__tests__/runner/protocol-runner-session.test.ts (+135 / -166 net) — deleted 6 it.skip + 1 describe.skip blocks + their TODO Phase 44 comment headers; deleted 2 now-empty describe wrappers (Rule 3); added new describe('session — awaiting-loop-pick (RUN-06)') with 7 concrete tests

key-decisions:
  - "Render arm position: inserted BEFORE awaiting-snippet-fill (per plan instruction 'picker variants stay adjacent') by deleting the Plan 02a placeholder at end of switch and re-adding the real arm in the canonical slot. This keeps render() switch ordered: idle → at-node → awaiting-snippet-pick → awaiting-loop-pick → awaiting-snippet-fill → complete → error → default. TypeScript exhaustiveness is preserved either way (case order does not affect type checking)"
  - "Edge filter pattern (Pitfall 4): used graph.edges.filter(e => e.fromNodeId === state.nodeId), NOT graph.adjacency.get(state.nodeId). Adjacency map returns child node IDs but loses edge labels — picker UX requires label text, so adjacency would force a second graph.edges lookup per edge. Filter is the validator's pattern (graph-validator.ts:98) too — single source of truth"
  - "Exit button class branching: edge.label === 'выход' (literal 6-char Cyrillic, NO trim, NO lowercase, exact match per Phase 43 D-08). Body buttons get .rp-loop-body-btn (accent-coloured); exit button gets .rp-loop-exit-btn (border-modifier neutral). Visual distinction signals to user that one branch leaves the loop while others stay inside"
  - "syncManualEdit BEFORE chooseLoopBranch (Pitfall 7 / BUG-01): identical rhythm to chooseAnswer/chooseSnippetBranch click handlers. Without this, manual textarea edits made by the user during the picker halt would be lost when the runner advances. Step-back handler does NOT call syncManualEdit — step-back overwrites accumulator from the undo snapshot anyway"
  - "Empty describe deletion (Rule 3): vitest reports 'Error: No test found in suite' as a test failure for any describe() block that ends up empty. Plan-deletion-step left two SESSION-01 describes empty (restoreFrom + round-trip), so I collapsed them with the existing 'getSerializableState() (SESSION-01)' describe via removing the wrapper headers — net effect: the same SESSION-01 tests live under one describe instead of three. New RUN-06 describe is non-empty so stands alone"
  - "Empty-headerText guard: the plan instruction says 'when headerText is non-empty render <p>' — empty headerText is a legitimate authored state (per Phase 43 parser fallback to '') so the picker MUST render even when headerText is '' (just without the <p> header). Code matches: `if (node.headerText !== '') { questionZone.createEl('p', ...) }`"
  - "Step-back inside picker arm: copies the at-node arm pattern verbatim (`runner.stepBack()` → `void autoSaveSession()` → `this.render()` — note `render()` not `renderAsync()` per the at-node analog). Step-back leaves loop frame popped via the undo snapshot's loopContextStack restore"
  - "Test 5 wording: 'canStepBack is true even on first halt after start()' — exercises the B2 invariant (Plan 02a Step A1) where loop-entry pushes undo regardless of previousCursor=null; without B2, canStepBack would be false on the first picker halt and the round-trip would lose the contract"

patterns-established:
  - "Picker UI pattern for the unified loop: render arm is ~50 lines, mirrors the question/answer button list inside at-node but uses graph.edges.filter rather than adjacency. Future picker-style halts (e.g. Phase 45+ if added) can copy this rhythm: graph guard → node lookup → optional header → edge filter → button list with click handler → step-back → preview/toolbar"
  - "Empty-suite cleanup after bulk it.skip removal: anytime a plan deletes all tests inside a describe, also delete or merge the wrapper. Document as Rule 3 (blocking issue from cascade)"

requirements-completed:
  - "RUN-01 (UI half complete): RunnerView renders LoopNode.headerText above one button per outgoing edge of the loop node — body branches with rp-loop-body-btn, «выход» with rp-loop-exit-btn — under the awaiting-loop-pick state. State-machine half was Plan 02a"
  - "RUN-05 (UI half complete): step-back button on the picker fires runner.stepBack() + autoSaveSession() + render() — restores pre-loop accumulated text and node via the undo snapshot Plan 02a pushed at loop entry"
  - "RUN-06 (integration half complete): 7 new tests cover non-null serialization, all required PersistedSession fields, currentNodeId/status round-trip, accumulatedText round-trip, canStepBack=true round-trip (B2), JSON idempotency, loopContextStack iteration=2 round-trip — all green against unified-loop-valid.canvas"

# Metrics
duration: ~5min
completed: 2026-04-17
---

# Phase 44 Plan 03: RunnerView Picker UI + Session Round-Trip Tests Summary

**Replaced the Plan 02a `awaiting-loop-pick` exhaustiveness stub in `RunnerView.render()` with the real picker arm (header text + one button per outgoing edge using `graph.edges.filter` + step-back), appended four Phase 44 CSS classes to `src/styles/loop-support.css` under the `/* Phase 44: Unified loop picker (RUN-01) */` marker (Phase 6 block byte-preserved per CLAUDE.md), regenerated `styles.css` and `src/styles.css` via `npm run build`, and rewrote 6 `it.skip` + 1 `describe.skip` blocks in `protocol-runner-session.test.ts` against `unified-loop-valid.canvas` as 7 new green RUN-06 tests. Full suite: 402 passed + 1 skipped / 0 failed (was 395 + 8 — net +7 passing, -7 skipped); only RUN-08 skip remains (preserved per Plan 02b for Phase 45). With this plan, all 7 RUN requirements close at both the runtime and UI/test layers; Phase 44 is functionally complete pending verifier sign-off.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-17T13:46:45Z
- **Completed:** 2026-04-17T13:52:03Z
- **Tasks:** 2 / 2
- **Files modified:** 5 (1 view, 1 CSS source, 2 generated CSS, 1 test)
- **Files created:** 0

## Accomplishments

### Task 1 — Picker render arm + Phase 44 CSS + rebuild

**Step A — `src/views/runner-view.ts`:**
- Replaced the Plan 02a placeholder `case 'awaiting-loop-pick'` arm (which lived after `case 'error'` as a temp exhaustiveness stub printing 'Loop picker (Phase 44 Plan 03 will render this).') with the real picker arm
- New arm position: inserted between `case 'awaiting-snippet-pick':` (line ~434-450) and `case 'awaiting-snippet-fill':` (now line ~510) — picker variants stay adjacent
- Arm structure (53 lines):
  1. Graph null guard → renderError
  2. `this.graph.nodes.get(state.nodeId)` lookup with kind === 'loop' assertion → renderError on miss
  3. Conditional `<p class="rp-loop-header-text">` rendering when `node.headerText !== ''`
  4. `this.graph.edges.filter(e => e.fromNodeId === state.nodeId)` — Pitfall 4 (NOT adjacency)
  5. `<div class="rp-loop-picker-list">` parent + button-per-edge loop with `rp-loop-exit-btn` for «выход» / `rp-loop-body-btn` for others, click handler does syncManualEdit (Pitfall 7) → `runner.chooseLoopBranch(edge.id)` → autoSaveSession + renderAsync
  6. Step-back button when `state.canStepBack` (RUN-05 — verbatim copy of at-node arm: stepBack + autoSaveSession + render — note `render()` not `renderAsync()`)
  7. `renderPreviewZone(previewZone, state.accumulatedText)` + `renderOutputToolbar(outputToolbar, state.accumulatedText, false)`
- `handleSelectorSelect` `needsConfirmation` (line 251-255) extended to include `state.status === 'awaiting-loop-pick'` (parity with at-node / awaiting-snippet-pick / awaiting-snippet-fill — in-progress picker counts as in-progress session)

**Step B — `src/styles/loop-support.css`:**
- Appended 44 lines (4 new classes + section marker) below the existing Phase 6 block:
  - `/* Phase 44: Unified loop picker (RUN-01) */` section marker
  - `.rp-loop-header-text` — font-medium, font-semibold, text-normal, margin-bottom 8px
  - `.rp-loop-picker-list` — flex column, gap 6px, margin-top 8px
  - `.rp-loop-body-btn, .rp-loop-exit-btn` — shared shape (text-align left, padding, border-radius, no border, cursor pointer, font-small)
  - `.rp-loop-body-btn` — interactive-accent background + text-on-accent + hover variant
  - `.rp-loop-exit-btn` — background-modifier-border + text-normal + hover variant with opacity 0.9
- Phase 6 block (`.rp-loop-iteration-label`, `.rp-loop-btn-row`, `.rp-loop-again-btn`, `.rp-loop-done-btn`) preserved byte-identically (CLAUDE.md never-delete; Phase 45 may garbage-collect if truly unused)

**Step C — Rebuild:**
- `npm run build` exited 0
- esbuild concatenated CSS_FILES (`runner-view`, `canvas-selector`, `editor-panel`, `snippet-manager`, `snippet-fill-modal`, `loop-support`) → `styles.css` (root) + `src/styles.css` (convenience copy)
- All 4 Phase 44 classes present in both regenerated outputs (8 hits each: header + picker-list + body-btn × 2 hits per name due to grep counting hover variants and combined selectors)

### Task 2 — Session round-trip test rewrites

**Step A — Deletions (in reverse-line order to keep anchors stable):**
1. Deleted `describe.skip('Loop context stack survives session round-trip (SESSION-05)')` block (~33 lines including the inner `it` body that called the deleted `chooseLoopAction` API + inspected the deleted `loopIterationLabel` field) along with its 4-line `TODO Phase 44` comment header above
2. Deleted `it.skip('getSerializableState() → JSON.stringify → JSON.parse → restoreFrom() produces identical getState()')` (~30 lines + 3-line TODO comment)
3. Deleted `it.skip('canStepBack is true when undoStack was non-empty in saved state')` (~18 lines + 1-line TODO comment)
4. Deleted `it.skip('restores accumulatedText correctly (SESSION-05)')` (~21 lines + 3-line TODO comment)
5. Deleted `it.skip('restores currentNodeId and status correctly')` (~22 lines + 2-line TODO comment)
6. Deleted `it.skip('serialized state has all required PersistedSession fields')` (~16 lines + 1-line TODO comment)
7. Deleted `it.skip('returns non-null when runner is at-node awaiting user input')` (~14 lines + 3-line TODO comment)

**Step A.5 (Rule 3 deviation — see Deviations section):** Steps 4 and 5 left their parent `describe('ProtocolRunner.restoreFrom() (SESSION-01, SESSION-05)')` empty; step 2 left its parent `describe('ProtocolRunner session round-trip serialization (SESSION-01, SESSION-07)')` empty. Vitest fails on empty suites. Removed the two empty `describe` wrapper headers entirely — net effect: the surviving SESSION-01 tests now live under the single `'ProtocolRunner.getSerializableState() (SESSION-01)'` describe.

**Step B — New describe block appended at end of file:**

```typescript
describe('session — awaiting-loop-pick (RUN-06)', () => {
  it('returns non-null at awaiting-loop-pick state with loop node id', ...)
  it('serialized awaiting-loop-pick state has all required PersistedSession fields', ...)
  it('restores awaiting-loop-pick currentNodeId and status correctly', ...)
  it('restores accumulatedText after body-branch walk + dead-end + picker', ...)
  it('canStepBack is true in restored awaiting-loop-pick (B2 invariant)', ...)
  it('getSerializableState → JSON.stringify → JSON.parse → restoreFrom is idempotent', ...)
  it('loopContextStack with iteration=2 survives JSON round-trip (SESSION-05)', ...)
});
```

All 7 tests use `loadGraph('unified-loop-valid.canvas')`; tests 4 + 6 + 7 also call `chooseLoopBranch('e2'); chooseAnswer('n-a1')` to drive iteration 1 → iteration 2 via the back-edge e5 → B1 re-entry guard.

## Task Commits

1. **Task 1: Add loop picker render arm + Phase 44 CSS (RUN-01, RUN-05)** — `5e0448d` (feat)
2. **Task 2: Rewrite skipped session-roundtrip tests for awaiting-loop-pick (RUN-06)** — `ae1d97a` (test)

## Files Created/Modified

### Created

- None — Plan 03 modifies existing files only.

### Modified

- `src/views/runner-view.ts` (+62 / -12 net) — replaced Plan 02a placeholder arm with real picker render + needsConfirmation update
- `src/styles/loop-support.css` (+44 / 0) — appended Phase 44 picker classes
- `styles.css` (regenerated) — esbuild concatenation
- `src/styles.css` (regenerated) — esbuild secondary output
- `src/__tests__/runner/protocol-runner-session.test.ts` (+135 / -166 net) — 7 deletions + 7 additions

## Verification Results

### Plan-level done criteria (all green)

**Task 1:**
- ✅ `grep -c "case 'awaiting-loop-pick'" src/views/runner-view.ts` → **1**
- ✅ `grep -c 'chooseLoopBranch(edge.id)' src/views/runner-view.ts` → **1** (≥1)
- ✅ `grep -c 'awaiting-loop-pick' src/views/runner-view.ts` → **2** (render arm + needsConfirmation; ≥2)
- ✅ `grep -c 'Phase 44: Unified loop picker' src/styles/loop-support.css` → **1**
- ✅ `grep -c 'Phase 6: Loop Support' src/styles/loop-support.css` → **1** (legacy block preserved)
- ✅ `grep -c '.rp-loop-picker-list\|.rp-loop-header-text\|.rp-loop-exit-btn\|.rp-loop-body-btn' styles.css` → **8** (all four classes present, multiple hits due to combined selectors + hover)
- ✅ `grep -c '.rp-loop-picker-list\|.rp-loop-header-text\|.rp-loop-exit-btn\|.rp-loop-body-btn' src/styles.css` → **8** (secondary output)
- ✅ `npm run build` → exit 0
- ✅ `npx tsc --noEmit --skipLibCheck` → exit 0

**Task 2:**
- ✅ `grep -c 'it.skip\|describe.skip' src/__tests__/runner/protocol-runner-session.test.ts` → **0**
- ✅ `grep -c 'loop-body.canvas' src/__tests__/runner/protocol-runner-session.test.ts` → **0**
- ✅ `grep -c 'awaiting-loop-pick' src/__tests__/runner/protocol-runner-session.test.ts` → **17** (≥6 — multiple per test)
- ✅ `grep -c 'unified-loop-valid.canvas' src/__tests__/runner/protocol-runner-session.test.ts` → **7** (≥6)
- ✅ `grep -c 'TODO Phase 44' src/__tests__/runner/protocol-runner-session.test.ts` → **0**
- ✅ `npx vitest run src/__tests__/runner/protocol-runner-session.test.ts` → 14 passed / 0 failed (7 new RUN-06 + 7 pre-existing snippet/branch tests)
- ✅ `npm test -- --run` → **402 passed + 1 skipped / 0 failed** (was 395 + 8 — net +7 passing, -7 skipped)

### Full-plan verification

- ✅ `npx tsc --noEmit --skipLibCheck` → exit 0
- ✅ `npm test -- --run` → 402 passed + 1 skipped / 0 failed (29 test files)
- ✅ `npm run build` → exit 0 (production bundle generated; dev vault copy succeeded)
- ✅ Both regenerated CSS outputs (`styles.css`, `src/styles.css`) contain all four Phase 44 classes
- ✅ `grep -c 'Phase 6: Loop Support' src/styles/loop-support.css` → 1 (legacy block untouched)
- ✅ `grep -c 'Phase 44: Unified loop picker' src/styles/loop-support.css` → 1
- ✅ RunnerView render switch handles all 8 runner-state variants (idle, at-node, awaiting-snippet-pick, awaiting-loop-pick, awaiting-snippet-fill, complete, error, default exhaustiveness arm) without TypeScript exhaustiveness error
- ✅ `git diff --diff-filter=D --name-only HEAD~2 HEAD` → empty (no file deletions)
- ✅ Manual UAT (per 44-VALIDATION.md) — deferred to user runtime smoke-test in Obsidian; scripted automated coverage cannot exercise the actual ItemView DOM in a headless test (vitest does not load Obsidian)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed two now-empty describe wrappers in protocol-runner-session.test.ts**
- **Found during:** Task 2 verify (`npx vitest run` reported `Error: No test found in suite` × 2)
- **Issue:** Plan Step A told me to delete 6 `it.skip` blocks. Deletions 4 + 5 emptied `describe('ProtocolRunner.restoreFrom() (SESSION-01, SESSION-05)')`; deletion 2 emptied `describe('ProtocolRunner session round-trip serialization (SESSION-01, SESSION-07)')`. Vitest treats an empty describe block as a test failure (`Error: No test found in suite`).
- **Fix:** Deleted both empty describe wrapper headers (and their preceding `// ──` section comment lines). Surviving SESSION-01 tests now live under the single `describe('ProtocolRunner.getSerializableState() (SESSION-01)')` block. The new `describe('session — awaiting-loop-pick (RUN-06)')` block is non-empty so stands alone.
- **Files modified:** `src/__tests__/runner/protocol-runner-session.test.ts`
- **Commit:** `ae1d97a` (folded into the same Task 2 commit since the empty wrappers only emerged after the deletions)
- **Rationale:** Plan instruction "DO NOT touch any other describe..." was about preserving SESSION-01 / D-22 / Phase-31 D-09 / snippet-pick describes that contain green tests. Removing wrapper headers around zero tests is not "touching tests" — it is post-deletion cleanup. Treating as Rule 3 (blocking — cannot satisfy `npm test exits 0` otherwise).

### TDD note

Plan 03 task 2 has `tdd="true"`. The pattern is "RED via JSON-roundtrip assertion against a state-machine that already supports `awaiting-loop-pick` from Plan 02a". Per the existing Plan 02a runtime contract (PersistedSession.runnerStatus union widened, getSerializableState/restoreFrom signatures widened, AwaitingLoopPickState fully serializable via existing currentNodeId + accumulatedText + loopContextStack carry-through), the new RUN-06 tests would have been GREEN immediately upon writing them. To honour the TDD spirit, I confirmed before commit that:
1. The deleted `it.skip` blocks were already RED at the test-runner level (skip is a meta-RED state — Plan 02a deliberately preserved them for this rewrite)
2. The new tests assert behaviour that did not previously exist in the test suite (i.e. zero tests targeted `awaiting-loop-pick` round-trip before this plan)
3. All 7 new tests pass on first run against the existing runtime — no implementation code changes required (Plan 02a already delivered the round-trip support; Plan 03 only writes the assertions)

This is the canonical "test-only commit" pattern Plan 02b established (`02ee321`). Single test commit `ae1d97a` covers both gates.

### Auth gates

None — fully autonomous execution.

### Architectural decisions

None — no Rule 4 checkpoints needed.

## Handoff Notes

### For Phase 44 verifier

- **All 7 RUN requirements satisfied** at both runtime + UI + test layers across Plan 02a, Plan 02b, Plan 03, Plan 04
- **Manual UAT** per 44-VALIDATION.md remains as the only outstanding verification step — automated tests cover everything except the actual ItemView DOM render in Obsidian (vitest does not load Obsidian; RunnerView extends ItemView). UAT instructions:
  1. `npm run dev` (watch build) + open Obsidian dev vault
  2. Open a canvas containing a `loop` node with `radiprotocol_headerText` set to e.g. "Lesion loop" and at least one body branch + the literal `выход` exit edge
  3. Start the protocol → verify picker shows header text on top, body-branch button(s) labelled with edge labels (accent-coloured), `выход` button (border-modifier neutral-coloured), step-back button when canStepBack
  4. Click a body-branch button → walks to neighbour; complete branch → returns to picker (RUN-02)
  5. Click `выход` → loop frame pops, advance along exit edge → completes (RUN-03)
  6. Mid-picker, close Obsidian → reopen → verify session resumes at same picker with same accumulated text (RUN-06 manual)
  7. Mid-picker, click step-back → restores pre-loop accumulated text + node (RUN-05)
- **Nested loops (RUN-04):** load `unified-loop-nested.canvas` for visual validation that inner `выход` returns to outer picker without exiting

### For Phase 45 (LOOP-05 / LOOP-06)

- **Editor panel form for unified `loop` kind:** the current editor-panel-view.ts has a `default` arm catching the `loop` kind; Plan 45 fills in `case 'loop'` with a form for `headerText` (mirrors `case 'question'` for `questionText` shape). The legacy `case 'loop-start': case 'loop-end':` informational stub from Plan 04 stays untouched (Phase 46 owns full removal once `RPNodeKind` drops legacy kinds)
- **NodePickerModal entry for `loop`:** Plan 45 adds a "Loop" entry to the node picker modal so authors can drop a unified loop node onto canvases without manual JSON editing
- **CSS already styled** by Plan 03 — picker UI is fully production-ready; Plan 45 only touches editor + picker modal

### For Phase 46 (legacy free-text-input / kind removal)

- The `case 'loop-start': case 'loop-end':` informational stub in editor-panel-view.ts is the candidate for removal once parser/validator/runtime no longer need legacy kinds in the union
- The four Phase 6 CSS classes (`.rp-loop-iteration-label`, `.rp-loop-btn-row`, `.rp-loop-again-btn`, `.rp-loop-done-btn`) in `src/styles/loop-support.css` are no longer used by any RunnerView arm — Phase 46 may garbage-collect them as part of dead-CSS sweep (CLAUDE.md never-delete preserved them through Phase 44)

## Known Stubs

None introduced or remaining. The Plan 02a `awaiting-loop-pick` placeholder is now replaced by the real picker render. The only acknowledged stub remaining in the codebase from this phase is the editor-panel `case 'loop-start': case 'loop-end':` informational stub from Plan 04, which is intentional handoff to Phase 46.

## Threat Flags

None — UI render arm reads from already-validated graph (validator runs before runtime), uses Obsidian DOM helpers (`createEl`, `createDiv`, `registerDomEvent`) — no `innerHTML`, no new file access, no new network surface, no schema change at trust boundaries.

## TDD Gate Compliance

Plan 03 Task 1 (`tdd="true"`): no separate RED commit because the test surface for RunnerView render is the manual UAT step (vitest cannot exercise the ItemView DOM). The Plan 02b state-machine tests at `protocol-runner-loop-picker.test.ts` continue to pin the runtime contract — green throughout this plan.

Plan 03 Task 2 (`tdd="true"`): the 7 new tests target a state (`awaiting-loop-pick` round-trip) for which zero tests existed prior to this plan. The deleted `it.skip` blocks were the historical RED placeholders. Because Plan 02a already delivered the round-trip support at the runtime level, the new tests pass immediately on commit — same as Plan 02b's RUN-01..RUN-05 + W4 tests. Single test() commit per the test-only-commit pattern.

## Self-Check: PASSED

- ✅ FOUND: `src/views/runner-view.ts` (modified — placeholder arm replaced with real picker render; needsConfirmation extended)
- ✅ FOUND: `src/styles/loop-support.css` (modified — Phase 44 marker + 4 classes appended)
- ✅ FOUND: `styles.css` (regenerated — contains rp-loop-picker-list etc.)
- ✅ FOUND: `src/styles.css` (regenerated — contains rp-loop-picker-list etc.)
- ✅ FOUND: `src/__tests__/runner/protocol-runner-session.test.ts` (modified — 7 skips deleted, 7 new RUN-06 tests added, 2 empty describes removed)
- ✅ FOUND commit: `5e0448d` — `feat(44-03): add loop picker render arm + Phase 44 CSS (RUN-01, RUN-05)`
- ✅ FOUND commit: `ae1d97a` — `test(44-03): rewrite skipped session-roundtrip tests for awaiting-loop-pick (RUN-06)`
