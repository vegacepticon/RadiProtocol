---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 07
type: execute
wave: 4
depends_on: ["43-02", "43-03", "43-04", "43-05", "43-06"]
files_modified:
  - src/__tests__/graph-validator.test.ts
  - src/__tests__/session-service.test.ts
  - src/__tests__/runner/protocol-runner.test.ts
  - src/__tests__/runner/protocol-runner-session.test.ts
autonomous: true
requirements:
  - LOOP-04
  - MIGRATE-01
user_setup: []

must_haves:
  truths:
    - "graph-validator.test.ts содержит positive-тест для unified-loop-valid.canvas (0 LOOP-04 ошибок) — LOOP-04 happy path"
    - "graph-validator.test.ts содержит отдельные тесты для missing «выход», duplicate «выход» и no-body sub-checks (D-08.1, D-08.2, D-08.3)"
    - "graph-validator.test.ts содержит тест migration-error для loop-start.canvas и loop-body.canvas — assert'ы проверяют дословные лексемы «loop-start», «loop-end», «loop», «выход» (MIGRATE-01, D-07)"
    - "graph-validator.test.ts содержит тест что cycle через unified loop узел НЕ даёт unintentional-cycle ошибки (D-09)"
    - "graph-validator.test.ts содержит тест что legacy канвас с loop-start и 0 outgoing edges даёт migration-error (а не LOOP-04) — D-CL-02 order check"
    - "graph-validator.test.ts НЕ содержит устаревший тест 'detects orphaned loop-end node' (D-19 удалил)"
    - "session-service.test.ts строки 137, 195, 197 используют поле loopNodeId (не loopStartId) — Surprise #6 compile-green"
    - "session-service.test.ts содержит новый тест: validateSessionNodeIds возвращает missing для сессии со ссылкой на legacy loop ID БЕЗ throw (graceful reject — D-20)"
    - "protocol-runner.test.ts блок describe('loop support (LOOP-01 through LOOP-05, RUN-09)') помечен .skip (D-18)"
    - "protocol-runner.test.ts блок describe('loop-start missing continue edge (RUN-08)') помечен .skip (D-18)"
    - "protocol-runner-session.test.ts блок describe('Loop context stack survives session round-trip (SESSION-05)') помечен .skip (D-18)"
    - "npm run build завершается exit 0"
    - "npm test завершается exit 0 — все тесты проходят (.skip не считаются failures)"
  artifacts:
    - path: "src/__tests__/graph-validator.test.ts"
      provides: "LOOP-04 + MIGRATE-01 + D-09 тесты; удалён тест Check 6"
      contains: "unified-loop-valid.canvas"
    - path: "src/__tests__/session-service.test.ts"
      provides: "loopNodeId compile-fix + D-20 graceful-reject тест"
      contains: "loopNodeId"
    - path: "src/__tests__/runner/protocol-runner.test.ts"
      provides: ".skip пометки для loop-related describe-блоков"
      contains: "describe.skip"
    - path: "src/__tests__/runner/protocol-runner-session.test.ts"
      provides: ".skip пометка для SESSION-05 Loop context stack теста"
      contains: "describe.skip"
  key_links:
    - from: "src/__tests__/graph-validator.test.ts"
      to: "src/__tests__/fixtures/unified-loop-*.canvas"
      via: "parseFixture() helper"
      pattern: "parseFixture\\('unified-loop-"
    - from: "src/__tests__/graph-validator.test.ts"
      to: "src/graph/graph-validator.ts"
      via: "validator.validate(graph) возвращает errors[] с точными строками из Plan 05"
      pattern: "«выход»"
    - from: "src/__tests__/session-service.test.ts"
      to: "src/sessions/session-model.ts"
      via: "PersistedLoopContext shape с полем loopNodeId (после Plan 03 Task 2)"
      pattern: "loopNodeId:"
---

<objective>
Завершить Phase 43 — обновить test-корпус под unified-loop модель. После этого плана все тесты проекта компилируются и проходят (`npm test` exit 0), а новые LOOP-04 / MIGRATE-01 проверки формализованы:

1. **D-19 graph-validator тесты** — добавить positive + negative тесты для unified loop через fixtures из Plan 06; добавить migration-error тесты для legacy fixtures (D-16); добавить cycle-through-loop-node тест; удалить старый «orphaned loop-end» тест.
2. **D-20 + Surprise #6 session-service** — переименовать inline `loopStartId` → `loopNodeId` в трёх call-sites (compile-green против PersistedLoopContext shape из Plan 03) + добавить тест graceful-reject.
3. **D-18 runner-тесты .skip** — пометить loop-related блоки `.skip` в `protocol-runner.test.ts` и `protocol-runner-session.test.ts` БЕЗ удаления тел, чтобы Phase 44 переписал их поверх контура.

Purpose: Тесты — финальный guard, чтобы Phase 43-изменения (модель / парсер / validator / fixtures) закрепить в CI. Без них regression в Phase 44+ снова может начать «тихо удалять» части Phase 43 (см. CLAUDE.md про recurring regressions). Surprise #2 объясняет, почему `chooseLoopAction` остаётся stub-методом в Plan 04 — чтобы тела `.skip`-тестов продолжали TypeScript-компилироваться, ссылаясь на него.

Output:
- `src/__tests__/graph-validator.test.ts` — обновлённый с новыми LOOP-04/MIGRATE-01/D-09 тестами, без старого Check 6 теста
- `src/__tests__/session-service.test.ts` — `loopStartId` → `loopNodeId` в строках 137/195/197 + новый D-20 тест
- `src/__tests__/runner/protocol-runner.test.ts` — `.skip` на loop-related describe-блоках
- `src/__tests__/runner/protocol-runner-session.test.ts` — `.skip` на `Loop context stack survives session round-trip (SESSION-05)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-02-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-03-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-04-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-05-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-06-SUMMARY.md
@CLAUDE.md
@src/__tests__/graph-validator.test.ts
@src/__tests__/session-service.test.ts
@src/__tests__/runner/protocol-runner.test.ts
@src/__tests__/runner/protocol-runner-session.test.ts

<interfaces>
```ts
// parseFixture / loadFixture helpers (graph-validator.test.ts:9-18):
function loadFixture(name: string): string;
function parseFixture(name: string): ProtocolGraph;
// Usage: const graph = parseFixture('unified-loop-valid.canvas');

// Validator API:
const validator = new GraphValidator();
const errors: string[] = validator.validate(graph);

// Точные error-строки из Plan 05 (validator — см. 43-05-SUMMARY.md / graph-validator.ts):
// Migration Check (D-07):
//   'Канвас содержит устаревшие узлы loop-start/loop-end: "<label>", ... . Пересоберите цикл с единым узлом loop: метка «выход» ...'
// LOOP-04 D-08.1:
//   'Loop node "<label>" не имеет ребра «выход». Добавьте исходящее ребро с меткой «выход» ...'
// LOOP-04 D-08.2:
//   'Loop node "<label>" имеет несколько рёбер «выход»: <edge-ids>. Должно быть ровно одно исходящее ребро ...'
// LOOP-04 D-08.3:
//   'Loop node "<label>" не имеет ни одной body-ветви. Добавьте хотя бы одно исходящее ребро ...'

// session-service PersistedLoopContext shape (после Plan 03 Task 2):
interface PersistedLoopContext { loopNodeId: string; iteration: number; textBeforeLoop: string; }

// validateSessionNodeIds signature:
function validateSessionNodeIds(session: PersistedSession, graph: ProtocolGraph): string[];
// Возвращает массив missing node IDs. Никогда не throws.
```
</interfaces>
</context>

<project_rules_to_honor>
- **CLAUDE.md never-remove**: в `graph-validator.test.ts` НЕ трогать describe `'valid protocols'` > `linear.canvas`/`branching.canvas`, describe `'GraphValidator — Phase 31'`, describe `'GraphValidator — snippet node (Phase 29, D-12)'`, тест `'returns all errors as plain English strings...'`. В `session-service.test.ts` — не трогать SESSION-01, SESSION-02, SESSION-04, SESSION-06, SESSION-07 тесты. В `protocol-runner.test.ts` — все не-loop describe-блоки (`initial state`, `start() linear`, `iteration cap (RUN-09, D-08)` — text-block-based, не loop!, `Separator logic`, `ProtocolRunner has zero Obsidian API imports`, `syncManualEdit`, snippet-тесты). В `protocol-runner-session.test.ts` — SESSION-01, SESSION-03, D-22, Phase 31 D-09 тесты.
- **CLAUDE.md append-only per phase**: новые тесты добавлять отдельным `describe('GraphValidator — Phase 43: ...', () => {...})` блоком в конце `graph-validator.test.ts` (параллельно блоку «Phase 31» и «snippet (Phase 29)»). НЕ переписывать существующие блоки.
- **TDD-подобная дисциплина**: тесты пишутся против контрактов из Plan 05 (точные строки) — если тест падает на grep'е конкретной русской формулировки, значит Plan 05 executor использовал отличающуюся фразу. Если такое произошло, тест должен использовать более ТОЛЕРАНТНЫЙ матчер, который проверяет обязательные лексемы (слова `Loop node`, `«выход»`, `body`, имя узла) а не буквальную фразу целиком — чтобы тест отражал contract'ы D-07/D-08, а не конкретную формулировку.
- **Никаких .skip для тестов, которые должны проходить** — `.skip` применяется ТОЛЬКО к loop-related runner-блокам (D-18), которые ссылаются на удалённый loop-runtime.
</project_rules_to_honor>

<tasks>

<task type="auto">
  <name>Task 1: .skip loop-related блоки в protocol-runner.test.ts и protocol-runner-session.test.ts (D-18)</name>

  <read_first>
    - `src/__tests__/runner/protocol-runner.test.ts` (строки 256-267 `loop-start missing continue edge (RUN-08)`; строки 450-531 `loop support (LOOP-01 through LOOP-05, RUN-09)`)
    - `src/__tests__/runner/protocol-runner-session.test.ts` (строки 319-348 `Loop context stack survives session round-trip (SESSION-05)`)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-18 (policy: .skip, не delete)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секции про `protocol-runner.test.ts`, `protocol-runner-session.test.ts`, Surprise #2 (`chooseLoopAction` остаётся stub — тела `.skip` тестов всё равно компилируются и ссылаются на него)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-04-SUMMARY.md` (подтвердить что `chooseLoopAction` в `protocol-runner.ts` действительно остался как `@deprecated` stub с теми же аргументами `'again' | 'done'`)
    - `CLAUDE.md` never-remove — НЕ трогать тела тестов, только добавить `.skip` на describe-блоках + TODO комментарий
  </read_first>

  <objective>
    Пометить три describe-блока `.skip`, сохранив тела целиком (контур для Phase 44). После этого vitest пропускает их (counted as skipped, не failed), а TypeScript всё равно компилирует тела — что требует чтобы `chooseLoopAction`, `maxLoopIterations`, `loopIterationLabel`, `isAtLoopEnd` как символы продолжали существовать (Plan 04 Task 1 + Task 2 это уже гарантировали).
  </objective>

  <action>
  **1. В `src/__tests__/runner/protocol-runner.test.ts` — две правки:**

  **1.1. Блок `loop-start missing continue edge (RUN-08)` (строка 256)** — заменить `describe(` на `describe.skip(` и добавить TODO-комментарий СРАЗУ перед `describe.skip`:

  ```ts
  // TODO Phase 44: rewrite for unified loop — RUN-08 (loop-start missing continue edge) теряет
  // смысл: loop-start/loop-end kind удалены, canvas-ы с ними отвергаются GraphValidator'ом через
  // migration-error до runtime. Контур теста сохранён для истории; Phase 44 перепишет под
  // unified-loop «missing «выход» edge» если нужно runtime-level проверять (валидатор уже проверяет).
  describe.skip('loop-start missing continue edge (RUN-08)', () => {
    it('transitions to error state when loop-start has no continue edge', () => {
      // ... тело НЕ трогать ...
    });
  });
  ```

  **1.2. Блок `loop support (LOOP-01 through LOOP-05, RUN-09)` (строка 450)** — заменить `describe(` на `describe.skip(` и добавить TODO-комментарий:

  ```ts
  // TODO Phase 44: rewrite for unified loop (RUN-01..RUN-07). LoopEndNode удалён, chooseLoopAction
  // превращён в @deprecated stub (Phase 43 D-14). Тела тестов сохранены как исторический контур
  // поведения (iteration counter, chooseLoopAction('again'/'done'), stepBack из loop body, per-loop
  // maxIterations cap) — Phase 44 реализует unified picker и перепишет эти проверки поверх LoopNode.
  describe.skip('loop support (LOOP-01 through LOOP-05, RUN-09)', () => {
    // ... helper reachLoopEnd + все it-блоки — НЕ трогать ...
  });
  ```

  **Не трогать helper `reachLoopEnd`** внутри этого describe — он компилируется против сигнатуры `ProtocolRunner.chooseAnswer()` (не удалён).

  **ВАЖНО про RUN-09 describe НЕ трогать**: блок `describe('iteration cap (RUN-09, D-08)', ...)` на строке 269 — ЭТО НЕ loop-тест (тестирует auto-advance cap через chain of text-blocks, maxIterations параметр `ProtocolRunner` конструктора — который Plan 04 НЕ трогал, он остаётся общим cap'ом). `.skip` на него НЕ ставить.

  **НЕ ТРОГАТЬ в этом файле:**
  - Любые другие describe-блоки (`initial state`, `start() linear`, `iteration cap (RUN-09, D-08)` на строке 269, `Separator logic`, `ProtocolRunner has zero Obsidian API imports`, `syncManualEdit`, snippet-тесты и т.д.)
  - Тела двух помеченных .skip блоков — они должны компилироваться
  - Импорты — они не меняются (ProtocolGraph, ProtocolRunner, CanvasParser всё ещё нужны)

  **2. В `src/__tests__/runner/protocol-runner-session.test.ts` — одна правка:**

  **2.1. Блок `Loop context stack survives session round-trip (SESSION-05)` (строка 319)** — заменить `describe(` на `describe.skip(`:

  ```ts
  // TODO Phase 44: rewrite for unified loop (SESSION-05 round-trip). LoopContext.loopNodeId
  // уже переименовано (Phase 43 D-04), но тело теста использует loadGraph('loop-body.canvas') +
  // chooseLoopAction('again'), которые отрезают legacy runtime. Phase 44 перепишет используя
  // unified-loop-valid.canvas fixture после реализации runtime picker'а.
  describe.skip('Loop context stack survives session round-trip (SESSION-05)', () => {
    it('loopContextStack with iteration=2 is restored correctly and getState() reflects loopIterationLabel', () => {
      // ... тело НЕ трогать ...
    });
  });
  ```

  **НЕ ТРОГАТЬ в этом файле:**
  - Блоки `ProtocolRunner.getSerializableState()`, `ProtocolRunner.restoreFrom()`, `ProtocolRunner session round-trip serialization`, `session — awaiting-snippet-pick (D-22)`, `Phase 31 D-09` — не модифицировать
  - Тело `.skip`'нутого блока — НЕ трогать (контур сохраняется для Phase 44)
  - Импорты — не меняются
  </action>

  <acceptance_criteria>
    - `.skip` применён к трём describe-блокам:
      ```bash
      grep -q "describe\.skip('loop-start missing continue edge (RUN-08)'" src/__tests__/runner/protocol-runner.test.ts
      grep -q "describe\.skip('loop support (LOOP-01 through LOOP-05, RUN-09)'" src/__tests__/runner/protocol-runner.test.ts
      grep -q "describe\.skip('Loop context stack survives session round-trip (SESSION-05)'" src/__tests__/runner/protocol-runner-session.test.ts
      ```
    - TODO-комментарии с «Phase 44» перед каждым `.skip`:
      ```bash
      grep -B2 "describe\.skip('loop-start missing continue edge" src/__tests__/runner/protocol-runner.test.ts | grep -q "TODO Phase 44"
      grep -B2 "describe\.skip('loop support" src/__tests__/runner/protocol-runner.test.ts | grep -q "TODO Phase 44"
      grep -B2 "describe\.skip('Loop context stack" src/__tests__/runner/protocol-runner-session.test.ts | grep -q "TODO Phase 44"
      ```
    - `describe('iteration cap (RUN-09, D-08)'` — НЕ помечен `.skip` (text-block auto-advance cap):
      ```bash
      grep -q "describe('iteration cap (RUN-09, D-08)'" src/__tests__/runner/protocol-runner.test.ts
      ! grep "describe\.skip('iteration cap (RUN-09, D-08)'" src/__tests__/runner/protocol-runner.test.ts
      ```
    - Helper `reachLoopEnd` сохранён (тело .skip'нутого блока не тронуто):
      ```bash
      grep -q "function reachLoopEnd" src/__tests__/runner/protocol-runner.test.ts
      ```
    - `chooseLoopAction` references всё ещё присутствуют внутри .skip тел (compile-check):
      ```bash
      grep -q "chooseLoopAction" src/__tests__/runner/protocol-runner.test.ts
      grep -q "chooseLoopAction" src/__tests__/runner/protocol-runner-session.test.ts
      ```
    - Other describe-блоки нетронуты:
      ```bash
      grep -q "describe('initial state (RUN-08 — idle state)'" src/__tests__/runner/protocol-runner.test.ts
      grep -q "describe('Separator logic (SEP-01, SEP-02, D-01 through D-04)'" src/__tests__/runner/protocol-runner.test.ts
      grep -q "describe('syncManualEdit" src/__tests__/runner/protocol-runner.test.ts
      grep -q "describe('ProtocolRunner.getSerializableState" src/__tests__/runner/protocol-runner-session.test.ts
      grep -q "describe('Phase 31 D-09" src/__tests__/runner/protocol-runner-session.test.ts
      ```
    - Компиляция файлов проходит (полный build зелёный после всех планов):
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "runner/protocol-runner\.test\.ts|runner/protocol-runner-session\.test\.ts")
      ```
    - vitest прогон этих файлов не падает (skip-ы не считаются failures):
      ```bash
      npx vitest run src/__tests__/runner/protocol-runner.test.ts src/__tests__/runner/protocol-runner-session.test.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      grep -q "describe\.skip('loop-start missing continue edge (RUN-08)'" src/__tests__/runner/protocol-runner.test.ts \
        && grep -q "describe\.skip('loop support (LOOP-01 through LOOP-05, RUN-09)'" src/__tests__/runner/protocol-runner.test.ts \
        && grep -q "describe\.skip('Loop context stack survives session round-trip (SESSION-05)'" src/__tests__/runner/protocol-runner-session.test.ts \
        && grep -q "function reachLoopEnd" src/__tests__/runner/protocol-runner.test.ts \
        && grep -q "describe('iteration cap (RUN-09, D-08)'" src/__tests__/runner/protocol-runner.test.ts \
        && ! grep "describe\.skip('iteration cap (RUN-09, D-08)'" src/__tests__/runner/protocol-runner.test.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "runner/protocol-runner.*\.test\.ts") \
        && npx vitest run src/__tests__/runner/protocol-runner.test.ts src/__tests__/runner/protocol-runner-session.test.ts > /dev/null 2>&1
    </automated>
  </verify>

  <done>
    - Три describe-блока помечены `.skip` с TODO-комментариями Phase 44
    - Тела .skip-блоков нетронуты (контур сохранён)
    - Не-loop describe-блоки нетронуты (iteration cap RUN-09, Separator, syncManualEdit и т.д.)
    - Файлы компилируются и vitest-прогон проходит
  </done>
</task>

<task type="auto">
  <name>Task 2: session-service.test.ts — переименовать inline loopStartId → loopNodeId (Surprise #6) + добавить D-20 graceful-reject тест</name>

  <read_first>
    - `src/__tests__/session-service.test.ts` (весь файл — 207 строк; особое внимание строкам 133-142 SESSION-03 loopStartId-тест, строки 195-197 SESSION-07 round-trip)
    - `src/sessions/session-model.ts` (после Plan 03 Task 2 — подтвердить что PersistedLoopContext имеет поле `loopNodeId: string`)
    - `src/sessions/session-service.ts` (после Plan 03 Task 3 — подтвердить что `validateSessionNodeIds` читает `frame.loopNodeId`)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-04 + D-13 + D-20
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/__tests__/session-service.test.ts`» + Surprise #6
    - `CLAUDE.md` never-remove — НЕ трогать SESSION-01, SESSION-02, SESSION-04, SESSION-06 тесты и `makeSession`/`makeVaultMock`/`makeAppMock` фабрики
  </read_first>

  <objective>
    После этой задачи тест-файл компилируется против нового `PersistedLoopContext` shape (поле `loopNodeId`) и покрывает D-20 требование (graceful reject legacy session без throw).
  </objective>

  <action>
  **1. Переименовать существующие inline `loopStartId` → `loopNodeId` (три строки, Surprise #6).**

  **Строка 133 (`it` заголовок)** — переименовать `loopStartId` → `loopNodeId` в названии теста И в объекте:

  Найти (строка 133):
  ```ts
  it('returns missing loopStartId from loopContextStack when node removed', () => {
    const session = makeSession({
      currentNodeId: 'n-q1',
      undoStack: [],
      loopContextStack: [{ loopStartId: 'n-ls-deleted', iteration: 1, textBeforeLoop: '' }],
    });
    const graph = makeGraph(['n-q1']);
    const missing = validateSessionNodeIds(session, graph as never);
    expect(missing).toContain('n-ls-deleted');
  });
  ```

  Заменить НА:
  ```ts
  it('returns missing loopNodeId from loopContextStack when node removed', () => {
    const session = makeSession({
      currentNodeId: 'n-q1',
      undoStack: [],
      loopContextStack: [{ loopNodeId: 'n-ls-deleted', iteration: 1, textBeforeLoop: '' }],
    });
    const graph = makeGraph(['n-q1']);
    const missing = validateSessionNodeIds(session, graph as never);
    expect(missing).toContain('n-ls-deleted');
  });
  ```

  **Строки 195 и 197 (SESSION-07 round-trip audit)** — переименовать `loopStartId` → `loopNodeId` в двух местах:

  Найти (внутри `it('JSON round-trip of PersistedSession contains no Set objects ...'):` строки 192-198):
  ```ts
  const session = makeSession({
    undoStack: [
      { nodeId: 'n-start', textSnapshot: '', loopContextStack: [] },
      { nodeId: 'n-q1',    textSnapshot: 'Liver', loopContextStack: [{ loopStartId: 'n-ls1', iteration: 1, textBeforeLoop: '' }] },
    ],
    loopContextStack: [{ loopStartId: 'n-ls1', iteration: 2, textBeforeLoop: 'Liver' }],
  });
  ```

  Заменить НА:
  ```ts
  const session = makeSession({
    undoStack: [
      { nodeId: 'n-start', textSnapshot: '', loopContextStack: [] },
      { nodeId: 'n-q1',    textSnapshot: 'Liver', loopContextStack: [{ loopNodeId: 'n-ls1', iteration: 1, textBeforeLoop: '' }] },
    ],
    loopContextStack: [{ loopNodeId: 'n-ls1', iteration: 2, textBeforeLoop: 'Liver' }],
  });
  ```

  **2. Добавить новый D-20 тест в describe `validateSessionNodeIds() (SESSION-03)` блок.**

  Добавить НОВЫЙ `it(...)` внутри существующего `describe('validateSessionNodeIds() (SESSION-03)', ...)` ПОСЛЕ теста `'returns missing loopNodeId from loopContextStack when node removed'` (то есть последним `it` в этом describe-блоке):

  ```ts
  // Phase 43 D-20 — graceful reject сессии со ссылкой на legacy loop ID.
  // Старый канвас с loop-start/loop-end теперь отклоняется GraphValidator'ом ДО runner'а,
  // но backstop-path через validateSessionNodeIds → missing IDs → RunnerView.sessionService.clear()
  // должен работать без throw (D-13 graceful reject).
  it('gracefully returns missing legacy loop node ID without throwing (D-20, D-13)', () => {
    const session = makeSession({
      currentNodeId: 'n-q1',
      undoStack: [
        { nodeId: 'n-start', textSnapshot: '', loopContextStack: [] },
      ],
      // Legacy ID ссылается на узел, которого нет в новом графе (бывший loop-start ID):
      loopContextStack: [{ loopNodeId: 'n-legacy-ls1', iteration: 1, textBeforeLoop: '' }],
    });
    const graph = makeGraph(['n-q1', 'n-start']);
    let missing: string[] = [];
    expect(() => { missing = validateSessionNodeIds(session, graph as never); }).not.toThrow();
    expect(missing).toContain('n-legacy-ls1');
  });
  ```

  **НЕ ТРОГАТЬ:**
  - `makeVaultMock`, `makeAppMock`, `makeSession` фабрики (строки 8-43)
  - describe `'SessionService API surface (SESSION-01)'`
  - describe `'SessionService.save() write behavior (SESSION-01)'`
  - describe `'SessionService.hasSession() (SESSION-02)'`
  - остальные существующие `it`-тесты внутри SESSION-03 (кроме переименования loopStartId на строке 133)
  - describe `'canvas mtime comparison logic (SESSION-04)'`
  - describe `'SessionService.load() graceful degradation (SESSION-06)'`
  - describe `'PersistedSession JSON serialization audit (SESSION-07)'` тело-метадата (только две строки 195/197 с полем)
  - импорты в начале файла
  </action>

  <acceptance_criteria>
    - ВСЕ inline вхождения `loopStartId` удалены из файла:
      ```bash
      ! grep "loopStartId" src/__tests__/session-service.test.ts
      ```
    - `loopNodeId` присутствует на минимум трёх строках (133 переименованная + 195 + 197 + новый D-20 тест):
      ```bash
      count=$(grep -c "loopNodeId" src/__tests__/session-service.test.ts)
      test "$count" -ge 4
      ```
    - Переименованный it-заголовок:
      ```bash
      grep -q "returns missing loopNodeId from loopContextStack when node removed" src/__tests__/session-service.test.ts
      ! grep "returns missing loopStartId from loopContextStack" src/__tests__/session-service.test.ts
      ```
    - Новый D-20 тест присутствует:
      ```bash
      grep -q "gracefully returns missing legacy loop node ID without throwing (D-20, D-13)" src/__tests__/session-service.test.ts
      grep -q "n-legacy-ls1" src/__tests__/session-service.test.ts
      grep -q "not\.toThrow" src/__tests__/session-service.test.ts
      ```
    - Существующие describe-блоки нетронуты:
      ```bash
      grep -q "describe('SessionService API surface (SESSION-01)'" src/__tests__/session-service.test.ts
      grep -q "describe('SessionService.hasSession() (SESSION-02)'" src/__tests__/session-service.test.ts
      grep -q "describe('validateSessionNodeIds() (SESSION-03)'" src/__tests__/session-service.test.ts
      grep -q "describe('canvas mtime comparison logic (SESSION-04)'" src/__tests__/session-service.test.ts
      grep -q "describe('SessionService.load() graceful degradation (SESSION-06)'" src/__tests__/session-service.test.ts
      grep -q "describe('PersistedSession JSON serialization audit (SESSION-07)'" src/__tests__/session-service.test.ts
      ```
    - Компиляция проходит (после Plan 03 Task 2 / Task 3):
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "session-service.test.ts")
      ```
    - vitest-прогон файла проходит:
      ```bash
      npx vitest run src/__tests__/session-service.test.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      ! grep "loopStartId" src/__tests__/session-service.test.ts \
        && grep -q "returns missing loopNodeId from loopContextStack when node removed" src/__tests__/session-service.test.ts \
        && grep -q "gracefully returns missing legacy loop node ID without throwing (D-20, D-13)" src/__tests__/session-service.test.ts \
        && grep -q "n-legacy-ls1" src/__tests__/session-service.test.ts \
        && grep -q "not\.toThrow" src/__tests__/session-service.test.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "session-service.test.ts") \
        && npx vitest run src/__tests__/session-service.test.ts > /dev/null 2>&1
    </automated>
  </verify>

  <done>
    - Все `loopStartId` переименованы в `loopNodeId` (три inline-литерала + it-заголовок)
    - Новый D-20 тест присутствует и проходит (не throws, возвращает missing)
    - Существующие тесты нетронуты
    - Файл компилируется; vitest run — 0 failures
  </done>
</task>

<task type="auto">
  <name>Task 3: graph-validator.test.ts — LOOP-04 + MIGRATE-01 + D-09 тесты, удаление устаревшего Check 6 теста</name>

  <read_first>
    - `src/__tests__/graph-validator.test.ts` (весь файл — 215 строк)
    - `src/__tests__/fixtures/unified-loop-valid.canvas`, `unified-loop-missing-exit.canvas`, `unified-loop-duplicate-exit.canvas`, `unified-loop-no-body.canvas` (после Plan 06 — убедиться что они существуют; проверить структуру и edge labels)
    - `src/__tests__/fixtures/loop-start.canvas`, `loop-body.canvas` (legacy fixtures, нетронуты — reassigned для migration-error тестирования per D-16)
    - `src/__tests__/fixtures/cycle.canvas` (существующий — unintentional cycle WITHOUT loop узла; используется в D-09 тесте как negative control)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секции D-07, D-08, D-09, D-10, D-16, D-19
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/__tests__/graph-validator.test.ts`» + обязательные лексемы migration-error (D-07)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-05-SUMMARY.md` (финальные error-строки из Plan 05 — точные фразы)
    - `CLAUDE.md` never-remove — НЕ трогать:
      - describe `'GraphValidator'` > `'valid protocols'` (linear.canvas, branching.canvas)
      - describe `'GraphValidator'` > `'error detection (PARSE-07, PARSE-08)'` за исключением: (a) УДАЛИТЬ тест `'detects orphaned loop-end node'` (D-19, D-10), (b) ОСТАЛЬНЫЕ тесты внутри этого describe сохранены — `dead-end`, `cycle`, `multiple start nodes`, `missing start`, `unreachable nodes`, `plain English strings`
      - describe `'GraphValidator — Phase 31'` — целиком
      - describe `'GraphValidator — snippet node (Phase 29, D-12)'` — целиком
      - describe `'GraphValidator'` > `'loop validation (LOOP-01, LOOP-06)'` > `'valid loop-body graph passes validation with zero errors (LOOP-01)'` — ПЕРЕИНТЕРПРЕТИРОВАТЬ: теперь `loop-body.canvas` — legacy canvas, validator возвращает migration-error. Этот тест надо ПЕРЕПИСАТЬ (не удалить): он становится MIGRATE-01 тестом.
  </read_first>

  <objective>
    После этой задачи файл содержит:
    1. УДАЛЁН тест `'detects orphaned loop-end node'` (D-10 — Check 6 удалён в Plan 05)
    2. ПЕРЕПИСАН тест `'valid loop-body graph passes validation with zero errors (LOOP-01)'` → теперь это MIGRATE-01 тест (loop-body.canvas даёт migration-error, т.к. содержит loop-start+loop-end узлы)
    3. ДОБАВЛЕН новый describe-блок `'GraphValidator — Phase 43: unified loop + migration (LOOP-04, MIGRATE-01)'` в конце файла со всеми LOOP-04 + MIGRATE-01 + D-09 + D-CL-02 order тестами
  </objective>

  <action>

  **1. УДАЛИТЬ тест `'detects orphaned loop-end node'` (строки 113-132).**

  Найти блок:
  ```ts
  it('detects orphaned loop-end node', () => {
    const orphanLoopEndJson = JSON.stringify({
      nodes: [
        // ...
      ],
      edges: [/* ... */]
    });
    const parser = new CanvasParser();
    const result = parser.parse(orphanLoopEndJson, 'orphan-loop-end.canvas');
    expect(result.success).toBe(true);
    if (!result.success) return;
    const validator = new GraphValidator();
    const errors = validator.validate(result.graph);
    expect(errors.some(e => e.toLowerCase().includes('loop') || e.toLowerCase().includes('orphan'))).toBe(true);
  });
  ```

  Удалить целиком (весь `it(...)` блок). Комментарий-пометка на месте удаления:

  ```ts
  // Phase 43 D-10: test 'detects orphaned loop-end node' удалён — Check 6 исчез из validator'а
  // вместе с LoopEndNode kind. Legacy loop-end узлы теперь ловятся Migration Check'ом (MIGRATE-01),
  // см. тесты в describe 'GraphValidator — Phase 43: unified loop + migration' ниже.
  ```

  **2. ПЕРЕПИСАТЬ тест `'valid loop-body graph passes validation with zero errors (LOOP-01)'`.**

  Найти describe-блок `'loop validation (LOOP-01, LOOP-06)'` (строка 146) и тест внутри (строки 147-152):
  ```ts
  describe('loop validation (LOOP-01, LOOP-06)', () => {
    it('valid loop-body graph passes validation with zero errors (LOOP-01)', () => {
      const graph = parseFixture('loop-body.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      expect(errors).toHaveLength(0);
    });
  });
  ```

  Заменить ВЕСЬ блок (describe целиком) на:
  ```ts
  // Phase 43 D-16 + D-19: loop-body.canvas и loop-start.canvas теперь legacy канвасы.
  // Проверка happy-path для unified loop — см. тест 'unified-loop-valid.canvas passes ...'
  // в describe 'GraphValidator — Phase 43' ниже.
  // Сам describe-блок loop validation удалён: единственный его тест переформулирован в
  // MIGRATE-01 (legacy → migration-error) в Phase 43-тестах ниже.
  ```

  (То есть убираем `describe('loop validation (LOOP-01, LOOP-06)', () => { ... })` целиком. Новый LOOP-04 happy-path будет в Phase 43 describe.)

  **3. ДОБАВИТЬ новый describe-блок в конце файла** — после существующих `GraphValidator — Phase 31` и `GraphValidator — snippet node (Phase 29, D-12)` describe-блоков:

  ```ts
  // ─────────────────────────────────────────────────────────────────────────────
  // Phase 43 D-19 — LOOP-04 + MIGRATE-01 + cycle-through-loop tests
  // ─────────────────────────────────────────────────────────────────────────────
  describe('GraphValidator — Phase 43: unified loop + migration (LOOP-04, MIGRATE-01)', () => {

    // ── LOOP-04 happy path ──────────────────────────────────────────────────────
    it('unified-loop-valid.canvas passes LOOP-04 checks (no «выход» / body errors)', () => {
      // Fixture: один loop узел с ровно одним ребром «выход» + body-веткой (back-edge).
      const graph = parseFixture('unified-loop-valid.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      // Не должно быть LOOP-04-специфичных ошибок. (Другие checks могут что-то вернуть —
      // тест-контракт: отсутствие именно LOOP-04 сообщений.)
      expect(errors.some(e => e.includes('не имеет ребра «выход»'))).toBe(false);
      expect(errors.some(e => e.includes('имеет несколько рёбер «выход»'))).toBe(false);
      expect(errors.some(e => e.includes('не имеет ни одной body-ветви'))).toBe(false);
      // И никаких migration-ошибок (в этом канвасе нет legacy узлов):
      expect(errors.some(e => e.includes('устаревшие узлы loop-start/loop-end'))).toBe(false);
    });

    // ── LOOP-04 D-08.1 missing «выход» ──────────────────────────────────────────
    it('unified-loop-missing-exit.canvas flags missing «выход» edge (D-08.1)', () => {
      const graph = parseFixture('unified-loop-missing-exit.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      expect(
        errors.some(e => e.includes('«выход»') && e.includes('не имеет ребра')),
      ).toBe(true);
    });

    // ── LOOP-04 D-08.2 duplicate «выход» (with edge IDs) ────────────────────────
    it('unified-loop-duplicate-exit.canvas flags duplicate «выход» edges with edge IDs (D-08.2)', () => {
      const graph = parseFixture('unified-loop-duplicate-exit.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      expect(
        errors.some(e => e.includes('несколько рёбер «выход»')),
      ).toBe(true);
      // Обе edge-ID (e3, e4 в fixture) должны упоминаться в error-строке.
      const duplicateErr = errors.find(e => e.includes('несколько рёбер «выход»'));
      expect(duplicateErr).toBeDefined();
      if (duplicateErr === undefined) return;
      expect(duplicateErr).toContain('e3');
      expect(duplicateErr).toContain('e4');
    });

    // ── LOOP-04 D-08.3 no body ──────────────────────────────────────────────────
    it('unified-loop-no-body.canvas flags no-body-branch (D-08.3)', () => {
      const graph = parseFixture('unified-loop-no-body.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      expect(
        errors.some(e => e.includes('не имеет ни одной body-ветви')),
      ).toBe(true);
    });

    // ── MIGRATE-01 (D-07) — legacy loop-body.canvas ─────────────────────────────
    it('legacy loop-body.canvas returns migration-error with required literals (MIGRATE-01, D-07)', () => {
      const graph = parseFixture('loop-body.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      // Должна быть ровно одна сводная migration-строка, содержащая все обязательные лексемы.
      const migrationErr = errors.find(e =>
        e.includes('loop-start') &&
        e.includes('loop-end') &&
        e.includes('loop') &&
        e.includes('«выход»'),
      );
      expect(migrationErr).toBeDefined();
      if (migrationErr === undefined) return;
      // Русская формулировка: «устаревшие узлы» или эквивалент из Plan 05.
      expect(migrationErr).toMatch(/устаревш/);
    });

    // ── MIGRATE-01 (D-07) — legacy loop-start.canvas ────────────────────────────
    it('legacy loop-start.canvas returns migration-error with required literals (MIGRATE-01, D-07)', () => {
      const graph = parseFixture('loop-start.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      const migrationErr = errors.find(e =>
        e.includes('loop-start') &&
        e.includes('loop-end') &&
        e.includes('loop') &&
        e.includes('«выход»'),
      );
      expect(migrationErr).toBeDefined();
    });

    // ── D-CL-02 order: migration check runs BEFORE LOOP-04 (early-return) ───────
    it('legacy canvas with 0 outgoing edges gives migration-error, NOT LOOP-04 «выход» error (D-CL-02 order)', () => {
      // loop-start.canvas содержит loop-start без continue-edge. Без early-return
      // в Migration Check, LOOP-04 бы сгенерировал «не имеет ребра «выход»» поверх.
      // Правильный порядок (D-CL-02): migration check срабатывает первым и делает return.
      const graph = parseFixture('loop-start.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      // Migration-error присутствует:
      expect(errors.some(e => e.includes('устаревш'))).toBe(true);
      // НЕТ LOOP-04 «не имеет ребра «выход»» ошибки, хотя loop-start формально такое ребро не имеет:
      expect(errors.some(e => e.includes('не имеет ребра «выход»'))).toBe(false);
    });

    // ── D-09: cycle through unified loop node is NOT flagged as unintentional ───
    it('cycle through unified loop node is NOT flagged as unintentional (D-09)', () => {
      // unified-loop-valid.canvas содержит cycle n-loop → n-q1 → n-a1 → n-loop,
      // проходящий через loop-узел — detectUnintentionalCycles теперь маркирует
      // это как намеренный цикл (kind === 'loop').
      const graph = parseFixture('unified-loop-valid.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      // Не должно быть cycle-error'а:
      expect(errors.some(e => e.toLowerCase().includes('unintentional cycle'))).toBe(false);
    });

    // ── D-09 negative control: cycle WITHOUT loop node IS flagged ───────────────
    it('cycle WITHOUT loop node is still flagged as unintentional (D-09 negative control)', () => {
      // Существующий cycle.canvas fixture — cycle из не-loop узлов → должен flag'аться.
      const graph = parseFixture('cycle.canvas');
      const validator = new GraphValidator();
      const errors = validator.validate(graph);
      expect(errors.some(e => e.toLowerCase().includes('unintentional cycle'))).toBe(true);
      // Сообщение обновлено на «loop node» (не «loop-end node»):
      expect(errors.some(e => e.includes('loop node'))).toBe(true);
      expect(errors.some(e => e.includes('loop-end node'))).toBe(false);
    });
  });
  ```

  **Важное про толерантность assertion'ов:** все grep'ы по русским фразам используют `includes('...')` для ключевых лексем (например `«выход»`, `не имеет ребра`, `устаревш`), а не полные предложения. Это защищает от небольших отличий формулировок (Plan 05 D-CL-01 оставляет небольшой люфт). Но обязательные лексемы D-07 (`loop-start`, `loop-end`, `loop`, `«выход»`) — жёсткие: они явно описаны в CONTEXT.md как contract.

  **Важное про unified-loop-valid.canvas edge labels:** fixture (Plan 06) содержит edge `e2 label='проверка'` (body) + `e3 label='выход'` (exit) + `e4` (без label, body q1→a1) + `e5` (без label, back-edge a1→loop). Эта структура — тело цикла, которое detectUnintentionalCycles теперь не флагает (D-09).

  **НЕ ТРОГАТЬ:**
  - Helper функции `loadFixture`, `parseFixture` (строки 9-18)
  - describe `'GraphValidator'` > `'valid protocols'` (linear, branching) — строки 21-35
  - describe `'GraphValidator'` > `'error detection (PARSE-07, PARSE-08)'` КРОМЕ удаления теста 'detects orphaned loop-end node'
  - describe `'GraphValidator — Phase 31'` — строки 156-205
  - describe `'GraphValidator — snippet node (Phase 29, D-12)'` — строки 207-214
  - импорты (CanvasParser, GraphValidator, vitest, fs, path)
  </action>

  <acceptance_criteria>
    - Устаревший тест удалён:
      ```bash
      ! grep "detects orphaned loop-end node" src/__tests__/graph-validator.test.ts
      ! grep "orphan-loop-end.canvas" src/__tests__/graph-validator.test.ts
      ```
    - Старый describe 'loop validation (LOOP-01, LOOP-06)' удалён:
      ```bash
      ! grep "describe('loop validation (LOOP-01, LOOP-06)'" src/__tests__/graph-validator.test.ts
      ! grep "valid loop-body graph passes validation with zero errors" src/__tests__/graph-validator.test.ts
      ```
    - Новый Phase 43 describe-блок присутствует:
      ```bash
      grep -q "describe('GraphValidator — Phase 43: unified loop + migration (LOOP-04, MIGRATE-01)'" src/__tests__/graph-validator.test.ts
      ```
    - Все unified-loop fixtures упоминаются в новых тестах:
      ```bash
      grep -q "parseFixture('unified-loop-valid.canvas')" src/__tests__/graph-validator.test.ts
      grep -q "parseFixture('unified-loop-missing-exit.canvas')" src/__tests__/graph-validator.test.ts
      grep -q "parseFixture('unified-loop-duplicate-exit.canvas')" src/__tests__/graph-validator.test.ts
      grep -q "parseFixture('unified-loop-no-body.canvas')" src/__tests__/graph-validator.test.ts
      ```
    - Legacy fixtures упоминаются в migration-тестах:
      ```bash
      grep -q "parseFixture('loop-body.canvas')" src/__tests__/graph-validator.test.ts
      grep -q "parseFixture('loop-start.canvas')" src/__tests__/graph-validator.test.ts
      ```
    - Обязательные лексемы D-07 (MIGRATE-01) проверяются в тесте:
      ```bash
      grep -q "includes('loop-start')" src/__tests__/graph-validator.test.ts
      grep -q "includes('loop-end')" src/__tests__/graph-validator.test.ts
      grep -q "includes('loop')" src/__tests__/graph-validator.test.ts
      grep -q "includes('«выход»')" src/__tests__/graph-validator.test.ts
      ```
    - LOOP-04 substeps purpose-assertions на месте:
      ```bash
      grep -q "не имеет ребра «выход»" src/__tests__/graph-validator.test.ts      # D-08.1
      grep -q "несколько рёбер «выход»" src/__tests__/graph-validator.test.ts     # D-08.2
      grep -q "не имеет ни одной body-ветви" src/__tests__/graph-validator.test.ts # D-08.3
      ```
    - D-CL-02 order тест присутствует:
      ```bash
      grep -q "D-CL-02 order" src/__tests__/graph-validator.test.ts
      ```
    - D-09 cycle-through-loop-node тест присутствует:
      ```bash
      grep -q "cycle through unified loop node is NOT flagged as unintentional (D-09)" src/__tests__/graph-validator.test.ts
      grep -q "cycle WITHOUT loop node is still flagged" src/__tests__/graph-validator.test.ts
      ```
    - Существующие describe-блоки нетронуты:
      ```bash
      grep -q "describe('valid protocols'" src/__tests__/graph-validator.test.ts
      grep -q "describe('error detection (PARSE-07, PARSE-08)'" src/__tests__/graph-validator.test.ts
      grep -q "describe('GraphValidator — Phase 31: mixed and snippet-only question branches'" src/__tests__/graph-validator.test.ts
      grep -q "describe('GraphValidator — snippet node (Phase 29, D-12)'" src/__tests__/graph-validator.test.ts
      ```
    - Компиляция проходит:
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "graph-validator.test.ts")
      ```
    - vitest-прогон файла — все тесты проходят:
      ```bash
      npx vitest run src/__tests__/graph-validator.test.ts
      ```
    - Полный build и test:
      ```bash
      npm run build
      npm test
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # 1. Устаревшие тесты удалены
      ! grep "detects orphaned loop-end node" src/__tests__/graph-validator.test.ts \
        && ! grep "describe('loop validation (LOOP-01, LOOP-06)'" src/__tests__/graph-validator.test.ts \
        && ! grep "valid loop-body graph passes validation with zero errors" src/__tests__/graph-validator.test.ts

      # 2. Новые тесты присутствуют
      grep -q "describe('GraphValidator — Phase 43: unified loop + migration (LOOP-04, MIGRATE-01)'" src/__tests__/graph-validator.test.ts \
        && grep -q "parseFixture('unified-loop-valid.canvas')" src/__tests__/graph-validator.test.ts \
        && grep -q "parseFixture('unified-loop-missing-exit.canvas')" src/__tests__/graph-validator.test.ts \
        && grep -q "parseFixture('unified-loop-duplicate-exit.canvas')" src/__tests__/graph-validator.test.ts \
        && grep -q "parseFixture('unified-loop-no-body.canvas')" src/__tests__/graph-validator.test.ts \
        && grep -q "D-CL-02 order" src/__tests__/graph-validator.test.ts \
        && grep -q "cycle through unified loop node is NOT flagged" src/__tests__/graph-validator.test.ts

      # 3. Существующие тесты нетронуты
      grep -q "describe('GraphValidator — Phase 31" src/__tests__/graph-validator.test.ts \
        && grep -q "describe('GraphValidator — snippet node (Phase 29, D-12)'" src/__tests__/graph-validator.test.ts

      # 4. Final build + test green
      npm run build > /dev/null 2>&1 \
        && npm test > /dev/null 2>&1
    </automated>
  </verify>

  <done>
    - Тест 'detects orphaned loop-end node' удалён
    - describe 'loop validation (LOOP-01, LOOP-06)' удалён (тест перепакован в MIGRATE-01)
    - Новый describe 'GraphValidator — Phase 43: unified loop + migration' присутствует с 9 тестами (LOOP-04 happy path, 3 LOOP-04 sub-checks, 2 MIGRATE-01, D-CL-02 order, D-09 positive, D-09 negative control)
    - Все обязательные лексемы D-07 проверяются
    - Все unified-loop-*.canvas fixtures упоминаются
    - legacy loop-body.canvas / loop-start.canvas fixtures используются в migration-тестах
    - Существующие описания сохранены
    - `npm run build` и `npm test` exit 0 (финальная wave-4 проверка)
  </done>
</task>

</tasks>

<verification>
  - После всех трёх задач:
    - `npm run build` → exit 0 (TypeScript-compile всего проекта)
    - `npm test` → exit 0 (vitest-прогон всех тестовых файлов; .skip counted as skipped, не failure)
  - grep-проверки из `<acceptance_criteria>` каждой задачи проходят
  - **Независимая проверка covered REQ-IDs:**
    - **LOOP-04** покрывается через: unified-loop-valid (happy path), unified-loop-missing-exit (D-08.1), unified-loop-duplicate-exit (D-08.2), unified-loop-no-body (D-08.3), D-CL-02 order тест
    - **MIGRATE-01** покрывается через: loop-body.canvas migration-тест, loop-start.canvas migration-тест, обязательные лексемы D-07 (loop-start, loop-end, loop, «выход»)
    - **MIGRATE-02** НЕ требует отдельного теста (покрыт mentally: migration-error возвращается через `errors[]` массив, который RunnerView error panel рендерит без изменений — см. Plan 05 must_haves)
  - **Mental walkthrough:**
    1. Разработчик случайно откроет legacy `loop-body.canvas` → parser строит LoopStartNode+LoopEndNode (legacy parseable per D-06) → validator Migration Check → one migration-error → errors[] → RunnerView error panel. Тест MIGRATE-01 подтверждает.
    2. Разработчик создаст unified loop без ребра «выход» → parser строит LoopNode → validator LOOP-04 D-08.1 → «не имеет ребра «выход»». Тест подтверждает.
    3. Разработчик создаст валидный unified loop с back-edge → parser строит LoopNode → detectUnintentionalCycles видит cycle through kind==='loop' → НЕ флагает. Тест D-09 подтверждает.
</verification>

<success_criteria>
  - `grep -q "describe\.skip('loop support (LOOP-01 through LOOP-05, RUN-09)'" src/__tests__/runner/protocol-runner.test.ts` → exit 0
  - `grep -q "describe\.skip('loop-start missing continue edge (RUN-08)'" src/__tests__/runner/protocol-runner.test.ts` → exit 0
  - `grep -q "describe\.skip('Loop context stack survives session round-trip (SESSION-05)'" src/__tests__/runner/protocol-runner-session.test.ts` → exit 0
  - `! grep "loopStartId" src/__tests__/session-service.test.ts` → exit 0
  - `grep -q "gracefully returns missing legacy loop node ID without throwing (D-20, D-13)" src/__tests__/session-service.test.ts` → exit 0
  - `! grep "detects orphaned loop-end node" src/__tests__/graph-validator.test.ts` → exit 0
  - `grep -q "describe('GraphValidator — Phase 43: unified loop + migration (LOOP-04, MIGRATE-01)'" src/__tests__/graph-validator.test.ts` → exit 0
  - `grep -q "parseFixture('unified-loop-valid.canvas')" src/__tests__/graph-validator.test.ts` → exit 0
  - `npm run build` → exit 0
  - `npm test` → exit 0
  - Commit содержит ровно 4 файла: `src/__tests__/graph-validator.test.ts`, `src/__tests__/session-service.test.ts`, `src/__tests__/runner/protocol-runner.test.ts`, `src/__tests__/runner/protocol-runner-session.test.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-07-SUMMARY.md`
</output>
