---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/canvas/node-color-map.ts
  - src/sessions/session-model.ts
  - src/sessions/session-service.ts
autonomous: true
requirements:
  - LOOP-01
user_setup: []

must_haves:
  truths:
    - "NODE_COLOR_MAP содержит 'loop': '1' (красный)"
    - "Ключи 'loop-start' и 'loop-end' удалены из NODE_COLOR_MAP"
    - "PersistedLoopContext использует поле loopNodeId (не loopStartId)"
    - "validateSessionNodeIds читает frame.loopNodeId (не frame.loopStartId)"
    - "Сессия со старой формой (loopStartId) gracefully теряется через validateSessionNodeIds → missing-ids → RunnerView.sessionService.clear() (Option B из PATTERNS.md)"
  artifacts:
    - path: "src/canvas/node-color-map.ts"
      provides: "NODE_COLOR_MAP с ключом 'loop'"
      contains: "'loop':"
    - path: "src/sessions/session-model.ts"
      provides: "PersistedLoopContext с loopNodeId"
      contains: "loopNodeId: string"
    - path: "src/sessions/session-service.ts"
      provides: "validateSessionNodeIds с frame.loopNodeId доступом"
      contains: "frame.loopNodeId"
  key_links:
    - from: "src/canvas/node-color-map.ts"
      to: "src/graph/graph-model.ts"
      via: "Record<RPNodeKind, string> exhaustiveness"
      pattern: "Record<RPNodeKind"
    - from: "src/sessions/session-service.ts"
      to: "src/sessions/session-model.ts"
      via: "PersistedLoopContext shape"
      pattern: "frame\\.loopNodeId"
---

<objective>
Закрыть exhaustiveness-требования, которые форсит `RPNodeKind` union (Plan 01) в двух структурно-зависимых файлах:

1. **`node-color-map.ts`** — `Record<RPNodeKind, string>` тип заставляет TS требовать ключ `'loop'` и запрещает ключи `'loop-start'` / `'loop-end'` если они убраны из union. Но Plan 01 оставил legacy kinds в union (D-CL-05 вариант b) — значит D-12 требует УДАЛИТЬ ключи `'loop-start'` / `'loop-end'` из map и ДОБАВИТЬ `'loop'`, после чего Record станет `Partial<Record<...>>`? Нет — читать D-12 внимательно: map должен быть полный (Record), поэтому сохраняем ключи для legacy kinds тоже ИЛИ меняем тип map на `Partial`. См. `<action>` ниже — корректный выбор D-12 vs реальность.

2. **`session-model.ts` + `session-service.ts`** — переименование `loopStartId` → `loopNodeId` (D-04 + D-13) в PersistedLoopContext и во всех use-sites в validateSessionNodeIds.

Purpose: Плюс исправление reader'а, который иначе крашится при попытке прочитать `frame.loopStartId` — поля больше нет (TS-ошибка). Session layer ломается автоматически от Plan 01 изменения LoopContext.

Output:
- `src/canvas/node-color-map.ts` с `'loop': '1'` и (стратегия из D-12 + реальность union'а) решением для legacy ключей
- `src/sessions/session-model.ts` с `loopNodeId` вместо `loopStartId` в PersistedLoopContext
- `src/sessions/session-service.ts` с `frame.loopNodeId` вместо `frame.loopStartId` во всех местах
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@src/graph/graph-model.ts
@src/canvas/node-color-map.ts
@src/sessions/session-model.ts
@src/sessions/session-service.ts

<interfaces>
```ts
// Current NODE_COLOR_MAP (node-color-map.ts:12-21):
export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
  'start':           '4',
  'question':        '5',
  'answer':          '2',
  'free-text-input': '2',
  'text-block':      '3',
  'loop-start':      '1',
  'loop-end':        '1',
  'snippet':         '6',
};

// Current PersistedLoopContext (session-model.ts:9-13):
export interface PersistedLoopContext {
  loopStartId: string;
  iteration: number;
  textBeforeLoop: string;
}

// Current validateSessionNodeIds use-sites (session-service.ts:~133-141):
for (const frame of entry.loopContextStack) {
  if (!graph.nodes.has(frame.loopStartId)) {
    missing.push(frame.loopStartId);
  }
}
for (const frame of session.loopContextStack) {
  if (!graph.nodes.has(frame.loopStartId)) {
    missing.push(frame.loopStartId);
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить NODE_COLOR_MAP — добавить 'loop', сохранить legacy ключи (Plan 01 оставил их в union)</name>

  <read_first>
    - `src/canvas/node-color-map.ts` (весь файл — 21 строка, прочитать целиком)
    - `src/graph/graph-model.ts` (после Plan 01 — проверить что `RPNodeKind` содержит 'loop-start', 'loop-end', 'loop')
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-12
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/canvas/node-color-map.ts` (config, static Record)»
  </read_first>

  <objective>
    Добавить `'loop': '1'` (red — тот же цвет, что был у loop-start/loop-end). Поскольку Plan 01 сохранил `'loop-start'`/`'loop-end'` в `RPNodeKind` union (D-CL-05 вариант b), `Record<RPNodeKind, string>` требует их в map'е — оставляем legacy entries c комментарием-пометкой `@deprecated`. CONTEXT.md D-12 просил «удалить ключи loop-start/loop-end» — но этот decision был написан в предположении что union не содержит legacy kinds; реальность (Plan 01) иная, и TS-compile форсит сохранение записей. Это коррекция по реальности типа, не отступление от D-12.
  </objective>

  <action>
  В `src/canvas/node-color-map.ts` внести изменения:

  **1. В Record<RPNodeKind, string>** добавить новый ключ `'loop': '1'`. Legacy ключи `'loop-start'` / `'loop-end'` ОСТАВИТЬ (TS требует — они всё ещё в union после Plan 01) с обновлённым комментарием. Итоговый вид:

  ```ts
  export const NODE_COLOR_MAP: Record<RPNodeKind, string> = {
    'start':           '4',  // green  — entry point ("go" semantics)
    'question':        '5',  // cyan   — information gathering
    'answer':          '2',  // orange — action / selection
    'free-text-input': '2',  // orange — user input action (same family as answer)
    'text-block':      '3',  // yellow — passive content
    'loop-start':      '1',  // red    — @deprecated Phase 43 D-CL-05 (legacy, см. MIGRATE-01)
    'loop-end':        '1',  // red    — @deprecated Phase 43 D-CL-05 (legacy)
    'snippet':         '6',  // purple — snippet node (Phase 29, D-11)
    'loop':            '1',  // red    — Phase 43 D-12 unified loop (LOOP-01, LOOP-06 scaffolding)
  };
  ```

  **ПРИМЕЧАНИЕ:** Не удалять существующие комментарии `D-01`, `D-11`. Новая запись `'loop'` получает свой собственный phase-marker `Phase 43 D-12`.

  **НЕ ТРОГАТЬ:** любые другие части файла (импорт, export-имя, любые существующие комментарии вне указанных строк).
  </action>

  <acceptance_criteria>
    - Новый ключ присутствует:
      ```bash
      grep -Pq "^\s*'loop':\s*'1'," src/canvas/node-color-map.ts
      grep -q "Phase 43 D-12" src/canvas/node-color-map.ts
      ```
    - Legacy ключи сохранены с `@deprecated` пометкой:
      ```bash
      grep -Pq "^\s*'loop-start':\s*'1'" src/canvas/node-color-map.ts
      grep -Pq "^\s*'loop-end':\s*'1'" src/canvas/node-color-map.ts
      grep -q "@deprecated Phase 43" src/canvas/node-color-map.ts
      ```
    - Существующие комментарии `D-01` / `D-11` / `Phase 29` не удалены:
      ```bash
      grep -q "Phase 29, D-11" src/canvas/node-color-map.ts
      ```
    - TypeScript compile проходит для этого файла (Record exhaustiveness соблюдён):
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "node-color-map.ts")
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      grep -Pq "^\s*'loop':\s*'1'," src/canvas/node-color-map.ts \
        && grep -q "Phase 43 D-12" src/canvas/node-color-map.ts \
        && grep -Pq "^\s*'loop-start':" src/canvas/node-color-map.ts \
        && grep -Pq "^\s*'loop-end':" src/canvas/node-color-map.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "node-color-map.ts")
    </automated>
  </verify>

  <done>
    - `'loop': '1'` добавлен в NODE_COLOR_MAP
    - Legacy ключи сохранены (TS-exhaustiveness)
    - `node-color-map.ts` компилируется без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 2: Переименовать PersistedLoopContext.loopStartId → loopNodeId</name>

  <read_first>
    - `src/sessions/session-model.ts` (весь файл)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-04 + D-13
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/sessions/session-model.ts` (model)»
  </read_first>

  <objective>
    Выровнять persisted session shape с runtime LoopContext (который Plan 01 уже переименовал на `loopNodeId`). Break-compat принят в D-13 — старые сессии теряются через existing graceful-reject path (validateSessionNodeIds → missing-ids → sessionService.clear()).
  </objective>

  <action>
  В `src/sessions/session-model.ts` найти `PersistedLoopContext` (строки ~9-13) и переименовать поле `loopStartId` → `loopNodeId`. JSDoc комментарий обновить:

  ```ts
  /**
   * Persisted frame of a loop context — serialized to session JSON.
   * Phase 43 D-04 / D-13: field renamed from loopStartId to loopNodeId alongside
   * unified loop node (LOOP-01). Break-compat: old sessions with loopStartId
   * gracefully rejected through validateSessionNodeIds → missing IDs → clear().
   */
  export interface PersistedLoopContext {
    loopNodeId: string;
    iteration: number;
    textBeforeLoop: string;
  }
  ```

  **НЕ ТРОГАТЬ:** любые другие interface'ы / типы в этом файле (`PersistedSession`, `PersistedUndoEntry` — последний содержит `loopContextStack: PersistedLoopContext[]`, который через вложенный array получит новое поле транзитивно).
  </action>

  <acceptance_criteria>
    - Поле переименовано:
      ```bash
      grep -Pzo "export interface PersistedLoopContext \{[^}]*\}" src/sessions/session-model.ts | grep -q "loopNodeId: string"
      ! grep -Pzo "export interface PersistedLoopContext \{[^}]*loopStartId[^}]*\}" src/sessions/session-model.ts
      ```
    - Phase marker:
      ```bash
      grep -q "Phase 43 D-04" src/sessions/session-model.ts
      ```
    - File компилируется (после Plan 01):
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "session-model.ts")
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      grep -Pzo "export interface PersistedLoopContext \{[^}]*\}" src/sessions/session-model.ts | grep -q "loopNodeId: string" \
        && ! grep -Pzo "export interface PersistedLoopContext \{[^}]*loopStartId[^}]*\}" src/sessions/session-model.ts \
        && grep -q "Phase 43 D-04" src/sessions/session-model.ts
    </automated>
  </verify>

  <done>
    - `PersistedLoopContext.loopStartId` переименовано в `loopNodeId`
    - Phase-marker `Phase 43 D-04` на месте
    - Другие типы в `session-model.ts` неизменны
  </done>
</task>

<task type="auto">
  <name>Task 3: Обновить validateSessionNodeIds — frame.loopStartId → frame.loopNodeId во всех use-sites</name>

  <read_first>
    - `src/sessions/session-service.ts` (особенно функция `validateSessionNodeIds`, строки ~116-148)
    - `src/sessions/session-model.ts` (после правок Task 2 — подтвердить новое поле)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секция D-13
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/sessions/session-service.ts`»
    - `CLAUDE.md` правило never-remove — `save`, `clear`, `hasSession`, `sessionFilePath`, `load`, `ensureFolderPath`, mutex-usage — НЕ трогать
  </read_first>

  <objective>
    Выровнять session-service reader с новым shape модели. Option B из PATTERNS.md — использовать существующий validateSessionNodeIds → missing flow для graceful reject старых сессий. Никаких новых проверок в `load()` не нужно (тест в Plan 07 D-20 проверит что legacy loopNodeId-less сессия → JSON.parse даёт undefined → validateSessionNodeIds вернёт missing).
  </objective>

  <action>
  В `src/sessions/session-service.ts` найти функцию `validateSessionNodeIds` (обычно в конце файла, строки ~116-148) и заменить ВСЕ обращения `frame.loopStartId` на `frame.loopNodeId`. Типично это 2-3 call-site:

  1. Внутри цикла по `session.undoStack[*].loopContextStack`:
     ```ts
     for (const frame of entry.loopContextStack) {
       if (!graph.nodes.has(frame.loopNodeId)) {        // было: frame.loopStartId
         missing.push(frame.loopNodeId);                // было: frame.loopStartId
       }
     }
     ```

  2. Внутри цикла по `session.loopContextStack`:
     ```ts
     for (const frame of session.loopContextStack) {
       if (!graph.nodes.has(frame.loopNodeId)) {        // было: frame.loopStartId
         missing.push(frame.loopNodeId);                // было: frame.loopStartId
       }
     }
     ```

  Добавить phase-marker комментарий в начале функции или возле изменений:

  ```ts
  // Phase 43 D-04 / D-13: loopStartId → loopNodeId (unified loop, LOOP-01).
  // Old sessions referencing removed legacy loop IDs surface here as missing;
  // RunnerView.sessionService.clear() handles graceful reset.
  ```

  **НЕ ТРОГАТЬ:**
  - `async load(...)` / `async save(...)` / `async clear(...)` / `async hasSession(...)` / `sessionFilePath(...)` — не модифицировать
  - `mutex`, `ensureFolderPath`, `vault.adapter.*` использование — не трогать
  - проверку `session.currentNodeId` (верх функции) — не трогать
  - проверку `entry.nodeId` в undo-loop — не трогать
  - dedup через `filter((id, idx) => missing.indexOf(id) === idx)` (SESSION-07) — не трогать

  **TypeScript compile** после этой правки: `session-service.ts` должен стать зелёным (Plan 01+Plan 03 Task 2 уже переименовали поле в модели; здесь — последний reader, который ломался).
  </action>

  <acceptance_criteria>
    - Все `frame.loopStartId` в validateSessionNodeIds заменены:
      ```bash
      ! grep -Pzo "function validateSessionNodeIds[^}]*(?:\{[^}]*\})*[^}]*\}" src/sessions/session-service.ts | grep "frame\.loopStartId"
      grep -q "frame\.loopNodeId" src/sessions/session-service.ts
      ```
    - Phase-marker добавлен:
      ```bash
      grep -q "Phase 43 D-04" src/sessions/session-service.ts
      ```
    - Файл компилируется без ошибок:
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "session-service.ts")
      ```
    - Существующие async-методы не модифицированы:
      ```bash
      grep -q "async load(" src/sessions/session-service.ts
      grep -q "async save(" src/sessions/session-service.ts
      grep -q "async clear(" src/sessions/session-service.ts
      grep -q "async hasSession(" src/sessions/session-service.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      ! grep "frame\.loopStartId" src/sessions/session-service.ts \
        && grep -q "frame\.loopNodeId" src/sessions/session-service.ts \
        && grep -q "Phase 43 D-04" src/sessions/session-service.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "session-service.ts")
    </automated>
  </verify>

  <done>
    - Все `frame.loopStartId` → `frame.loopNodeId` в validateSessionNodeIds
    - Phase-marker добавлен
    - `session-service.ts` компилируется без ошибок
    - async-методы нетронуты
  </done>
</task>

</tasks>

<verification>
  - После всех трёх задач: `npx tsc --noEmit --skipLibCheck` — для файлов этого плана (`node-color-map.ts`, `session-model.ts`, `session-service.ts`) ошибок нет
  - Существующие vitest-тесты `session-service.test.ts` ДОЛЖНЫ упасть в компиляции на строках 137, 195, 197 (inline loopStartId literals) — это expected, будет починено в Plan 07 Task 1 (D-18 cleanup). В этом plan'е нас интересует только source-compile.
  - Интеграционный build `npm run build` ВСЁ ЕЩЁ МОЖЕТ падать — остаются downstream plans 04 (runner), 05 (validator). Это нормально для wave 2.
</verification>

<success_criteria>
  - `grep -q "'loop':" src/canvas/node-color-map.ts` → exit 0
  - `grep -q "loopNodeId: string" src/sessions/session-model.ts` → exit 0
  - `! grep "frame\.loopStartId" src/sessions/session-service.ts` → exit 0
  - Для трёх файлов плана `tsc --noEmit --skipLibCheck` не выдаёт ошибок
  - Commit содержит ровно три файла: `src/canvas/node-color-map.ts`, `src/sessions/session-model.ts`, `src/sessions/session-service.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-03-SUMMARY.md`
</output>
