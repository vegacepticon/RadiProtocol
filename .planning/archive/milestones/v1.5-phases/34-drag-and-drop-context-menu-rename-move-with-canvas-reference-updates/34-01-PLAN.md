---
phase: 34
plan: 01
type: execute
wave: 2
depends_on: [34-00]
files_modified:
  - src/views/folder-picker-modal.ts
  - src/views/snippet-manager-view.ts
  - src/__tests__/snippet-tree-dnd.test.ts
autonomous: true
requirements: [MOVE-03, MOVE-05, RENAME-03]
must_haves:
  truths:
    - "Context menu on file or folder shows «Переместить в…» item"
    - "Selecting «Переместить в…» opens FolderPickerModal listing all snippet folders"
    - "Choosing a folder calls moveSnippet (file) or moveFolder (folder)"
    - "After folder move, rewriteCanvasRefs is called with snippet-root-relative keys (D-03)"
    - "FolderPickerModal excludes the source folder and its descendants when moving a folder"
  artifacts:
    - path: "src/views/folder-picker-modal.ts"
      provides: "SuggestModal<string> listing all snippet folders"
      contains: "class FolderPickerModal"
    - path: "src/views/snippet-manager-view.ts"
      provides: "Переместить в… menu item + openMovePicker handler"
      contains: "Переместить в"
  key_links:
    - from: "snippet-manager-view.ts openMovePicker"
      to: "FolderPickerModal"
      via: "new FolderPickerModal(app, folders, onChoose).open()"
      pattern: "new FolderPickerModal"
    - from: "openMovePicker handler"
      to: "snippetService.moveFolder"
      via: "await call after user selection"
      pattern: "moveFolder\\("
    - from: "openMovePicker handler (folder branch)"
      to: "rewriteCanvasRefs"
      via: "toCanvasKey(old/new, snippetRoot)"
      pattern: "rewriteCanvasRefs"
---

<objective>
Реализовать «Переместить в…» context-menu flow: новый `FolderPickerModal`, меню-пункты для файлов и папок в дереве, и orchestrator, который вызывает service + canvas-ref-sync для folder case.

Purpose: Даёт пользователю дискретный способ переместить сниппет без drag-gesture (accessibility + точность). Не зависит от DnD из Plan 02 — оба потребляют один и тот же service API.

Output:
- `src/views/folder-picker-modal.ts` (NEW, ~50 LOC)
- `openMovePicker` метод + два menu item в `snippet-manager-view.ts`
- Реальные тесты в `snippet-tree-dnd.test.ts` под describe('context menu Move to…')
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-CONTEXT.md
@.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-PATTERNS.md
@src/views/snippet-manager-view.ts
@src/views/node-picker-modal.ts
@src/snippets/canvas-ref-sync.ts
@CLAUDE.md

<interfaces>
From Plan 00:
```typescript
class SnippetService {
  async moveSnippet(oldPath: string, newFolder: string): Promise<string>;
  async moveFolder(oldPath: string, newParent: string): Promise<string>;
  async listAllFolders(): Promise<string[]>;
}
export function toCanvasKey(vaultPath: string, snippetRoot: string): string;
```

From src/views/node-picker-modal.ts (template to copy):
```typescript
import { App, SuggestModal } from 'obsidian';
export class NodePickerModal extends SuggestModal<NodeOption> { /* ~50 LOC */ }
```

From src/snippets/canvas-ref-sync.ts (unchanged):
```typescript
rewriteCanvasRefs(app, mapping: Map<string, string>): Promise<CanvasSyncResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FolderPickerModal</name>
  <files>src/views/folder-picker-modal.ts</files>
  <behavior>
    - Extends `SuggestModal<string>` from obsidian
    - Constructor: `(app, folders: string[], onChoose: (folder) => void)`
    - getSuggestions: empty query → all folders; non-empty → case-insensitive substring filter
    - renderSuggestion: creates `<div>` with the folder path
    - onChooseSuggestion: invokes onChoose callback
    - Placeholder: «Выберите папку…»
  </behavior>
  <action>
    Create `src/views/folder-picker-modal.ts` using the template from 34-PATTERNS.md lines 228-256 verbatim. Import from `obsidian`. Export class `FolderPickerModal`. No extra logic — this modal is a dumb picker; exclusion of invalid targets happens in the caller before passing `folders`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    - File exists, compiles, exports FolderPickerModal
    - No tests yet (picker is trivial; exercised indirectly via Task 2 tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add «Переместить в…» menu items and openMovePicker orchestrator</name>
  <files>src/views/snippet-manager-view.ts, src/__tests__/snippet-tree-dnd.test.ts</files>
  <behavior>
    - `openContextMenu` for file node gets a new «Переместить в…» item (between «Редактировать» and «Удалить»)
    - `openContextMenu` for folder node gets a new «Переместить в…» item (between «Создать подпапку» and «Удалить папку»)
    - Selecting the menu item calls `openMovePicker(node)`
    - `openMovePicker` method:
      - builds folder list via `plugin.snippetService.listAllFolders()`
      - for FILE node: excludes current parent folder (no-op destination)
      - for FOLDER node: excludes self AND all paths starting with `node.path + '/'`
      - opens FolderPickerModal; on selection → try/catch:
        - FILE: `await snippetService.moveSnippet(node.path, chosen)` — NO canvas-ref-sync (D-03 следствие 2 — files are canvas-invisible)
        - FOLDER:
          1. `const oldPath = node.path; const newPath = await snippetService.moveFolder(oldPath, chosen);`
          2. `const snippetRoot = plugin.settings.snippetFolderPath;`
          3. `const mapping = new Map([[toCanvasKey(oldPath, snippetRoot), toCanvasKey(newPath, snippetRoot)]]);`
          4. `const result = await rewriteCanvasRefs(this.app, mapping);`
          5. Notice: `'Папка перемещена. Обновлено канвасов: ' + result.updated.length + ', пропущено: ' + result.skipped.length + '.'`
          6. Expand-state rewrite: walk `settings.snippetTreeExpandedPaths`, replace entries where entry === oldPath OR entry.startsWith(oldPath + '/') → new prefix. Save settings.
      - catch: `new Notice('Не удалось переместить: ' + (e as Error).message)`; log to console.error
    - Real tests replace `it.todo` for `context menu Move to…` describe block.
  </behavior>
  <action>
    1. Read `src/views/snippet-manager-view.ts` — locate `openContextMenu` (lines 327-365 per patterns) and existing imports.
    2. Add imports at top (join the existing obsidian import list; DO NOT remove existing imports):
       ```typescript
       import { FolderPickerModal } from './folder-picker-modal';
       import { rewriteCanvasRefs } from '../snippets/canvas-ref-sync';
       import { toCanvasKey } from '../snippets/snippet-service';
       ```
    3. In the file branch of `openContextMenu`, add the «Переместить в…» `menu.addItem` BETWEEN the existing Редактировать and Удалить items. Use icon `'folder-input'`. onClick → `void this.openMovePicker(node)`.
    4. In the folder branch, add the same item BETWEEN «Создать подпапку» and «Удалить папку».
    5. Add private async method `openMovePicker(node: TreeNode)` implementing the orchestration from `<behavior>`. Use existing error-handling pattern (try/catch + Notice in Russian).
    6. **CLAUDE.md rule:** do NOT remove any existing handler, import, or method. Every edit is additive. If a line exists that is not from Phase 34, do not touch it.
    7. Update `src/__tests__/snippet-tree-dnd.test.ts`: replace the two `it.todo` under `describe('context menu Move to…')` with real tests:
       - Test 1: `openMovePicker` on file node — mock `snippetService.moveSnippet` and assert it's called with correct args when picker onChoose fires; assert `rewriteCanvasRefs` NOT called.
       - Test 2: `openMovePicker` on folder node — assert `moveFolder` called, then `rewriteCanvasRefs` called with `Map([['a', 'b/a']])` (or similar snippet-root-relative keys).
       - Test 3: folder exclusion — assert that the folders array passed to FolderPickerModal excludes the source folder and its descendants.
       - Test 4: expand-state prefix rewrite — assert settings.snippetTreeExpandedPaths is rewritten after folder move.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/snippet-tree-dnd.test.ts -t "context menu" --reporter=default</automated>
  </verify>
  <done>
    - Two new menu items present in both branches of openContextMenu
    - openMovePicker method implemented
    - Canvas-ref-sync called ONLY on folder move
    - Expand-state prefix-rewritten after folder move
    - Zero `it.todo` remain under `describe('context menu Move to…')`
    - `npx vitest run src/__tests__/snippet-tree-dnd.test.ts -t "context menu"` green
    - No existing code removed from snippet-manager-view.ts (git diff additive only in touched sections)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/snippet-tree-dnd.test.ts -t "context menu"` green
- `npx tsc --noEmit` clean
- Visual: context menu shows «Переместить в…» on both file and folder rows (verified in Plan 05 human UAT)
</verification>

<success_criteria>
Advances Phase 34 Success Criterion #2 (context menu «Move to…» + picker) and #4 (canvas-ref sync fan-out on folder move, first production caller of `rewriteCanvasRefs` with correct key format per D-03).
</success_criteria>

<output>
Create `.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-01-SUMMARY.md`
</output>
