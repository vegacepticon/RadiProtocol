---
phase: 34
plan: 05
type: execute
wave: 5
depends_on: [34-00, 34-01, 34-02, 34-03, 34-04]
files_modified:
  - src/__tests__/snippet-tree-dnd.test.ts
  - src/__tests__/snippet-tree-inline-rename.test.ts
  - .planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-UAT.md
autonomous: false
requirements: [MOVE-01, MOVE-02, MOVE-03, MOVE-04, MOVE-05, RENAME-01, RENAME-02, RENAME-03]
must_haves:
  truths:
    - "Zero `it.todo` remain in any Phase 34 test file"
    - "`npm test` full suite green"
    - "`npm run build` clean"
    - "`npx tsc --noEmit` clean"
    - "Human operator completes UAT checklist in a real Obsidian vault"
    - "All five Phase 34 Success Criteria verified"
  artifacts:
    - path: "34-UAT.md"
      provides: "Human verification checklist (25-35 steps) covering every requirement"
      contains: "F2"
  key_links:
    - from: "UAT"
      to: "Phase 34 roadmap Success Criteria 1-5"
      via: "one UAT step per criterion (and then some)"
      pattern: "Success Criterion"
---

<objective>
Финальная проверка Phase 34: все автоматические тесты зелёные, сборка чистая, zero `it.todo`, и human operator выполняет UAT-чеклист в реальном Obsidian vault с настоящими `.canvas` файлами.

Purpose: Закрывает Success Criterion #5 (survives Obsidian reload), проверяет Criterion #4 end-to-end (canvas ссылки разрешаются в runner после move/rename), и ловит всё, что headless DOM-тесты пропускают (drag image, native cursor, recursive folder rename на большом subtree).

Output:
- 34-UAT.md чеклист (коммитится в phase dir)
- Human sign-off в форме checkpoint:human-verify
- Все предыдущие планы подтверждены зелёными
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-CONTEXT.md
@.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-VALIDATION.md
@.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-05-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test audit — zero it.todo, full suite + build gates</name>
  <files></files>
  <action>
    1. Run `grep -rn "it\.todo\|describe\.skip\|it\.skip" src/__tests__/snippet-tree-dnd.test.ts src/__tests__/snippet-tree-inline-rename.test.ts src/__tests__/snippet-service-move.test.ts` — MUST return zero matches. If any remain, fail the task and report which upstream plan didn't fill its contract.
    2. Run `npm test` — full vitest suite MUST be green. If not, fail and report the failing spec.
    3. Run `npx tsc --noEmit -p tsconfig.json` — MUST be clean.
    4. Run `npm run build` — MUST complete without errors. Confirm `styles.css` regenerated with Phase 34 rules.
    5. Verify no raw `styles.css` edits: `git diff styles.css` should only reflect automated esbuild output (the planner's CSS changes were all in `src/styles/snippet-manager.css`).
    6. Verify CLAUDE.md compliance: `git diff src/views/snippet-manager-view.ts src/snippets/snippet-service.ts src/styles/snippet-manager.css` — every hunk must be additive OR a legitimate Phase-34 refactor (e.g., the `performMove` extraction in Plan 02). No Phase 33 code silently deleted.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - All three commands exit 0
    - Zero `it.todo` in Phase 34 test files
    - `git diff` shows no unintended deletions in shared files
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 34-UAT.md human verification checklist</name>
  <files>.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-UAT.md</files>
  <action>
    Create `34-UAT.md` following the structure of `33-UAT.md` (or `33-05-PLAN.md` if UAT lives there). Required sections and steps:

    **## Preflight (3 steps)**
    - Build dev: `npm run dev` running, plugin reloaded in Obsidian
    - Test vault has ≥ 3 nested snippet folders and ≥ 2 `.canvas` files referencing snippets at different depths
    - Note: take a git-snapshot / backup of the vault snippet folder before UAT

    **## Part A — Drag-and-Drop (6 steps, MOVE-01/02)**
    1. Drag a `.json` snippet file onto another folder in the tree — file appears under new folder, disappears from old. Reload Obsidian — still there.
    2. Drag a `.md` snippet onto another folder — same.
    3. Drag a folder with 2+ nested snippets onto a different top-level folder — whole subtree moves.
    4. Try dragging a folder onto itself — rejected (red outline, no-op).
    5. Try dragging a folder onto one of its own descendants — rejected.
    6. Drag a file from Obsidian's own file explorer over a snippet row — our drop handler ignores it (no `drop-target` class appears).

    **## Part B — Context Menu «Move to…» (4 steps, MOVE-03)**
    7. Right-click a file → «Переместить в…» → picker opens with all folders; select one → file moved.
    8. Right-click a folder → «Переместить в…» → picker opens WITHOUT the source folder or its descendants in the list.
    9. Start typing in the picker — fuzzy filter narrows the list.
    10. Press Escape in picker — no move happens.

    **## Part C — Modal «Папка» field (2 steps, MOVE-04)**
    11. Double-click (or «Редактировать») a snippet → change «Папка» dropdown to another folder → Save. Notice reads «Сниппет перемещён.» — **no** «Обновлено канвасов» suffix.
    12. Edit same snippet, change only content (not folder) → Save — no move, no "перемещён" notice.

    **## Part D — Inline rename (8 steps, RENAME-01/02)**
    13. Click a file row to focus it, press F2 → input appears with basename (no extension) selected. Type new name, Enter → file renamed.
    14. F2 on a file row, press Escape → cancelled, label restored.
    15. F2, type new name, click elsewhere (blur) → committed.
    16. F2, type new name, press Enter → commit once (not twice from Enter+blur race).
    17. Context menu «Переименовать» on a file → same flow works.
    18. F2 on a folder row → rename folder.
    19. Context menu «Переименовать» on a folder → works.
    20. Rename to an existing sibling name → collision error Notice in Russian, source unchanged.

    **## Part E — Canvas reference sync (6 steps, MOVE-05, RENAME-03)**
    21. Create a `.canvas` with a SnippetNode whose `radiprotocol_subfolderPath` points to `folder-a/subfolder`. Close canvas.
    22. Rename `folder-a` to `folder-x` via F2. Reopen canvas — SnippetNode still resolves; open the runner on that canvas → snippet picker finds the snippets.
    23. Move `folder-x/subfolder` into `folder-b` via drag-and-drop. Reopen canvas — still resolves.
    24. Deeply nested case: rename a grandparent folder → all descendants' canvas refs still work.
    25. File-only rename: rename a `.json` file — canvas does NOT change (expected: canvases reference folders, not files).
    26. File-only move: move a file between folders via DnD — canvas does NOT change (same reason).

    **## Part F — Persistence + edge cases (5 steps, Success Criterion #5)**
    27. Perform a mix of moves and renames; reload Obsidian fully (Ctrl+R or quit+reopen).
    28. Verify tree state survives: expand-state for renamed/moved folders preserved.
    29. Verify snippet content byte-for-byte: open one moved file, content identical to pre-move.
    30. Move a folder while it's expanded in the tree — after move, the folder appears expanded in its new location.
    31. Rapidly drag-move multiple files in succession (≥ 5 moves in 5 seconds) — no lost files, no stuck drop-target class, tree redraws correctly.

    **## Sign-off**
    - [ ] All Part A-F steps pass
    - [ ] Success Criterion #1 verified (A1-A6)
    - [ ] Success Criterion #2 verified (B7-B10, C11-C12)
    - [ ] Success Criterion #3 verified (D13-D20)
    - [ ] Success Criterion #4 verified (E21-E26)
    - [ ] Success Criterion #5 verified (F27-F31)
    - [ ] Signed: _______ Date: _______
  </action>
  <verify>Manual file review; file exists with all sections.</verify>
  <done>
    - 34-UAT.md exists with ≥ 31 concrete UAT steps
    - Every Success Criterion traced to UAT steps
    - Every Req ID (MOVE-01..05, RENAME-01..03) covered by at least one step
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT in real Obsidian vault</name>
  <what-built>
    Full Phase 34 feature set: drag-and-drop for snippet files and folders, «Переместить в…» context menu with folder picker, F2 inline rename for files and folders, and automatic canvas-ref-sync on every folder rename/move. Placebo rewriteCanvasRefs call from Phase 33 modal has been removed.
  </what-built>
  <how-to-verify>
    1. `npm run dev` in the repo; reload plugin in your real Obsidian vault.
    2. Open `.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-UAT.md`.
    3. Work through each step sequentially. Tick each box as it passes.
    4. For any failure: note the step number, describe the failure, and reply with the details. Do NOT sign off if any step fails.
    5. On successful completion, reply with «approved» and record the date in the Sign-off section of 34-UAT.md.
  </how-to-verify>
  <resume-signal>Reply «approved» or describe failures step-by-step.</resume-signal>
</task>

</tasks>

<verification>
- Task 1: `npm test && npx tsc --noEmit && npm run build` all green
- Task 2: 34-UAT.md exists and covers all 5 success criteria + all 8 req IDs
- Task 3: Human approval recorded
</verification>

<success_criteria>
Closes ALL five Phase 34 Success Criteria:
1. ✅ DnD file and folder moves — Plan 02 + UAT Part A
2. ✅ Context menu «Move to…» + modal «Папка» — Plan 01 + Plan 04 + UAT Parts B, C
3. ✅ F2 inline rename for files and folders — Plan 03 + UAT Part D
4. ✅ Canvas-ref sync on folder rename/move — Plans 01/02/03 + UAT Part E
5. ✅ Persistence across reload — UAT Part F
</success_criteria>

<output>
Create `.planning/phases/34-drag-and-drop-context-menu-rename-move-with-canvas-reference-updates/34-05-SUMMARY.md`
</output>
