---
phase: 35
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/fixtures/snippets/phase-35/plain.md
  - src/__tests__/fixtures/snippets/phase-35/empty.md
  - src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md
  - src/__tests__/runner-extensions.test.ts
autonomous: true
requirements:
  - MD-01
  - MD-02
  - MD-03
  - MD-04
must_haves:
  truths:
    - "MD-сниппеты появляются в picker рядом с JSON со своим префиксом"
    - "Клик по MD-row вставляет content верbatim без fill-in модалки"
    - "Drill-down в подпапку показывает вложенные MD-файлы"
    - "Mixed answer+snippet branching маршрутизирует в MD-ветку без ошибок"
    - "Step back после MD-вставки откатывает accumulatedText и currentNodeId"
    - "Пустой MD-файл — валидный пик (completeSnippet(''))"
    - "Session save/resume сохраняет вставленный MD-контент через accumulatedText"
  artifacts:
    - path: "src/__tests__/fixtures/snippets/phase-35/plain.md"
      provides: "Непустой MD-файл для верifikации verbatim insert"
    - path: "src/__tests__/fixtures/snippets/phase-35/empty.md"
      provides: "Empty MD для D-03 (clickable, completeSnippet(''))"
    - path: "src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md"
      provides: "Вложенный MD для drill-down MD-03"
    - path: "src/__tests__/runner-extensions.test.ts"
      provides: "Failing test stubs для MD-01..MD-04, D-03, D-06, session"
      contains: "md picker row, md click completes, md drill-down, mixed branch md, step-back md, empty md, session md resume"
  key_links:
    - from: "runner-extensions.test.ts"
      to: "SnippetService.listFolder mock"
      via: "fake listing возвращающий Snippet[] с kind: 'md'"
      pattern: "kind:\\s*['\"]md['\"]"
    - from: "runner-extensions.test.ts"
      to: "ProtocolRunner.pickSnippet + completeSnippet contract"
      via: "прямой вызов runner API из теста"
      pattern: "pickSnippet\\(.*\\).*completeSnippet\\("
---

<objective>
Wave 0: создать MD-fixtures и failing test stubs для всех требований Phase 35 (MD-01..MD-04) плюс D-03 empty MD, D-06 step-back, SC-05 session round-trip.

Purpose: Nyquist-compliance — каждая задача Plan 02 должна иметь автоматизированный verify. Тесты RED на этом этапе, GREEN после Plan 02.
Output: 3 MD-фикстуры + 7 новых test cases в runner-extensions.test.ts, все RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-CONTEXT.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-RESEARCH.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-PATTERNS.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-VALIDATION.md
@src/snippets/snippet-model.ts
@src/__tests__/runner-extensions.test.ts
@src/views/runner-view.ts
@src/runner/protocol-runner.ts

<interfaces>
From src/snippets/snippet-model.ts:
```typescript
export interface JsonSnippet {
  kind: 'json';
  id?: string;
  name: string;
  path: string;
  template: string;
  placeholders: Placeholder[];
}
export interface MdSnippet {
  kind: 'md';
  name: string;
  path: string;
  content: string;
}
export type Snippet = JsonSnippet | MdSnippet;
```

From src/runner/protocol-runner.ts (contract, DO NOT modify in this phase):
```typescript
pickSnippet(snippetId: string): void;   // undo-before-mutate; no-op if status !== 'awaiting-snippet-pick'
completeSnippet(renderedText: string): void;  // no-op if status !== 'awaiting-snippet-fill'
stepBack(): void;  // pops undoStack → restores accumulatedText + currentNodeId
```

From src/views/runner-view.ts (current JSON-only filter to be removed in Plan 02):
```typescript
// renderSnippetPicker loop (line ~625):
for (const snippet of listing.snippets) {
  if (snippet.kind !== 'json') continue;   // ← removed in Plan 02
  const jsonSnippet = snippet;
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать MD-fixture файлы для тестов picker'а</name>
  <files>
    src/__tests__/fixtures/snippets/phase-35/plain.md
    src/__tests__/fixtures/snippets/phase-35/empty.md
    src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md
  </files>
  <read_first>
    src/__tests__/fixtures/
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-CONTEXT.md
  </read_first>
  <behavior>
    - plain.md содержит непустой multi-line текст для verbatim insert
    - empty.md — ровно 0 байт (для D-03)
    - subfolder/nested.md содержит узнаваемую строку для drill-down теста
  </behavior>
  <action>
Создать ровно 3 файла:

1. `src/__tests__/fixtures/snippets/phase-35/plain.md` с содержимым (ровно эти байты, без BOM):
```
# Protocol note

First paragraph line.
Second paragraph line.
```

2. `src/__tests__/fixtures/snippets/phase-35/empty.md` — пустой файл (0 байт, ничего не писать).

3. `src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md` с содержимым:
```
Nested MD marker: rp-phase-35-nested
```

Эти файлы — fixture data only. Tests НЕ читают их через vault API — они читают через `fs.readFileSync` в test-setup либо инлайнят content в fake `MdSnippet` объект (Patterns § Fixture location — inline OK). Файлы существуют как source-of-truth для human-verify и будущих integration-тестов.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='src/__tests__/fixtures/snippets/phase-35'; if(!fs.existsSync(p+'/plain.md')||!fs.existsSync(p+'/empty.md')||!fs.existsSync(p+'/subfolder/nested.md')){process.exit(1)} if(fs.readFileSync(p+'/empty.md').length!==0){console.error('empty.md not empty');process.exit(1)} if(!fs.readFileSync(p+'/plain.md','utf8').includes('First paragraph')){process.exit(1)} if(!fs.readFileSync(p+'/subfolder/nested.md','utf8').includes('rp-phase-35-nested')){process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/__tests__/fixtures/snippets/phase-35/plain.md` exists
    - `ls src/__tests__/fixtures/snippets/phase-35/empty.md` exists и size = 0
    - `ls src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md` exists
    - `grep "First paragraph" src/__tests__/fixtures/snippets/phase-35/plain.md` matches
    - `grep "rp-phase-35-nested" src/__tests__/fixtures/snippets/phase-35/subfolder/nested.md` matches
  </acceptance_criteria>
  <done>Три fixture-файла созданы с ожидаемым содержимым и размерами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Добавить 7 failing test stubs в runner-extensions.test.ts</name>
  <files>src/__tests__/runner-extensions.test.ts</files>
  <read_first>
    src/__tests__/runner-extensions.test.ts
    src/__tests__/runner/protocol-runner.test.ts
    src/snippets/snippet-model.ts
    src/runner/protocol-runner.ts
    src/views/runner-view.ts
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-PATTERNS.md
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-VALIDATION.md
  </read_first>
  <behavior>
    Новый `describe('Phase 35 — MD snippets in Runner picker', ...)` блок в конце файла, содержащий 7 `it(...)` блоков. Все должны падать пока Plan 02 не реализует изменения.

    Test names (MUST match exact -t grep patterns из 35-VALIDATION.md):
    1. "md picker row" — MD-01: renderSnippetPicker рендерит MD-row с префиксом '📝' рядом с '📄' для JSON. Проверить через fake SnippetService.listFolder возвращающий mixed `Snippet[]`, monтировать RunnerView или напрямую дёрнуть renderSnippetPicker, проверить что DOM содержит кнопки с текстом начинающимся на `📝 ` и `📄 `.
    2. "md click completes" — MD-02: клик по MD-row вызывает runner.pickSnippet(path) → runner.completeSnippet(content) verbatim, без modal. Проверить итоговый `accumulatedText` содержит точное содержимое MD байт-в-байт (с учётом separator prefix). Проверить что SnippetFillInModal НЕ был сконструирован.
    3. "md drill-down" — MD-03: fake listing где root содержит folder "subfolder", а listing(subfolder) содержит md-файл. После клика на "📁 subfolder" picker показывает MD-row с префиксом '📝'.
    4. "mixed branch md" — MD-04: graph-фикстура с question node → SnippetNode (branch-entered picker, Phase 31 flow) → MD-сниппет. После прохождения branch MD content попадает в accumulatedText, runner advances через outgoing edge.
    5. "step-back md" — D-06: после MD-вставки runner.stepBack() откатывает accumulatedText к pre-pick snapshot и currentNodeId на snippet node; status === 'awaiting-snippet-pick'.
    6. "empty md" — D-03: MdSnippet с content='' → pickSnippet + completeSnippet('') → runner advances без ошибок; accumulatedText НЕ содержит отдельного separator для пустой вставки (зеркалит JSON empty template path).
    7. "session md resume" — SC-05: после MD-вставки сериализовать session (SessionService.serialize или Session object), десериализовать, проверить что restored session.accumulatedText содержит MD content байт-в-байт.
  </behavior>
  <action>
1. Прочитать существующий `src/__tests__/runner-extensions.test.ts` целиком для понимания текущих import'ов, fake `SnippetService` pattern, graph-фикстур и test-utils.

2. Прочитать `src/__tests__/runner/protocol-runner.test.ts:601-692` — это канонический образец для `pickSnippet → completeSnippet → stepBack` тестов, копировать его arrange/act/assert структуру.

3. В конец `runner-extensions.test.ts` добавить НОВЫЙ `describe('Phase 35 — MD snippets in Runner picker', () => { ... })` блок. НЕ модифицировать существующие describe-блоки. НЕ трогать 3 pre-existing red-теста (Pitfall 5 из RESEARCH.md).

4. Создать helper-фабрику для MD fixture в начале нового describe:
```typescript
function makeMdSnippet(name: string, content: string, folder = ''): MdSnippet {
  return {
    kind: 'md',
    name,
    path: folder ? `.radiprotocol/snippets/${folder}/${name}.md` : `.radiprotocol/snippets/${name}.md`,
    content,
  };
}
```

5. Создать 7 `it(...)` блоков с точными именами выше. Каждый тест должен:
   - **Arrange:** построить runner/view с fake SnippetService возвращающим смешанный Snippet[] (JSON + MD). Для view-тестов (1, 3) — если существующий helper для RunnerView mount отсутствует, использовать direct renderSnippetPicker вызов или jsdom DOM assertions по pattern из `runner-extensions.test.ts` head.
   - **Act:** вызвать нужное поведение (click, pickSnippet+completeSnippet, stepBack, serialize/deserialize).
   - **Assert:** конкретные expect-ы описанные в `<behavior>`.

6. Для теста 2 (MD-02) — CRITICAL assertion на verbatim: содержимое `plain.md` = `"# Protocol note\n\nFirst paragraph line.\nSecond paragraph line.\n"` — сравнить `accumulatedText.endsWith(plainMdContent)` ИЛИ проверить включение через `toContain`, но БЕЗ trim/normalize. Assertion, что `new SnippetFillInModal` НЕ вызвался — через vi.spyOn на конструктор или assertion что DOM не содержит открытого модального элемента.

7. Для теста 4 (mixed branch md) — использовать существующий graph-builder helper (если есть в test-utils) или скопировать минимальный Phase 31 graph-фикстуру из runner-extensions.test.ts. Branch должен иметь SnippetNode ведущий к MD-сниппету, после `completeSnippet(content)` runner продвигается на outgoing edge.

8. Для теста 7 (session md resume) — использовать существующий SessionService test helper (см. `src/__tests__/session-service.test.ts` для образца сериализации). Если helper отсутствует — прямое `JSON.parse(JSON.stringify(runner.getState()))` достаточно, потому что session сериализуется через `accumulatedText` (см. RESEARCH § Runtime State Inventory).

9. ВСЕ 7 тестов должны падать на текущем коде (`if (snippet.kind !== 'json') continue;` ещё на месте, handler не расширен) — это RED фаза TDD. Wave 0 done criterion = тесты присутствуют и падают с осмысленными ошибками (не compile errors).

10. **Важно:** НЕ удалять и НЕ модифицировать ни одну существующую строку в этом файле. Только append нового describe в конец.
  </action>
  <verify>
    <automated>cd Z:/projects/RadiProtocolObsidian && npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35" --reporter=verbose 2>&1 | tee /tmp/p35-w0.log; grep -c "md picker row\|md click completes\|md drill-down\|mixed branch md\|step-back md\|empty md\|session md resume" /tmp/p35-w0.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe.*Phase 35 — MD snippets" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*md picker row" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*md click completes" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*md drill-down" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*mixed branch md" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*step-back md" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*empty md" src/__tests__/runner-extensions.test.ts` == 1
    - `grep -c "it(.*session md resume" src/__tests__/runner-extensions.test.ts` == 1
    - `npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35"` компилируется (нет TS errors) и все 7 Phase 35 тестов запускаются и падают (RED)
    - НЕ тронут ни один существующий `describe` / `it` блок в файле (git diff показывает только append в конце)
  </acceptance_criteria>
  <done>7 failing Phase 35 тестов присутствуют, компилируются и валидно падают. Никаких правок в существующих тестах файла.</done>
</task>

</tasks>

<verification>
- Все 3 fixture-файла существуют с правильным content/size
- 7 новых test stubs compile и падают (RED)
- Ни один существующий тест не модифицирован и не удалён
- `npm test -- runner-extensions -t "Phase 35"` запускается без compile errors
</verification>

<success_criteria>
Wave 0 готова к Plan 02: test infrastructure для MD-01..MD-04 + D-03 + D-06 + SC-05 на месте. `35-VALIDATION.md` Per-Task Verification Map может отметить fixture и test-stub строки как ✅ созданные (но тесты всё ещё ❌ до Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/35-markdown-snippets-in-protocol-runner/35-01-SUMMARY.md` с фиксацией:
- Путей fixture-файлов
- Списком имён добавленных Phase 35 тестов
- RED-состоянием (ожидаемым) — каждый тест должен падать на текущем filter'е
</output>
