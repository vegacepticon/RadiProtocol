---
phase: 35
plan: 02
type: execute
wave: 1
depends_on:
  - "35-01"
files_modified:
  - src/views/runner-view.ts
autonomous: false
requirements:
  - MD-01
  - MD-02
  - MD-03
  - MD-04
must_haves:
  truths:
    - "Runner picker показывает .md файлы с префиксом 📝 рядом с .json (префикс 📄)"
    - "Клик по .md row не открывает fill-in modal, а сразу вставляет content верbatim"
    - "Drill-down в subfolder корректно показывает вложенные .md файлы"
    - "Mixed answer+snippet branching (Phase 31) маршрутизирует в .md branch и advances через outgoing edge"
    - "Step back после .md вставки откатывает accumulatedText и currentNodeId симметрично JSON"
    - "Пустой .md файл — валидный клик без ошибок"
    - "Session save/resume содержит byte-identical MD content через accumulatedText"
  artifacts:
    - path: "src/views/runner-view.ts"
      provides: "renderSnippetPicker с MD-row branch + handleSnippetPickerSelection со Snippet union signature + MD kind-switch"
      contains: "snippet.kind === 'md'"
  key_links:
    - from: "renderSnippetPicker"
      to: "handleSnippetPickerSelection"
      via: "click handler на MD row"
      pattern: "kind === ['\"]md['\"]"
    - from: "handleSnippetPickerSelection (md branch)"
      to: "ProtocolRunner.completeSnippet"
      via: "прямой вызов с snippet.content"
      pattern: "completeSnippet\\(snippet\\.content\\)"
    - from: "handleSnippetPickerSelection"
      to: "ProtocolRunner.pickSnippet"
      via: "всегда вызывается ДО kind-switch с snippet.path"
      pattern: "pickSnippet\\(snippet\\.(path|id)"
---

<objective>
Wave 1: расширить `runner-view.ts` так, чтобы MD-сниппеты появлялись в picker'е рядом с JSON и клик по ним вставлял content verbatim через существующий `pickSnippet → completeSnippet` контракт. Ноль касаний runner-core, ноль CSS diff, ноль новых runner-state'ов.

Purpose: Закрыть MD-01..MD-04 через surgical edit одного файла (2 метода), сделать 7 Wave 0 тестов зелёными.
Output: `src/views/runner-view.ts` с `Snippet`-union handler + MD prefix rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-CONTEXT.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-RESEARCH.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-PATTERNS.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-UI-SPEC.md
@.planning/phases/35-markdown-snippets-in-protocol-runner/35-01-SUMMARY.md
@src/views/runner-view.ts
@src/snippets/snippet-model.ts
@src/runner/protocol-runner.ts

<interfaces>
Current runner-view.ts hot spots:

renderSnippetPicker loop (~line 623-638):
```typescript
for (const snippet of listing.snippets) {
  if (snippet.kind !== 'json') continue;   // ← REMOVE
  const jsonSnippet = snippet;
  const row = list.createEl('button', {
    cls: 'rp-snippet-item-row',
    text: jsonSnippet.name,                  // ← prefix с 📄
  });
  this.registerDomEvent(row, 'click', () => {
    void this.handleSnippetPickerSelection(jsonSnippet);
  });
}
```

handleSnippetPickerSelection (~line 663-695):
```typescript
private async handleSnippetPickerSelection(snippet: SnippetFile): Promise<void> {
  this.runner.syncManualEdit(this.previewTextarea?.value ?? '');
  this.runner.pickSnippet(snippet.id ?? snippet.name);

  if (snippet.placeholders.length === 0) {
    this.runner.completeSnippet(snippet.template);
    void this.autoSaveSession();
    this.snippetPickerPath = [];
    this.snippetPickerNodeId = null;
    this.render();
    return;
  }
  // ... SnippetFillInModal flow
}
```

From snippet-model.ts:
```typescript
export type Snippet = JsonSnippet | MdSnippet;
// MdSnippet: { kind: 'md'; name: string; path: string; content: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить handleSnippetPickerSelection до Snippet union + MD kind branch</name>
  <files>src/views/runner-view.ts</files>
  <read_first>
    src/views/runner-view.ts
    src/snippets/snippet-model.ts
    src/runner/protocol-runner.ts
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-PATTERNS.md
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-RESEARCH.md
    ./CLAUDE.md
  </read_first>
  <behavior>
    - handleSnippetPickerSelection принимает `snippet: Snippet` (union)
    - MD-branch — ПЕРВАЯ проверка после `syncManualEdit + pickSnippet` (до любого обращения к `placeholders` / `template`)
    - MD-branch вызывает `completeSnippet(snippet.content)` verbatim, без любых трансформаций
    - MD-branch не открывает SnippetFillInModal
    - MD-branch делает те же cleanup действия, что JSON zero-placeholder path (autoSaveSession + reset picker state + render)
    - JSON-branch полностью сохранён, семантически не меняется (после narrow TypeScript видит JsonSnippet)
  </behavior>
  <action>
1. Прочитать `src/views/runner-view.ts` целиком. Найти текущий импорт `SnippetFile` из `src/snippets/snippet-model.ts`.

2. **Импорт:** добавить `Snippet` к существующему import-statement из `src/snippets/snippet-model.ts`. Например:
```typescript
import { Snippet, SnippetFile } from '../snippets/snippet-model';
```
(оставить `SnippetFile` — может использоваться в других местах; НЕ удалять ни один существующий import).

3. **Сигнатура handler'а:** заменить ТОЛЬКО тип параметра:
```typescript
// БЫЛО:
private async handleSnippetPickerSelection(snippet: SnippetFile): Promise<void> {
// СТАЛО:
private async handleSnippetPickerSelection(snippet: Snippet): Promise<void> {
```

4. **MD branch — первой проверкой после `pickSnippet`:** вставить сразу ПОСЛЕ `this.runner.pickSnippet(...)` и ДО `if (snippet.placeholders.length === 0)`:

```typescript
// Phase 35 (MD-02, D-02, D-03, D-04): MD snippets insert verbatim, no modal.
// MUST be checked BEFORE JSON-only fields (placeholders/template).
if (snippet.kind === 'md') {
  this.runner.completeSnippet(snippet.content);
  void this.autoSaveSession();
  this.snippetPickerPath = [];
  this.snippetPickerNodeId = null;
  this.render();
  return;
}
```

5. **`pickSnippet` argument:** изменить `this.runner.pickSnippet(snippet.id ?? snippet.name)` на стабильную строку, работающую для обоих kinds. MdSnippet не имеет `id`, но имеет `path`. Следуя RESEARCH § Open Question 1 (рекомендация: `snippet.path`):

```typescript
this.runner.pickSnippet(snippet.path);
```

ВАЖНО: `pickSnippet` — string-only используется внутри runner для идентификации в fill-modal; MD-branch modal не открывает, так что identity не критична. JSON-branch — путь уникален так же, как был `id ?? name`. Проверить grep'ом, нет ли где-то ещё callsite'ов, зависящих от `snippet.id` как identifier — если есть, оставить `snippet.id ?? snippet.name` для JSON и использовать `snippet.path` только в kind-switch.

**Alternative (если safer):** оставить оригинал `snippet.id ?? snippet.name` как строка — TypeScript narrow по `kind === 'md'` убедится что `id` недоступен. Тогда для union нужно:
```typescript
const pickId = snippet.kind === 'md' ? snippet.path : (snippet.id ?? snippet.name);
this.runner.pickSnippet(pickId);
```
Использовать этот вариант — он 100% backward-compatible для JSON callsite'а.

6. **JSON branch — не трогать.** После вставки MD if-block, существующие строки `if (snippet.placeholders.length === 0) { ... }` и modal-flow остаются байт-в-байт. TypeScript narrow от `if (snippet.kind === 'md') { return; }` гарантирует что ниже `snippet: JsonSnippet` и обращения к `placeholders`/`template` валидны.

7. **Запустить `npm run build`** чтобы убедиться, что TS-компиляция проходит без ошибок.

8. **CLAUDE.md compliance:** НЕ удалять ни одну существующую строку кроме точечной замены типа параметра и `pickSnippet` argument. НЕ трогать ни один другой метод в файле. НЕ трогать CSS. `git diff src/views/runner-view.ts` должен показывать только:
   - Обновлённый import line
   - 1 заменённая сигнатура handler'а
   - 1 заменённая строка `pickSnippet(...)` (или 2 строки если const pickId)
   - Новый блок `if (snippet.kind === 'md') { ... }`
  </action>
  <verify>
    <automated>cd Z:/projects/RadiProtocolObsidian && npm run build 2>&1 | tail -20 && npx vitest run src/__tests__/runner-extensions.test.ts -t "md click completes" -t "step-back md" -t "empty md" -t "mixed branch md" -t "session md resume" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "snippet: Snippet" src/views/runner-view.ts` >= 1 (handler signature)
    - `grep -c "snippet.kind === 'md'" src/views/runner-view.ts` >= 1
    - `grep -c "completeSnippet(snippet.content)" src/views/runner-view.ts` == 1
    - `grep -c "if (snippet.kind !== 'json') continue" src/views/runner-view.ts` — это проверяется в Task 2; здесь НЕ требуется убрать (Task 2 этим занимается)
    - `npm run build` exits 0
    - Тесты "md click completes", "step-back md", "empty md", "mixed branch md", "session md resume" → PASS
    - `git diff src/views/runner-view.ts` содержит только add/modify relevant для handler'а; никаких удалений не связанных с handler'ом; никакие другие методы не тронуты
  </acceptance_criteria>
  <done>handleSnippetPickerSelection принимает Snippet union, MD-branch коммитит content verbatim, TS компилируется, handler-level тесты зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Убрать JSON-only фильтр + добавить kind-aware префикс в renderSnippetPicker</name>
  <files>src/views/runner-view.ts</files>
  <read_first>
    src/views/runner-view.ts
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-UI-SPEC.md
    .planning/phases/35-markdown-snippets-in-protocol-runner/35-PATTERNS.md
  </read_first>
  <behavior>
    - Цикл `for (const snippet of listing.snippets)` обрабатывает ВСЕ kinds (фильтр `kind !== 'json'` снят)
    - JSON row отображается как `📄 {snippet.name}`
    - MD row отображается как `📝 {snippet.name}`
    - Обе ветки используют тот же CSS-class `rp-snippet-item-row` (UI-SPEC: zero CSS diff)
    - Обе ветки используют тот же click-handler `handleSnippetPickerSelection(snippet)` (разветвление по kind внутри handler'а)
    - Folder-rows (`📁 ...`) не тронуты
    - Order из `SnippetService.listFolder()` сохраняется (без kind-группировки — out of scope)
  </behavior>
  <action>
1. Найти цикл snippets в `renderSnippetPicker` (~строки 623-638). Текущий код:

```typescript
for (const snippet of listing.snippets) {
  if (snippet.kind !== 'json') continue;
  const jsonSnippet = snippet;
  const row = list.createEl('button', {
    cls: 'rp-snippet-item-row',
    text: jsonSnippet.name,
  });
  this.registerDomEvent(row, 'click', () => {
    void this.handleSnippetPickerSelection(jsonSnippet);
  });
}
```

2. Заменить ВЕСЬ цикл на:

```typescript
// Phase 35: MD and JSON snippets both appear in picker (MD-01).
// Prefix glyph differentiates kind (D-01): 📄 JSON, 📝 MD.
// Click always routes through handleSnippetPickerSelection(Snippet);
// MD branch inside handler skips fill-in modal (MD-02, D-04).
for (const snippet of listing.snippets) {
  const prefix = snippet.kind === 'md' ? '📝' : '📄';
  const row = list.createEl('button', {
    cls: 'rp-snippet-item-row',
    text: `${prefix} ${snippet.name}`,
  });
  this.registerDomEvent(row, 'click', () => {
    void this.handleSnippetPickerSelection(snippet);
  });
}
```

Точечная замена — ни одной другой строки файла не трогать. НЕ удалять folder-row loop выше. НЕ удалять any code ниже.

3. Замечания:
   - `snippet.name` — basename, используется для обоих kinds (D-05).
   - TypeScript widened signature `handleSnippetPickerSelection(snippet: Snippet)` из Task 1 делает этот вызов type-safe без `as`-casts.
   - CSS-класс `rp-snippet-item-row` переиспользуется (UI-SPEC Component Inventory — zero CSS diff).
   - НЕТ отдельного `<span>` для эмодзи — префикс часть text-ноды (accessibility requirement из UI-SPEC Accessibility).

4. Запустить `npm run build` для проверки TS.

5. Запустить полный Phase 35 suite:
```bash
npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35"
```
Все 7 тестов (включая "md picker row" и "md drill-down" которые требуют rendering) должны стать зелёными.

6. **CLAUDE.md compliance:** 
   - `git diff src/views/runner-view.ts` показывает ТОЛЬКО изменения в теле цикла snippets + Task 1 изменения. 
   - НИКАКИЕ другие методы/loop'ы/handler'ы не тронуты.
   - Folder loop (`for (const folderName of listing.folders)`) — НЕ трогать.
   - Нет правок в `src/styles/runner-view.css` (zero CSS diff per UI-SPEC).
  </action>
  <verify>
    <automated>cd Z:/projects/RadiProtocolObsidian && npm run build 2>&1 | tail -10 && npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35" --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (snippet.kind !== 'json') continue" src/views/runner-view.ts` == 0 (фильтр удалён)
    - `grep -c "📝" src/views/runner-view.ts` >= 1
    - `grep -c "📄" src/views/runner-view.ts` >= 1
    - `grep -c "rp-snippet-item-row" src/views/runner-view.ts` >= 1 (класс сохранён)
    - `git diff src/styles/runner-view.css` — пусто (zero CSS changes per UI-SPEC)
    - `npm run build` exits 0
    - `npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35"` — все 7 Phase 35 тестов PASS
    - `git diff src/views/runner-view.ts` — не затрагивает folder-loop и никакие другие методы, кроме `renderSnippetPicker` snippet-loop и `handleSnippetPickerSelection` (из Task 1)
  </acceptance_criteria>
  <done>Фильтр удалён, MD и JSON обе отображаются с префиксами, `npm run build` проходит, все 7 Phase 35 тестов зелёные, CSS не тронут.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify MD picker в реальном Obsidian</name>
  <files>(human verification — no files modified)</files>
  <action>Выполнить 11-шаговую verification процедуру в реальном Obsidian (см. how-to-verify). Это checkpoint, автоматизация невозможна — эмодзи-рендеринг зависит от OS font, click feel и session persistence через Obsidian reload требуют live environment.</action>
  <verify><automated>echo "Human verification required — see how-to-verify block"</automated></verify>
  <done>User прошёл все 11 шагов verification и ответил "approved" (или описал issues для revision).</done>
  <what-built>
    MD-сниппеты в Runner snippet picker с префиксом 📝, click-to-insert verbatim без fill-in модалки, drill-down и mixed branching работают.
  </what-built>
  <how-to-verify>
    1. `npm run build` — убедиться `main.js` свежий.
    2. Reload Obsidian plugin (Ctrl+P → "Reload app without saving" или toggle плагина).
    3. Подготовить тестовый vault: создать несколько файлов в `.radiprotocol/snippets/`:
       - `test-json.json` (любой JSON snippet с 0 плейсхолдерами)
       - `test-md.md` с содержимым "Hello from MD snippet\nLine 2"
       - `subfolder/nested-md.md` с содержимым "Nested MD content"
       - `empty.md` — пустой файл
    4. Открыть канвас с snippet-pick node (или question node с snippet branch).
    5. Запустить Protocol Runner, дойти до snippet-pick state. **Проверить:**
       - В picker'е видно `📄 test-json`, `📝 test-md`, `📝 empty`, `📁 subfolder` одновременно
       - Префиксы читаются визуально как разные иконки (📄 vs 📝)
    6. Клик по `📝 test-md` → **проверить:**
       - НЕТ открывшейся fill-in modal
       - В accumulated text появилось ровно "Hello from MD snippet\nLine 2" (byte-exact)
       - Runner продвинулся дальше (status перешёл с awaiting-snippet-pick)
    7. Кликнуть `Step back` → **проверить:**
       - Accumulated text откатился до pre-MD состояния
       - Picker открыт заново на той же папке
    8. Drill-down: клик на `📁 subfolder` → **проверить:**
       - Видно `📝 nested-md`
       - Клик по нему вставляет "Nested MD content"
       - Breadcrumb / `Up` работает
    9. Клик по `📝 empty` → **проверить:**
       - Нет ошибок в консоли
       - Accumulated text не меняется (или меняется на пустую строку)
       - Runner advances
    10. Закрыть и переоткрыть Obsidian / сделать reload → открыть sessions → **проверить:**
        - Сохранённая session с уже вставленным MD содержимым корректно восстанавливается
        - MD content присутствует в restored accumulated text
    11. Открыть DevTools Console — **проверить:** нет ошибок / warnings про snippet picker или runner-view.

    Если любой пункт failing — описать shortest repro, прикрепить console log, вернуть "issues" с деталями.
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 steps pass. Otherwise describe failing step(s) and console errors.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` zero errors
- `npx vitest run src/__tests__/runner-extensions.test.ts -t "Phase 35"` — 7/7 green
- `npm test` — full suite green (за исключением 3 pre-existing red-тестов carry-over с phase-26 branch, документировано в CONTEXT.md Pitfall 2)
- `git diff src/styles/runner-view.css` — пусто (UI-SPEC contract)
- `git diff src/views/runner-view.ts` — только 2 метода (`renderSnippetPicker` snippet-loop, `handleSnippetPickerSelection`) + 1 import-line
- Human verify 11 steps pass
</verification>

<success_criteria>
- MD-01: picker рендерит .md и .json рядом с разными glyph-префиксами ✓
- MD-02: клик по .md вставляет content верbatim без модалки ✓
- MD-03: drill-down в subfolder показывает и коммитит вложенные .md ✓
- MD-04: branch-entered picker (Phase 31) маршрутизирует в .md ✓
- D-06: step-back симметрично JSON (unit test + human verify) ✓
- SC-05: session save/resume содержит MD content ✓
- Zero CSS diff, zero runner-core diff, zero нового state ✓
</success_criteria>

<output>
After completion, create `.planning/phases/35-markdown-snippets-in-protocol-runner/35-02-SUMMARY.md` с:
- Списком изменённых строк в `runner-view.ts`
- Результатом `npm run build` + test suite
- Human verify sign-off
</output>
