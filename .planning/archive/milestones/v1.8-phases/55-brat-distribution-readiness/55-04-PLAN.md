---
phase: 55-brat-distribution-readiness
plan: 04
type: execute
wave: 4
depends_on: [55-03]
files_modified:
  - .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
autonomous: true
requirements: [BRAT-01]
must_haves:
  truths:
    - "A runbook file exists at `.planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md`"
    - "Runbook contains a `## Pre-flight (automated)` section invoking the preflight script from Plan 03"
    - "Runbook contains a `## Push the tag` section with the exact `git push origin 1.8.0` command"
    - "Runbook contains a `## Create the GitHub Release (web UI)` section with step-by-step instructions (tag selection, title `1.8.0`, 3 loose assets drag-and-drop, pre-release UNCHECKED, latest checked, Publish)"
    - "Runbook contains a `## Release Notes` section with a ready-to-paste markdown block grouped per D7 (Runner, Node Editor, Loop, Snippet, Distribution)"
    - "Release Notes section starts with an A3-guardrail note about trimming bullets for phases not yet marked Complete"
    - "Runbook contains a `## Post-release verification` section with the BRAT fresh-vault smoke test (SC-3 manual-only check)"
    - "Runbook references the BRAT identifier `vegacepticon/RadiProtocol` verbatim"
  artifacts:
    - path: ".planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md"
      provides: "User-facing, copy-pasteable runbook for the web-UI Release step + post-publish verification"
      contains: "## Release Notes"
  key_links:
    - from: "runbook §Pre-flight"
      to: "scripts/release-preflight.sh (from Plan 03)"
      via: "bash invocation with full path"
      pattern: "bash \\.planning/phases/55-brat-distribution-readiness/scripts/release-preflight\\.sh"
    - from: "runbook §Push the tag"
      to: "git tag 1.8.0 (from Plan 03)"
      via: "exact `git push origin 1.8.0` command"
      pattern: "git push origin 1\\.8\\.0"
    - from: "runbook §Post-release verification"
      to: "BRAT install smoke test in fresh vault"
      via: "identifier `vegacepticon/RadiProtocol` pasted into BRAT dialog"
      pattern: "vegacepticon/RadiProtocol"
---

<objective>
Create the user-facing release runbook — the single document the repository owner follows once to publish RadiProtocol v1.8.0 to GitHub so BRAT users can install it.

Purpose: Closes SC-2 (user creates the GitHub Release via web UI per D6) and frames SC-3 (BRAT install smoke test as the final human verification). All local preparation is already done by Plans 01–03; this runbook hands the user a deterministic, copy-pasteable sequence.

Output: ONE markdown file in the phase directory, containing five required sections (Pre-flight, Push tag, Web-UI Release, Release Notes, Post-release verification) with exact commands, exact UI steps, and a curated v1.8 changelog block ready to paste into the Release description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/55-brat-distribution-readiness/55-CONTEXT.md
@.planning/phases/55-brat-distribution-readiness/55-RESEARCH.md
@.planning/phases/55-brat-distribution-readiness/55-PATTERNS.md
@.planning/phases/55-brat-distribution-readiness/55-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 55-04-01: Author 55-RELEASE-RUNBOOK.md — the user's single-document release playbook</name>
  <files>.planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/55-brat-distribution-readiness/55-CONTEXT.md (D6 = user-driven via web UI; D7 = curated changelog; D8 = NOT pre-release, latest; D9 = public handle only)
    - .planning/phases/55-brat-distribution-readiness/55-RESEARCH.md §Code Examples "Curated changelog input — phase → user-visible bullets" (lines 393–410 — per-phase one-liners) + Assumption A3 (lines 429–430 — some phases may be incomplete at release time; runbook must say "trim to Complete only")
    - .planning/phases/55-brat-distribution-readiness/55-PATTERNS.md §6 "55-RELEASE-RUNBOOK.md" (analog = 53-UAT.md frontmatter + 54-03-SUMMARY.md narrative-with-bash-blocks style; full section skeleton)
    - .planning/phases/55-brat-distribution-readiness/55-VALIDATION.md §Manual-Only Verifications (SC-2 web-UI steps, SC-3 fresh-vault BRAT install)
    - .planning/ROADMAP.md (Phase 47–54, 56 goals — source for per-area changelog bullets; read once to ensure bullets match the shipped phases)
  </read_first>
  <action>
Write the runbook using a bash heredoc with a UNIQUE sentinel `RUNBOOK_EOF_55`. This framing is critical because the runbook body contains nested markdown code fences (```bash, ```markdown, ```json, etc.) — a unique sentinel guarantees the shell cannot accidentally terminate the document at an inner closing backtick.

**Write mechanism (MANDATORY — use this exact form):**

1. Ensure the target directory exists (it already does — the phase dir is created).
2. Invoke the following shell command, copy-pasting the body verbatim. Everything BETWEEN the opening `<<'RUNBOOK_EOF_55'` line and the closing `RUNBOOK_EOF_55` sentinel line is the exact file content:

```bash
cat > .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md <<'RUNBOOK_EOF_55'
---
phase: 55-brat-distribution-readiness
type: release-runbook
status: ready
performed_on: <YYYY-MM-DD — user fills at publish time>
final_verdict: pending
source:
  - 55-CONTEXT.md
  - 55-RESEARCH.md
  - 55-PATTERNS.md
  - 55-VALIDATION.md
created: 2026-04-21
---

# Phase 55 — v1.8.0 Release Runbook

> Step-by-step guide for publishing RadiProtocol v1.8.0 to GitHub so users can install via BRAT with identifier `vegacepticon/RadiProtocol`.
>
> Everything local (manifest bump, versions.json cleanup, .gitignore, build, commit, tag, preflight) has already been performed by Plans 01–03. Your job as the repository owner: run the preflight, push the tag, create the Release in the web UI, then verify.

---

## Pre-flight (automated)

From the repo root, run:

```bash
bash .planning/phases/55-brat-distribution-readiness/scripts/release-preflight.sh
```

Expected final line of stdout:
```
SC-1 local verification: PASS
```

If the script exits non-zero, STOP — do not push, do not publish. Read the failing section header in stdout and fix (re-run Plans 01–03 as needed) before continuing.

---

## Push the tag

The release-prep commit and annotated tag `1.8.0` already exist locally (Plan 03). Push them:

```bash
git push origin main
git push origin 1.8.0
```

(Only the tag push is strictly required for the Release step below — the branch push is recommended to keep `origin/main` in sync with the commit the tag points at.)

Verify the tag is visible on GitHub:
- Visit <https://github.com/vegacepticon/RadiProtocol/tags>
- Confirm `1.8.0` (no `v` prefix) appears at the top.
- If you see `v1.8.0` instead, STOP — a prefixed tag was created by mistake. Delete it locally and on origin, then return to Plan 03 Task 55-03-03.

---

## Create the GitHub Release (web UI)

BRAT does not read bare tags — it reads GitHub **Release** objects. A tag without a Release is invisible to BRAT (Research Pitfall 4).

1. Open <https://github.com/vegacepticon/RadiProtocol/releases/new>
2. **Choose a tag**: select `1.8.0` from the dropdown (already pushed in the previous section).
3. **Release title**: type exactly `1.8.0` — no `v` prefix, no suffix, no extra whitespace. (Pitfall 5 — tag name, release title, and `manifest.version` must all byte-match.)
4. **Description**: paste the entire contents of the [Release Notes](#release-notes) section below into the description field. Before pasting, read the A3 guardrail at the top of that section and trim any bullets for phases that are NOT yet marked Complete in `.planning/ROADMAP.md`.
5. **Attach binaries**: drag-and-drop these three files from your repo root, ONE AT A TIME (or all at once, but as individual loose files — DO NOT zip them per Pitfall 6):
   - `manifest.json`
   - `main.js`
   - `styles.css`
   Confirm the "Assets" list shows three separate downloadable entries. If it shows a single `.zip`, remove the zip and re-upload the three loose files.
6. **Pre-release flag**: leave `Set as a pre-release` UNCHECKED (per D8 — v1.8 passed UAT, so a normal Release is the honest signal).
7. **Latest release**: `Set as the latest release` must be CHECKED (default — BRAT selects by latest SemVer).
8. Click **Publish release**.

Immediately after publishing, verify in the browser:
- URL redirects to `https://github.com/vegacepticon/RadiProtocol/releases/tag/1.8.0`
- "Latest" green badge visible next to the release title
- Assets section shows three separate entries: `manifest.json`, `main.js`, `styles.css`
- Click each asset link — each downloads the individual file (not a zip, not a 404)

Optional API smoke-check (works without `gh` CLI):

```bash
curl -s https://api.github.com/repos/vegacepticon/RadiProtocol/releases/latest \
  | node -e 'const d=JSON.parse(require("fs").readFileSync(0,"utf8")); const names=(d.assets||[]).map(a=>a.name).sort().join(","); console.log("tag:", d.tag_name, "name:", d.name, "assets:", names); process.exit(d.tag_name==="1.8.0" && names==="main.js,manifest.json,styles.css" ? 0 : 1)'
```

Expected stdout:
```
tag: 1.8.0  name: 1.8.0  assets: main.js,manifest.json,styles.css
```

---

## Release Notes

> **A3 guardrail (read before pasting):** Some phases below may not be marked Complete in `.planning/ROADMAP.md` at the moment you publish. Before pasting this block into the Release description, open the ROADMAP Progress table and TRIM any bullet whose phase is not yet marked Complete. Do not ship a changelog that mentions features that were not actually included in the build.

### Runner
- **Regression fixes (Phase 47)**: manual textarea edits preserved across loop transitions; textarea auto-scrolls to the insertion point after choice click; choice buttons render Cyrillic descenders and parentheses fully.
- **Skip & Close buttons (Phase 53)**: Runner now has a Close button (with confirmation) and a Skip button that advances without appending any text.
- **Inline protocol display mode (Phase 54)**: new `Run protocol in inline` command — opens a floating modal that appends answers directly into the active note instead of the Runner pane.

### Node Editor
- **UX polish (Phases 48 + 48.1)**: obsolete "Snippet ID" field removed from Text blocks; new nodes are placed below the anchor (vertical tree); Answer form shows Display label above Answer text; Question textarea auto-grows; quick-create buttons relocated to a bottom vertical stack; tighter spacing between form content and the toolbar.

### Loop
- **Exit-edge convention (Phases 49 + 50 + 50.1)**: loop exit labels are now editable via the edge (no longer hardcoded «выход»); editing an Answer's Display label keeps every incoming edge label in sync, and vice-versa; loop exit edges are now distinguished by a leading `+` prefix; body edges can carry free-form labels; five clear Russian validator errors guide authors through malformed loops.

### Snippet
- **Picker overhaul (Phase 51)**: unified hierarchical snippet picker with drill-down, breadcrumb, and tree-wide search; Snippet nodes can bind to a specific file.
- **JSON placeholder rework (Phase 52)**: JSON placeholders simplified to two types — free-text and unified choice (with multi-select); options editor fixed; legacy types surface clear validation errors.
- **Button UX reversal (Phase 56)**: file-bound Snippet nodes render as buttons and insert content directly on click; folder-selection in the Snippet editor now shows a "committed" state indicator after clicking «Выбрать эту папку».

### Distribution
- **BRAT-ready (Phase 55 — this release)**: the repository is now installable via BRAT (Beta Reviewers Auto-update Tester) with identifier `vegacepticon/RadiProtocol`. `manifest.json`, `main.js`, and `styles.css` are attached as loose assets on every Release.

### Install via BRAT
1. Install the **BRAT** plugin from Obsidian's Community Plugins list (if not already installed).
2. Open Command Palette → `BRAT: Add a beta plugin for testing`.
3. Paste `vegacepticon/RadiProtocol` into the dialog.
4. Click `Add Plugin`.
5. Once BRAT reports the plugin installed, enable it in Settings → Community Plugins.
6. Open Command Palette → `RadiProtocol: Open Runner view`.

---

## Post-release verification

### SC-3 — Fresh-vault BRAT install smoke test (manual, 5 minutes)

1. Create a new empty vault in Obsidian (`File → New vault`).
2. Enable Community Plugins in Settings (disable Restricted Mode if prompted).
3. Install **BRAT** from the Community Plugins browser and enable it.
4. Open Command Palette → `BRAT: Add a beta plugin for testing`.
5. Paste `vegacepticon/RadiProtocol` and click `Add Plugin`.
6. Wait for BRAT's "plugin installed" notice (downloads the three loose assets from the Release).
7. Go to Settings → Community Plugins → find **RadiProtocol** in the installed list → toggle ON.
8. Open Command Palette → `RadiProtocol: Open Runner view`.
9. Confirm:
   - Plugin appears in the Community Plugins list with author `vegacepticon`.
   - Plugin enables without errors in the Developer Console.
   - Runner view opens in the workspace (selector placeholder visible: `Select a protocol…`).
10. If any step fails, open DevTools (`Ctrl+Shift+I`) → Console tab → copy the red error messages → return to this runbook's §Create the GitHub Release and verify the three assets uploaded correctly.

### Final verdict

- ✅ If all three checks pass (preflight, web-UI Release with 3 loose assets, fresh-vault BRAT install): mark `final_verdict: success` in this file's frontmatter and commit.
- ❌ If any check fails: mark `final_verdict: failed`, capture the specific failure, and return to the relevant plan (01/02/03) or this runbook's §Create the GitHub Release.

---

## Anti-patterns (do NOT do any of these)

- `git tag v1.8.0` — breaks BRAT's byte-match rule (Pitfall 5). Use `1.8.0` without prefix.
- Zipping the three assets into a single `.zip` — BRAT cannot extract it (Pitfall 6).
- Checking "Set as a pre-release" — v1.8 passed UAT; `latest` is the honest signal (D8).
- Editing the Release title to add a descriptive suffix (e.g., `1.8.0 — UX Polish`) — the tag, release name, and `manifest.version` must all read exactly `1.8.0`. Descriptive text goes in the description body, not the title.
- Running `gh release create …` — `gh` CLI is not installed in this environment and D6 locks the web UI path. Do not install `gh` just for this step.
RUNBOOK_EOF_55
```

**Why this mechanism:** the body of the runbook contains many nested ```bash, ```markdown, ```json, and ``` fences. A plain "write this markdown block" instruction would be ambiguous — the first inner closing ``` could be misread as the end of the file. The heredoc sentinel `RUNBOOK_EOF_55` is unique, appears nowhere inside the body, and is the UNAMBIGUOUS terminator. Do NOT substitute a different sentinel. Do NOT indent the closing `RUNBOOK_EOF_55` line — it must be at column 0 on its own line, exactly as shown.

**Alternative (if the heredoc is unavailable in your shell):** use an explicit BEGIN_RUNBOOK / END_RUNBOOK pair — write everything between those two markers (exclusive) to the target path via the Write tool:

- BEGIN_RUNBOOK: the line immediately after this marker is the first line of the file (`---` frontmatter opener).
- END_RUNBOOK: the line immediately before this marker is the last line of the file (the final `gh release create …` anti-pattern bullet).

Use whichever mechanism is available, but do NOT invent a third framing.

After writing the file, run this acceptance sweep:
```bash
grep -q '^## Pre-flight (automated)$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^## Push the tag$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^## Create the GitHub Release (web UI)$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^## Release Notes$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^## Post-release verification$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q 'bash .planning/phases/55-brat-distribution-readiness/scripts/release-preflight.sh' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q 'git push origin 1.8.0' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q 'vegacepticon/RadiProtocol' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^### Runner$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^### Node Editor$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^### Loop$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^### Snippet$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
grep -q '^### Distribution$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
# Sentinel must NOT appear inside the written file (only the file name in this plan references it)
! grep -q 'RUNBOOK_EOF_55' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
```

All 14 greps must exit 0.

Commit the runbook:
```bash
git add .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md
git commit -m "$(cat <<'COMMIT_EOF_55'
docs(55): add v1.8.0 release runbook

User-facing copy-pasteable playbook covering pre-flight, tag push, web-UI Release
creation (per D6), curated changelog (per D7), and post-publish BRAT smoke test
(SC-3). Includes A3 guardrail reminding the user to trim bullets for
not-yet-Complete phases before pasting.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
COMMIT_EOF_55
)"
```
  </action>
  <verify>
    <automated>test -f .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q '^## Pre-flight (automated)$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q '^## Push the tag$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q '^## Create the GitHub Release (web UI)$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q '^## Release Notes$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q '^## Post-release verification$' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q 'bash .planning/phases/55-brat-distribution-readiness/scripts/release-preflight.sh' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q 'git push origin 1.8.0' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && grep -q 'vegacepticon/RadiProtocol' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md && echo 'runbook OK'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md`
    - Contains heading `## Pre-flight (automated)` (literal match)
    - Contains heading `## Push the tag` (literal match)
    - Contains heading `## Create the GitHub Release (web UI)` (literal match)
    - Contains heading `## Release Notes` (literal match)
    - Contains heading `## Post-release verification` (literal match)
    - Contains literal string `bash .planning/phases/55-brat-distribution-readiness/scripts/release-preflight.sh`
    - Contains literal string `git push origin 1.8.0`
    - Contains literal string `vegacepticon/RadiProtocol` (BRAT identifier — at least 2 occurrences: once in Push-tag verification URL, once in BRAT Add-plugin step)
    - Contains the five curated-changelog group headers: `### Runner`, `### Node Editor`, `### Loop`, `### Snippet`, `### Distribution`
    - Contains the A3 guardrail string (a paragraph mentioning "trim" and "Complete" and "ROADMAP" in the Release Notes section — verify with `grep -qi 'trim' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md` and `grep -qi 'A3 guardrail' .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md`)
    - Contains the anti-pattern `Set as a pre-release` UNCHECKED instruction (per D8)
    - Contains `final_verdict: pending` in frontmatter
    - Sentinel `RUNBOOK_EOF_55` does NOT appear inside the written file (sanity check — the sentinel is a shell-level terminator, not part of the runbook content)
    - Commit titled `docs(55): add v1.8.0 release runbook` is on HEAD
    - `git log --oneline -1 -- .planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md` returns the just-created commit
  </acceptance_criteria>
  <done>55-RELEASE-RUNBOOK.md exists with all five required sections, the three literal strings (preflight path, push command, BRAT identifier), the five changelog group headers, and the A3 guardrail paragraph; committed with a `docs(55): add v1.8.0 release runbook` commit.</done>
</task>

</tasks>

<verification>
After the task lands, these assertions must ALL pass:

```bash
RUNBOOK=.planning/phases/55-brat-distribution-readiness/55-RELEASE-RUNBOOK.md

# File exists
test -f "$RUNBOOK"

# Five required top-level sections
grep -q '^## Pre-flight (automated)$' "$RUNBOOK"
grep -q '^## Push the tag$' "$RUNBOOK"
grep -q '^## Create the GitHub Release (web UI)$' "$RUNBOOK"
grep -q '^## Release Notes$' "$RUNBOOK"
grep -q '^## Post-release verification$' "$RUNBOOK"

# Five curated-changelog groups (per D7)
grep -q '^### Runner$' "$RUNBOOK"
grep -q '^### Node Editor$' "$RUNBOOK"
grep -q '^### Loop$' "$RUNBOOK"
grep -q '^### Snippet$' "$RUNBOOK"
grep -q '^### Distribution$' "$RUNBOOK"

# Three required literal strings
grep -q 'bash .planning/phases/55-brat-distribution-readiness/scripts/release-preflight.sh' "$RUNBOOK"
grep -q 'git push origin 1.8.0' "$RUNBOOK"
grep -q 'vegacepticon/RadiProtocol' "$RUNBOOK"

# Commit
git log --oneline -1 -- "$RUNBOOK" | grep -q 'docs(55): add v1.8.0 release runbook'
```

All 14 must exit 0.
</verification>

<success_criteria>
- `55-RELEASE-RUNBOOK.md` exists in the phase directory with all five required sections
- Preflight script invocation referenced verbatim (links Plan 04 to Plan 03)
- Exact `git push origin 1.8.0` command present (unprefixed per D1)
- BRAT identifier `vegacepticon/RadiProtocol` referenced verbatim (at least in the install step)
- Five curated-changelog area headers present per D7 (Runner, Node Editor, Loop, Snippet, Distribution)
- A3 guardrail paragraph present at the top of Release Notes (instructs user to trim not-yet-Complete phases)
- Pre-release flag is explicitly UNCHECKED (per D8)
- Post-release verification includes SC-3 fresh-vault BRAT smoke test
- Runbook committed with a `docs(55)` commit on HEAD
- Phase is now fully ready for the user to execute the web-UI Release
</success_criteria>

<output>
After completion, create `.planning/phases/55-brat-distribution-readiness/55-04-SUMMARY.md` documenting: the runbook file path, section headings verified, the list of phases sampled for the changelog (with notes on which were Complete at runbook-authoring time per A3), and the commit SHA. The summary must explicitly state that SC-2 and SC-3 are now blocked only on user action (web-UI Release + fresh-vault install), not on planner/executor work.
</output>
