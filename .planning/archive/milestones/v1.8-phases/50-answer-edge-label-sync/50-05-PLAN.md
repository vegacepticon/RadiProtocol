---
phase: 50-answer-edge-label-sync
plan: 05
type: execute
wave: 4
depends_on: ["50-01", "50-02", "50-03", "50-04"]
files_modified: []
requirements: [EDGE-02]
autonomous: false

must_haves:
  truths:
    - "npm run build exits 0 with main.js deployed to TEST-BASE vault"
    - "npm test — all prior Phase 49 greens (466) + Phase 50 additions all pass; zero failed"
    - "Zero new eslint/tsc errors introduced by Phase 50 plans 01-04"
    - "Editing Display label in Node Editor with canvas open writes Answer.displayLabel AND updates every incoming Q→A edge.label in ONE saveLiveBatch call (Pattern B single disk write)"
    - "Editing any incoming edge label on canvas triggers vault.on('modify'), reconciler picks first non-empty incoming label, writes Answer.displayLabel + re-syncs every OTHER incoming edge, and self-terminates on the follow-up modify event (diff=∅)"
    - "Multi-incoming Answer fixture shows the shared-label invariant: all incoming edges carry identical label at rest after any reconcile pass"
    - "Clearing Display label to empty strips the 'label' key from every incoming edge (not label: '')"
    - "Clearing any incoming edge label to empty clears Answer.displayLabel AND strips the 'label' key from every OTHER incoming edge"
    - "Code comment at EdgeLabelSyncService reconcile entry point cites .planning/notes/answer-label-edge-sync.md; code comment at editor-panel-view.ts:442 Display label Setting cites same note"
    - "No rules/functions/event-listeners from prior phases deleted in shared files (main.ts, editor-panel-view.ts, canvas-live-editor.ts, types/canvas-internal.d.ts, canvas-write-back.test.ts, canvas-parser.test.ts)"
  artifacts:
    - path: "main.js"
      provides: "Production bundle including EdgeLabelSyncService + saveLiveEdges/saveLiveBatch extension + reconciler"
    - path: ".planning/phases/50-answer-edge-label-sync/50-UAT.md"
      provides: "UAT sign-off log from human verification"
      contains: "PASS"
  key_links:
    - from: "npm test"
      to: "All 5 Phase 50 must-haves (D-04/D-07/D-08/D-09 unit + D-14 atomic + D-08 strip-key integration)"
      via: "vitest run — full suite"
      pattern: "passed"
    - from: "UAT TEST-BASE"
      to: "EDGE-02 human validation"
      via: "canvas-open edit-both-sides + canvas-closed edit + multi-incoming + clearing symmetry"
      pattern: "approved"
---

<objective>
Фаза-уровневый gate для Phase 50. Без новых кодовых изменений — только:
1. **Automated gate (Task 1):** сборка, полный запуск тестов, grep-аудит источников истины (code-comments citing `.planning/notes/answer-label-edge-sync.md` в нужных местах), deploy `main.js` в TEST-BASE vault для live UAT.
2. **Human UAT gate (Task 2):** проверка EDGE-02 пользователем по 5 сценариям на реальном Obsidian с TEST-BASE vault.

Purpose: убедиться, что Plans 01-04 собираются в шипабельную поставку, все invariants D-04/D-07/D-08/D-09/D-14 видны в runtime, и user подтверждает bi-directional sync на живом canvas.
Output: `50-UAT.md` с результатами; при PASS — EDGE-02 закрыт, Phase 50 готов к orchestrator phase-level gates (regression gate, code review, verify_phase_goal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md
@.planning/phases/50-answer-edge-label-sync/50-01-SUMMARY.md
@.planning/phases/50-answer-edge-label-sync/50-02-SUMMARY.md
@.planning/phases/50-answer-edge-label-sync/50-03-SUMMARY.md
@.planning/phases/50-answer-edge-label-sync/50-04-SUMMARY.md
@.planning/notes/answer-label-edge-sync.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated integration gate — build + test + grep audit + deploy</name>
  <files></files>

  <read_first>
    - Все 4 prior SUMMARYs (01/02/03/04) — узнать, какие конкретные acceptance criteria уже верифицированы per plan, чтобы этот gate не дублировал, а агрегировал.
    - `package.json` — узнать команды `build`, `test`, `dev` если нужны.
    - `CLAUDE.md` — напомнить: `styles.css` НЕ должен быть в diff Phase 50 (фаза CSS-free).
  </read_first>

  <action>

### Step 1 — Clean build
```bash
npm run build 2>&1
```
Ожидание: exit code 0, `main.js` создан в корне проекта. Если build падает — stop, fix, re-run (не переходить к Step 2 до зелёного билда).

### Step 2 — Full test suite
```bash
npm test 2>&1
```
Ожидание: **≥ (466 + Phase 50 additions) passed / 1 skipped / 0 failed**.

Минимально ожидаемые счёты (Plan 02 ≥9 reconciler tests + Plan 02 Task 3 = 2 parser tests + Plan 03 Task 2 = 5 write-back tests = +16 relative to Phase 49 baseline):
- `passed: ≥482`
- `skipped: 1`
- `failed: 0`

Если есть failed — stop, задокументировать failure в executor SUMMARY, откатиться на нужный Plan и сделать gap-closure (отдельный цикл). Не продолжать к UAT с красными тестами.

### Step 3 — Audit: canonical refs present

```bash
grep -l "answer-label-edge-sync.md" src/ -r
```
Ожидание: следующие файлы присутствуют в выдаче (минимум):
- `src/types/canvas-internal.d.ts` (Plan 01 — комментарий над `CanvasEdgeData`)
- `src/graph/edge-label-reconciler.ts` (Plan 02 — module header)
- `src/canvas/canvas-live-editor.ts` (Plan 03 — doc-комментарий `saveLiveEdges` + сигнатурный комментарий `saveLiveBatch`)
- `src/canvas/edge-label-sync-service.ts` (Plan 04 Task 1)
- `src/main.ts` (Plan 04 Task 2 — комментарий перед instantiate)
- `src/views/editor-panel-view.ts` (Plan 04 Task 3 — D-10 code-comment над Display label Setting)

**Минимум 6 файлов.** Если какой-то из 6 отсутствует — это regression предшествующего Plan; отметить в UAT.md как blocker и остановить gate.

### Step 4 — Audit: D-14 atomicity (ровно 1 vault.modify / 1 setData per write cycle)

```bash
grep -n "setData" src/canvas/canvas-live-editor.ts
```
Ожидание: в `saveLiveBatch` — ровно 1 успешная `view.canvas.setData(updatedData)` (строка внутри try); в `saveLiveEdges` — ровно 1 `view.canvas.setData(updatedData)`. Rollback setData в catch — отдельные вызовы (это NOT счёт в пользу D-14 violation; это Shared Pattern F).

```bash
grep -n "vault.modify" src/canvas/edge-label-sync-service.ts
```
Ожидание: ровно 1 hit в `reconcile()` Strategy A branch.

```bash
grep -n "vault.modify" src/views/editor-panel-view.ts
```
Ожидание: ровно 1 hit в `saveNodeEdits` (prior existing) — никакого ВТОРОГО `vault.modify` для edges (это доказательство D-14: node+edges в ОДНОМ вызове).

### Step 5 — Audit: zero CSS changes

```bash
git diff --stat src/styles/
git diff --stat styles.css
```
Ожидание: **пусто для обоих**. Phase 50 — CSS-free. Если есть изменения — это regression (CLAUDE.md: никогда не коммитить правки `styles.css` напрямую; и Phase 50 PATTERNS.md / CONTEXT.md не предусматривают CSS правок).

### Step 6 — Audit: Shared Pattern G — no prior code deleted

```bash
git log --oneline -20
git diff HEAD~<N> src/main.ts | head -50                    # проверка на удалённые блоки
git diff HEAD~<N> src/views/editor-panel-view.ts | head -50
git diff HEAD~<N> src/canvas/canvas-live-editor.ts | head -50
git diff HEAD~<N> src/types/canvas-internal.d.ts
git diff HEAD~<N> src/__tests__/canvas-write-back.test.ts
git diff HEAD~<N> src/__tests__/canvas-parser.test.ts
```
(`<N>` — количество commit'ов со старта Phase 50 Plan 01.)

Ожидание: diff показывает ТОЛЬКО additions + targeted in-place edits per Plan scope. **Ни одной строки**, относящейся к prior phases, НЕ удалено. Если найдены unrelated deletions — блокирующая regression, откат на соответствующий Plan.

### Step 7 — Deploy to TEST-BASE vault

```bash
# (зависит от setup пользователя — TEST-BASE pathpatch)
# Обычно: копирование main.js + manifest.json в <TEST-BASE>/.obsidian/plugins/radiprotocol/
# Узнать актуальный TEST-BASE path: из prior Phase 49 Plan 05 SUMMARY было развернуто ровно туда же.
```

Если деплой-скрипт существует (например, `npm run deploy` или прямой `cp` в README) — использовать его. Иначе попросить user перезагрузить плагин вручную в Obsidian (TEST-BASE) через Community plugins → Reload.

### Step 8 — Create UAT skeleton

Создать `.planning/phases/50-answer-edge-label-sync/50-UAT.md` со следующей структурой (будет заполнен в Task 2):

```markdown
# Phase 50 UAT — Answer ↔ Edge Label Sync

**Date:** <YYYY-MM-DD>
**Build:** <commit-sha> / main.js deployed to TEST-BASE
**Tests:** <passed>/<skipped>/<failed>
**Tester:** <user>

## Automated gate (Task 1)

- [ ] npm run build exit 0
- [ ] npm test: <N> passed / 1 skipped / 0 failed
- [ ] Canonical refs audit: ≥6 files cite `.planning/notes/answer-label-edge-sync.md`
- [ ] D-14 atomicity audit: ≤1 setData per saveLive method, ≤1 vault.modify per write cycle
- [ ] Zero CSS changes in Phase 50
- [ ] Shared Pattern G: no prior-phase code deletions in shared files

## Human UAT (Task 2) — 5 scenarios

### Scenario 1: Canvas open — Display label edit in Node Editor → incoming edge updates
- [ ] STEPS: ...
- [ ] EXPECTED: ...
- [ ] RESULT: PASS / FAIL + screenshot

### Scenario 2: Canvas open — edge label edit on canvas → Node Editor Display label updates
- [ ] STEPS: ...
- [ ] EXPECTED: ...
- [ ] RESULT: PASS / FAIL

### Scenario 3: Canvas closed — displayLabel edit (Strategy A path)
- [ ] STEPS: ...
- [ ] EXPECTED: ...
- [ ] RESULT: PASS / FAIL

### Scenario 4: Multi-incoming Answer — sibling edges re-sync
- [ ] STEPS: ...
- [ ] EXPECTED: ...
- [ ] RESULT: PASS / FAIL

### Scenario 5: Clearing symmetry (both directions)
- [ ] STEPS: ...
- [ ] EXPECTED: ...
- [ ] RESULT: PASS / FAIL

## Verdict

- [ ] All 5 scenarios PASS → EDGE-02 closed, Phase 50 shippable.
- [ ] Blocker found → document below + create `50-0X-PLAN.md` gap-closure.
```

В конце Task 1 заполнить первые 6 чекбоксов «Automated gate» на основе Step 1-6 результатов. Human UAT (Task 2) заполнит остальные.
  </action>

  <verify>
    <automated>npm run build && npm test</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `npm run build` exit 0; `main.js` существует и был пересобран.
    - [ ] `npm test` — passed ≥482 / skipped 1 / failed 0 (или выше).
    - [ ] Canonical refs audit (Step 3): ≥6 файлов содержат строку `answer-label-edge-sync.md`.
    - [ ] D-14 audit (Step 4): `grep -c "view.canvas.setData(updatedData)" src/canvas/canvas-live-editor.ts` returns exactly 3 (один в saveLive, один в saveLiveBatch, один в saveLiveEdges — все внутри try).
    - [ ] CSS audit (Step 5): `git diff --stat src/styles/` empty; `git diff --stat styles.css` empty.
    - [ ] Shared Pattern G audit (Step 6): diff relative to Phase 49 tip — каждый из 6 shared файлов показывает ONLY additions + explicit in-place edits per Plan scope; zero unrelated deletions.
    - [ ] `main.js` deployed to TEST-BASE (Step 7); user перезагрузил плагин (либо через скрипт, либо ручная reload).
    - [ ] `.planning/phases/50-answer-edge-label-sync/50-UAT.md` создан со скелетом + Automated gate заполнен.
  </acceptance_criteria>

  <done>
    Phase 50 — build зелёный, тесты зелёные, canonical refs на местах, D-14 atomicity доказана через counted grep'ы, shared-file diff чист. UAT.md готов к заполнению пользователем.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — bi-directional sync in TEST-BASE Obsidian vault</name>

  <what-built>
    Phase 50 shipped: EDGE-02 bi-directional `Answer.displayLabel` ↔ Question→Answer edge label sync.

    **Runtime-observable additions:**
    1. Открытие TEST-BASE vault в Obsidian → плагин подгружает `EdgeLabelSyncService` с подпиской на `vault.on('modify')` для `.canvas` файлов.
    2. В Node Editor у Answer-ноды «Display label (optional)» — то же поле, что в Phase 48; при вводе значения через 800ms debounce идёт save. Теперь save атомарно обновляет и `radiprotocol_displayLabel` на ноде, и `label` на каждом incoming Question→Answer edge.
    3. При редактировании edge label прямо на canvas: vault.on('modify') → reconciler → автоматическая синхронизация `displayLabel` на Answer-ноде и всех других incoming edges того же Answer.
    4. Очистка displayLabel (Node Editor) или edge label (canvas) симметрично снимает `label` ключ на всех связанных edges.

    **Что НЕ меняется:**
    - Phase 48 NODEUI-03 layout (Display label выше Answer text) сохранён.
    - Phase 49 loop exit edge convention — не затронут.
    - Runner workflow — не затронут (runner читает `displayLabel ?? answerText` через `nodeLabel()` из `node-label.ts:22`, поведение то же; Phase 50 просто гарантирует консистентность displayLabel на edges).
  </what-built>

  <how-to-verify>
    **Предусловия:**
    - Obsidian открыт, TEST-BASE vault подключён, плагин RadiProtocol перезагружен после `npm run build` из Task 1.
    - Для scenarios 1-2 и 4-5: откройте любой существующий `.canvas` с Question→Answer связкой в TEST-BASE.
    - Для scenario 3: создайте/найдите `.canvas`, закройте canvas tab (но vault открыт).

    **Все сценарии предполагают наблюдать результат и в Node Editor, и на canvas (edge label ribbon видим у края ребра в Obsidian Canvas). Используйте Ctrl-Shift-I → Elements если нужно видеть disk-JSON (через `view.canvas.getData()` в console) для точной верификации.**

    ### Scenario 1 — Canvas open, Display label → incoming edge (Pattern B D-06/D-14)
    1. Откройте canvas с Question→Answer связкой, где incoming edge имеет label «Старое», а `displayLabel = "Старое"`.
    2. Откройте Node Editor на Answer-ноде.
    3. В поле «Display label (optional)» введите «Новое». Через 1 секунду (800ms debounce + 250ms reconciler debounce при повторном модификейт-событии) наблюдайте:
       - Edge ribbon на canvas сменился на «Новое».
       - Если открыть canvas JSON (Obsidian: Cmd-P → Reveal file in finder → открыть canvas в text editor), оба поля совпадают: `radiprotocol_displayLabel: "Новое"` на Answer-ноде И `label: "Новое"` на edge.
    4. **EXPECTED**: синхронизация в ≤1 секунду после окончания ввода; edge ribbon и Node Editor field всегда совпадают.

    ### Scenario 2 — Canvas open, edge label → displayLabel (D-04 inbound reconcile)
    1. Сохраните состояние Scenario 1 («Новое» на обеих сторонах). Не закрывая canvas, кликните на edge label ribbon и отредактируйте его в «Ещё новее».
    2. Наблюдайте Node Editor на Answer-ноде: поле «Display label (optional)» через 250ms (reconciler debounce) должно обновиться на «Ещё новее». (Если Node Editor открыт на этой ноде — значение обновится при следующем render; может потребоваться повторный клик по ноде.)
    3. Откройте canvas JSON — `radiprotocol_displayLabel` тоже стал «Ещё новее».
    4. **EXPECTED**: reconciler подхватил edge edit, применил edge-wins → Answer.displayLabel, self-terminated на follow-up modify event (D-07 — наблюдать отсутствие бесконечного цикла записи; console log не должен спамиться).

    ### Scenario 3 — Canvas closed, displayLabel → edges (Strategy A D-13/D-14)
    1. Закройте canvas tab (но vault остаётся открыт).
    2. Откройте Node Editor на той же Answer-ноде (через palette: «Open node editor» или правым кликом в file explorer).
    3. Измените Display label на «Третье». Подождите 1 секунду.
    4. Откройте canvas JSON извне (через Finder / explorer → VS Code или другой редактор, не Obsidian). Убедитесь, что `radiprotocol_displayLabel: "Третье"` на ноде И `label: "Третье"` на edge.
    5. Re-open canvas в Obsidian — edge ribbon показывает «Третье».
    6. **EXPECTED**: Strategy A path записал node+edges в одном `vault.modify()`; нет расхождения displayLabel vs edge.label.

    ### Scenario 4 — Multi-incoming Answer — sibling re-sync
    1. Создайте canvas с двумя Question-нодами → одним Answer-нодой (используя quick-create buttons). Две edges Q→A должны обе быть incoming.
    2. Установите label «A» на первом edge (drag label ribbon, type «A»). Подождите секунду.
    3. Наблюдайте:
       - Второе edge тоже получило label «A» (sibling re-sync, D-04 step 4).
       - Answer-нода получила `displayLabel = "A"` (отобразить в Node Editor или смотреть canvas JSON).
    4. Измените label второго edge на «B». Подождите секунду.
    5. Наблюдайте: edge-wins выберет «A» (первое edge — detected first in `graph.edges` order), и второе edge будет ре-синхронизировано обратно в «A».
    6. **EXPECTED**: все incoming edges в любой момент времени after-reconcile показывают одинаковый label. Multi-incoming trade-off (D-10) работает: per-edge override невозможен.
    7. **Если** второе edge в итоге остаётся «B» (не ре-синхронизируется в «A») — это BUG, reconciler не enumerates с правильным ordering. Docurment как FAIL + file path + steps to reproduce.

    ### Scenario 5 — Clearing symmetry
    **Часть 5a (displayLabel → edges):**
    1. На любой Answer-ноде с displayLabel="X" и incoming edges имеющими label="X", в Node Editor очистите поле Display label (delete текста, поле пустое).
    2. Подождите секунду.
    3. Откройте canvas JSON. **EXPECTED**: у Answer-ноды отсутствует ключ `radiprotocol_displayLabel` (полностью удалён, не пустая строка); у каждого incoming edge отсутствует ключ `label` (полностью удалён, не `label: ""`).
    4. На canvas — edge ribbons больше не показываются (Obsidian рендерит ribbon только при наличии label).

    **Часть 5b (edge label → displayLabel):**
    1. Установите displayLabel="Y" на Answer-ноде, оба incoming edges получили label="Y".
    2. Очистите label на одном edge (delete ribbon text, edit empty, deselect).
    3. Подождите секунду.
    4. **EXPECTED**: Answer.displayLabel теперь undefined (поле в Node Editor пустое); второе edge тоже потеряло свой label (строго D-09: clearing ANY incoming edge → clears displayLabel AND every OTHER incoming edge). Canvas JSON подтверждает отсутствие ключей `radiprotocol_displayLabel` и `label`.

    ### Final
    Заполните все 5 сценариев в `50-UAT.md` значениями PASS / FAIL + при FAIL приложите:
    - actual vs expected JSON
    - screenshots canvas ribbon и Node Editor
    - console log (Obsidian DevTools → Console) от момента изменения до 3 сек спустя (могут быть `console.warn` from reconciler).

    **Если все 5 PASS** → verdict: «EDGE-02 closed, Phase 50 shippable».
    **Если есть FAIL** → остановить gate, передать этот `50-UAT.md` orchestrator'у для создания gap-closure plan (`/gsd-plan-phase --gaps`).
  </how-to-verify>

  <resume-signal>
    Type "approved" если все 5 сценариев PASS. Иначе опишите, какой scenario FAILED и приложите detail к `50-UAT.md`.
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run build` && `npm test` exit 0 (Task 1 automated).
- `50-UAT.md` существует с заполненными 6 automated checkboxes и 5 scenario результатами.
- Human verdict: "approved" → EDGE-02 closed, ROADMAP Phase 50 check-box готов к flip на ✅.
</verification>

<success_criteria>
Phase 50 закрыта по всем 3 ROADMAP Success Criteria:
1. Display label edit → incoming edge update (Pattern B когда canvas open, Strategy A когда canvas closed) — доказано Scenarios 1 + 3.
2. Edge label edit → Answer.displayLabel + re-sync other incoming edges — доказано Scenarios 2 + 4.
3. Multi-incoming topology — все incoming edges share один label; per-edge override невозможен (out of scope) + code-comments `.planning/notes/answer-label-edge-sync.md` в обоих критических местах — доказано Task 1 Step 3 + Scenario 4.
</success_criteria>

<output>
After completion, create `.planning/phases/50-answer-edge-label-sync/50-05-SUMMARY.md` bundling SUMMARY + STATE + ROADMAP + REQUIREMENTS updates (rollup commit convention from Phase 49 Plan 05).
</output>
