---
phase: 57-requirements-traceability-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TRACE-57
  - INLINE-01
  - INLINE-02
  - INLINE-03
  - INLINE-04
  - INLINE-05
tags:
  - gap-closure
  - documentation
  - traceability
must_haves:
  truths:
    - "REQUIREMENTS.md `## v1.8 Requirements` block has a new `### Inline Protocol Display Mode (INLINE)` section after `### Runner Skip & Close (RUNNER)` and before `### Distribution (BRAT)`"
    - "INLINE-01..05 body text is byte-identical to ROADMAP.md §Phase 54 SC 1-5 (lines 365-369)"
    - "NODEUI-01..05 checkboxes are `[x]` with annotation `✅ Closed by Phase 48 (2026-04-19)`"
    - "PICKER-01 and PICKER-02 checkboxes are `[x]` with annotation `✅ Closed by Phase 51 (2026-04-20)`"
    - "RUNNER-SKIP-01..03 checkboxes are `[x]` with annotation `✅ Closed by Phase 53 (2026-04-21)`"
    - "BRAT-01 checkbox is `[x]` with annotation `✅ Closed by Phase 55 (2026-04-21)`"
    - "RUNNER-CLOSE-01/02/03 remain `[ ]` (pending Phase 58 — NOT flipped by Phase 57)"
    - "REQUIREMENTS.md has a new `## Coverage Summary (as of 2026-04-21)` block between `## v1.8 Requirements` heading and `### Runner Regressions (RUNFIX)`"
    - "Traceability table has 5 new rows for INLINE-01..05 (Phase 54, ✅ 2026-04-21) inserted between RUNNER-CLOSE-03 and BRAT-01, chronological order"
    - "Traceability table rows for the 11 flipped requirements show updated ✅ status with completion date"
    - "ROADMAP.md §Progress table Phase 57 row shows `1/1 | Complete | 2026-04-21`"
    - "STATE.md has a Phase 57 session record under `### v1.8 Execution Log (compact)`"
    - "Zero changes to src/, main.js, styles.css, tests/ — verified by `git diff --name-only HEAD~1 HEAD` showing only .planning/*.md files"
    - "Single atomic commit contains all three file edits (REQUIREMENTS.md + ROADMAP.md + STATE.md)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Post-gap-closure requirements file with INLINE-01..05 section, 11 flipped checkboxes, refreshed traceability table, coverage summary block"
      contains: "### Inline Protocol Display Mode (INLINE)"
    - path: ".planning/ROADMAP.md"
      provides: "Progress table Phase 57 row marked Complete (2026-04-21)"
      contains: "| 57 | v1.8 | 1/1 | Complete | 2026-04-21 |"
    - path: ".planning/STATE.md"
      provides: "Session record for Phase 57 completion in v1.8 Execution Log"
      contains: "**Phase 57**"
  key_links:
    - from: ".planning/REQUIREMENTS.md `### Inline Protocol Display Mode (INLINE)` body"
      to: ".planning/ROADMAP.md §Phase 54 SC 1-5 (lines 365-369)"
      via: "verbatim copy preserving backticks, em-dashes, parenthetical clauses"
      pattern: "each INLINE-0N requirement body identical to its corresponding SC line"
    - from: ".planning/REQUIREMENTS.md Traceability table INLINE-01..05 rows"
      to: ".planning/REQUIREMENTS.md `### Inline Protocol Display Mode (INLINE)` section"
      via: "5 new table rows inserted before BRAT-01 row"
      pattern: "| INLINE-0N | Phase 54 | ✅ complete (2026-04-21) |"
    - from: ".planning/REQUIREMENTS.md Coverage Summary block"
      to: ".planning/REQUIREMENTS.md v1.8 Requirements body"
      via: "count of closed / pending requirements derived from post-flip state"
      pattern: "Requirements closed: N of M"
---

<objective>
Close all REQUIREMENTS.md traceability gaps identified in `.planning/v1.8-MILESTONE-AUDIT.md` §gaps.requirements in a single atomic commit:

1. Promote Phase 54 (Inline Protocol Display Mode) to a formal REQ-ID family by adding 5 new requirements `INLINE-01..05` whose body text is copied VERBATIM from ROADMAP.md §Phase 54 Success Criteria 1-5.
2. Flip EXACTLY 11 stale `[ ]` → `[x]` checkboxes (NODEUI-01..05, PICKER-01, PICKER-02, RUNNER-SKIP-01..03, BRAT-01) with short-form completion annotations.
3. Add a new `## Coverage Summary` block at the top of `## v1.8 Requirements`.
4. Refresh the Traceability table with 5 new INLINE rows and updated statuses for the 11 flipped REQ-IDs.
5. Update ROADMAP.md §Progress table Phase 57 row and append STATE.md session record.
6. Commit as ONE atomic documentation commit — no intermediate commits, no source-file touches.

Purpose: Unblocks Phase 58 (`54-VERIFICATION.md` must cite INLINE-01..05 REQ-IDs that don't yet exist) and brings REQUIREMENTS.md into sync with actual v1.8 plan-level delivery state per audit finding «milestone-wide: REQUIREMENTS.md traceability stale».

Output: One commit touching only `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` + `.planning/STATE.md`. Zero code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-requirements-traceability-refresh/57-CONTEXT.md
@.planning/v1.8-MILESTONE-AUDIT.md

<interfaces>
<!-- Verbatim source text for INLINE-01..05 — copy from ROADMAP.md §Phase 54 Success Criteria (lines 365-369) -->
<!-- Preserve backticks, em-dashes, parenthetical clauses byte-for-byte -->

From ROADMAP.md §Phase 54 Success Criteria (lines 365-369):
```
  1. A new command `Run protocol in inline` is registered in the Obsidian command palette; executing it with an active note opens a canvas picker (scoped to the `Protocol` folder), and selecting a canvas spawns a floating modal anchored over that note, starting at the canvas's entry node, per `.planning/notes/inline-protocol-mode.md`
  2. The floating modal does not block editing of the underlying note — the user can continue to type / edit the note while the modal is visible; selecting an answer option appends the resulting text to the **end** of that specific note (never cursor position, never another note)
  3. The Runner's internal textarea is not used in inline mode — the note itself is the buffer; there is no "commit all at once" step and no staging area; Obsidian's native undo is the only rollback path
  4. The protocol is bound to the source note for the duration of the run — if the user switches to a different note, the modal closes or freezes (discuss-phase picks one) and output never silently redirects elsewhere; on return to the source note the run resumes or is shown as ended (discuss-phase picks one)
  5. The existing `sidebar` and `tab` display modes are unchanged — no regression in their behavior; inline is strictly additive and reachable only through the new command, not through settings, canvas attributes, or the existing Runner launch paths
```

<!-- Target REQUIREMENTS.md anchors — use grep to locate, not hardcoded line numbers -->
<!-- INLINE section insertion anchor: immediately after the last bullet of `### Runner Skip & Close (RUNNER)` (RUNNER-CLOSE-03 Signal line), and immediately before the heading `### Distribution (BRAT)` -->
<!-- Coverage Summary anchor: immediately after heading `## v1.8 Requirements` and immediately before heading `### Runner Regressions (RUNFIX)` -->
<!-- Traceability table INLINE rows anchor: between the RUNNER-CLOSE-03 row and the BRAT-01 row; CURRENT FILE already has 5 placeholder INLINE rows marked "pending — to be introduced by Phase 57" — those rows MUST be REPLACED, not duplicated -->

<!-- 11 checkbox flip list (EXACT — RUNNER-CLOSE-01/02/03 NOT in this list per D-07) -->
Requirements to flip [ ] → [x] with short-form annotation `✅ Closed by Phase X (YYYY-MM-DD)`:
- NODEUI-01 → Phase 48 (2026-04-19)
- NODEUI-02 → Phase 48 (2026-04-19)
- NODEUI-03 → Phase 48 (2026-04-19)
- NODEUI-04 → Phase 48 (2026-04-19)
- NODEUI-05 → Phase 48 (2026-04-19)
- PICKER-01 → Phase 51 (2026-04-20)
- PICKER-02 → Phase 51 (2026-04-20)
- RUNNER-SKIP-01 → Phase 53 (2026-04-21)
- RUNNER-SKIP-02 → Phase 53 (2026-04-21)
- RUNNER-SKIP-03 → Phase 53 (2026-04-21)
- BRAT-01 → Phase 55 (2026-04-21)

<!-- Completion date format (D-08): short form `✅ Closed by Phase X (YYYY-MM-DD).` appended to the SAME LINE as the requirement body, BEFORE the Source/Signal sub-bullets, matching the RUNFIX-01 pattern at REQUIREMENTS.md line 15 -->

<!-- RUNFIX-01 template (mirror this structure for INLINE entries — but body text comes verbatim from ROADMAP SC) -->
```
- [x] **RUNFIX-01**: <body text>. ✅ Closed by Phase 47 Plan 01 (2026-04-18).
  - **Source:** <path>
  - **Signal:** <one-line observable post-condition>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert INLINE-01..05 section + flip 11 stale checkboxes + add Coverage Summary block in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (full file — observe existing section order, RUNFIX-01 annotation format on line 15, EDGE-01 annotation pattern on line 55, Traceability table structure starting at line 135)
    - `.planning/ROADMAP.md` lines 360-370 (Phase 54 Success Criteria — verbatim source for INLINE-01..05 bodies)
    - `.planning/v1.8-MILESTONE-AUDIT.md` §gaps.requirements (authoritative flip list — already in execution_context)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` in-place. Execute ALL of the following sub-edits as parts of a single pass (use multiple Edit tool calls on the same file; do NOT rewrite the whole file).

**Sub-edit A — Add Coverage Summary block** (D-06, SC-4):

Locate the heading `## v1.8 Requirements` (expected near line 11). Immediately after that heading, before the next heading `### Runner Regressions (RUNFIX)`, insert the following block (separated from surrounding content by a blank line above and below):

```markdown
## Coverage Summary (as of 2026-04-21)

- **Requirements closed:** 23 of 26 (88%) — RUNFIX-01..03, NODEUI-01..05, EDGE-01/02/03, PHLD-01, PICKER-01/02, RUNNER-SKIP-01..03, BRAT-01, INLINE-01..05
- **Deferred to Phase 58 (verification-artifact backfill):** RUNNER-CLOSE-01..03 — UAT PASS 2026-04-21, awaiting `53-VERIFICATION.md` before traceability flip
- **Historical / superseded:** EDGE-01 (Phase 49 closure preserved as historical milestone; EDGE-03 at Phase 50.1 is the active contract for the loop-exit discriminator)
```

Verify the count by re-counting the `[x]` checkboxes after all sub-edits are applied. If the final tally differs from 23 of 26, correct the "N of M" number BEFORE committing — do NOT commit a mismatched summary. Expected math: 7 pre-existing `[x]` (RUNFIX-01..03 + EDGE-01/02/03 + PHLD-01) + 11 flipped by Sub-edit C (NODEUI-01..05 + PICKER-01/02 + RUNNER-SKIP-01..03 + BRAT-01) + 5 new INLINE-01..05 = 23 closed; 3 remaining `[ ]` (RUNNER-CLOSE-01..03) = 26 total.

**Sub-edit B — Insert `### Inline Protocol Display Mode (INLINE)` section** (D-01, D-02, D-03, SC-1):

Locate the heading `### Distribution (BRAT)` (expected near the end of `## v1.8 Requirements`). Immediately BEFORE that heading (i.e., after the last bullet of the `### Runner Skip & Close (RUNNER)` section — the `Signal:` line of RUNNER-CLOSE-03), insert the following block (separated from surrounding content by a blank line above and below):

```markdown
### Inline Protocol Display Mode (INLINE)

- [x] **INLINE-01**: A new command `Run protocol in inline` is registered in the Obsidian command palette; executing it with an active note opens a canvas picker (scoped to the `Protocol` folder), and selecting a canvas spawns a floating modal anchored over that note, starting at the canvas's entry node, per `.planning/notes/inline-protocol-mode.md`. ✅ Closed by Phase 54 (2026-04-21).
  - **Source:** `.planning/phases/54-inline-protocol-display-mode/54-CONTEXT.md` + `.planning/notes/inline-protocol-mode.md`
  - **Signal:** command registered; canvas picker scoped to `Protocol/`; selecting a canvas opens a floating modal over the active note.

- [x] **INLINE-02**: The floating modal does not block editing of the underlying note — the user can continue to type / edit the note while the modal is visible; selecting an answer option appends the resulting text to the **end** of that specific note (never cursor position, never another note). ✅ Closed by Phase 54 (2026-04-21).
  - **Source:** `.planning/phases/54-inline-protocol-display-mode/54-CONTEXT.md` + `.planning/notes/inline-protocol-mode.md`
  - **Signal:** floating modal is non-blocking; answer selection appends to the end of the source note only.

- [x] **INLINE-03**: The Runner's internal textarea is not used in inline mode — the note itself is the buffer; there is no "commit all at once" step and no staging area; Obsidian's native undo is the only rollback path. ✅ Closed by Phase 54 (2026-04-21).
  - **Source:** `.planning/phases/54-inline-protocol-display-mode/54-CONTEXT.md` + `.planning/notes/inline-protocol-mode.md`
  - **Signal:** note-as-buffer (no staging area); rollback routed through Obsidian's native undo.

- [x] **INLINE-04**: The protocol is bound to the source note for the duration of the run — if the user switches to a different note, the modal closes or freezes (discuss-phase picks one) and output never silently redirects elsewhere; on return to the source note the run resumes or is shown as ended (discuss-phase picks one). ✅ Closed by Phase 54 (2026-04-21).
  - **Source:** `.planning/phases/54-inline-protocol-display-mode/54-CONTEXT.md` + `.planning/notes/inline-protocol-mode.md`
  - **Signal:** source-note binding enforced; output never redirects to another note even on active-note switch.

- [x] **INLINE-05**: The existing `sidebar` and `tab` display modes are unchanged — no regression in their behavior; inline is strictly additive and reachable only through the new command, not through settings, canvas attributes, or the existing Runner launch paths. ✅ Closed by Phase 54 (2026-04-21).
  - **Source:** `.planning/phases/54-inline-protocol-display-mode/54-CONTEXT.md` + `.planning/notes/inline-protocol-mode.md`
  - **Signal:** inline mode is additive; sidebar/tab launch paths untouched.
```

**VERBATIM REQUIREMENT (D-01, SC-1):** The first sentence of each `INLINE-0N` body (up to and including the trailing period before `✅ Closed by...`) MUST be byte-identical to the corresponding Phase 54 Success Criterion in ROADMAP.md lines 365-369. Preserve backticks around `Run protocol in inline`, `Protocol`, `.planning/notes/inline-protocol-mode.md`, `sidebar`, `tab`; preserve em-dashes `—`; preserve parenthetical clauses `(discuss-phase picks one)`; preserve bold markers `**end**`. After editing, diff-check by eye against ROADMAP lines 365-369.

**Sub-edit C — Flip 11 stale checkboxes** (D-07, D-08, SC-2):

For each of the following REQ-IDs, locate the existing line starting with `- [ ] **<REQ-ID>**:` and perform TWO changes on that line only:
1. Change `[ ]` to `[x]`.
2. Append ` ✅ Closed by Phase X (YYYY-MM-DD).` at the end of the requirement body line (before the `  - **Source:**` sub-bullet, matching RUNFIX-01's pattern at line 15).

Exact flips:
- `NODEUI-01` → append ` ✅ Closed by Phase 48 (2026-04-19).`
- `NODEUI-02` → append ` ✅ Closed by Phase 48 (2026-04-19).`
- `NODEUI-03` → append ` ✅ Closed by Phase 48 (2026-04-19).`
- `NODEUI-04` → append ` ✅ Closed by Phase 48 (2026-04-19).`
- `NODEUI-05` → append ` ✅ Closed by Phase 48 (2026-04-19).`
- `PICKER-01` → append ` ✅ Closed by Phase 51 (2026-04-20).` (Note: the PICKER-01 body has a nested bullet list before Source; the completion annotation goes at the end of the final sentence of the top-level bullet, just before the `  - **Source:**` sub-bullet — preserve the nested `- if it is the sole option...` / `- if it sits among sibling options...` / `- the placeholder fill-in modal...` bullets intact.)
- `PICKER-02` → append ` ✅ Closed by Phase 51 (2026-04-20).`
- `RUNNER-SKIP-01` → append ` ✅ Closed by Phase 53 (2026-04-21).`
- `RUNNER-SKIP-02` → append ` ✅ Closed by Phase 53 (2026-04-21).`
- `RUNNER-SKIP-03` → append ` ✅ Closed by Phase 53 (2026-04-21).`
- `BRAT-01` → append ` ✅ Closed by Phase 55 (2026-04-21).`

**CRITICAL — DO NOT flip:** `RUNNER-CLOSE-01`, `RUNNER-CLOSE-02`, `RUNNER-CLOSE-03`. These stay `[ ]` until Phase 58 authors `53-VERIFICATION.md`. Per D-07 exactly and CLAUDE.md «Never remove existing code you didn't add» — leave these three checkboxes and their annotations untouched.

**Sub-edit D — Refresh Traceability table** (SC-3):

Locate the Traceability table (starting near line 135, heading `## Traceability`). Perform the following row edits:

1. **Flip status for 11 rows** — for each flipped REQ-ID above, replace the old "UAT PASS DATE — verification artefact pending Phase 58" or similar row-right-column text with a clean `✅ complete (YYYY-MM-DD)` using the same date as the body annotation:
   - `NODEUI-01` → `| NODEUI-01 | Phase 48 | ✅ complete (2026-04-19) |`
   - `NODEUI-02` → `| NODEUI-02 | Phase 48 | ✅ complete (2026-04-19) |`
   - `NODEUI-03` → `| NODEUI-03 | Phase 48 | ✅ complete (2026-04-19) |`
   - `NODEUI-04` → `| NODEUI-04 | Phase 48 | ✅ complete (2026-04-19) |`
   - `NODEUI-05` → `| NODEUI-05 | Phase 48 | ✅ complete (2026-04-19) |`
   - `PICKER-01` → `| PICKER-01 | Phase 51 (+ Phase 56 reversal) | ✅ complete (2026-04-20) |` (preserve Phase 56 cross-reference per D-09)
   - `PICKER-02` → `| PICKER-02 | Phase 51 | ✅ complete (2026-04-20) |`
   - `RUNNER-SKIP-01` → `| RUNNER-SKIP-01  | Phase 53 | ✅ complete (2026-04-21) |`
   - `RUNNER-SKIP-02` → `| RUNNER-SKIP-02  | Phase 53 | ✅ complete (2026-04-21) |`
   - `RUNNER-SKIP-03` → `| RUNNER-SKIP-03  | Phase 53 | ✅ complete (2026-04-21) |`
   - `BRAT-01` → `| BRAT-01   | Phase 55 | ✅ complete (2026-04-21) |`

2. **Leave RUNNER-CLOSE-01/02/03 rows unchanged** — they keep the "UAT PASS 2026-04-21 — pre-checked without VERIFICATION.md backing; reset per audit, pending Phase 58" text.

3. **Leave EDGE-01 row unchanged** per D-05/D-10 — the existing annotation `⚠ historical (Phase 49 UAT PASS 2026-04-19) — superseded by EDGE-03 (Phase 50.1) as active contract` is already acceptable.

4. **Replace the 5 placeholder INLINE rows** — the current file has 5 rows of the form `| INLINE-0N | Phase 54 | pending — to be introduced by Phase 57 (...) |`. Replace those 5 rows in-place with:
   ```
   | INLINE-01       | Phase 54 | ✅ complete (2026-04-21) |
   | INLINE-02       | Phase 54 | ✅ complete (2026-04-21) |
   | INLINE-03       | Phase 54 | ✅ complete (2026-04-21) |
   | INLINE-04       | Phase 54 | ✅ complete (2026-04-21) |
   | INLINE-05       | Phase 54 | ✅ complete (2026-04-21) |
   ```
   Keep their position (between RUNNER-CLOSE-03 and BRAT-01, chronological phase order per Claude's Discretion note in 57-CONTEXT.md).

5. **Leave the trailing paragraph at line 164 unchanged** — `Phase 57 introduces INLINE-01..05 and flips stale checkboxes, Phase 58 authors the missing VERIFICATION.md artefacts.` is already the correct post-closure note.

**Out of scope for Task 1:**
- Do NOT touch any bullet, section, or sentence outside the edits enumerated above.
- Do NOT reformat unrelated rows in the Traceability table.
- Do NOT modify the `## Out of Scope (explicit)` table.
- Do NOT modify EDGE-01/02/03 requirement bodies (D-05, D-10).
- Do NOT touch RUNNER-CLOSE-01/02/03 requirement bodies or traceability rows (D-07).
- Do NOT add new items to the `## Out of Scope` table.
- Zero changes to `src/`, `main.js`, `styles.css`, `tests/` per SC-5 — this task ONLY edits `.planning/REQUIREMENTS.md`.
  </action>
  <acceptance_criteria>
All of the following must be true after the edit (use shell commands from the repo root on git-bash / Windows bash):

1. `grep -c "^- \[x\] \*\*NODEUI-0" .planning/REQUIREMENTS.md` returns `5`
2. `grep -c "^- \[x\] \*\*RUNNER-SKIP-0" .planning/REQUIREMENTS.md` returns `3`
3. `grep -c "^- \[ \] \*\*RUNNER-CLOSE-0" .planning/REQUIREMENTS.md` returns `3` (RUNNER-CLOSE-01/02/03 remain unflipped)
4. `grep "^- \[x\] \*\*PICKER-01" .planning/REQUIREMENTS.md` matches and contains `✅ Closed by Phase 51 (2026-04-20)`
5. `grep "^- \[x\] \*\*PICKER-02" .planning/REQUIREMENTS.md` matches and contains `✅ Closed by Phase 51 (2026-04-20)`
6. `grep "^- \[x\] \*\*BRAT-01" .planning/REQUIREMENTS.md` matches and contains `✅ Closed by Phase 55 (2026-04-21)`
7. `grep -c "^- \[x\] \*\*INLINE-0" .planning/REQUIREMENTS.md` returns `5`
8. `grep -c "^### Inline Protocol Display Mode (INLINE)" .planning/REQUIREMENTS.md` returns `1`
9. `grep -c "^## Coverage Summary" .planning/REQUIREMENTS.md` returns `1`
10. `grep -c "^| INLINE-0. *| Phase 54 | ✅ complete (2026-04-21) |" .planning/REQUIREMENTS.md` returns `5` (5 new traceability rows)
11. `grep -c "pending — to be introduced by Phase 57" .planning/REQUIREMENTS.md` returns `0` (placeholder INLINE rows replaced)
12. `grep "EDGE-01" .planning/REQUIREMENTS.md | grep -c "⚠ historical"` returns `1` (EDGE-01 row annotation preserved)
13. **Verbatim check — INLINE-01 body matches ROADMAP SC-1:** the sentence between `**INLINE-01**:` and `✅ Closed by Phase 54` must match ROADMAP.md line 365's SC-1 text starting after `1. `. Spot-check by running: `grep -A1 "INLINE-01" .planning/REQUIREMENTS.md | head -2` and comparing against `sed -n '365p' .planning/ROADMAP.md`.
14. Spot-check INLINE-02 contains the markdown bold `**end**`, em-dash `—`, and parenthetical `(never cursor position, never another note)`.
15. Spot-check INLINE-04 contains the parenthetical `(discuss-phase picks one)` twice.
16. `git diff --name-only` shows ONLY `.planning/REQUIREMENTS.md` modified (Task 1 does not touch other files).
17. `grep -E "^(src/|tests/|main\.js|styles\.css)" <(git diff --name-only)` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; [ "$(grep -c "^- \[x\] \*\*NODEUI-0" .planning/REQUIREMENTS.md)" = "5" ] && [ "$(grep -c "^- \[x\] \*\*RUNNER-SKIP-0" .planning/REQUIREMENTS.md)" = "3" ] && [ "$(grep -c "^- \[ \] \*\*RUNNER-CLOSE-0" .planning/REQUIREMENTS.md)" = "3" ] && [ "$(grep -c "^- \[x\] \*\*INLINE-0" .planning/REQUIREMENTS.md)" = "5" ] && [ "$(grep -c "^### Inline Protocol Display Mode (INLINE)" .planning/REQUIREMENTS.md)" = "1" ] && [ "$(grep -c "^## Coverage Summary" .planning/REQUIREMENTS.md)" = "1" ] && [ "$(grep -c "pending — to be introduced by Phase 57" .planning/REQUIREMENTS.md)" = "0" ] && echo "TASK-1 OK"'</automated>
  </verify>
  <done>
REQUIREMENTS.md contains the new INLINE section, Coverage Summary block, 11 flipped checkboxes with annotations, refreshed traceability rows for the 11 flips + 5 INLINE rows, and zero changes to any other REQUIREMENTS.md content. RUNNER-CLOSE-01/02/03 are still `[ ]`. EDGE-01 row still reads `⚠ historical`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md §Progress table Phase 57 row</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 447-475 (§Progress table — verify Phase 57 row format matches the existing table)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` §Progress table. Locate the row for Phase 57, currently:

```
| 57 | v1.8 | 0/? | Planned (GAP CLOSURE) | — |
```

Replace with:

```
| 57 | v1.8 | 1/1 | Complete (GAP CLOSURE) | 2026-04-21 |
```

Do NOT touch any other row in the Progress table (Phase 58 stays `| 58 | v1.8 | 0/? | Planned (GAP CLOSURE, depends on 57) | — |`).

Also locate the `Plans:` list under `### Phase 57:` (expected near line 427) — if the `**Plans**: TBD (plan-phase will decompose — likely 1-2 plans)` line is still present, leave it as-is (optional polish only; do not introduce a per-plan checkbox list unless one is already present). Per Phase 57 SC-5 the phase ships as a single plan — the Progress table row is the authoritative count.

**Out of scope for Task 2:**
- Do NOT edit any other row in the Progress table.
- Do NOT edit Phase 57 Success Criteria text (lines 421-426).
- Do NOT edit any other phase section.
  </action>
  <acceptance_criteria>
1. `grep -c "^| 57 | v1.8 | 1/1 | Complete (GAP CLOSURE) | 2026-04-21 |" .planning/ROADMAP.md` returns `1`
2. `grep -c "^| 57 | v1.8 | 0/? | Planned" .planning/ROADMAP.md` returns `0`
3. `grep -c "^| 58 | v1.8 | 0/? | Planned" .planning/ROADMAP.md` returns `1` (Phase 58 row untouched)
4. `git diff --name-only` shows `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` modified (and only those two so far).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; [ "$(grep -c "^| 57 | v1.8 | 1/1 | Complete (GAP CLOSURE) | 2026-04-21 |" .planning/ROADMAP.md)" = "1" ] && [ "$(grep -c "^| 57 | v1.8 | 0/? | Planned" .planning/ROADMAP.md)" = "0" ] && [ "$(grep -c "^| 58 | v1.8 | 0/? | Planned" .planning/ROADMAP.md)" = "1" ] && echo "TASK-2 OK"'</automated>
  </verify>
  <done>
ROADMAP.md §Progress table Phase 57 row reads `| 57 | v1.8 | 1/1 | Complete (GAP CLOSURE) | 2026-04-21 |`; Phase 58 row untouched; no other rows edited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 57 session record to STATE.md v1.8 Execution Log</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` (full file — observe `### v1.8 Execution Log (compact)` block structure; each phase gets one line of the form `- **Phase N** — ...`)
  </read_first>
  <action>
Edit `.planning/STATE.md` to record Phase 57 completion. Perform TWO sub-edits:

**Sub-edit A — Append Phase 57 entry to execution log:**

Locate the last bullet of `### v1.8 Execution Log (compact)` (currently `- **Phase 56** — Snippet Button UX Reversal — ...UAT PASS 5/5.` per STATE.md line 108). Immediately after that line, append:

```
- **Phase 57** — REQUIREMENTS Traceability Refresh + Phase 54 Promotion (GAP CLOSURE) — Plan 01 shipped 2026-04-21. Single atomic commit: REQUIREMENTS.md gained Coverage Summary block + new `### Inline Protocol Display Mode (INLINE)` section with INLINE-01..05 (verbatim from ROADMAP §Phase 54 SC 1-5) + flipped 11 stale checkboxes (NODEUI-01..05 Phase 48, PICKER-01/02 Phase 51, RUNNER-SKIP-01..03 Phase 53, BRAT-01 Phase 55) + refreshed Traceability table. RUNNER-CLOSE-01..03 remain `[ ]` pending Phase 58. Zero source changes (docs-only).
```

**Sub-edit B — Update frontmatter counters and status line:**

In the YAML frontmatter (top of STATE.md):

1. `last_updated:` — change to `"2026-04-21T17:00:00.000Z"` (any time ≥ current frontmatter's `14:30:00`, same date).
2. `last_activity:` — change to `2026-04-21 -- Phase 57 shipped (gap closure, docs-only): REQUIREMENTS.md traceability refreshed, INLINE-01..05 promoted, 11 stale checkboxes flipped`.
3. `progress.total_phases:` — change from `12` to `13`.
4. `progress.completed_phases:` — change from `12` to `13`.
5. `progress.total_plans:` — change from `48` to `49`.
6. `progress.completed_plans:` — change from `48` to `49`.
7. `progress.percent:` — stays at `100` (13/13 still rounds to 100).

(`stopped_at:` may optionally be touched to append «…+ Phase 57 gap-closure shipped 2026-04-21; Phase 58 pending.» — this is optional polish. If unsure, leave `stopped_at` unchanged.)

**Sub-edit C — Update the «Most recent work» paragraph (optional polish):**

Under `## Current Position`, the line starting `**Most recent work (2026-04-21):**` lists Phases 54/55/56. Prepend one bullet:

```
- **Phase 57 — REQUIREMENTS Traceability Refresh (GAP CLOSURE)** — ✅ Complete (docs-only). Single atomic commit: INLINE-01..05 promoted, 11 stale checkboxes flipped, Coverage Summary added. Unblocks Phase 58 (which authors missing VERIFICATION.md files and cites INLINE-01..05).
```

**Out of scope for Task 3:**
- Do NOT rewrite the historical v1.0-v1.7 Shipped section.
- Do NOT edit the Standing Pitfalls list.
- Do NOT modify the v1.8 Phase Map table (it's already flagged stale).
- Do NOT touch the Open Tech Debt list.
- Do NOT touch other phase entries in the execution log.
  </action>
  <acceptance_criteria>
1. `grep -c "^- \*\*Phase 57\*\* — REQUIREMENTS Traceability Refresh" .planning/STATE.md` returns `1`
2. `grep -c "^completed_phases: 13" .planning/STATE.md` returns `1` (frontmatter updated)
3. `grep -c "^  completed_phases: 13$" .planning/STATE.md` returns `1` (indent-sensitive match — YAML nested under `progress:`)
4. `grep -c "^  total_plans: 49$" .planning/STATE.md` returns `1`
5. `grep -c "^  completed_plans: 49$" .planning/STATE.md` returns `1`
6. `git diff --name-only` now shows three files modified: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.
7. `grep -E "^(src/|tests/|main\.js|styles\.css)" <(git diff --name-only)` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; [ "$(grep -c "Phase 57.*REQUIREMENTS Traceability Refresh" .planning/STATE.md)" -ge "1" ] && [ "$(grep -c "^  completed_phases: 13$" .planning/STATE.md)" = "1" ] && [ "$(grep -c "^  total_plans: 49$" .planning/STATE.md)" = "1" ] && [ "$(grep -c "^  completed_plans: 49$" .planning/STATE.md)" = "1" ] && echo "TASK-3 OK"'</automated>
  </verify>
  <done>
STATE.md frontmatter counters reflect Phase 57 (13/13 phases, 49/49 plans); `### v1.8 Execution Log (compact)` has a new bullet for Phase 57; no other sections disturbed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Single atomic commit (REQUIREMENTS.md + ROADMAP.md + STATE.md)</name>
  <files>(git commit only — no file edits)</files>
  <read_first>
    - Run `git status` and `git diff --name-only` to confirm ONLY the three planning docs are modified.
  </read_first>
  <action>
Per D-04 and Phase 57 SC-5: commit all three file edits in ONE atomic commit.

Steps:

1. Verify working tree contains ONLY `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` as modified (no other files, no untracked files that should be staged). If other files appear in `git status --short` unexpectedly, STOP and report — do not commit.

2. Stage the three files explicitly by name (do not use `git add -A` or `git add .`):
   ```
   git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
   ```

3. Commit with the message template below (use HEREDOC to preserve formatting):
   ```
   docs(57): refresh REQUIREMENTS.md traceability + promote Phase 54 to INLINE-01..05 (gap closure)

   Closes audit findings from .planning/v1.8-MILESTONE-AUDIT.md §gaps.requirements:
   - Introduces INLINE-01..05 REQ-ID family (verbatim ROADMAP §Phase 54 SC 1-5)
   - Flips 11 stale checkboxes: NODEUI-01..05 (Phase 48, 2026-04-19), PICKER-01/02 (Phase 51, 2026-04-20), RUNNER-SKIP-01..03 (Phase 53, 2026-04-21), BRAT-01 (Phase 55, 2026-04-21)
   - Adds Coverage Summary block (23/26 closed, 88%)
   - Refreshes Traceability table with 5 INLINE rows + 11 flipped statuses
   - RUNNER-CLOSE-01..03 deliberately unchanged — stay [ ] pending Phase 58 VERIFICATION.md backfill
   - EDGE-01 row annotation preserved (historical milestone, EDGE-03 active contract)

   Updates:
   - .planning/REQUIREMENTS.md — gap closure edits above
   - .planning/ROADMAP.md — §Progress table Phase 57 row → 1/1 Complete (2026-04-21)
   - .planning/STATE.md — v1.8 Execution Log entry for Phase 57 + frontmatter counters (13/13 phases, 49/49 plans)

   Zero source-file changes (docs-only) per Phase 57 SC-5.
   Unblocks Phase 58: 54-VERIFICATION.md can now cite INLINE-01..05.
   ```

4. Verify the commit landed: `git log -1 --stat` should show exactly those three files, all under `.planning/`, and NO files under `src/`, `tests/`, or root `main.js` / `styles.css`.

**Atomicity requirement (D-04):** Task 4 MUST produce exactly ONE commit for this plan. If the commit fails (hook rejection, etc.), fix the underlying issue and commit fresh — do NOT amend previous Phase 57 attempts and do NOT split into multiple commits.
  </action>
  <acceptance_criteria>
1. `git log -1 --format=%s` returns a line starting with `docs(57): refresh REQUIREMENTS.md traceability`
2. `git log -1 --name-only --format=` (then filter blank lines) lists exactly 3 files: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.
3. `git log -1 --name-only --format= | grep -E "^(src/|tests/|main\.js$|styles\.css$)"` returns nothing.
4. `git status --short` returns empty (clean working tree after commit).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; git log -1 --format=%s | grep -q "^docs(57):" && [ "$(git log -1 --name-only --format= | grep -c .)" = "3" ] && [ "$(git log -1 --name-only --format= | grep -cE "^(src/|tests/|main\.js$|styles\.css$)")" = "0" ] && [ -z "$(git status --short)" ] && echo "TASK-4 OK"'</automated>
  </verify>
  <done>
Single atomic commit `docs(57): ...` landed on main touching exactly `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` + `.planning/STATE.md`. Working tree clean. Zero source-file touches.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (beyond per-task acceptance_criteria):

1. **Verbatim diff check (SC-1):** Extract the INLINE body sentences from REQUIREMENTS.md and diff against ROADMAP §Phase 54 SC 1-5:
   ```
   # Normalize list-prefix stripping — compare content only
   for N in 1 2 3 4 5; do
     ROADMAP_LINE=$(sed -n "$((364+N))p" .planning/ROADMAP.md | sed -E 's/^  [0-9]+\. //')
     REQ_LINE=$(grep -A0 "\\*\\*INLINE-0${N}\\*\\*:" .planning/REQUIREMENTS.md | sed -E 's/^- \[x\] \*\*INLINE-0.\*\*: //; s/ ✅ Closed by Phase 54 \(2026-04-21\)\.$//')
     [ "$ROADMAP_LINE" = "$REQ_LINE" ] || echo "MISMATCH INLINE-0$N"
   done
   ```
   Expect zero MISMATCH lines. If any appear, the verbatim contract (D-01, SC-1) is violated — re-edit before re-committing.

2. **11-flip tally (SC-2):** `grep -c "✅ Closed by Phase \(48\|51\|53\|55\)" .planning/REQUIREMENTS.md` returns at least `11` (five NODEUI + two PICKER + three RUNNER-SKIP + one BRAT = 11). INLINE-01..05 use "Phase 54" and are not counted.

3. **Traceability table shape (SC-3):** `grep -c "^| INLINE-0" .planning/REQUIREMENTS.md` returns `5`; `grep "^| EDGE-01" .planning/REQUIREMENTS.md` still contains `⚠ historical`.

4. **Coverage Summary presence (SC-4):** `grep -c "^## Coverage Summary" .planning/REQUIREMENTS.md` returns `1`; grep confirms no `[ ]` in `## v1.8 Requirements` block except the three RUNNER-CLOSE entries (deliberately deferred):
   ```
   awk '/^## v1.8 Requirements/,/^## Out of Scope/' .planning/REQUIREMENTS.md | grep -c "^- \[ \]"
   # Expect: 3 (RUNNER-CLOSE-01, RUNNER-CLOSE-02, RUNNER-CLOSE-03)
   ```

5. **Zero source changes (SC-5):** `git show --stat HEAD | grep -E "^\s*(src/|tests/|main\.js|styles\.css)"` returns empty.

6. **Single-commit atomicity (D-04):** `git log --oneline --since="2026-04-21 00:00" -- .planning/REQUIREMENTS.md` shows exactly ONE commit from today touching REQUIREMENTS.md (the Phase 57 commit). If two commits appear, D-04 is violated.
</verification>

<success_criteria>
Measurable completion (maps 1:1 to ROADMAP §Phase 57 SC 1-5):

- **SC-1:** `### Inline Protocol Display Mode (INLINE)` section exists in REQUIREMENTS.md between `### Runner Skip & Close (RUNNER)` and `### Distribution (BRAT)`; INLINE-01..05 bodies byte-identical to ROADMAP lines 365-369.
- **SC-2:** Exactly 11 checkboxes flipped to `[x]` with short-form completion annotations (NODEUI-01..05 / PICKER-01/02 / RUNNER-SKIP-01..03 / BRAT-01).
- **SC-3:** Traceability table has 5 new INLINE rows (Phase 54, ✅ 2026-04-21) + 11 rows with refreshed ✅ statuses; EDGE-01 row annotation preserved per D-05.
- **SC-4:** `## Coverage Summary (as of 2026-04-21)` block exists near the top of `## v1.8 Requirements`; only 3 unflipped `[ ]` entries remain in the v1.8 block (RUNNER-CLOSE-01/02/03 — deferred to Phase 58).
- **SC-5:** Single atomic commit touching only `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` + `.planning/STATE.md`; zero changes to `src/`, `main.js`, `styles.css`, `tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/57-requirements-traceability-refresh/57-01-SUMMARY.md` documenting:
- Exact count and list of checkboxes flipped (11)
- INLINE-01..05 verbatim-diff confirmation against ROADMAP.md lines 365-369
- Traceability table deltas (5 new rows, 11 flipped statuses, EDGE-01 preserved)
- Commit SHA of the single atomic commit
- Any deviations from the plan (there should be none — docs-only scope)
- Handoff note: Phase 58 may now cite INLINE-01..05 REQ-IDs when authoring `54-VERIFICATION.md`.
</output>
