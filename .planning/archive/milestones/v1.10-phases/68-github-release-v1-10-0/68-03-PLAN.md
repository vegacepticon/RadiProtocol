---
phase: 68-github-release-v1-10-0
plan: 03
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - manifest.json
  - versions.json
  - package.json
  - .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh
autonomous: true
requirements: [RELEASE-READY-V1.10]
must_haves:
  truths:
    - "One atomic release-prep commit contains manifest.json, versions.json, package.json, and the v1.10.0 preflight script"
    - "Annotated unprefixed tag 1.10.0 points at the release-prep commit"
    - "No v1.10.0 tag exists"
    - "Preflight exits 0 after the tag exists"
  artifacts:
    - path: ".git/refs/tags/1.10.0"
      provides: "Unprefixed annotated release tag consumed by GitHub Release creation"
      contains: "1.10.0"
    - path: ".planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh"
      provides: "Release preflight committed with v1.10.0 metadata"
      contains: "SC-1 local verification: PASS"
  key_links:
    - from: "release-prep commit"
      to: "tag 1.10.0"
      via: "tag points at HEAD after atomic commit"
      pattern: "git rev-parse 1.10.0^{commit}"
---

<objective>
Seal the local v1.10.0 release state by committing the release-prep files atomically and creating the unprefixed annotated tag `1.10.0`.

Purpose: GitHub Release publication in Plan 04 must point at a clean, preflighted commit whose metadata, build assets, and tag shape follow the Phase 62 BRAT pattern.

Output: Atomic release-prep commit at HEAD and local annotated tag `1.10.0`. Nothing is pushed.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/68-github-release-v1-10-0/68-01-PLAN.md
@.planning/phases/68-github-release-v1-10-0/68-02-PLAN.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-02-PLAN.md
@manifest.json
@versions.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 68-03-01: Commit release-prep files atomically</name>
  <files>manifest.json, versions.json, package.json, .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh</files>
  <read_first>
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-02-PLAN.md (atomic release-prep commit precedent)
    - CLAUDE.md (stage explicit files only; never commit generated styles.css directly)
  </read_first>
  <action>Run `bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh`. Stage only `manifest.json`, `versions.json`, `package.json`, and `.planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh`. Verify the staged set contains exactly those four paths and excludes `main.js`, `styles.css`, `package-lock.json`, and unrelated planning files. Commit with message `chore(68): prepare 1.10.0 release` and body noting v1.10.0 alignment, preserved `1.8.0`/`1.9.0` mappings, and the new preflight harness. Do not push, do not amend, and do not use `--no-verify`.</action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^chore(68): prepare 1.10.0 release' && git show --name-only --format='' HEAD | grep -qx 'manifest.json' && git show --name-only --format='' HEAD | grep -qx 'versions.json' && git show --name-only --format='' HEAD | grep -qx 'package.json' && git show --name-only --format='' HEAD | grep -qx '.planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh' && ! git show --name-only --format='' HEAD | grep -Eq '^(main.js|styles.css|package-lock.json)$'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` starts with `chore(68): prepare 1.10.0 release`.
    - `git show --name-only --format='' HEAD` lists the three version files and the preflight script.
    - `git show --name-only --format='' HEAD` does not list `main.js`, `styles.css`, or `package-lock.json`.
  </acceptance_criteria>
  <done>Release-prep commit is on HEAD with exactly the intended tracked files.</done>
</task>

<task type="auto">
  <name>Task 68-03-02: Create unprefixed annotated tag 1.10.0</name>
  <files>(none — git tag only)</files>
  <read_first>
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-02-PLAN.md (unprefixed tag precedent)
    - manifest.json
  </read_first>
  <action>Verify HEAD is the `chore(68): prepare 1.10.0 release` commit, `manifest.json.version` is `1.10.0`, no `1.10.0` tag exists, and no `v1.10.0` tag exists. Run exactly `git tag -a 1.10.0 -m "RadiProtocol v1.10.0"`. The tag name must be `1.10.0` with no `v` prefix; the annotation body may contain `v1.10.0`. Do not push the tag in this plan.</action>
  <verify>
    <automated>git rev-parse --verify 1.10.0 >/dev/null && [ "$(git rev-parse 1.10.0^{commit})" = "$(git rev-parse HEAD)" ] && git cat-file -t 1.10.0 | grep -qx tag && [ -z "$(git tag -l 'v1.10.0')" ] && bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list '1.10.0'` prints exactly `1.10.0`.
    - `git tag --list 'v1.10.0'` prints nothing.
    - `git rev-parse 1.10.0^{commit}` equals `git rev-parse HEAD`.
    - `git cat-file -t 1.10.0` prints `tag`.
    - Preflight exits 0 after the tag exists and prints `SC-1 local verification: PASS`.
  </acceptance_criteria>
  <done>Annotated tag `1.10.0` points at HEAD; no prefixed collision exists; nothing has been pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub Release | The local tag will be pushed and selected in GitHub's Release UI. |
| Commit staging → repository history | Incorrect staged files could ship generated assets or unrelated changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03-01 | T | release-prep commit staged set | mitigate | Stage explicit files only and verify commit file list excludes generated assets and package-lock.json. |
| T-68-03-02 | S | v-prefixed tag collision | mitigate | Pre-tag checks reject `v1.10.0`; acceptance asserts only `1.10.0` exists. |
| T-68-03-03 | R | ambiguous tag target | mitigate | Verify `git rev-parse 1.10.0^{commit}` equals HEAD after tag creation. |
</threat_model>

<verification>
```bash
git log -1 --format='%s' | grep -q '^chore(68): prepare 1.10.0 release'
git rev-parse --verify 1.10.0 >/dev/null
test -z "$(git tag -l 'v1.10.0')"
test "$(git rev-parse 1.10.0^{commit})" = "$(git rev-parse HEAD)"
bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh
```
</verification>

<success_criteria>
- Atomic release-prep commit exists at HEAD.
- Annotated unprefixed tag `1.10.0` points at HEAD.
- No `v1.10.0` tag exists.
- Preflight passes in post-tag state.
- Nothing has been pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/68-github-release-v1-10-0/68-03-SUMMARY.md` with commit SHA, tag object SHA, tag target SHA, preflight PASS output, and confirmation that nothing was pushed.
</output>
