---
phase: 68-github-release-v1-10-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - versions.json
  - package.json
autonomous: true
requirements: [RELEASE-READY-V1.10]
must_haves:
  truths:
    - "manifest.json.version reads exactly \"1.10.0\""
    - "package.json.version reads exactly \"1.10.0\""
    - "versions.json preserves existing 1.8.0 and 1.9.0 entries and adds 1.10.0 mapped to 1.5.7"
    - "manifest metadata remains unchanged: id radiprotocol, name RadiProtocol, minAppVersion 1.5.7, author vegacepticon, authorUrl https://github.com/vegacepticon, fundingUrl empty, isDesktopOnly true"
  artifacts:
    - path: "manifest.json"
      provides: "Obsidian and BRAT plugin metadata for the v1.10.0 asset"
      contains: "\"version\": \"1.10.0\""
    - path: "versions.json"
      provides: "Historical Obsidian minAppVersion map for BRAT compatibility"
      contains: "\"1.10.0\": \"1.5.7\""
    - path: "package.json"
      provides: "npm package version aligned with release metadata"
      contains: "\"version\": \"1.10.0\""
  key_links:
    - from: "manifest.json.version"
      to: "Git tag 1.10.0 created in Plan 02"
      via: "BRAT requires byte-exact match between manifest version, tag, and GitHub Release title"
      pattern: "\"version\": \"1.10.0\""
    - from: "versions.json 1.8.0/1.9.0 entries"
      to: "prior BRAT releases"
      via: "historical mappings must remain available for older installs"
      pattern: "\"1.8.0\".*\"1.9.0\".*\"1.10.0\""
---

<objective>
Align the version-bearing release metadata files on `1.10.0` while preserving all existing BRAT compatibility mappings and manifest metadata.

Purpose: Phase 68 ships the v1.10 milestone using the established BRAT release pattern from Phases 55 and 62. The file-side version strings must be correct before any production build, preflight, tag, or GitHub Release step can be trusted.

Output: `manifest.json`, `versions.json`, and `package.json` updated for v1.10.0; no build or tag yet.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-CONTEXT.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-01-PLAN.md
@manifest.json
@versions.json
@package.json

<interfaces>
Current release metadata before this plan:
```json
manifest.json.version = "1.9.0"
manifest.json.minAppVersion = "1.5.7"
manifest.json.author = "vegacepticon"
manifest.json.authorUrl = "https://github.com/vegacepticon"
versions.json = { "1.8.0": "1.5.7", "1.9.0": "1.5.7" }
package.json.version = "1.9.0"
```

Target release metadata after this plan:
```json
manifest.json.version = "1.10.0"
versions.json = { "1.8.0": "1.5.7", "1.9.0": "1.5.7", "1.10.0": "1.5.7" }
package.json.version = "1.10.0"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 68-01-01: Update manifest.json version only</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json
    - .planning/ROADMAP.md (Phase 68 release target and success criteria)
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-01-PLAN.md (Phase 62 metadata-preservation precedent)
  </read_first>
  <behavior>
    - `manifest.json.version` becomes `1.10.0`.
    - `manifest.json.minAppVersion` remains `1.5.7`.
    - `manifest.json.author`, `authorUrl`, `fundingUrl`, `id`, `name`, `description`, and `isDesktopOnly` remain unchanged.
  </behavior>
  <action>Change exactly the `version` field in `manifest.json` from `"1.9.0"` to `"1.10.0"`. Do not edit any other key, whitespace convention, or metadata. This carries forward the Phase 62 D3/D9 decisions: minAppVersion remains `1.5.7`, author remains `vegacepticon`, authorUrl remains `https://github.com/vegacepticon`, and fundingUrl remains the empty string.</action>
  <verify>
    <automated>node -e "const m=require('./manifest.json'); if(m.version!=='1.10.0') throw new Error('manifest version '+m.version); if(m.minAppVersion!=='1.5.7') throw new Error('minAppVersion changed'); if(m.author!=='vegacepticon') throw new Error('author changed'); if(m.authorUrl!=='https://github.com/vegacepticon') throw new Error('authorUrl changed'); if(m.fundingUrl!=='') throw new Error('fundingUrl changed'); if(m.id!=='radiprotocol'||m.name!=='RadiProtocol'||m.isDesktopOnly!==true) throw new Error('manifest metadata changed'); console.log('manifest v1.10.0 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "process.stdout.write(require('./manifest.json').version)"` prints `1.10.0`.
    - `node -e "process.stdout.write(require('./manifest.json').minAppVersion)"` prints `1.5.7`.
    - `git diff manifest.json` shows only the version field changed from `1.9.0` to `1.10.0`.
  </acceptance_criteria>
  <done>manifest.json is aligned to v1.10.0 with all non-version metadata preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 68-01-02: Append versions.json mapping for 1.10.0</name>
  <files>versions.json</files>
  <read_first>
    - versions.json
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-01-PLAN.md (D4 preserve-history precedent)
  </read_first>
  <behavior>
    - `versions.json` contains exactly keys `1.8.0`, `1.9.0`, and `1.10.0`.
    - All three entries map to `1.5.7`.
    - Prior entries are not removed or reordered destructively.
  </behavior>
  <action>Rewrite `versions.json` to contain exactly this map with 2-space indentation and a trailing newline: `{ "1.8.0": "1.5.7", "1.9.0": "1.5.7", "1.10.0": "1.5.7" }` rendered across four key/value lines. Preserve `1.8.0` and `1.9.0` because they correspond to shipped BRAT releases; add only the new `1.10.0` mapping.</action>
  <verify>
    <automated>node -e "const v=require('./versions.json'); const keys=Object.keys(v).sort(); if(keys.join(',')!=='1.10.0,1.8.0,1.9.0') throw new Error('keys '+keys.join(',')); for (const k of ['1.8.0','1.9.0','1.10.0']) if(v[k]!=='1.5.7') throw new Error(k+' maps to '+v[k]); console.log('versions v1.10.0 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "process.stdout.write(Object.keys(require('./versions.json')).sort().join(','))"` prints `1.10.0,1.8.0,1.9.0`.
    - `node -e "process.stdout.write(require('./versions.json')['1.10.0'])"` prints `1.5.7`.
    - `git diff versions.json` shows the `1.10.0` entry added while `1.8.0` and `1.9.0` remain present.
  </acceptance_criteria>
  <done>versions.json preserves shipped release history and adds the v1.10.0 compatibility mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 68-01-03: Update package.json release version only</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-01-PLAN.md (single-line package version precedent)
  </read_first>
  <behavior>
    - `package.json.version` becomes `1.10.0`.
    - `scripts.build`, `scripts.test`, and `scripts.version` remain unchanged.
  </behavior>
  <action>Change only the top-level `version` field in `package.json` from `"1.9.0"` to `"1.10.0"`. Do not edit scripts, dependencies, devDependencies, package name, description, main, license, or package-lock.json in this plan.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.version!=='1.10.0') throw new Error('package version '+p.version); if(p.scripts.build!=='tsc -noEmit -skipLibCheck && node esbuild.config.mjs production') throw new Error('build script changed'); if(p.scripts.version!=='node version-bump.mjs && git add manifest.json versions.json') throw new Error('version script changed'); console.log('package v1.10.0 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "process.stdout.write(require('./package.json').version)"` prints `1.10.0`.
    - `node -e "process.stdout.write(require('./package.json').scripts.build)"` prints `tsc -noEmit -skipLibCheck && node esbuild.config.mjs production`.
    - `git diff package.json` shows only the top-level version field changed.
  </acceptance_criteria>
  <done>package.json is aligned to v1.10.0 with scripts and dependencies preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo metadata → BRAT resolver | Version metadata committed here is later packaged into GitHub Release assets consumed by BRAT. |
| Historical versions.json → older users | Removing existing release mappings can break compatibility checks for users on prior Obsidian/plugin versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | S | manifest/package/tag mismatch | mitigate | Tasks assert `1.10.0` in manifest and package; Plan 02 preflight rechecks alignment with tag before release. |
| T-68-01-02 | T | versions.json history | mitigate | Task 68-01-02 asserts all three shipped keys and `1.5.7` values. |
| T-68-01-03 | I | manifest public metadata | accept | Metadata is unchanged from prior shipped releases; task asserts author/authorUrl/fundingUrl remain known public values. |
</threat_model>

<verification>
Run after all tasks:

```bash
node -e "const m=require('./manifest.json'), p=require('./package.json'), v=require('./versions.json'); if(m.version!=='1.10.0'||p.version!=='1.10.0') process.exit(1); for (const k of ['1.8.0','1.9.0','1.10.0']) if(v[k]!=='1.5.7') process.exit(1); console.log('version alignment PASS')"
```
</verification>

<success_criteria>
- All three release metadata files agree on `1.10.0`.
- `versions.json` preserves `1.8.0` and `1.9.0` and adds `1.10.0`.
- Non-version manifest metadata and package scripts are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/68-github-release-v1-10-0/68-01-SUMMARY.md` with changed files, verification output, and a note that Plan 02 will build, preflight, commit, and tag.
</output>
