---
phase: 64-node-editor-polish-auto-grow-text-block-quick-create
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - src/views/editor-panel-view.ts
  - src/styles/editor-panel.css
  - src/__tests__/editor-panel-forms.test.ts
  - styles.css
autonomous: true
requirements:
  - EDITOR-04
must_haves:
  truths:
    - "Every authored multiline Node Editor field grows on load and input and shrinks when content is removed."
    - "No managed Node Editor multiline field shows an inner scrollbar for normal multiline content."
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "Shared growable textarea helper used by Question, Answer, Text block, Loop, and Snippet branch label fields"
      contains: "requestAnimationFrame"
    - path: "src/styles/editor-panel.css"
      provides: "Phase 64 append-only managed textarea styling"
      contains: "Phase 64"
    - path: "styles.css"
      provides: "Generated CSS output rebuilt by npm run build"
      contains: "Phase 64"
  key_links:
    - from: "src/views/editor-panel-view.ts"
      to: "src/__tests__/editor-panel-forms.test.ts"
      via: "shared helper and per-kind pending edit callbacks"
      pattern: "renderGrowable.*textarea|rp-growable-textarea"
    - from: "src/styles/editor-panel.css"
      to: "styles.css"
      via: "esbuild CSS concatenation"
      pattern: "Phase 64"
---

<objective>
Implement shared auto-grow textarea behavior across all Phase 64 Node Editor fields.

Purpose: Deliver EDITOR-04 exactly as locked by D1 and D2, reusing the Phase 48 Question behavior instead of creating a second sizing model.
Output: A small local helper in `EditorPanelView`, converted field renderers, append-only CSS, generated CSS rebuild, and green forms tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-CONTEXT.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-UI-SPEC.md
@src/views/editor-panel-view.ts
@src/styles/editor-panel.css
@src/__tests__/editor-panel-forms.test.ts

<interfaces>
Existing Question behavior to preserve and generalize:
```typescript
requestAnimationFrame(() => {
  textarea.style.height = 'auto';
  textarea.style.height = textarea.scrollHeight + 'px';
});
this.registerDomEvent(textarea, 'input', () => {
  textarea.style.height = 'auto';
  textarea.style.height = textarea.scrollHeight + 'px';
  this.pendingEdits[...] = textarea.value;
  this.scheduleAutoSave();
});
```

Required callbacks per D1/D2:
```typescript
Question: v => { radiprotocol_questionText = v; text = v; }
Answer: v => { radiprotocol_answerText = v; text = v; }
Text block: v => { radiprotocol_content = v; text = v; }
Loop: v => { radiprotocol_headerText = v; text = v; }
Snippet branch label: v => { radiprotocol_snippetLabel = v || undefined; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared growable textarea renderer</name>
  <files>src/views/editor-panel-view.ts, src/__tests__/editor-panel-forms.test.ts</files>
  <behavior>
    - Helper renders visible label, helper description, and a full-width textarea with shared managed classes.
    - Helper sizes once on form load using requestAnimationFrame and resizes on every input using height auto → scrollHeight px.
    - Helper invokes a caller-supplied value callback before `scheduleAutoSave()` so existing pending edit semantics remain exact.
  </behavior>
  <action>Create a private helper in `EditorPanelView`, e.g. `renderGrowableTextarea(container, { blockClass, textareaClass, label, desc, value, onInput })`, that emits custom DOM like the Phase 48 Question block. Replace the existing Question custom block with the helper first, preserving `rp-question-textarea` for backward-compatible tests/CSS while also adding a shared class such as `rp-growable-textarea`. Do not use `Setting.addTextArea` for managed fields because D1 requires reliable sizing and tests need DOM control.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-forms.test.ts</automated>
  </verify>
  <done>Question still passes existing tests through the new helper, and the helper exposes enough hooks/classes for Phase 64 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Answer, Text block, Loop, and Snippet branch label to growable fields</name>
  <files>src/views/editor-panel-view.ts, src/__tests__/editor-panel-forms.test.ts</files>
  <behavior>
    - Answer text, Text block Content, Loop Header text, and Snippet Branch label use the shared helper.
    - Existing labels/descriptions stay unchanged per UI-SPEC.
    - Pending edit keys stay exactly as locked in D1/D2.
    - Snippet branch label changes from `Setting.addText` to a growable textarea while preserving `v || undefined` normalization.
  </behavior>
  <action>Replace the four remaining authored multiline field renderers with the helper. Keep Display label, separator dropdowns, target picker, and all non-target fields unchanged. For Snippet branch label, keep label `Branch label`, the existing description, and `radiprotocol_snippetLabel = v || undefined` per D2. Avoid touching Runner UI or CanvasNodeFactory.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-forms.test.ts</automated>
  </verify>
  <done>Targeted forms tests pass for all five fields, including initial sizing, input resizing, shrink behavior, and pending edit keys.</done>
</task>

<task type="auto">
  <name>Task 3: Append managed textarea CSS and rebuild generated CSS</name>
  <files>src/styles/editor-panel.css, styles.css</files>
  <action>Append a `/* Phase 64: growable Node Editor textareas */` section at the bottom of `src/styles/editor-panel.css` per D5. Add shared managed textarea rules: full width, `box-sizing: border-box`, `overflow-y: hidden`, and no horizontal overflow; preserve theme focus rings and do not recolor labels/borders. Run `npm run build` to regenerate `styles.css`; do not hand-edit `styles.css`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/styles/editor-panel.css` has append-only Phase 64 rules, `styles.css` is generated by build, and the production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author input → Node Editor DOM | User-authored protocol text enters textarea elements. |
| Node Editor DOM → canvas JSON edits | Input values are copied into `pendingEdits` and saved to canvas data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02-01 | Tampering | `pendingEdits` mapping | mitigate | Keep exact D1/D2 field keys and preserve empty-to-undefined normalization for Snippet branch label. |
| T-64-02-02 | Denial of Service | Textarea sizing | mitigate | Use the existing lightweight height reset/scrollHeight pattern only on load/input; no observers or unbounded timers. |
| T-64-02-03 | Information Disclosure | DOM rendering | mitigate | Continue assigning textarea `.value`; do not introduce `innerHTML` or HTML parsing. |
</threat_model>

<verification>
1. `npm test -- src/__tests__/editor-panel-forms.test.ts`
2. `npm run build`
</verification>

<success_criteria>
- EDITOR-04 fields named in ROADMAP and CONTEXT D1/D2 all auto-grow on load, typing, paste/input, deletion, and re-render.
- Individual managed fields avoid inner scrollbars while the panel may scroll as a whole.
- No generated CSS is edited directly; `styles.css` changes only via build.
</success_criteria>

<output>
After completion, create `.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-02-SUMMARY.md`.
</output>
