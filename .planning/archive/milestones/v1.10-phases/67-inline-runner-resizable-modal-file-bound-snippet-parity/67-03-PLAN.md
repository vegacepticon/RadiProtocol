---
phase: 67-inline-runner-resizable-modal-file-bound-snippet-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 67-01
  - 67-02
files_modified:
  - .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md
autonomous: false
requirements:
  - INLINE-FIX-06
  - INLINE-FIX-07
must_haves:
  truths:
    - "Human verified in real Obsidian that the Inline Runner modal can be resized via drag from the SE corner; size persists across tab switch and Obsidian restart."
    - "Human verified that restoring on a smaller-viewport Obsidian window clamps width/height (no off-screen modal)."
    - "Human verified that legacy `data.json` records lacking width/height load with default 360×240 dimensions and remain resizable."
    - "Human verified in real Obsidian that a loop-body branch targeting a file-bound Snippet inserts the configured file in BOTH sidebar Runner and Inline Runner — no picker dialog."
    - "Human verified that loop-body branches targeting directory-bound Snippets STILL show the picker (Phase 30 D-07 not regressed)."
    - "Human verified Phase 54 invariants intact: Inline Runner does not block note editing; answers append to source note; position persists from Phase 60."
  artifacts:
    - path: ".planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md"
      provides: "Scripted UAT checklist with 6 scenario blocks (FIX-06 resize, FIX-06 persistence, FIX-06 clamp, FIX-06 back-compat, FIX-07 file-bound loop-body, FIX-07 directory-bound regression guard) + Phase 54/60 regression guards."
      contains: "Phase 67 UAT Checklist"
  key_links:
    - from: ".planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md"
      to: "human-tester (this user)"
      via: "checkpoint:human-verify checklist"
      pattern: "PASS|FAIL"
---

<objective>
Author the Phase 67 UAT checklist and pause for the user to manually verify both INLINE-FIX-06 (resize + persistence + clamp) and INLINE-FIX-07 (file-bound snippet parity in loop-body and direct-edge traversals) in a real Obsidian vault. Automated tests in plans 67-01 and 67-02 cover the unit/integration layers; Phase 54/60 invariants are regression-guarded by visual confirmation in real Obsidian where the floating-modal interactions cannot be faithfully simulated by the FakeDOM tests.

Purpose: Closes the human-verification gap that automated tests cannot cover — native CSS `resize: both` SE-corner cursor states, real `loadData/saveData` persistence across an actual Obsidian restart, real `SnippetFillInModal` rendering inside `case 'awaiting-snippet-fill'` arm of both runner views.
Output: A scripted `67-UAT.md` checklist matching the Phase 60-04 / Phase 65-02 cadence, then a `checkpoint:human-verify` block that pauses for the user to run through it and report PASS/FAIL.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-CONTEXT.md
@.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-PATTERNS.md
@.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-01-SUMMARY.md
@.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase 67 UAT checklist</name>

  <read_first>
    - `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-CONTEXT.md` — domain section + decisions D-01..D-18
    - `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-01-SUMMARY.md` — confirms FIX-06 implementation shape (debounce timing, clamp constants, default-fallback values)
    - `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-02-SUMMARY.md` — confirms FIX-07 implementation shape (commit references for D-14 mandate exception)
    - `.planning/phases/65-runner-footer-layout-back-skip-row/65-VERIFICATION.md` if present (analog UAT format from a prior v1.10 phase) — fall back to `.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-UAT.md` for the v1.10 cadence template
    - `.planning/ROADMAP.md` §Phase 67 success criteria 1-5 (what must be TRUE)
  </read_first>

  <action>
    Create `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` with the following content. Adapt the format to match the prior v1.10 UAT files if a different cadence convention is found in the read_first analog (executor confirms by inspection — the format below is the FALLBACK template and is acceptable on its own).

    ```markdown
    # Phase 67 UAT Checklist — Inline Runner Resizable Modal & File-Bound Snippet Parity

    **Goal:** Human verifies in a real Obsidian vault that INLINE-FIX-06 (resize + persistence + clamp + back-compat) and INLINE-FIX-07 (file-bound snippet parity in loop-body / direct-edge traversals) ship correctly without regressing Phase 54 invariants (Inline Runner does not block note editing) or Phase 60 invariants (position persists, clamps on viewport change).

    **Prerequisites:**
    1. Latest `main.js` and `styles.css` built via `npm run build` (root of repo).
    2. Test vault with the plugin loaded (e.g. via BRAT or symlinked plugin folder).
    3. A `.canvas` protocol file containing:
       - At least one Loop node with one body branch terminating at a Snippet node bound to a `.md` file via the Node Editor (`📄` caption visible on canvas).
       - At least one Loop node with one body branch terminating at a Snippet node bound to a directory (`📁` caption visible — directory-bound).
       - At least one regular Question with two Answer branches (for Phase 54/60 regression guard).

    ---

    ## Scenario 1 — INLINE-FIX-06 §1: Resize works smoothly

    **Setup:** Open any note. Run command `Run protocol in inline` and select a canvas.

    **Steps:**
    1. Locate the SE (bottom-right) corner of the floating Inline Runner modal — the browser's native resize grip should be visible.
    2. Drag the SE corner outward (resize larger).
    3. Drag the SE corner inward (resize smaller). Stop at a size between min and max bounds.
    4. Try to resize below `min-width: 240px` — the modal should refuse to shrink below 240px wide.
    5. Try to resize below `min-height: 120px` — the modal should refuse to shrink below 120px tall.
    6. Try to resize beyond `viewport - 32px` — the modal should refuse to grow beyond `100vw - 32px` wide and `100vh - 32px` tall.

    **Expected:**
    - Resize is smooth (no jumps, no focus loss).
    - During an active resize, a subtle elevated shadow is visible (`.is-resizing` class).
    - Min/max bounds are enforced at the CSS level — the cursor can attempt to drag past them, but the modal's box stops at the boundary.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 2 — INLINE-FIX-06 §2: Persistence across tab switch + Obsidian restart

    **Setup:** Continuing from Scenario 1 with a non-default size set.

    **Steps:**
    1. Note the current Inline Runner modal width and height (visually estimate or use browser DevTools `getBoundingClientRect`).
    2. Switch to another Obsidian tab (or the file explorer).
    3. Switch back to the original note's tab.
    4. **Expected:** Modal restores at the same width/height as before the switch.
    5. Close the Inline Runner modal (× button).
    6. Re-invoke `Run protocol in inline` on the same canvas.
    7. **Expected:** Modal opens at the same width/height (saved from step 1).
    8. Restart Obsidian (Cmd/Ctrl-Q, then reopen the vault).
    9. Open the same note and re-invoke `Run protocol in inline`.
    10. **Expected:** Modal opens at the saved width/height — persistence survived plugin reload.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 3 — INLINE-FIX-06 §3: Clamp-on-restore after viewport change

    **Setup:** Use the saved size from Scenario 2 (assumed to be larger than ~50% of current viewport).

    **Steps:**
    1. Resize the Obsidian window to the smallest practical size (e.g. 600×400 px).
    2. Open the Inline Runner.
    3. **Expected:** Modal restores at the smaller of (saved size) vs (`viewport - 32px`) — width ≤ 568, height ≤ 368.
    4. Modal is fully visible inside the viewport — no off-screen portion.
    5. Modal remains draggable (header drag still works) and resizable (SE corner still grabbable).
    6. Resize the Obsidian window back to its original (large) size.
    7. The modal stays at the clamped size (does NOT spring back to the originally saved larger size — re-clamp persists the smaller value per D-11).

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 4 — INLINE-FIX-06 §4: Legacy `data.json` back-compat (D-06 default fallback)

    **Setup:** Manually edit the test vault's `.obsidian/plugins/radiprotocol/data.json` and replace the `inlineRunnerPosition` field with a position-only payload (no width/height) — e.g. `"inlineRunnerPosition": {"left": 100, "top": 100}`. Save the file.

    **Steps:**
    1. Restart Obsidian (Cmd/Ctrl-Q).
    2. Open a note and run `Run protocol in inline`.
    3. **Expected:** Modal opens at position (100, 100) and at the default size 360×240 (the D-06 fallback constants `INLINE_RUNNER_DEFAULT_WIDTH=360` / `INLINE_RUNNER_DEFAULT_HEIGHT=240`).
    4. Resize the modal once (e.g. to 500×300).
    5. Inspect `data.json` — the `inlineRunnerPosition` field now contains `width` and `height` keys alongside `left`/`top`.
    6. The settings field name is still `inlineRunnerPosition` (NOT `inlineRunnerLayout`) — confirms D-05 on-disk back-compat.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 5 — INLINE-FIX-07 §1: Loop-body file-bound snippet inserts in INLINE Runner

    **Setup:** A canvas with a Loop node whose body branch terminates at a Snippet node bound to a `.md` file (e.g. `abdomen/ct.md` containing some markdown body text).

    **Steps:**
    1. Open a note. Run `Run protocol in inline` and select the canvas.
    2. Reach the loop picker — the body branch button caption should read `📄 <filename-stem>` (or `📄 <snippetLabel>` if the node has a `snippetLabel` set).
    3. Click the body branch button.
    4. **Expected (FIX-07):**
       - The configured file's content is appended to the active note (`abdomen/ct.md` body text appears at the end of the note).
       - The picker dialog (`SnippetTreePicker`) does NOT appear at any point.
       - If the file is `.json` with placeholders, the `SnippetFillInModal` opens INSTEAD of the picker (Phase 59 INLINE-FIX-05 behaviour preserved).
    5. The runner advances past the snippet and either returns to the loop picker (loop body re-entry) or exits via the `+exit` edge — no stuck state.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 6 — INLINE-FIX-07 §2: Loop-body file-bound snippet inserts in SIDEBAR Runner (parity)

    **Setup:** Same canvas as Scenario 5.

    **Steps:**
    1. Open the sidebar Runner (e.g. via `Open RadiProtocol Runner` command).
    2. Select the canvas; reach the loop picker.
    3. Body branch button caption is `📄 …` (same character-for-character as inline mode — D-15 caption parity).
    4. Click the body branch button.
    5. **Expected:** Same behaviour as Scenario 5 — file content is appended; no picker dialog appears.
    6. Step-back from the post-insert state restores the loop picker view; back again restores the pre-loop state. (Phase 66 step-back invariants preserved.)

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 7 — INLINE-FIX-07 regression guard: Directory-bound snippet still shows picker

    **Setup:** Canvas with a Loop node whose body branch terminates at a Snippet node bound to a DIRECTORY (e.g. `Findings/Chest`) — no `radiprotocol_snippetPath` set.

    **Steps:**
    1. Open the Inline Runner; reach the loop picker.
    2. Body branch button caption reads `📁 <subfolderPath or snippetLabel>` — directory-bound (Phase 30 D-07 caption preserved per D-15).
    3. Click the body branch button.
    4. **Expected:** The `SnippetTreePicker` opens — NOT a direct file insert. (Phase 30 D-07 picker path is preserved for directory-bound; only file-bound is fast-tracked.)
    5. Pick any snippet from the picker; it appends to the note as expected.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Scenario 8 — Phase 54 / Phase 60 regression guard

    **Steps:**
    1. With the Inline Runner open and the active note focused, type a few characters in the note's body. (Phase 54 D2 invariant: modal does not block note editing.)
    2. **Expected:** Typing works in the underlying note while the modal is visible.
    3. Drag the modal header to a new position (Phase 60 D-01).
    4. Switch tabs and switch back — position restores at the dragged location (Phase 60 persistence).
    5. Reach a Question node; click an Answer button.
    6. **Expected:** Answer text is appended to the active note (Phase 54 D3 invariant).
    7. Step-back from the post-answer state — answer is removed from the note.

    **Result:** [ ] PASS  [ ] FAIL  ▸ Notes: ___________________________________________

    ---

    ## Sign-off

    All 8 scenarios PASSED — Phase 67 UAT approved.

    Tester: ____________________  Date: ____________________

    Outstanding issues (if any FAIL): ____________________________________________
    ```

    Save the file. The format mirrors prior v1.10 UAT checklists (Phase 64-03 sign-off table, Phase 65-02 visual checklist).
  </action>

  <verify>
    <automated>test -f .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md && grep -c "Scenario " .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` exists.
    - `grep -c "Scenario " .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` returns 8 (Scenarios 1-8).
    - `grep -c "INLINE-FIX-06" .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` returns >= 4 (Scenarios 1-4 reference FIX-06).
    - `grep -c "INLINE-FIX-07" .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` returns >= 2 (Scenarios 5-7 reference FIX-07).
    - `grep -c "Phase 54" .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` >= 1 (regression guard for Inline Runner not-blocking-note-editing invariant).
    - `grep -c "Phase 60" .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` >= 1 (regression guard for position persistence).
    - `grep -cE "PASS|FAIL" .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` >= 8 (one result line per scenario).
  </acceptance_criteria>

  <done>
    `67-UAT.md` is committed with 8 scripted scenarios covering both FIX-06 (resize, persistence, clamp, back-compat) and FIX-07 (loop-body file-bound parity in inline + sidebar, directory-bound regression guard) plus Phase 54/60 invariant guards. Format mirrors prior v1.10 UAT checklists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pause for human UAT in real Obsidian</name>

  <what-built>
    Plans 67-01 and 67-02 shipped together:
    - **FIX-06**: Inline Runner is now resizable via the SE-corner native CSS grip; user-set width/height persist via `loadData/saveData` alongside Phase 60 position; restored dimensions clamp to `viewport - 32px`; legacy on-disk position-only payloads load with default 360×240 fallback (D-06).
    - **FIX-07**: Runner-core `advanceThrough` case `'snippet'` now branches on `radiprotocol_snippetPath` (D-14) — loop-body and direct-edge traversals to file-bound Snippet nodes route directly to `awaiting-snippet-fill` and dispatch the file content via the existing `case 'awaiting-snippet-fill'` arms in both `runner-view.ts` and `inline-runner-modal.ts`. `node-label.ts` snippet arm shows `📄`-prefixed file-bound captions matching sibling-button grammar (D-15).
    - **CSS**: Phase 67 append-only block in `src/styles/inline-runner.css` (`resize: both`, `min-width: 240px`, `min-height: 120px`, `.is-resizing` shadow); `styles.css` regenerated by `npm run build`.
    - **Documentation**: ROADMAP.md `Depends on` line amended (D-13 — runner-core is shared scope); STATE.md Standing Pitfall #13 added; commit message for the `protocol-runner.ts:736-741` replacement quotes the removed 4-line block per CLAUDE.md mandate exception.
  </what-built>

  <how-to-verify>
    1. Pull latest `main`, run `npm install && npm run build`. Verify `styles.css` contains `Phase 67: resizable inline runner` and `resize: both` (e.g. `grep -c "Phase 67" styles.css` returns >= 1).
    2. In a test vault with the plugin loaded (BRAT or symlinked), open `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` and walk through all 8 scenarios:
       - Scenario 1: Resize works smoothly (drag SE corner; min/max bounds enforced).
       - Scenario 2: Persistence across tab switch + Obsidian restart.
       - Scenario 3: Clamp-on-restore after window-resize.
       - Scenario 4: Legacy `data.json` back-compat (manually strip width/height; restart; default fallback applies).
       - Scenario 5: Loop-body file-bound snippet inserts in INLINE Runner.
       - Scenario 6: Loop-body file-bound snippet inserts in SIDEBAR Runner.
       - Scenario 7: Directory-bound regression guard — picker still appears for directory-bound.
       - Scenario 8: Phase 54/60 regression guard (no note-edit blocking; position persistence intact).
    3. Mark each scenario PASS or FAIL in the checklist file. Commit the filled-in checklist.
    4. If any scenario FAILS, report the failure mode below — do NOT mark the phase complete.
  </how-to-verify>

  <resume-signal>
    Reply `approved` when all 8 scenarios PASS and the filled-in `67-UAT.md` is committed. If any scenario FAILS, reply with the scenario number(s), the observed behaviour, and any DevTools/console output — the planner will determine whether a gap-closure plan or a revision is needed.
  </resume-signal>
</task>

</tasks>

<verification>
- File `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md` exists with 8 scenarios.
- Human reports all 8 scenarios PASS in real Obsidian.
- Filled-in `67-UAT.md` (with `[x] PASS` markers) is committed.
</verification>

<success_criteria>
- Phase 67 ROADMAP success criteria 1-5 (resize smooth, persistence across tab+reload, clamp-on-restore, file-bound inline parity, no Phase 54/60 regressions) are confirmed by human visual verification in real Obsidian — completing the closure loop that automated tests cannot reach.
- Both INLINE-FIX-06 and INLINE-FIX-07 marked Implemented in REQUIREMENTS.md (a follow-up housekeeping commit by the orchestrator after this checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-03-SUMMARY.md` capturing the UAT outcome and any user-reported deviations.
</output>
