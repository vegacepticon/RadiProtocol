---
phase: 43-unified-loop-graph-model-parser-validator-migration-errors
plan: 05
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - src/graph/graph-validator.ts
autonomous: true
requirements:
  - LOOP-04
  - MIGRATE-01
  - MIGRATE-02
user_setup: []

must_haves:
  truths:
    - "Канвас с узлом kind='loop-start' или 'loop-end' даёт errors с одной сводной migration-строкой на русском, содержащей дословно «loop-start», «loop-end», «loop», «выход» (кавычки-ёлочки)"
    - "Migration-check делает early-return errors перед LOOP-04 проверкой (D-CL-02 — иначе legacy-узлы получат сбивающую ошибку «loop не имеет ребра «выход»»)"
    - "Loop-узел без outgoing-ребра `label === 'выход'` даёт ошибку содержащую 'выход' и nodeLabel (D-08.1)"
    - "Loop-узел с 2+ outgoing-рёбрами `label === 'выход'` даёт ошибку с перечислением edge IDs (D-08.2)"
    - "Loop-узел без body-ветвей (только «выход») даёт ошибку «не имеет ни одной body-ветви» (D-08.3)"
    - "detectUnintentionalCycles маркирует намеренный цикл как кольцо, проходящее через kind==='loop' (не 'loop-end')"
    - "Старый Check 6 (orphaned loop-end) удалён"
    - "nodeLabel() switch — exhaustive по всем kinds включая 'loop', с legacy-fallbacks для 'loop-start'/'loop-end'"
    - "MIGRATE-02 удовлетворяется: migration-error просто становится ещё одной строкой в errors[] — RunnerView error panel рендерит её без изменений"
  artifacts:
    - path: "src/graph/graph-validator.ts"
      provides: "Migration Check + LOOP-04 Checks + updated cycle detection + nodeLabel exhaustive switch"
      contains: "«выход»"
  key_links:
    - from: "src/graph/graph-validator.ts"
      to: "src/graph/graph-model.ts"
      via: "RPNode discriminated narrowing (node.kind === 'loop' / 'loop-start' / 'loop-end')"
      pattern: "node\\.kind === 'loop'"
    - from: "src/graph/graph-validator.ts"
      to: "src/views/runner-view.ts"
      via: "errors[] массив — rendered существующим error-panel'ом (MIGRATE-02 без изменений в view)"
      pattern: "errors\\.push"
---

<objective>
Реализовать валидатор unified-loop node (LOOP-04 — три sub-проверки D-08.1/2/3) + migration-error для legacy loop-start/loop-end (MIGRATE-01, D-07) + обновление cycle detection (D-09) + удаление мёртвого Check 6 (D-10) + обновление `nodeLabel()` exhaustive switch (D-11).

Это **сердце Phase 43**: без этих правок пользователь не увидит чёткого сообщения об ошибке при открытии legacy-канваса, а валидные unified-loop канвасы пройдут мимо LOOP-04 проверок.

MIGRATE-02 удовлетворяется автоматически: RunnerView уже рендерит `errors[]` в error panel (не меняется).

Purpose: Single point of validation для всех Phase 43 correctness checks. Держит entire Phase 43 feature-loop (parser → validator → error panel) закрытым.

Output: Обновлённый `src/graph/graph-validator.ts` с:
- Новый Check 0 (migration) — early-return если legacy kinds найдены
- Новый Check LOOP-04 (три substeps: missing «выход», duplicate «выход», no body) для каждого loop-узла
- `detectUnintentionalCycles` помечает `kind === 'loop'` вместо `'loop-end'`
- Check 6 (orphaned loop-end) удалён
- `nodeLabel()` exhaustive switch обновлён (case 'loop' + сохранённые legacy cases)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-01-SUMMARY.md
@.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-02-SUMMARY.md
@src/graph/graph-validator.ts
@src/graph/graph-model.ts
@src/graph/canvas-parser.ts

<interfaces>
```ts
// Graph model types (after Plan 01):
export interface LoopNode extends RPNodeBase { kind: 'loop'; headerText: string; }
export interface LoopStartNode extends RPNodeBase { /* @deprecated */ kind: 'loop-start'; loopLabel: string; exitLabel: string; maxIterations: number; }
export interface LoopEndNode extends RPNodeBase { /* @deprecated */ kind: 'loop-end'; loopStartId: string; }

// Graph helpers available:
graph.nodes: Map<string, RPNode>
graph.edges: RPEdge[]                  // RPEdge = { id: string; fromNodeId: string; toNodeId: string; label?: string }
graph.adjacency: Map<string, string[]>
graph.reverseAdjacency: Map<string, string[]>
graph.startNodeId: string

// Existing helper (graph-validator.ts:192-203):
private nodeLabel(node: RPNode): string { switch (node.kind) { /* ... */ } }

// Existing pattern: Checks are numbered in comments; errors.push(...) is the output channel.
```

Pattern templates (см. `43-PATTERNS.md` → «Check N numbered validator blocks»):
```ts
// Migration aggregate pattern (one error per canvas, listing all legacy nodes):
const legacyNodes: string[] = [];
for (const [id, node] of graph.nodes) {
  if (node.kind === 'loop-start' || node.kind === 'loop-end') {
    legacyNodes.push(`"${this.nodeLabel(node)}"`);
  }
}
if (legacyNodes.length > 0) {
  errors.push(`...russian migration message with «loop-start», «loop-end», «loop», «выход» literals...`);
  return errors;  // early-return (D-CL-02)
}

// LOOP-04 pattern (per-loop-node substeps):
for (const [id, node] of graph.nodes) {
  if (node.kind !== 'loop') continue;
  const outgoing = graph.edges.filter(e => e.fromNodeId === id);
  const exitEdges = outgoing.filter(e => e.label === 'выход');
  const bodyEdges = outgoing.filter(e => e.label !== 'выход');
  // D-08.1 / D-08.2 / D-08.3 checks...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать Migration Check (D-07) + LOOP-04 Checks (D-08) + обновить cycle detection (D-09) + удалить Check 6 (D-10) + обновить nodeLabel switch (D-11)</name>

  <read_first>
    - `src/graph/graph-validator.ts` (весь файл — 205 строк)
    - `src/graph/graph-model.ts` (после Plan 01 — подтвердить что `LoopNode`, `LoopStartNode`, `LoopEndNode` все ещё в `RPNode` union)
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-CONTEXT.md` секции D-07, D-08, D-09, D-10, D-11, D-CL-01, D-CL-02
    - `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-PATTERNS.md` секция «`src/graph/graph-validator.ts`» полностью — Check 3/4/5/6 examples, nodeLabel() switch, и Shared Pattern «Check N numbered validator blocks»
    - `CLAUDE.md` never-remove — Checks 1, 2, 3, 4, 5 и комментарий Phase 5 TODO (строки 86-93) НЕ ТРОГАТЬ; `bfsReachable()` helper — НЕ ТРОГАТЬ
    - Обязательный D-07 чек-лист (Pattern Map → Surprise #4):
      > Migration Check стоит ПЕРЕД LOOP-04, делает `return errors;` после `errors.push()` (D-CL-02 — иначе legacy-loop-start узлы получат вводящую в заблуждение ошибку «не имеет ребра «выход»»).
  </read_first>

  <objective>
    После этой задачи `validate()` даёт user-facing correct errors во всех трёх случаях:
    1. Legacy канвас → одна migration-строка с рекомендацией rebuild
    2. Валидный unified канвас → 0 errors
    3. Unified канвас с проблемами ребра «выход» / body-веток → specific LOOP-04 ошибки
  </objective>

  <action>
  В `src/graph/graph-validator.ts` внести СЛЕДУЮЩИЕ изменения.

  **1. Добавить новый Check (migration) ПЕРЕД Check 3 reachability** — между закрывающим блоком «Check 1 & 2» (строка ~34) и началом «Check 3: Reachability» (строка ~36). Это важно для early-return (D-CL-02) — legacy канвас не должен получать сбивающие ошибки от последующих checks.

  ```ts
  // Check (migration): Legacy loop-start / loop-end nodes (Phase 43 D-07, MIGRATE-01).
  // Если найдены — возвращаем одну сводную ошибку на русском и прекращаем валидацию,
  // иначе LOOP-04 и cycle-check выдают ошибки поверх, что сбивает автора (D-CL-02).
  const legacyLoopNodes: string[] = [];
  for (const [, node] of graph.nodes) {
    if (node.kind === 'loop-start' || node.kind === 'loop-end') {
      legacyLoopNodes.push(`"${this.nodeLabel(node)}"`);
    }
  }
  if (legacyLoopNodes.length > 0) {
    errors.push(
      `Канвас содержит устаревшие узлы loop-start/loop-end: ${legacyLoopNodes.join(', ')}. ` +
      `Пересоберите цикл с единым узлом loop: метка «выход» на одном из исходящих рёбер ` +
      `обозначает ветвь выхода, остальные исходящие рёбра — тело цикла.`
    );
    return errors;
  }
  ```

  **ТОЧНЫЕ ТРЕБОВАНИЯ К СООБЩЕНИЮ (Д-07):** итоговый string должен содержать дословно:
  - `loop-start` (в составе «loop-start/loop-end» или отдельно)
  - `loop-end`
  - `loop` (отдельным словом — в «узлом loop»)
  - `«выход»` (в кавычках-ёлочках — U+00AB `«` и U+00BB `»`)
  - перечисление имён легаси-узлов через `nodeLabel()` (см. pattern Check 3)

  **2. Добавить новый Check LOOP-04 ПОСЛЕ Check 5 (Dead-end questions)** — между концом Check 5 и началом Check 6 (который скоро удалим). Порядок:

  ```
  Check 1 & 2 (start nodes)
  Check (migration)  ← NEW, early-return if triggered
  Check 3 (reachability)
  Check 4 (cycles)
  Check 5 (dead-end questions)
  Check (LOOP-04)  ← NEW — D-08.1/2/3
  (Check 6 orphaned loop-end DELETED)
  ```

  LOOP-04 block:

  ```ts
  // Check (LOOP-04): каждый unified loop-узел должен иметь
  //  1) ровно одно исходящее ребро с label === 'выход' (D-08.1)
  //  2) не более одного ребра «выход» (D-08.2) — дубликаты флагаем отдельно
  //  3) минимум одно body-ребро (label !== 'выход') (D-08.3)
  // «выход» — exact-match, case-sensitive, без trim. Контракт с автором.
  for (const [id, node] of graph.nodes) {
    if (node.kind !== 'loop') continue;
    const outgoing = graph.edges.filter(e => e.fromNodeId === id);
    const exitEdges = outgoing.filter(e => e.label === 'выход');
    const bodyEdges = outgoing.filter(e => e.label !== 'выход');
    const label = this.nodeLabel(node);
    // D-08.1 — missing «выход»
    if (exitEdges.length === 0) {
      errors.push(
        `Loop node "${label}" не имеет ребра «выход». ` +
        `Добавьте исходящее ребро с меткой «выход», обозначающее ветвь выхода из цикла.`
      );
    }
    // D-08.2 — duplicate «выход»
    if (exitEdges.length > 1) {
      const dupIds = exitEdges.map(e => e.id).join(', ');
      errors.push(
        `Loop node "${label}" имеет несколько рёбер «выход»: ${dupIds}. ` +
        `Должно быть ровно одно исходящее ребро с меткой «выход».`
      );
    }
    // D-08.3 — no body
    if (bodyEdges.length === 0) {
      errors.push(
        `Loop node "${label}" не имеет ни одной body-ветви. ` +
        `Добавьте хотя бы одно исходящее ребро с меткой, отличной от «выход».`
      );
    }
  }
  ```

  **ТОЧНЫЕ ТРЕБОВАНИЯ К СООБЩЕНИЯМ (D-08):** обязательные лексемы — `«выход»` в кавычках-ёлочках; слово `Loop node`; имя узла через `this.nodeLabel(node)`; для D-08.2 — перечисление edge IDs.

  **3. Обновить `detectUnintentionalCycles` (D-09)** — заменить `kind === 'loop-end'` маркер на `kind === 'loop'` и обновить текст ошибки.

  Найти (строки ~150-152):
  ```ts
  const passesViaLoopEnd = cycleNodes.some(id => {
    const n = graph.nodes.get(id);
    return n?.kind === 'loop-end';
  });

  if (!passesViaLoopEnd) { /* ... */ }
  ```

  Заменить НА:
  ```ts
  // Phase 43 D-09 — намеренный цикл теперь проходит через unified loop node
  // (ранее проходил через loop-end). Имя переменной обновлено.
  const passesViaLoopNode = cycleNodes.some(id => {
    const n = graph.nodes.get(id);
    return n?.kind === 'loop';
  });

  if (!passesViaLoopNode) {
    // ... существующий код построения cycleLabel ...
    errors.push(
      `Unintentional cycle detected: ${cycleLabel}. ` +
      'Cycles must pass through a loop node. Remove the back-edge or route the cycle through a loop node.'
    );
  }
  ```

  (Обратите внимание: английский язык сохраняется для этого Check — только migration-error на русском по D-07. Сообщение-изменение: `'loop-end node'` → `'loop node'`, `'use a loop-start/loop-end pair'` → `'route the cycle through a loop node'`.)

  **4. Удалить Check 6 (orphaned loop-end) целиком (D-10)** — строки ~73-84:

  ```ts
  // Check 6: Orphaned loop-end nodes
  for (const [id, node] of graph.nodes) {
    if (node.kind === 'loop-end') {
      const matchingLoopStart = graph.nodes.get(node.loopStartId);
      if (!matchingLoopStart || matchingLoopStart.kind !== 'loop-start') {
        errors.push(/* ... */);
      }
    }
  }
  ```

  Удаляем блок целиком. Заменяющий комментарий на его месте:

  ```ts
  // Check 6 (orphaned loop-end) удалён в Phase 43 D-10 — LoopEndNode больше не существует
  // как живой kind в валидных канвасах. Legacy loop-end узлы отклоняются Migration Check'ом
  // (см. выше, D-07), до достижения этой точки.
  ```

  **5. Обновить `nodeLabel()` switch (D-11)** — добавить `case 'loop':` ветвь, сохранить `case 'loop-start':` / `case 'loop-end':` (legacy нужны для Migration Check'а чтобы он мог перечислить узлы):

  Найти (строки ~192-203):
  ```ts
  private nodeLabel(node: RPNode): string {
    switch (node.kind) {
      case 'start': return `start (${node.id})`;
      case 'question': return node.questionText || node.id;
      case 'answer': return (node.displayLabel ?? node.answerText) || node.id;
      case 'free-text-input': return node.promptLabel || node.id;
      case 'text-block': return node.content.slice(0, 30) || node.id;
      case 'loop-start': return node.loopLabel || node.id;
      case 'loop-end': return `loop-end (${node.id})`;
      case 'snippet': return node.subfolderPath ? `snippet (${node.subfolderPath})` : 'snippet (root)';
    }
  }
  ```

  Заменить НА (добавлена `case 'loop':`, остальное — без изменений):
  ```ts
  /**
   * Returns a human-readable label for a node (for use in error messages).
   */
  private nodeLabel(node: RPNode): string {
    switch (node.kind) {
      case 'start': return `start (${node.id})`;
      case 'question': return node.questionText || node.id;
      case 'answer': return (node.displayLabel ?? node.answerText) || node.id;
      case 'free-text-input': return node.promptLabel || node.id;
      case 'text-block': return node.content.slice(0, 30) || node.id;
      case 'loop-start': return node.loopLabel || node.id;                              // @deprecated Phase 43 D-CL-05
      case 'loop-end': return `loop-end (${node.id})`;                                  // @deprecated Phase 43 D-CL-05
      case 'snippet': return node.subfolderPath ? `snippet (${node.subfolderPath})` : 'snippet (root)';
      case 'loop': return node.headerText || node.id;                                   // Phase 43 D-11 (LOOP-02)
    }
  }
  ```

  **НЕ ТРОГАТЬ:**
  - Check 1 & 2 (start nodes) — строки ~14-34
  - Check 3 (reachability) — строки ~36-53
  - Check 4 (cycles) — строки ~55-58 + `detectUnintentionalCycles` функция (кроме единственной правки в Task 3 выше)
  - Check 5 (dead-end questions) — строки ~60-71
  - TODO Phase 5 комментарий — строки ~86-93
  - `bfsReachable()` helper — строки ~98-119
  - Файл-level комментарий `// Pure module — zero Obsidian API imports (PARSE-07, NFR-01)`
  - Импорт `import type { ProtocolGraph, RPNode } from './graph-model';`
  </action>

  <acceptance_criteria>
    - Migration Check присутствует. D-07 даёт planner/executor люфт финальной формулировки, но 4 лексемы + устаревший-маркер обязательны (D-07 a-d). Проверяются толерантно — finalize executor может переформулировать вступительное слово:
      ```bash
      grep -q "Phase 43 D-07" src/graph/graph-validator.ts
      grep -q "loop-start" src/graph/graph-validator.ts
      grep -q "loop-end" src/graph/graph-validator.ts
      grep -q "узлом loop" src/graph/graph-validator.ts
      grep -q "«выход»" src/graph/graph-validator.ts
      grep -qiE "устаревш|legacy" src/graph/graph-validator.ts
      ```
      Note: симметрично с толерантным матчером в Plan 07 Task 3 (`expect(e.includes('loop-start') && ...)`). Exact-match фразы «Канвас содержит устаревшие узлы...» НЕ требуется — finalize executor волен переформулировать (D-CL-01 люфт).
    - Migration Check делает early-return (ищем `return errors;` после migration-push):
      ```bash
      grep -Pzo "legacyLoopNodes\.length > 0[^{]*\{[^}]*errors\.push[^}]*return errors;" src/graph/graph-validator.ts | grep -q "return errors;"
      ```
    - LOOP-04 три substeps на месте:
      ```bash
      grep -q "не имеет ребра «выход»" src/graph/graph-validator.ts
      grep -q "имеет несколько рёбер «выход»" src/graph/graph-validator.ts
      grep -q "не имеет ни одной body-ветви" src/graph/graph-validator.ts
      grep -q "exitEdges = outgoing.filter(e => e.label === 'выход')" src/graph/graph-validator.ts
      grep -q "bodyEdges = outgoing.filter(e => e.label !== 'выход')" src/graph/graph-validator.ts
      ```
    - Cycle marker обновлён:
      ```bash
      grep -q "passesViaLoopNode" src/graph/graph-validator.ts
      ! grep "passesViaLoopEnd" src/graph/graph-validator.ts
      grep -q "n?.kind === 'loop'" src/graph/graph-validator.ts
      grep -q "Cycles must pass through a loop node" src/graph/graph-validator.ts
      ! grep "loop-end node" src/graph/graph-validator.ts
      ```
    - Check 6 (orphaned loop-end) удалён (в .ts больше нет `matchingLoopStart` / `node.loopStartId` вне migration-path):
      ```bash
      ! grep "matchingLoopStart" src/graph/graph-validator.ts
      ! grep "Orphaned loop-end" src/graph/graph-validator.ts
      # исходный текст ошибки `Loop-end node "..." references loop-start` не должен присутствовать:
      ! grep "references loop-start" src/graph/graph-validator.ts
      ```
    - `nodeLabel()` содержит `case 'loop':` и сохранённые legacy:
      ```bash
      grep -q "case 'loop': return node.headerText" src/graph/graph-validator.ts
      grep -q "case 'loop-start': return node.loopLabel" src/graph/graph-validator.ts
      grep -q "case 'loop-end':" src/graph/graph-validator.ts
      grep -q "case 'snippet':" src/graph/graph-validator.ts
      ```
    - Check 1..5 и TODO Phase 5 комментарий сохранены:
      ```bash
      grep -q "Check 1 & 2" src/graph/graph-validator.ts
      grep -q "Check 3: Reachability" src/graph/graph-validator.ts
      grep -q "Check 4: Unintentional cycles" src/graph/graph-validator.ts
      grep -q "Check 5: Dead-end questions" src/graph/graph-validator.ts
      grep -q "TODO: Phase 5" src/graph/graph-validator.ts
      ```
    - Файл компилируется и весь build проходит:
      ```bash
      ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "graph-validator.ts")
      npm run build > /tmp/build-plan05.log 2>&1   # exit 0
      ```
    - Phase-markers на месте:
      ```bash
      grep -q "Phase 43 D-07" src/graph/graph-validator.ts
      grep -q "Phase 43 D-09" src/graph/graph-validator.ts
      grep -q "Phase 43 D-10" src/graph/graph-validator.ts
      grep -q "Phase 43 D-11" src/graph/graph-validator.ts
      ```
  </acceptance_criteria>

  <verify>
    <automated>
      # Ключевые утверждения
      grep -q "«выход»" src/graph/graph-validator.ts \
        && grep -q "Канвас содержит устаревшие узлы loop-start/loop-end" src/graph/graph-validator.ts \
        && grep -q "не имеет ребра «выход»" src/graph/graph-validator.ts \
        && grep -q "имеет несколько рёбер «выход»" src/graph/graph-validator.ts \
        && grep -q "не имеет ни одной body-ветви" src/graph/graph-validator.ts \
        && grep -q "passesViaLoopNode" src/graph/graph-validator.ts \
        && ! grep "passesViaLoopEnd" src/graph/graph-validator.ts \
        && ! grep "matchingLoopStart" src/graph/graph-validator.ts \
        && grep -q "case 'loop': return node.headerText" src/graph/graph-validator.ts \
        && grep -q "TODO: Phase 5" src/graph/graph-validator.ts \
        && grep -q "Phase 43 D-07" src/graph/graph-validator.ts \
        && ! (npx tsc --noEmit --skipLibCheck 2>&1 | grep "graph-validator.ts") \
        && npm run build > /dev/null 2>&1
    </automated>
  </verify>

  <done>
    - Migration Check (D-07) реализован с early-return, русским текстом и всеми обязательными литералами («loop-start», «loop-end», «loop», «выход»)
    - LOOP-04 три substeps (D-08.1/2/3) реализованы
    - Cycle detection (D-09) обновлён — маркер `kind === 'loop'`
    - Check 6 (D-10) удалён
    - nodeLabel() (D-11) содержит `case 'loop':`, сохранены все существующие cases
    - `bfsReachable`, Checks 1..5, TODO Phase 5 — нетронуты
    - Весь проект собирается (`npm run build` exit 0)
  </done>
</task>

</tasks>

<verification>
  - Ручной smoke-test сценариев (до автоматических тестов в Plan 07):
    1. fixture `loop-start.canvas` → parser строит LoopStartNode → validator → migration-error с «loop-start», «loop-end», «loop», «выход»
    2. fixture `unified-loop-valid.canvas` (из Plan 06) → validator → 0 errors
    3. fixture `unified-loop-missing-exit.canvas` → validator → ошибка D-08.1
    4. fixture `unified-loop-duplicate-exit.canvas` → validator → ошибка D-08.2
    5. fixture `unified-loop-no-body.canvas` → validator → ошибка D-08.3
  - Все точные сверки покрываются автоматически в Plan 07 (D-19 tests)
  - `npm run build` exit 0 (полный build) — контрольная точка wave 3
</verification>

<success_criteria>
  - Migration-error текст содержит дословно все 4 литерала: `loop-start`, `loop-end`, `loop`, `«выход»`
  - LOOP-04 три substeps генерируют отдельные errors с соответствующими русскими текстами и edge IDs для duplicate
  - `grep -q "passesViaLoopNode" src/graph/graph-validator.ts` → exit 0
  - `! grep "matchingLoopStart" src/graph/graph-validator.ts` → exit 0
  - `! grep "Check 6:" src/graph/graph-validator.ts` → exit 0 (или заменён на комментарий про удаление)
  - `grep -q "case 'loop': return node.headerText" src/graph/graph-validator.ts` → exit 0
  - `npm run build` → exit 0
  - Commit содержит ТОЛЬКО `src/graph/graph-validator.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/43-unified-loop-graph-model-parser-validator-migration-errors/43-05-SUMMARY.md`
</output>
