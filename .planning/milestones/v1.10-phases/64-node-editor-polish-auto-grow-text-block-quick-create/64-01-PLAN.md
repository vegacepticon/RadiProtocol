---
phase: 64-node-editor-polish-auto-grow-text-block-quick-create
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/__tests__/editor-panel-forms.test.ts
  - src/__tests__/editor-panel-create.test.ts
autonomous: true
requirements:
  - EDITOR-04
  - EDITOR-06
must_haves:
  truths:
    - "Automated tests fail until every authored Node Editor multiline field uses the shared auto-grow behavior."
    - "Automated tests fail until the toolbar exposes and wires a Create text block quick-create button."
  artifacts:
    - path: "src/__tests__/editor-panel-forms.test.ts"
      provides: "EDITOR-04 regression tests for grow-on-load, grow-on-input, and shrink-on-delete across Question, Answer, Text block, Loop, and Snippet branch label fields"
      contains: "Phase 64"
    - path: "src/__tests__/editor-panel-create.test.ts"
      provides: "EDITOR-06 regression tests for Create text block button rendering, accessibility copy, factory call, and in-memory form load"
      contains: "text-block"
  key_links:
    - from: "src/__tests__/editor-panel-forms.test.ts"
      to: "src/views/editor-panel-view.ts"
      via: "private buildKindForm/renderForm tests"
      pattern: "buildKindForm.*(question|answer|text-block|loop|snippet)"
    - from: "src/__tests__/editor-panel-create.test.ts"
      to: "src/views/editor-panel-view.ts"
      via: "onQuickCreate and renderToolbar tests"
      pattern: "onQuickCreate.*text-block"
---

<objective>
Create the Phase 64 RED regression surface before implementation.

Purpose: Lock EDITOR-04 and EDITOR-06 behavior so executors cannot partially implement auto-grow or quick-create and still pass tests.
Output: Failing or newly expanded Vitest coverage in the existing Node Editor test files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-CONTEXT.md
@.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-UI-SPEC.md
@src/views/editor-panel-view.ts
@src/styles/editor-panel.css
@src/__tests__/editor-panel-forms.test.ts
@src/__tests__/editor-panel-create.test.ts

<interfaces>
From `src/views/editor-panel-view.ts`:
```typescript
private buildKindForm(container: HTMLElement, nodeRecord: Record<string, unknown>, kind: RPNodeKind | null): void;
private async onQuickCreate(kind: 'question' | 'answer' | 'snippet' | 'loop'): Promise<void>;
private renderToolbar(container: HTMLElement): void;
```

Existing pending-edit keys that tests must protect per D1/D2:
```typescript
question: radiprotocol_questionText + text
answer: radiprotocol_answerText + text
text-block: radiprotocol_content + text
loop: radiprotocol_headerText + text
snippet branch label: radiprotocol_snippetLabel, empty string => undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend auto-grow textarea RED tests</name>
  <files>src/__tests__/editor-panel-forms.test.ts</files>
  <behavior>
    - Question, Answer, Text block, Loop, and Snippet branch label fields each render as managed growable textareas per D1 and D2.
    - Each managed textarea sizes on initial render by setting height from `scrollHeight` inside the requestAnimationFrame path.
    - Each managed textarea input event sets `height = 'auto'` before setting `height = scrollHeight + 'px'`, allowing growth and shrink.
    - Input events preserve existing pending edit keys exactly, including Snippet branch label empty string => `undefined`.
  </behavior>
  <action>Extend the existing Phase 48 `NODEUI-04` tests instead of replacing them (D6). Upgrade the fake DOM helpers only as needed to capture multiple textarea instances, their labels/classes, requestAnimationFrame sizing, input callbacks, and height assignment history. Add Phase 64-specific test cases covering Answer text, Text block Content, Loop Header text, and Snippet Branch label in addition to preserving the existing Question tests. Do not change production code in this plan.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-forms.test.ts</automated>
  </verify>
  <done>The forms test file contains Phase 64 RED coverage for all five named fields and preserves existing Phase 48 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend text-block quick-create RED tests</name>
  <files>src/__tests__/editor-panel-create.test.ts</files>
  <behavior>
    - Toolbar rendering includes a native button with visible text, `aria-label`, and `title` all matching `Create text block` per D3/D4 and UI-SPEC.
    - Clicking or invoking quick-create for text-block calls `CanvasNodeFactory.createNode(canvasPath, 'text-block', currentNodeId)` through the existing `onQuickCreate` path per D3.
    - Successful text-block creation renders the Text-block form from `result.canvasNode.getData()` without waiting for disk flush.
  </behavior>
  <action>Extend `EditorPanelView quick-create` coverage in the existing test file. Add a direct `onQuickCreate('text-block')` test, a successful-create render test using `radiprotocol_nodeType: 'text-block'`, and a toolbar render test that captures the new button's copy/accessibility attributes. Do not duplicate `CanvasNodeFactory` placement/color tests because D6 says those remain canonical unless the factory changes.</action>
  <verify>
    <automated>npm test -- src/__tests__/editor-panel-create.test.ts</automated>
  </verify>
  <done>The create test file fails before implementation if text-block is missing from the quick-create union, toolbar, factory call, or in-memory render path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → private view methods | Tests invoke private methods with mocked DOM and Obsidian APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | Tampering | Test mocks | mitigate | Keep tests scoped to existing `EditorPanelView` contracts and assert exact pending edit keys from D1/D2 so mock-only behavior cannot redefine production semantics. |
| T-64-01-02 | Repudiation | Regression coverage | mitigate | Name tests with EDITOR-04/EDITOR-06 and Phase 64 markers so verification traces failures to the source requirement. |
</threat_model>

<verification>
Run both targeted test files after adding RED coverage. The suite may fail until Plans 02 and 03 implement the behavior; failures must point to missing production behavior, not malformed test setup.
</verification>

<success_criteria>
- `src/__tests__/editor-panel-forms.test.ts` proves all five fields are required to share auto-grow behavior.
- `src/__tests__/editor-panel-create.test.ts` proves text-block quick-create must use the existing factory path and render from in-memory data.
- Existing Phase 48/39/42 tests are extended, not replaced.
</success_criteria>

<output>
After completion, create `.planning/phases/64-node-editor-polish-auto-grow-text-block-quick-create/64-01-SUMMARY.md`.
</output>
