---
phase: 68-github-release-v1-10-0
plan: 04
type: execute
wave: 4
depends_on: [68-03]
files_modified:
  - .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md
autonomous: false
requirements: [RELEASE-READY-V1.10]
must_haves:
  truths:
    - "Runbook exists at .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md"
    - "Runbook opens with a blocking Phase 66 UAT gate and stops release if 66-UAT.md is not complete/pass"
    - "Runbook instructs the operator to run v1.10.0 preflight before pushing or publishing"
    - "Runbook instructs GitHub Release title/tag to be exactly 1.10.0 and assets to be loose manifest.json, main.js, styles.css"
    - "Release notes cover Phases 63-67 and their v1.10 requirement IDs without including deferred future work"
    - "Human operator publishes or verifies the GitHub Release and performs BRAT smoke verification"
  artifacts:
    - path: ".planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md"
      provides: "Copy-pasteable v1.10.0 release procedure and post-publish verification record"
      contains: "## Pre-publish check — Phase 66 UAT gate"
  key_links:
    - from: "runbook Phase 66 gate"
      to: ".planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md"
      via: "status must be complete and summary issues/pending must be 0"
      pattern: "status: complete"
    - from: "runbook Release step"
      to: "GitHub Release 1.10.0"
      via: "web UI upload of loose assets"
      pattern: "manifest.json.*main.js.*styles.css"
    - from: "runbook BRAT smoke test"
      to: "vegacepticon/RadiProtocol"
      via: "BRAT add-plugin dialog"
      pattern: "vegacepticon/RadiProtocol"
---

<objective>
Create and execute the v1.10.0 release runbook handoff: Phase 66 UAT gate, preflight, push/tag publication steps, curated release notes for Phases 63-67, GitHub Release asset checklist, and BRAT smoke verification.

Purpose: The final release action has a human-only web UI component and a real Obsidian/BRAT verification step. This plan makes that manual path deterministic, auditable, and gated on the last v1.10 UAT evidence.

Output: `68-RELEASE-RUNBOOK.md`, then a blocking human action checkpoint for GitHub Release + BRAT verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md
@.planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md
@.planning/phases/68-github-release-v1-10-0/68-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 68-04-01: Author v1.10.0 release runbook</name>
  <files>.planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md</files>
  <read_first>
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-RELEASE-RUNBOOK.md (direct template)
    - .planning/ROADMAP.md (Phase 63-68 details and success criteria)
    - .planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md (blocking gate evidence)
    - .planning/phases/67-inline-runner-resizable-modal-file-bound-snippet-parity/67-UAT.md (release note evidence)
  </read_first>
  <action>Create `.planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md` with these sections in order: YAML frontmatter (`phase`, `type: release-runbook`, `status: ready`, `performed_on: pending`, `final_verdict: pending`), title `# Phase 68 — v1.10.0 Release Runbook`, `## Pre-publish check — Phase 66 UAT gate`, `## Preflight (automated)`, `## Push commit and tag`, `## Create the GitHub Release (web UI)`, `## Release Notes`, `## Post-release verification`, and `## Anti-patterns (do NOT do any of these)`. The Phase 66 gate must instruct the operator to confirm `.planning/phases/66-runner-step-back-reliability-scroll-pinning/66-UAT.md` has `status: complete`, `passed: 9`, `issues: 0`, `pending: 0`, and that ROADMAP no longer describes Phase 66 UAT as pending. The release notes must include: Phase 63 EDITOR-03/EDITOR-05 bidirectional Canvas↔Node Editor sync; Phase 64 EDITOR-04/EDITOR-06 auto-grow textareas and text-block quick-create; Phase 65 RUNNER-02 Back/Skip footer layout; Phase 66 RUNNER-03/RUNNER-04 step-back reliability and scroll pinning; Phase 67 INLINE-FIX-06/INLINE-FIX-07 resizable Inline Runner and file-bound snippet parity; Distribution v1.10.0 with BRAT identifier `vegacepticon/RadiProtocol`. The GitHub Release section must instruct title/tag exactly `1.10.0`, loose assets `manifest.json`, `main.js`, `styles.css`, normal Latest release, not prerelease after gates pass.</action>
  <verify>
    <automated>test -f .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fxq '## Pre-publish check — Phase 66 UAT gate' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fxq '## Preflight (automated)' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fxq '## Create the GitHub Release (web UI)' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'git push origin 1.10.0' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'vegacepticon/RadiProtocol' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'EDITOR-03' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'RUNNER-04' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md && grep -Fq 'INLINE-FIX-07' .planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - Runbook file exists.
    - Required sections appear exactly as headings.
    - Runbook contains `1.10.0`, `git push origin 1.10.0`, `manifest.json`, `main.js`, `styles.css`, and `vegacepticon/RadiProtocol`.
    - Runbook contains all v1.10 requirement IDs: `EDITOR-03`, `EDITOR-04`, `EDITOR-05`, `EDITOR-06`, `RUNNER-02`, `RUNNER-03`, `RUNNER-04`, `INLINE-FIX-06`, `INLINE-FIX-07`.
    - Runbook frontmatter includes `final_verdict: pending`.
  </acceptance_criteria>
  <done>Runbook is ready, complete, and verified with exact strings for the UAT gate, publish steps, release notes, and BRAT smoke test.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 68-04-02: Publish GitHub Release and verify BRAT install</name>
  <what-built>The agent created the v1.10.0 release runbook and Plan 02 prepared the local release commit/tag/artifacts.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md`.
    2. Complete the Phase 66 UAT gate exactly as written. Proceed only if `66-UAT.md` is complete/pass and ROADMAP no longer marks Phase 66 UAT pending.
    3. Run `bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh`; final line must be `SC-1 local verification: PASS`.
    4. Push `main` and tag `1.10.0` as written in the runbook.
    5. In GitHub web UI, create Release title/tag exactly `1.10.0`; upload loose root assets `manifest.json`, `main.js`, `styles.css`; publish as Latest, not prerelease.
    6. Run the runbook's API smoke check or browser asset check; assets must be exactly `main.js,manifest.json,styles.css`.
    7. Install via BRAT in a clean Obsidian vault using `vegacepticon/RadiProtocol`; enable RadiProtocol; confirm plugin version `1.10.0`, Runner view opens, Node Editor/Runner/Inline smoke checks listed in the runbook pass.
    8. Update runbook frontmatter `performed_on` and `final_verdict: success` or `failed` with notes, then commit that runbook update.
  </how-to-verify>
  <resume-signal>Type `approved` after Release + BRAT smoke verification pass, or describe the failed step and observed error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook → human release operator | Any ambiguity can cause a bad GitHub Release, wrong tag, or missing asset. |
| GitHub web UI → BRAT users | Public Release assets are downloaded by users via BRAT. |
| UAT docs → release readiness | Phase 66 UAT status controls whether v1.10.0 can honestly ship as Latest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03-01 | T | publishing without Phase 66 UAT | mitigate | Runbook first operational section is a blocking Phase 66 UAT gate; checkpoint repeats it before publish. |
| T-68-03-02 | S | wrong release title/tag | mitigate | Runbook and checkpoint require exact `1.10.0`; anti-patterns forbid `v1.10.0` and suffixes. |
| T-68-03-03 | D | zipped/missing assets break BRAT | mitigate | Runbook specifies three loose assets and includes API/browser asset verification. |
| T-68-03-04 | R | no evidence release was verified | mitigate | Checkpoint requires runbook final_verdict update and commit after BRAT smoke verification. |
</threat_model>

<verification>
Plan-level verification before the human checkpoint:

```bash
RUNBOOK=.planning/phases/68-github-release-v1-10-0/68-RELEASE-RUNBOOK.md
test -f "$RUNBOOK"
grep -Fxq '## Pre-publish check — Phase 66 UAT gate' "$RUNBOOK"
grep -Fq 'bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh' "$RUNBOOK"
grep -Fq 'git push origin 1.10.0' "$RUNBOOK"
grep -Fq 'vegacepticon/RadiProtocol' "$RUNBOOK"
```
</verification>

<success_criteria>
- Runbook exists with Phase 66 UAT gate, preflight, push/tag, GitHub web UI release, release notes, BRAT smoke verification, and anti-pattern sections.
- Human checkpoint completes only after GitHub Release `1.10.0` exists with loose root assets `manifest.json`, `main.js`, `styles.css`.
- BRAT clean-vault install succeeds and RadiProtocol enables at version `1.10.0`.
- Runbook final verdict is updated and committed after verification.
</success_criteria>

<output>
After completion, create `.planning/phases/68-github-release-v1-10-0/68-04-SUMMARY.md` with runbook path, release URL, asset verification result, BRAT smoke result, final verdict commit SHA, and any deviations.
</output>
