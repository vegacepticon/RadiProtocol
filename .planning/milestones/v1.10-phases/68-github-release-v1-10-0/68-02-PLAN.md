---
phase: 68-github-release-v1-10-0
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - main.js
  - styles.css
  - .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh
autonomous: true
requirements: [RELEASE-READY-V1.10]
must_haves:
  truths:
    - "npm run build exits 0 after the v1.10.0 version bump"
    - "main.js and styles.css exist at repo root and are non-empty generated artifacts"
    - "main.js and styles.css remain gitignored and are not staged or committed"
    - "A v1.10.0 release-preflight script exists and exits 0"
    - "Preflight script is ready for the commit/tag plan to use"
  artifacts:
    - path: "main.js"
      provides: "Production plugin bundle generated by esbuild"
      min_lines: 1
    - path: "styles.css"
      provides: "Generated production CSS bundle"
      min_lines: 1
    - path: ".planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh"
      provides: "Aggregated local release verification for v1.10.0"
      contains: "SC-1 local verification: PASS"
  key_links:
    - from: "preflight script"
      to: "version files, tag state, build artifacts, gitignore hygiene"
      via: "single bash script run before any publish step"
      pattern: "SC-1 local verification: PASS"
---

<objective>
Build the v1.10.0 production artifacts and author the local preflight script.

Purpose: This produces the generated assets and reusable verification harness consumed by Plan 03's atomic release-prep commit and tag.

Output: Green build artifacts and a v1.10.0 preflight script. No commit, tag, or push happens in this plan.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-github-release-v1-10-0/68-01-PLAN.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-02-PLAN.md
@.planning/milestones/v1.9-phases/62-brat-release-v1.9.0/scripts/release-preflight.sh
@.gitignore
@package.json

<interfaces>
Phase 62 precedent locked the BRAT tag convention: use an unprefixed SemVer tag (`1.10.0`), not `v1.10.0`. Build artifacts `main.js` and `styles.css` are generated at repo root by `npm run build` and must remain ignored.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 68-02-01: Build production artifacts against v1.10.0 metadata</name>
  <files>main.js, styles.css</files>
  <read_first>
    - package.json (build script)
    - manifest.json
    - versions.json
    - .gitignore
    - CLAUDE.md (build process and generated styles.css rule)
  </read_first>
  <action>Before building, assert `manifest.json.version`, `package.json.version`, and `versions.json['1.10.0']` all equal the target values from Plan 01. Then run `npm run build`, which invokes `tsc -noEmit -skipLibCheck && node esbuild.config.mjs production`. Do not edit source files to fix unrelated failures inside this release plan; a build failure means an upstream phase left the repo unshippable and must be reported before publication.</action>
  <verify>
    <automated>node -e "const m=require('./manifest.json'),p=require('./package.json'),v=require('./versions.json'); if(m.version!=='1.10.0'||p.version!=='1.10.0'||v['1.10.0']!=='1.5.7') process.exit(1)"; npm run build; node -e "const fs=require('fs'); for (const f of ['main.js','styles.css']) if(!fs.existsSync(f)||fs.statSync(f).size===0) throw new Error(f+' missing/empty'); console.log('build artifacts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `node -e "require('fs').statSync('main.js').size>0||process.exit(1)"` exits 0.
    - `node -e "require('fs').statSync('styles.css').size>0||process.exit(1)"` exits 0.
    - `git status --porcelain main.js styles.css` prints nothing because both are ignored/generated assets.
  </acceptance_criteria>
  <done>Production artifacts are regenerated, non-empty, and not tracked by git.</done>
</task>

<task type="auto">
  <name>Task 68-02-02: Author v1.10.0 release-preflight script</name>
  <files>.planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh</files>
  <read_first>
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/scripts/release-preflight.sh
    - .planning/milestones/v1.9-phases/62-brat-release-v1.9.0/62-02-PLAN.md
    - .gitignore
  </read_first>
  <action>Create `.planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh` adapted from Phase 62. It must use `#!/usr/bin/env bash`, `set -euo pipefail`, assert `manifest.json.version === 1.10.0`, `package.json.version === 1.10.0`, `versions.json` keys exactly include `1.8.0`, `1.9.0`, and `1.10.0` mapped to `1.5.7`, assert manifest metadata remains unchanged, accept a missing `1.10.0` tag before Task 68-02-04 while rejecting any `v1.10.0` tag, assert `main.js` and `styles.css` are ignored/not indexed, run `npm run build`, assert both artifacts are non-empty, and end with the exact line `SC-1 local verification: PASS`.</action>
  <verify>
    <automated>test -f .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh && grep -Fq 'set -euo pipefail' .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh && grep -Fq '1.10.0' .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh && grep -Fq 'v1.10.0' .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh && grep -Fq 'SC-1 local verification: PASS' .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh && bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script file exists at `.planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh`.
    - Script contains `1.10.0`, `v1.10.0`, `1.8.0`, `1.9.0`, `main.js`, `styles.css`, and `SC-1 local verification: PASS`.
    - `bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh` exits 0 before the tag exists and prints that missing `1.10.0` is acceptable pre-tag.
  </acceptance_criteria>
  <done>Preflight script exists and passes on the pre-tag release state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated artifacts → public GitHub assets | `main.js` and `styles.css` are uploaded later as public release assets. |
| Preflight script → release operator | The script is the release operator's local truth gate before pushing/publishing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-01 | D | BRAT install broken by missing generated assets | mitigate | Build task and preflight assert non-empty `main.js` and `styles.css`. |
| T-68-02-02 | I | accidental secret in assets | accept | Project has no env-secret bundling path; `.env` is ignored and preflight does not read it. Risk accepted as low. |
</threat_model>

<verification>
End-to-end local release readiness:

```bash
bash .planning/phases/68-github-release-v1-10-0/scripts/release-preflight.sh
test -s main.js
test -s styles.css
```
</verification>

<success_criteria>
- Production build passes and generated assets exist.
- Preflight script exists and passes before tag creation.
- No release-prep commit, tag, push, or GitHub Release publication has happened yet.
</success_criteria>

<output>
After completion, create `.planning/phases/68-github-release-v1-10-0/68-02-SUMMARY.md` with build output status, preflight stdout, artifact sizes, and confirmation that no commit/tag/push happened in this plan.
</output>
