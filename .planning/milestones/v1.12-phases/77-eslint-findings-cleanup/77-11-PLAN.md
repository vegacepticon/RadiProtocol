---
phase: 77
plan: 11
type: execute
wave: 6
depends_on: ["01", "07", "08"]
files_modified:
  - src/views/editor-panel-view.ts
  - src/views/inline-runner-modal.ts
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "3 × no-tfile-tfolder-cast cleared at editor-panel-view.ts:284, editor-panel-view.ts:381, inline-runner-modal.ts:599"
    - "Each `as TFile` cast replaced by an `instanceof TFile` runtime guard with early-return-on-false"
    - "Vault read calls below the guard receive the narrowed TFile type — no runtime regression"
    - "Phase 63 canvas-sync + Phase 67 inline-runner behavior preserved (both files participate in those features)"
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "instanceof TFile guards at lines 284 + 381"
    - path: "src/views/inline-runner-modal.ts"
      provides: "instanceof TFile guard at line 599"
  key_links: []
---

<objective>
Clear 3 × `obsidianmd/no-tfile-tfolder-cast` lint errors per RESEARCH.md inventory:
- `editor-panel-view.ts:284` — `await this.plugin.app.vault.read(file as TFile)` style cast
- `editor-panel-view.ts:381` — same shape
- `inline-runner-modal.ts:599` — same shape

Replace each `as TFile` cast with an `instanceof TFile` runtime guard returning early on the false branch, per existing in-file precedent at `editor-panel-view.ts:400` and at `main.ts:419`.

Plan 11 runs in Wave 6 AFTER Plans 07 and 08 (which also touch these files for static-styles migrations). Sequencing prevents race-condition diffs in the «accumulated files» CLAUDE.md flags as regression-prone.

Per CLAUDE.md, diff to each file is bounded strictly to the cast-fix region (lines 280-290, 375-385 in editor-panel-view.ts; lines 595-605 in inline-runner-modal.ts).

Purpose: ROADMAP SC#1 (lint exit 0) + type-safety improvement.
Output: 3 cast-to-guard rewrites; vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/views/editor-panel-view.ts
@src/views/inline-runner-modal.ts

<interfaces>
<!-- In-file precedent (editor-panel-view.ts:400, also main.ts:419): -->
```typescript
if (!(file instanceof TFile)) {
  // user-facing error or early return
  new Notice('Could not read canvas file (not a file).');
  return;
}
// 'file' narrowed to TFile from here
raw = await this.plugin.app.vault.read(file);
```

Site-specific notes (per RESEARCH.md):
- editor-panel-view.ts:284 — inside an async method that reads canvas. Early return appropriate; user-facing Notice optional but good UX.
- editor-panel-view.ts:381 — different async method but same shape.
- inline-runner-modal.ts:599 — inside the inline runner's vault read path. Behavior on guard-fail must NOT crash the modal — early return + Notice is correct.

Pattern §13 from PATTERNS.md confirms: «Identical shape at all 3 call sites».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 3 × `as TFile` casts with instanceof TFile guards</name>
  <files>
    src/views/editor-panel-view.ts
    src/views/inline-runner-modal.ts
  </files>
  <read_first>
    - src/views/editor-panel-view.ts:280-290 (line 284 cast site)
    - src/views/editor-panel-view.ts:375-385 (line 381 cast site)
    - src/views/editor-panel-view.ts:395-410 (the existing line 400 instanceof precedent)
    - src/views/inline-runner-modal.ts:595-605 (line 599 cast site)
    - src/main.ts:415-425 (line 419 instanceof precedent)
    - .planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md §13
    - .planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md §«Code Examples» Example 4
  </read_first>
  <action>
    For EACH of the 3 cast sites, apply the SAME shape per the existing line 400 / line 419 precedent.

    **Site 1 — `src/views/editor-panel-view.ts:284`:**
    Before:
    ```typescript
    raw = await this.plugin.app.vault.read(file as TFile);
    ```
    After (verify the surrounding context to determine whether `file` is the variable name; adjust accordingly):
    ```typescript
    if (!(file instanceof TFile)) {
      new Notice('Could not read canvas file (not a file).');
      return;
    }
    raw = await this.plugin.app.vault.read(file);
    ```
    Place the guard immediately above the existing read call.

    Confirm `TFile` is already imported at the top of the file (`import { TFile } from 'obsidian'` or similar). If not, ADD it — but the file likely already imports TFile because the cast was using it.

    **Site 2 — `src/views/editor-panel-view.ts:381`:**
    Same shape — add `if (!(file instanceof TFile))` guard immediately above the cast site; remove the `as TFile` cast.

    **Site 3 — `src/views/inline-runner-modal.ts:599`:**
    Same shape. Confirm `TFile` is imported in this file (Read line 1-30); add import if missing.

    Per CLAUDE.md, the diff to each file is STRICTLY bounded to ~5-10 lines around each cast site. Do NOT touch unrelated functions.

    **Commit message:**
    ```
    refactor(views): replace 3 × `as TFile` casts with instanceof TFile guards

    Migrates obsidianmd/no-tfile-tfolder-cast violations:
    - editor-panel-view.ts:284
    - editor-panel-view.ts:381
    - inline-runner-modal.ts:599

    Each cast `vault.read(file as TFile)` becomes a runtime guard
    `if (!(file instanceof TFile)) { Notice; return; }` followed by
    the unchanged vault.read(file) call (file now narrowed to TFile).
    Pattern matches existing in-file precedent at editor-panel-view.ts:400
    and main.ts:419.

    No runtime behavior change in the happy path. The guard adds a
    safer fail-soft branch (Notice + return) when called with a
    non-file Path.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/views/editor-panel-view.ts src/views/inline-runner-modal.ts 2>&1 | grep -c "no-tfile-tfolder-cast"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `grep -c "as TFile" src/views/editor-panel-view.ts` returns 0 (or strictly less than the pre-edit count if the file had unrelated `as TFile` uses elsewhere — verify pre-edit count first).
    - `grep -c "as TFile" src/views/inline-runner-modal.ts` returns 0 at line 599 region.
    - `grep -c "instanceof TFile" src/views/editor-panel-view.ts` returns ≥ 3 (line 400 precedent + 2 new guards).
    - `grep -c "instanceof TFile" src/views/inline-runner-modal.ts` returns ≥ 1.
    - `npx eslint src/views/editor-panel-view.ts src/views/inline-runner-modal.ts 2>&1 | grep -c "no-tfile-tfolder-cast"` returns 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 — Phase 63 canvas-sync, Phase 67 inline-runner-modal tests pass.
  </acceptance_criteria>
  <done>
    All 3 cast sites converted to instanceof guards; lint clear; tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/views/editor-panel-view.ts src/views/inline-runner-modal.ts` reports 0 `obsidianmd/no-tfile-tfolder-cast` errors.
2. `npm run lint` total decreases by 3.
3. `npm run build` exits 0.
4. `npm test` exits 0 — Phase 63 canvas-sync test (`editor-panel-canvas-sync.test.ts`), Phase 67 inline-runner tests still pass.
</verification>

<success_criteria>
- 3 `as TFile` casts converted to `instanceof TFile` guards.
- Existing in-file precedent (editor-panel-view.ts:400) shape mirrored exactly.
- Build + tests green.
- No diff outside the 3 cast-site regions.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-11-SUMMARY.md` with:
- 3 sites listed with before/after snippets.
- Confirmation `npx eslint` shows 0 no-tfile-tfolder-cast errors for both files.
- Test results.
</output>
</content>
</invoke>