---
phase: 77
plan: 04
type: execute
wave: 3
depends_on: ["01", "03"]
files_modified:
  - src/views/snippet-manager-view.ts
  - src/styles/snippet-manager.css
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "snippet-manager-view.ts no longer contains literal `style.display = 'none'/'inline-block'`, `style['display'] = ...`, `style.width = '12px'` at lines 276, 286, 287, 361, 911, 937"
    - "Display toggles use el.toggleClass('rp-snippet-tree-{state}', boolean) per D-04 + D-05"
    - "Spacer uses class `rp-snippet-tree-spacer { width: 12px }` per D-04"
    - "src/styles/snippet-manager.css gains a Phase 77 block at the END (separate from Plan 03's Phase 77 block — both are appended sequentially)"
    - "Line 275 template-literal `${depth * 16}px` is NOT migrated (Pitfall 7 — rule does not flag template literals)"
    - "Existing snippet-manager-view vitest tests stay green"
  artifacts:
    - path: "src/views/snippet-manager-view.ts"
      provides: "Class-based show/hide of inline-rename label + tree spacer"
    - path: "src/styles/snippet-manager.css"
      provides: "Phase 77 snippet-tree-* rules"
      contains: "rp-snippet-tree-spacer"
  key_links:
    - from: "src/views/snippet-manager-view.ts"
      to: "src/styles/snippet-manager.css"
      via: "rp-snippet-tree-* classes"
      pattern: "rp-snippet-tree-(spacer|label-hidden)"
---

<objective>
Migrate the 6 `obsidianmd/no-static-styles-assignment` violations in `src/views/snippet-manager-view.ts` (lines 276, 286, 287, 361, 911, 937) per D-04 first branch.

Plan 04 runs in Wave 3 (sequential after Plan 03 because both append to `src/styles/snippet-manager.css`; serializing prevents concurrent-append conflicts).

Lines 911/937 use `style['display']` (computed property — eslint flags as static literal). Line 287 is `'12px'` literal width. Line 275 is template literal `${depth * 16}px` and is NOT flagged (Pitfall 7); leave it untouched.

Purpose: ROADMAP SC#2 (no-static-styles-assignment cleanup); follows analog at `runner-view.ts:229,427` (`closeBtn.toggleClass('is-hidden', condition)`).
Output: edited `snippet-manager-view.ts`, appended block in `snippet-manager.css`, regenerated `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/views/snippet-manager-view.ts
@src/styles/snippet-manager.css

<interfaces>
<!-- Pitfall 7 from RESEARCH.md: rule only flags Literal type, not TemplateLiteral.
     Line 275 (`${depth * 16}px`) is NOT flagged. Line 287 (`'12px'`) IS flagged. -->

Flagged lines per RESEARCH.md table:
- 276: display = ... literal
- 286: display = ... literal
- 287: spacer.style.width = '12px' (literal, FLAGGED)
- 361: display = ... literal
- 911: el.style['display'] = 'none' or '' (computed prop, FLAGGED)
- 937: el.style['display'] = 'none' or '' (computed prop, FLAGGED)

NOT flagged (leave alone per Pitfall 7):
- 275: indent.style.width = `${depth * 16}px` (template literal — passes rule)

Class names introduced (planner discretion per D-05):
- `rp-snippet-tree-spacer` — replaces line 287's `width = '12px'`. Permanent class on the spacer element.
- `rp-snippet-tree-label-hidden` — toggles inline-rename label hide/show (lines 911/937 region).
- Additional rp-snippet-tree-* classes for lines 276/286/361 as needed per Task 1 mapping.

Existing analog (`runner-view.ts:229`):
```typescript
closeBtn.toggleClass('is-hidden', this.canvasFilePath === null);
```
Per D-05, NEW classes use `rp-` prefix; do NOT introduce new `is-*` classes.

CSS append target: `src/styles/snippet-manager.css` (Plan 03 just appended its own Phase 77 block; Plan 04 appends a SECOND Phase 77 block AFTER Plan 03's. Both blocks coexist with separate `/* Phase 77: ... */` headers.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read snippet-manager-view.ts at flagged lines + map each to class strategy</name>
  <files>(read-only)</files>
  <read_first>
    - src/views/snippet-manager-view.ts (read 270-295, 355-365, 905-945 — the regions containing flagged lines)
    - .planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md §4 «snippet-manager-view.ts:276,286,287,361,911,937»
    - CLAUDE.md (root) — append-only per phase
  </read_first>
  <action>
    Confirm each flagged line's exact `style.x = '<literal>'` assignment and identify:
    - Which DOM element is targeted.
    - Whether the line is a show↔hide pair (collapses into one toggleClass) or a permanent display value (collapses into permanent addClass).
    - The natural state name (`label-hidden`, `actions-hidden`, etc.).
    - Confirm line 275's `${depth * 16}px` template literal is NOT flagged (it stays as-is).

    Build a mapping:
    ```
    Line 276: <elem>.style.display = '<value>' → rp-snippet-tree-<state>, toggleClass(<expr>)
    Line 286: ...
    Line 287: spacer.style.width = '12px' → permanent addClass('rp-snippet-tree-spacer')
    Line 361: ...
    Line 911: el.style['display'] = '<value>' → toggleClass('rp-snippet-tree-label-hidden', <expr>)
    Line 937: ...
    ```
  </action>
  <verify>
    <automated>echo "Read-only mapping task; verification is the executor's confirmation that all 6 flagged lines + the unflagged-line-275 are accounted for."</automated>
  </verify>
  <acceptance_criteria>
    - Mapping table covers lines 276, 286, 287, 361, 911, 937.
    - Line 275 explicitly noted as «NOT migrated — template literal, not flagged» in the mapping.
  </acceptance_criteria>
  <done>
    Mapping ready; ready to apply in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply class migration + append CSS rules + regenerate styles.css</name>
  <files>
    src/views/snippet-manager-view.ts
    src/styles/snippet-manager.css
  </files>
  <read_first>
    - (mapping from Task 1)
    - src/styles/snippet-manager.css — confirm Plan 03's Phase 77 block already at END; Plan 04 appends a SECOND Phase 77 block AFTER Plan 03's.
  </read_first>
  <action>
    **Edit 1 — `src/views/snippet-manager-view.ts`:**
    For each flagged line per Task 1's mapping:
    - Replace `style.display = '<value>'` literal pairs with `el.toggleClass('rp-snippet-tree-<state>', boolean)`.
    - Replace line 287's `spacer.style.width = '12px'` with `spacer.addClass('rp-snippet-tree-spacer')` at the spacer's creation site.
    - Replace `el.style['display'] = ...` (computed property — lines 911/937) with `el.toggleClass('rp-snippet-tree-label-hidden', booleanExpr)`.

    Leave line 275's `indent.style.width = `${depth * 16}px`` UNCHANGED (Pitfall 7 — template literal not flagged).

    Per CLAUDE.md, do NOT touch unrelated lines. The shared file `snippet-manager-view.ts` is one of the «accumulated files» CLAUDE.md flags as regression-prone; this edit is bounded strictly to lines 276, 286, 287, 361, 911, 937 and their immediate context.

    **Edit 2 — `src/styles/snippet-manager.css`:**
    Append at the END of the file (AFTER Plan 03's Phase 77 block; this becomes the second Phase 77 block in the same file):
    ```css

    /* Phase 77: snippet-manager-view tree spacer + inline-rename label visibility (LINT-01) */
    .rp-snippet-tree-spacer {
      width: 12px;
    }
    .rp-snippet-tree-label-hidden {
      display: none;
    }
    /* Add additional rp-snippet-tree-* state classes per Task 1 mapping */
    ```
    Adapt the rule list to match actual classes introduced. NEVER edit existing rules above. Use `grep -c "rp-snippet-tree-spacer" src/styles/snippet-manager.css` BEFORE writing — if the class already exists, ABORT and reconcile (it must not duplicate Plan 03's block).

    **Edit 3 — `npm run build`** regenerates `styles.css`.

    **Commit message:**
    ```
    refactor(snippet-manager-view): replace 6 inline display/width styles with rp-snippet-tree-* classes

    Migrates obsidianmd/no-static-styles-assignment violations at lines
    276, 286, 287, 361, 911, 937 to el.toggleClass('rp-snippet-tree-*', bool)
    and el.addClass('rp-snippet-tree-spacer') per D-04/D-05. Lines
    911/937 use computed property style['display']; rule flags them
    same as literal access. Line 275 (`${depth * 16}px` template literal)
    intentionally NOT migrated — rule does not flag template literals
    (Pitfall 7 from 77-RESEARCH.md). CSS rules appended to
    src/styles/snippet-manager.css per D-07 with /* Phase 77 */ header.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/views/snippet-manager-view.ts 2>&1 | grep -c "no-static-styles-assignment"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `grep -nE "style\\.display\\s*=\\s*'" src/views/snippet-manager-view.ts | grep -v '^[^:]*:275:'` returns 0 matches (lines 276/286/361 cleared; line 275 left untouched).
    - `grep -nE "style\\['display'\\]" src/views/snippet-manager-view.ts` returns 0 matches (lines 911/937 cleared).
    - `grep -c "style.width = '12px'" src/views/snippet-manager-view.ts` returns 0 (line 287 cleared).
    - `grep -c 'indent.style.width = `${depth' src/views/snippet-manager-view.ts` returns ≥ 1 (line 275 INTACT — verifies Pitfall 7 compliance).
    - `grep -cE "rp-snippet-tree-" src/views/snippet-manager-view.ts` returns ≥ 6.
    - `grep -c "rp-snippet-tree-spacer" src/styles/snippet-manager.css` returns ≥ 1.
    - `grep -c "rp-snippet-tree-label-hidden" src/styles/snippet-manager.css` returns ≥ 1.
    - `grep -c "/\\* Phase 77:" src/styles/snippet-manager.css` returns ≥ 2 (Plan 03's block + this plan's block).
    - `npx eslint src/views/snippet-manager-view.ts 2>&1 | grep -c "no-static-styles-assignment"` returns 0.
    - `npm run build` exits 0; `grep -c "rp-snippet-tree-spacer" styles.css` returns ≥ 1.
    - `npm test` exits 0.
    - `git diff src/styles/snippet-manager.css` shows ONLY additions (zero `-` lines).
  </acceptance_criteria>
  <done>
    6 static-styles violations cleared in `snippet-manager-view.ts`; CSS rules appended; build + tests green; line 275 template literal untouched.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/views/snippet-manager-view.ts` reports 0 `obsidianmd/no-static-styles-assignment` errors.
2. `npm run lint` total count strictly decreases by 6.
3. Line 275 template-literal width assignment is intact (Pitfall 7 verification).
4. `npm run build` regenerates `styles.css` with new rules.
5. `npm test` green.
6. CSS diff additive only; both Plan 03 and Plan 04 Phase 77 blocks present in `snippet-manager.css`.
</verification>

<success_criteria>
- 6 static-styles violations cleared.
- D-05 naming `rp-snippet-tree-{state}` followed.
- Pitfall 7 honored — template-literal line 275 untouched.
- CSS append-only contract held.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-04-SUMMARY.md` with:
- Mapping (line → class + boolean expression).
- Confirmation that line 275 was not modified (Pitfall 7).
- New CSS rules introduced.
- `npx eslint <file>` before/after counts.
</output>
</content>
</invoke>