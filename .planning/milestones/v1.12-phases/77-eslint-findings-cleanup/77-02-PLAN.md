---
phase: 77
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/views/snippet-fill-in-modal.ts
  - src/styles/snippet-fill-modal.css
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "snippet-fill-in-modal.ts:148-150 no longer contain literal `style.x = ...` assignments — eslint exits 0 for this file"
    - "Single CSS class `rp-snippet-fill-option-row` provides display:flex + align-items:center + gap:4px"
    - "src/styles/snippet-fill-modal.css gains a Phase 77 block at the END only (append-only per CLAUDE.md)"
    - "npm run build regenerates styles.css with the new rule included"
    - "Existing snippet-fill-in-modal vitest tests stay green (no behaviour regression)"
  artifacts:
    - path: "src/views/snippet-fill-in-modal.ts"
      provides: "Option row creation via createDiv({cls: 'rp-snippet-fill-option-row'})"
    - path: "src/styles/snippet-fill-modal.css"
      provides: "Phase 77 .rp-snippet-fill-option-row rule"
      contains: "/* Phase 77:"
  key_links:
    - from: "src/views/snippet-fill-in-modal.ts"
      to: "src/styles/snippet-fill-modal.css"
      via: "class name 'rp-snippet-fill-option-row'"
      pattern: "rp-snippet-fill-option-row"
---

<objective>
Migrate the 3 static-styles violations at `src/views/snippet-fill-in-modal.ts:148-150` (`style.display = 'flex'`, `style.alignItems = 'center'`, `style.gap = '4px'`) to a single CSS class `rp-snippet-fill-option-row` per D-04 first branch (composite class for multiple style props). This is the simplest Wave 2 migration — used as the proof-of-pattern before more complex files.

Purpose: ROADMAP SC#2 — convert `obsidianmd/no-static-styles-assignment` violations to CSS class toggles + per-feature CSS rules per CLAUDE.md.
Output: edited `snippet-fill-in-modal.ts`, appended block in `snippet-fill-modal.css`, regenerated `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/views/snippet-fill-in-modal.ts
@src/styles/snippet-fill-modal.css

<interfaces>
<!-- The migration target — verified line numbers per RESEARCH.md table -->

Current source (`src/views/snippet-fill-in-modal.ts:147-150`, exact shape):
```typescript
const row = optionsDiv.createDiv();
row.style.display = 'flex';      // FLAGGED line 148
row.style.alignItems = 'center'; // FLAGGED line 149
row.style.gap = '4px';           // FLAGGED line 150
```

Target shape:
```typescript
const row = optionsDiv.createDiv({ cls: 'rp-snippet-fill-option-row' });
```

Existing analog (`runner-view.ts:1053`):
```typescript
const textarea = zone.createEl('textarea', { cls: 'rp-preview-textarea' });
```

CSS append target file `src/styles/snippet-fill-modal.css` (75 LOC; last meaningful comment at top is Phase 5). Append at the END.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate snippet-fill-in-modal option row to CSS class + append CSS rule</name>
  <files>
    src/views/snippet-fill-in-modal.ts
    src/styles/snippet-fill-modal.css
  </files>
  <read_first>
    - src/views/snippet-fill-in-modal.ts (full file — locate lines 147-150 precisely)
    - src/styles/snippet-fill-modal.css (full file — confirm last existing rule, append AFTER it)
    - .planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md §2 «snippet-fill-in-modal.ts:148-150 — composite class»
    - CLAUDE.md (root) — CSS files: append-only per phase
    - .planning/codebase/CONVENTIONS.md §CSS architecture (rp- namespace)
  </read_first>
  <action>
    **Edit 1 — `src/views/snippet-fill-in-modal.ts`:**
    Locate the row creation block (around lines 147-150). The exact existing shape is:
    ```typescript
    const row = optionsDiv.createDiv();
    row.style.display = 'flex';
    row.style.alignItems = 'center';
    row.style.gap = '4px';
    ```
    Replace with a single line:
    ```typescript
    const row = optionsDiv.createDiv({ cls: 'rp-snippet-fill-option-row' });
    ```
    Do NOT touch any other line in this file. The variable `row` continues to be used by the surrounding code unchanged.

    **Edit 2 — `src/styles/snippet-fill-modal.css`:**
    Append at the END of the file (DO NOT modify anything above):
    ```css

    /* Phase 77: snippet-fill-in-modal option row — replaces inline display/alignItems/gap (LINT-01) */
    .rp-snippet-fill-option-row {
      display: flex;
      align-items: center;
      gap: 4px;
    }
    ```
    The leading blank line ensures separation from the existing last rule. Per CLAUDE.md «CSS files: append-only per phase» — never edit existing rules above.

    **Edit 3 — regenerate `styles.css` via `npm run build`** (esbuild concatenates CSS_FILES into root `styles.css`). Per CLAUDE.md, never edit `styles.css` directly.

    **Commit message (D-11 stage 2 per-file commit):**
    ```
    refactor(snippet-fill-in-modal): replace inline styles with rp-snippet-fill-option-row class

    Migrates 3 obsidianmd/no-static-styles-assignment violations
    (display=flex, alignItems=center, gap=4px) at lines 148-150 to a
    single composite CSS class per D-04 strategy. CSS rule appended to
    src/styles/snippet-fill-modal.css per D-07 mapping with /* Phase 77 */
    header per CLAUDE.md append-only contract.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/views/snippet-fill-in-modal.ts 2>&1 | tail -3</automated>
    <!-- Expect: 0 errors (or strictly less than pre-edit) for this file -->
  </verify>
  <acceptance_criteria>
    - `grep -c "style.display = 'flex'" src/views/snippet-fill-in-modal.ts` returns 0 (the literal flagged line is gone).
    - `grep -c "style.alignItems = 'center'" src/views/snippet-fill-in-modal.ts` returns 0.
    - `grep -c "style.gap = '4px'" src/views/snippet-fill-in-modal.ts` returns 0.
    - `grep -c "rp-snippet-fill-option-row" src/views/snippet-fill-in-modal.ts` returns ≥ 1.
    - `grep -c "rp-snippet-fill-option-row" src/styles/snippet-fill-modal.css` returns ≥ 1.
    - `grep -c "/\* Phase 77:" src/styles/snippet-fill-modal.css` returns ≥ 1 (append marker present).
    - `npx eslint src/views/snippet-fill-in-modal.ts` exits 0 for that file.
    - `npm run build` exits 0 (regenerates `styles.css` with the new rule concatenated in).
    - `grep -c "rp-snippet-fill-option-row" styles.css` returns ≥ 1 (build picked up the new rule).
    - `npm test -- src/__tests__/views/snippet-fill-in-modal.test.ts` exits 0 if such file exists; else `npm test` exits 0.
    - `git diff src/styles/snippet-fill-modal.css` shows ONLY additions (no `-` lines, per CLAUDE.md append-only).
  </acceptance_criteria>
  <done>
    Three inline-style assignments removed, single class `rp-snippet-fill-option-row` introduced, CSS rule appended with Phase 77 header, build green, focused lint exits 0 for this file, vitest suite still passes.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/views/snippet-fill-in-modal.ts` exits 0 for that specific file.
2. Total `npm run lint` finding count strictly decreases from the post-Plan-01 baseline (Plan 01 baseline minus 3).
3. `npm run build` exits 0; `styles.css` contains the new rule.
4. `npm test` exits 0.
5. CSS file diff shows additions only (no deletions).
</verification>

<success_criteria>
- 3 static-styles violations cleared in `snippet-fill-in-modal.ts`.
- `src/styles/snippet-fill-modal.css` gained one new Phase-77-tagged block.
- CSS append-only contract held.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-02-SUMMARY.md` with:
- Lines edited in `snippet-fill-in-modal.ts` (147-150).
- New CSS rule appended in `snippet-fill-modal.css`.
- `npx eslint <file>` before/after counts.
- `npm run build` and `npm test` confirmed exit 0.
</output>
</content>
</invoke>