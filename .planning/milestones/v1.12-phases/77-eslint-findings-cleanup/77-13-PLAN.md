---
phase: 77
plan: 13
type: execute
wave: 8
depends_on: ["01"]
files_modified:
  - src/views/node-picker-modal.ts
  - src/snippets/snippet-service.ts
  - src/__tests__/snippet-editor-modal.test.ts
  - src/__tests__/views/editor-panel-canvas-sync.test.ts
  - .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md
autonomous: true
requirements: [LINT-01]
threat_model_ref: 77-01-PLAN.md
must_haves:
  truths:
    - "1 × no-constant-binary-expression at node-picker-modal.ts:71 is fixed (drop unreachable `|| id`)"
    - "1 × no-control-regex at snippet-service.ts:488 is wrapped with per-line eslint-disable + Phase 77 rationale citing intentional control-char strip"
    - "Unused eslint-disable directives at src/__tests__/snippet-editor-modal.test.ts:411 + 648 and src/__tests__/views/editor-panel-canvas-sync.test.ts:267 are individually verified-then-removed (per-site verification — directive rule names are NOT homogeneous; line 267 is no-await-in-loop, NOT no-explicit-any)"
    - "2 × prefer-file-manager-trash-file warnings (snippet-service.ts:240, 283) are DOCUMENTED out-of-scope in 77-VERIFICATION.md per D-13 with UX-impact rationale"
    - "If `eslint --fix` produces additional whitespace fixes (D-13 mentioned 4 fixable warnings), those land in this plan"
    - "After this plan, `npm run lint` exit code is 0 — phase completion is finalized in Plan 14"
  artifacts:
    - path: "src/views/node-picker-modal.ts"
      provides: "Cleaned `|| id` truthy-cascade at line 71"
    - path: "src/snippets/snippet-service.ts"
      provides: "Per-line eslint-disable on line 488 control-char regex"
    - path: ".planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md"
      provides: "Documentation of 2 prefer-file-manager-trash-file warnings as out-of-scope per D-13 + ROADMAP SC#5"
  key_links:
    - from: "src/snippets/snippet-service.ts:488 disable directive"
      to: "commit message rationale"
      via: "ROADMAP SC#3 audit trail"
      pattern: "Phase 32: intentional control-char strip"
---

<objective>
Address the residual single-instance lint rules + warning handling per D-13:

- 1 × `no-constant-binary-expression` at `node-picker-modal.ts:71` — drop unreachable `|| id` after truthy literal `'(корень snippets)'`.
- 1 × `no-control-regex` at `snippet-service.ts:488` — wrap with per-line `eslint-disable-next-line` + Phase 77 rationale (intentional control-char strip; Pitfall 6).
- 3 × unused `eslint-disable` directive warnings in test files — **per-site verification required** because the suppressed rule names are NOT homogeneous:
  - `src/__tests__/snippet-editor-modal.test.ts:411` — directive: `@typescript-eslint/no-explicit-any` (verified 2026-04-30 by reading the file).
  - `src/__tests__/snippet-editor-modal.test.ts:648` — directive: `@typescript-eslint/no-require-imports, @typescript-eslint/no-var-requires` (verified — multi-rule suppress).
  - `src/__tests__/views/editor-panel-canvas-sync.test.ts:267` — directive: `no-await-in-loop` (verified — DIFFERENT rule from above; do NOT assume `no-explicit-any`).
- 2 × `prefer-file-manager-trash-file` warnings — DOCUMENT as out-of-scope per D-13 + Pitfall 5 (UX semantics). Create `77-VERIFICATION.md` listing each remaining warning with rationale (ROADMAP SC#5 mandate).

If `npx eslint --fix src/` surfaces any additional fixable whitespace warnings (per D-13 «4 fixable through eslint --fix»), those land here too.

Plan 13 runs in Wave 8 — needs ALL prior plans done so we can truly assess «what's left». No file collision with prior plans.

Purpose: ROADMAP SC#5 (warnings handling) + the last error-class cleanups before final-gate Plan 14.
Output: cleaned-up lint surface; `77-VERIFICATION.md` created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md
@.planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md
@.planning/phases/77-eslint-findings-cleanup/77-PATTERNS.md
@CLAUDE.md
@src/views/node-picker-modal.ts
@src/snippets/snippet-service.ts

<interfaces>
<!-- Site 1 — node-picker-modal.ts:71 (verified per RESEARCH.md): -->
```typescript
options.push({ id, label: s.subfolderPath || '(корень snippets)' || id, kind: 'snippet' });
```
The literal `'(корень snippets)'` is always truthy → `|| id` is unreachable. Fix: drop `|| id`.

After:
```typescript
options.push({ id, label: s.subfolderPath || '(корень snippets)', kind: 'snippet' });
```

<!-- Site 2 — snippet-service.ts:488 (Pitfall 6 — intentional control-char strip): -->
```typescript
const control = /[ -]/g;
```
This regex strips control characters from user-supplied snippet name/template — defense-in-depth. The `no-control-regex` lint rule fires but the regex is intentional.

Fix: per-line disable WITH rationale:
```typescript
// eslint-disable-next-line no-control-regex -- Phase 32: intentional control-char strip; prevents invisible-char injection in snippet content (sanitization defense)
const control = /[ -]/g;
```

<!-- Sites 3-5 — unused eslint-disable directive warnings (3 in tests):

CRITICAL: file paths and rule names verified by reading the files (2026-04-30).
Both snippet-editor-modal test directives live in src/__tests__/snippet-editor-modal.test.ts
(top-level — NO `views/` segment in the path). The third directive lives in
src/__tests__/views/editor-panel-canvas-sync.test.ts.

Per-site verification (executor MUST re-confirm rule name from the file content,
not from this header — homogeneous «all are no-explicit-any» assumption is FALSE):

- src/__tests__/snippet-editor-modal.test.ts:411 — directive suppresses
  `@typescript-eslint/no-explicit-any` (file content verified 2026-04-30).
- src/__tests__/snippet-editor-modal.test.ts:648 — directive suppresses
  `@typescript-eslint/no-require-imports, @typescript-eslint/no-var-requires`
  (multi-rule directive, file content verified 2026-04-30).
- src/__tests__/views/editor-panel-canvas-sync.test.ts:267 — directive suppresses
  `no-await-in-loop` (file content verified 2026-04-30 — DIFFERENT rule from the
  two above; assuming homogeneous rule names will mis-classify and break SC#1).

Each fix is a single-line deletion of the `// eslint-disable-next-line ...` comment,
BUT only after eslint confirms the directive is genuinely UNUSED. If eslint reports
the directive as still active (rule below it firing), the disable stays and the
underlying violation must be addressed separately. -->

<!-- Sites 6-7 — prefer-file-manager-trash-file warnings (DOCUMENT, don't fix): -->
- snippet-service.ts:240 — `vault.trash(file, false)` for snippet folder delete.
- snippet-service.ts:283 — `vault.delete(file)` or similar.

Per D-13 + Pitfall 5 + ROADMAP SC#5 — DO NOT migrate these; replacing `vault.trash` with `app.fileManager.trashFile` changes UX semantics (vault `.trash/` → user-configured: system trash / vault trash / permanent). Behaviour change requires a UX decision phase outside LINT-01 scope.

Create `77-VERIFICATION.md` documenting both warnings as out-of-scope with the above rationale, satisfying ROADMAP SC#5: «Pre-existing 6 warnings are either resolved or explicitly documented as out-of-scope... phase VERIFICATION.md lists each remaining warning with rationale».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix no-constant-binary-expression + wrap no-control-regex with rationale</name>
  <files>
    src/views/node-picker-modal.ts
    src/snippets/snippet-service.ts
  </files>
  <read_first>
    - src/views/node-picker-modal.ts:65-80 (around line 71)
    - src/snippets/snippet-service.ts:480-495 (around line 488 control regex)
    - .planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md §«Code Examples» Example 5 + §Pitfall 6
  </read_first>
  <action>
    **Edit 1 — `src/views/node-picker-modal.ts:71`:**
    Drop the unreachable `|| id`:
    ```typescript
    // before:
    options.push({ id, label: s.subfolderPath || '(корень snippets)' || id, kind: 'snippet' });
    // after:
    options.push({ id, label: s.subfolderPath || '(корень snippets)', kind: 'snippet' });
    ```

    **Edit 2 — `src/snippets/snippet-service.ts:488`:**
    Add per-line disable + rationale immediately above the control regex:
    ```typescript
    // eslint-disable-next-line no-control-regex -- Phase 32: intentional control-char strip; prevents invisible-char injection in snippet content (sanitization defense)
    const control = /[ -]/g;
    ```
    The exact regex literal must match what's currently on line 488 — Read the file first to confirm. Phase 32 is correct because that's when `cleanControlChars` was introduced (per the «Phase 32» pattern in commit history); the rationale cites the original intent.

    **Commit message (single commit covering both edits):**
    ```
    refactor(misc): drop unreachable `|| id` + annotate intentional control-char regex

    Edit 1: node-picker-modal.ts:71 — drops `|| id` from truthy-cascade
    `s.subfolderPath || '(корень snippets)' || id`. The middle literal
    is always truthy → `|| id` unreachable (no-constant-binary-expression).

    Edit 2: snippet-service.ts:488 — wraps the control-char strip regex
    with per-line eslint-disable-next-line no-control-regex citing
    Phase 32's intentional sanitization (Pitfall 6 from 77-RESEARCH.md);
    ROADMAP SC#3 «per-file disable acceptable ONLY with written
    justification» is honored.

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>npx eslint src/views/node-picker-modal.ts src/snippets/snippet-service.ts 2>&1 | grep -cE "no-constant-binary-expression|no-control-regex"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `grep -c "'(корень snippets)' || id" src/views/node-picker-modal.ts` returns 0.
    - `grep -c "eslint-disable-next-line no-control-regex" src/snippets/snippet-service.ts` returns ≥ 1.
    - `grep -c "Phase 32" src/snippets/snippet-service.ts` returns ≥ 1 (rationale present near the disable).
    - `npx eslint src/views/node-picker-modal.ts 2>&1 | grep -c "no-constant-binary-expression"` returns 0.
    - `npx eslint src/snippets/snippet-service.ts 2>&1 | grep -c "no-control-regex"` returns 0.
    - `npm run build && npm test` green.
  </acceptance_criteria>
  <done>
    Both single-instance errors cleared.
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-site verification + removal of 3 unused eslint-disable directives in test files</name>
  <files>
    src/__tests__/snippet-editor-modal.test.ts
    src/__tests__/views/editor-panel-canvas-sync.test.ts
  </files>
  <read_first>
    - src/__tests__/snippet-editor-modal.test.ts:405-415 (line 411 directive — verified to suppress `@typescript-eslint/no-explicit-any`)
    - src/__tests__/snippet-editor-modal.test.ts:642-655 (line 648 directive — verified to suppress `@typescript-eslint/no-require-imports, @typescript-eslint/no-var-requires`)
    - src/__tests__/views/editor-panel-canvas-sync.test.ts:262-272 (line 267 directive — verified to suppress `no-await-in-loop`, NOT `no-explicit-any`)
    - **CRITICAL FILE PATH NOTE:** the snippet-editor-modal test file is at top-level `src/__tests__/snippet-editor-modal.test.ts` — there is NO `views/` segment. (The mistaken path `src/__tests__/views/snippet-editor-modal.test.ts` does NOT exist.)
  </read_first>
  <action>
    **Per-site verification step (mandatory before deletion):**

    For EACH of the 3 directive sites, execute this sequence:

    1. **Read the directive line in the file** to confirm the exact rule name:
       ```bash
       sed -n '411p' src/__tests__/snippet-editor-modal.test.ts
       sed -n '648p' src/__tests__/snippet-editor-modal.test.ts
       sed -n '267p' src/__tests__/views/editor-panel-canvas-sync.test.ts
       ```
       Expected outputs (verified 2026-04-30):
       - line 411: `// eslint-disable-next-line @typescript-eslint/no-explicit-any`
       - line 648: `// eslint-disable-next-line @typescript-eslint/no-require-imports, @typescript-eslint/no-var-requires`
       - line 267: `// eslint-disable-next-line no-await-in-loop`

    2. **Run focused lint to verify the directive is reported as UNUSED:**
       ```bash
       npx eslint src/__tests__/snippet-editor-modal.test.ts 2>&1 | grep -E "(411|648).*Unused eslint-disable"
       npx eslint src/__tests__/views/editor-panel-canvas-sync.test.ts 2>&1 | grep -E "267.*Unused eslint-disable"
       ```
       Each command should report the directive as unused. If a directive is NOT reported as unused (i.e. eslint says the rule below IS firing and the directive is genuinely needed), DO NOT delete it — instead, retain the directive and add a per-site rationale comment explaining why the disable is needed. Note this case in the SUMMARY.

    3. **For each verified-unused directive, delete the comment line.** The line BELOW stays unchanged.

       Special case for line 267 (`no-await-in-loop`): The directive sits inside a `for` loop body that does `await Promise.resolve()`. If eslint reports it unused, deletion is safe. If eslint says `no-await-in-loop` IS firing on the `await` below, retain the directive with rationale: «intentional sequential microtask drain in test helper; parallel `await Promise.all` would defeat the drain semantics». Since prior phases already had this directive landing as «unused», deletion is the expected outcome — but verify before assuming.

    Apply per-site treatment. Total expected: 3 deletions; the contingent-retain branches are safety nets, not the expected path.

    Per CLAUDE.md «only modify code relevant to current phase» — diff is bounded to the 3 single-line deletions (or comment-edits if retain-branch triggers).

    **Commit message:**
    ```
    test(misc): remove 3 verified-unused eslint-disable directives per D-13

    Per-site verification (each directive's rule name is heterogeneous —
    do not assume homogeneity):
    - src/__tests__/snippet-editor-modal.test.ts:411 — directive
      suppressed `@typescript-eslint/no-explicit-any`; eslint reports
      it as unused (no `any` violation in block below). Deleted.
    - src/__tests__/snippet-editor-modal.test.ts:648 — directive
      suppressed `@typescript-eslint/no-require-imports,
      @typescript-eslint/no-var-requires`; eslint reports unused
      (uses `await import()` instead of require). Deleted.
    - src/__tests__/views/editor-panel-canvas-sync.test.ts:267 —
      directive suppressed `no-await-in-loop` (NOT no-explicit-any);
      eslint reports unused after rule-config tightening upstream.
      Deleted.

    Removes 3 of the 6 warnings flagged at phase start (the 4th unused-
    disable was Plan 05's snippet-editor-modal.ts:143).

    Phase 77 / LINT-01 / D-13.
    ```
  </action>
  <verify>
    <automated>npx eslint src/__tests__/snippet-editor-modal.test.ts src/__tests__/views/editor-panel-canvas-sync.test.ts 2>&1 | grep -cE "unused.*eslint-disable"</automated>
    <!-- Expect 0 -->
  </verify>
  <acceptance_criteria>
    - `npx eslint src/__tests__/snippet-editor-modal.test.ts src/__tests__/views/editor-panel-canvas-sync.test.ts 2>&1 | grep -cE "unused.*eslint-disable"` returns 0.
    - `npx eslint src/__tests__/snippet-editor-modal.test.ts 2>&1 | grep -cE "error" | head -1` returns the SAME error-count as before this task minus the unused-disable warnings (i.e. NO new errors introduced by deletion).
    - `npx eslint src/__tests__/views/editor-panel-canvas-sync.test.ts 2>&1 | grep -cE "error" | head -1` returns the SAME error-count as before this task minus the unused-disable warning (i.e. NO new errors introduced by deletion). Critically, `no-await-in-loop` MUST NOT fire after deletion of line 267 directive — if it does, the directive must be RESTORED with rationale and SUMMARY must document the change.
    - `npm test -- src/__tests__/snippet-editor-modal.test.ts src/__tests__/views/editor-panel-canvas-sync.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    3 unused-disable warnings cleared via per-site verification; tests pass; no new lint errors introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run npx eslint --fix to clear any remaining whitespace warnings</name>
  <files>(any files eslint --fix touches)</files>
  <read_first>
    - .planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md D-13 («4 fixable through eslint --fix»)
  </read_first>
  <action>
    Run:
    ```bash
    npx eslint --fix src/
    ```

    Inspect `git diff` — only whitespace-style changes should appear. If any non-whitespace fix was made, ABORT and review (eslint --fix can occasionally apply rules that change semantics; D-13 narrative said «видимо whitespace-style» — verify).

    Stage only files where the diff is whitespace-style. Per CLAUDE.md «only modify code relevant to current phase» — if eslint --fix touches unrelated lines, exclude them with `git checkout -- <file>` and add justification in the SUMMARY.

    **Commit message:**
    ```
    chore(lint): apply eslint --fix for whitespace warnings

    Per D-13: «4 fixable через eslint --fix; видимо whitespace-style».
    No semantic changes inspected; pure whitespace per `git diff`.

    Phase 77 / LINT-01 / D-13.
    ```

    **If no fixable warnings surface** (eslint --fix produces zero diff): skip this task — no commit needed. Note in SUMMARY: «Task 3 produced no diff; D-13's 4 fixable warnings were already cleared by prior plans».
  </action>
  <verify>
    <automated>npx eslint src/ 2>&1 | grep -cE "warning"</automated>
    <!-- Expect ≤ 2 (only the prefer-file-manager-trash-file warnings, documented in Task 4) -->
  </verify>
  <acceptance_criteria>
    - After this task, `npx eslint src/ 2>&1 | grep -cE "warning"` returns ≤ 2 (the only remaining warnings should be the 2 documented prefer-file-manager-trash-file ones from Task 4).
    - If a commit was made, `git diff HEAD~ HEAD` shows only whitespace changes.
    - `npm run build && npm test` green.
  </acceptance_criteria>
  <done>
    eslint --fix applied (or skipped if no diff); only documented warnings remain.
  </done>
</task>

<task type="auto">
  <name>Task 4: Document 2 × prefer-file-manager-trash-file warnings as out-of-scope in 77-VERIFICATION.md</name>
  <files>.planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/77-eslint-findings-cleanup/77-CONTEXT.md D-13
    - .planning/phases/77-eslint-findings-cleanup/77-RESEARCH.md §Pitfall 5
    - .planning/ROADMAP.md §Phase 77 SC#5
    - src/snippets/snippet-service.ts:235-290 (the actual call sites)
  </read_first>
  <action>
    Create the file `.planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` with content:

    ```markdown
    ---
    phase: 77
    plan: VERIFICATION
    status: out-of-scope-warnings-documented
    created: <today's date YYYY-MM-DD>
    ---

    # Phase 77 — VERIFICATION

    Per ROADMAP SC#5 («Pre-existing 6 warnings are either resolved or explicitly documented as out-of-scope... phase VERIFICATION.md lists each remaining warning with rationale») — this file documents the 2 warnings retained in tree at phase close.

    ## Warnings Retained Out-of-Scope

    | # | File | Line | Rule | Rationale |
    |---|------|------|------|-----------|
    | 1 | `src/snippets/snippet-service.ts` | 240 | `obsidianmd/prefer-file-manager-trash-file` | Calls `vault.trash(file, false)` which is hardcoded to use Obsidian's vault-relative `.trash/` folder. Replacing with `app.fileManager.trashFile(file)` would respect user setting `Files & Links → Deleted files`, which can be `system trash` (macOS Trash, Windows Recycle Bin), `Obsidian's .trash/`, or `Permanently delete`. Behaviour change is a UX decision — users relying on `.trash/` placement would suddenly receive OS Trash placement. Per D-13 and 77-RESEARCH.md Pitfall 5, this is documented out-of-scope for LINT-01; UX-decision phase required before migration. |
    | 2 | `src/snippets/snippet-service.ts` | 283 | `obsidianmd/prefer-file-manager-trash-file` | Same rule, same call shape, same UX-impact rationale. |

    ## Resolved Warnings (for traceability)

    | # | File:line | Rule | Resolution |
    |---|-----------|------|------------|
    | 1 | `src/views/snippet-editor-modal.ts:143` | unused `eslint-disable` directive (no-explicit-any) | Removed in Plan 05 (D-13). |
    | 2 | `src/__tests__/snippet-editor-modal.test.ts:411` | unused `eslint-disable` directive (no-explicit-any) | Removed in Plan 13 Task 2 after per-site verification. |
    | 3 | `src/__tests__/snippet-editor-modal.test.ts:648` | unused `eslint-disable` directive (no-require-imports, no-var-requires) | Removed in Plan 13 Task 2 after per-site verification. |
    | 4 | `src/__tests__/views/editor-panel-canvas-sync.test.ts:267` | unused `eslint-disable` directive (no-await-in-loop, NOT no-explicit-any) | Removed in Plan 13 Task 2 after per-site verification. |

    ## Phase Completion Status

    - All 517 errors cleared (Plans 01-13).
    - 4 of 6 warnings cleared (Plans 05, 13).
    - 2 of 6 warnings retained out-of-scope per D-13 + ROADMAP SC#5 (above).

    `npm run lint` exit 0 confirmed in Plan 14 (final gate).

    ## Manual UAT (per VALIDATION.md «Manual-Only Verifications»)

    To be completed before phase close:
    - [ ] Snippet manager — rename input show/hide visual unchanged.
    - [ ] Inline runner — drag/resize position persistence works.
    - [ ] Runner preview zone — auto-grow textarea works.
    - [ ] Editor panel — Question/Answer/Snippet/Loop/Text-block forms — auto-grow textarea works.
    ```

    Substitute today's date in the frontmatter.

    **Commit message:**
    ```
    docs(77): create VERIFICATION.md documenting 2 retained warnings + resolved warning trace

    Per ROADMAP SC#5: phase VERIFICATION.md lists each remaining
    warning with rationale. The 2 prefer-file-manager-trash-file
    warnings at snippet-service.ts:240, 283 are documented out-of-
    scope per D-13 + 77-RESEARCH.md Pitfall 5 (vault.trash → 
    fileManager.trashFile changes UX semantics — UX decision phase
    required).

    Also traces the 4 resolved unused-eslint-disable directives with
    their actual rule names (heterogeneous: no-explicit-any,
    no-require-imports/no-var-requires, no-await-in-loop).

    Phase 77 / LINT-01.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md && grep -c "prefer-file-manager-trash-file" .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md</automated>
    <!-- Expect ≥ 2 -->
  </verify>
  <acceptance_criteria>
    - `.planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` exists.
    - `grep -c "prefer-file-manager-trash-file" .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` returns ≥ 2.
    - `grep -c "snippet-service.ts:240" .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` returns ≥ 1.
    - `grep -c "snippet-service.ts:283" .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` returns ≥ 1.
    - `grep -c "Phase 77 / LINT-01" .planning/phases/77-eslint-findings-cleanup/77-VERIFICATION.md` returns ≥ 0 (header is fine even without commit-tag string; check is commit message ROADMAP SC#5 satisfied).
  </acceptance_criteria>
  <done>
    77-VERIFICATION.md created with both retained warnings + resolved-warnings trace.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/` reports 0 errors AND ≤ 2 warnings (only the 2 documented prefer-file-manager-trash-file warnings).
2. `npm run build && npm test` green.
3. `77-VERIFICATION.md` exists and lists both retained warnings with rationale.
4. ROADMAP SC#5 satisfied — phase VERIFICATION.md present.
</verification>

<success_criteria>
- 1 no-constant-binary-expression + 1 no-control-regex + 3 unused-disable + 0-4 whitespace fixes applied.
- 2 prefer-file-manager-trash-file warnings documented out-of-scope.
- `77-VERIFICATION.md` complete.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/77-eslint-findings-cleanup/77-13-SUMMARY.md` with:
- Tasks 1-4 disposition (errors fixed, warnings cleared, warnings documented).
- Final lint count delta (from start of Plan 13 to end).
- VERIFICATION.md location confirmation.
- Per-site verification log for Task 2 (rule names confirmed, eslint-reports-unused confirmed for each, deletion vs retain-with-rationale outcome).
</output>
</content>
</invoke>