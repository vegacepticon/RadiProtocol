---
phase: 75
plan: 02
title: "Migrate awaiting-loop-pick and shared footer"
status: completed
requirements: [DEDUP-01]
depends_on: [75-01]
wave: 2
risk: medium
---

# Plan 75-02 — Migrate awaiting-loop-pick + footer

## Objective

Move the duplicated `awaiting-loop-pick` render path and Back/Skip footer into shared renderer modules.

## Scope

Create/modify:

- `src/runner/render/render-loop-picker.ts`
- `src/runner/render/render-footer.ts`
- `src/views/runner-view.ts`
- `src/views/inline-runner-modal.ts`

## Behavior to preserve

- Loop branch labels, exit-prefix handling, and node labels.
- `RunnerView` manual edit capture before `runner.chooseLoopBranch`.
- `RunnerView` autosave after runner mutation.
- `InlineRunnerModal` accumulator-delta append after runner mutation.
- Back button synchronous double-click guard.
- Snippet picker unmount before Back when relevant.

## Verification

Run:

```bash
npx vitest run loop-body-file-bound
npm test -- RunnerView inline-runner runner-snippet
npm run build
npm run lint
```

## Success criteria

- `awaiting-loop-pick` rendering is declared in shared `src/runner/render/render-loop-picker.ts`.
- Both host shells delegate loop picker rendering to shared code.
- Loop-body/file-bound tests pass for both sidebar and inline paths.
