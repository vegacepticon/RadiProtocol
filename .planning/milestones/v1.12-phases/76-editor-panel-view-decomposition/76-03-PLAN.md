---
phase: 76
plan: 03
title: "Extract answer-form"
status: completed
requirements: [SPLIT-01, SPLIT-02]
depends_on: [76-02]
wave: 3
---

# Plan 76-03 — Extract answer-form

## Goal
Move Answer node form rendering to `answer-form.ts` while preserving display-label edge sync and existing field order.

## Scope

Create:
- `src/views/editor-panel/forms/answer-form.ts`

Modify:
- `src/views/editor-panel-view.ts`
- `_shared.ts` only if needed for reusable types

## Implementation steps

1. Add `renderAnswerForm(container, nodeRecord, ctx)`.
2. Preserve current field order exactly:
   - display label / answer label input
   - answer text growable textarea
   - separator override
3. Preserve `pendingEdits` keys exactly, including display-label and answer-text keys.
4. Preserve Phase 63 `registerFieldRef` for displayLabel and answer text.
5. Replace only the `answer` switch arm with delegation.

## Critical invariants

- Do not change edge-label sync semantics.
- Do not change `collectIncomingEdgeEdits` / `saveNodeEdits`; that remains dispatcher-owned.
- Do not move autosave timer state out of the view.
- Do not reorder fields; tests assert ordering.

## Verification

Run:

```bash
npm test -- editor-panel-forms
npm test -- editor-panel-canvas-sync
npm test -- editor-panel
npm run build
npm run lint
wc -l src/views/editor-panel-view.ts
git grep -n "radiprotocol_displayLabel\|radiprotocol_answerText" src/views/editor-panel/forms/answer-form.ts
```

Expected:
- answer form tests green
- displayLabel focus-stash canvas-sync tests green
- build/lint green

## Commit message

```text
refactor(76-03): extract answer editor form
```
