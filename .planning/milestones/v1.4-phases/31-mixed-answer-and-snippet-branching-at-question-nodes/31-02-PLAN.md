---
phase: 31-mixed-answer-and-snippet-branching-at-question-nodes
plan: 02
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/views/editor-panel-view.ts
autonomous: true
requirements:
  - SNIPPET-BRANCH-03
must_haves:
  truths:
    - "Node Editor для snippet-узла показывает text input для `snippetLabel`"
    - "Node Editor для snippet-узла показывает dropdown для `radiprotocol_snippetSeparator` с опциями: default/newline/space"
    - "Изменение любого из двух новых полей триггерит autoSave через scheduleAutoSave"
    - "Empty string/default нормализуется в pendingEdits как undefined"
  artifacts:
    - path: "src/views/editor-panel-view.ts"
      provides: "Два новых Setting-блока внутри case 'snippet'"
      contains: "radiprotocol_snippetLabel"
  key_links:
    - from: "onChange snippetLabel text input"
      to: "this.pendingEdits['radiprotocol_snippetLabel']"
      via: "scheduleAutoSave"
      pattern: "pendingEdits\\['radiprotocol_snippetLabel'\\]"
    - from: "onChange snippetSeparator dropdown"
      to: "this.pendingEdits['radiprotocol_snippetSeparator']"
      via: "scheduleAutoSave"
      pattern: "pendingEdits\\['radiprotocol_snippetSeparator'\\]"
---

<objective>
Добавить в Node Editor (case 'snippet') два новых контрола — text input для `snippetLabel` и dropdown для `snippetSeparator`. Зависит от Plan 01: читает `this.node?.snippetLabel` и `this.node?.radiprotocol_snippetSeparator` — поля интерфейса SnippetNode, которые добавляет Plan 01 в graph-model.ts. Выполняется в wave 2 после Plan 01.

Purpose: Даёт автору возможность редактировать snippet-ноду так, чтобы она показывалась осмысленной кнопкой на развилке и использовала нужный separator.
Output: Два новых Setting-блока внутри существующего `case 'snippet'` в `buildKindForm`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md
@src/views/editor-panel-view.ts

<interfaces>
<!-- editor-panel-view.ts case 'snippet' (lines 566-613) — subfolderPath dropdown pattern: -->
```ts
case 'snippet': {
  new Setting(container).setHeading().setName('Snippet node');
  const subfolderSetting = new Setting(container)
    .setName('Subfolder path')
    .setDesc('...');
  void (async () => {
    try {
      // ...
      subfolderSetting.addDropdown(drop => {
        drop.addOption('', '\u2014 root (all snippets) \u2014');
        // ...
        drop.setValue(currentPath);
        drop.onChange(v => {
          this.pendingEdits['radiprotocol_subfolderPath'] = v || undefined;
          this.pendingEdits['text'] = v;
          this.scheduleAutoSave();
        });
      });
    } catch { /* ... */ }
  })();
  break;
}
```

<!-- Current snippet node data access (assume this.node or this.pendingEdits carry the active values; use same lookup as subfolderPath). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add snippetLabel text input + snippetSeparator dropdown to snippet form</name>
  <files>src/views/editor-panel-view.ts</files>
  <read_first>
    - src/views/editor-panel-view.ts (lines 566-613 — полный `case 'snippet'` блок; также грepнуть `scheduleAutoSave` чтобы увидеть сигнатуру и как другие формы её вызывают; грepнуть `pendingEdits` для pattern `= v || undefined`)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md §7
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md D-01, D-04
    - CLAUDE.md (shared-file rule — НЕ удалять код соседних case)
  </read_first>
  <action>
    1. Найти закрывающий `})();` async IIFE для subfolderPath dropdown (около line 611) и закрывающий `break;` для `case 'snippet'`. Новый код добавлять МЕЖДУ ними. НЕ трогать subfolderPath dropdown, НЕ переписывать async IIFE, НЕ трогать `setHeading`.

    2. Добавить text input для snippetLabel ПОСЛЕ закрывающего IIFE (vanilla sync Setting, без async):
       ```ts
       // Phase 31 D-01: optional label shown on branch-list button when this snippet node
       // is reached as a variant of a question. Empty fallback = "📁 Snippet".
       new Setting(container)
         .setName('Branch label')
         .setDesc('Shown on the branch-list button when a question has outgoing edges to this snippet. Leave empty to use "📁 Snippet".')
         .addText(text => {
           text.setValue(
             (typeof this.pendingEdits['radiprotocol_snippetLabel'] === 'string'
               ? (this.pendingEdits['radiprotocol_snippetLabel'] as string)
               : (this.node?.snippetLabel as string | undefined)) ?? ''
           );
           text.onChange(v => {
             this.pendingEdits['radiprotocol_snippetLabel'] = v || undefined;
             this.scheduleAutoSave();
           });
         });
       ```
       Примечание: если в текущем `case 'snippet'` используется другой паттерн чтения текущего значения (например `this.currentNodeProps?.[...]` или какой-то helper вместо `this.node?.*`) — скопировать ТОТ ЖЕ паттерн чтения, что использует subfolderPath dropdown на line 566-613. Цель: консистентность с соседней формой, не введение нового способа доступа.

    3. Добавить separator dropdown сразу после text input:
       ```ts
       // Phase 31 D-04: per-node separator override. '' = use global default from settings.
       new Setting(container)
         .setName('Separator override')
         .setDesc('How the rendered snippet text is joined to the accumulated protocol. Default uses the global Text Separator setting.')
         .addDropdown(drop => {
           drop.addOption('', '\u2014 use global default \u2014');
           drop.addOption('newline', 'Newline');
           drop.addOption('space', 'Space');
           const current =
             (this.pendingEdits['radiprotocol_snippetSeparator'] as string | undefined)
             ?? (this.node?.radiprotocol_snippetSeparator as string | undefined)
             ?? '';
           drop.setValue(current);
           drop.onChange(v => {
             this.pendingEdits['radiprotocol_snippetSeparator'] = (v === 'space' || v === 'newline') ? v : undefined;
             this.scheduleAutoSave();
           });
         });
       ```
       Снова: паттерн чтения текущего значения скопировать из subfolderPath блока, если там другой способ доступа.

    4. НЕ трогать ни один другой case в switch. НЕ удалять, не переупорядочивать существующие Setting-блоки. Сохранить `break;` на своём месте.
  </action>
  <verify>
    <automated>npm run build</automated>
    <!-- Nyquist exception: view-form wiring. No editor-panel-view test harness exists in repo. -->
    <!-- Behavior validated via UAT (open snippet node → edit fields → autoSave → round-trip through parser from Plan 01). -->
    <!-- Documented in 31-VALIDATION.md § "Nyquist Exceptions (view-form tasks)". -->
  </verify>
  <acceptance_criteria>
    - `grep -n "radiprotocol_snippetLabel" src/views/editor-panel-view.ts` → ≥2 matches (read + write)
    - `grep -n "radiprotocol_snippetSeparator" src/views/editor-panel-view.ts` → ≥2 matches
    - `grep -n "Branch label" src/views/editor-panel-view.ts` → 1 match
    - `grep -n "Separator override" src/views/editor-panel-view.ts` → 1 match
    - `grep -c "radiprotocol_subfolderPath" src/views/editor-panel-view.ts` ≥ 1 (regression: subfolderPath form сохранён)
    - `npm run build` завершается без TypeScript ошибок
    - Линейно: весь существующий case 'snippet' код (setHeading, subfolderSetting, async IIFE) присутствует в файле после правки
  </acceptance_criteria>
  <done>Node Editor для snippet-узла отображает Branch label text input и Separator override dropdown; оба писут в `this.pendingEdits` с empty-to-undefined нормализацией и триггерят `scheduleAutoSave`. Build зелёный.</done>
</task>

</tasks>

<verification>
- `npm run build` зелёный
- Ручная проверка (в wave 3 или на UAT): открыть snippet-узел в Node Editor → видны оба новых поля → изменение каждого триггерит autoSave → перезагрузка canvas показывает что значения round-trip через parser из плана 01 (зависимость валидируется в wave 2 manual smoke)
- `git diff src/views/editor-panel-view.ts` должен быть ограничен двумя append-блоками внутри case 'snippet'
</verification>

<success_criteria>
- Автор snippet-узла может задать Branch label и per-node separator через Node Editor
- Empty/default значения корректно нормализуются в undefined
- Нет регрессий в других формах editor-panel-view
</success_criteria>

<output>
Создать `.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-02-SUMMARY.md` (на русском) после завершения.
</output>
</content>
</invoke>