---
phase: 31-mixed-answer-and-snippet-branching-at-question-nodes
plan: 03
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/runner/protocol-runner.ts
  - src/__tests__/runner/protocol-runner.test.ts
  - src/__tests__/runner/protocol-runner-session.test.ts
autonomous: true
requirements:
  - SNIPPET-BRANCH-01
  - SNIPPET-BRANCH-02
  - SNIPPET-BRANCH-05
must_haves:
  truths:
    - "chooseSnippetBranch(snippetId) валиден только когда runnerStatus === 'at-node' и текущий узел question"
    - "chooseSnippetBranch пушит UndoEntry с флагом returnToBranchList=true ДО мутации"
    - "chooseSnippetBranch устанавливает currentNodeId на snippet-узел и runnerStatus = 'awaiting-snippet-pick' без вставки в accumulator"
    - "stepBack с записью returnToBranchList=true восстанавливает question-узел и runnerStatus='at-node', очищая snippetId/snippetNodeId"
    - "completeSnippet применяет per-node snippetSeparator override для kind='snippet' (в дополнение к существующему text-block path)"
    - "resolveSeparator работает для SnippetNode через поле radiprotocol_snippetSeparator"
    - "getSerializableState / restoreFrom round-trip returnToBranchList flag в undoStack entries"
  artifacts:
    - path: "src/runner/protocol-runner.ts"
      provides: "chooseSnippetBranch method, UndoEntry.returnToBranchList support, resolveSeparator overload for snippet, completeSnippet snippet-kind separator hook, serialization round-trip"
      contains: "chooseSnippetBranch"
  key_links:
    - from: "chooseSnippetBranch"
      to: "undoStack.push({...returnToBranchList: true})"
      via: "before mutation"
      pattern: "returnToBranchList: true"
    - from: "stepBack"
      to: "entry.returnToBranchList"
      via: "early return branch"
      pattern: "returnToBranchList"
    - from: "completeSnippet"
      to: "resolveSeparator(snippetNode) for kind==='snippet'"
      via: "expanded condition"
      pattern: "kind === 'snippet'"
---

<objective>
Расширить чистый state-machine `ProtocolRunner` для поддержки branch-entered snippet picker: новый метод `chooseSnippetBranch`, флаг `returnToBranchList` в UndoEntry, расширение `resolveSeparator` и `completeSnippet` на `kind === 'snippet'`, и serialization round-trip нового флага. Plus: runner-state.ts `UndoEntry` gets the new optional field.

Purpose: Делает всё runtime-поведение фазы 31 реальным — без этого runner не сможет обработать клик по snippet-варианту на развилке или правильно выполнить step-back.
Output: protocol-runner.ts + runner-state.ts UndoEntry type + расширенные тесты state-machine и session resume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-RESEARCH.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-VALIDATION.md
@src/runner/protocol-runner.ts
@src/runner/runner-state.ts
@.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-01-PLAN.md

<interfaces>
<!-- runner-state.ts:95-103 UndoEntry: -->
```ts
export interface UndoEntry {
  nodeId: string;
  textSnapshot: string;
  loopContextStack: LoopContext[];
}
```

<!-- protocol-runner.ts:73-101 chooseAnswer pattern (copy shape for chooseSnippetBranch): -->
```ts
chooseAnswer(answerId: string): void {
  if (this.runnerStatus !== 'at-node') return;
  if (this.graph === null || this.currentNodeId === null) return;
  const answerNode = this.graph.nodes.get(answerId);
  if (answerNode === undefined || answerNode.kind !== 'answer') {
    this.transitionToError(`Answer node '${answerId}' not found or is not an answer node.`);
    return;
  }
  this.undoStack.push({
    nodeId: this.currentNodeId,
    textSnapshot: this.accumulator.snapshot(),
    loopContextStack: [...this.loopContextStack],
  });
  this.accumulator.appendWithSeparator(answerNode.answerText, this.resolveSeparator(answerNode));
  const neighbors = this.graph.adjacency.get(answerId);
  const next = neighbors !== undefined ? neighbors[0] : undefined;
  if (next === undefined) { this.transitionToComplete(); return; }
  this.advanceThrough(next);
}
```

<!-- protocol-runner.ts:147-158 stepBack: -->
```ts
stepBack(): void {
  const entry = this.undoStack.pop();
  if (entry === undefined) return;
  this.currentNodeId = entry.nodeId;
  this.accumulator.restoreTo(entry.textSnapshot);
  this.loopContextStack = [...entry.loopContextStack];
  this.runnerStatus = 'at-node';
  this.errorMessage = null;
  this.snippetId = null;
  this.snippetNodeId = null;
}
```

<!-- protocol-runner.ts completeSnippet separator hook (lines 189-211, actual hook ~195): -->
```ts
const snippetSep = (snippetNode?.kind === 'text-block')
  ? this.resolveSeparator(snippetNode)
  : this.defaultSeparator;
this.accumulator.appendWithSeparator(renderedText, snippetSep);
```

<!-- protocol-runner.ts:440-452 resolveSeparator: -->
```ts
private resolveSeparator(
  node: AnswerNode | FreeTextInputNode | TextBlockNode,
): 'newline' | 'space' {
  return node.radiprotocol_separator ?? this.defaultSeparator;
}
```

<!-- Phase 30 pickSnippet is the existing method for picker → fill transition. chooseSnippetBranch is DIFFERENT:
     it transitions at-node(question) → awaiting-snippet-pick with the snippet-node as currentNodeId. -->

<!-- getSerializableState undoStack serialise (protocol-runner.ts:380-384): -->
```ts
undoStack: this.undoStack.map(e => ({
  nodeId: e.nodeId,
  textSnapshot: e.textSnapshot,
  loopContextStack: e.loopContextStack.map(f => ({ ...f })),
})),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UndoEntry.returnToBranchList + chooseSnippetBranch + stepBack + resolveSeparator/completeSnippet extension</name>
  <files>src/runner/runner-state.ts, src/runner/protocol-runner.ts, src/__tests__/runner/protocol-runner.test.ts</files>
  <read_first>
    - src/runner/runner-state.ts (lines 95-103 UndoEntry)
    - src/runner/protocol-runner.ts (lines 73-101 chooseAnswer, 147-158 stepBack, 189-211 completeSnippet, 440-452 resolveSeparator, и existing `case 'snippet'` в advanceThrough — для context)
    - src/__tests__/runner/protocol-runner.test.ts (существующие тесты `pickSnippet`, `completeSnippet` для pattern/fixtures)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-PATTERNS.md §4a, §4b, §4c, §4d
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-RESEARCH.md (разделы "Step-back / UndoStack Semantics" и "Separator integration")
    - CLAUDE.md (append-only discipline в protocol-runner.ts)
  </read_first>
  <behavior>
    Fixture: graph = question Q → [answer A1, answer A2, snippet S1, snippet S2], где S1.subfolderPath='foo', S2.radiprotocol_snippetSeparator='space'.
    - Test 1: runner at Q → `chooseSnippetBranch(S1.id)` → runnerStatus === 'awaiting-snippet-pick' && currentNodeId === S1.id && accumulator unchanged
    - Test 2: после Test 1 → undoStack.length === 1 && top.nodeId === Q.id && top.returnToBranchList === true
    - Test 3: после Test 1 → `stepBack()` → runnerStatus === 'at-node' && currentNodeId === Q.id && snippetId/snippetNodeId === null && undoStack.length === 0
    - Test 4: runner at Q → `chooseSnippetBranch('nonexistent')` → transitionToError called; runnerStatus === 'error'
    - Test 5: runner at Q → `chooseSnippetBranch(A1.id)` (wrong kind) → transitionToError; status === 'error'
    - Test 6: runner at Q → `chooseSnippetBranch(S1.id)` из состояния где текущий узел не question — попытка с runnerStatus='at-node' но currentNodeId указывает на text-block — отклоняется (no-op или error; use same guard as chooseAnswer)
    - Test 7 (D-04): `completeSnippet('hello')` когда currentNodeId=S2 и S2.radiprotocol_snippetSeparator='space' → accumulator join использует space (не глобальный default 'newline')
    - Test 8 (D-04): `completeSnippet('hello')` когда currentNodeId=S1 и S1 без snippetSeparator → использует defaultSeparator runner'а
    - Test 9 (regression Phase 30, Path A): войти в snippet-узел через auto-advance после answer → step-back откатывает к предшественнику (не к branch list), потому что UndoEntry пушит chooseAnswer без returnToBranchList
    - Test 10: chain — chooseSnippetBranch → pickSnippet → stepBack (откат с fill на pick) → stepBack (откат с pick к branch list, question видно снова) → stepBack снова (полный откат дальше question'а, если возможен)
  </behavior>
  <action>
    1. **runner-state.ts** — расширить `UndoEntry` optional флагом:
       ```ts
       export interface UndoEntry {
         nodeId: string;
         textSnapshot: string;
         loopContextStack: LoopContext[];
         /** Phase 31 D-08: marks an entry pushed by chooseSnippetBranch. stepBack
          *  restores currentNodeId (the question) and runnerStatus='at-node' — so the
          *  user returns to the branch list, not the question's predecessor. */
         returnToBranchList?: boolean;
       }
       ```
       НЕ трогать другие interfaces.

    2. **protocol-runner.ts — resolveSeparator extension (~line 446)**:
       ```ts
       private resolveSeparator(
         node: AnswerNode | FreeTextInputNode | TextBlockNode | SnippetNode,
       ): 'newline' | 'space' {
         if (node.kind === 'snippet') {
           return node.radiprotocol_snippetSeparator ?? this.defaultSeparator;
         }
         return node.radiprotocol_separator ?? this.defaultSeparator;
       }
       ```
       Импорт `SnippetNode` type добавить наверху файла если ещё не импортирован.

    3. **protocol-runner.ts — completeSnippet separator hook (~line 195)**: расширить условие:
       ```ts
       const snippetSep = (snippetNode?.kind === 'text-block' || snippetNode?.kind === 'snippet')
         ? this.resolveSeparator(snippetNode)
         : this.defaultSeparator;
       ```
       Всё остальное в completeSnippet не трогать.

    4. **protocol-runner.ts — new method `chooseSnippetBranch`**. Разместить рядом с `chooseAnswer` (после него). Копировать shape chooseAnswer, но:
       - Валидировать `runnerStatus === 'at-node'`, `this.graph !== null`, `this.currentNodeId !== null`
       - Валидировать текущий узел: `currentNode.kind === 'question'` (иначе `transitionToError` с понятным сообщением)
       - Валидировать target: `snippetNode.kind === 'snippet'` AND `this.graph.adjacency.get(this.currentNodeId)?.includes(snippetNodeId) === true` (иначе transitionToError)
       - **Push UndoEntry BEFORE mutation** (Pitfall 1) с `returnToBranchList: true`:
         ```ts
         this.undoStack.push({
           nodeId: this.currentNodeId,  // question id
           textSnapshot: this.accumulator.snapshot(),
           loopContextStack: [...this.loopContextStack],
           returnToBranchList: true,
         });
         ```
       - Мутация: `this.currentNodeId = snippetNodeId; this.runnerStatus = 'awaiting-snippet-pick';` и выставить `this.snippetNodeId = snippetNodeId` если это поле используется Phase 30 picker (см. Phase 30 pickSnippet — скопировать ту же инициализацию которую делает Phase 30 `advanceThrough case 'snippet'`). НЕ вызывать `advanceThrough`, НЕ трогать `accumulator`.

    5. **protocol-runner.ts — stepBack (~line 147)**: добавить early branch ДО остальной логики:
       ```ts
       stepBack(): void {
         const entry = this.undoStack.pop();
         if (entry === undefined) return;
         if (entry.returnToBranchList === true) {
           // Phase 31 D-08: close the picker, return to the question branch list.
           this.currentNodeId = entry.nodeId;
           this.accumulator.restoreTo(entry.textSnapshot);
           this.loopContextStack = [...entry.loopContextStack];
           this.runnerStatus = 'at-node';
           this.snippetId = null;
           this.snippetNodeId = null;
           this.errorMessage = null;
           return;
         }
         // existing body unchanged
         this.currentNodeId = entry.nodeId;
         this.accumulator.restoreTo(entry.textSnapshot);
         this.loopContextStack = [...entry.loopContextStack];
         this.runnerStatus = 'at-node';
         this.errorMessage = null;
         this.snippetId = null;
         this.snippetNodeId = null;
       }
       ```

    6. **protocol-runner.ts — getSerializableState / restoreFrom (~lines 359-435)**:
       - В `getSerializableState` в `.map(e => ({...}))` добавить `returnToBranchList: e.returnToBranchList,`
       - В `restoreFrom` в симметричном `.map` для восстановления undoStack добавить `returnToBranchList: e.returnToBranchList,`
       - Обновить TypeScript return type / parameter type так, чтобы элемент undoStack включал `returnToBranchList?: boolean`
       - НЕ добавлять новых top-level полей; НЕ менять whitelist resumable statuses

    7. **src/__tests__/runner/protocol-runner.test.ts** — новый describe `Phase 31: chooseSnippetBranch + branch-list step-back + per-node snippet separator` с тестами 1-10 из `<behavior>`. Fixture-factory — скопировать стиль существующих тестов (которые строят Graph вручную с nodes Map и adjacency Map).
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/runner/protocol-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "returnToBranchList" src/runner/runner-state.ts` → 1 match (на определении поля)
    - `grep -n "chooseSnippetBranch" src/runner/protocol-runner.ts` → ≥1 match
    - `grep -n "returnToBranchList: true" src/runner/protocol-runner.ts` → ≥1 match (push в chooseSnippetBranch)
    - `grep -n "returnToBranchList === true" src/runner/protocol-runner.ts` → 1 match (stepBack early branch)
    - `grep -n "kind === 'snippet'" src/runner/protocol-runner.ts` → ≥2 matches (resolveSeparator + completeSnippet)
    - `grep -c "returnToBranchList" src/runner/protocol-runner.ts` ≥ 4 (push + stepBack check + getSerializableState + restoreFrom)
    - `grep -n "Phase 31" src/__tests__/runner/protocol-runner.test.ts` → ≥1 match
    - vitest: все тесты зелёные, **включая** существующие Phase 30 pickSnippet/completeSnippet тесты (regression guard)
    - `grep -c "chooseAnswer" src/runner/protocol-runner.ts` ≥ 1 (chooseAnswer не удалён)
    - `grep -c "pickSnippet" src/runner/protocol-runner.ts` ≥ 1 (Phase 30 pickSnippet не удалён)
  </acceptance_criteria>
  <done>protocol-runner обрабатывает chooseSnippetBranch, stepBack различает branch-entered picker от auto-advanced, completeSnippet применяет per-node separator к snippet-узлам. Все unit тесты зелёные; zero regressions в Phase 30 тестах.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session resume round-trip для returnToBranchList flag</name>
  <files>src/__tests__/runner/protocol-runner-session.test.ts</files>
  <read_first>
    - src/__tests__/runner/protocol-runner-session.test.ts (существующие session round-trip тесты)
    - src/runner/protocol-runner.ts — getSerializableState / restoreFrom (после изменений Task 1)
    - .planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-CONTEXT.md D-09
  </read_first>
  <behavior>
    - Test 1: runner at question Q → `chooseSnippetBranch(S1.id)` → `getSerializableState()` → новый runner + `restoreFrom(state)` → runnerStatus === 'awaiting-snippet-pick' && currentNodeId === S1.id && undoStack[0].returnToBranchList === true
    - Test 2: после restore → `stepBack()` → runnerStatus === 'at-node' && currentNodeId === Q.id (подтверждает что flag пережил round-trip и семантика работает)
    - Test 3: runner at question без выбора (только branch list открыт, никаких кликов) → serialize/restore → runnerStatus === 'at-node' && currentNodeId === Q.id (resume показывает branch list заново, D-09)
  </behavior>
  <action>
    1. Добавить в `src/__tests__/runner/protocol-runner-session.test.ts` новый describe `Phase 31 D-09: branch-entered picker session round-trip`.
    2. Использовать существующий fixture helper/pattern файла (не изобретать новый — копировать стиль соседних session тестов).
    3. Написать три теста из `<behavior>`.
    4. Не трогать существующие тесты в файле.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/runner/protocol-runner-session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 31" src/__tests__/runner/protocol-runner-session.test.ts` → ≥1 match
    - `grep -n "returnToBranchList" src/__tests__/runner/protocol-runner-session.test.ts` → ≥1 match
    - vitest session file: все существующие тесты зелёные + 3 новых теста фазы 31 зелёные
  </acceptance_criteria>
  <done>Session serialize/restore корректно round-trip'ит флаг `returnToBranchList` в undoStack entries; resume branch-entered picker работает.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` вся suite зелёная
- `npm run build` без TypeScript ошибок
- `git diff src/runner/protocol-runner.ts` показывает ТОЛЬКО: импорт SnippetNode (если нужен), новый chooseSnippetBranch метод, расширенный resolveSeparator, расширенное условие в completeSnippet, early branch в stepBack, новое поле в getSerializableState/restoreFrom. Ничего не удалено.
</verification>

<success_criteria>
- Runner предоставляет `chooseSnippetBranch(snippetNodeId)` с правильной валидацией и undo snapshot
- Step-back из branch-entered picker возвращает к question (не предшественнику)
- Per-node `radiprotocol_snippetSeparator` применяется во всех snippet код-путях (branching + Phase 30 auto-advance)
- Session resume round-trip'ит `returnToBranchList`
- Zero регрессий в Phase 30 runner/session тестах
</success_criteria>

<output>
Создать `.planning/phases/31-mixed-answer-and-snippet-branching-at-question-nodes/31-03-SUMMARY.md` (на русском) после завершения.
</output>
</content>
</invoke>