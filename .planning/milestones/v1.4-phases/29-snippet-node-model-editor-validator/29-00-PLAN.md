---
phase: 29-snippet-node-model-editor-validator
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/fixtures/snippet-node.canvas
  - src/__tests__/fixtures/snippet-node-no-path.canvas
  - src/__tests__/canvas-parser.test.ts
  - src/__tests__/graph-validator.test.ts
autonomous: true
requirements:
  - SNIPPET-NODE-01
  - SNIPPET-NODE-08

must_haves:
  truths:
    - "canvas-parser.test.ts содержит тесты для snippet kind, которые сначала КРАСНЫЕ (до Plan 01)"
    - "graph-validator.test.ts содержит тест: snippet-узел без subfolderPath не генерирует ошибок"
    - "Оба fixture-файла существуют и содержат валидный JSON canvas-формата"
  artifacts:
    - path: "src/__tests__/fixtures/snippet-node.canvas"
      provides: "Fixture: snippet-узел с radiprotocol_subfolderPath = 'CT/adrenal'"
      contains: "radiprotocol_nodeType.*snippet"
    - path: "src/__tests__/fixtures/snippet-node-no-path.canvas"
      provides: "Fixture: snippet-узел без radiprotocol_subfolderPath"
      contains: "radiprotocol_nodeType.*snippet"
    - path: "src/__tests__/canvas-parser.test.ts"
      provides: "Tests: parse snippet kind with/without subfolderPath"
      exports: []
    - path: "src/__tests__/graph-validator.test.ts"
      provides: "Test: snippet node without subfolderPath passes validation"
      exports: []
  key_links:
    - from: "src/__tests__/fixtures/snippet-node.canvas"
      to: "src/__tests__/canvas-parser.test.ts"
      via: "loadFixture('snippet-node.canvas')"
      pattern: "snippet-node\\.canvas"
    - from: "src/__tests__/fixtures/snippet-node-no-path.canvas"
      to: "src/__tests__/graph-validator.test.ts"
      via: "parseFixture('snippet-node-no-path.canvas')"
      pattern: "snippet-node-no-path\\.canvas"
---

<objective>
Создать тест-инфраструктуру (fixtures + failing test cases) для фазы 29.

Purpose: TDD-подход — тесты пишутся до реализации. После Wave 0 тесты RED (провалят, т.к. 'snippet' ещё не в RPNodeKind). После Wave 1 они станут GREEN.
Output: 2 fixture-файла, новые it() cases в canvas-parser.test.ts и graph-validator.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-snippet-node-model-editor-validator/29-CONTEXT.md
@.planning/phases/29-snippet-node-model-editor-validator/29-RESEARCH.md

<interfaces>
<!-- Fixture format: по образцу src/__tests__/fixtures/linear.canvas -->
<!-- Тест-паттерн: по образцу src/__tests__/canvas-parser.test.ts (loadFixture + describe + it) -->
<!-- GraphValidator тесты: по образцу src/__tests__/graph-validator.test.ts (parseFixture + validate) -->

Существующий паттерн fixture (linear.canvas):
```json
{
  "nodes": [
    {
      "id": "n-start",
      "type": "text",
      "text": "Start",
      "x": 0, "y": 0, "width": 200, "height": 60,
      "radiprotocol_nodeType": "start"
    }
  ],
  "edges": [...]
}
```

Существующий паттерн теста (canvas-parser.test.ts):
```typescript
it('parses linear.canvas and returns a ProtocolGraph with correct node kinds', () => {
  const parser = new CanvasParser();
  const result = parser.parse(loadFixture('linear.canvas'), 'linear.canvas');
  expect(result.success).toBe(true);
  if (!result.success) return;
  expect(result.graph.nodes.get('n-start')?.kind).toBe('start');
});
```

GraphValidator тест-паттерн:
```typescript
function parseFixture(name: string) {
  const parser = new CanvasParser();
  const result = parser.parse(loadFixture(name), name);
  if (!result.success) throw new Error(`Fixture ${name} failed to parse: ${result.error}`);
  return result.graph;
}
it('returns no errors for linear.canvas', () => {
  const graph = parseFixture('linear.canvas');
  const validator = new GraphValidator();
  const errors = validator.validate(graph);
  expect(errors).toHaveLength(0);
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать canvas fixtures для snippet-узла</name>
  <files>src/__tests__/fixtures/snippet-node.canvas, src/__tests__/fixtures/snippet-node-no-path.canvas</files>
  <read_first>
    - src/__tests__/fixtures/linear.canvas (паттерн формата fixture — скопировать структуру)
    - src/__tests__/fixtures/text-block.canvas (пример optional-поля в fixture)
  </read_first>
  <behavior>
    - snippet-node.canvas: минимальный валидный граф — start → snippet. snippet-узел содержит `radiprotocol_nodeType: "snippet"` и `radiprotocol_subfolderPath: "CT/adrenal"`. Поле `text` = `"CT/adrenal"` (per D-10).
    - snippet-node-no-path.canvas: то же самое, но без поля `radiprotocol_subfolderPath`. Узел всё равно валидный (D-12 — отсутствие пути = корень).
  </behavior>
  <action>
Создать `src/__tests__/fixtures/snippet-node.canvas`:
```json
{
  "nodes": [
    {
      "id": "n-start",
      "type": "text",
      "text": "Start",
      "x": 0, "y": 0, "width": 200, "height": 60,
      "radiprotocol_nodeType": "start"
    },
    {
      "id": "n-snippet1",
      "type": "text",
      "text": "CT/adrenal",
      "x": 0, "y": 120, "width": 200, "height": 60,
      "radiprotocol_nodeType": "snippet",
      "radiprotocol_subfolderPath": "CT/adrenal"
    }
  ],
  "edges": [
    { "id": "e1", "fromNode": "n-start", "toNode": "n-snippet1" }
  ]
}
```

Создать `src/__tests__/fixtures/snippet-node-no-path.canvas`:
```json
{
  "nodes": [
    {
      "id": "n-start",
      "type": "text",
      "text": "Start",
      "x": 0, "y": 0, "width": 200, "height": 60,
      "radiprotocol_nodeType": "start"
    },
    {
      "id": "n-snippet1",
      "type": "text",
      "text": "",
      "x": 0, "y": 120, "width": 200, "height": 60,
      "radiprotocol_nodeType": "snippet"
    }
  ],
  "edges": [
    { "id": "e1", "fromNode": "n-start", "toNode": "n-snippet1" }
  ]
}
```
  </action>
  <verify>
    <automated>MISSING — Wave 0 создаёт эти файлы; тесты из Task 2 подтвердят корректность после Plan 01</automated>
  </verify>
  <done>Оба файла существуют, содержат валидный JSON, `radiprotocol_nodeType` = `"snippet"` присутствует в каждом</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Написать failing tests для парсера и валидатора</name>
  <files>src/__tests__/canvas-parser.test.ts, src/__tests__/graph-validator.test.ts</files>
  <read_first>
    - src/__tests__/canvas-parser.test.ts (прочитать полностью — добавляем в конец, не перезаписываем)
    - src/__tests__/graph-validator.test.ts (прочитать полностью — добавляем в конец)
    - src/__tests__/fixtures/snippet-node.canvas (только что созданный — для уверенности в именах полей)
  </read_first>
  <behavior>
    - canvas-parser тест 1: `parses snippet-node.canvas — returns SnippetNode with kind "snippet" and subfolderPath "CT/adrenal"` → ожидаем `node.kind === 'snippet'`, `(node as any).subfolderPath === 'CT/adrenal'`
    - canvas-parser тест 2: `parses snippet-node-no-path.canvas — returns SnippetNode with subfolderPath undefined` → ожидаем `node.kind === 'snippet'`, `(node as any).subfolderPath === undefined`
    - graph-validator тест: `returns no errors for snippet-node-no-path.canvas (D-12 — missing subfolderPath is valid)` → `errors.length === 0`
    - Эти тесты RED после Wave 0 (т.к. `'snippet'` не в `validKinds[]` ещё), GREEN после Plan 01.
  </behavior>
  <action>
В `src/__tests__/canvas-parser.test.ts` добавить новый `describe` блок в конец файла (ПОСЛЕ закрывающей скобки последнего describe, ПЕРЕД закрывающей скобкой файла, или как отдельный describe):

```typescript
describe('CanvasParser — snippet node (Phase 29)', () => {
  it('parses snippet-node.canvas — returns SnippetNode with kind "snippet" and subfolderPath "CT/adrenal"', () => {
    const parser = new CanvasParser();
    const result = parser.parse(loadFixture('snippet-node.canvas'), 'snippet-node.canvas');
    expect(result.success).toBe(true);
    if (!result.success) return;
    const node = result.graph.nodes.get('n-snippet1');
    expect(node).toBeDefined();
    expect(node?.kind).toBe('snippet');
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    expect((node as any).subfolderPath).toBe('CT/adrenal');
  });

  it('parses snippet-node-no-path.canvas — returns SnippetNode with subfolderPath undefined', () => {
    const parser = new CanvasParser();
    const result = parser.parse(loadFixture('snippet-node-no-path.canvas'), 'snippet-node-no-path.canvas');
    expect(result.success).toBe(true);
    if (!result.success) return;
    const node = result.graph.nodes.get('n-snippet1');
    expect(node).toBeDefined();
    expect(node?.kind).toBe('snippet');
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    expect((node as any).subfolderPath).toBeUndefined();
  });
});
```

В `src/__tests__/graph-validator.test.ts` добавить новый `describe` блок в конец файла:

```typescript
describe('GraphValidator — snippet node (Phase 29, D-12)', () => {
  it('returns no errors for snippet-node-no-path.canvas (missing subfolderPath is valid per D-12)', () => {
    const graph = parseFixture('snippet-node-no-path.canvas');
    const validator = new GraphValidator();
    const errors = validator.validate(graph);
    expect(errors).toHaveLength(0);
  });
});
```

КРИТИЧНО: Не удалять и не изменять существующие тесты. Только добавлять в конец файла.
  </action>
  <verify>
    <automated>npm test -- canvas-parser 2>&1 | tail -20</automated>
  </verify>
  <done>Тесты добавлены. `npm test -- canvas-parser` показывает новые failing tests с сообщением вида "Node 'n-snippet1' has unknown radiprotocol_nodeType: 'snippet'" — это ожидаемо RED-состояние TDD. Существующие тесты по-прежнему GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → parser | Тестовые JSON-данные поступают в CanvasParser.parse() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-00-01 | Tampering | fixture files | accept | Fixtures — статические тестовые данные в репо; изменяются только через git |
</threat_model>

<verification>
После Wave 0:
1. `ls src/__tests__/fixtures/snippet-node.canvas src/__tests__/fixtures/snippet-node-no-path.canvas` — оба файла существуют
2. `npm test -- canvas-parser` — существующие тесты GREEN; новые тесты RED (expected: snippet kind unknown)
3. `npm test -- graph-validator` — существующие тесты GREEN; новый тест RED (fixture fails to parse)
</verification>

<success_criteria>
- Оба fixture-файла существуют с корректным JSON
- `snippet-node.canvas` содержит `"radiprotocol_subfolderPath": "CT/adrenal"`
- `snippet-node-no-path.canvas` НЕ содержит поля `radiprotocol_subfolderPath`
- `canvas-parser.test.ts` содержит describe "snippet node (Phase 29)" с 2 it() блоками
- `graph-validator.test.ts` содержит describe "snippet node (Phase 29, D-12)" с 1 it() блоком
- Все ранее существовавшие тесты остаются GREEN
</success_criteria>

<output>
После выполнения создать `.planning/phases/29-snippet-node-model-editor-validator/29-00-SUMMARY.md`
</output>
