---
phase: 33
plan: 04
type: execute
wave: 3
depends_on: [1, 2, 3]
files_modified:
  - src/views/snippet-manager-view.ts
  - src/styles/snippet-manager.css
  - src/__tests__/snippet-tree-view.test.ts
  - src/__tests__/snippet-vault-watcher.test.ts
autonomous: true
requirements: [TREE-01, TREE-02, TREE-03, TREE-04, FOLDER-01, FOLDER-02, FOLDER-03, SYNC-01, SYNC-02, SYNC-03, DEL-02, DEL-03, MODAL-04]
must_haves:
  truths:
    - "SnippetManagerView renders a collapsible folder tree rooted at settings.snippetFolderPath"
    - "Folders and files show lucide type icons; empty folders render with '(пусто)' label"
    - "Clicking a file row opens SnippetEditorModal in edit mode"
    - "Global '+ Новый' button opens SnippetEditorModal in create mode with folder pre-filled to root"
    - "Per-folder hover '+ Новый' button opens create modal with that folder pre-filled"
    - "Folder context menu offers 'Создать сниппет здесь', 'Создать подпапку', 'Удалить папку'"
    - "Snippet context menu offers 'Редактировать', 'Удалить'"
    - "Deleting a snippet or folder opens ConfirmModal, then calls service.delete / service.deleteFolder"
    - "Vault create/delete/rename events inside snippetFolderPath trigger a 120ms debounced redraw"
    - "Vault events outside snippetFolderPath are ignored"
    - "onClose unregisters event listeners (via registerEvent auto-detach)"
    - "Expand/collapse state persists in settings.snippetTreeExpandedPaths across reloads"
    - "Legacy master-detail layout is fully removed from snippet-manager-view.ts"
  artifacts:
    - path: "src/views/snippet-manager-view.ts"
      provides: "SnippetManagerView (rewritten as tree view), same SNIPPET_MANAGER_VIEW_TYPE constant"
      exports: ["SnippetManagerView", "SNIPPET_MANAGER_VIEW_TYPE"]
      contains: "registerEvent"
      min_lines: 350
    - path: "src/styles/snippet-manager.css"
      provides: "Tree view CSS under radi-snippet-tree-* prefix + modal CSS under radi-snippet-editor-modal-*"
      contains: "radi-snippet-tree-row"
    - path: "src/__tests__/snippet-tree-view.test.ts"
      provides: "TREE-01..04, FOLDER-01..03, DEL-02/03 coverage"
    - path: "src/__tests__/snippet-vault-watcher.test.ts"
      provides: "SYNC-01..03 coverage with prefix-filter assertions"
  key_links:
    - from: "SnippetManagerView row click"
      to: "new SnippetEditorModal(app, plugin, { mode: 'edit', snippet })"
      via: "await modal.result then redraw"
      pattern: "mode: 'edit'"
    - from: "SnippetManagerView vault watcher"
      to: "debounced redraw via setTimeout"
      via: "registerEvent + prefix filter + clearTimeout"
      pattern: "registerEvent"
    - from: "SnippetManagerView.onClose"
      to: "Obsidian auto-detach of registerEvent refs + clearTimeout of pending debounce"
      via: "framework-managed cleanup"
      pattern: "clearTimeout"
---

<objective>
Rewrite `SnippetManagerView` as a vanilla-TS recursive folder-tree backed by `ItemView`, wire vault watchers with prefix filter + 120ms debounced redraw, wire context menus and hover buttons for folder/snippet CRUD, and integrate `SnippetEditorModal` as the sole create/edit surface. This is the largest plan in Phase 33 — it connects every earlier artifact into a single working experience.

Purpose: Ship the user-facing Phase 33 experience end-to-end. All service methods (Plan 01), extracted chip editor + generic ConfirmModal (Plan 02), and the unified SnippetEditorModal (Plan 03) come together here. After this plan, the feature is functionally complete.

Output: Rewritten `src/views/snippet-manager-view.ts` (same filename, same view-type constant, new internal implementation), updated `src/styles/snippet-manager.css` with tree styles + modal styles, and real tests in the Wave 0 stub files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-CONTEXT.md
@.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-UI-SPEC.md
@.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-RESEARCH.md
@.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-PATTERNS.md
@CLAUDE.md

@src/views/snippet-manager-view.ts
@src/views/editor-panel-view.ts
@src/views/runner-view.ts
@src/views/snippet-editor-modal.ts
@src/views/confirm-modal.ts
@src/snippets/snippet-service.ts
@src/main.ts
@src/styles/snippet-manager.css

<interfaces>
Preserved public surface (must not change):
```typescript
export const SNIPPET_MANAGER_VIEW_TYPE = 'radiprotocol-snippet-manager';
export class SnippetManagerView extends ItemView {
  constructor(leaf: WorkspaceLeaf, plugin: RadiProtocolPlugin);
  getViewType(): string;
  getDisplayText(): string;
  getIcon(): string;
  onOpen(): Promise<void>;
  onClose(): Promise<void>;
}
```

From src/views/snippet-editor-modal.ts (Plan 03):
```typescript
export class SnippetEditorModal extends Modal {
  readonly result: Promise<SnippetEditorResult>;
  constructor(app: App, plugin: RadiProtocolPlugin, options: SnippetEditorOptions);
}
```

From src/views/confirm-modal.ts (Plan 02):
```typescript
export class ConfirmModal extends Modal {
  readonly result: Promise<ConfirmResult>;
  constructor(app: App, options: ConfirmModalOptions);
}
```

From src/snippets/snippet-service.ts (Plan 01 additions):
```typescript
listFolder(path: string): Promise<SnippetListEntry[]>;
createFolder(path: string): Promise<void>;
deleteFolder(path: string): Promise<void>;
listFolderDescendants(path: string): Promise<{ files: string[]; folders: string[]; total: number }>;
delete(path: string): Promise<void>;
```

From src/settings.ts (Plan 01 addition):
```typescript
interface RadiProtocolSettings {
  snippetFolderPath: string;
  snippetTreeExpandedPaths: string[];
  // ...
}
```

Tree model (internal to this view):
```typescript
interface TreeNodeFolder { kind: 'folder'; path: string; name: string; children: TreeNode[]; }
interface TreeNodeFile { kind: 'file'; path: string; name: string; snippetKind: 'json' | 'md'; }
type TreeNode = TreeNodeFolder | TreeNodeFile;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite SnippetManagerView as recursive tree view with watchers + context menus + modal wiring</name>
  <files>src/views/snippet-manager-view.ts</files>
  <read_first>
    - src/views/snippet-manager-view.ts (entire legacy file — understand what is deleted)
    - src/views/editor-panel-view.ts (ItemView skeleton pattern lines 8-51)
    - src/views/runner-view.ts (vault.on + registerEvent pattern lines 178-202)
    - src/views/snippet-editor-modal.ts (Plan 03 output)
    - src/views/confirm-modal.ts (Plan 02 output)
    - src/snippets/snippet-service.ts (service API — especially listFolder, delete, deleteFolder, createFolder, listFolderDescendants)
    - src/main.ts (registration — lines 8, 62, 161-165 to preserve)
    - src/settings.ts (snippetFolderPath, snippetTreeExpandedPaths)
    - .planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-UI-SPEC.md (full — DOM contract lines 205-213, icons 218-232, interaction states 234-242, context menu 249-264, debounce 266-268, copy contract 108-188)
    - .planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-CONTEXT.md (D-01, D-02, D-03, D-04, D-13, D-14, D-15, D-18)
    - .planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-PATTERNS.md (tree view section lines 26-108)
  </read_first>
  <behavior>
    File is rewritten wholesale. Preserve only:
    - `export const SNIPPET_MANAGER_VIEW_TYPE = 'radiprotocol-snippet-manager';` (unchanged line — so main.ts needs no edit)
    - `export class SnippetManagerView extends ItemView { ... }` class name (so main.ts import line 8 keeps working)

    Delete entirely: master-detail layout rendering, id-based path logic, legacy form panel, legacy chip editor code (extracted in Plan 02 — the modal now owns it).

    New internal shape:
    1. Fields: `plugin`, `treeRootEl: HTMLElement`, `treeData: TreeNode[]`, `currentlyEditingPath: string | null`, `redrawTimer: number | null`.
    2. `onOpen()`:
       - Build the view chrome: a header strip with a `+ Новый` button (icon `plus`, text «Новый»), and a tree container below.
       - Call `await this.rebuildTreeModel()` then `this.renderTree()`.
       - Register three vault event listeners via `this.registerEvent(this.app.vault.on('create' | 'delete' | 'rename', handler))` — each handler applies D-18 prefix filter, then calls `this.scheduleRedraw()`.
       - Register DOM events on header `+ Новый` button via `this.registerDomEvent`.
    3. `onClose()`: clear any pending `redrawTimer`, empty `contentEl`. Vault events auto-detach via `registerEvent`.
    4. `rebuildTreeModel(): Promise<void>` — walks `plugin.snippetService.listFolder(root)` recursively into a `TreeNode[]` tree. For each folder, recurse. Apply D-04 sort (folders first, then files, both A-Z via localeCompare).
    5. `renderTree(): void` — empties `treeRootEl`, then calls `renderNode(container, node, depth)` for each root-level child. Empty root shows the empty-state copy from UI-SPEC. Expand state is read from `plugin.settings.snippetTreeExpandedPaths` — any path in that array is rendered expanded.
    6. `renderNode(container, node, depth)` — creates `<div class="radi-snippet-tree-row" data-path=... data-kind=...>` with:
       - `<span class="radi-snippet-tree-indent" style="width: ${depth * 16}px">`
       - Chevron span (folders only) — `setIcon(span, expanded ? 'chevron-down' : 'chevron-right')`
       - Icon span — `setIcon(span, iconForNode(node))` where iconForNode maps folder → `folder` / `folder-open`, json → `file-json`, md → `file-text`
       - Label span with `node.name`
       - Hover actions span (folders only) — contains a `+ Новый` button (icon `plus`) with tooltip `Создать в этой папке` — opacity 0 by default, 1 on hover
       - For folders: if empty children array AND expanded, render a single muted `(пусто)` row as the only child (TREE-04 empty folders)
       - For folders: if expanded, recurse into children with `depth + 1`
    7. Row click behavior:
       - File row → `openEditModal(node.path)`
       - Folder row → `toggleExpand(node.path)`: mutate `settings.snippetTreeExpandedPaths` (add/remove), `await plugin.saveSettings()`, `renderTree()`.
    8. Row hover `+ Новый` (folder only) → `openCreateModal(node.path)` — opens SnippetEditorModal with `initialFolder: node.path`.
    9. Row right-click (both file and folder) → open Obsidian `Menu`:
       - File: `Редактировать` → `openEditModal(path)`; separator; `Удалить` (icon `trash`) → `handleDeleteSnippet(path, name)`
       - Folder: `Создать сниппет здесь` (icon `plus`) → `openCreateModal(path)`; `Создать подпапку` (icon `folder-plus`) → `handleCreateSubfolder(path)`; separator; `Удалить папку` (icon `trash`) → `handleDeleteFolder(path, name)`
       Use `Menu` class imported from `'obsidian'`. Attach via `this.registerDomEvent(row, 'contextmenu', ...)`.
    10. Global header `+ Новый` button → `openCreateModal(plugin.settings.snippetFolderPath)` (D-10 — root).
    11. `openEditModal(path)`:
       ```typescript
       const snippet = await this.plugin.snippetService.load(path);
       if (!snippet) { new Notice('Сниппет не найден.'); await this.rebuildTreeModel(); this.renderTree(); return; }
       this.currentlyEditingPath = path;
       const modal = new SnippetEditorModal(this.app, this.plugin, { mode: 'edit', initialFolder: dirname(path), snippet });
       modal.open();
       const result = await modal.result;
       this.currentlyEditingPath = null;
       if (result.saved) { await this.rebuildTreeModel(); this.renderTree(); }
       ```
    12. `openCreateModal(folderPath)`:
       ```typescript
       const modal = new SnippetEditorModal(this.app, this.plugin, { mode: 'create', initialFolder: folderPath });
       modal.open();
       const result = await modal.result;
       if (result.saved) { await this.rebuildTreeModel(); this.renderTree(); }
       ```
    13. `handleCreateSubfolder(parentPath)`:
       - Open a small inline ConfirmModal variant — OR — use a minimal prompt modal. Simplest: open a `ConfirmModal` whose `body` is a custom `HTMLElement` containing a text input + label «Имя подпапки». On confirm, read the input value, validate non-empty, call `plugin.snippetService.createFolder(parentPath + '/' + trimmedName)`, then `rebuildTreeModel` + `renderTree`. Expand the new path by adding to `settings.snippetTreeExpandedPaths` before render.
       - Title: «Создать подпапку», confirmLabel: «Создать», cancelLabel: «Отмена», destructive: false.
    14. `handleDeleteSnippet(path, name)`:
       - Open `ConfirmModal` with title «Удалить сниппет?», body «Сниппет «{name}» будет перемещён в корзину Obsidian. Его можно восстановить через системный файловый менеджер.», confirmLabel «Удалить», destructive: true.
       - On 'confirm': `await plugin.snippetService.delete(path)`, `new Notice('Сниппет перемещён в корзину.')`, rebuild + render.
       - On error: `new Notice('Не удалось удалить: ' + e.message)`.
       - DEL-03 is satisfied transitively: tree rebuilds; runner picker also re-lists on its next open (no extra wiring).
    15. `handleDeleteFolder(path, name)`:
       - First call `descendants = await plugin.snippetService.listFolderDescendants(path)`.
       - Build body as a DocumentFragment: `<p>В папке находится {total} элементов. Все они будут перемещены в корзину:</p>` + `<ul>` with up to 10 `<code>` lines of relative paths (strip the root prefix for readability) + if total > 10 append `<p class="radi-muted">…и ещё {total - 10} элементов.</p>`.
       - Open `ConfirmModal` with title `Удалить папку {name}?`, the built-body HTMLElement, confirmLabel «Удалить папку», destructive: true.
       - On 'confirm': `await plugin.snippetService.deleteFolder(path)`, `new Notice('Папка перемещена в корзину.')`, rebuild + render.
    16. Vault watcher:
       ```typescript
       private shouldHandle(filePath: string): boolean {
         const root = this.plugin.settings.snippetFolderPath;
         return filePath === root || filePath.startsWith(root + '/');  // D-18
       }
       private scheduleRedraw(): void {
         if (this.redrawTimer !== null) window.clearTimeout(this.redrawTimer);
         this.redrawTimer = window.setTimeout(async () => {
           this.redrawTimer = null;
           try { await this.rebuildTreeModel(); this.renderTree(); }
           catch (e) { new Notice('Не удалось обновить список сниппетов. Подробности в консоли.'); console.error(e); }
         }, 120);
       }
       ```
       In the three `vault.on` handlers, filter via `shouldHandle(file.path)` for create/delete, and `shouldHandle(file.path) || shouldHandle(oldPath)` for rename.
    17. Empty state: if `treeData` has zero children, render the empty-state block (heading + body + ghost `+ Новый` prompt).
    18. Keyboard focus / keyboard shortcut inside tree: not required this phase (planner discretion in CONTEXT.md) — skip unless trivial.

    **CLAUDE.md compliance notes:**
    - The rewrite is wholesale: because the legacy master-detail layout is deleted as a phase deliverable (TREE-02), the "never remove existing code" rule does not block deleting the old snippet-manager-view code. Plan 02's extraction of the chip editor is the precondition that makes this wholesale deletion safe — the load-bearing code now lives in `snippet-chip-editor.ts`.
    - However, do NOT touch `src/main.ts` — lines 8, 62, 161-165 must keep working unchanged. That is verified by keeping the class name `SnippetManagerView` and the constant `SNIPPET_MANAGER_VIEW_TYPE` exactly as-is.
    - Do NOT touch any other view file, any snippet service code, any settings code, any style file from other features. If you need something, it should already exist from Plans 01–03.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const SNIPPET_MANAGER_VIEW_TYPE" src/views/snippet-manager-view.ts` returns 1 and the value is still `'radiprotocol-snippet-manager'`
    - `grep -c "export class SnippetManagerView extends ItemView" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "SnippetEditorModal" src/views/snippet-manager-view.ts` returns ≥2 (open create + edit)
    - `grep -c "ConfirmModal" src/views/snippet-manager-view.ts` returns ≥2 (delete snippet + delete folder + create subfolder)
    - `grep -c "registerEvent" src/views/snippet-manager-view.ts` returns ≥3 (create, delete, rename)
    - `grep -c "this\.app\.vault\.on\('create'" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "this\.app\.vault\.on\('delete'" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "this\.app\.vault\.on\('rename'" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "setTimeout" src/views/snippet-manager-view.ts` returns ≥1 (debounced redraw)
    - `grep -c "120" src/views/snippet-manager-view.ts` returns ≥1 (debounce window)
    - `grep -c "startsWith(root + '/')" src/views/snippet-manager-view.ts` returns ≥1 (D-18 prefix match)
    - `grep -c "snippetTreeExpandedPaths" src/views/snippet-manager-view.ts` returns ≥2 (read + write)
    - `grep -c "listFolderDescendants" src/views/snippet-manager-view.ts` returns ≥1 (folder delete confirm body)
    - `grep -c "deleteFolder" src/views/snippet-manager-view.ts` returns ≥1
    - `grep -c "createFolder" src/views/snippet-manager-view.ts` returns ≥1
    - `grep -c "setIcon" src/views/snippet-manager-view.ts` returns ≥3 (chevron, icon, plus)
    - `grep -c "radi-snippet-tree-row" src/views/snippet-manager-view.ts` returns ≥1
    - `grep -c "Создать подпапку" src/views/snippet-manager-view.ts` returns ≥1
    - `grep -c "Удалить папку" src/views/snippet-manager-view.ts` returns ≥1
    - `grep -c "Сниппет перемещён в корзину" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "Не удалось обновить список сниппетов" src/views/snippet-manager-view.ts` returns 1
    - `grep -c "data.id" src/views/snippet-manager-view.ts` returns 0 (no legacy id-based path logic remains)
    - `grep -c "draft\.id" src/views/snippet-manager-view.ts` returns 0
    - `wc -l src/views/snippet-manager-view.ts` ≥350
    - `git diff src/main.ts` returns empty (main.ts unchanged)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Tree view renders, context menus fire, vault watcher debounces, SnippetEditorModal opens on click and "+ Новый", folder CRUD works through service methods, legacy master-detail layout is gone, `main.ts` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace legacy snippet-manager CSS with tree + modal styles</name>
  <files>src/styles/snippet-manager.css</files>
  <read_first>
    - src/styles/snippet-manager.css (entire file — identify which selectors the rewritten view actually uses vs legacy dead selectors)
    - src/views/snippet-manager-view.ts (Task 1 output — list of `radi-snippet-tree-*` class names it uses)
    - src/views/snippet-editor-modal.ts (Plan 03 — list of modal class names it uses)
    - src/views/snippet-chip-editor.ts (Plan 02 — chip editor class names still in use)
    - .planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-UI-SPEC.md (spacing scale, colors, typography sections)
    - CLAUDE.md (CSS append-only within phase; CSS files in src/styles/ only)
  </read_first>
  <action>
    `src/styles/snippet-manager.css` is a carried feature file. Three categories of rules:

    **A. Rules still used by the rewritten view or the new modal or the extracted chip editor — KEEP.** Identify these by grepping the rewritten `snippet-manager-view.ts`, new `snippet-editor-modal.ts`, and `snippet-chip-editor.ts` for class names. Anything matched must stay.

    **B. Rules referenced ONLY by legacy master-detail selectors (e.g. `.radi-snippet-manager-*` two-column layout, legacy form panel, legacy list panel) — REMOVE.** These are dead code in the same commit that deletes their only consumers (Task 1). This is the narrow exception CLAUDE.md allows: you only delete rules whose consumers were deleted in this same phase, and only after grep-verifying zero remaining references.

    **C. Tree + modal rules to APPEND.** Add a new section at the bottom of the file with a phase header comment:
    ```
    /* ========================================================================
       Phase 33: Snippet Tree View + Editor Modal
       ======================================================================== */
    ```

    Tree view rules (use Obsidian CSS variables only per UI-SPEC; no hex literals):
    - `.radi-snippet-tree-root` — container fills the view pane, uses `--background-primary`
    - `.radi-snippet-tree-header` — flex row with the global "+ Новый" button, padding `var(--size-4-2)`, border-bottom `var(--background-modifier-border)`
    - `.radi-snippet-tree-row` — display flex, align-items center, gap `var(--size-2-1)`, padding `var(--size-4-1) var(--size-4-2)`, min-height 28px, cursor pointer, color `var(--text-normal)`, font `var(--font-ui-small)`
    - `.radi-snippet-tree-row:hover` — background `var(--background-modifier-hover)`
    - `.radi-snippet-tree-row:focus-visible` — outline 2px solid `var(--interactive-accent)`, outline-offset -2px
    - `.radi-snippet-tree-row[data-editing="true"]` — border-left 2px solid `var(--interactive-accent)`, background `var(--background-modifier-active-hover)`
    - `.radi-snippet-tree-indent` — flex-shrink 0 (width set inline per depth)
    - `.radi-snippet-tree-chevron` — width 12px, height 12px, color `var(--text-muted)`
    - `.radi-snippet-tree-icon` — color `var(--text-muted)`, flex-shrink 0
    - `.radi-snippet-tree-label` — flex 1, overflow ellipsis
    - `.radi-snippet-tree-actions` — opacity 0, transition opacity 150ms ease-out
    - `.radi-snippet-tree-row:hover .radi-snippet-tree-actions` — opacity 1
    - `.radi-snippet-tree-actions button` — background transparent, border none, color `var(--text-muted)`, padding `var(--size-2-1)`, cursor pointer, min-width 24px, min-height 24px
    - `.radi-snippet-tree-empty-placeholder` — color `var(--text-muted)`, font-style italic, padding-left (inherits from row)
    - `.radi-snippet-tree-empty-state` — centered column, padding `var(--size-4-4)`, gap `var(--size-4-2)`, color `var(--text-muted)`, text-align center

    Modal rules:
    - `.radi-snippet-editor-modal` — default Obsidian modal padding (rely on base class); `padding: var(--size-4-3)`
    - `.radi-snippet-editor-modal .radi-field` — display flex, flex-direction column, gap `var(--size-4-1)`, margin-bottom `var(--size-4-2)`
    - `.radi-snippet-editor-modal label` — font-weight `var(--font-semibold)`, font-size `var(--font-ui-small)`
    - `.radi-snippet-editor-modal input[type="text"]`, `.radi-snippet-editor-modal select`, `.radi-snippet-editor-modal textarea` — font `var(--font-ui-medium)`, width 100%
    - `.radi-snippet-editor-modal .radi-collision-error` — color `var(--text-error)`, font-size `var(--font-ui-smaller)`, margin-top `var(--size-2-1)`; `display: none` by default; `.radi-collision-error.is-visible { display: block; }`
    - `.radi-snippet-editor-modal .radi-type-toggle` — segmented control styling: display flex, gap 0, border `1px solid var(--background-modifier-border)`, border-radius `var(--radius-s)`
    - `.radi-snippet-editor-modal .radi-type-toggle button` — flex 1, background transparent, color `var(--text-muted)`, padding `var(--size-4-1) var(--size-4-2)`, border none
    - `.radi-snippet-editor-modal .radi-type-toggle button.is-active` — background `var(--background-modifier-hover)`, color `var(--text-normal)`
    - `.radi-snippet-editor-modal .modal-button-container` — margin-top `var(--size-4-3)`, padding-top `var(--size-4-2)`, border-top `1px solid var(--background-modifier-border)`, gap `var(--size-4-1)`

    After all CSS edits, **run the build** per CLAUDE.md:
    ```bash
    npm run build
    ```
    Do NOT edit `styles.css` directly — it is a generated artifact. Only `src/styles/snippet-manager.css` is edited; the build regenerates `styles.css`.

    If any other CSS file in `src/styles/` contains rules you'd want to "touch up" — do NOT. Other features are strictly out of scope.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "radi-snippet-tree-row" src/styles/snippet-manager.css` returns ≥1
    - `grep -c "radi-snippet-editor-modal" src/styles/snippet-manager.css` returns ≥1
    - `grep -c "Phase 33" src/styles/snippet-manager.css` returns ≥1 (phase section header comment)
    - `grep -c "#" src/styles/snippet-manager.css` with hex-color pattern `#[0-9a-fA-F]\{3,6\}` returns 0 (no hex literals per UI-SPEC)
    - `grep -c "var(--" src/styles/snippet-manager.css` returns ≥15 (heavy use of Obsidian tokens)
    - `grep -c "150ms" src/styles/snippet-manager.css` returns ≥1 (hover fade per UI-SPEC)
    - `npm run build` exits 0 and regenerates `styles.css` at repo root
    - `git diff src/styles/` shows ONLY `snippet-manager.css` modified; no other styles file touched
  </acceptance_criteria>
  <done>CSS uses only Obsidian variables, tree + modal styles are present, legacy master-detail rules that are no longer referenced are removed, build regenerates `styles.css` cleanly, no other feature's CSS file is touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real tests for TREE-01..04, FOLDER-01..03, DEL-02/03, SYNC-01..03</name>
  <files>
    src/__tests__/snippet-tree-view.test.ts,
    src/__tests__/snippet-vault-watcher.test.ts
  </files>
  <read_first>
    - src/__tests__/snippet-tree-view.test.ts (current Wave 0 stubs from Plan 01)
    - src/__tests__/snippet-vault-watcher.test.ts (current Wave 0 stubs from Plan 01)
    - src/__tests__/RunnerView.test.ts (ItemView test harness pattern — mock leaf + mock app)
    - src/__tests__/editor-panel.test.ts (if exists — ItemView test examples)
    - src/views/snippet-manager-view.ts (Task 1 output)
    - src/__tests__/snippet-service.test.ts (makeVault factory reuse)
  </read_first>
  <behavior>
    Fill the Wave 0 stubs with real assertions. Mock out heavyweight Obsidian DOM where possible; the goal is to test the view's decision logic, not pixel layout.
  </behavior>
  <action>
    **snippet-tree-view.test.ts** — replace each `it.skip` with a real `it`:

    - **TREE-01:** Instantiate view with a mock leaf/plugin whose `snippetService.listFolder` returns a nested structure; call `onOpen`; assert DOM has one `.radi-snippet-tree-row` per file + folder at the root level, and the root row for each folder uses the `folder` icon.
    - **TREE-02:** Grep-style assertion: the view source file (imported as text via `fs.readFileSync`) does NOT contain strings `radi-snippet-manager-list` or `radi-snippet-form-panel` (legacy master-detail class names). If those strings were different in the legacy file, substitute the ones grep actually found in the pre-rewrite version. At minimum, assert `snippet-manager-view.ts` no longer contains `radi-snippet-manager-list`.
    - **TREE-03:** Spy on `SnippetEditorModal` constructor via `vi.mock('../views/snippet-editor-modal', ...)`. Simulate click on a file row; assert the modal was constructed with `mode: 'edit'` and the clicked snippet.
    - **TREE-04:** Mock `listFolder` to return a folder with zero files/subfolders; render the view with that folder expanded via `settings.snippetTreeExpandedPaths`; assert a row with class `radi-snippet-tree-empty-placeholder` (and text `(пусто)`) is rendered inside the folder body.
    - **FOLDER-01:** Mock `plugin.snippetService.createFolder` as a spy. Simulate opening context menu on a folder row and choosing `Создать подпапку`; fill the prompt input with `sub`; confirm; assert `createFolder` was called with `parentPath + '/sub'` and the tree was rebuilt.
    - **FOLDER-02:** Mock `listFolderDescendants` to return 12 items; spy on `deleteFolder`; trigger folder-delete flow; assert the ConfirmModal body contains the first 10 items and the `…и ещё 2 элементов.` tail; confirm; assert `deleteFolder` was called with the folder path.
    - **FOLDER-03:** Hover-action test — simulate hover on a folder row, find the injected `+ Новый` button, click it; assert `SnippetEditorModal` was constructed with `mode: 'create'` and `initialFolder` equal to that folder path.
    - **DEL-02:** Mock `delete` as spy; simulate context menu `Удалить` on a snippet; assert ConfirmModal opens with title `Удалить сниппет?` and body containing the snippet name; confirm; assert `delete` was called.
    - **DEL-03:** Integration-style: mock the service to return N files, then after simulated delete call rebuild, assert the deleted path is no longer in `listFolder` result and no row with that path exists. (Runner-picker re-list on open is already guaranteed by existing runner-view.ts:566 — an integration note in the test, not a separate test.)

    **snippet-vault-watcher.test.ts** — replace each `it.skip` with a real `it`:

    - **SYNC-01:** Mock `vault.on` to capture the three registered handlers. Call `onOpen`; assert three handlers were registered (create, delete, rename). Fire the create handler with a file inside the snippet root; assert rebuild happens (spy on `rebuildTreeModel` or on `listFolder`) after the 120ms debounce (use `vi.useFakeTimers` + `vi.advanceTimersByTime(120)`).
    - **SYNC-02:** Fire the create handler with a file at path `some/other/path.json` (outside root); advance timers 120ms; assert `rebuildTreeModel`/`listFolder` was NOT called.
    - **SYNC-03:** Assert that `this.registerEvent` was used (not raw `vault.on`) for all three subscriptions, by checking that the spy on `registerEvent` was called three times with `EventRef`-shaped objects. Additionally, call `onClose` and assert `window.clearTimeout` was called (if a pending redraw timer was scheduled). The `registerEvent` pattern guarantees auto-detach on view teardown — per Obsidian docs and per runner-view.ts:215-219 reference. Also assert that raw `vault.offref` is NOT called directly (framework handles it).

    Test infra: reuse whatever ItemView mock harness exists elsewhere (check RunnerView.test.ts + editor-panel.test.ts first). If none exists, write a minimal inline helper at the top of `snippet-tree-view.test.ts`:
    ```typescript
    function makeMockLeaf() { /* stub with containerEl, registerEvent spy, etc. */ }
    function makeMockPlugin(overrides?) { /* returns { app, settings, snippetService, saveSettings } */ }
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/snippet-tree-view.test.ts src/__tests__/snippet-vault-watcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it\.skip" src/__tests__/snippet-tree-view.test.ts` returns 0
    - `grep -c "it\.skip" src/__tests__/snippet-vault-watcher.test.ts` returns 0
    - `grep -c "TREE-0[1-4]" src/__tests__/snippet-tree-view.test.ts` returns ≥4
    - `grep -c "FOLDER-0[1-3]" src/__tests__/snippet-tree-view.test.ts` returns ≥3
    - `grep -c "DEL-0[23]" src/__tests__/snippet-tree-view.test.ts` returns ≥2
    - `grep -c "SYNC-0[1-3]" src/__tests__/snippet-vault-watcher.test.ts` returns ≥3
    - `grep -c "advanceTimersByTime.*120" src/__tests__/snippet-vault-watcher.test.ts` returns ≥1 (debounce asserted)
    - `grep -c "registerEvent" src/__tests__/snippet-vault-watcher.test.ts` returns ≥1
    - `npm test -- --run` (full suite) exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All skipped stubs are replaced by real passing tests that cover TREE-01..04, FOLDER-01..03, DEL-02/03, SYNC-01..03. Full test suite green, build green.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (TypeScript compile + esbuild bundle + styles.css regen)
- `npm test -- --run` — all tests pass, zero skipped for Phase 33 scope
- `git diff src/main.ts` — empty (main.ts unchanged per CLAUDE.md)
- `grep -r "radi-snippet-manager-list\|radi-snippet-form-panel" src/` — zero matches (legacy master-detail layout fully removed)
- `grep "draft\.id\|snippet\.id" src/views/snippet-manager-view.ts` — zero matches (no legacy id-based path logic)
- Legacy chip editor code from `snippet-manager-view.ts` is gone; chip editor now lives only in `snippet-chip-editor.ts`
</verification>

<success_criteria>
- Tree view renders with nested folders and files, type icons, empty-folder indicators
- Clicking a file opens edit modal; "+ Новый" opens create modal with correct pre-fill
- Context menus fire for snippet (Редактировать / Удалить) and folder (Создать сниппет / Создать подпапку / Удалить папку)
- Confirm modals show correct copy with descendant lists for folder delete
- Vault watchers debounce 120ms, filter by snippetFolderPath prefix, auto-detach on close
- Expand state persists in settings across reloads
- Legacy master-detail layout is completely gone
- `main.ts` is byte-for-byte unchanged
- All 13 requirements (TREE-01..04, FOLDER-01..03, SYNC-01..03, DEL-02/03, MODAL-04) covered by automated tests
</success_criteria>

<output>
Create `.planning/phases/33-tree-ui-modal-create-edit-folder-operations-vault-watcher/33-04-SUMMARY.md` listing: legacy code removed (approx line count), new tree view line count, CSS rules added, tests added per requirement, any interpretive decisions made (e.g., handleCreateSubfolder prompt style, hover button placement details).
</output>
</content>
</invoke>