---
phase: 61-settings-folder-autocomplete
plan: 01
type: execute
wave: 2
depends_on: [00]
files_modified:
  - src/views/folder-suggest.ts
  - src/__tests__/views/folder-suggest.test.ts
autonomous: true
requirements: [SETTINGS-01]
must_haves:
  truths:
    - "Existing vault folders are suggested while the user types."
    - "Suggestions use Obsidian's AbstractInputSuggest surface, not custom popup DOM."
    - "Choosing a suggestion writes the full vault-relative folder path into the input."
  artifacts:
    - path: "src/views/folder-suggest.ts"
      provides: "Reusable AbstractInputSuggest-based folder autocomplete"
      exports: ["FolderSuggest", "getFolderSuggestions"]
    - path: "src/__tests__/views/folder-suggest.test.ts"
      provides: "GREEN behavior coverage for FolderSuggest"
  key_links:
    - from: "src/views/folder-suggest.ts"
      to: "app.vault.getAllFolders(false)"
      via: "folder enumeration"
      pattern: "getAllFolders\(false\)"
---

<objective>
Implement the reusable FolderSuggest component required by SETTINGS-01.

Purpose: centralize folder-autocomplete behavior so all settings fields share one Obsidian-native suggester implementation.
Output: `src/views/folder-suggest.ts` plus passing component tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-settings-folder-autocomplete/61-DISCOVERY.md
@.planning/phases/61-settings-folder-autocomplete/61-00-SUMMARY.md
@src/__tests__/views/folder-suggest.test.ts

<interfaces>
Create this exact public surface:
```ts
export function getFolderSuggestions(app: App, query: string): string[];

export class FolderSuggest extends AbstractInputSuggest<string> {
  constructor(app: App, inputEl: HTMLInputElement);
  protected getSuggestions(query: string): string[];
  renderSuggestion(folderPath: string, el: HTMLElement): void;
  selectSuggestion(folderPath: string, evt: MouseEvent | KeyboardEvent): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement folder enumeration and filtering</name>
  <files>src/views/folder-suggest.ts, src/__tests__/views/folder-suggest.test.ts</files>
  <behavior>
    - `getFolderSuggestions(app, '')` returns all non-root folder paths sorted alphabetically.
    - `getFolderSuggestions(app, 'ct')` returns paths containing `ct` case-insensitively.
    - Missing `getAllFolders` in a mock-like app returns an empty list defensively, not a thrown exception.
  </behavior>
  <action>Create `src/views/folder-suggest.ts`. Import `App` and `AbstractInputSuggest` from `obsidian`. Implement `getFolderSuggestions(app, query)` using `app.vault.getAllFolders(false)`, mapping each `TFolder.path`, excluding empty/root paths, trimming the query, filtering by `folderPath.toLowerCase().includes(queryLower)`, and sorting with `localeCompare`. Do not crawl the adapter and do not add a dependency.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/folder-suggest.test.ts</automated>
  </verify>
  <done>Suggestion/filter tests pass and prove nested folder paths such as `Protocols/CT` are returned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AbstractInputSuggest rendering and selection</name>
  <files>src/views/folder-suggest.ts, src/__tests__/views/folder-suggest.test.ts</files>
  <behavior>
    - `FolderSuggest` extends `AbstractInputSuggest<string>` and delegates `getSuggestions()` to `getFolderSuggestions()`.
    - `renderSuggestion()` renders the folder path as text via `createEl`.
    - `selectSuggestion()` sets the input value and dispatches an `input` event for existing settings persistence.
  </behavior>
  <action>Implement `FolderSuggest` with constructor `(app: App, inputEl: HTMLInputElement)`. In `renderSuggestion`, call `el.createEl('div', { text: folderPath })`. In `selectSuggestion`, call `this.setValue(folderPath)`, then dispatch an `input` event on the attached input element using `new Event('input', { bubbles: true })`, and then call `super.selectSuggestion(folderPath, evt)` if required by the Obsidian API behavior. Avoid `innerHTML`, custom popup DOM, or duplicated save callbacks.</action>
  <verify>
    <automated>npx vitest run src/__tests__/views/folder-suggest.test.ts</automated>
  </verify>
  <done>All FolderSuggest tests pass; selected folder path reaches the same input-event pathway as user typing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault metadata → DOM | Folder names from the user's vault are rendered into a suggestion popover. |
| suggestion selection → settings input | Selected path is copied into a settings input and persisted by existing handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Information Disclosure | FolderSuggest suggestions | accept | User is already inside their own vault settings; only local folder paths are listed. |
| T-61-01-02 | XSS | renderSuggestion | mitigate | Render with `createEl({ text })`; no `innerHTML` or string-to-DOM APIs. |
| T-61-01-03 | Tampering | selectSuggestion | mitigate | Selection writes only the chosen `TFolder.path` and lets existing settings validation/default code persist it. |
</threat_model>

<verification>
`npx vitest run src/__tests__/views/folder-suggest.test.ts` passes.
</verification>

<success_criteria>
Reusable `FolderSuggest` exists, is Obsidian-native, and passes behavior tests without changes to settings wiring yet.
</success_criteria>

<output>
After completion, create `.planning/phases/61-settings-folder-autocomplete/61-01-SUMMARY.md`
</output>
