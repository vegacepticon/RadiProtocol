---
phase: 61-settings-folder-autocomplete
plan: 02
type: execute
wave: 3
depends_on: [01]
files_modified:
  - src/settings.ts
  - src/__tests__/settings-tab.test.ts
autonomous: true
requirements: [SETTINGS-01]
must_haves:
  truths:
    - "Protocols folder, Snippets folder, and Output folder settings inputs all have autocomplete."
    - "Selecting a suggested folder persists through the existing settings save-on-change path."
    - "Session folder remains unchanged because it is outside SETTINGS-01."
  artifacts:
    - path: "src/settings.ts"
      provides: "FolderSuggest wiring for all SETTINGS-01 fields"
    - path: "src/__tests__/settings-tab.test.ts"
      provides: "Settings wiring regression tests"
  key_links:
    - from: "src/settings.ts"
      to: "src/views/folder-suggest.ts"
      via: "import and `new FolderSuggest(this.app, text.inputEl)` in each scoped addText callback"
      pattern: "new FolderSuggest\(this\.app, text\.inputEl\)"
---

<objective>
Wire `FolderSuggest` into every settings path field in SETTINGS-01 scope.

Purpose: deliver the user-facing autocomplete behavior while preserving existing typed-value persistence and fallback defaults.
Output: settings tab attaches FolderSuggest to Protocol canvas folder, Output folder, and Snippet folder.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-settings-folder-autocomplete/61-DISCOVERY.md
@.planning/phases/61-settings-folder-autocomplete/61-01-SUMMARY.md
@src/settings.ts
@src/views/folder-suggest.ts
@src/__tests__/settings-tab.test.ts

<interfaces>
Existing settings fields in `src/settings.ts`:
```ts
protocolFolderPath: string;
outputFolderPath: string;
snippetFolderPath: string;
sessionFolderPath: string; // not in SETTINGS-01 scope
```
Use `TextComponent.inputEl` from each `addText(text => ...)` callback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attach FolderSuggest to scoped settings fields</name>
  <files>src/settings.ts, src/__tests__/settings-tab.test.ts</files>
  <behavior>
    - Protocol canvas folder text input constructs one `FolderSuggest` with its input element.
    - Output folder text input constructs one `FolderSuggest` with its input element.
    - Snippet folder text input constructs one `FolderSuggest` with its input element.
    - Session folder constructs no `FolderSuggest`.
  </behavior>
  <action>Import `FolderSuggest` from `./views/folder-suggest`. In each of the three SETTINGS-01 `addText` callbacks (Protocol canvas folder, Output folder, Snippet folder), convert the concise chained expression to a block if needed, preserve the existing `setPlaceholder`, `setValue`, and `onChange` logic byte-for-byte where possible, and add `new FolderSuggest(this.app, text.inputEl);` inside the callback. Do not attach autocomplete to `sessionFolderPath`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/settings-tab.test.ts</automated>
  </verify>
  <done>Settings attachment tests pass and prove exactly three FolderSuggest instances are created for the in-scope fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve save-on-change and defaults</name>
  <files>src/settings.ts, src/__tests__/settings-tab.test.ts</files>
  <behavior>
    - Typing Protocol canvas folder saves `value.trim()` including empty string.
    - Typing Output folder empty value persists fallback `RadiProtocol Output`.
    - Typing Snippet folder empty value persists fallback `.radiprotocol/snippets`.
    - Selecting a suggestion triggers the same onChange pathway as typing.
  </behavior>
  <action>Use the Plan 00 tests to verify existing save semantics remain intact after adding `FolderSuggest`. If the mock needs to invoke input listeners to mirror Obsidian `TextComponent.onChange`, update only the mock/test harness; do not duplicate settings persistence inside `FolderSuggest` or `settings.ts`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/settings-tab.test.ts src/__tests__/views/folder-suggest.test.ts</automated>
  </verify>
  <done>Focused settings and FolderSuggest suites pass; selected suggestions persist via the existing field-specific save handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected suggestion → persisted plugin data | Selected folder path flows into `RadiProtocolSettings` through existing save callbacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Tampering | settings persistence | mitigate | Preserve existing trim/fallback logic and test it for all three wired fields. |
| T-61-02-02 | Denial of Service | settings display | mitigate | FolderSuggest construction must be lightweight; enumeration happens in suggest query path, not by custom DOM loops in `display()`. |
</threat_model>

<verification>
`npx vitest run src/__tests__/settings-tab.test.ts src/__tests__/views/folder-suggest.test.ts` passes.
</verification>

<success_criteria>
All three SETTINGS-01 fields have reusable autocomplete and no existing save behavior regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/61-settings-folder-autocomplete/61-02-SUMMARY.md`
</output>
