---
phase: 60-inline-runner-layout-position-persistence
plan: 04
type: execute
wave: 4
depends_on: [60-01, 60-02, 60-03]
files_modified:
  - .planning/phases/60-inline-runner-layout-position-persistence/60-VALIDATION.md
  - .planning/phases/60-inline-runner-layout-position-persistence/60-UAT.md
autonomous: false
requirements:
  - INLINE-FIX-02
  - INLINE-FIX-03
must_haves:
  truths:
    - "Automated suite, typecheck, and build pass after all Phase 60 changes."
    - "Human UAT confirms compact footprint and persisted clamped drag position in real Obsidian."
  artifacts:
    - path: ".planning/phases/60-inline-runner-layout-position-persistence/60-VALIDATION.md"
      provides: "Automated verification evidence"
    - path: ".planning/phases/60-inline-runner-layout-position-persistence/60-UAT.md"
      provides: "Manual UAT checklist and result"
  key_links:
    - from: "Phase 60 implementation"
      to: "INLINE-FIX-02/03 ROADMAP criteria"
      via: "VALIDATION.md and UAT.md evidence"
      pattern: "INLINE-FIX-0[23]"
---

<objective>
Verify Phase 60 end-to-end with automated checks and live Obsidian UAT.

Purpose: Position persistence and compact layout are visual/runtime behaviors; automated tests must be complemented by real Obsidian confirmation.
Output: VALIDATION.md and UAT.md documenting pass/fail evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-inline-runner-layout-position-persistence/60-CONTEXT.md
@.planning/milestones/v1.8-phases/54-inline-protocol-display-mode/54-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 04.1: Run automated Phase 60 verification and record evidence</name>
  <files>.planning/phases/60-inline-runner-layout-position-persistence/60-VALIDATION.md</files>
  <action>Run the focused position tests, regression tests, full Vitest suite, TypeScript typecheck, and production build. Create `60-VALIDATION.md` with commands, exit status, and coverage mapping for INLINE-FIX-02 and INLINE-FIX-03. If any command fails, fix implementation before writing passed status.</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts src/__tests__/regression.test.ts && npm test && npx tsc --noEmit --skipLibCheck && npm run build</automated>
  </verify>
  <done>`60-VALIDATION.md` records all automated checks passing and maps them to both Phase 60 requirements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Compact draggable Inline Runner with persisted, clamped position.</what-built>
  <how-to-verify>
    1. Build/install the plugin in the dev Obsidian vault.
    2. Open a markdown note and run `Run protocol in inline`.
    3. Confirm the modal opens as a compact floating panel that does not cover the active typing line at default window size (INLINE-FIX-03).
    4. Drag the header to a new visible location; switch to another tab and back; confirm the modal is still at the dragged location (INLINE-FIX-02).
    5. Close Obsidian or reload the plugin, run `Run protocol in inline` again, and confirm the modal restores to the last dragged location.
    6. Simulate a smaller viewport/window; confirm the modal clamps into view and remains draggable.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the failed step.</resume-signal>
</task>

<task type="auto">
  <name>Task 04.2: Record UAT result</name>
  <files>.planning/phases/60-inline-runner-layout-position-persistence/60-UAT.md</files>
  <action>After the human checkpoint, create `60-UAT.md` with the exact UAT result. If the user reported issues, mark status `failed` or `partial` and list gaps by requirement ID; do not mark passed.</action>
  <verify>
    <automated>Test-Path .planning/phases/60-inline-runner-layout-position-persistence/60-UAT.md</automated>
  </verify>
  <done>`60-UAT.md` exists and accurately records approved or failed manual verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation→release readiness | Verification evidence determines whether Phase 60 can be considered complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Repudiation | validation evidence | mitigate | Record exact commands and UAT steps in phase artifacts. |
| T-60-04-02 | DoS | visual overlap | mitigate | Human verifies compact footprint in real Obsidian before pass. |
</threat_model>

<verification>
Overall phase gate: focused tests + full tests + typecheck + build + human UAT.
</verification>

<success_criteria>
Phase 60 passes automated validation and live Obsidian UAT for compact layout, drag persistence, reload restore, and viewport clamping.
</success_criteria>

<output>
After completion, create `.planning/phases/60-inline-runner-layout-position-persistence/60-04-SUMMARY.md`
</output>
