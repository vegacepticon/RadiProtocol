---
phase: 60-inline-runner-layout-position-persistence
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/views/inline-runner-modal.ts
  - src/__tests__/views/inline-runner-position.test.ts
autonomous: true
requirements:
  - INLINE-FIX-02
must_haves:
  truths:
    - "User can drag the Inline Runner by its header."
    - "Switching away from and back to the note preserves the dragged position."
    - "Closing/reopening or plugin reload restores the last dragged position, clamped to viewport."
  artifacts:
    - path: "src/views/inline-runner-modal.ts"
      provides: "Drag handlers, restore path, layout-change clamp"
      contains: "pointerdown"
    - path: "src/__tests__/views/inline-runner-position.test.ts"
      provides: "Passing drag/restore/clamp tests"
  key_links:
    - from: "src/views/inline-runner-modal.ts"
      to: "RadiProtocolPlugin.saveInlineRunnerPosition"
      via: "persist after drag end"
      pattern: "saveInlineRunnerPosition"
---

<objective>
Wire dragging, restore, and layout-change clamping into InlineRunnerModal.

Purpose: Users must be able to place the floating runner where it does not block their note, and that placement must survive tab switches and reloads.
Output: Pointer-based header drag, saved-position restore, and viewport-safe re-clamping on layout changes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-inline-runner-layout-position-persistence/60-CONTEXT.md
@src/views/inline-runner-modal.ts
@src/__tests__/views/inline-runner-position.test.ts

<interfaces>
Use the Plan 60-01 plugin methods:
```typescript
this.plugin.getInlineRunnerPosition(): InlineRunnerPosition | null;
await this.plugin.saveInlineRunnerPosition(pos: InlineRunnerPosition | null): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02.1: Apply saved/default position on open</name>
  <files>src/views/inline-runner-modal.ts, src/__tests__/views/inline-runner-position.test.ts</files>
  <behavior>
    - With no saved position, opening uses a compact default placement near the lower-right of the viewport, not the Phase 54 full-width bottom bar.
    - With saved position, opening applies clamped `left`/`top`, clears `right`/`bottom` anchoring, and leaves `transform` empty.
    - Active-leaf hide/show toggles `is-hidden` only; it does not recompute back to default.
  </behavior>
  <action>Replace the Phase 54 `updateModalPosition()` note-width anchoring behavior for Phase 60 with a `restoreOrDefaultPosition()` flow. On `open()`, after `buildContainer()` and render setup, read `this.plugin.getInlineRunnerPosition()`, measure container dimensions, clamp via Plan 60-01 helper, and apply `style.left/top`. If no saved position exists, compute default compact lower-right coordinates from viewport size and container dimensions. Preserve D1/D2 active note hide/close behavior. Do not modify snippet/answer/loop render arms.</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Opening the inline runner uses saved or compact default coordinates and no longer reanchors to note width on every layout check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02.2: Add header drag and persist on drag end</name>
  <files>src/views/inline-runner-modal.ts, src/__tests__/views/inline-runner-position.test.ts</files>
  <behavior>
    - Pointer drag from `.rp-inline-runner-header` moves the container.
    - Drag movement clamps to viewport during drag.
    - Pointer up saves the final clamped coordinates per D-01.
    - Close removes document-level pointer listeners to avoid leaks.
  </behavior>
  <action>Add pointer event listeners to `headerEl` in `buildContainer()` or a dedicated `enableDragging()` method. Use `registerDomEvent` only if available in this plain class context; otherwise attach `pointermove`/`pointerup` to `document` and explicitly remove them in `close()` and on pointerup. Set cursor/grab affordance via CSS in Plan 60-03, but the behavior lands here. Persist only on drag end to avoid excessive `saveData()` writes. Do not implement resize or auto-fade (D-04).</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Dragging works through pointer events, persists final position, and leaks no document listeners after close.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02.3: Re-clamp on layout changes without resetting position</name>
  <files>src/views/inline-runner-modal.ts, src/__tests__/views/inline-runner-position.test.ts</files>
  <behavior>
    - `layout-change` and viewport resize clamp the current position into the new viewport.
    - Re-clamping does not overwrite a valid saved user position unless the applied visible position changes.
    - Tab switch hide/show preserves current coordinates.
  </behavior>
  <action>Keep the existing `workspaceLayoutRef` subscription but change its callback to clamp the current position rather than re-align to the active CodeMirror editor. Add a `resize` listener on `window` if tests require monitor/resolution-change behavior; remove it in `close()`. If clamping changes coordinates, persist the clamped coordinates so future opens do not repeatedly restore off-screen values. This implements D-02.</action>
  <verify>
    <automated>npm test -- src/__tests__/views/inline-runner-position.test.ts</automated>
  </verify>
  <done>Layout changes never send the modal off-screen and never reset it to Phase 54 default anchoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer events→DOM position | User input controls inline runner coordinates. |
| workspace layout events→DOM position | Runtime viewport/layout changes trigger repositioning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | DoS | document pointer listeners | mitigate | Remove listeners on pointerup and close; test leak-sensitive path. |
| T-60-02-02 | Tampering | pointer coordinates | mitigate | Clamp every applied drag coordinate before DOM style write. |
</threat_model>

<verification>
Run `npm test -- src/__tests__/views/inline-runner-position.test.ts` and `npx tsc --noEmit --skipLibCheck`.
</verification>

<success_criteria>
User drag-position persists across hide/show and reopen, and restored coordinates are always visible/draggable.
</success_criteria>

<output>
After completion, create `.planning/phases/60-inline-runner-layout-position-persistence/60-02-SUMMARY.md`
</output>
