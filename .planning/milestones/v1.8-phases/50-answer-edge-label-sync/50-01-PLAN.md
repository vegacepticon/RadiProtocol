---
phase: 50-answer-edge-label-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/canvas-internal.d.ts
requirements: [EDGE-02]
autonomous: true

must_haves:
  truths:
    - "CanvasData.edges is typed as CanvasEdgeData[] (no more unknown[])"
    - "CanvasEdgeData declares id, fromNode, toNode, optional label, with index signature for forward-compat"
    - "Tsc --noEmit passes; no existing consumer of CanvasData.edges breaks"
  artifacts:
    - path: "src/types/canvas-internal.d.ts"
      provides: "Typed CanvasEdgeData interface mirroring CanvasNodeData shape"
      contains: "interface CanvasEdgeData"
  key_links:
    - from: "src/types/canvas-internal.d.ts"
      to: "src/canvas/canvas-live-editor.ts"
      via: "CanvasData import already present (line 12)"
      pattern: "import type .* CanvasData"
---

<objective>
Поднять тип `CanvasData.edges` с `unknown[]` (текущая строка 20) до строго типизированного `CanvasEdgeData[]`. Это D-15 — предпосылка Волны 2 (без нового типа `saveLiveEdges` из Plan 02 не скомпилируется).

Purpose: убрать `unknown[]` в канвасных edge-операциях Phase 50 и получить compile-time гарантии для `edge.id / fromNode / toNode / label`.
Output: обновлённый `src/types/canvas-internal.d.ts` с новым exported `CanvasEdgeData` + типизированным `CanvasData.edges`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md
@.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md
@.planning/notes/answer-label-edge-sync.md

<interfaces>
<!-- Existing CanvasNodeData shape (analog) from src/types/canvas-internal.d.ts:7-16 -->

```typescript
export interface CanvasNodeData {
  id: string;
  x: number;
  y: number;
  width: number;
  height: number;
  type: string;
  color?: string;
  [key: string]: unknown; // radiprotocol_* fields and any other custom properties
}

export interface CanvasData {
  nodes: CanvasNodeData[];
  edges: unknown[];   // ← change to CanvasEdgeData[]
}
```

<!-- RawCanvasEdge (shape source) from src/graph/canvas-parser.ts:36-41 — field names to mirror -->
<!-- id: string; fromNode: string; toNode: string; label?: string -->

<!-- Downstream consumer that will rely on the stronger type — src/canvas/canvas-live-editor.ts:12 -->
```typescript
import type { CanvasViewInternal, CanvasNodeData, CanvasData } from '../types/canvas-internal';
```

<!-- Local loose-edge type inside editor-panel-view.ts:215 (intentionally left as-is per PATTERNS.md §3 ripple-audit) -->
```typescript
let canvasData: { nodes: Array<Record<string, unknown>>; edges: unknown[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить CanvasEdgeData и типизировать CanvasData.edges</name>
  <files>src/types/canvas-internal.d.ts</files>

  <read_first>
    - `src/types/canvas-internal.d.ts` (полный файл, 86 строк — прочитать целиком)
    - `.planning/phases/50-answer-edge-label-sync/50-PATTERNS.md` §3 «Type-def» (строки 282-333)
    - `.planning/phases/50-answer-edge-label-sync/50-CONTEXT.md` D-15
    - `src/graph/canvas-parser.ts:36-41` — `RawCanvasEdge` shape (field-name source)
    - `CLAUDE.md` — Shared Pattern G (append-only) применим к `canvas-internal.d.ts`
  </read_first>

  <behavior>
    - Test 1 (tsc --noEmit): проект компилируется без ошибок после изменения.
    - Test 2 (grep): точно одно вхождение `interface CanvasEdgeData` в `src/types/canvas-internal.d.ts`.
    - Test 3 (grep): строка `edges: CanvasEdgeData[];` присутствует в `CanvasData`.
    - Test 4 (grep): `edges: unknown[]` отсутствует в `src/types/canvas-internal.d.ts`.
    - Test 5: `npm test` остаётся зелёным (466 passed / 1 skipped — Phase 49 baseline).
  </behavior>

  <action>
1. **Прочитать `src/types/canvas-internal.d.ts` целиком** (86 строк). Запомнить расположение `CanvasNodeData` (строки 7-16) и `CanvasData` (строки 18-21).

2. **Добавить новый exported `CanvasEdgeData` interface сразу после `CanvasNodeData`** (то есть на позиции текущей строки 17, между `CanvasNodeData` и `CanvasData`). Это append-only относительно окружающих блоков — никакие существующие строки не удаляются и не переупорядочиваются.

   Точный текст, который нужно вставить:
   ```typescript

   // Phase 50 D-15: typed edge data (was unknown[]). Field names mirror the raw
   // canvas JSON edge shape parsed by CanvasParser (src/graph/canvas-parser.ts:36-41)
   // and Obsidian's internal canvas model. Index signature keeps the interface
   // forward-compat with fromSide / toSide / color / etc. — same escape hatch
   // CanvasNodeData uses above. Design source: .planning/notes/answer-label-edge-sync.md
   export interface CanvasEdgeData {
     id: string;
     fromNode: string;
     toNode: string;
     label?: string;
     [key: string]: unknown; // forward-compat: fromSide, toSide, color, etc.
   }
   ```

3. **Изменить `CanvasData.edges` с `unknown[]` на `CanvasEdgeData[]`.** Только одна in-place правка строки 20 (после вставки выше номер строки сдвинется, но правка точечная — изменяется **только** тип поля `edges`):
   ```typescript
   export interface CanvasData {
     nodes: CanvasNodeData[];
     edges: CanvasEdgeData[];   // ← Phase 50 D-15 (was unknown[])
   }
   ```
   Комментарий `// ← Phase 50 D-15 (was unknown[])` должен присутствовать verbatim на строке с `edges`.

4. **НЕ трогать** никакие другие интерфейсы в файле: `CanvasNodeInternal` (строки 23-34), `CanvasInternal` (36-73), `CanvasViewInternal` (75-85). Они остаются character-for-character как сейчас.

5. **НЕ трогать** локальный тип в `editor-panel-view.ts:215` (`let canvasData: { nodes: ...; edges: unknown[] }`) — PATTERNS.md §3 «Ripple-audit targets» явно оставляет его as-is.

6. После правки запустить `npx tsc --noEmit --skipLibCheck` и убедиться, что exit code = 0. Любая ошибка компиляции в `canvas-live-editor.ts` (единственный внешний consumer `CanvasData`) означает регрессию типа и должна быть устранена ДО коммита.

7. Запустить `npm test` — ожидание: **466 passed / 1 skipped / 0 failed** (Phase 49 baseline сохранён; новых тестов здесь нет, только type-lift).
  </action>

  <verify>
    <automated>npx tsc --noEmit --skipLibCheck && npm test</automated>
  </verify>

  <acceptance_criteria>
    - [ ] Файл `src/types/canvas-internal.d.ts` содержит `export interface CanvasEdgeData {` — grep-verifiable точная строка.
    - [ ] Файл `src/types/canvas-internal.d.ts` содержит `fromNode: string;` и `toNode: string;` внутри `CanvasEdgeData`.
    - [ ] Файл `src/types/canvas-internal.d.ts` содержит `label?: string;` внутри `CanvasEdgeData`.
    - [ ] Файл `src/types/canvas-internal.d.ts` содержит `[key: string]: unknown;` внутри `CanvasEdgeData` (forward-compat index signature).
    - [ ] Файл `src/types/canvas-internal.d.ts` содержит строку `edges: CanvasEdgeData[];` (замена `edges: unknown[];`).
    - [ ] `grep -n "edges: unknown\[\]" src/types/canvas-internal.d.ts` — 0 результатов (старый тип полностью убран из этого файла).
    - [ ] `grep -n "answer-label-edge-sync.md" src/types/canvas-internal.d.ts` — ≥1 результат (канонический ref в комментарии — D-10/D-16).
    - [ ] `npx tsc --noEmit --skipLibCheck` exit code = 0 (проект компилируется).
    - [ ] `npm test` — 466 passed / 1 skipped / 0 failed (Phase 49 baseline).
    - [ ] **Shared Pattern G** (CLAUDE.md append-only): diff `src/types/canvas-internal.d.ts` показывает ТОЛЬКО добавление нового блока `CanvasEdgeData` + точечную правку одной строки `edges: ...` в `CanvasData`. Все остальные интерфейсы (`CanvasNodeData`, `CanvasNodeInternal`, `CanvasInternal`, `CanvasViewInternal`) побайтово неизменны. Ни одно правило/функция/event-listener из предыдущих фаз не удалено (diff показывает только additions и таргетированную in-place правку, без unrelated deletions).
    - [ ] **НЕ менять** `editor-panel-view.ts:215` local type — PATTERNS.md §3 прямо оставляет его as-is.
    - [ ] Никаких CSS-изменений (Phase 50 CSS-free — PATTERNS.md / CONTEXT.md).
  </acceptance_criteria>

  <done>
    `CanvasEdgeData` объявлен, `CanvasData.edges` типизирован, компиляция и тесты зелёные. Волна 2 (Plan 02 reconciler + Plan 03 saveLiveEdges) может теперь импортировать `CanvasEdgeData` без ошибок tsc.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --skipLibCheck` exit 0
- `npm test` — 466 passed / 1 skipped / 0 failed
- `grep -c "interface CanvasEdgeData" src/types/canvas-internal.d.ts` → 1
- `grep -c "edges: unknown\[\]" src/types/canvas-internal.d.ts` → 0
- Diff-чек: только одна секция добавлена (CanvasEdgeData) + одна строка изменена (CanvasData.edges).
</verification>

<success_criteria>
Type-lift завершён. Downstream plans (`saveLiveEdges`, reconciler diff application) могут импортировать `CanvasEdgeData` из `../types/canvas-internal` и работать с `data.edges[].id / label / fromNode / toNode` как с типизированными полями.
</success_criteria>

<output>
After completion, create `.planning/phases/50-answer-edge-label-sync/50-01-SUMMARY.md`
</output>
