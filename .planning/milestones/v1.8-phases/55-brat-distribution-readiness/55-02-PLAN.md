---
phase: 55-brat-distribution-readiness
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - .gitignore
  - styles.css
autonomous: true
requirements: [BRAT-01]
must_haves:
  truths:
    - ".gitignore contains a literal `styles.css` line in the Build output section"
    - "styles.css is no longer in the git index (`git ls-files styles.css` returns empty)"
    - "styles.css still exists on disk (it is the built artifact — next `npm run build` regenerates it)"
    - ".gitignore keeps ALL pre-existing lines (main.js, main.js.map, node_modules/, .env, .DS_Store, Thumbs.db, section comments) byte-identical per CLAUDE.md append-only rule"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore rules — now also covering styles.css"
      contains: "styles.css"
    - path: "styles.css"
      provides: "Esbuild-generated CSS bundle (on disk, not tracked)"
      min_lines: 1
  key_links:
    - from: ".gitignore `styles.css` line"
      to: "`git rm --cached styles.css` (same commit — enforced by Plan 03)"
      via: "Both actions MUST land in the same commit per Research Pitfall 3"
      pattern: "^styles\\.css$"
    - from: "styles.css on disk"
      to: "esbuild cssPlugin.onEnd (esbuild.config.mjs:58)"
      via: "npm run build regenerates the file every time"
      pattern: "fs.writeFileSync\\('styles\\.css'"
---

<objective>
Untrack `styles.css` from git (it is a generated esbuild artifact that should behave exactly like `main.js`) while preserving the file on disk.

Purpose: D5 closes the source↔dist drift that today causes `styles.css` to appear as a tracked-but-noisy modification after every build. After this plan: `npm run build` regenerates the file, `git status` stays clean.

Output: `.gitignore` has a new `styles.css` line appended to the "Build output" section; `styles.css` removed from the git index (file remains on disk). No commit yet — Plan 03 lands the single atomic release-prep commit covering Plans 01 + 02 + 03's build outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/55-brat-distribution-readiness/55-CONTEXT.md
@.planning/phases/55-brat-distribution-readiness/55-RESEARCH.md
@.planning/phases/55-brat-distribution-readiness/55-PATTERNS.md
@.gitignore

<interfaces>
<!-- Current `.gitignore` disk state — VERIFIED. Must be EXTENDED in the Build output section, NOT rewritten. -->

Current `.gitignore` (14 lines):
```
# Dependencies
node_modules/

# Build output
main.js
main.js.map

# Dev vault config (never commit vault paths)
.env

# OS files
.DS_Store
Thumbs.db
```

Section style:
- `# Section header` comment line
- Patterns listed beneath with no leading `/`, no trailing comments
- Blank line between sections

CLAUDE.md rule (critical): "Never remove existing code you didn't add." Every pre-existing line must stay byte-identical. Only the `styles.css` line is added.

esbuild reference (for understanding, NOT for editing — read-only): `esbuild.config.mjs:58` does `fs.writeFileSync('styles.css', combined)` — this is why the file regenerates on every build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 55-02-01: Append `styles.css` to `.gitignore` Build output section</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current disk state — 14 lines, 2-space section comments pattern)
    - CLAUDE.md §"Never remove existing code you didn't add" (lines 19–28) — enforces that ALL existing lines stay byte-identical
    - .planning/phases/55-brat-distribution-readiness/55-CONTEXT.md (D5 — both main.js and styles.css ignored; add styles.css, also git rm --cached)
    - .planning/phases/55-brat-distribution-readiness/55-PATTERNS.md §4 ".gitignore" (exact diff, style rules)
    - .planning/phases/55-brat-distribution-readiness/55-RESEARCH.md §Code Examples ".gitignore append" (lines 330–346) + §Pitfalls Pitfall 3 (lines 272–277 — .gitignore + rm --cached must be together)
  </read_first>
  <action>
Apply EXACTLY this diff to `.gitignore` — append a single line `styles.css` into the existing "Build output" section, immediately after `main.js.map`:

```diff
 # Build output
 main.js
 main.js.map
+styles.css
```

Final state of `.gitignore` (15 lines):
```
# Dependencies
node_modules/

# Build output
main.js
main.js.map
styles.css

# Dev vault config (never commit vault paths)
.env

# OS files
.DS_Store
Thumbs.db
```

Rules:
- Bare pattern `styles.css` (no leading `/`, no trailing comment — matches existing style for `main.js` and `main.js.map`).
- Inserted directly BELOW `main.js.map` (groups all esbuild outputs together).
- Blank line between "Build output" section and "Dev vault config" section preserved.
- ALL other lines byte-identical — no edits to `node_modules/`, `main.js`, `main.js.map`, `.env`, `.DS_Store`, `Thumbs.db`, or any section comment.
- Trailing newline at EOF preserved.
  </action>
  <verify>
    <automated>grep -Fxq 'styles.css' .gitignore && grep -Fxq 'main.js' .gitignore && grep -Fxq 'main.js.map' .gitignore && grep -Fxq 'node_modules/' .gitignore && grep -Fxq '.env' .gitignore && grep -Fxq '.DS_Store' .gitignore && grep -Fxq 'Thumbs.db' .gitignore && echo '.gitignore OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fxq 'styles.css' .gitignore` exits 0 (the new line is present)
    - `grep -Fxq 'main.js' .gitignore` exits 0 (pre-existing line preserved)
    - `grep -Fxq 'main.js.map' .gitignore` exits 0 (pre-existing line preserved)
    - `grep -Fxq 'node_modules/' .gitignore` exits 0
    - `grep -Fxq '.env' .gitignore` exits 0
    - `grep -Fxq '.DS_Store' .gitignore` exits 0
    - `grep -Fxq 'Thumbs.db' .gitignore` exits 0
    - `grep -c '^' .gitignore` returns `14` (15 content lines: 14 if we count via grep -c '^' on a file without final newline) — alternative: `wc -l < .gitignore` returns `15` (one line added to the 14-line original)
    - `git diff .gitignore` shows exactly ONE addition (`+styles.css`) — no other diffs
    - `git check-ignore styles.css` echoes `styles.css` (confirms the ignore rule matches)
  </acceptance_criteria>
  <done>.gitignore has the new `styles.css` line in the Build output section; all pre-existing lines byte-identical; git check-ignore confirms the file is now ignored.</done>
</task>

<task type="auto">
  <name>Task 55-02-02: Remove styles.css from git index (file stays on disk)</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (disk file — currently 42274 bytes per Research §Runtime State Inventory; must remain on disk after this task)
    - .planning/phases/55-brat-distribution-readiness/55-CONTEXT.md (D5 — `git rm --cached styles.css`)
    - .planning/phases/55-brat-distribution-readiness/55-PATTERNS.md §5 "styles.css" (exact command, ordering constraint)
    - .planning/phases/55-brat-distribution-readiness/55-RESEARCH.md §Pitfalls Pitfall 3 (same-commit requirement) + Pitfall 7 (file stays on disk — do NOT run `git clean`)
  </read_first>
  <action>
Run EXACTLY this command:

```bash
git rm --cached styles.css
```

Expected stdout (Git output):
```
rm 'styles.css'
```

This:
- Removes `styles.css` from the git index (next commit will record a deletion-from-tracking).
- LEAVES the file on disk at its current content (42274 bytes esbuild output from the last build).
- Combined with Task 55-02-01's `.gitignore` addition, subsequent `npm run build` regenerates the file and `git status` stays clean.

Do NOT run `git commit` — Plan 03 owns the atomic release-prep commit.
Do NOT run `git clean` — Research Pitfall 7 warns this would delete the on-disk file, forcing a rebuild before release.
Do NOT run `rm styles.css` — same reason; only `git rm --cached` is permitted.

CLAUDE.md note: `styles.css` is a generated file. After this plan, the rule "CSS output: `styles.css` in root — generated by esbuild, do not edit directly" still stands; the file just stops being tracked.
  </action>
  <verify>
    <automated>test -s styles.css && ! git ls-files --error-unmatch styles.css 2>/dev/null && git check-ignore styles.css >/dev/null && git diff --cached --name-status | grep -q '^D.*styles\.css$' && echo 'styles.css untrack OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test -s styles.css` exits 0 (file exists on disk, non-zero size)
    - `git ls-files --error-unmatch styles.css` exits non-zero (file is no longer in the index — the core goal)
    - `git check-ignore styles.css` exits 0 (ignore rule from Task 55-02-01 matches)
    - `git diff --cached --name-status` shows `D\tstyles.css` (staged deletion of index entry)
    - `git status --porcelain styles.css` shows `D  styles.css` (staged deletion, no working-tree modification)
    - `ls -la styles.css` shows the file on disk with its pre-task size (unchanged content)
  </acceptance_criteria>
  <done>styles.css is removed from the git index with a staged deletion; file exists on disk unchanged; ignore rule active; no commit yet (Plan 03 commits).</done>
</task>

</tasks>

<verification>
After both tasks, these assertions must ALL pass:

```bash
# .gitignore rule
grep -Fxq 'styles.css' .gitignore
# Index state
! git ls-files --error-unmatch styles.css 2>/dev/null
# Ignore matches
git check-ignore styles.css >/dev/null
# Disk state preserved
test -s styles.css
# Staged deletion ready for commit (Plan 03 will commit it)
git diff --cached --name-status | grep -q '^D.*styles\.css$'
# Pre-existing .gitignore lines preserved
grep -Fxq 'main.js' .gitignore
grep -Fxq 'main.js.map' .gitignore
```

All seven must exit 0.

Ordering invariant (CRITICAL per Research Pitfall 3): the `.gitignore` addition AND the `git rm --cached` MUST be present simultaneously in the working tree before Plan 03's commit. If either is missing when Plan 03 stages, Plan 03 must abort and return to this plan.
</verification>

<success_criteria>
- `styles.css` is no longer tracked by git (matches `main.js`'s pre-existing correct state)
- `styles.css` still exists on disk, ready for Plan 03's rebuild
- `.gitignore` contains the new `styles.css` rule alongside the 12 pre-existing lines (byte-identical)
- Staged deletion of `styles.css` from index is ready for Plan 03's atomic commit
- No commit performed in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/55-brat-distribution-readiness/55-02-SUMMARY.md` describing the `.gitignore` append and the `git rm --cached` operation, confirming the file remains on disk, and noting that the staged deletion will land in Plan 03's atomic commit.
</output>
