---
phase: 50.1-loop-exit-plus-prefix
plan: 02
subsystem: graph/validator
tags:
  - loop-exit
  - validator
  - tdd
dependency_graph:
  requires:
    - "50.1-01 (isExitEdge + stripExitPrefix exports)"
    - "50.1-04 (+-prefix fixture migration + 3 new fixtures)"
  provides:
    - "LOOP-04 D-04..D-08 verbatim Russian error copy"
    - "Phase 49↔50 conflict regression (labeled body edge with +-exit coexist)"
  affects:
    - "Plan 50.1-03 (Runner dispatch + RunnerView caption strip will run against the new validator)"
tech_stack:
  added: []
  patterns:
    - "TDD RED → GREEN gate sequence (test commit before feat commit)"
    - "Verbatim Russian error-copy pattern: CONTEXT.md is the source of truth; strings pasted character-for-character into validator + tests"
    - "Per-offending-edge error accumulation (D-08) follows Phase 49 pattern — multiple errors per loop node collect in RunnerView Error panel"
key_files:
  created: []
  modified:
    - src/graph/graph-validator.ts
    - src/__tests__/graph-validator.test.ts
decisions:
  - "Kept isLabeledEdge import in graph-validator.ts — D-05 branch needs it to detect 'labeled but non-+ prefix' legacy edges; also acts as a trip-wire against accidental removal."
  - "Error-check ordering implemented as locked in CONTEXT.md: D-04/D-05 → D-06 → D-08 → D-07. All five checks accumulate independently so a degenerate canvas (e.g. two +-edges both empty) surfaces D-06 + two D-08 entries simultaneously."
  - "D-08 iterates exitEdges after D-06, so an offending fixture with ≥2 +-edges whose captions are all empty would emit D-06 AND one D-08 per edge — matches CONTEXT.md per-edge accumulation rule."
  - "MIGRATE-01 block (lines 40-53) left byte-identical; its literal «выход» is the sole remaining Phase 49-era exit-label occurrence in runtime code (to be audited by Plan 05)."
metrics:
  duration_minutes: ~5
  tasks_completed: 2
  files_touched: 2
  commits: 2
  completed: "2026-04-19"
requirements:
  - EDGE-03
---

# Phase 50.1 Plan 02: LOOP-04 D-04..D-08 Rewrite Summary

One-liner: Rewrote GraphValidator LOOP-04 from Phase 49's three-error convention (D-01/D-02/D-03, «помеченное ребро» semantics) to the Phase 50.1 `+`-prefix five-error convention (D-04..D-08) with locked Russian error copy, per-offending-edge D-08 accumulation, and a Phase 49↔50 conflict regression ensuring labeled body edges coexist with a `+`-exit.

## What Shipped

### `src/graph/graph-validator.ts` (modified)

- **Import extended (line 5):** `{ isLabeledEdge, isExitEdge, stripExitPrefix }` now all imported from `./node-label`. `isLabeledEdge` retained deliberately — the D-05 branch needs it to detect "labeled but no `+` prefix" legacy edges.
- **LOOP-04 block rewritten (lines 92-149):** 34-line block grew to 58 lines to accommodate five distinct error branches with locked Russian copy. Ordering: **D-04/D-05 → D-06 → D-08 → D-07**.
- **Phase 49 D-01/D-02/D-03 strings removed** from the LOOP-04 runtime block. Verified via grep: zero occurrences of `«Пометьте ровно одно исходящее ребро — его метка станет подписью кнопки выхода.»`, `«несколько помеченных исходящих рёбер»`, `«не имеет тела — добавьте исходящее ребро без метки»`.
- **MIGRATE-01 block preserved byte-identical** (lines 37-53) — its literal «выход» (at line 49) remains as the sole runtime occurrence of the Phase 49-era exit label; Plan 05 audits this.
- **All other validator checks** (start-node, reachability, cycles, dead-end questions, cycle-through-loop detection, helper methods) untouched byte-identical per CLAUDE.md shared-file rule.

### `src/__tests__/graph-validator.test.ts` (modified)

- **6 LOOP-04 tests rewritten** to D-04..D-08 wording + 2 new tests added (D-05 legacy-hint, D-08 empty caption), +1 Phase 49↔50 conflict regression (labeled body edge coexists with `+`-exit).
- **Stray-body-label test role flipped** from D-02 violation to VALIDATES (non-`+` body labels are legal under Phase 50.1).
- **D-CL-02 order test** kept its migration-error assertion but now also guards that no `"+"-рёбер` wording leaks into a legacy-migration canvas.
- **MIGRATE-01 tests** (lines 307-336) untouched byte-identical.
- **Cycle-through-loop + cycle-without-loop tests** (lines 358-378) untouched byte-identical.
- **All outer describes** (valid protocols, error detection PARSE-07/08, mixed snippet branches, snippet-node) untouched byte-identical.

## Verbatim D-04..D-08 Russian Copy (pasted character-for-character from CONTEXT.md)

| Code | String |
|------|--------|
| D-04 | `Loop-узел "{label}" не имеет выхода. Пометьте ровно одно исходящее ребро префиксом "+" — текст после "+" станет подписью кнопки выхода.` |
| D-05 | `Loop-узел "{label}" не имеет выхода с префиксом "+". Добавьте "+" к одному из помеченных рёбер ({edgeIds}) — текст после "+" станет подписью кнопки выхода.` |
| D-06 | `Loop-узел "{label}" имеет несколько "+"-рёбер: {edgeIds}. Должно быть ровно одно выходное ребро — уберите "+" с остальных.` |
| D-07 | `Loop-узел "{label}" не имеет тела — добавьте исходящее ребро без префикса "+".` |
| D-08 | `Loop-узел "{label}": "+"-ребро {edgeId} не имеет подписи — добавьте текст после "+".` |

## Commits

| # | SHA       | Gate  | Message                                                                                               |
| - | --------- | ----- | ----------------------------------------------------------------------------------------------------- |
| 1 | `d23af70` | RED   | `test(50.1-02): rewrite LOOP-04 assertions to D-04..D-08 wording + Phase 49↔50 conflict regression (RED)` |
| 2 | `2e29540` | GREEN | `feat(50.1-02): rewrite LOOP-04 with D-04..D-08 "+"-prefix error copy (GREEN)`                        |

RED commit `d23af70` was proven failing: 6 failures (D-04 wording mismatch on missing-exit, D-05 fixture not yet detected, D-06 wording mismatch on duplicate-exit, D-07 wording mismatch on no-body, D-08 fixture not yet detected, labeled-body-canvas body-edge misclassified as second exit under Phase 49 logic).

GREEN commit `2e29540` flipped all 24 tests green on first run — no iteration needed.

## Acceptance Criteria — Verified

- `grep "import.*stripExitPrefix.*from './node-label'" src/graph/graph-validator.ts` = 1 ✓
- `grep "import.*isExitEdge.*from './node-label'" src/graph/graph-validator.ts` = 1 ✓ (same import line as stripExitPrefix)
- `grep "isLabeledEdge" src/graph/graph-validator.ts` = 3 ≥ 2 ✓ (import + D-05 branch + comment)
- D-04 verbatim: grep finds the full locked string ✓
- D-05 verbatim: `не имеет выхода с префиксом "+". Добавьте "+" к одному из помеченных рёбер` found ✓
- D-06 verbatim: `имеет несколько "+"-рёбер: ${dupIds}. Должно быть ровно одно выходное ребро — уберите "+" с остальных.` found ✓
- D-07 verbatim: `не имеет тела — добавьте исходящее ребро без префикса "+".` found ✓
- D-08 verbatim: `"+"-ребро ${edge.id} не имеет подписи — добавьте текст после "+".` found ✓
- Phase 49 D-01 string removed: 0 occurrences of `Пометьте ровно одно исходящее ребро — его метка станет подписью` ✓
- Phase 49 D-02 fragment removed: 0 occurrences of `несколько помеченных исходящих рёбер` ✓
- Phase 49 D-03 fragment removed: 0 occurrences of `не имеет тела — добавьте исходящее ребро без метки` ✓
- MIGRATE-01 preserved: 1 × `устаревшие узлы loop-start/loop-end`, 1 × `метка «выход» на одном из исходящих рёбер` ✓
- Error-check ordering: D-04 branch at line 115 < D-06 branch at line 130 < D-08 branch at line 138 < D-07 branch at line 146 — matches locked ordering ✓
- `npx vitest run src/__tests__/graph-validator.test.ts` → 24/24 pass (GREEN) ✓
- `npx vitest run src/__tests__/edge-label-reconciler.test.ts src/__tests__/graph/node-label.test.ts` → 51/51 pass (untouched surface preserved) ✓
- RED commit matches `^test\(50\.1-02\):.*RED` ✓
- GREEN commit matches `^feat\(50\.1-02\):.*GREEN` ✓

## Downstream Ready

- Plan 50.1-03 (Runner dispatch + RunnerView caption strip) can rely on validator rejecting malformed canvases before Runner start. `stripExitPrefix` is already exported and covered by Plan 01 tests; Runner-side wiring is all that remains.
- Plan 50.1-05 audit can now grep the tree for remaining `"выход"` literals — only the MIGRATE-01 message (graph-validator.ts:49) should remain.

## Deviations from Plan

None — plan executed exactly as written. Zero auto-fixes, zero Rule 4 escalations. Both RED and GREEN landed on first check.

## Known Stubs

None. D-04..D-08 error strings are wired with verbatim Russian copy; D-08 iteration over `+`-edges is implemented fully; all fixtures from Plan 04 exercise the new paths.

## TDD Gate Compliance

- RED gate present: commit `d23af70` — `test(50.1-02): ... (RED)` proven failing (6/24 failed before impl).
- GREEN gate present: commit `2e29540` — `feat(50.1-02): ... (GREEN)` — all 24 tests pass.
- REFACTOR gate: not required (rewrite landed clean; zero cleanup needed).

## Self-Check: PASSED

- FOUND: src/graph/graph-validator.ts (modified — LOOP-04 block rewritten, import extended)
- FOUND: src/__tests__/graph-validator.test.ts (modified — LOOP-04 describe block rewritten + 2 new tests + 1 regression)
- FOUND: commit d23af70 (RED)
- FOUND: commit 2e29540 (GREEN)
- VERIFIED: `npx vitest run src/__tests__/graph-validator.test.ts` → 24/24 pass
- VERIFIED: `npx vitest run src/__tests__/edge-label-reconciler.test.ts src/__tests__/graph/node-label.test.ts` → 51/51 pass (adjacent suites untouched)
