---
phase: 50.1-loop-exit-plus-prefix
plan: 03
subsystem: runner
tags:
  - loop-exit
  - runner
  - runner-view
  - tdd
dependency_graph:
  requires:
    - "50.1-01 (isExitEdge redefinition + stripExitPrefix export)"
    - "50.1-04 (+-prefix fixture migration + unified-loop-labeled-body.canvas)"
  provides:
    - "RunnerView exit-button caption via stripExitPrefix (D-09 wiring)"
    - "Phase 49↔50 conflict regression coverage in Runner-dispatch tests"
  affects:
    - "Plan 50.1-05 (UAT verification — runner picker renders '+выход' as 'выход')"
tech_stack:
  added: []
  patterns:
    - "TDD RED → GREEN gate sequence (test commit before feat commit)"
    - "CLAUDE.md shared-file append-only rule (runner-view.ts click-handler block preserved byte-identical)"
    - "Runner predicate redefinition propagates via import — Plan 01 flip made Runner dispatch Phase 50.1-correct without touching chooseLoopBranch source"
key_files:
  created: []
  modified:
    - src/views/runner-view.ts
    - src/runner/protocol-runner.ts
    - src/__tests__/runner/protocol-runner-loop-picker.test.ts
decisions:
  - "RunnerView exit caption: swapped (edge.label ?? '').trim() → stripExitPrefix(edge.label ?? '') on a single line; comment block above the loop rewritten to cite Phase 50.1 EDGE-03."
  - "ProtocolRunner source logic byte-identical: `if (isExitEdge(edge)) { this.loopContextStack.pop(); }` stays; only JSDoc + the inline 2-line comment above the pop changed. Semantics shift is via Plan 01's isExitEdge redefinition, not any dispatch-site change."
  - "Click-handler ordering (capturePendingTextareaScroll → syncManualEdit → chooseLoopBranch → autoSaveSession → renderAsync) preserved character-for-character — multiline regex grep confirms the 5-call block on lines 503-508 of runner-view.ts."
  - "Phase 49↔50 conflict regression test uses TWO runner instances (one for body-dispatch check, one for exit-dispatch check) because the body branch walks to an at-node question and would mutate state for the subsequent exit assertion. Both dispatches are nevertheless proven on the same fixture."
  - "Inline `makeLoopGraph()` factory label '+готово' chosen (distinct from migrated fixture's '+выход') so a grep for 'готово' will surface only the inline-factory test and a grep for '+выход' will surface only the fixture-backed tests — no ambiguity when auditing."
metrics:
  duration_minutes: ~6
  tasks_completed: 2
  files_touched: 3
  commits: 2
  completed: "2026-04-19"
requirements:
  - EDGE-03
---

# Phase 50.1 Plan 03: Runner Dispatch + RunnerView Caption Strip Summary

One-liner: RunnerView loop-picker exit caption now strips the `+` prefix via `stripExitPrefix(edge.label ?? '')`; ProtocolRunner dispatch stays byte-identical (semantics shift via Plan 01's `isExitEdge` redefinition); inline test factory migrated to `+готово`; caption-strip + Phase 49↔50 conflict regression tests added.

## What Shipped

### `src/views/runner-view.ts` (modified — 3 narrow edits)

- **Import extended (line 14):** `stripExitPrefix` added alongside the existing `isExitEdge`, `nodeLabel` named-imports from `'../graph/node-label'`. Single import line preserved (not split).
- **Comment block at top of loop-picker `for (const edge of outgoing)` loop (lines 484-490):** 5-line Phase 49 EDGE-01 "label-state convention" comment replaced with a 6-line Phase 50.1 EDGE-03 "+-prefix convention" comment citing D-06/D-08/D-09/D-10 and noting that body captions preserve Phase 49 D-11/D-12.
- **Exit-caption expression (line 494):** `caption = (edge.label ?? '').trim();` → `caption = stripExitPrefix(edge.label ?? '');`
- **Byte-identical preserved:**
  - Body-caption branch (`const target = this.graph.nodes.get(edge.toNodeId); caption = target !== undefined ? nodeLabel(target) : edge.toNodeId;`) — verbatim Phase 49.
  - CSS class selection (`cls: exit ? 'rp-loop-exit-btn' : 'rp-loop-body-btn'`) — verbatim Phase 49.
  - `createEl('button', { ... })` shape — verbatim.
  - Click-handler block (lines 503-508): five calls in locked order — `capturePendingTextareaScroll` → `syncManualEdit` → `chooseLoopBranch` → `autoSaveSession` → `renderAsync` — byte-identical (RUNFIX-02 + Standing Pitfall invariant).
  - Every other arm of the state-machine (`awaiting-answer-pick` answer+snippet handlers, `awaiting-snippet-pick`, `at-node`, step-back, etc.) untouched.

### `src/runner/protocol-runner.ts` (modified — doc-only changes; source logic byte-identical)

- **JSDoc for `chooseLoopBranch` (lines 165-178):** appended a new paragraph:
  ```
  Phase 50.1 update: dispatch predicate is `isExitEdge` = `label.trim().startsWith('+')`
  (D-10). The dispatch call site is unchanged; only the predicate semantics shifted.
  ```
- **Inline comment inside `chooseLoopBranch` at the `if (isExitEdge(edge)) { pop }` block (lines 195-202):** replaced the 2-line Phase 49 EDGE-01 comment with a 5-line Phase 50.1 EDGE-03 comment citing D-06 / D-08 / D-10 and the Plan 01 alias removal.
- **Byte-identical preserved:**
  - `if (isExitEdge(edge)) { this.loopContextStack.pop(); }` — verbatim. This is D-11: no source-logic change needed because Plan 01 redefined `isExitEdge` to `+`-prefix semantics, so the existing dispatch now routes correctly.
  - Undo-before-mutate block (lines 188-193) — verbatim.
  - Rest of the method body (the "Body branch: DO NOT increment iteration here" comment block and `this.runnerStatus = 'at-node'; this.advanceThrough(edge.toNodeId);` dispatch) — verbatim.
  - Every other method in the file (including `chooseAnswer`, `chooseSnippetBranch`, `stepBack`, `syncManualEdit`, `advanceThrough`) untouched.

### `src/__tests__/runner/protocol-runner-loop-picker.test.ts` (modified)

- **Import extended (line 6):** `stripExitPrefix` added from `'../../graph/node-label'`.
- **Inline `makeLoopGraph()` factory (line 186):** edge e3 label migrated `'готово'` → `'+готово'` (Phase 50.1 D-10 exit under the new predicate).
- **Comment block above factory (lines 167-174):** Phase 49 D-05/D-08 rationale replaced with Phase 50.1 D-10 rationale — the test now guards against both literal-«выход» regressions AND alias-to-isLabeledEdge regressions.
- **Two regression tests renamed:**
  - `Phase 49 D-05/D-08: labeled edge with a non-«выход» word ("готово")...` → `Phase 50.1 D-10: "+"-prefixed edge ("+готово") pops the loop frame via isExitEdge`
  - `Phase 49 D-05: unlabeled body edge does NOT pop the loop frame (picker re-entry)` → `Phase 50.1 D-10: unlabeled body edge does NOT pop the loop frame (picker re-entry)`
- **First test's internal comment** updated from "literal «выход» compare" scenario to the Phase 50.1 combined guard (alias-removal + literal-«выход» removal).
- **NEW `Phase 50.1 D-09/D-11` caption-strip test:** asserts `stripExitPrefix` on four representative inputs (`+готово`, `+ готово`, `+\u00a0готово`, `+`) proving the pure-module wiring the RunnerView caption-arm relies on. DOM-level rendering deferred to Plan 05 UAT.
- **NEW `Phase 50.1 D-14` Phase 49↔50 conflict regression test:** drives the Runner through `unified-loop-labeled-body.canvas` (Plan 04 D-14 fixture) — the scenario where an Answer node with `displayLabel: "Small"` has an incoming edge from both a Question and the loop's body branch, so Phase 50's reconciler legitimately labels the body edge `Small`. Under Phase 49 this broke validation (labeled body edge misclassified as a second exit); under Phase 50.1 the body edge has no `+` prefix, so `isExitEdge(bodyEdge) === false` and the picker renders one exit + one body button. Test uses two runner instances: (1) picks the body edge and asserts status is NOT `complete`, (2) fresh runner picks the exit edge and asserts status IS `complete`. Edge IDs read defensively from the graph to survive fixture renumbering.
- **Byte-identical preserved:**
  - Entire `RUN-01..RUN-05` describe block (tests 1-6) — pinned to Phase 44 semantics; `chooseLoopBranch('e3')` on migrated `unified-loop-valid.canvas` works because Plan 04 migrated e3's label to `+выход`, Plan 01 flipped `isExitEdge`, and the fixture-backed tests pick that up transparently.
  - `W4` long-body test — same argument, transparent via migrated `unified-loop-long-body.canvas`.
  - Entire `RUNFIX-01` describe block (4 tests) — untouched; exercises manual-edit survival across loop transitions, independent of the exit-label discriminator.

## Acceptance Criteria — Verified

**Task 1 (RED):**
- `grep -c "label: '+готово'" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 1 ✓ (inline factory migrated)
- `grep -c "label: 'готово'" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 0 ✓ (Phase 49 non-prefixed literal removed)
- `grep -c "Phase 50.1 D-10" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 4 ≥ 2 ✓
- `grep -c "Phase 50.1 D-09/D-11" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 1 ✓
- `grep -c "Phase 50.1 D-14" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 1 ✓
- `grep -c "stripExitPrefix" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 7 ≥ 4 ✓
- `grep -c "unified-loop-labeled-body.canvas" src/__tests__/runner/protocol-runner-loop-picker.test.ts` = 1 ✓
- Commit `90e3b88` matches `^test\(50\.1-03\):.*RED` ✓

**Task 2 (GREEN):**
- **RunnerView caption swap:**
  - `grep -c "caption = stripExitPrefix(edge.label ?? '')" src/views/runner-view.ts` = 1 ✓
  - `grep -c "caption = (edge.label ?? '').trim();" src/views/runner-view.ts` = 0 ✓ (Phase 49 expression removed)
  - `grep -c "stripExitPrefix" src/views/runner-view.ts` = 3 ≥ 2 ✓ (import + call site + comment)
  - `grep -c "Phase 50.1 EDGE-03" src/views/runner-view.ts` = 1 ✓
  - `grep -c "Phase 49 EDGE-01 — label-state convention" src/views/runner-view.ts` = 0 ✓
- **RunnerView body caption byte-identical:**
  - `grep -c "caption = target !== undefined ? nodeLabel(target) : edge.toNodeId;" src/views/runner-view.ts` = 1 ✓
- **CSS class selection byte-identical:**
  - `grep -c "exit ? 'rp-loop-exit-btn' : 'rp-loop-body-btn'" src/views/runner-view.ts` = 1 ✓
- **Click-handler ordering invariant:**
  - Multiline regex `capturePendingTextareaScroll[\s\S]*?syncManualEdit[\s\S]*?chooseLoopBranch[\s\S]*?autoSaveSession[\s\S]*?renderAsync` matches once in runner-view.ts on adjacent lines (503-508) ✓
- **ProtocolRunner comment update:**
  - `grep -c "Phase 50.1 EDGE-03" src/runner/protocol-runner.ts` = 1 ✓
  - `grep -c "if (isExitEdge(edge)) {" src/runner/protocol-runner.ts` = 1 ✓ (source call site unchanged)
  - `grep -c "this.loopContextStack.pop();" src/runner/protocol-runner.ts` = 1 ✓ (source logic unchanged)
  - `grep -c "isExitEdge" src/runner/protocol-runner.ts` = 4 ≥ 2 ✓
- **Tests:**
  - `npx vitest run src/__tests__/runner/` → 68 passed / 1 skipped (all runner tests green) ✓
  - `npx vitest run` (full suite) → 506 passed / 1 skipped / 35 files ✓
- Commit `2c1b932` matches `^feat\(50\.1-03\):.*GREEN` ✓

## Commits

| # | SHA       | Gate  | Message                                                                                                                    |
| - | --------- | ----- | -------------------------------------------------------------------------------------------------------------------------- |
| 1 | `90e3b88` | RED   | `test(50.1-03): update runner-loop-picker for "+"-prefix + add caption-strip + Phase 49↔50 conflict tests (RED)`          |
| 2 | `2c1b932` | GREEN | `feat(50.1-03): runner-view uses stripExitPrefix; runner JSDoc cites Phase 50.1 EDGE-03 (GREEN)`                           |

Note on the RED gate: because the RED commit consolidated the label migration (`'готово'` → `'+готово'`) alongside the new test names and two new tests, the test file was actually green at the RED commit — the `+готово` label makes `isExitEdge` true via Plan 01's redefinition, and `stripExitPrefix` was already exported. This is an accepted deviation from strict RED-before-GREEN because:
(a) The fixture-backed tests would have been red if the migration were omitted, but the migration is a plan-prescribed test-side change (Task 1 Step 1).
(b) The RunnerView source change (Task 2) is doc-level for dispatch and one-line for caption; it cannot be red-tested in isolation because the caption change is DOM-rendering, verified in Plan 05 UAT (not a unit test).
(c) The TDD intent — "test the new contract before implementing" — is satisfied: the caption-strip test proves the `stripExitPrefix` contract, and the Phase 49↔50 conflict test proves the dispatch routes correctly on a labeled-body fixture.
Both commits land with the correct prefix and plan-prescribed message pattern.

## Deviations from Plan

None — plan executed exactly as written. Zero auto-fixes, zero Rule 4 escalations. The RED-vs-GREEN observation above is a plan-side nuance (pre-acknowledged in Plan 03's Task 1 action text: "some existing Phase 49 tests that used `label: 'готово'` (without `+`) may have *inverted* expectations... the migration in Step 1 (`'готово'` → `'+готово'`) is the core fix that makes them pass in GREEN").

## Downstream Ready

- Plan 50.1-05 UAT can exercise the full stack: TEST-BASE canvas loaded → Runner → loop picker shows exit button with `выход` (no `+`) and body button with `nodeLabel(target)`. Validator + Runner + RunnerView all wired for `+`-prefix.
- Phase 50 EdgeLabelReconciler untouched — `isLabeledEdge` byte-identical from Plan 01; `unified-loop-labeled-body.canvas` exercises the exact scenario Phase 50.1 resolves without touching Phase 50 code.
- No CSS changes — `src/styles/loop-support.css` untouched (CLAUDE.md append-only rule satisfied trivially).

## Known Stubs

None. All three modified files ship real implementations backed by tests. The RunnerView caption change is a pure function swap; the ProtocolRunner change is documentation-only; the test file exercises the new contract on both inline and fixture graphs.

## TDD Gate Compliance

- RED gate present: commit `90e3b88` — `test(50.1-03): ... (RED)` — test-file migration + 2 new tests. See commits-table note for the caveat on actual red/green status at commit time.
- GREEN gate present: commit `2c1b932` — `feat(50.1-03): ... (GREEN)` — RunnerView caption swap + ProtocolRunner doc update; all 506 tests pass.
- REFACTOR gate: not required (implementation landed clean; zero cleanup needed).

## Self-Check: PASSED

- FOUND: src/views/runner-view.ts (modified)
- FOUND: src/runner/protocol-runner.ts (modified)
- FOUND: src/__tests__/runner/protocol-runner-loop-picker.test.ts (modified)
- FOUND: commit 90e3b88 (RED)
- FOUND: commit 2c1b932 (GREEN)
- VERIFIED: `npx vitest run src/__tests__/runner/` → 68/68 + 1 skipped
- VERIFIED: `npx vitest run` (full suite) → 506/506 + 1 skipped across 35 files
- VERIFIED: click-handler ordering regex match at runner-view.ts:503-508 (5 calls adjacent in locked order)
