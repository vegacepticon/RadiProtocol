---
phase: 36-dead-code-audit-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/runner-view.css
  - src/__tests__/runner-extensions.test.ts
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "No unused TypeScript exports remain (excluding Obsidian callback registrations)"
    - "All dead CSS rules (.rp-legend* and any other orphaned selectors) are removed"
    - "The 3 RED test stubs in runner-extensions.test.ts are removed"
    - "Full test suite passes with zero regressions after all removals"
  artifacts:
    - path: "src/styles/runner-view.css"
      provides: "Runner CSS without dead .rp-legend rules"
      contains: ".rp-question-text"
    - path: "src/__tests__/runner-extensions.test.ts"
      provides: "Clean test file with only passing Phase 35 tests"
  key_links:
    - from: "src/styles/runner-view.css"
      to: "styles.css"
      via: "esbuild concatenation"
      pattern: "npm run build"
---

<objective>
Remove all dead code from the RadiProtocol codebase: unused TypeScript exports, orphaned CSS rules, and stale RED test stubs from Phase 26.

Purpose: Clean up tech debt accumulated across 5 milestones so subsequent v1.6 phases build on a clean codebase.
Output: Cleaned source files, passing test suite, clean build output.
</objective>

<execution_context>
@Z:\Users\user\.claude\get-shit-done\workflows\execute-plan.md
@Z:\Users\user\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dead code analysis and remove unused TypeScript exports</name>
  <read_first>
    - package.json (to see current devDependencies and scripts)
    - tsconfig.json (to understand TS config for Knip)
    - src/main.ts (entry point — Obsidian callback registrations live here)
  </read_first>
  <files>package.json, any src/**/*.ts files with unused exports</files>
  <action>
1. Install Knip as a devDependency: `npm install -D knip`
2. Run `npx knip --reporter compact` to identify unused exports, files, and dependencies
3. Review each reported item against Obsidian plugin patterns:
   - KEEP: anything registered via `this.registerEvent()`, `this.addCommand()`, `this.registerView()`, `this.addSettingTab()`, or exported as the default plugin class
   - KEEP: type declarations in `.d.ts` files (canvas-internal.d.ts)
   - KEEP: test utilities and fixtures
   - REMOVE: genuinely unused exports, functions, and imports
4. For each removal, verify the export is not used transitively (grep for the function/class name across the codebase)
5. After each batch of removals, run `npm test -- --run` to verify zero regressions
6. Run `npm run build` to verify clean compilation

IMPORTANT: Do NOT remove any function or export that is referenced by Obsidian's plugin lifecycle (onload, onunload, loadData, saveData). These appear "unused" to static analysis but are called by the framework at runtime.
  </action>
  <verify>
    <automated>cd Z:/projects/RadiProtocolObsidian && npx knip --reporter compact 2>&1 | head -50 && npm test -- --run 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx knip` reports no unused exports that are not Obsidian callback registrations or type declarations
    - `npm test -- --run` exits 0 with all tests passing
    - `npm run build` exits 0 with no errors
    - package.json contains "knip" in devDependencies
  </acceptance_criteria>
  <done>Knip reports zero actionable unused exports; test suite and build pass cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead CSS rules and stale RED test stubs</name>
  <read_first>
    - src/styles/runner-view.css (contains .rp-legend* dead rules at lines 71-94)
    - src/__tests__/runner-extensions.test.ts (contains 3 RED stubs in first describe block, lines 17-57)
  </read_first>
  <files>src/styles/runner-view.css, src/__tests__/runner-extensions.test.ts</files>
  <action>
1. In `src/styles/runner-view.css`: Remove the 3 dead CSS rule blocks that are never referenced in any TypeScript file:
   - `.rp-legend` (lines 71-77)
   - `.rp-legend-row` (lines 79-86)
   - `.rp-legend-swatch` (lines 88-94)
   These selectors have zero references in any `.ts` file (confirmed by grep).

2. In `src/__tests__/runner-extensions.test.ts`: Remove the entire first `describe('ProtocolRunner extensions (RUN-11, D-04, D-05, D-07)')` block (lines 17-57) containing the 3 RED stubs:
   - `setAccumulatedText` method test — method does not exist in ProtocolRunner
   - `setAccumulatedText clears undo stack` test — method does not exist
   - `start() accepts optional startNodeId parameter` test — parameter not supported
   These are Phase 26 legacy stubs for features that were never implemented. The second describe block (`Phase 35 — MD snippets in Runner picker`) with its 7 GREEN tests MUST be preserved intact.

   After removing the first describe block, also remove any imports that become unused (check if `CanvasParser`, `fs`, `path`, `fixturesDir`, `loadGraph` are still needed by the Phase 35 block — they ARE still needed, so keep them).

3. Run `npm run build` to regenerate `styles.css` after CSS changes.
4. Run `npm test -- --run` to confirm all remaining tests pass.
  </action>
  <verify>
    <automated>cd Z:/projects/RadiProtocolObsidian && grep -c "rp-legend" src/styles/runner-view.css; grep -c "RUN-11" src/__tests__/runner-extensions.test.ts; grep -c "Phase 35" src/__tests__/runner-extensions.test.ts; npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rp-legend" src/styles/runner-view.css` returns 0
    - `grep -c "RUN-11" src/__tests__/runner-extensions.test.ts` returns 0
    - `grep -c "Phase 35" src/__tests__/runner-extensions.test.ts` returns at least 1 (Phase 35 tests preserved)
    - `npm test -- --run` exits 0 — all tests pass (previously 3 failures from RED stubs, now 0)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dead CSS removed, 3 RED test stubs removed, Phase 35 tests intact, full suite green, build clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static analysis tool output | Knip may report false positives on Obsidian runtime callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | Dead code removal | mitigate | Two-pass audit: Knip identifies candidates, manual grep confirms zero references before removal. Full test suite run after each batch. |
| T-36-02 | Denial of Service | False positive removal | mitigate | Never remove Obsidian lifecycle methods (onload/onunload), registerEvent callbacks, or addCommand handlers — even if Knip flags them |
</threat_model>

<verification>
1. `npx knip --reporter compact` shows no actionable unused exports
2. `grep -r "rp-legend" src/styles/` returns nothing
3. `npm test -- --run` exits 0 with 0 failures
4. `npm run build` exits 0
</verification>

<success_criteria>
- Zero unused TypeScript exports (excluding Obsidian framework callbacks)
- Zero dead CSS rules (.rp-legend* removed)
- Zero RED test stubs (Phase 26 stubs removed)
- Full test suite passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/36-dead-code-audit-and-cleanup/36-01-SUMMARY.md`
</output>
